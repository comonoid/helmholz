# PLAN.md — план реализации helmholz

Начат 2026-07-24. **Основание сменено 2026-07-25**: дискретизация задаётся
геометрией и числом пикселей, НЕ длиной волны; отсюда несущий (Трефтц) базис
вместо λ-разрешающей сетки.

Этот файл описывает ОДНУ архитектуру. Измерения вытесненного λ-маршрута (v1)
переехали в `result/V1_LAMBDA_ROUTE.md` — там факты, а не планы; сводные
прогоны v1 в `result/M8_REPORT.md`. Дизайн зафиксирован в CLAUDE.md.
Язык: C (gnu11), гейт качества — CLAUDE.md (ccheck/cverify/lean, всегда).

СОСТОЯНИЕ ОДНОЙ СТРОКОЙ (итог сессии 07-25).
ЗАКРЫТО И ИЗМЕРЕНО: представимость целиком (M9b §5-8), закон точности фазы
`W_max = λ/(π·δk/k)`, разрезы, диадические оболочки (вопрос 3), направленная
форма несущей (вопрос 12), конформность разреза (вопрос 10, обе половины),
терминация домена (вопрос 1: DtN работает, поглощение нет), угловая
разреженность (M9c части 1-3 + матрица 2×2 с провалившимся негативным
контролем), и СКВОЗНОЕ РЕШЕНИЕ С ОПЕРАТОРОМ: домен 200 000 λ решается точно
874 неизвестными против 3.2 млн у сетки λ/8 — отношение 3700×.
ГЛАВНЫЙ ОТРИЦАТЕЛЬНЫЙ РЕЗУЛЬТАТ: холодное решение реального времени НЕ ДАЁТ.
LSQR сходится, но итераций 165 → 320 при росте домена в 1000 раз (~0.4·dim);
блочный Гаусс-Зейдель по уровням вообще расходится на знаконеопределённом
операторе (на матрице Грама он работал — успех НЕ переносится).
**M11 ИЗМЕРЕН 07-26 (result/M11_REPORT.md), И ЭТО ЧАСТИЧНО ОТРИЦАТЕЛЬНЫЙ
РЕЗУЛЬТАТ.** Инкрементальность РЕАЛЬНА: возмущение локализовано на изменившихся
элементах (концентрация 2.1–5.2), число проходов НЕ растёт с размером сцены
(40 → 18 при росте домена в 1000 раз), доля сменивших оболочку линейна по
δ/R_near, дрейфа за 64 кадра НЕТ. Три негативных контроля сработали, включая
главный: движение ИСТОЧНИКА дороже движения камеры в 3–10 раз — тезис «поле не
зависит от камеры» подтверждён количественно. НО выигрыш — МНОЖИТЕЛЬ 4–6, а не
класс: тёплых 10–70 против холодных 86–146, порог приёмки («≤10 при холодных
≥150») НЕ ВЗЯТ. Механизм назван: тёплый старт снимает только `log(e₀/цель)`
(e₀ падает в 50–500 раз, итерации — в 2–8), а СКОРОСТЬ сходимости не трогает.
Скорость же плоха структурно: **LSQR не собирает AᴴA, но сходится как CG на
нормальных уравнениях, то есть по cond²**, а базис вырожден точно по построению.
СЛЕДУЮЩИЙ ШАГ, КРИТИЧЕСКИЙ ПУТЬ СМЕЩЁН: **не инкрементальность, а СКОРОСТЬ
СХОДИМОСТИ** — предобуславливание и выбор метода. По убыванию зрелости:
(1) вопрос 5 (поглощение только в оболочке ⇒ вещественная симметричная
внутренность ⇒ MINRES, сходимость по cond, а не cond² — квадратный корень в
показателе, больше, чем даст любой тёплый старт); (2) сдвинутый лапласиан как
предобуславливатель; (3) дефляция крупных оболочек (ОПЕРАТОРНЫХ медленных мод,
не точного ядра базиса — его трогать нельзя); (4) рециклинг подпространств
Крылова между кадрами (в M11 сознательно НЕ применялся ⇒ измеренное есть НИЖНЯЯ
оценка выигрыша). Параллельно: разрезы переходят в ПРЕДПОСЫЛКУ M11 — без них
контраст сцены нельзя поднять до уровня, где инкремент вообще зависит от сцены
(в M11 вакуум и сцена дали одинаковые числа). Затем плотное рассеяние и 2D/3D.
Плана Б нет: фальсификация означает возврат к сетке λ/8, то есть к физической
демонстрации волновых эффектов, а не к рендереру.
## ЧИТАТЬ ПЕРВЫМ: `result/M18_FORMULATION_AUDIT.md` (07-26)
Постановка на два порядка дороже необходимого, и причина НЕ в физике. Задача
«цилиндр 1λ в круге 5λ» решается сейчас за 9000 итераций LSQR при dim 5456 —
`cond(A) ~ 1e4`, тогда как обычная сетка λ/10 с прямым решателем дала бы ту же
точность за секунды при 30 000 неизвестных. Три названных причины, все —
проектные решения: (1) фрейм избыточен в `16·ND = 128` раз на точку (носитель φ
равен 4W при шаге W); (2) 43% столбцов не имеют уравнений (строки до R, столбцы
до R+4W); (3) нормы строк оператора расходятся в 1e9 (тонкие дольки разреза), а
уравновешивание поднимает их шум до веса уравнения.
**ОБА ДЕЙСТВИЯ ВЫПОЛНЕНЫ — `result/M19_BASELINE_REPORT.md`, и они переставили
приоритеты.** Эталон стоимости снять НЕ УДАЛОСЬ: обычный базис на этом стенде
искалечен самой постановкой (в ВАКУУМЕ, где точное решение представимо точно,
ошибка 0.208/0.158/0.508 при λ/4 / λ/8 / λ/16 — РАСТЁТ при измельчении). Причина:
операторные строки ОДНОРОДНЫ, их ~dim, а привод несут 91 строка DtN, и `u=0`
удовлетворяет однородным строкам точно. Несущий базис иммунен (его строки
выполняются тождественно) — и потому три сессии маскировал этот дефект.
Свип по ND при неподвижном домене ОСЛАБИЛ подозреваемого №1: 0.532/0.223/0.0107
при ND=2/4/8 — направления не балласт, а аппроксимационная сила.
Поправка к M18: «9000 итераций» — это до МИНИМУМА; до рабочей точности 1000 (2%)
и 4000 (1%), стоимость была завышена в 4–9 раз.
**КВАДРАТНАЯ ПОСТАНОВКА СДЕЛАНА — `result/M20_SQUARE_REPORT.md`.** DtN переставлен
из отдельных строк в ГРАНИЧНЫЙ ЧЛЕН уравнений (теорема Грина по Ω даёт член, на
который он вешается); `entry()` и Нитше остались целиком. Три патологии сняты
разом: столбцов без уравнений нет, однородные строки не переголосовывают привод,
система квадратная. Обычный базис при λ/8 в вакууме стал точнее в 15 раз
(0.158 → 0.0106) и снова СХОДИТСЯ при измельчении.
**ЗНАМЕНАТЕЛЬ ИЗМЕРЕН, и он скромный:** на одной сцене и одной постановке
обычный базис (dim 6045) даёт 2.08%/3.61%, несущий (dim 4688) — **0.67%/1.08%**,
то есть **втрое точнее при 0.78 от неизвестных**. Ровно как предсказывал аудит
среза («при W ≤ λ/2 несущий базис не дешевле сетки λ/8»). Крупный элемент, где
выигрыш обязан быть большим, этот стенд не меряет.
Инженерная находка: граничный блок ПЛОТЕН среди приграничных функций (8.1e6
элементов при W=λ/2, ND=8) и обязан храниться разложенным — ранг 2·mmax+1.
Гейт масштабирования решателя (M17) ОТМЕНЁН: он мерил показатель, не объяснив
константу.

**M15 ВЗЯТ 07-26 (result/M15_REPORT.md): НИТШЕ ВМЕСТО КОЛЛОКАЦИИ НА РАЗРЕЗЕ.**
Ручка снята и это измерено с обеих сторон одним бинарём: коллокация по числу
точек — яма с разбросом 17×, Нитше по штрафу — разброс 1.7× на ЧЕТЫРЁХ порядках
β, плато 4% вокруг ПРЕДСКАЗАННОГО `β = γ(k_max + 2√15/W) = 24.9 λ⁻¹`. Критерий A5
выполнен впервые (поле ядра 0.376 → 0.0053, врезка ниже переписана). Вакуум с
принудительным разрезом вернулся к уровню неразрезанного базиса — **разрез стал
бесплатным**. Цилиндр n=1.5: 1.4% на r=2λ против NC-D 0.564, то есть порог гейта
D2 (5%) пройден — но **гейт не объявляется взятым**: диаграмма рассеяния не
мерилась, ND вдвое ниже закона, LSQR не на плато. Узкое место сместилось туда же,
куда его двигал M11: СКОРОСТЬ СХОДИМОСТИ, а не постановка.
NB ДЛЯ СЛЕДУЮЩЕЙ СЕССИИ: артефактов собственных стендов уже ВОСЕМНАДЦАТЬ (17 —
аргумент `it=` стенда среза читался из позиции, оставшейся от прежней сигнатуры, и
свип по нему давал ПОБИТОВО одинаковый ответ: третья за проект реализация подписи
«число не меняется при изменении параметра, которым его меняют». Лечение —
`ключ=значение` и ABORT на неизвестном ключе, а не заплатка. 18 — знак согласующей
пары Нитше был выведен неверно, и **проверка на СИММЕТРИЮ прошла с ним (8e-16),
потому что переворот знака у симметричной пары оставляет матрицу симметричной**;
поймала только СОГЛАСОВАННОСТЬ на явно выписанном точном решении (|Ac| = 0.40).
Мораль: симметрия слепа к знаку симметричной пары; 15 — запас
на разбиение единицы в гейте D1 среза; 16 — негативный контроль, сломанный
СОГЛАСОВАННО: NC1 в M14 поворачивал направление и в базисе, И В ПОЛЕ, поле
осталось точно представимым, и контроль вернул ровно тот же машинный ноль, что
исправный разрез. Признак: негативному контролю нечего делать на полу метрики.
Ломать надо ОДНУ сторону сравнения), две
поправки концептуальные. Почти каждый стенд сначала врал правдоподобно; спасали
только заранее записанные негативные контроли и фальсификаторы. Отчёты в result/
описывают каждый артефакт — читать их до того, как строить новый стенд.
Два свежих урока стоят отдельного слова: (13) порог сходимости, равный тому,
что метод и так достигает, — не измерение, а термометр в собственном кармане
(при цели 1e-2 негативный контроль «телепорт» проходил СЛУЧАЙНО, возвращая ноль
итераций); (14) пол уточнения у геометрии — это ЛЕСТНИЦА, а не патч: на краю
патча возникает скачок ширины элемента, и он стоит ровно как разрыв среды
(в вакууме давал 15–25% глобальной ошибки там, где поле обязано быть точным).

## ОЧЕРЕДЬ ПЕРЕСТАВЛЕНА 07-26: ВЕРТИКАЛЬНЫЙ СРЕЗ ВПЕРЁД ВСЕГО
**ИСПОЛНЯЕМЫЙ ПЛАН — `SLICE_PLAN.md`, ПРОШЁЛ АУДИТ 07-26** (сам аудит там же).
Итог аудита в трёх строках: гейт «цилиндр, 5% против Ми» при равномерном базисе
без разрезов НЕДОСТИЖИМ (предсказание 10–30%, выведено из F5 M9b и проверено на
M11), поэтому он переведён в НЕГАТИВНЫЙ КОНТРОЛЬ с предсказанным провалом, а
гейт взял ГЛАДКИЙ радиальный рассеиватель (разрыва нет ⇒ пола F5 нет). Найдены
три вещи, которых в плане не было: окружность НЕСЕПАРАБЕЛЬНА (блокирует D2 до
всякого разговора о разрезах; решается градуированными слоями с O(δ²)); ND=8
занижено в 5 раз (закон `ND ≈ 3+20·W/λ`, два независимых вывода) ⇒ настоящая
размерность плановой конфигурации 61 000, а не 10 400; запас на разбиение
единицы в 2D возводится в квадрат ⇒ требование `R ≥ 10·W`. Бюджет 8 ч → 11 ч
(разрезы на КРИВОЙ границе — отдельные +6–8 ч).
**И ГЛАВНОЕ АРХИТЕКТУРНОЕ СЛЕДСТВИЕ, ВРЕЗКА A4: «LOD ПО БАЗИСУ БЕСПЛАТЕН»
ПОНИЖЕН ИЗ ИЗМЕРЕННОГО В УСЛОВНЫЙ.** Он держится на разрезе (без разреза пол
20.6%, M9b F5), а разрез измерен только в 1D, где граница — точка. Для плоского
разреза в 2D/3D интегрируемость закрыта выводом (теорема о дивергенции ⇒
замкнутая форма, кубатура по тетраэдрам не нужна), точность НЕ измерена; для
КРИВОЙ границы возникает потолок размера элемента `W_max = sqrt(8ρ·δ_доп)`, В
КОТОРЫЙ РАССТОЯНИЕ НЕ ВХОДИТ, и на кривых поверхностях счёт перестаёт падать с
расстоянием (комната того же порядка, город — нет). **ЭТОТ ПОТОЛОК СНЯТ В ТОТ ЖЕ
ДЕНЬ (вопрос юзера): линейная интерполяция границы в узле переносит его с
ЭЛЕМЕНТА на ЯЧЕЙКУ СРЕДЫ** — фасет вместо лесенки, и фасеты добавляют РАБОТУ
СБОРКИ `(4W/h)^(d−1)` на разрезанную запись, но НЕ неизвестные.
**И ВСЁ ЭТО ИЗМЕРЕНО ТЕМ ЖЕ ДНЁМ (M14, result/M14_REPORT.md):** плоский косой
разрез в 2D переносится полностью (0.229 без него против 1.3e-7 = пол метрики,
при любом угле), интегрируется в ЗАМКНУТОЙ ФОРМЕ (кубатура по тетраэдрам для
этого класса подынтегральных не нужна), а закон ошибки от смещения локуса
оказался `err = 0.125·(kδ)²·sqrt(δ/W)` — в 27 раз мягче фазового, по которому
считалась оценка выше. Фасет при λ=550 нм, ρ=0.5 м и цели 1% — **1.1 мм**, и он
зависит от размера элемента только как `W^{1/5}`. **«LOD по базису бесплатен»
возвращается в ИЗМЕРЕННОЕ**, с оговоркой: измерено на ПРЯМОЙ границе, в
ПРОЕКЦИИ, в 2D. Открыты конформность в 2D, галёркинская постановка и 3D.
ВАЖНОЕ ЧИСЛО ИЗ АУДИТА, ЗАПИСАННОЕ ДО ПРОГОНА: в 2D dim почти НЕ ЗАВИСИТ от W
(крупные элементы отыгрываются направлениями один в один), и при W ≤ λ/2
несущий базис НЕ ДЕШЕВЛЕ сетки λ/8. Выигрыш живёт только при широком элементе
и УЗКОМ угловом содержании; у рассеивателя оно широкое, то есть срез меряет
ХУДШИЙ для метода случай.
(2D-сборка → DtN на окружности → решение → картинка). Причина смены приоритета названа прямо: за две сессии 14 измерений,
и форма у всех одна — убран очередной обходной путь. Знание выросло, артефакт не
сдвинулся. У проекта НЕТ НИ ОДНОГО ВЕРТИКАЛЬНОГО СРЕЗА, всё горизонтально, и
поэтому никто не знает, что упрётся ПЕРВЫМ: разрезы, предобуславливание, число
направлений или терминация. Срез отвечает именно на это.
СНЯТА СТЕНА ПО ТЕРМИНАЦИИ: `TERMINATION_REPORT` записал «в 2D DtN нелокален» —
верно, но на ОКРУЖНОСТИ он диагонален по угловым гармоникам
(`∂ᵣû_m = k·Hₘ′(kR)/Hₘ(kR)·û_m`), то есть применяется одним FFT по границе.
Точно, без слоёв. Оговорка: число гармоник ~`kR`, в сценическом режиме это ~200
и рабоче, при оптической λ — 10⁸ и требует отбора по наблюдаемости.
Что из очереди ниже ВЫРЕЗАНО ИЗ СРЕЗА сознательно: LOD-оболочки (в 2D базис
равномерный ⇒ **первая картинка НЕ демонстрирует утверждение о стоимости, только
что конвейер жив**), разрезы, предобуславливание M12 (аудит написан, достаётся
только если шаг 3 упрётся в решатель), когерентность.

## ОЧЕРЕДЬ ИЗМЕРЕНИЙ (переставлена 07-25 по аудиту плана)
0. **M9b [ГОТОВО]** — гейт пройден, красный флаг не поднят. Отчёт:
   result/M9b_REPORT.md (включая §5, проекционный тест).
1. **Дешёвые замеры на проекционном стенде [ГОТОВО 07-25]** —
   result/M9b_REPORT.md §6-7:
   - закон точности фазы ПОДТВЕРЖДЁН с константой: `W_max = λ/(π·δk/k)`,
     произведение постоянно на пяти порядках (вопрос 13 закрыт);
   - разрез ПОДТВЕРЖДЁН: машинный ноль при любой ширине до 1e5 λ против 20% у
     гладкого, ценой удвоения степеней свободы только на задетых элементах
     (вопрос 10, первая половина). Конформность НЕ закрыта — см. вопрос 10.
   ОСТАЛОСЬ ЗДЕСЬ ЖЕ (дёшево, тот же стенд): конформность на поле, представимом
   на сторонах лишь приближённо.
2. **Рефакторинг src/carrier.{c,h} [ГОТОВО 07-25]** — направленная форма
   несущей (вопрос 12) + разрезы. Приёмка пройдена вся:
   (а) пролёт не изменился — tools/carrier_scale воспроизвёл числа до
   последнего знака (0.0399/0.0355/0.0443), плюс тест span-равенства на поле
   ВНЕ пролёта (1.3k) в tests/test_carrier;
   (б) направления отделимы — чистая e^{+ikx} кладёт на −k функции 1e-8 от
   амплитуды, и удаление половины базиса НЕ ухудшает приближение. С парой
   cos/sin это утверждение невыразимо в принципе;
   (в) разрез: элементы по разные стороны не перекрываются, запись через разрез
   строго ноль; количественно разрез измерен в M9b §7 (машинный ноль до 1e5 λ);
   (г) ccheck CLEAN, весь набор зелёный (test_phi 980/980, test_carrier 8/8,
   остальные без регрессий).
   ПОБОЧНО: вызовов интегралов вдвое меньше (83902 → 41986 на той же задаче) —
   билинейная форма Галёркина складывает показатели, и четыре тригонометрических
   случая схлопнулись в одно правило `ω = kx_i + kx_j`.
   ЗАФИКСИРОВАНО В API: форма Галёркина БИЛИНЕЙНА (тестовая функция НЕ
   сопрягается) — иначе это не смена координат, а другая система; L2-скалярное
   произведение в проекции, наоборот, сопрягает. Разные объекты, не путать.
   Медиум теперь хранит АБСОЛЮТНОЕ k², а не поправку к глобальному фону:
   глобального фона больше нет, каждый элемент меряет среду относительно
   СВОЕЙ несущей — так «несущая локальна по среде» выражено операционально.
3. **Вопрос 3 (диадические оболочки) [ГОТОВО 07-25]** — result/SHELLS_REPORT.md.
   Правило: оболочка j покрывает [W_j/ε, 2W_j/ε), диадические кольца.
   ГЛАВНОЕ: избыточность отслеживает ПЕРЕКРЫТИЕ, а не глубину — доля ранга
   постоянна при росте числа оболочек с 8 до 27 и зависит только от ε
   (0.70/0.82/0.90/0.95 при перекрытии 2.0/1.0/0.5/0.25). Лестницу можно
   наращивать сколь угодно глубоко. Прежний провал был конструкцией, не
   глубиной. Доля изменившихся при сдвиге камеры линейна по сдвиг/R_near
   (коэффициент 0.16) с полом ~2%: A2 подтверждён количественно.
4. **M9c** — 2D, угловая разреженность.
   **Часть 1 [ГОТОВО 07-25]** — result/M9c_ANGLE.md: число несущих на элемент
   зависит ТОЛЬКО от произведения Δθ·W/λ (три пары (W,Δθ), различающиеся по W
   в 1000 раз, дают одно N), закон `N ≈ 3 + 3.5·(Δθ·W/λ)`. Второй сомножитель
   счёта размерности ограничен и вычислим — он задаётся угловой шириной поля,
   а не размером элемента. Сжатие пропадает при Δθ·W/λ ≳ 5-10, то есть в
   спекл-режиме: A3 подтверждён измерением.
   **Часть 2 [ГОТОВО 07-25]**: амплитуда НЕ спасает. Слабый, но угловой
   РАЗМАЗАННЫЙ фон несжимаем — выбросить целиком можно только если его ПОЛНАЯ
   энергия ниже квадрата целевой точности (1e-4 для 1%). Проверено арифметикой
   до совпадения с измерением. ВЫВОД: разреженность — это КОНЦЕНТРАЦИЯ, а не
   малость амплитуды; когерентное поле сжимаемо только там, где направленно
   сконцентрировано, а диффузная часть обязана уходить в УСРЕДНЁННЫЙ канал,
   иначе стоит полного счёта мод. Режим когерентности из «внешнего цикла,
   который ничего не ломает» становится УСЛОВИЕМ сходимости счёта размерности.
   **Часть 3 [ГОТОВО 07-25]** — result/M9c_SCENE.md, сгенерированная сцена с
   ТОЧНЫМ многократным рассеянием (Фолди–Лакс, точечные рассеиватели, эталон
   H₀⁽¹⁾ без сетки). ГРАНИЦА НАЙДЕНА И РЕЗКАЯ: 1 рассеиватель — 1 несущая при
   любом W; 8 — частичное сжатие (17-52% слотов); 40 и далее — НАСЫЩЕНИЕ, нужны
   ВСЕ слоты, и 1% недостижим даже с полным набором (1.6% → 6.0% по мере роста
   плотности). Порог ~1e-3 рассеивателей на λ² при силе f=0.35.
   Подтверждает A3 и M9c часть 2 на ФИЗИЧЕСКОЙ сцене: когерентное поле
   сжимаемо по направлениям только при слабом рассеянии; диффузная часть
   обязана уходить в усреднённый канал.
   ОГОВОРКИ: сила рассеивателя — параметр модели, правильная величина это
   МУТНОСТЬ (плотность × сечение); 2D даёт kW слотов вместо (kW)²; пробный
   элемент стоит в центре облака, то есть в худшем месте.
   **МАТРИЦА 2×2 [ГОТОВО 07-25]**: A обычный+мелко 961/0.0032, C несущая+мелко
   1922/0.0004, **B обычный+LOD 247/1.0000 — НЕГАТИВНЫЙ КОНТРОЛЬ СРАБОТАЛ**,
   D несущая+LOD 494/0.0000. Несущая доказана как УСЛОВИЕ, а не оптимизация.
   Сравнение занижено в пользу A: его окно ±60λ, у D весь домен 2000λ; полный
   домен при λ/8 потребовал бы ~32000 неизвестных.
   ФАЛЬСИФИКАТОРЫ M9c: 1,2,3,4 — ИЗМЕРЕНЫ И ПРОЙДЕНЫ. 5 (число итераций до
   точности ПОЛЯ) — НЕ ИЗМЕРЕН, требует решения с оператором. Это единственное,
   что отделяет «быстрее трассировки» от непроверенного.
   ОСТАЛОСЬ: эталоны Зоммерфельд/цилиндр (не структурные, а валидационные).
5. **Вопрос 4 (решатель) и каскад. [ЧАСТИЧНО ЗАКРЫТ 07-25]** —
   result/SOLVE_REPORT.md. ПРЯМОЕ решение с оператором ТОЧНО на всех масштабах:
   домен 200 000 λ решается 874 неизвестными (машинный ноль), рост
   логарифмический; сетка λ/8 потребовала бы 3.2 млн — отношение 3700×. Это
   первое сквозное подтверждение архитектуры на ОПЕРАТОРЕ, а не на
   представимости.
   НО КАСКАД НЕ СХОДИТСЯ: блочный Гаусс-Зейдель по уровням даёт 1.0 и
   расходится к 4-му проходу. Это уточняет CASCADE_REPORT: там каскад работал
   на матрице ГРАМА (положительно определённой), здесь — ОПЕРАТОР, а он
   знаконеопределён. Успех на Граме НЕ ПЕРЕНОСИТСЯ.
   **ФАЛЬСИФИКАТОР 5 ИЗМЕРЕН 07-25** (SOLVE_REPORT, раздел LSQR): LSQR СХОДИТСЯ
   (в отличие от блочного Гаусса-Зейделя), но число итераций НЕ ОГРАНИЧЕНО —
   165 → 320 при росте домена в 1000 раз, примерно 0.4·dim, то есть
   логарифмически. При 3D-оценке 1e8 неизвестных это ~5 с на кадр: холодное
   решение реального времени НЕ ДАЁТ.
   СЛЕДСТВИЕ: утверждение о реальном времени обязано опираться на
   ИНКРЕМЕНТАЛЬНОСТЬ, а не на быстрое решение с нуля. Холодный старт — разовая
   цена. Фальсификатор расщеплён: холодные итерации не ограничены (измерено),
   итерации ПОПРАВКИ не измерены — это M11, и он переходит из «проверки
   гипотезы» в КРИТИЧЕСКИЙ ПУТЬ.
   (исходная формулировка)
   **Первая половина [ГОТОВО 07-25]** — result/CASCADE_REPORT.md: каскад
   СНИМАЕТ межуровневую избыточность полностью (ранг 100% на КАЖДОМ уровне
   против 89% монолитно) и не платит за это точностью (7.47% против 7.03%).
   Утверждение плана с первого дня подтверждено впервые. Остаточные 7% — цена
   неразрезанных элементов на скачках амплитуды (M9b §7), не каскада.
   ПОПРАВКА: идея «подбирать модуль несущей» ОТМЕНЕНА — базис трефтцевый, в
   кусочно-постоянной среде модуль задан средой, подбирается только
   направление; переменным модуль становится лишь в градиентных средах через
   ВКБ-фазу.
   **Оператор проверен без границы [ГОТОВО 07-25]** — tests/test_carrier_op.c,
   10/10, ccheck CLEAN. Приём: в однородной среде плоская волна — точное
   решение, и по разбиению единицы лежит в пролёте с ПОСТОЯННЫМИ
   коэффициентами, значит дискретный оператор обязан её АННИГИЛИРОВАТЬ. Это
   проверяет сборку, сокращение k², разбиение единицы и член среды разом,
   точно, БЕЗ ГРАНИЦЫ И БЕЗ РЕШЕНИЯ. Измерено: невязка 7e-15 (0.1λ), 4e-13
   (1λ), 3e-10 (10λ), 5e-7 (100λ) — рост от динамического диапазона записей,
   не от ошибки. Негативный контроль (несовпадающая среда) даёт заметную
   невязку.
   ДВЕ НАХОДКИ ЭТОГО ТЕСТА:
   (1) базис ОБЯЗАН держать оба направления. Форма билинейна, значит
   ⟨B_i, L B_j⟩ при обоих +k несёт e^{2ikx} и экспоненциально мала при W≫λ:
   тестовая функция, реально видящая волну +k, — это −k. Базис из одного
   направления делает ВСЮ матрицу ничтожной, и негативный контроль проходит
   ложно. Первая версия теста имела ровно эту дыру.
   (2) **hz_carrier.kx СДЕЛАН КОМПЛЕКСНЫМ [ГОТОВО 07-25]**. В поглощающей среде
   волновое число комплексно; при α=0.05 вещественная несущая давала невязку
   95%. С комплексной — 7.6e-11, аннигиляция восстановлена. Заодно на комплексный
   ω обобщены оба осциллирующих интеграла в src/phi.c (ряд и замкнутые формы
   работают для комплексного аргумента как есть; 980/980 без регрессий).
   ПОПРАВКА К СОБСТВЕННОМУ ВЫВОДУ: я записал «Im(k)·W ≲ 1» как закон размера —
   это НЕВЕРНО. С комплексной несущей затухающая волна лежит в пролёте ТОЧНО,
   с постоянными коэффициентами, ровно как незатухающая; представимость не
   ограничена ничем. Единственный предел — ПЛАВАЮЩАЯ ТОЧНОСТЬ: перепад
   exp(−Im(k)·4W) поперёк носителя. Замерено: невязка 4e-13 при Im(k)W=0.16,
   2e-11 при 0.63, 8e-11 при 2.5, 4e-5 при 10 (перепад e^-40, край double),
   дальше числа теряют смысл — поле само underflow.
   Вопрос 5 всё равно поддержан: вещественная внутренность снимает проблему
   диапазона и даёт вещественный симметричный оператор (MINRES).
   ОСТАЛОСЬ: решение с оператором (там сразу вернётся вопрос 1), число
   итераций, поведение при сильном контрасте, 2D.
6. **Вопрос 1 (терминация домена) [ИЗМЕРЕН 07-25]** —
   result/TERMINATION_REPORT.md. В 1D третья схема (элементы на функции Грина)
   ВЫРОЖДАЕТСЯ во вторую (DtN): внешность — одна плоская волна. Померены две.
   **DtN РАБОТАЕТ при любом размере элемента**: 0.7-5% при элементах до 200λ,
   ценой ДВУХ СТРОК. Поглощающий слой НЕ работает (0.83-1.2 при всех W); две
   гипотезы проверены и отвергнуты (усечение по рангу — ранг ПОЛНЫЙ;
   динамический диапазон — вылечен нормировкой на оптическую глубину, ошибка
   осталась). Причина не установлена, записана как гипотеза.
   **ВЫВОД: условие излучения накладывать ЯВНО, а не добывать поглощением.**
   Для 2D/3D ведущий кандидат — третья схема (внешность аналитически:
   функция Грина / граничный интеграл), а НЕ PML. Это переворачивает наследство
   v1, где поглощающая оболочка была основным механизмом.
   ПОБОЧНАЯ НАХОДКА, КРУПНАЯ: фаза несущей должна отсчитываться ОТ ЭЛЕМЕНТА,
   `e^{i·kx·(x−nW)}`, а не от начала координат. При вещественной несущей это
   безобидно, при комплексной — разница между рабочим базисом и нерабочим
   (слой в 1000λ от начала приходил с амплитудой e^-175). Исправлено в
   src/carrier.c; независимый эффект: невязка аннигиляции при Im(k)·W=10 упала
   4e-5 → 1.8e-12, то есть «предел динамического диапазона» из прошлого отчёта
   был артефактом глобальной привязки, а не свойством.
7. **M11 (инкремент при движении камеры) [ИЗМЕРЕН 07-26]** —
   result/M11_REPORT.md, аудит result/M11_AUDIT.md, стенд tools/carrier_incr.c.
   Инкрементальность реальна, выигрыш — множитель 4–6, не класс. Подробности в
   статусной строке выше. ОСТАЛОСЬ ЗДЕСЬ ЖЕ: NC1 (телепорт) в текущем виде
   несравним — он двигает окно измерения вместе с камерой; мерить в НЕПОДВИЖНОМ
   окне. И 2D: переизбрание НАПРАВЛЕНИЙ несущих при движении камеры не проверено
   вовсе.
8. **СКОРОСТЬ СХОДИМОСТИ — НОВЫЙ КРИТИЧЕСКИЙ ПУТЬ (поднято 07-26 по M11).**
   Вопрос 5 → MINRES, сдвинутый лапласиан, дефляция, рециклинг Крылова.
   Обоснование и порядок — в статусной строке.
8a. **M14 (разрез в 2D) [ИЗМЕРЕН 07-26] — result/M14_REPORT.md, аудит
   result/M14_AUDIT.md, машинерия src/cut2d.{c,h} (19/19), стенд
   tools/carrier_cut2d.c. ВЕРДИКТ: разрез переносится в 2D полностью и при
   любом угле (0.229 без него против 1.3e-7 с ним = пол метрики), пол
   неразрезанного элемента подтверждён (0.229 против 1D 0.206) и не убывает до
   10^5 lambda, и он на 22/23 состоит из ИЗЛОМА, а не из волнового числа.
   Закон смещённого разреза измерен: err = 0.125(k d)^2.5 (kW)^-0.5 — ни один
   из трёх предсказанных не подтвердился, измеренный в 27 раз мягче. Отсюда
   правило размера фасета. Подробности — врезка A4.
   ОСТАЛОСЬ: конформность в 2D, галёркинская постановка, 3D.
   (исходная постановка)
   Обоснование целиком — врезка A4 выше. Стенд ПРОЕКЦИОННЫЙ (без решателя, без
   DtN, без границы домена — приём M9b §7): наклонный слой есть повёрнутая
   1D-слоистая задача, эталон аналитический и бесплатный. Свип по W на пять
   порядков, три ветви с ПРЕДСКАЗАНИЯМИ: без разреза — полка ~20% (обязана
   воспроизвести F5, это негативный контроль); точный косой разрез — машинный
   ноль (если верна интегрируемость по A4, условие 1); разрез, смещённый на
   стрелку дуги — даёт ПОКАЗАТЕЛЬ закона из трёх кандидатов A4. Плюс
   галёркинская чувствительность к неконформности (оговорка M9b §8).
   Стоит 3–4 часа и закрывает разом маршрут B среза и условность LOD.
9. **M10** (оператор объекта).
10. Пересборка M4 (активный набор) и M6 (решатель) под новую архитектуру.

## ОСНОВАНИЕ

### Требование (директива юзера 07-25)
Шаг сетки и размер потенциала задаются ДЕТАЛЯМИ ОБЪЕКТОВ, на камере — ЧИСЛОМ
ПИКСЕЛЕЙ. Препятствия любого размера задаются без оглядки на λ. Сцена уже
такая (k² на узел, размеры в ячейках) — λ-зависима была только дискретизация.

### Почему Гельмгольц это может, а эйконал нет
Эйлеровы солверы эйконала дают однозначное первое вступление; многозначность
требует 6D фазового пространства — отсюда лучи. Гельмгольц линеен, и
суперпозиция держит любое число ветвей даром: каустики конечны, вихри
возникают как нули суммы, бухгалтерии ветвей нет. Отображение u ↦ (A,S)
нелинейно — поэтому амплитудно-фазовые постановки (Маделунг) теряют
суперпозицию, а линейное уравнение на интенсивность структурно не даёт
интерференции (это уравнение переноса, оно же трассировка путей).

### Почему несущая — условие, а не оптимизация
Узловой базис требованию НЕ удовлетворяет: гладкий бугор шириной W ≫ λ не
представляет осцилляцию (в (∇²+k²)φ член ∇²φ ~ 1/W² пренебрежим против k²φ),
плюс загрязнение Babuška–Sauter. Удовлетворяет только несущий
(Трефтц/PUM/UWVF) базис φ(x/W−n)·e^{ik·x}: в (d²/dx²+k²)[φ cos] член k²
сокращается АНАЛИТИЧЕСКИ — вот что делает крупные ячейки законными.
Плоские волны выбраны не из лучевых соображений, а потому что
`e^{i(kₓx+k_yy+k_zz)}` ФАКТОРИЗУЕТСЯ: тензорная сепарабельность сборки цела,
сферические гармоники её ломают.
Фаза перестала быть НЕИЗВЕСТНОЙ и стала ПРЕДПИСАННОЙ. Дискретна только
медленная комплексная огибающая. Развязалась ПРЕДСТАВИМОСТЬ фазы, но не её
точность: огибающая по-прежнему копит дисперсию с расстоянием (в 1D измерено:
0.022 против 0.034 на 42λ) — это регулятор «размер элемента», меряется в M9b.

### Где λ остаётся законно (и почему это не отступление)
λ ушла ИЗ СЕТКИ. Из физики её убрать нельзя — она в уравнении. Остаётся:
- в операторе: несётся множителем `e^{ik·x}` аналитически, ценой ноль по сетке;
- как отсечка эванесценции: деталь мельче λ не излучает ⇒ говорит, когда
  прекращать уточнение;
- в дифракционном пределе λ/D — но только как ПОЛ для ε. Само ε задаётся
  числом пикселей: L = R · (поле зрения / пиксели). λ/D лишь говорит, ниже
  какого ε лишние пиксели ничего не купят.
Шаг сетки λ не задаёт нигде.

### Счёт размерности = (число элементов) × (число мод на элемент)
- **элементы**: LOD `L = εR` + схлопывание пустоты октодеревом.
  КЛЮЧЕВОЕ СВОЙСТВО: счёт НА ОКТАВУ РАССТОЯНИЯ не зависит от расстояния.
  Поверхности в слое [R, 2R] имеют площадь ~c·R², элементы там размера εR ⇒
  `c·R²/(εR)² = c/ε²`, R сокращается. Итого
  `всего ≈ (c/ε²)·log₂(R_max/R_min)` — размер сцены входит ПОД ЛОГАРИФМ.
  Здесь c учитывает ПОЛНУЮ СФЕРУ вокруг камеры и перекрытые слои: свет идёт и
  из-за пределов конуса, и сквозь невидимые поверхности, ограничивать набор
  фрустумом НЕЛЬЗЯ.
  ВНИМАНИЕ (аудит B1): `c` ниже — ДОПУЩЕНИЕ, а не измеренная величина. Взято
  «полная сфера × 3 слоя перекрытий»; в городе слоёв заметно больше, а рычаг 5
  (отсечение по амплитуде за непрозрачным) режет его радикально в другую
  сторону. Числа ниже — оценка порядка при явных допущениях; `c` должна стать
  ИЗМЕРЯЕМОЙ на реальной сцене, а не константой в тексте.
  Числа при 1000 пикселей на 60° (ε≈1e-3), c ≈ 4π·3 ≈ 40:
  комната (0.5..10 м, 4-5 октав) ≈ **2e8**; город (1 м..10 км, 13 октав)
  ≈ **5e8**, то есть в 2-3 раза дороже комнаты при линейном размере в 1e4 раз
  больше. Память города ≈ 8 ГБ (статическое предвычисление, не покадровое).
  ИСПРАВЛЕНИЕ 07-25: ранее здесь стояло «≈1e7» — это был счёт ТОЛЬКО ПО КОНУСУ
  И ОДНОМУ СЛОЮ, то есть без вклада вне конуса и без перекрытий. Занижено на
  порядок-полтора.
  ОГОВОРКА: числа предполагают И схлопывание пустоты, И ГЛАДКОСТЬ поверхностей
  (вопрос 2). Дальше режут рычаги 4 (сопряжённая важность) и 5 (отсечение по
  амплитуде) — оба ДИНАМИЧЕСКИЕ, требуют решения, поэтому заранее не оценены.
- **моды**: `(kL)^(d−1)` — это ГРАНИЦА ЁМКОСТИ (сколько независимых волновых
  мод область размера L способна вместить), НЕ стоимость. Сколько нужно на
  самом деле — определяется полем: у гладкой поверхности это «падающая +
  отражённая», две несущие независимо от L (Френель аналитичен). Богатым
  локальное поле становится при структурированной поверхности или сильном
  многократном рассеянии. **Фактическая потребность неизвестна и является
  предметом измерения M9c** (фальсификатор 1).
- **A3 — УГЛОВАЯ РАЗРЕЖЕННОСТЬ И СПЕКЛЫ ЭТО ОДНО УСЛОВИЕ (аудит 07-25).**
  План ведёт M9c (сколько несущих нужно элементу) и вопрос 6 (когерентность,
  спеклы) как два разных вопроса. Физически это одно, увиденное дважды: спекл
  ЕСТЬ суперпозиция многих направлений, то есть отсутствие разреженности. Где
  спеклы — крупный элемент с двумя-тремя несущими не работает; где работает —
  спеклов нет. Следствие для приёмки: M9c, поставленный на ГЛАДКОЙ сцене,
  покажет разреженность и создаст ЛОЖНУЮ УВЕРЕННОСТЬ для рассеивающей. Сцены
  M9c обязаны включать многократное рассеяние, иначе измерение неполно.
- для канала «элемент → апертура» теорема о модах связи даёт
  `N = L²D²/(λR)² = (εD/λ)²`, что при ε на дифракционном пределе равно 1 —
  одна несущая на элемент, независимо от расстояния. Это счёт КАНАЛА НА
  КАМЕРУ, не локального поля.
- переполнение несущими лечится тем, что уже в фундаменте: намеренно
  вырожденная система + min-norm решатели. Классическая болезнь Трефтца
  (обусловленность) совпала с уже принятым лекарством.

### Два требования к несущему базису [ИЗМЕРЕНО 07-25, result/M9b_REPORT.md §5]
Проекционный тест (представимость, без решателя и без границы домена):
1. **НЕСУЩАЯ ЛОКАЛЬНА ПО СРЕДЕ.** `k` берётся из локального `k(x)`, не глобально.
   Плоская волна с `k' = 1.5k`, изображаемая несущей с `k`, даёт **100% ошибки
   при W ≥ 10λ** — на гладчайшем поле, без всякой сингулярности. С одной
   глобальной несущей каждая граница материала потребовала бы элементов λ/8.
   Локальная несущая на W=λ: 0.42 → 0.040, в 10 раз.
2. **БАЗИС РАЗРЫВЕН НА ГРАНИЦЕ СРЕДЫ.** Элемент, гладко накрывающий разрыв,
   портится ЦЕЛИКОМ: ошибка не убывает как 1/√W, хотя дефект точечный — значит
   невязка размазана по всему носителю (F3/F5 упираются в ~20% при любой
   ширине). ФОРМУЛИРОВКА УТОЧНЕНА: это НЕ «элемент не пересекает границу» —
   такое требование ломает LOD. Реализуется РАЗРЕЗАНИЕМ элемента, см. раздел
   «Разрезы» ниже. Что разрез это чинит — вывод по механизму, прямо НЕ
   измерено (вопрос 10).
Там же ДОКАЗАНО прямо: свободное распространение представимо ТОЧНО при любой
ширине элемента (ноль ошибки до 10⁵λ). Это основное утверждение архитектуры, и
теперь оно не теория, а измерение.
NB: проекция — НИЖНЯЯ граница для галёркинского решения. Провал в ней
приговор, успех ничего не обещает про решение.

### Разрезы: базис разрывен НА границе среды
ФОРМУЛИРОВКА (исправлена 07-25): не «элемент не пересекает границу» — такое
требование ломает LOD (у длинной стены размер элемента ограничивался бы
расстоянием ДО СТЕНЫ, то есть геометрическое измельчение к каждой поверхности и
возврат семи порядков). Правильно: **базис РАЗРЫВЕН на границе**, и достигается
это РАЗРЕЗАНИЕМ элемента, а не измельчением.
Крупный элемент остаётся крупным, его функция режется поверхностью:
`φ·[слева]·e^{ik₁x}` и `φ·[справа]·e^{ik₂x}` — две степени свободы у того же
элемента. Длинная стена = крупные элементы с обеих сторон, каждый разрезан
плоскостью. Доплата — одна степень свободы на пару (элемент × материальная
область), то есть **счёт растёт со сложностью МАТЕРИАЛА, а не с числом длин
волн**. Элемент через десятислойный пирог стоит 10 коэффициентов независимо от
того, 10 там длин волн или 10⁷.
Техника стандартная (cut-cell, XFEM, хевисайдово обогащение, Nitsche).
СЛЕДСТВИЕ ДЛЯ ОЧЕРЕДИ: этап 2 (плоскость в узле, клиппинг куба, тетраэдры,
кубатура) переезжает из «следующих этапов» в КРИТИЧЕСКИЙ ПУТЬ. Без разрезов
либо стены ломают LOD, либо возвращается лесенка — а лесенка это искусственная
шероховатость λ-масштаба, то есть дорогой случай вопроса 2.

### Разрез и несущая — партнёры, не альтернативы
Соблазн «раз есть разрывы, несущая не нужна» ошибочен, и разница
категориальная:
- **разрез** — ЛОКАЛЬНАЯ починка на КОНЕЧНОМ числе поверхностей (в комнате
  ~10⁷ LOD-элементов на поверхностях);
- **несущая** — про осцилляцию ВО ВНУТРЕННОСТИ (в комнате 10 м при λ=550 нм
  это ~1.8·10⁷ длин волн по оси, ~10²² по объёму).
Разрезав базис на всех поверхностях, вы не трогаете эти 10²² осцилляций между
ними. Довести разрезами до конца можно только режа каждые λ/8 — это и есть
сетка λ/8. Уберите разрез — крупный элемент портится на каждой границе (F4:
72%). Уберите несущую — крупный элемент бесполезен везде.
МЕХАНИЗМ, почему несущая работает: она **общий фазовый опорник**. Одна
непрерывная функция на всю область; при переходе из элемента в элемент фаза
ПРОДОЛЖАЕТСЯ автоматически, элементы её наследуют, а не согласовывают. У
обычных бугров общего опорника нет, каждый собирает осцилляцию из полиномов
заново, и соседи обязаны совпасть по фазе — отсюда 8 точек на λ.
Стыковка держится РАЗБИЕНИЕМ ЕДИНИЦЫ: пока Σφ(x/W−n)=4, постоянные
коэффициенты воспроизводят плоскую волну точно (F1, ноль при 10⁵λ). Когда оно
ломается — появляется паразитная амплитудная модуляция; в масштабном стенде это
стоило сразу 30%.
Итог: **несущая — общий фазовый опорник, разрез — место, где опорник меняется.**

### Искажённая несущая для градиентных сред
Для плавно меняющегося k правильное обобщение — ВКБ-фаза `e^{iS}`, `S' = k(x)`:
```
(d²/dx² + k²)[φ e^{iS}] = (φ'' + 2ik φ' + i k' φ) · e^{iS}
```
Член k² СОКРАЩАЕТСЯ ПО-ПРЕЖНЕМУ ТОЧНО, добавляется одно слагаемое ∝ градиенту.
Каустики НЕ смертельны (в отличие от лучевого маршрута): заимствуется только
ФАЗА, амплитуда — коэффициент из линейного решателя, на каустике несколько
несущих накладываются, сумма конечна.
В 3D S МНОГОЗНАЧНА — искажение не спасает от ветвления, каждая ветвь своя
несущая (это M9c). Для этапа 1 (кусочно-постоянное k) искажать нечего.

### Закон точности фазы (следствие F2, не измерен количественно)
Если фаза несущей ошибается больше ~1 рад поперёк элемента, разницу обязана
отработать огибающая — это механизм отказа F2. Отсюда
```
δk/k ≲ λ/(2π·W)         и        δx ≲ λ/(2π·Δn/n)
```
Второе — про ПОЛОЖЕНИЕ ГРАНИЦЫ: сдвиг границы на δx уводит фазу на (k₂−k₁)δx.
При контрасте Δn/n=0.5 это δx ≲ 0.3λ. F2 — грубая точка на этой кривой
(δk/k=0.5 отказал при W≳λ, закон предсказывает ~2λ). ИЗМЕРИТЬ свипом δk/k в
проекционном стенде: даёт W_max(δk/k), то есть «насколько точно надо знать
среду» — входной параметр для всего остального.

### LOD ПО БАЗИСУ БЕСПЛАТЕН, LOD ПО ГЕОМЕТРИИ ЗАПРЕЩЁН
Точность геометрических ДАННЫХ и размер ЭЛЕМЕНТОВ независимы. Плоскость — три
double, задать её с точностью 1e-9 м стоит столько же, сколько с точностью 1 см.
Элемент 10⁶λ, разрезанный точно заданной плоскостью, законен: S = ∫k считается
по АНАЛИТИЧЕСКОЙ геометрии, ошибки δx там нет вовсе.
Требование δx ≲ 0.3λ кусается только когда огрубляется сама геометрия:
- вокселизованная граница (известна с точностью до ячейки) — та самая лесенка;
- геометрический LOD в привычном смысле (упрощение модели вдали, слияние
  деталей) — ФАЗУ ЛОМАЕТ, делать нельзя.
Дерево среды обязано хранить точные границы ВЕЗДЕ, включая даль. Это ровно
третий регулятор — но он тем самым повышается в статусе: не оптимизация
(развязка геометрии и базиса ради стоимости), а ТРЕБОВАНИЕ КОРРЕКТНОСТИ.
Стоит это ничего, пока поверхности гладкие: стена 5 м = один узел с одной
плоскостью независимо от числа длин волн. Взрыв только на микрорельефе —
вопрос 2.

**A4 — «LOD ПО БАЗИСУ БЕСПЛАТЕН» ПОНИЖЕН ИЗ ИЗМЕРЕННОГО В УСЛОВНЫЙ (аудит среза
07-26; нумерация аудитов ПЛАНА, не путать с A4 аудита M9).**

> **ИЗМЕРЕНО ТЕМ ЖЕ ДНЁМ — M14, `result/M14_REPORT.md`. Оба условия ниже
> закрыты или смягчены; текст врезки оставлен как запись рассуждения, которое
> к замеру привело, но ЧИТАТЬ ЕГО НАДО С ЭТИМ АБЗАЦЕМ.**
> **Условие 1 (плоский косой разрез в 2D) — ЗАКРЫТО.** Без разреза пол 0.229 и
> не убывает до 10⁵λ; с разрезом 1.3e-7, и это ПОЛ МЕТРИКИ (независимо измерен
> негативным контролем «нулевой контраст»: 9.4e-8 там, где истина тождественно
> ноль). Отношение 1.8·10⁶, от угла границы не зависит (0°/15°/30°/45°).
> Интегрирование — замкнутая форма, O(рёбер), от ω не зависит (`src/cut2d.c`,
> 19/19). **Уточнение, которого не было ни в одном плане: 22 из 23 пунктов пола
> — это ИЗЛОМ, а не волновое число.** Дать накрывающему элементу все три
> истинных направления («бедняцкий разрез») снимает 3%, и только разрывность
> снимает всё.
> **Условие 2 (кривая граница) — СМЯГЧЕНО НА ТРИ ПОРЯДКА.** Закон ошибки от
> смещения локуса разреза ИЗМЕРЕН и не совпал ни с одним из трёх кандидатов
> ниже: `err = 0.125·(kδ)^2.5·(kW)^{−1/2} = 0.125·(kδ)²·sqrt(δ/W)`, показатели
> 2.49 и −0.50, константа с разбросом 4% на двух порядках по W. Отсюда
> проектное правило `h = sqrt(8ρ/k)·[ε·sqrt(kW)/C]^{1/5}`: при λ=550 нм,
> ρ=0.5 м и цели 1% фасет 1.1 мм (а не 0.042 мм по фазовому закону) —
> **в 27 раз крупнее по стороне, в 747 раз меньше фасетов в 3D**. Фасет зависит
> от элемента только как `W^{1/5}`, то есть задаётся ПОВЕРХНОСТЬЮ. Комната:
> 7.6e6 фасетов на 10 м² кривизны — 4% бюджета элементов.
> **ОСТАЛОСЬ ОТКРЫТЫМ:** конформность в 2D (здесь каждая сторона представима
> точно ⇒ непрерывность наследуется от поля — та же дыра, что M9b §7 закрыл в
> §8), поведение в ГАЛЁРКИНСКОЙ постановке, 3D, знакопеременное смещение.
Заголовок выше держится на разрезе: без него крупный элемент на границе среды
упирается в ПОЛ 20.6% (M9b F5), не убывающий с шириной, и LOD умирает вместе с
ним. Разрез этот пол снимает — измерено, машинный ноль до 10⁵λ (M9b §7). Но
измерено это В 1D, где граница есть ТОЧКА, а разрез — деление отрезка. Перенос
в 2D/3D распадается на два условия, и оба сейчас открыты.

**Условие 1 — плоский разрез в 2D/3D. Интегрируемость: ЗАКРЫТА ВЫВОДОМ,
точность: НЕ ИЗМЕРЕНА.**
Разрез ломает сепарабельность, но НЕ аналитичность. Подынтегральное выражение —
(кусочный полином)×(плоская волна) по многограннику; теорема о дивергенции
(`F = (u,0)`, `∂u/∂x = p·e^{i(ax+by)}`, интегрируется по частям в замкнутом
виде) сводит его к рёберным интегралам вида `poly(t)·e^{iγt}`, тоже замкнутым;
вырождение `γ→0` — обычный полином, развилка «ряд/замкнутая форма» уже есть в
`phi.c`. Стоимость O(рёбер) × клиппинг узлами φ (≤16 подобластей на пару).
**СЛЕДСТВИЕ ДЛЯ ЭТАПА 2: кубатура степени 6 по тетраэдрам для этого класса
подынтегральных НЕ НУЖНА** — она закладывалась под общий гладкий интегранд,
которого здесь нет.
Не измерено: что КОСОЙ разрез в 2D воспроизводит одномерный машинный ноль, и
что галёркинская постановка не наказывает неконформность сильнее, чем L2 (сама
L2-проекция скачок не штрафует вообще — оговорка M9b §8).

**Условие 2 — КРИВАЯ граница. Здесь LOD получает потолок, не зависящий от
расстояния.**
Заменять дугу хордой (плоскостью) внутри элемента — это ГЕОМЕТРИЧЕСКИЙ LOD,
запрещённый разделом выше; хорда — тот же грех, что вокселизация, только
гладкий. Если запрет соблюдать формально, стрелка дуги `δ = W²/(8ρ)` даёт
```
W_max = sqrt(8·ρ·δ_доп)        ρ — радиус кривизны ПОВЕРХНОСТИ
```
и это ПОТОЛОК РАЗМЕРА ЭЛЕМЕНТА, В КОТОРЫЙ РАССТОЯНИЕ НЕ ВХОДИТ. Оценка
(вычислена, НЕ измерена; λ=550 нм, Δn/n=0.5, ρ=0.5 м):

| закон допуска на δ | δ_доп | W_max | в λ | R* = W_max/ε |
|---|---|---|---|---|
| фазовый, 1 рад (закон плана) | 0.32λ | 0.84 мм | 1500λ | 0.84 м |
| фазовый, 5% амплитуды | 0.016λ | 0.19 мм | 350λ | 0.19 м |
| рэлеевский, `(kδ)² ≤ 5%` | 0.036λ | 0.28 мм | 500λ | 0.28 м |

За `R*` кривая поверхность ограничена ПОЛОМ, а не потолком `L = εR`, то есть в
комнате — практически сразу. Счёт на ней перестаёт падать с расстоянием:
`A/W_max² ≈ 1.3e7 элементов на м²`. Десять м² кривизны дают 1.3e8 против всего
бюджета комнаты 2e8 — комната того же порядка, ГОРОД НЕТ: каждый кривой объект
стоит своей ближней цены НАВСЕГДА, и счёт становится ограниченным ВХОДОМ. Это
ровно то, ради отказа от чего выбран камерный якорь.
**СЛЕДСТВИЕ ДЛЯ СЧЁТА РАЗМЕРНОСТИ: числа 2e8 / 5e8 выведены по одному лишь
потолку `c/ε²` на октаву, то есть в предположении, что пол НИКОГДА не связывает.
Для кривых поверхностей он связывает почти всюду.** Оговорка о гладкости у этих
чисел была про микрорельеф (вопрос 2); теперь к ней добавляется КРИВИЗНА
гладкой поверхности, что не одно и то же.

**ВЫХОД, который архитектура уже подразумевает: в узле КВАДРИКА, а не плоскость.**
Разрез идёт по истинной поверхности, стрелка дуги исчезает, размер элемента
ограничен только ИЗМЕНЕНИЕМ кривизны (для сферы и цилиндра — ничем). Цена:
точная полиэдральная формула условия 1 не работает, остаётся «внутренний
интеграл точный, внешний квадратура» — константа на РАЗРЕЗАННЫЙ элемент, не
измельчение. LOD выживает, дорожает разрезанный элемент.

**ВЫХОД ВТОРОЙ, ДЕШЕВЛЕ И УЖЕ ЗАПИСАННЫЙ В CLAUDE.md: ЛИНЕЙНАЯ ИНТЕРПОЛЯЦИЯ
ГРАНИЦЫ В УЗЛЕ СРЕДЫ (вопрос юзера 07-26).** Это снимает главную мрачность
абзаца про кривизну, и вот почему: **потолок кривизны сидит не на размере
ЭЛЕМЕНТА W, а на размере ЯЧЕЙКИ СРЕДЫ h** — если локус разреза не обязан быть
ОДНОЙ плоскостью на элемент. Он и не обязан: разрез есть индикатор стороны
`χ(x)`, и ничто не требует, чтобы поверхность внутри элемента была плоской.
Кусочно-линейная (фасетированная) поверхность даёт `χ`, интеграл по которому
берётся как СУММА по граничным ячейкам, в каждой — многоугольник, то есть
условие 1 (замкнутая форма) применяется поячеечно. Регулятор 3 плана («дерево
среды остаётся мелким НЕЗАВИСИМО») ровно об этом.

Насколько это важно количественно (λ=550 нм, ρ=0.5 м, допуск 1 рад ⇒
δ_доп = 0.32λ):

| представление границы | шаг ячейки | в λ | ячеек в 2D | в 3D |
|---|---|---|---|---|
| лесенка (воксель) | `h = 2δ_доп` = 0.35 мкм | 0.64 | ×5.7e6 | ×1.35e10 |
| **линейная (фасет)** | `h = sqrt(8ρδ_доп)` = **0.84 мм** | **1526** | 1 | 1 |

**Это не удобство, это разница между «невозможно» и «возможно»**, и она
объясняет, почему в CLAUDE.md «лесенки нет» стоит с первого дня. Фасетов на
сферу ρ=0.5 м — 4.5e6; на 10 м² кривизны в комнате — 1.4e7, то есть 7% от
бюджета элементов 2e8. Плоская стена стоит НОЛЬ фасетов (линейная функция там
точна) — вот где «стена 5 м = один узел» остаётся правдой.

**ГЛАВНОЕ СВОЙСТВО: фасеты добавляют РАБОТУ СБОРКИ, а не НЕИЗВЕСТНЫЕ.** Разрез
по-прежнему стоит одну степень свободы на (элемент × материальная область)
независимо от числа фасетов под носителем. Платится `(4W/h)^(d−1)`
многоугольных интегралов на разрезанную запись — то есть стоимость идёт за
ГЕОМЕТРИЧЕСКОЙ сложностью, а не за числом длин волн. Это ровно то утверждение,
ради которого архитектура затевалась, только теперь у него есть цена в
конкретных единицах.

**И ФИЗИЧЕСКИЙ ДОВОД, который сильнее счётного (замечание юзера про
рассеивания).** Лесенка — это не «неточная граница», это НАСТОЯЩАЯ
ДИФРАКЦИОННАЯ РЕШЁТКА с периодом h и амплитудой h/2: она рассеивает в порядки
под углами λ/h, то есть широкоугольно, и это паразитное рассеяние физически
неотличимо от шероховатости поверхности (дорогой случай вопроса 2, созданный на
пустом месте). Хуже того, **у лесенки нормаль ВСЕГДА по оси**, значит зеркальное
отражение уходит в заведомо неверном направлении — для рендерера это не
погрешность, а другая картинка. У фасета нормаль верна с точностью `h/ρ`
= 1.7e-3 рад, а остаточное рассеяние — гладкий бугор амплитуды `h²/8ρ` с
рассеянием `(2kδ)²` под МАЛЫМИ углами. То есть линейная интерполяция не только
дешевле в 1e10 раз, она убирает паразитный эффект, а не уменьшает его.
ОГОВОРКИ, чтобы не переоценить: поверхность выходит C⁰, но не C¹ — на рёбрах
фасетов излом нормали порядка `h/ρ`, того же порядка, что и остаточная стрелка,
так что нового источника ошибки не появляется; и у корнерной интерполяции есть
классическая неоднозначность седловых случаев (marching cubes), её надо решать
детерминированно, иначе топология границы будет зависеть от порядка обхода.

**СВЯЗЬ С M14: измеряемое и геометрическое разделяются начисто.** M14 меряет
ЗАКОН `ошибка = f(kδ)` (смещение локуса разреза от истинной границы), а
геометрия даёт `δ = (W/m)²/(8ρ)` для m фасетов на элемент. Вместе они дают
проектное правило «сколько фасетов на элемент», и ни одна половина не выводится
из другой.

**ЧТО ИМЕННО НЕИЗВЕСТНО (и почему это меряется, а не выводится).** У ошибки от
смещённого разреза три кандидата с РАЗНЫМИ показателями, не только константами:
фазовый `kδ`, рэлеевский `(kδ)²` и F5-подобный `sqrt(δ/W)·kδ` (если смещённый
разрез травит элемент так же, как отсутствующий — а F5 говорит именно, что
дефект внутри носителя размазывается по всему носителю). Разброс по `W_max` —
2–3 раза, по счёту элементов — 4–9 раз.
**ИТОГ: «LOD по базису бесплатен» = ИЗМЕРЕНО в 1D + ВЫВЕДЕНО для плоского
разреза + НЕ ИЗМЕРЕНО для кривой границы. Дальше цитировать как условное.**
Закрывается M14 (см. очередь): проекционный стенд, наклонный слой = повёрнутая
1D-слоистая задача ⇒ точный эталон даром, решателя и границы домена не нужно —
тот же приём, которым разрез мерился в 1D.

**A1 — РАЗРЕШЕНИЕ ПРОТИВОРЕЧИЯ С ВОПРОСОМ 2 (аудит плана 07-25).** Вопрос 2
предлагает свернуть локальное рассеяние в эффективный оператор — на первый
взгляд это ровно запрещённое здесь огрубление геометрии. Разница есть, и её
надо держать явно:
- **геометрическое** огрубление приближает ФОРМУ (сдвигает границы, сливает
  детали) ⇒ уводит фазу ⇒ запрещено;
- **рассеивательная** свёртка форму не трогает: она сохраняет ТОЧНОЕ поведение
  на тех модах, которые вообще наблюдаемы, и выбрасывает ненаблюдаемые.
НО: свёртка теряет фазовые соотношения ВНУТРИ свёрнутой области. Значит
```
геометрический LOD ЗАПРЕЩЁН в когерентном режиме,
эффективный оператор РАЗРЕШЁН в усреднённом.
```
Это связывает вопросы 2, 6 и правило выше в один узел; до аудита они лежали
порознь, и следующая сессия выбрала бы наугад. Режим когерентности — параметр
сцены (вопрос 6), значит и допустимость свёртки задаётся сценой, а не кодом.

### Три независимых регулятора
1. **LOD `L = εR`** — ПОТОЛОК размера элемента, задаётся угловым разрешением
   апертуры (то есть числом пикселей).
2. **Уточнение у геометрии** — ПОЛ размера элемента, задаётся КОНТРАСТОМ k,
   а не расстоянием. Измерено в v1 (E3): слабый контраст 1.2 — точных
   интегралов достаточно; сильный 3.0 — уточнение обязательно.
3. **Дерево среды остаётся мелким НЕЗАВИСИМО**: подэлементная деталь входит
   через точный интеграл k² по носителю, а не через измельчение базиса.
   Геометрия и базис развязаны.

### Поле не зависит от камеры
u решает то же уравнение, что бы ни делал наблюдатель; от камеры зависит
только ПРЕДСТАВЛЕНИЕ (через LOD). Движение камеры — ПЕРЕПРОЕКЦИЯ известного
поля в новый базис (рестрикция/пролонгация по двухмасштабному соотношению),
а не пересчёт. У трассировки путей аналога нет: её выборка заякорена на
камеру, каждый кадр начинается заново. Это архитектурное свойство, а не
поздняя оптимизация.
- поворот на месте: R не меняется ни для чего ⇒ стоимость = оператор камеры;
- сдвиг: пересчитывается только сменившее LOD-класс ⇒ стоимость ∝
  ОТНОСИТЕЛЬНОМУ смещению;
- движется источник: оператор цел, меняется правая часть ⇒ рециклинг Крылова;
  движется объект — локальное обновление оператора.

**ИСТОЧНИКИ — ЭТО ТО ЖЕ УРАВНЕНИЕ, ОТДЕЛЬНОЙ МЕХАНИКИ НЕТ (решение юзера
07-26).** Источник входит в `∇²u + k²u = f` как поле `f(x)` в своём
октодереве — ровно так же, как среда входит через `k²(x)`. Следствия:
- **решать по источникам НЕ НАДО.** По умолчанию одно решение с суммарным `f`;
  разложение по слагаемым холодно строго дороже (N систем вместо одной) и
  достаётся только как приём, когда конкретный источник поехал. Массивов
  «на источник» не существует нигде — есть одно поле в дереве;
- **пол уточнения общий.** Излом поля у излучателя портит накрывший его крупный
  элемент по ВСЕМУ носителю — ровно как разрыв среды (M9b F5). Измерено:
  M11_REPORT, артефакт 14 (патч у источника дал 15–25% глобальной ошибки
  в ВАКУУМЕ; полная диадическая лестница — 7e-8). Уточнение у геометрии — это
  ЛЕСТНИЦА, а не патч, и для источника, и для среды;
- **LOD и запрет тоже общие.** Слияние излучателей уводит фазу так же, как
  слияние геометрии, поэтому запрет на геометрический LOD переносится на дерево
  источников дословно, и лазейка та же — усреднённый (некогерентный) режим
  (A1, вопрос 6). Отдельного закона для света не заводить;
  **ЯКОРЬ ЗДЕСЬ ДРУГОЙ, И ЭТО ЛОВУШКА (07-26).** «Далёкий источник можно грубее»
  ВЕРНО, но НЕ по расстоянию до камеры: лампа за километр, освещающая ближнюю
  стену, сидит в самой грубой камерной оболочке, а вклад в видимое поле у неё
  полновесный. Это ровно открытый вопрос 11 — правило `L = εR` выведено для
  канала «элемент → апертура» и не лицензирует огрубление того, что ВЛИЯЕТ на
  видимую область. Огрубление источника лицензируют две другие величины:
  (а) УГЛОВАЯ НЕРАЗРЕШИМОСТЬ из освещаемой области — скопление далёких
  излучателей отдаёт туда поле с угловым содержанием по угловому размеру
  скопления, то есть горстку мод связи вместо N излучателей (это M10,
  применённый к источникам, и здесь порядки на светлой стороне);
  (б) ДОЛЯ в локальном поле — допуск `цель/доля`, а критерий выбрасывания на
  СУММЕ отброшенных, не на каждом (урок M9c часть 2).
  Огрубляется по-разному: протяжённость и кратность — да (некогерентный режим);
  амплитуда — да; ПОЛОЖЕНИЕ — только до `λ/2π`, но допуск ФИКСИРОВАННЫЙ, а не
  падающий как `λ/L`, как у среды.
  ОГОВОРКА: критерий по доле ДИНАМИЧЕСКИЙ (долю знаешь после решения) ⇒ это
  рычаги 4-5, а не статический пол. Статична только угловая часть;
- **асимметрия ОДНА, и она в правой части против матрицы.** `k²` сидит в `A`,
  `f` — в правой части. Поэтому: (а) сдвиг источника оставляет `A` целой,
  сдвиг объекта — нет; (б) суперпозиция точна по `f` (`A⁺` линеен, ядро базиса
  даёт нулевое поле) и не существует по `k²` — это и есть многократное
  рассеяние; (в) ошибка среды МУЛЬТИПЛИКАТИВНА и копит фазу как `k·(δk/k)·L`
  (закон `W_max = λ/(π·δk/k)`), ошибка источника АДДИТИВНА и от расстояния не
  зависит. То есть среду надо знать тем точнее, чем длиннее трасса, а источник —
  только до целевой точности. **Источники — лёгкая половина.** Число к этому
  утверждению не привязано: проверяется свипом допусков на `f` и на `k²` при
  одном размере элемента на проекционном стенде (предсказание: допуск на `f`
  порядка цели и от `L` не зависит, допуск на `k²` падает как `λ/L`);
- **при движении источника переиспользуется НЕ ВЕКТОР РЕШЕНИЯ, а машинерия
  оператора.** M11 измерил: старое решение при сдвиге источника ВРЕДНО —
  `e₀ = 2.00` против `1.00` у старта с нуля (сдвиг источника сдвигает фазу,
  разность двух волн равной амплитуды доходит до удвоенной), и итераций
  122–181 против холодных 86–146, то есть тёплый старт хуже холодного в
  1.3–1.4 раза. Переиспользуемый межкадровый объект здесь — `A`, её
  предобуславливатель и подпространство Крылова. Тем самым вопрос об источниках
  СЛИВАЕТСЯ с новым критическим путём (скорость сходимости), а не идёт рядом.

ИНКРЕМЕНТ ТОЧНО: «пересчитать только новые потенциалы» — оптимистично.
Система связана, новые неизвестные меняют старые. Правильно: старое решение
как начальное приближение, решаем ПОПРАВКУ, невязка которой ненулевая только
у новых потенциалов. Носитель мал ⇒ дёшево, но поправка РАСПРОСТРАНЯЕТСЯ.
Измеренный прецедент — E3: локальное уточнение не доходит до сенсора за один
проход, нужно 2-3. Значит цена инкремента = новые потенциалы + 2-3 прохода.

**A2 — НЕОБЪЯВЛЕННАЯ ЗАВИСИМОСТЬ (аудит плана 07-25). ВСЁ СКАЗАННОЕ ЗДЕСЬ ПРО
ИНКРЕМЕНТАЛЬНОСТЬ ДЕРЖИТСЯ НА ВОПРОСЕ 3.** При НЕПРЕРЫВНОМ `L = εR` сдвиг
камеры меняет `R` у каждого элемента, то есть меняет размеры ВСЕХ — инкремента
не существует вовсе, и утверждение о единицах итераций на кадр вместе с ним.
Инкремент возникает ТОЛЬКО при квантовании уровней диадическими оболочками:
тогда элемент меняет размер лишь пересекая границу оболочки, и при смещении δ
меняется доля `~δ/R` каждой оболочки (шаг 1 см при ближней оболочке 0.5 м — 2%
её элементов; сближение вплотную дороже, мерить, а не оценивать).
Поэтому вопрос 3 — не «проектная работа потом», а ПРЕДПОСЫЛКА вердикта по
реальному времени, и в очереди он поднят.

ОПЕРАТОР ТОЖЕ ИНКРЕМЕНТАЛЕН. Элемент матрицы `⟨B_i, L B_j⟩` зависит ТОЛЬКО от
пары элементов и от среды — камеры в формуле нет. У элементов, переживших
движение камеры, записи матрицы не меняются буквально; пересчитывать надо
только новые, и мемоизация 1D-интегралов (M5, 95% попаданий) собирает их из
готового. При диадических оболочках (вопрос 3) «что изменилось» становится
СЧЁТНЫМ: элементы не плывут по размеру непрерывно, а дискретно пересекают
границу оболочки; ушедшие снимаются точной рестрикцией.

### Якорь LOD: КАМЕРА (решено 07-25) + двухчастотная иерархия
Камерный якорь (`L = εR` от камеры) даёт стоимость, ограниченную **ВЫХОДОМ**:
∝ числу пикселей, от размера сцены только логарифмически. Сценный якорь
(размер от детали и контраста) дал бы стоимость, ограниченную **ВХОДОМ** —
растущую вместе со сценой линейно. Для рендеринга первое и есть весь смысл;
менять нельзя. Три регулятора уже и есть комбинация обоих принципов по
размеру (камерный потолок + сценный пол).
НОВОЕ ИЗМЕРЕНИЕ — ВРЕМЕННОЕ. Делить уровни ещё и по частоте обновления:
- **постоянное ядро** — несколько самых крупных уровней на всю сцену. Их мало,
  считаются ОДИН РАЗ, при движении камеры не трогаются. Несут глобальный
  перенос (непрямое освещение).
  ДЫРА (аудит B2): ядро объявлено привязанным к сцене, а якорь LOD решён в
  пользу камеры — чем тогда задаются РАЗМЕРЫ элементов ядра, не сказано.
  Кандидат: ядро = оболочки выше некоторого номера, размеры по-прежнему
  диадические, но их границы привязаны не к камере, а к сцене (тогда ядро
  переживает любое перемещение камеры внутри сцены). Не решено;
- **кадровая оболочка** — мелкие уровни там, где камера требует (εR).
  Обновляется инкрементально, даёт стоимость ∝ пикселям.
> **Уровень = и пространственный масштаб, и частота обновления.**
Крупный меняется редко, мелкий каждый кадр. Каскад из инструмента решателя
становится ещё и структурой переиспользования во времени — та же иерархия на
две задачи (в движках это кэширование освещённости, здесь выпадает из
структуры само).
НЕ ИЗВЕСТНО: где резать (сколько уровней в ядре) и сколько НАПРАВЛЕНИЙ нужно
ядру, чтобы обслужить ЛЮБУЮ камеру, а не одну — это M9c, применённый к крупным
уровням; у крупного элемента ёмкость по модам выше, вопрос не праздный.
ПРЕДЕЛЬНЫЙ СЛУЧАЙ: для статичной сцены при полностью камеро-независимом поле
кадр стоит ТОЛЬКО оператор камеры (два FFT) — не «дешевле», а другой класс
сложности. У трассировки путей такого режима нет структурно.

ОТЧЁТНОСТЬ: холодный старт и установившийся режим приводить ДВУМЯ числами.
У трассировки холодного старта нет; давать только выгодное — подтасовка.

## УНАСЛЕДОВАНО ИЗ v1 И ЖИВО
- **Галёркин ⟨Φᵢ, Lu−f⟩=0** — единственная рабочая формулировка. Чистое
  внутреннее МНК (коллокация, Грам/FOSLS) ВЫРЕЗАЕТ ИЗЛУЧЕНИЕ: u=0 даёт
  нулевую невязку в пустоте, минимизатор предпочитает тишину. Радиационное
  условие не в функционале — фундаментально. Тесты остаются негативным
  контролем. Не зависит от базиса.
- **Точная вырожденность** (Σ(−1)ⁿφ(x−n) ≡ 0, найквистова мода) ⇒ только
  min-norm решатели (LSQR, zgelsd); AᴴA не формировать никогда.

**A5 — ДВА РАЗНЫХ ЯВЛЕНИЯ ПОД ОДНИМ ИМЕНЕМ «ВЫРОЖДЕННОСТЬ», И ЛЕКАРСТВО ЕСТЬ
ТОЛЬКО У ОДНОГО (измерено 07-26 в срезе, SLICE_PLAN шаг 3).**
Строка выше — «только min-norm» — верна, но НЕ БЕЗУСЛОВНА, и условие до сих пор
нигде не было названо.

- **Вырожденность БАЗИСА** — столбцы линейно зависимы: найквистова мода
  `Σ(−1)ⁿφ ≡ 0` и межуровневая избыточность, где функции буквально равны.
  Безвредна по ОДНОЙ причине: **каждое направление ядра даёт НУЛЕВОЕ ПОЛЕ.**
  Решение как вектор не единственно, как ПОЛЕ — единственно, и какого
  представителя выберет min-norm, безразлично. Именно это свойство проверялось
  всегда, и именно на нём стоит вся конструкция «намеренно переполненный базис».
- **Дефицит УРАВНЕНИЙ** — строк меньше, чем нужно, чтобы закрепить поле.
  Свободные направления дают ДРУГОЕ ПОЛЕ, и min-norm выбирает из них меньшее по
  норме, то есть систематически не то. Измерено: 1241 строка на 2576 столбцов,
  ложный вектор даёт `|u| = 0.48` против истинного `0.95` и при этом выигрывает
  у истинного И по невязке (3.7e-5 против 1.5e-3), И по норме (0.61 против
  1.12). Решатель безупречен; задача действительно имеет много ответов.

**КРИТЕРИЙ, КОТОРЫЙ ИЗ ЭТОГО СЛЕДУЕТ И КОТОРЫЙ НАДО ТРЕБОВАТЬ ОТ КАЖДОЙ НОВОЙ
ПОСТАНОВКИ: ядро системы обязано лежать в НУЛЕВОМ ПОЛЕ.** Это проверяемое
условие, а не общее место: берём базис ядра (или несколько случайных векторов из
него), считаем поле, смотрим норму. Для базисного ядра оно выполнено по
построению; для системного — нет, и тогда min-norm из лекарства превращается в
механизм выбора неправильного ответа.
Аудит M12 подозревал этот класс (R5: «предобуславливание МЕНЯЕТ ВЫБОР РЕШЕНИЯ…
теоретически безвредно, но в этом проекте уже дважды оказывалось неверным») —
теперь он не подозрение, а измерение, и пришёл он не от предобуславливания, а от
усечения набора строк.
ПРАКТИЧЕСКИ: галёркинская постановка КВАДРАТНА по построению — одно уравнение на
каждую пробную функцию. Любое место, где строк оказалось меньше столбцов, надо
объяснять, а не принимать: `⟨B_i, Lu⟩ = 0` есть ИНТЕГРАЛЬНОЕ ТОЖДЕСТВО и
выполняется истинным решением при ЛЮБОЙ тестовой функции, независимо от того,
полон ли у неё набор соседей.

**A5 ВОССТАНОВЛЕН — ПРИЧИНА НАЙДЕНА И УСТРАНЕНА (07-26, M15, `result/M15_REPORT.md`).**
Нарушение критерия оказалось НЕ свойством переполненного базиса и не следствием
усечения строк, а ОДНИМ пропущенным членом: разрез даёт функцию со скачком,
сильная форма интегрируется по сторонам раздельно и теряет распределённые члены
на локусе, поэтому скачок для оператора БЕСПЛАТЕН. Добавление члена Нитше
(`src/nitsche2d.c`; собранная матрица становится ровно симметричной формой Нитше,
проверено симметрией 8e-16 и тождеством Грина против `cut2d`) даёт:
```
поле ядра d:  0.376 / 0.322 / 0.286   →   0.0053 / 0.0042 / 0.0039   (в 71 раз)
|d| при этом  0.904 → 0.768 — ядро БАЗИСА осталось большим, поле от него исчезло
```
**То есть утверждение «каждое направление ядра даёт нулевое поле» в 2D верно, но
только при условии, что КАЖДЫЙ разрыв базиса виден оператору.** Это условие надо
предъявлять каждой новой постановке наравне с самим критерием: cut ⇒ Nitsche,
всегда, иначе конструкция не менее точна, а НЕ ОПРЕДЕЛЕНА.
Штрафной коэффициент оценивается снизу (`β = γ(k_max + 2√15/W)`), а не
подбирается: плато 4% на множителе 4 вокруг предсказанного значения, разброс по
четырём порядкам β всего 1.7× — против ямы 17× у коллокационной альтернативы.

**ИСТОРИЯ ВОПРОСА — A5 ПРИМЕНЁН И ПРОВАЛЕН, ПЕРВОЕ ПРЯМОЕ ИЗМЕРЕНИЕ (07-26,
срез, вакуум). Абзац ниже — то, что мерилось ДО починки.**
Критерий проверен без нового кода: разность `d = c_LSQR − c_истинное` есть
вектор ядра, потому что оба вектора удовлетворяют системе (невязки 2.9e-5 и
8.2e-5 при единичных нормах строк):
```
|A d| / (|A| |d|)  ~  8.4e-7        поле от d:  0.41 / 0.35 / 0.31 / 0.28
```
То есть направление ядра с точностью 1e-7 даёт поле в 28–41% от падающей волны.
**ЯДРО НЕ ЛЕЖИТ В НУЛЕВОМ ПОЛЕ.** Это ровно то условие, без которого «намеренно
переполненный базис + min-norm» перестаёт работать, и в 2D оно НАРУШЕНО.
Дополнительно: поле ядра МАКСИМАЛЬНО В ЦЕНТРЕ и убывает наружу (0.41 → 0.28),
значит оно не локализовано во внешнем кольце свободных столбцов — списать на
краевой эффект нельзя. В непрерывной задаче однородная внешняя задача с DtN
решений не имеет (единственность), поэтому это ЧИСТО ДИСКРЕТИЗАЦИОННАЯ мода.
Проверено на двух доменах: R/W = 7 и R/W = 20 дают 0.405/0.346/0.307 —
совпадение до трёх знаков при вчетверо большем числе строк, то есть от размера
домена и от соотношения строк к столбцам мода НЕ ЗАВИСИТ.
- **Критерий сходимости — по ПОЛЮ/проекции, не по сырой невязке** (в v1
  невязка стагнировала на краевой моде, полю не вредящей).
- **Детерминизм**: параллельные редукции только с фиксированным порядком.
- **Повторные проходы — штатная часть**, не опция (E3).
- **Камера-оператор**: тонколинзовая фазовая маска e^{−ik|x|²/2f} × Френель,
  оба FFT, ноль новых неизвестных; суб-λ уровней у сенсора нет. Число
  неизвестных на апертуре = число разрешимых пятен = число пикселей.
- **Октодерево, аналитические интегралы, мемоизация, инфраструктура гейта.**
- **LANDMINE** (CLAUDE.md): openblas zgelsd бьёт стек на прямоугольных
  комплексных матрицах. Несущий базис переполнен ⇒ системы прямоугольные ⇒
  грабли ждут. Референсный LAPACK, openblas вне сборочных шеллов.

## ВЕХИ

### Готово
- **M0** инфраструктура: ccheck/cverify/lean, Makefile, тест-раннер.
- **M1** φ-модуль [932/932, ccheck CLEAN]. Аналитическое интегрирование на
  лету (таблицы на все пары уровней взрываются: разность уровней ~20,
  смещений 4·2^Δ). Расширен в M9a: `deriv ∈ {0,1,2}`,
  `hz_phi_prod_integral_osc(a,b,f1,f2,omega)` — стоимость сборки НЕ зависит
  от omega. Приёмка: разбиение единицы, нулевая мода, C¹, двухмасштабное
  соотношение, ⟨φ,φ⟩=92/15, ⟨φ'',φ⟩=−16/3, сверка с квадратурой, строгий 0
  вне носителей.
- **M3** октодеревья [27/27]: арена-аллокатор, сериализация, процедурные
  сцены, cverify на обходе.
- **M5** сборка matrix-free [19/19 против квадратуры]: тензорные
  произведения, мемоизация 1D-интегралов.
- **M7-ядро** камера-оператор: линза + угловой спектр, срезы полей, PNG.
- **M2.5** картинки: щель, тень бокса, линза-капля, корнелл (v1-режим).

### M9. Несущий (Трефтц) базис — КРИТИЧЕСКИЙ ПУТЬ
Спецификация, аудит и фальсификаторы: `result/M9_CARRIER_AUDIT.md`. Аудит
проведён ДО прогона; исходная формулировка эксперимента ЗАБРАКОВАНА за
самоподтверждение (направление несущей нельзя задавать «на апертуру» — это
лучевое допущение под другим именем).

- **M9a [ГОТОВО 07-25]** — аналитические осциллирующие интегралы. 932/932,
  ccheck CLEAN. Находка: при ω·h ~ 1e3 брутфорс-квадратура в double не может
  свидетельствовать точнее ~1.4e-16 (ошибка в слагаемых, не в сумме; Kahan и
  уплотнение не помогают). Эталон переведён на long double — аналитический
  путь совпадает с ним до 3e-19.

- **M9b [ГОТОВО 07-25]** — отчёт: `result/M9b_REPORT.md`. КРАСНЫЙ ФЛАГ НЕ
  ПОДНЯТ, M9c запускать. Сводка:
  - A1 закрыт: аналитика == квадратура до 2e-11, число вызовов не зависит от k;
  - выигрыш по стоимости ЕСТЬ, но СЛАБЕЕ выигрыша по неизвестным (dim ×0.13–
    0.24, сборка только ×0.33), потому что **nnz/строку РАСТЁТ при укрупнении**
    (13.9 → 24.4) — межуровневая связность. Стоимость не следует за dim;
  - **ГЛАВНОЕ: предел размера элемента задаёт СТРУКТУРА, а не перенос.**
    Равномерное укрупнение до 2λ рушит точность (0.283), а укрупнение ТОЛЬКО
    ДАЛЬНЕЙ зоны свободно до 16λ (ошибка плоская 0.017–0.035). Причина:
    плоская волна лежит в пролёте несущего базиса ТОЧНО при любом W (разбиение
    единицы), поэтому в пустоте дисперсии огибающей нет вовсе. Ошибка рождается
    там, где поле не плоская волна, и переносится дальше. Это подтверждает три
    регулятора, а не иллюстрирует их;
  - лучшая конфигурация: мелко у источника + 16λ дальше — dim 188 против 1034
    при ошибке 0.017 против 0.022;
  - вход в вопрос 3: стоимость шва между размерами — в пределах шума;
  - вход в вопрос 4: обусловленность 2.4e5 → 4.8e5 на диапазоне размеров 128× —
    болезнь Трефтца НЕ взрывается (в 1D, с одной несущей);
  - время плотного решения НЕНАДЁЖНО (разброс 21–33 c на одном входе), в
    метриках стоимости не использовать; ошибки воспроизводятся бит-в-бит.
  ОГРАНИЧЕНИЕ: 1D ⇒ угловая разреженность вырождена и НЕ проверена. Необходимое
  условие, не достаточное.

- **M9b (исходная спецификация)** — переделка `tools/carrier1d.c` с квадратуры
  на аналитическую сборку. **ГЕЙТ ВСЕГО ДАЛЬНЕЙШЕГО.**
  - четвёрка стоимости: неизвестные, ненулевые, флопы сборки, время;
  - срезы: ошибка(расстояние) при фикс. L и ошибка(L) при фикс. расстоянии —
    разделить дисперсию и угловое обеднение;
  - ДОБАВЛЕНО: стоимость ПЕРЕХОДА между размерами элементов (вход в вопрос 3;
    carrier1d уже смешивает мелкие и крупные зоны, переход есть, но не
    измерен); ненулевых на строку и структура разреженности (вход в вопрос 4
    и в бюджет памяти); обусловленность как функция размера элемента (вход в
    вопрос 4 и в напряжение «меньше неизвестных ⇔ больше итераций»);
  - воспроизвести числа квадратурной версии (FINE dim=1034 err@42=0.022;
    MIXED 1λ dim=248 err@42=0.034) в пределах 1e-3;
  - **КРАСНЫЙ ФЛАГ**: выигрыш только по неизвестным, но не по стоимости ⇒
    M9c НЕ запускать, архитектура не подтверждена.

- **M9c** — 2D, угловая разреженность как ИЗМЕРЕНИЕ.
  - набор КАНДИДАТНЫХ несущих на элемент, min-norm, мерить долю значимых;
  - матрица 2×2 {обычные, несущие} × {равномерно мелко, LOD}; ячейка
    «обычные + LOD» — негативный контроль с предсказанным отказом;
  - эталоны АНАЛИТИЧЕСКИЕ (верны при любом λ): H₀⁽¹⁾, Зоммерфельд на
    полуплоскости, точный ряд для цилиндра;
  - сцена с реальным разбросом глубин; граница НЕ измеряется, её стоимость —
    отдельной строкой (вопрос 1);
  - NB: 2D даёт систематически ОПТИМИСТИЧНЫЙ ответ (мод `(kL)^(d−1)`: в 2D
    линейно, в 3D квадратично). Экстраполировать ДОЛЮ выживших несущих, не
    абсолютное число.
  - **Приёмка / фальсификаторы (зафиксированы ДО прогона):**
    1. доля значимых несущих на элемент НЕ растёт с L;
    2. точность при равном ЧЕСТНОМ бюджете лучше базовой λ/8;
    3. «обычные потенциалы + LOD» обязаны провалиться;
    4. численный ранг растёт медленнее числа кандидатов;
    5. ЧИСЛО ИТЕРАЦИЙ до заданной точности ПОЛЯ. Бюджет: матвек упирается в
       ПАМЯТЬ (~1e9 ненулевых ⇒ ~16 мс при 1 ТБ/с) ⇒ на 30 кадрах допустимы
       ЕДИНИЦЫ итераций на кадр, достижимо только инкрементально. Мерить И
       холодный старт, И установившийся режим.
    Пункты 4 и 5 вместе проверяют напряжение: переполненный базис сокращает
    неизвестные и ОДНОВРЕМЕННО ухудшает обусловленность, а обусловленность —
    это итерации.

### M10. ОПЕРАТОР ОБЪЕКТА — далёкая геометрия в волновом виде
Заведена 07-25 (предложение юзера; обобщает вопрос 2 с микрорельефа на объект
целиком и закрывает противоречие A1).
ИДЕЯ. Далёкий объект вносит в кадр число мод, ограниченное его УГЛОВЫМ
размером относительно разрешения камеры — то есть числом покрываемых пикселей.
Значит его можно заменить не геометрией, а ОПЕРАТОРОМ РАССЕЯНИЯ ранга ≈ этому
числу: (входные моды = направления освещения) × (выходные = направления на
камеру). Здание в километре на 100 пикселей — оператор 100×q, а не миллионы
треугольников. Это волновой аналог импостеров, но ТОЧНЫЙ на наблюдаемых модах,
а не приближённый. Классические имена: эквивалентные источники, T-матрица,
поверхностные мультиполи.
ЧТО ДАЁТ: (а) снимает запрет на геометрический LOD, не нарушая его — форма не
трогается, выбрасываются только ненаблюдаемые моды; (б) оператор есть свойство
ОБЪЕКТА, не вида ⇒ переиспользуется между кадрами и между камерами; (в) те
самые семь порядков вопроса 2, но на любом масштабе, а не только на
шероховатости.
РИСКИ, которые и надо измерить:
- число ВХОДНЫХ мод при сложном освещении может оказаться немалым — оператор
  матрица, а не вектор;
- свёртка теряет внутреннюю фазу ⇒ законна в усреднённом режиме (A1). Для
  зеркала, линзы, решётки придётся считать честно — это граница применимости,
  и её надо очертить, а не обойти.
ПРИЁМКА: на объекте с известным аналитическим рассеянием (цилиндр, ряд Ми)
оператор ранга «число покрываемых пикселей» воспроизводит поле на апертуре с
точностью, не хуже прямого решения; рост ранга с угловым размером линеен, а не
квадратичен по (kD).

### M11. ИНКРЕМЕНТ ПРИ ДВИЖЕНИИ КАМЕРЫ [ИЗМЕРЕН 07-26]
ИТОГ: `result/M11_REPORT.md`. Аудит ДО прогона: `result/M11_AUDIT.md` (шесть
забракованных постановок, четыре фальсификатора, четыре негативных контроля).
Стенд: `tools/carrier_incr.c`, 1D, ccheck CLEAN.
Фальсификаторы 1, 2, 3 ПРОЙДЕНЫ; 4 (дрейф) пройден частично — накопления нет ни
при каком бюджете, но уровень установившейся ошибки задаётся бюджетом
(2 итерации/кадр → 2–3%, 5 → 1.4–2.3%, 20 → сходится по кадрам до 1.1e-3, что
втрое выше прямого пола). ПОРОГ ПРИЁМКИ НЕ ВЗЯТ (тёплых 10–70 при холодных
86–146). Отчётность двумя числами, как требовалось: холодный старт 86–146
итераций, установившийся режим 20 итераций на кадр.
Отклонение от постановки: сделано в 1D, а не 2D — все четыре фальсификатора в
1D выразимы, а 2D-оператора с терминацией не существует (в 2D DtN нелокален,
TERMINATION_REPORT). Цена отклонения названа: угловое переизбрание несущих не
проверено.

(исходная формулировка)
Заведена 07-25 (запрос юзера). ГИПОТЕЗА: при движении камеры пересчитывается
только высокочастотная часть, и что именно — определяется LOD и геометрией
сцены.
ЗАВИСИМОСТИ (без них эксперимент бессмысленен): вопрос 3 (диадические
оболочки — иначе меняется ВСЁ, см. A2) и M9c (нужны направления).
ПОСТАНОВКА: 2D (в 1D камеры нет), сцена с реальным разбросом глубин. Решение из
положения 1 → сдвиг камеры → мерить:
(а) сколько элементов сменило оболочку;
(б) насколько изменилось решение и ГДЕ (носитель возмущения);
(в) сколько проходов поправки до заданной точности ПОЛЯ;
(г) сравнение с холодным решением — два числа, как требует отчётность.
ФАЛЬСИФИКАТОРЫ (зафиксированы ДО прогона):
1. носитель возмущения НЕ локализуется вокруг новых элементов ⇒
   инкрементальность опровергнута;
2. число проходов поправки растёт с размером СЦЕНЫ, а не со сдвигом ⇒ то же;
3. доля сменивших оболочку растёт быстрее δ/R.
Любой из трёх снимает утверждение «единицы итераций на кадр», а вместе с ним
и вердикт по реальному времени.

### После M9: пересборка
- **M4** активный набор под три регулятора (LOD-потолок, контрастный пол,
  дерево среды). Критерий уточнения ДИНАМИЧЕСКИЙ — невязка появляется только
  в ходе решения; статическая часть здесь, динамика в M6.
- **M6** решатель — по результату вопроса 4.
- **M8** валидация (Ми-сфера, слой, корнелл, клин) + профиль + вердикт.

## Следующие этапы (отдельными планами)
- Этап 2: наклонные границы — клиппинг куба плоскостью, тетраэдризация,
  точная кубатура ≤6; замена integrate_cell() за тем же интерфейсом.
- Вокселизатор watertight OBJ → октодерево (SDF), сцены McGuire/Bitterli.
- GPU-бэкенд; CPU-референс остаётся гейтом бит-в-бит/ε-сверки.
- Градиентное k (кусочно-линейное), объёмные данные.

## ОТКРЫТЫЕ ВОПРОСЫ (закрываются измерением, не спором)

1. **ТЕРМИНАЦИЯ ДОМЕНА ПРИ ЭЛЕМЕНТАХ ≫ λ — В КРИТИЧЕСКОМ ПУТИ (07-25).**
   Пункт выводился за область M9 как «потом» — и в итоге ИСПОРТИЛ ЧЕТЫРЕ
   ИЗМЕРЕНИЯ ПОДРЯД: рампа съедала источник, полоса уровня была уже носителя,
   мелкий элемент масштабировался вместе с крупным, пик поглощения задавал
   λ-масштабную длину спада при элементах в сотни λ. Исключить его не удалось;
   любой замер с решателем упирается в него снова. Проекционный тест его
   обходит, но только временно.
   Поглощающая рампа v1 задана в
   единицах λ (квартичная, 2λ) и при реалистичном λ/L МЕНЬШЕ ОДНОГО ЭЛЕМЕНТА
   — представить её нечем. carrier1d обошёл мелкими элементами у границ:
   законно в бенчмарке, не масштабируется. АРХИТЕКТУРНАЯ ДЫРА, не тюнинг.
   Варианты: DtN/аналитическое условие излучения, PML в терминах огибающей,
   элементы на функции Грина. Поглощает старые вопросы «толщина оболочки» и
   отчасти «величина Im k²».

2. **ЭФФЕКТИВНОЕ ОПИСАНИЕ ПОДLOD-СТРУКТУРЫ** (шероховатость, микрорельеф).
   ОБОБЩЕНО 07-25 ДО ВЕХИ M10 «оператор объекта»: та же свёртка работает не
   только на шероховатости, но на объекте целиком, и ранг оператора задаётся
   числом покрываемых пикселей. Граница применимости — A1: свёртка теряет
   внутреннюю фазу, значит законна в усреднённом режиме, но не для зеркала и
   линзы.
   LOD сокращает число мод НА ВЫХОДЕ, а не стоимость локальной задачи, их
   порождающей. Комната 10 м при λ=550 нм: поверхности при λ-разрешении
   ≈ 3e14, LOD-элементы на них ≈ 1e7 — СЕМЬ ПОРЯДКОВ лежат целиком в вопросе
   «можно ли свернуть локальное рассеяние в несколько мод». Гладкая
   поверхность — можно и бесплатно (Френель). Структурированная — нужен
   эффективный оператор рассеяния на нескольких модах (волновой аналог
   фильтрованного BRDF); задача локальная, пообъектная, переиспользуемая
   между кадрами. Пока не сделана, «размер потенциалов по LOD» верно только
   для гладких поверхностей.

3. **КАСКАД: КАК НЕПРЕРЫВНЫЙ LOD ЛОЖИТСЯ НА ДИАДИЧЕСКУЮ ЛЕСТНИЦУ.**
   ПОДНЯТ 07-25 ПО АУДИТУ: это не «проектная работа потом», а ПРЕДПОСЫЛКА
   утверждения об инкрементальности и, через него, всего вердикта по реальному
   времени (см. A2 в разделе «Поле не зависит от камеры»). При непрерывном
   L = εR сдвиг камеры меняет размер КАЖДОГО элемента и инкремента не бывает.
   Двухмасштабное соотношение, вложенность спанов, интерполяция старта уровня
   и рестрикция требуют h = 2^j. LOD даёт L = εR — непрерывную функцию R.
   Гипотеза (не решение): квантовать R диадическими оболочками, L = 2^j —
   тогда при движении камеры потенциалы не «плавно меняют размер», а
   ДИСКРЕТНО пересекают границу оболочки: новые появляются счётно, уходящие
   снимаются точной рестрикцией. То есть диадичность, нужная для
   двухмасштабного соотношения, оказывается тем же, что делает межкадровый
   инкремент определённым. Требует спецификации: границы оболочек, переходы,
   перекрытие, что происходит с несущей при смене оболочки.
   ПРОЕКТНАЯ РАБОТА. Вход — M9b (стоимость перехода между размерами).

4. **АРХИТЕКТУРА РЕШАТЕЛЯ.** В v1 было: голова плотно (zgelsd, ~10³) + λ-пол
   структурно (FFT) + патчи. Теперь НЕТ НИ ГОЛОВЫ (самый крупный уровень
   зависит от R), НИ ПОЛА. Что решает систему — не описано. Переносится
   только: детерминизм, критерий по полю, повторные проходы, старт
   интерполяцией. Связано с вопросами 5 и 7. ПРОЕКТНАЯ РАБОТА, вход — M9b
   (разреженность, обусловленность от размера элемента).

5. **ПОГЛОЩЕНИЕ: ГЛОБАЛЬНОЕ ИЛИ ТОЛЬКО В ОБОЛОЧКЕ.** Глобальное Im k²
   вводилось против резонансов Гельмгольца. Но оно же связывает Re и Im поля
   ВЕЗДЕ (u = p+iq: связь идёт только через Im k²) и делает матрицу
   комплексно-симметричной, заставляя держать BiCGStab — который в v1
   расходился при контрасте 3.0. Если убрать поглощение в оболочку,
   внутренность становится ВЕЩЕСТВЕННОЙ симметричной знаконеопределённой ⇒
   применим MINRES: короткая рекурсия, невязка гарантированно не растёт,
   расходиться нечему. Размен: резонансы против устойчивости решателя.
   NB: экономии флопов тут нет (два вещественных БПФ пакуются в одно
   комплексное, код это уже делает) — выигрыш в КЛАССЕ решателя.

6. **КОГЕРЕНТНОСТЬ = ТОЧНОСТЬ ГЕОМЕТРИИ (переписан 07-25).** Это не два
   вопроса, а один: СКОЛЬКО ФАЗОВОЙ ИНФОРМАЦИИ В СЦЕНЕ РЕАЛЬНО ОПРЕДЕЛЕНО.
   ФИЗИКА ИСТОЧНИКОВ: у реального излучателя фазы просто нет. Длина
   когерентности `L_ког ≈ λ²/Δλ`: белый светодиод ~3 мкм, солнце ~1 мкм,
   лазер — метры. Поперечная когерентность от протяжённости `λR/D`: лампочка
   1 см на 3 м даёт ~165 мкм.
   СЛЕДСТВИЕ ПРИЯТНОЕ: жёсткие требования к фазе (закон δk/k ≲ λ/2πW и
   δx ≲ 0.3λ) действуют ТОЛЬКО ВНУТРИ ОБЪЁМА КОГЕРЕНТНОСТИ. Разности хода
   длиннее L_ког не интерферируют, их фаза в наблюдаемую не входит. Иначе
   рендеринг обычного света был бы невозможен в принципе.
   СЛЕДСТВИЕ НЕПРИЯТНОЕ: один монохромный расчёт по сцене, много большей
   L_ког, даёт интерференцию между путями, которой в природе НЕТ. Спеклы на
   slit_slice и cornell_camera — не «шум, который усредним», а артефакт
   невыполненного допущения. Кадры v1 неверны для обычного света, а не просто
   шумны.
   ИСПРАВЛЕНИЕ ОЦЕНКИ: ранее стояло «множитель 10-100x». Честно: число
   независимых спектральных выборок задаётся ЦЕЛЕВЫМ КОНТРАСТОМ спеклов
   (падает как 1/√N) ⇒ 10² для 10%, 10⁴ для 1%; выборки должны отстоять на
   δk ≳ 1/L_путь, иначе не независимы. Диапазон 10²–10⁴, занижено было на
   один-два порядка. Смягчает только то, что выборки смущающе параллельны и
   делят один оператор (блочное решение, рычаг 7).
   АРХИТЕКТУРНОЕ СЛЕДСТВИЕ: **режим когерентности должен быть ПАРАМЕТРОМ
   СЦЕНЫ, а не глобальным допущением.** Сейчас он глобальный и неявный. Жёстко
   когерентный режим осмыслен только там, где И геометрия аналитическая, И
   источник узкополосный: линза, зеркало, решётка, интерферометр. Для сцены,
   заданной грубее λ, когерентный расчёт не просто труден — он НЕ ОПРЕДЕЛЁН,
   и единственный корректный ответ усреднённый.
   Правильный объект для частичной когерентности — функция взаимной
   когерентности J(x₁,x₂): замкнутое ЛИНЕЙНОЕ уравнение, интерференция цела,
   спеклы гасятся физикой. Цена — 6 измерений вместо 3. СТАВКА ПРОЕКТА:
   когерентное 3D-поле дешевле, чем 6D фазовое пространство. Усреднение по k₀
   — бедняцкая аппроксимация J.

7. **ОБУСЛОВЛЕННОСТЬ НЕСУЩЕГО БАЗИСА vs ЧИСЛО ИТЕРАЦИЙ** → приёмка M9c п.4-5.
   Инструменты: численный ранг zgelsd при фиксированном rcond, профиль
   сингулярных чисел, влияние на ПОЛЕ. Число обусловленности как таковое
   бессмысленно — система вырождена ТОЧНО и по построению.

8. **ТЁПЛИЦЕВА СТРУКТУРА ВНУТРИ ДИАДИЧЕСКОЙ ОБОЛОЧКИ** — открыт, не мёртв.
   Глобальная тёплицева структура v1 была ОПРЕДЕЛЕНА регулярной решёткой λ/8
   и вместе с ней ушла. Но если оболочки диадические (вопрос 3), внутри одной
   оболочки элементы одного размера и лежат регулярно — локальная регулярность
   есть. Тёплиц «с дырами» (оболочка это не куб) и с зависимостью от
   направления несущей: не очевидно живой, но и не установлено мёртвый.
   Проверять после вопроса 3.

9. **ПОРЯДОК ОБХОДА ДЕРЕВА В СБОРКЕ** для локальности кэша → профилирование.

10. **КОНФОРМНОСТЬ РАЗРЕЗА** [ОБЕ ПОЛОВИНЫ ИЗМЕРЕНЫ 07-25. Вторая (M9b §8):
    скачок появляется РОВНО тогда, когда стороны перестают быть представимыми,
    и даже тогда он на 2-3 порядка меньше самой ошибки приближения (0.03-0.3%
    против 12-17%). Пока элемент справляется со своей стороной, непрерывность
    выходит машинной сама, без связей. ОГОВОРКА: L2-проекция скачок не
    штрафует, то есть это худший случай по величине; влияние на РЕШЕНИЕ —
    отдельный вопрос, требует оператора. Первая половина, result/M9b_REPORT.md §7:
    разрез чинит представимость ПОЛНОСТЬЮ — машинный ноль при любой ширине до
    1e5 λ против 20% у гладкого, ценой удвоения степеней свободы только на
    задетых элементах. Вторая половина — конформность — НЕ закрыта: скачок
    вышел машинным нулём лишь потому, что в том тесте каждая сторона
    представима ТОЧНО и непрерывность наследуется от поля. Проверять на поле,
    представимом на сторонах лишь приближённо.] Разрез даёт независимые коэффициенты по две
    стороны границы, а поле диэлектрика на ней НЕПРЕРЫВНО (u и поток). Наивный
    разрез поэтому неконформен — u получит скачок. Варианты: связи на
    коэффициенты, слабая постановка Nitsche/DG, хевисайдово обогащение поверх
    непрерывной части. Не решено, не измерено. NB: F5 показал, что НЕ
    разрезанный элемент портится целиком (пол ~20% при любой ширине); что
    разрез это чинит — вывод по механизму, ПРЯМО НЕ ИЗМЕРЕН. Проверяется тем же
    проекционным стендом.

11. **ОБОСНОВАНИЕ LOD ПОКРЫВАЕТ ТОЛЬКО КАМЕРНЫЙ КАНАЛ.** Правило L = εR
    выведено из счёта мод связи «элемент → апертура» ((εD/λ)² = 1). Для света,
    приходящего на видимую поверхность ИЗ ДАЛЁКОЙ ОБЛАСТИ, релевантен канал
    «элемент → элемент», а он богаче: (kL)^(d−1). Физически непрямое освещение
    заведомо низкочастотное (на этом стоит всё кэширование освещённости в
    движках), так что проблемы быть не должно — но у нас это НЕ ДОКАЗАНО, а
    M9c меряет угловую разреженность именно на камерном канале. Не принимать
    доказанное для камеры за доказанное вообще.

12. **ЭКСПОНЕНЦИАЛЬНАЯ (НАПРАВЛЕННАЯ) ФОРМА НЕСУЩЕЙ — предпосылка M9c.**
    Пара (φ·cos, φ·sin) смешивает +k и −k: выбросить одно направление, оставив
    другое, нельзя — это связь на пару коэффициентов, а не удаление функции.
    В форме (φ·e^{+ikx}, φ·e^{−ikx}) пролёт тот же, но каждое направление —
    отдельная функция, и отбор направлений становится выразим. В 3D +k и −k это
    «свет к камере» и «от камеры», держать их в одной функции неправильно.
    Побочно упрощается сборка: вместо четырёх случаев cos/sin одно правило
    `∫φᵢφⱼ e^{i(kⱼ−kᵢ)x}`. Рефакторинг src/carrier.{c,h}, ложится на тот же
    код, что и разрезы.
    NB: вещественная арифметика (вопрос 5) этому НЕ противоречит — это выбор
    координат в одном и том же пространстве. Представление экспоненциальное для
    отбора, арифметика вещественная в решателе.

13. **ЗАКОН ТОЧНОСТИ ФАЗЫ — ИЗМЕРЕН 07-25** (result/M9b_REPORT.md §6).
    W_max·(δk/k)/λ = 0.3142 = 1/π, ПОСТОЯННО на пяти порядках δk/k. То есть
    ```
    W_max = λ / (π · δk/k)
    ```
    набег фазы поперёк одной ширины элемента ≲ 2 рад на пороге 10%.
    ПРОЕКТНЫЕ ЧИСЛА: элементы 1e3 λ требуют δk/k ≲ 3e-4; 1e6 λ — 3e-7. Через
    δx ≲ 1/(k2−k1) переводится в допуск на положение границы. Входной параметр
    для градиентных сред (насколько точен должен быть ВКБ-эйконал) и для
    формата сцены.
