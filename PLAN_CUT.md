# PLAN_CUT.md — общий слой: геометрия разреза среды

Начат 2026-07-27. **Это НЕ третий продукт.** Оба продукта (`PLAN.md` — Гельмгольц,
`PLAN_TRANSPORT.md` — перенос) режут ячейку границей материала, и геометрия этого
реза у них одна. Физика над ней — разная, и сращивать её нельзя.

**СОСТАВ ФАЙЛА.** Аудит ДИЗАЙНА — Г1…Г17, закрыт, не переигрывается. Ниже него —
ПЛАН РЕАЛИЗАЦИИ (Р-2…Р-6), аудит ЭТОГО ПЛАНА (Г18…Г33) и доклады по выполненным
пунктам 2 и 4. Г32 и Г33 заведены уже по результату прогона, а не до него, и это
отмечено у каждой.

## АУДИТ ДО КОДА — СЕМНАДЦАТЬ НАХОДОК (Г1…Г8 до обсуждения, Г9…Г17 по ходу)

Проведён по заведённому порядку, ни строки кода ещё не написано. Главный вывод:
**шов проходит УЖЕ, чем «отсечение + тетраэдры», и вешать его надо на ЯЧЕЙКУ, а не
на дерево.** Две находки — устаревший текст в `CLAUDE.md`, который спроектировал бы
ядро под перенос и выдал волновой линии непригодный интерфейс.

### Г1. Общий слой — выпуклый многогранник С ТОПОЛОГИЕЙ, а не тетраэдры

`src/cut2d.h` никогда не материализует полигон: полуплоскости превращаются прямо в
рёберные интегралы (теорема о дивергенции), и тетраэдризация волновой линии не
нужна. Переносу она нужна и естественна — подынтегральное там действительный
полином низкой степени. Значит общее:

    отсечение коробки набором полуплоскостей
    + ТОПОЛОГИЯ результата: вершины, рёбра, грани, несущая плоскость каждой грани,
      какая сторона какой материал

Дальше расходятся: **перенос тетраэдризует и берёт полиномы; волна обходит грани и
рёбра и берёт полином × экспоненту в замкнутой форме.** Один и тот же многогранник,
два разных интегратора.

### Г2. Stage 2 в CLAUDE.md — УСТАРЕВШИЙ ТЕКСТ, и он лежит ровно на этом пути

`CLAUDE.md` обещает «planar cuts via cube clipping + tetrahedra + **exact degree-6
cubature**». `src/cut2d.h:10-13` говорит обратное и с измерением: подынтегральное
осциллирует как `e^{i(k_i+k_j)·x}` на носителе `4W`, при `W = 1e5 λ` это `1e6`
периодов, и «Sampling is therefore not an option in the assembly (M14 audit, R3)».
Кубатура степени 6 по тетраэдрам — это и есть sampling.

Противоречия по существу нет: текст Stage 2 написан ДО директивы о несущем базисе
(07-25), когда подынтегральное было полином × полином. Несущая сделала его
осциллирующим, и кубатура умерла — но абзац никто не удалил. Тот же класс, о
котором `CLAUDE.md` предупреждает сам: «Everything λ-calibrated in PLAN.md … is v1:
measured, superseded — DO NOT carry those numbers over».

**Почему это находка, а не опечатка:** проектируя общее ядро по действующему тексту
`CLAUDE.md`, я бы выдал наружу тетраэдры и кубатуру, то есть интерфейс ЛИНИИ
ПЕРЕНОСА, а волновая получила бы то, чем пользоваться не может. Правка внесена в
`CLAUDE.md` до кода.

### Г3. PAYLOAD — НЕ ЗНАЧЕНИЯ В УГЛАХ, А ССЫЛКА НА ОБЩИЙ АНАЛИТИЧЕСКИЙ ПРИМИТИВ

**ПЕРЕПИСАНО 07-27 после возражения пользователя. Первая редакция Г3 была неверна**,
и разбор ошибки полезнее самого вывода.

Первая редакция читала `CLAUDE.md` и `PLAN_TRANSPORT.md` буквально: там сказано
«**нулевое множество** которой есть граница», то есть язык НЕЯВНОЙ ФУНКЦИИ —
скалярное поле в объёме, интерполянт по 8 углам, трилинейный, нулевое множество
кривое. Возражение: интерполируется не поле, а **ГРАНИЦА**, а граница — поверхность,
объект двумерный. Интерполянт поверхности по 4 значениям БИЛИНЕЕН, а не трилинеен.
Возражение верное, и формулировку в обоих планах надо править.

**Но билинейная не годится тоже, и по резкой причине.** Билинейный патч это
`a + bx + cy + d·xy`: в нём НЕТ ни `x²`, ни `y²`. Локальная форма сферы и линзы —
ровно `z ≈ −(x²+y²)/2R`, поэтому билинейный патч не воспроизводит их кривизну
НИКАК; он ловит только седловую составляющую. Для случаев, где кривизна и важна
(«для сфер и линз обязательна» — хвост №4), билинейная даёт то же, что плоскость.
А её единственный лишний член `d·xy` убивает замкнутую форму волновой линии: на
патче `z = f(x,y)` возникает `e^{i k_z d·xy}`, и `∫∫e^{iγxy}` по прямоугольнику
есть интеграл фреснелева типа — не элементарный и без ω-независимой цены.

**Куда ведёт возражение на самом деле.** Если интерполируется ГРАНИЦА, то для
плоскости лучший интерполянт — сама плоскость:

    в ячейке лежит ССЫЛКА на ОБЩИЙ аналитический примитив
    плоскость = нормаль + смещение, 4 double, ОДИН раз на поверхность

Соседние ячейки ссылаются на ТОТ ЖЕ объект, поэтому водонепроницаемость точна ПО
ПОСТРОЕНИЮ — рёберных пересечений, согласования и щелей не существует как темы.
Это снимает первую редакцию Г3 целиком: «независимая плоскость на ячейку даёт щели»
есть проблема только там, где плоскость на ячейку своя.

И это ровно то, что оба плана просят, а первая редакция прочитала мимо:
`PLAN_TRANSPORT.md` — «поверхность: **АНАЛИТИЧЕСКИЙ примитив**»; `CLAUDE.md` —
«a plane is three doubles, and specifying it to 1e-9 m costs exactly what 1 cm
costs… the sub-λ requirement bites only when the geometry itself is coarsened».
Ссылка на общий примитив геометрию не грубит ВОВСЕ, то есть запрет на LOD по
геометрии соблюдён буквально, а не «с точностью `O(h²)`», как у любой интерполяции
по углам.

| вход | представление | точность |
|---|---|---|
| плоскость, треугольная сетка, фасетированный примитив | ссылка на общий примитив | **точно**, замкнутая форма в обеих линиях |
| сфера, цилиндр, линза | аналитический примитив + фасеты | ИЗМЕРЕННЫЙ закон `0.125(kd)²√(d/W)`, `hz_cut2d_disc` |
| процедурная среда без примитива (шум, CSG-блендинг) | только здесь — интерполяция значений, и только в переносе | квадратура разрешена только там |

Третья строка помечена как ВОЗМОЖНОСТЬ, не план: закладываться на неё значит
проектировать общее ядро под интегратор, которого у волновой линии быть не может, —
то есть повторить Г2 в новой форме.

### Г4. В узле — ИНДЕКС примитива, и не inline

`hz_onode` сейчас 24 байта (`int32_t child0` + `double complex k2`). Границу несёт
малая доля узлов, а LOD обещает `1e7…1e8` элементов (`CLAUDE.md`: комната ≈ 2e8),
так что любое поле, добавленное в узел, платится за КАЖДЫЙ узел.

После Г3 платить почти нечем: в узле нужен `int32_t` индекс примитива (и признак
«с какой стороны материал»), а сама геометрия лежит один раз в таблице
поверхностей. Но даже 4 байта на все `2e8` узлов — это 0.8 ГБ, поэтому слово
«optional» из `CLAUDE.md` по-прежнему исполняется БОКОВОЙ ТАБЛИЦЕЙ (индекс ЯЧЕЙКИ →
запись о границе), а не полем структуры. Заодно это единственный вариант, не
ломающий формат файла.

**ПРАВКА 07-27 (Г20): здесь стояло «индекс УЗЛА».** Ключ обязан быть абстрактным
`int32` индексом ячейки, который назначает хозяин структуры: для `hz_octree` это
индекс узла (`hz_oct_leaf`), для `tr2_mesh` — индекс ячейки. Со словом «узел» в
ключе таблица была бы непригодна переносу.

### Г5. hz_oct_save/load — сырой дамп массива узлов БЕЗ ВЕРСИИ ФОРМАТА

`src/octree.c:208` — заголовок это `{log2size, n}`, дальше `fwrite` всего массива
`hz_onode`. Любое изменение структуры узла молча превращает старые файлы в мусор,
и прочитаются они как ВАЛИДНЫЕ: проверяется только диапазон `child0`. Это дыра уже
сейчас, просто не выстрелила, потому что формат ни разу не менялся.

И это код на НЕДОВЕРЕННОМ входе — `CLAUDE.md` прямо называет scene-file parsers
классом для `cverify.sh`. Значит: magic + версия вводятся ДО того, как к дереву
прибавится что бы то ни было, и на `hz_oct_load` идёт CBMC. Здесь он применим —
арифметика целочисленная, в отличие от `sweep.c`, где CBMC утонул в плавающей.

### Г6. Разрезы у двух продуктов РАЗНОЙ ФИЗИЧЕСКОЙ ПРИРОДЫ

`CLAUDE.md` перечисляет «разрезы» в общей машинерии целиком. Это слишком широко:

| | волновая линия | перенос |
|---|---|---|
| что такое разрез | место смены НЕСУЩЕЙ ФАЗОВОЙ ССЫЛКИ | ГРАНИЧНОЕ УСЛОВИЕ (уравнение рендеринга) |
| поле через границу | сшивается слабо, Нитше | НЕ сшивается: BRDF/Френель |
| степени свободы | по одной с каждой стороны | развёртка на диэлектрике ОБРЫВАЕТСЯ (К5) |

Общая у них ГЕОМЕТРИЯ разреза, а не разрез. Формулировка в `CLAUDE.md` сужена.

### Г7. Общее ядро вешается на ЯЧЕЙКУ, а не на дерево

T2 завёл свою сетку `tr2_mesh` — целочисленные прямоугольники плюс грани как
ПЕРЕСЕЧЕНИЯ рёбер; волновая линия живёт на `hz_octree`, где граней нет вовсе. Если
общий разрез принимает «узел дерева», перенос обязан переехать на `hz_octree`, а он
для этого не годится. Значит интерфейс берёт **ЯЧЕЙКУ КАК КОРОБКУ** — целочисленные
границы плюс ОРИЕНТИРОВАННЫЕ ПОЛУПРОСТРАНСТВА — и не знает ни про одно из двух
деревьев. Это ещё сужает шов и снимает переезд с обеих линий.

**ПРАВКА 07-27 (Г18).** До этой строки здесь стояло «целочисленные границы плюс
УГЛОВЫЕ ЗНАЧЕНИЯ»: Г7 писалась до обсуждения, а Г3 угловые значения отвергла
целиком. Реализованный по старой формулировке интерфейс выдал бы наружу вход
отвергнутого варианта — повторение Г2 на поколение позже.

### Г8. ФАЛЬСИФИКАТОРЫ И ПРЕДСКАЗАНИЯ — записаны ДО кода

**ТАБЛИЦА ПРАВЛЕНА 07-27 по Г22, Г23, Г24, Г30 — правки внесены ЗДЕСЬ, а не только
в находках, потому что списком при написании тестов пункта 4 пользоваться будут
именно отсюда.**

| # | что мерится | предсказание |
|---|---|---|
| 1 | `V_внутр + Σ_j V_внешн,j` = объём ячейки | пол округления, ≤1e-15 отн. **Тождество, само по себе не проверяет ничего — только вместе со свипом из Г22** |
| 2a | точки на рёбрах у соседних ячеек ОДНОГО уровня и у двух граней внутри одной ячейки | совпадают ПОБИТОВО (целая топология ребра + одна формула) |
| 2b | то же у соседей РАЗНЫХ уровней | **не проверяемо пунктом 4**: общего ребра нет по построению, это шов Г11.3/Г12 и он в пункте 5 (Г23) |
| 3 | Σ площадей фасетов по обе стороны общей грани | совпадают ≤1e-15 ⇒ поверхность водонепроницаема |
| 4 | вырождения: плоскость через угол / через ребро / по грани / вне ячейки | Σ объёмов верна и куска нулевого объёма НЕ порождается. **Число кусков — только для ВНУТРЕННЕГО**: у дополнения оно зависит от порядка полуплоскостей (Г30) |
| 5 | площадь фасета для плоскости под углом к сетке | замкнутая форма (сечение куба плоскостью) ≤1e-15 |
| 6 | перестановка полуплоскостей на входе | `V_внутр` и Σ объёмов совпадают ПОБИТОВО (Г30; дёшево и сильно, в первой редакции этого не было) |

**НЕГАТИВНЫЕ КОНТРОЛИ, каждый со своей метрикой:**

- **на чтение геометрии** (заменяет контроль «сбить угловое значение» из первой
  редакции — он предписывал трогать УГЛОВЫЕ ЗНАЧЕНИЯ, которых после Г3 не
  существует, то есть был неисполним): свип смещения `off` через ячейку ⇒
  `V_внутр` обязана монотонно пройти от `0` до `V_коробки`. Затем подменить чтение
  `off` константой ⇒ зависимость ОБЯЗАНА пропасть, а Σ объёмов при этом всё равно
  сойдётся — и вот тогда видно, что метрика №1 меряет бухгалтерию, а не геометрию
  (Г22);
- **на водонепроницаемость:** заменить рёберные пересечения подгонкой плоскости по
  МНК ⇒ щели ОБЯЗАНЫ появиться и Σ площадей разойтись. Если не разошлась — тест
  проверяет не то, ради чего заведён (это ровно ошибка К13 из T2: константу
  воспроизводит любой оператор, и печь на градуированной сетке ничего не поймала);
- **на побитовость (к №2a):** считать пересечение в ЛОКАЛЬНЫХ координатах ячейки
  (вычесть `lo`, отсечь, прибавить обратно) ⇒ последние биты обязаны разойтись.
  Если не разошлись — сравниваются числа, которые совпали бы и без канонического
  ребра (Г23);
- **на площадь (к №5):** сбить нормаль на 1% ⇒ площадь обязана сдвинуться на
  `O(1%)`. Если не сдвинулась — площадь считается по коробке, а плоскость не
  читается (Г24);
- **на анизотропию (к №3 и №5):** посчитать площадь по вершинам в ЕДИНИЦАХ и по
  вершинам в МИРЕ ⇒ на кубической ячейке разница нулевая, на ячейке `1×1×8`
  обязана быть `O(1)`. Если и там ноль — площадь берётся не из вершин (Г21);
- **на вырождения:** плоскость точно через угол ⇒ проверить, что число кусков
  меняется скачком в ОБЕ стороны от вырождения, а не залипает.

Первая подпись артефакта здесь особенно вероятна: почти все эти числа
мешнезависимы по построению, а значит «не изменилось» ничего не доказывает, пока не
показано, что параметр вообще действует.

## ДОПОЛНЕНИЕ 07-27 ПО ХОДУ ОБСУЖДЕНИЯ — Г9…Г11

### Г9. У ПЛОСКОСТЕЙ ДВА ИСТОЧНИКА, И ЗАКОННОСТЬ ИХ РАЗНАЯ

Г3 покрывала только первый. Второй — постановка пользователя: **свойства задаются
октодеревом ОБЪЁМНО, а граница на произвольном уровне выводится из них ПЛОСКОСТЯМИ,
а не сочленением кубиков.** Это «иерархическая полигональная модель с автоматическим
LOD», и механизм известен: адаптивное dual contouring (Ju/Losasso/Schaefer/Warren
2002) — на узел одна вершина, минимизирующая квадратичную ошибку по фрагментам
границы внутри узла. Крупный узел получает НАИЛУЧШУЮ плоскость для мелкой детали
внутри себя, и это и есть автоматический LOD.

| источник плоскости | перенос | волновая линия |
|---|---|---|
| задана в сцене (примитив, треугольник) | точно | точно, на ЛЮБОМ уровне |
| выведена из объёмных данных (QEF) | законно и центрально (три режима по `εR`) | ГРУБЛЕНИЕ геометрии |

`CLAUDE.md` про второй случай в волновой линии говорит буквально: «the sub-λ
requirement… bites only when the geometry itself is coarsened: voxelised boundaries,
or **classic mesh-simplification LOD**. The medium octree must keep exact boundaries
EVERYWHERE, including far away». Автоматический LOD по границе — это он и есть.
Но это НЕ запрет, а СЧЁТ: закон ошибки уже измерен (M14 [3]),
`0.125(kd)²√(d/W)` при смещении `d`, и трюк со средней сагиттой в
`hz_cut2d_disc` делает смещение среднезначно нулевым — «дешёвая половина сделки».
Значит вопрос «до какого уровня подгонка законна при данном λ» ВЫЧИСЛИМ.

**Общее ядро от источника не зависит** — оно отсекает коробку набором плоскостей и
отдаёт топологию. Откуда плоскости пришли, ему знать не нужно.

### Г10. ОДНА ПЛОСКОСТЬ НА УЗЕЛ ⇒ ЩЕЛИ, И ЩЕЛЬ ХУЖЕ ЛЕСЕНКИ

Если каждый узел подгоняет плоскость независимо, на смене уровня плоскости соседей
на общей грани не совпадут. **И это артефакт заметнее исходного:** лесенка
светонепроницаема и читается как фактура, а щель — дырка, через которую течёт свет,
и на картинке она даёт яркую строчку по стыку уровней. То есть цель «будет красиво»
провалилась бы ровно тем способом, которым её пытались достичь.

Водонепроницаемость требует, чтобы точки на рёбрах были ОБЩИМИ, а не подогнанными.
Тогда на узел приходится не одна плоскость, а НЕСКОЛЬКО ПЛОСКИХ ФАСЕТОВ — веер
вокруг его вершины. Это не осложнение: `hz_cut2d_poly` уже принимает список
полуплоскостей (до `HZ_CUT_MAXHP` = 96), и в его комментарии прямо сказано «a
faceted material boundary is exactly this». Первая редакция Г3 зря сводила узел к
одной плоскости — существующая машинерия ждёт именно набора.

**Негативный контроль:** независимая подгонка на узел ⇒ Σ площадей фасетов по обе
стороны общей грани ОБЯЗАНА разойтись, и разойтись тем сильнее, чем больше перепад
уровней. Если не разошлась — тест не проверяет водонепроницаемость (это ровно ошибка
К13 из T2: константу воспроизводит любой оператор передачи).

### Г11. ИЕРАРХИЯ СЧИТАЕТСЯ ДО РЕНДЕРИНГА И ХРАНИТСЯ В ДЕРЕВЕ

Постановка пользователя: LOD границы считается ЗАРАНЕЕ, структура позволяет его
записать; в узле любого уровня лежат его фасеты. На кадре — выбрать уровень и
прочитать, никакой подгонки в реальном времени. Следствия:

1. **Стоимость уходит из кадрового бюджета целиком.** Это тот же приём, что уже
   записан в `CLAUDE.md`: «Coarse levels form a persistent, scene-anchored core
   computed once». Граница просто становится ещё одним его слоем.
2. **Законность для волновой линии становится ХРАНИМЫМ ЧИСЛОМ.** Раз подгонка
   офлайновая, для каждого узла можно посчитать смещение `d` и по измеренному закону
   `0.125(kd)²√(d/W)` записать, при каких λ этот уровень законен. Волновая линия
   тогда не решает ничего на кадре — она читает пометку. Это снимает половину
   расхождения из Г9.
3. **НО ШОВ ОСТАЁТСЯ, и предвычисление его не лечит.** LOD по построению держит
   активное множество на МНОГИХ уровнях одновременно (`CLAUDE.md`: «per octave of
   distance the count is independent of distance»), поэтому даже если каждый уровень
   ПО ОТДЕЛЬНОСТИ водонепроницаем, стык крупного узла с мелким на кадре — это стык
   двух РАЗНЫХ уровней. Классическая задача сшивки LOD.
   Решение, согласованное с тем, что уже построено в T2: там грань есть ПЕРЕСЕЧЕНИЕ
   рёбер соседей, висячего узла как особого случая нет. Так же и здесь — граница
   фасета на общей грани берётся с БОЛЕЕ МЕЛКОЙ стороны, и крупный узел просто
   получает на этой грани больше рёбер фасета. Водонепроницаемо без шаблонов
   перехода (Transvoxel-подобных) и тоже предвычислимо.
4. **Возможное следствие, которое надо решить измерением:** шаблонная сшивка
   потребовала бы 2:1-сбалансированного дерева, тогда как развёртка T2 сознательно
   сделана на НЕсбалансированном (перепад 4:1 законен и для граней тяжелее). Вариант
   из пункта 3 балансировки не требует — это довод в его пользу, но проверить надо
   кодом, а не рассуждением.

### Г12. DUAL CONTOURING — ЧТО ХРАНИТСЯ, И ПОЧЕМУ ЭТО НЕ «ПОЛИГОНАЛЬНОЕ ДЕРЕВО»

**Заменяет вариант сшивки из Г11.3** (брать границу фасета с более мелкой стороны):
тот работоспособен, но крак-фри на произвольном перепаде уровней — заглавное
свойство адаптивного DC, ради которого он и придуман в отличие от marching cubes.

    в узле:      ОДНА вершина (минимум QEF) + нормаль + невязка ≈ 4…7 double
    полигоны:    НЕ ХРАНЯТСЯ — порождаются обходом cellProc → faceProc → edgeProc
                 до МИНИМАЛЬНЫХ рёбер; каждое даёт один полигон, соединяющий
                 вершины делящих его узлов ЛЮБОГО уровня

Отсюда: ни балансировки 2:1, ни шаблонов перехода (Г11.4 снимается). Дорогая часть —
подгонка QEF по мелким данным — офлайновая и НЕ ЗАВИСИТ от того, какой срез дерева
выберет камера; на кадре остаётся обход ценой `O(размера видимой поверхности)`, то
есть ровно столько, сколько выданных полигонов.

DC нужны ЭРМИТОВЫ данные: не «внутри/снаружи», а точка на ребре ПЛЮС НОРМАЛЬ. Это
больше, чем занятость вокселя, но именно нормаль даёт точные резкие рёбра — стык
стены с полом остаётся острым на любом уровне, marching cubes его скругляет.

ОГОВОРКИ К ИЗМЕРЕНИЮ, НЕ К ОБСУЖДЕНИЮ (адрес у обеих один, см. Г14):
1. вершина QEF может уехать ЗА пределы своего узла. Для картинки косметика, для
   ядра разреза — нет: вся конструкция стоит на «поверхность внутри этой коробки».
   Штатное лечение — зажим в узел, ценой точности;
2. DC умеет давать НЕМАНИФОЛДНЫЕ конфигурации (есть Manifold DC). Для картинки
   терпимо, для «какая сторона какой материал» в развёртке — нет.

### Г13. РАЗРУШАЕМОСТЬ — ДОВОД ЗА ЭТОТ ВЫБОР, А НЕ ПРОТИВ

Полигоны в дереве не хранятся, хранится объёмное — то есть представление остаётся
около-воксельным. Поэтому правка локальна (пересчитать вершины QEF в затронутых
узлах), а связность чинить не надо: её нет как хранимой структуры, она возникает из
обхода. Этим семейством и сделаны разрушаемые воксельные движки.

**QEF АДДИТИВНА** — матрица родителя есть сумма матриц детей, на этом построено
восходящее упрощение у Ju et al. Значит после правки листа офлайновая иерархия (Г11)
чинится сложением по цепочке предков, `O(глубины)`, без перефитинга. Предвычисление
переживает разрушаемость.

ТРЕБОВАНИЕ СО ШВОВ ПЕРЕЕЗЖАЕТ В ПРОВЕРЯЕМОЕ МЕСТО. Швы перестают быть предметом
авторинга (их нет как объекта), но взамен:

    было:  «сделать швы правильными»                    — ручное, автоматом не проверить
    стало: входное тело ЗАМКНУТО и ОРИЕНТИРУЕМО,
           на рёбрах записаны ИСТИННЫЕ нормали          — гейт

И то же на разрушении: **скол обязан писать НОРМАЛИ, а не только занятость**, иначе
свежая поверхность излома выйдет скруглённой, «мыльной», хотя должна быть острой.

### Г14. ТОНКОЕ ТЕЛО НЕ ВЛЕЗАЕТ В ЗНАКОВОЕ ПОЛЕ ПО МАСШТАБУ

**ПЕРЕПИСАНО: первая редакция утверждала, что травинка — открытый лист без
«внутри», и что дело в РОДЕ объекта. Пользователь поправил: внутренность есть, это
замкнутая поверхность из десятка-другого полигонов.** Вывод выживает, но причина
другая и она СИЛЬНЕЕ, потому что не зависит от топологии.

Дело в масштабе. Размер элемента задаётся пикселем, `L = εR`; при `ε ≈ 1e-3` уже на
одном метре пиксель равен 1 мм, а травинка 0.2 мм толщиной. **Толщина ниже размера
элемента на ЛЮБОМ расстоянии**, а не «на далёком». Значит знаковое поле её не
понесёт никогда, и то же верно для всех тонких тел: листва, ткань, бумага, оконное
стекло, жесть. Тонкие воксели тут не некрасивы, а РЕСУРСОЁМКИ — и считают то, чего
никто не видит.

| | пиксель | вдоль (10 см) | ширина (5 мм) | толщина (0.2 мм) |
|---|---|---|---|---|
| 1 м | 1 мм | 100 px | 5 px | **0.2 px** |
| 5 м | 5 мм | 20 px | **1 px** | — |
| 30 м | 3 см | 3 px | — | — |
| 100 м | 10 см | **1 px** | — | — |

**ВНУТРЕННОСТЬ НУЖНА РОВНО ОДНИМ ЧИСЛОМ.** Всё, что от неё требуется, — оптическая
толщина хорды `τ = σ·(длина хорды)`, а хорда берётся из двух ограничивающих
поверхностей аналитически, `O(1)` на пересечение. Объёмных элементов внутри тонкого
тела не нужно ни на одном уровне.

**И это АДРЕС обеих оговорок Г12:** ровно на тонком теле вершина QEF уезжает из узла
и лезут неманифолдные конфигурации. Оговорки не абстрактные.

### Г15. ТОНКОЕ ВЫРОВНЕННОЕ ТЕЛО — ДРУГОЙ СЛУЧАЙ, И ЕГО СПАСАЮТ АНИЗОТРОПНЫЕ ЯЧЕЙКИ

    тонкое ВЫРОВНЕННОЕ с сеткой (стекло 4 мм, стена, пол, жесть)
        → анизотропная ячейка 4 мм × 1 м × 1 м, платим по ОДНОЙ оси;
          знаковое поле годится
    тонкое ПРОИЗВОЛЬНО ОРИЕНТИРОВАННОЕ (травинка, лист)
        → примитив (поверхности) + аналитическая хорда; знаковое поле не годится
    МНОГО тонких тел подпиксельно
        → среда с направленным σ_t

Анизотропные ячейки уже есть (`tr2_mesh_aniso`, проверены в T2 на печи и балансе).
Случайно ориентированные травинки ими не спасти — их не выровнять с осепараллельным
деревом.

### Г16. РЕЖИМ ВЫБИРАЕТСЯ ПО КАЖДОЙ ОСИ, А НЕ ПО ОБЪЕКТУ

Таблица трёх режимов в `PLAN_TRANSPORT.md` выбирает режим НА ОБЪЕКТ («трава ближе
3 м — настоящая геометрия»). Но на 3 м травинка это 33 пикселя вдоль и 1/15 пикселя
в толщину — она подпиксельна и надпиксельна ОДНОВРЕМЕННО, по разным осям. Значит
режим надо выбирать по каждой оси отдельно — а это собственное правило плана
«РАЗМЕР ЭЛЕМЕНТА — ПО КАЖДОЙ ОСИ ОТДЕЛЬНО», просто записанное для сетки, а не для
выбора режима. Направленное `σ_t = G(ω)·u_L` — прямое следствие: анизотропия берётся
из распределения ориентаций.

Итоговое представление травинки — три вида, переключение ПО ПРИЗНАКУ, не по объекту:

| диапазон | что ещё надпиксельно | представление | роль дерева |
|---|---|---|---|
| `R < ~5 м` | длина и ширина | треугольники ПО ССЫЛКЕ (те же ~15 полигонов) | пространственный индекс: список ссылок в листе |
| `~5…30 м` | пучки и просветы (1–3 см) | среда `σ_t(x,ω) = G(ω)·u_L(x)` | носитель ОБЪЁМНЫХ свойств |
| `R > ~30 м` | ничего | поверхность + эффективная BRDF (1D-задача Р5) | сворачивается: плоскость почвы + BRDF |

**СЛЕДСТВИЕ ДЛЯ PAYLOAD: трёх видов носителя границы, и все три вынужденные.**
Треугольник — не частный случай плоскости: у него есть КРАЙ, а у плоскости нет.

| | ссылка на примитив | эрмитово дерево + DC | объёмная среда |
|---|---|---|---|
| тонкий открытый лист | **да** | **невозможно по роду** | да (как среда) |
| резкие углы | точно | точно при нормалях | н/п |
| разрушаемость | нет | **да** | да |
| волновая линия | точно на любом уровне | со счётом ошибки | н/п |

### Г17. ИНТЕРПОЛЯЦИЯ СВОЙСТВ — ДРУГОЙ ВОПРОС, НО В ПЕРЕНОСЕ НЕ ТРЕБОВАНИЕ

**ПОНИЖЕНО 07-27 в том же обсуждении, где и заведено.** Первая редакция называла
линейные свойства требованием на основании «на метровой ячейке кусочно-постоянная
`σ_t` даёт скачки в масштабе сетки». Возражение пользователя: интерполяция свойств
внутри вокселя не обязательна, потому что видимую неоднородность дают ТЕНИ. Возражение
сильнее исходного довода, и вот почему:

1. **Для линейной `σ` внутри ячейки `∫σ ds = σ̄·h` ТОЧНО.** То есть
   кусочно-постоянная `σ`, взятая как СРЕДНЕЕ по ячейке, воспроизводит оптическую
   толщину линейной `σ` без ошибки; балансное уравнение при `v = 1` — то, которое
   несёт энергию — совпадает побитово. Расходятся только наклонные моменты `∫σLξ`.
2. **Точность схемы всё равно DG1** (в T2 измерено 2.90 по функционалу). Уточнять
   представление свойств выше порядка поля — то же, что брать квадратуру высокого
   порядка для схемы второго: порядка это не даёт.
3. **Видимая неоднородность живёт в ОСВЕЩЁННОСТИ, а не в свойствах**, а поле DG1
   уже несёт её линейно внутри ячейки плюс разрез на кромках тени (Р6: прямая
   составляющая — мелкая сетка с разрезами, косвенная — грубая).

**ЧТО ИЗ ТРЕБОВАНИЯ ВЫЖИВАЕТ:** постоянная обязана быть СРЕДНИМ (или подогнанным
эффективным), а не произвольной выборкой. Значение в углу или в одном узле — первый
порядок, и вот оно бы вылезло. А «среднее» для полога — не наивная средняя плотность
(`⟨e^{−σL}⟩ ≠ e^{−⟨σ⟩L}`), а подогнанное эффективное со сгущением. Значит нужная
машинерия — **ГОМОГЕНИЗАЦИЯ, а не интерполяция**, и она уже решена офлайновой
подгонкой под функционал мелкой модели (`PLAN_TRANSPORT.md`). Одно вместо двух.

**ФАЛЬСИФИКАТОР, который закроет или откроет вопрос:** решить участок с пятнистой
плотностью при постоянной-средней `σ` и при линейной, и сравнить разницу с ошибкой,
ожидаемой от порядка DG1. Если разница НИЖЕ ошибки схемы — интерполяция свойств в
переносе есть мёртвый вес, и `lean.sh` тут не поможет, потому что код бы работал.

**В ВОЛНОВОЙ ЛИНИИ ЛИНЕЙНОЕ `k` ОСТАЁТСЯ, И ПО ДРУГОЙ ПРИЧИНЕ** — структурной, не
по точности: при градиентной среде несущая становится фазой ВКБ, `S' = k(x)`, и
`CLAUDE.md` отмечает, что член `k²` при этом сокращается ТОЧНО, оставляя один член
`∝ k'`. Там линейное `k` — условие существования несущей, а не уточнение сетки.
Требование ПО-ПРОДУКТОВОЕ, как и разрез в Г6.

Ниже — исходный разбор, он остаётся верным в части «это другой вопрос, чем граница».

Всё выше (Г3, Г9, Г14) — про интерполяцию **ГРАНИЦЫ**, и там ответ отрицательный.
Интерполяция **СВОЙСТВ** внутри ячейки (`σ_t`, `σ_s`, `k²`) не наследует ни одного
из возражений:

| возражение к интерполяции границы | к интерполяции свойств |
|---|---|
| щели между ячейками (Г10) | не бывает: свойства не образуют поверхность |
| грубление геометрии, запрет LOD (Г9) | не касается: запрет про ГРАНИЦУ, не про `k` |
| ломает замкнутую форму (Г3, `e^{iγxy}`) | не ломает: линейные свойства оставляют всё полиномиальным |

ИСХОДНЫЙ ДОВОД, ПОНИЖЕННЫЙ (оставлен, чтобы не переигрывать заново): в полосе травы
5–30 м ячейка метровая (поле по горизонтали гладкое, Р4/Р6), и кусочно-постоянная
`σ_t` вносит скачки в масштабе СЕТКИ, а не сцены. Довод оказался слабым: скачок
появляется лишь там, где наблюдаемое чувствительно именно к `σ_t`, а средняя по
ячейке постоянная даёт ТОЧНУЮ оптическую толщину линейной `σ` (см. выше, п. 1).

Требование стояло в `PLAN_TRANSPORT.md` («линейно интерполируемые по узлу»); в T2
оно сознательно отложено (там `σ_t`, `σ_s` константны в ячейке, `ε` уже DG1).
Стоимость: произведение `σ_t·L` двух линейных даёт квадратику, `∫σ_t L v` требует
точных моментов до третьей степени — замкнутая форма, без квадратуры, просто больше
слагаемых. В волновой линии это же — переход несущей к фазе ВКБ, `S' = k(x)`, и
`CLAUDE.md` уже отмечает, что член `k²` при этом всё равно сокращается точно.

**НО НЕПРЕРЫВНОСТЬ И РАЗРЫВ ДОЛЖНЫ БЫТЬ РАЗВЕДЕНЫ, И ЭТО ТО ЖЕ ПРАВИЛО, ЧТО У
НЕСУЩЕЙ.** Свойства обязаны быть НЕПРЕРЫВНЫ внутри материальной области (иначе
скачки на гранях остаются, просто меньшие) и РАЗРЫВНЫ на границе материала (стена
против воздуха — там скачок физический). Реализуется тем же приёмом, что и всё
остальное: значения берутся в УГЛАХ, общих для соседей внутри области, а разрыв
несёт РАЗРЕЗ. Дословно то же, что в волновой линии про несущую: «THE CARRIER IS A
SHARED PHASE REFERENCE… THE CUT IS WHERE THE REFERENCE CHANGES». Одномерное ядро T1
это уже умеет (`sig_lo`/`sig_hi` раздельно на ячейку), двумерное T2 — ещё нет.

## ПОРЯДОК РАБОТ

1. Правки `CLAUDE.md` по Г2 и Г6 (устаревший Stage 2, сужение «разрезов»). **Сделано.**
2. `magic + версия` в формате дерева и CBMC на `hz_oct_load` (Г5) — до любых
   изменений структуры. Дыра существует независимо от разреза. **Сделано 07-27,
   доклад в конце файла.** По ходу поправлены Г27 (механизм зависания назван
   неверно) и заведена Г32 (циклы ловит достижимость, а не `c > i`).
3. Таблица поверхностей + таблица фасетов + боковая таблица **(сделано 07-27,
   доклад в конце файла)**
   «**ЯЧЕЙКА (абстрактный `int32`) → НАБОР ФАСЕТОВ, сторона**» (Г3, Г4, Г10).
   **Ключ ИМЕННО ячейка, а не узел дерева (Г20): формулировка «узел → …» стояла
   здесь до 07-27 и отдавала наружу устройство волновой линии — перенос живёт на
   `tr2_mesh`.** Два источника плоскостей (Г9), пометка законности уровня для
   волновой линии (Г11.2, поле `dmax`, до пункта 5 хранит «не вычислено»).
4. Общее ядро: отсечение коробки набором плоскостей → топология многогранника
   **Сделано 07-27, доклад в конце файла; по ходу заведена Г33 (механизм Г31).**
   (Г1, Г7), с фальсификаторами Г8 в их ПРАВЛЕНОЙ редакции (Г21…Г24, Г30).
   **ПУНКТЫ 4 И 3 ПЕРЕСТАВЛЕНЫ 07-27: ядро пишется ПЕРВЫМ.** Довод: таблицы
   пункта 3 — хранилище для того, что производит пункт 5 и потребляет пункт 4, и
   пока нет ни производителя, ни потребителя, проверить их можно только на
   согласованность с самими собой. Ядро же проверяемо в одиночку — полуплоскости
   передаются массивом, таблица ему не нужна, — и вся геометрическая опасность
   (вырождения, водонепроницаемость, побитовость) сидит именно в нём. Жёсткой
   зависимости нет: `hz_hspace` это четыре double и живёт в Р-4. Побочно это
   делает применимой Г31, единственную со статусом «ждёт».
5. Предвычисление иерархии границы (Г11), сшивка обходом DC (Г12; вариант Г11.3 —
   запасной). Здесь же — три вида носителя
   границы (Г14…Г16) и замер, какой из них несёт основную массу в сцене T7.
   ГОМОГЕНИЗАЦИЯ вместо интерполяции свойств (Г17): подгонка эффективной
   постоянной офлайн, а не линейная σ по узлу.
   Балансировка 2:1 (Г11.4) при обходе DC не требуется — проверить кодом.
6. Потребители: перенос тетраэдризует (открывает T4), волна обходит грани и рёбра
   (открывает хвост №5 — 3D).

Шаги 2 и 3 в волновой линии — ДОБАВЛЕНИЕ, не переделка: трёхмерного разреза нет ни
в одной линии, а `cut2d`/`nitsche2d` двумерные и остаются нетронутыми. Значительная
переделка возникла бы только при шве по ИНТЕГРИРОВАНИЮ, и его здесь нет.

## ПЛАН РЕАЛИЗАЦИИ (внесён 07-27; аудит ДИЗАЙНА Г1…Г17 не переигрывается)

Здесь — КАК писать, а не ЧТО выбирать. Выбор сделан выше и считается закрытым.
Пункты 2, 3, 4 порядка работ детально; 5 и 6 эскизно.

**ГДЕ ЛЕЖИТ ОБЩИЙ СЛОЙ.** Новый каталог `src/cut/` — ни волновой, ни переносный.
Правило `CLAUDE.md` «`src/` кроме `src/transport/` — волновая линия» его бы забрало
в волновую, поэтому правило требует поправки (и заодно `src/octree.*`, которое
`CLAUDE.md` уже называет общей машинерией, а расположение говорит обратное).

    src/octree.c/.h      ОБЩЕЕ (правка формата — пункт 2)
    src/cut/surf.c/.h    таблица поверхностей + фасетов + боковая таблица (пункт 3)
    src/cut/poly3.c/.h   ядро отсечения коробки → топология многогранника (пункт 4)
    tests/test_octfmt.c  фальсификаторы формата
    tests/test_poly3.c   фальсификаторы Г8
    tests/cbmc_octree.c  оснастка CBMC (не в `make test`)

### Р-2. ФОРМАТ ФАЙЛА ДЕРЕВА: MAGIC, ВЕРСИЯ, ПРОВЕРКА ТОПОЛОГИИ (Г5)

**Что сейчас** (`src/octree.c:208`): `{log2size, n}` + `fwrite` массива `hz_onode`.
Три дыры, а не одна: (a) нет magic и версии — Г5; (b) сырой дамп структуры пишет
БАЙТЫ ВЫРАВНИВАНИЯ, а они неинициализированы (`hz_onode` = `int32` + 4 байта
набивки + `double complex`; узлы из `realloc` набивку не трогают) — файл
невоспроизводим, а `valgrind` обязан ругаться на запись неинициализированного;
(c) проверка `child0 + 8 > n` не запрещает ни ЦИКЛ, ни ДВУХ РОДИТЕЛЕЙ, ни глубину
больше `log2size`, а `hz_oct_at` на цикле `child0 == i` зависает навсегда.

**Формат v1** — заголовок 40 байт, дальше три РАЗДЕЛЬНЫХ массива (SoA). Разделение
убирает набивку из файла целиком и отвязывает файл от layout структуры, поэтому
боковые таблицы пункта 3 не потребуют смены версии узла.

    off  0  char   magic[8]   "HZOCTRE\0"
    off  8  uint32 version    1
    off 12  uint32 flags      0; НЕИЗВЕСТНЫЙ БИТ ⇒ ОТКАЗ (fail closed)
    off 16  uint32 endian     0x04030201 — свидетель порядка байт целых
    off 20  int32  log2size
    off 24  int32  n
    off 28  int32  reserved   0
    off 32  double canary     1.0 — свидетель IEEE754 и порядка байт плавающих
    ---     int32  child0[n]
    ---     double k2re[n]
    ---     double k2im[n]

Версии без размера записи мало (смена `double`→`float` версию не меняет), поэтому
свидетелей два и оба проверяются на равенство точному значению, а не на диапазон.
Чтение массивов — КУСКАМИ по `HZ_OCT_IOCHUNK = 4096` элементов (32 КиБ, попадает в
L2; временной памяти сверх арены не берём — при `n = 2^26` три полных буфера стоили
бы 1.3 ГБ поверх 1.5 ГБ арены).

**РАЗДЕЛЕНИЕ ВВОДА-ВЫВОДА И ПРОВЕРКИ.** CBMC не умеет `FILE*`, поэтому «CBMC на
`hz_oct_load`» буквально невыполним. Загрузка режется на три части, и формальный
слой идёт на среднюю и на ту, что решает про аллокацию:

    hz_oct_hdr_decode(const uint8_t h[40], hz_octree *t)   чистая, целочисленная
    hz_oct_load(...)                                        оболочка ввода-вывода
    hz_oct_validate(const hz_octree *t)                     чистая, целочисленная

**ИНВАРИАНТЫ `hz_oct_validate` — ОДИН ПРОХОД ВПЕРЁД, БЕЗ РЕКУРСИИ.** Ключ в том,
что `node_alloc8` выдаёт блоки по 8 начиная с индекса 1 и СТРОГО ВПЕРЁД, поэтому
«ребёнок с индексом больше родителя» — не эвристика, а свойство конструктора.

**ПОПРАВКА ПО ЗАМЕРУ (07-27): «и оно ОДНО даёт ацикличность» — так было написано
здесь, и это неверно, см. Г32.** Ацикличность даёт ДОСТИЖИМОСТЬ, а `c > i`
оказалась ей подчинена. Проверка остаётся (она даёт точный код ошибки и стоит одно
сравнение), но приписывать ей работу нельзя.

    n ≥ 1, n ≤ HZ_OCT_MAX_NODES, (n − 1) % 8 == 0, log2size ∈ [0, 20]
    depth[] : uint8, 0xFF = «не достигнут», depth[0] = 0
    для i = 0..n−1 по возрастанию:
      depth[i] == 0xFF                 ⇒ ОТКАЗ  (недостижимый узел = мусор в файле)
      c = child0[i]; c == −1           ⇒ лист, дальше
      c < 0                            ⇒ ОТКАЗ  (лист помечается ТОЛЬКО −1)
      c ≤ i                            ⇒ ОТКАЗ  (цикл; здесь и только здесь)
      c > n − 8                        ⇒ ОТКАЗ  (блок не влезает)
      (c − 1) % 8 != 0                 ⇒ ОТКАЗ  (не начало блока)
      depth[i] + 1 > log2size          ⇒ ОТКАЗ  (у узла размера 1 детей быть не может)
      для j = 0..7: depth[c+j] != 0xFF ⇒ ОТКАЗ  (второй родитель)
                    depth[c+j] = depth[i] + 1

Дети имеют индекс больше родителя, поэтому к моменту прихода в `i` все возможные
родители (< i) уже обработаны — проверка достижимости корректна в один проход.
Возврат — код инварианта (`enum hz_oct_bad`), а не «1»: тест обязан показывать, что
файл отвергнут ТЕМ инвариантом, который в нём испорчен, иначе он проверяет только
«что-нибудь сломалось». Черновик `depth[]` — `calloc` ВСТРОЕННЫЙ по месту
(`CLAUDE.md`, ложные срабатывания gcc-analyzer на обёртках), `n` байт, освобождается
там же.

**ЧТО ЭТИМ ДОКАЗЫВАЕТСЯ.** `hz_oct_at` крутит `size /= 2` без нижнего упора;
инвариант «глубина ≤ log2size» — ровно то, что нужно его циклу, чтобы завершиться.
То есть проверка не «на всякий случай», а под конкретный существующий цикл.

**CBMC.** `tests/cbmc_octree.c`, точка входа `harness`: недетерминированные
`n ≤ 12`, `log2size ≤ 3`, все `child0` недетерминированы; после `hz_oct_validate()`
== 0 утверждается каждый инвариант заново И прогоняется настоящий `hz_oct_at` на
недетерминированных координатах (то есть проверяется НЕ формула, а потребитель).
Запуск: `scripts/cverify.sh tests/cbmc_octree.c --function harness --unwind 20
--unwinding-assertions src/octree.c`.

**ФАЛЬСИФИКАТОРЫ ФОРМАТА** (`tests/test_octfmt.c`), предсказание — до прогона:

| # | вход | предсказание |
|---|---|---|
| 1 | сохранить → загрузить → сохранить | байт в байт |
| 2 | файл СТАРОГО формата (без magic) | ОТКАЗ, код «magic» |
| 3 | версия 2 | ОТКАЗ, код «version» |
| 4 | `flags` = 1 | ОТКАЗ, код «flags» |
| 5 | обрезанный на байт файл | ОТКАЗ, код «short read» |
| 6 | `child0[k] = k` (цикл) | ОТКАЗ, код «cycle»; БЕЗ него — зависание `hz_oct_at` |
| 7 | `child0[k] = c+1` (не начало блока) | ОТКАЗ, код «align» |
| 8 | два узла ссылаются на один блок | ОТКАЗ, код «two parents» |
| 9 | цепочка глубже `log2size` | ОТКАЗ, код «depth» |
| 10 | `canary` = 2.0 | ОТКАЗ, код «canary» |

**НЕГАТИВНЫЙ КОНТРОЛЬ, каждый со своей метрикой:**

- **что тест не отвергает ВСЁ:** правильный файл обязан ЗАГРУЗИТЬСЯ и совпасть с
  оригиналом поячеечно (`hz_oct_at` по всей сетке). Без этого таблица выше
  проходится реализацией `return 1;`;
- **на набивку:** одно и то же дерево строится ДВАЖДЫ в одном процессе (второе — на
  другом адресе кучи) и сохраняется в два файла ⇒ файлы обязаны совпасть побитово.
  **Честная оговорка: эта метрика слабая** — свежие страницы от `mmap` нулевые, и
  сырой дамп может пройти её по случайности. ГЕЙТОМ здесь работает `valgrind`
  («uninitialised byte(s)» на `fwrite`), а побитовое сравнение — подтверждение;
- **что проверка вообще действует:** каждый испорченный файл отличается от годного
  РОВНО ОДНИМ полем, и годный при этом грузится. Иначе «отвергнуто» ничего не
  говорит о том, ЧТО отвергнуто.

### Р-3. ТАБЛИЦА ПОВЕРХНОСТЕЙ И БОКОВАЯ ТАБЛИЦА (Г3, Г4, Г9, Г10, Г11.2)

Три таблицы, а не одна, потому что у них разные времена жизни и разные владельцы.

**1. Поверхности — АНАЛИТИЧЕСКИЕ ПРИМИТИВЫ, источник истины, LOD не подлежат.**

```c
typedef enum { HZ_SURF_PLANE = 0, HZ_SURF_SPHERE = 1, HZ_SURF_CYL = 2 } hz_surf_kind;
typedef struct {
  int32_t kind;
  double  p[7];      /* PLANE: n[3], off | SPHERE: c[3], r | CYL: a[3], d[3], r */
  int32_t tag_le;    /* НЕПРОЗРАЧНАЯ метка стороны {n·x ≤ off} (внутри примитива) */
  int32_t tag_ge;    /* то же для {n·x ≥ off} */
} hz_surf;
```

`tag_*` — `int32`, и общий слой в него НЕ СМОТРИТ: волновая линия положит туда
индекс своего `k²`, перенос — индекс своих `σ`. Это то место, где иначе просочилась
бы физика одной линии (Г2/Г6).

**2. Фасеты — ПЛОСКОСТИ, которыми режут.** Кривой примитив фасетируется (Г3, третья
строка таблицы); заданная плоскость есть свой собственный единственный фасет.

```c
typedef struct {
  double  n[3], off;  /* полупространство n·x ≤ off, В ЕДИНИЧНЫХ КООРДИНАТАХ дерева */
  int32_t surf;       /* примитив-родитель; −1 = плоскость задана прямо */
  double  dmax;       /* макс |смещение| фасет↔примитив по его области, МЕТРЫ;
                         < 0 = НЕ ВЫЧИСЛЕНО (Г11.2 — заполняется пунктом 5) */
} hz_facet;
```

`dmax` — та самая «пометка законности уровня» из Г11.2: волновая линия по
измеренному закону `0.125(kd)²√(d/W)` решает, законен ли уровень при данном λ.
Заполнить его может только тот, кто ФАСЕТИРУЕТ, то есть пункт 5. До тех пор поле
хранит `−1`, и потребитель обязан ОТКАЗАТЬСЯ, а не подставить ноль.

**3. Боковая таблица — КЛЮЧ ЕСТЬ АБСТРАКТНЫЙ ИНДЕКС ЯЧЕЙКИ, А НЕ УЗЕЛ ДЕРЕВА.**

```c
typedef struct { int32_t cell, f0, nf; } hz_cutrec;   /* массив СОРТИРОВАН по cell */
typedef struct {
  hz_cutrec *r;    int32_t nr, rcap;
  int32_t   *fref; int32_t nf, fcap;
} hz_cutmap;
```

`fref ≥ 0` — фасет `fref`, держим `{n·x ≤ off}`; `fref < 0` — фасет `~fref`, держим
`{n·x ≥ off}`. **Дополнение `~`, а не минус: `−0 == 0`, и нулевой фасет потерял бы
сторону.** Это и есть «сторона» из формулировки пункта 3.

Ключ `int32 cell` назначает ХОЗЯИН структуры: волновая линия — индекс узла
`hz_octree`, перенос — индекс ячейки `tr2_mesh`. Общий слой ни про одно из двух
деревьев не знает (Г7).

**ДВА ДОСТУПА, И ВТОРОЙ ОБЯЗАТЕЛЕН.** Точечный `hz_cutmap_find` — двоичный поиск,
`O(log n)`; этого мало, потому что сборка обходит ячейки ПО ВОЗРАСТАНИЮ, и логарифм
сел бы во внутренний цикл на всех `1e7…1e8` элементах. Поэтому есть курсор:

```c
typedef struct { const hz_cutmap *m; int32_t i; } hz_cutcur;
const hz_cutrec *hz_cutcur_next(hz_cutcur *c, int32_t cell); /* cell не убывает; O(1) */
```

**ДВА ИСТОЧНИКА ПЛОСКОСТЕЙ (Г9) — ОДИН ИНТЕРФЕЙС.** Заполнитель таблиц бывает
двух родов: из сцены (примитив/треугольник, `dmax = 0`) и из объёмных данных (QEF,
`dmax` считается). Таблицы у них ОДНИ И ТЕ ЖЕ; различие живёт в `surf` и `dmax`, а
не в структуре. Ядро пункта 4 источника не видит вовсе.

### Р-4. ЯДРО: ОТСЕЧЕНИЕ КОРОБКИ ПЛОСКОСТЯМИ → ТОПОЛОГИЯ (Г1, Г7, Г8)

**СИСТЕМА КООРДИНАТ — ЦЕЛОЧИСЛЕННЫЕ ЕДИНИЦЫ ХОЗЯИНА, а не мир.** Плоскость
переводится в единицы ОДИН РАЗ на (поверхность × дерево):

```c
typedef struct { double o[3], u[3]; } hz_frame;   /* мир = o + u∘i */
/* n'_a = n_a·u_a, off' = off − n·o. Один раз, поэтому все ячейки видят
   ПОБИТОВО ОДНУ И ТУ ЖЕ плоскость — на этом стоит водонепроницаемость. */
void hz_frame_plane(const hz_frame *fr, const double n[3], double off, hz_hspace *h);
```

**НО МЕТРИКА В ЕДИНИЦАХ НЕВЕРНА ПРИ АНИЗОТРОПНОЙ ЯЧЕЙКЕ** (Г15: `4 мм × 1 м × 1 м`
— штатный случай, а не экзотика). Аффинное сжатие не сохраняет углы, поэтому
нормаль и площадь В ЕДИНИЦАХ — не нормаль и не площадь в мире. Отсюда жёсткое
разделение: ядро отдаёт ТОПОЛОГИЮ и вершины В ЕДИНИЦАХ, а всё метрическое считает
отдельная функция, которой `hz_frame` передаётся ЯВНО. Объём — единственное
исключение: `V_мир = V_ед · u₀u₁u₂` точно.

```c
typedef struct { double n[3], off; } hz_hspace;   /* держим {n·x ≤ off} */

enum { HZ_P3_MAXH  = 96,                    /* как HZ_CUT_MAXHP в cut2d.h */
       HZ_P3_MAXF  = 6 + HZ_P3_MAXH,        /* коробка даёт 6, каждый рез ≤ 1 */
       HZ_P3_MAXV  = 2 * HZ_P3_MAXF - 4,    /* Эйлер: V ≤ 2F − 4 у выпуклого */
       HZ_P3_MAXFL = 6 * HZ_P3_MAXF - 12 }; /* Σ длин петель = 2E ≤ 6F − 12 */

typedef struct {
  int32_t nv, nf;
  double  v[HZ_P3_MAXV][3];
  int32_t fsrc[HZ_P3_MAXF];    /* ≥0: полуплоскость hid; <0: ~(грань коробки 0..5) */
  int32_t floff[HZ_P3_MAXF + 1], fl[HZ_P3_MAXFL];  /* петля вершин, CCW СНАРУЖИ */
} hz_poly3;
```

Ёмкости ВЫВЕДЕНЫ формулой Эйлера, а не назначены (`CLAUDE.md`: магических порогов
нет). Массивы фиксированы, `malloc` в ядре отсутствует — это же условие переноса на
GPU. Рёбра отдельным массивом НЕ хранятся: петли граней их задают, а волновой линии
рёберный интеграл нужен внутри обхода грани, то есть в том же порядке.

```c
int    hz_poly3_box(hz_poly3 *p, const int32_t lo[3], const int32_t hi[3]);
int    hz_poly3_clip(hz_poly3 *p, const hz_hspace *h, int32_t hid);
int    hz_poly3_cut(hz_poly3 *p, const int32_t lo[3], const int32_t hi[3],
                    const hz_hspace *h, const int32_t *hid, int nh);
int    hz_poly3_complement(hz_poly3 *out, int maxout, int *nout, /* …те же аргументы */);
double hz_poly3_volume(const hz_poly3 *p, const hz_frame *fr);
double hz_poly3_area(const hz_poly3 *p, int f, const hz_frame *fr);
void   hz_poly3_normal(const hz_poly3 *p, int f, const hz_frame *fr, double nout[3]);
```

**АЛГОРИТМ — ПОСЛЕДОВАТЕЛЬНОЕ ОТСЕЧЕНИЕ С КРЫШКОЙ** (Сазерленд–Ходжмен в 3D).
Знак `s_i = n·v_i − off` классифицирует вершину; каждая грань обходится петлёй,
пересечения дают новые вершины, а по одному новому ребру с каждой разрезанной грани
собирается ОДНА новая грань-крышка (её петля получается сцеплением этих рёбер).

**ТРИ ПРАВИЛА, НА КОТОРЫХ ДЕРЖИТСЯ ВОДОНЕПРОНИЦАЕМОСТЬ. Все три — про
ДЕТЕРМИНИЗМ, а не про допуск:**

1. **ε НЕТ.** Классификация — по знаку `s`, и `s == 0` есть «НА плоскости»,
   отдельный третий случай. Допуск не нужен именно потому, что обе соседние ячейки
   вычисляют `s` от ОДНОЙ плоскости в ОДНОЙ точке — результат совпадает побитово, и
   решения совпадают тоже. Вырождения лечит ТОПОЛОГИЯ: петля короче трёх различных
   вершин ⇒ грань выбрасывается, кусок нулевого объёма не рождается (Г8, №4);
2. **КАНОНИЧЕСКОЕ РЕБРО.** Пересечение считается от пары индексов вершин,
   упорядоченной как `(min, max)`, и только так: `t` против `1−t` расходятся в
   последнем бите, и щель Г10 появилась бы внутри одной ячейки, между двумя её
   гранями. Та же пара служит ключом склейки — вершина на общем ребре двух граней
   создаётся ОДИН раз (линейный поиск по ≤ 200 вершинам, детерминированный);
3. **СМЕНА СТОРОНЫ НЕ ЛОМАЕТ БИТЫ.** Сосед держит `{n·x ≥ off}`, то есть работает с
   `(−n, −off)`. В IEEE отрицание точно, поэтому `s` меняет ровно знак, а
   `t = s_a/(s_a − s_b)` не меняется ВОВСЕ. Кодировка `~fref` из Р-3 законна.
   **Следствие для сборки: этому файлу запрещены `-ffast-math` и слияние
   умножения-сложения** — контракция `n·v` меняет последний бит, а на нём всё и
   держится. `#pragma STDC FP_CONTRACT OFF` в начале `poly3.c`.

**ДОПОЛНЕНИЕ (внешность) — `m` НЕПЕРЕСЕКАЮЩИХСЯ ВЫПУКЛЫХ КУСКОВ.** Внешность
выпуклого тела не выпукла (`cut2d.h` берёт разность интегралов, но переносу нужны
настоящие тетраэдры и снаружи). Точное разбиение: кусок `j` = коробка, отсечённая
`h_1…h_{j−1}` как есть и `h_j` ПЕРЕВЁРНУТОЙ. Куски не пересекаются, их объединение —
ровно дополнение, число ≤ `m`, пустые не выдаются.

    ФАЛЬСИФИКАТОР №1 УСИЛИВАЕТСЯ:  V_внутр + Σ_j V_внешн,j = V_коробки

### Р-5. ЭСКИЗ: ПРЕДВЫЧИСЛЕНИЕ ИЕРАРХИИ ГРАНИЦЫ (Г11…Г16)

Офлайновый проход снизу вверх: эрмитовы данные на минимальных рёбрах → вершина QEF
на узел (матрица АДДИТИВНА, родитель = сумма детей, Г13) → фасеты узла → заполнение
`dmax` (и только здесь оно перестаёт быть `−1`). Крышка — зажим вершины в узел
(Г12.1) и манифолдная развязка (Г12.2), обе нужны ровно на тонком теле (Г14).
Выдача полигонов — обход `cellProc→faceProc→edgeProc`, полигоны НЕ хранятся.
Проверить кодом: балансировка 2:1 при обходе DC не нужна (Г11.4). Здесь же — три
вида носителя границы (Г14…Г16) и замер, какой несёт основную массу в сцене T7;
ГОМОГЕНИЗАЦИЯ вместо интерполяции свойств (Г17) с фальсификатором оттуда же.

### Р-6. ЭСКИЗ: ПОТРЕБИТЕЛИ

Перенос: `tr3_tet_from_poly()` — веер от вершины `v0` по треугольникам граней, `v0`
не содержащих; ≤ Σ длин петель тетраэдров; открывает T4. Волна:
`hz_wave3_face_walk()` — теорема о дивергенции второй раз, грань → её рёбра,
замкнутая форма полином × экспонента; открывает хвост №5. **Интеграторы общими не
делаются** (Г1, Г6): общий вход у них `hz_poly3`, и на этом всё.

## АУДИТ ПЛАНА РЕАЛИЗАЦИИ — Г18…Г31 (до единой строки кода)

Проведён по заведённому порядку над разделом «ПЛАН РЕАЛИЗАЦИИ» выше. Три класса,
в порядке приоритета: интерфейс, выданный под одну линию (класс Г2); число, которое
не изменится от параметра, которым его меняют; значение, использованное раньше,
чем установлено.

**СОСТОЯНИЕ ИСПРАВЛЕНИЙ.** Находка, оставленная только в списке находок, — это та же
ошибка, что Г19 (текст лежит на пути, следующая сессия берёт его как действующий),
поэтому каждая правка внесена ТУДА, ГДЕ ЕЁ ПРОЧТУТ, а не только сюда.

| # | куда внесено |
|---|---|
| Г18 | текст **Г7** переписан: «плюс ориентированные полупространства» |
| Г19 | **`PLAN_TRANSPORT.md`** — обе строки отменены |
| Г20 | текст **Г4** и **пункт 3 порядка работ**: ключ — ячейка, не узел |
| Г21 | Р-4 (разделение топологии и метрики) + негативный контроль в **Г8** |
| Г22 | **Г8** строка 1 переписана + свип `off` и подмена чтения в контролях |
| Г23 | **Г8** строка 2 разделена на 2a/2b + контроль на локальные координаты |
| Г24 | **Г8** — контроль «сбить нормаль на 1%» |
| Г25 | Р-3: `dmax < 0` = «не вычислено», потребитель ОТКАЗЫВАЕТСЯ |
| Г26 | **в коде**: `test_octfmt` — годный файл грузится и совпадает поячеечно |
| Г27 | **в коде**: `E_CYCLE` + контроль с таймаутом; текст поправлен по замеру |
| Г28 | **в коде**: свидетель порядка байт и канарейка IEEE754 |
| Г29 | **в коде**: `hdr_decode` / `load` / `validate` + оснастка на `hz_oct_leaf` |
| Г30 | **Г8** строка 4 сужена до внутреннего куска + новая строка 6 (перестановка) |
| Г31 | **в коде**: `-ffp-contract=off` в CFLAGS + страж `make check-fp`. Механизм пришлось заменить целиком — прагму GCC не реализует (Г33) |
| Г32 | **в коде и тексте**: Р-2 и Г27 переписаны, проверка оставлена как код ошибки |
| Г33 | **в коде**: страж на fma-ИНСТРУКЦИЯХ вместо сверки чисел; заведена по замеру |

Первая редакция Г8 содержала неисполнимый негативный контроль («сбить ОДНО угловое
значение»): угловых значений после Г3 нет, и запустить его было бы нечем. Заменён
на свип `off` с подменой чтения.

### Г18. Г7 НЕСЁТ УСТАРЕВШУЮ ФРАЗУ «ПЛЮС УГЛОВЫЕ ЗНАЧЕНИЯ» — КЛАСС Г2

Г7 писалась ДО обсуждения и описывает вход ядра как «целочисленные границы плюс
УГЛОВЫЕ ЗНАЧЕНИЯ». Г3 угловые значения отвергла целиком. Реализуя Г7 буквально, я
бы выдал наружу интерфейс отвергнутого варианта — ровно повторение Г2, только на
поколение позже. **Вход ядра: целочисленная коробка + ОРИЕНТИРОВАННЫЕ
ПОЛУПРОСТРАНСТВА, угловых значений нет.** Формулировка Г7 сужена этим абзацем.

### Г19. `PLAN_TRANSPORT.md` НЕ ПОПРАВЛЕН ПО Г3 И Г17

Г3 постановила: «формулировку в обоих планах надо править». Поправлен только
`CLAUDE.md`. `PLAN_TRANSPORT.md:54-55` до сих пор читает «среда: … линейно
интерполируемые по узлу» (отвергнуто Г17) и «поверхность: аналитический примитив,
ЛИНЕЙНАЯ ФУНКЦИЯ ПО УГЛАМ, НУЛЕВОЕ МНОЖЕСТВО которой есть граница» (отвергнуто Г3).
Это не косметика: текст лежит на пути реализации переноса, и следующая сессия
возьмёт его как действующий. Правится вместе с пунктом 2.

### Г20. КЛЮЧ БОКОВОЙ ТАБЛИЦЫ «УЗЕЛ» СДЕЛАЛ БЫ ЕЁ НЕПРИГОДНОЙ ДЛЯ ПЕРЕНОСА

Пункт 3 порядка работ назван «узел → набор фасетов, сторона». `hz_octree` — не
структура переноса (Г7: перенос живёт на `tr2_mesh`, где есть грани, а у октодерева
их нет). Таблица, ключом которой объявлен УЗЕЛ, отдаёт наружу устройство волновой
линии — снова класс Г2. **Ключ есть абстрактный `int32 cell`, назначаемый хозяином
структуры.** Проверяемо: боковая таблица обязана заполняться из `tr2_mesh` без
единой строки про октодерево, и это тест, а не намерение.

### Г21. МЕТРИКА В ЕДИНИЧНЫХ КООРДИНАТАХ НЕВЕРНА, И ИМЕННО НА АНИЗОТРОПНОЙ ЯЧЕЙКЕ

Перевод плоскости в единицы (Р-4) — аффинное сжатие с разными множителями по осям.
Объём переносится точно, УГЛЫ И ПЛОЩАДИ — нет. Наивная реализация вернула бы
`|n|`-нормированную нормаль в единицах и молча дала бы неверный поток через грань
ровно там, где Г15 обещает штатный случай `4 мм × 1 м × 1 м`. Ошибка не сработает
на кубической ячейке, то есть первый же тест её пропустит.
**Предсказание:** площадь наклонной грани в ячейке `1×1×1` и в ячейке `1×1×8`
относятся НЕ как объёмы. **Негативный контроль со своей метрикой:** посчитать
площадь по вершинам в единицах и по вершинам в мире; на кубической ячейке разница
обязана быть нулевой, на анизотропной — `O(1)`. Если и там ноль — площадь берётся
не из вершин.

### Г22. ФАЛЬСИФИКАТОР Г8-№1 (Σ ОБЪЁМОВ) — ПЕРВАЯ ПОДПИСЬ АРТЕФАКТА В ЧИСТОМ ВИДЕ

`V_внутр + ΣV_внешн = V_коробки` выполняется ТОЖДЕСТВЕННО для любого разбиения,
включая случай «плоскость не режет вовсе» и случай «плоскость прочитана как мусор,
но один и тот же мусор в обеих ветках». Само по себе это ничего не проверяет.
**Достраивается двумя вещами:** (а) свип `off` через ячейку — `V_внутр` обязана
монотонно пройти от `0` до `V_коробки`, и (б) негативный контроль: подменить чтение
`off` константой ⇒ `V_внутр` ОБЯЗАНА перестать зависеть от `off`. Если сумма при
этом всё ещё сходится — а она сойдётся — видно, что метрика №1 меряет бухгалтерию,
а не геометрию.

### Г23. ФАЛЬСИФИКАТОР Г8-№2 (ПОБИТОВОЕ СОВПАДЕНИЕ) НЕПРОВЕРЯЕМ ПУНКТОМ 4 В ОДИНОЧКУ

«Точки на рёбрах у соседних ячеек совпадают побитово» имеет смысл, только когда
ребро — ОДИН объект. У соседей РАЗНОГО УРОВНЯ общего ребра нет по построению; это
и есть шов Г11.3/Г12, и живёт он в пункте 5. **Фальсификатор разделяется:**
№2a — соседи одного уровня и две грани внутри одной ячейки (пункт 4, проверяемо
сейчас); №2b — разные уровни (пункт 5, вместе с обходом DC). Без разделения №2
пройдёт на равномерном случае и создаст ложную уверенность — та же ошибка, что К13
в T2 (константу воспроизводит любой оператор).
**Негативный контроль к №2a со своей метрикой:** считать пересечение в ЛОКАЛЬНЫХ
координатах ячейки (вычесть `lo`, отсечь, прибавить обратно) ⇒ последние биты
обязаны разойтись и совпадение упасть. Если не упало — тест сравнивает числа,
которые совпали бы и без канонического ребра.

### Г24. ФАЛЬСИФИКАТОР Г8-№5 (ПЛОЩАДЬ ФАСЕТА) — БЕЗ НЕГАТИВНОГО КОНТРОЛЯ

Г8 перечисляет пять фальсификаторов и три негативных контроля; №5 остался без
своего. Замкнутая форма сечения куба плоскостью — гладкая функция нормали, поэтому
контроль прямой: **сбить нормаль на 1% ⇒ площадь обязана сдвинуться на `O(1%)`.**
Если не сдвинулась — площадь считается по коробке, а плоскость не читается. Это
третий класс приоритета в чистом виде.

### Г25. `dmax` ЧИТАЕТСЯ ПУНКТОМ 6, А ЗАПОЛНЯЕТСЯ ПУНКТОМ 5

Поле законности уровня (Г11.2) заводится в пункте 3, но вычислить его может только
фасетизатор из пункта 5. Между этими пунктами волновая линия получит таблицу с
полем, которое существует и не установлено, — «значение используется раньше, чем
установлено», причём в форме, которую компилятор не увидит. **Правило: `dmax < 0`
означает «не вычислено», и потребитель обязан ОТКАЗАТЬСЯ (fail closed), а не
подставить ноль.** Ноль здесь — самое опасное значение: он означает «фасет точен».

### Г26. ПРОВЕРКА ФОРМАТА, ОТВЕРГАЮЩАЯ ВСЁ, ПРОЙДЁТ ВСЮ ТАБЛИЦУ Р-2

Десять строк таблицы фальсификаторов формата удовлетворяются реализацией
`hz_oct_load { return 1; }`. Это первая подпись артефакта, поставленная в тест
целиком. **Обязательная парная строка: годный файл грузится И совпадает с
оригиналом поячеечно по всей сетке.** Внесена в негативные контроли Р-2.

### Г27. ЦИКЛ В ФАЙЛЕ — НЕ ГИПОТЕТИЧЕСКАЯ ДЫРА, ОНА ВЕШАЕТ ПРОЦЕСС СЕЙЧАС

Действующая проверка (`src/octree.c`, старая редакция) требует только
`child0 + 8 ≤ n`. Этого мало: существует файл, который она принимает, а
`hz_oct_at` на нём не возвращается никогда. То есть Г5 недооценила — это не
«прочтётся как валидное», а ЗАВИСАНИЕ на недоверенном входе. Инвариант `c > i` из
Р-2 закрывает его целиком и стоит одно сравнение.
**Фальсификатор №6 в Р-2 обязан прогоняться с таймаутом**, иначе негативный
контроль «без проверки — зависание» невозможно наблюдать.

**ПОПРАВКА ПО РЕЗУЛЬТАТУ ПРОГОНА (07-27). Первая редакция Г27 назвала механизм
неверно, и негативный контроль это поймал — ради чего он и заводился.** Было
сказано: достаточно `child0[i] == i`, и тогда «`size` доходит до нуля, индекс не
меняется». Замер: **простая петля НЕ вешает.** Как только `size` проскакивает 1 и
становится нулём, все три сравнения `x ≥ lo + size` истинны, октантный индекс
залипает на 7, и `ni = child0 + 7` уводит обход ВПЕРЁД из петли.

Чтобы цикл замкнулся, он должен замкнуться ЧЕРЕЗ эту семёрку:

    n = 9, log2size = 2
    child0[0] = 1   корень делится
    child0[1] = 1   гонит size 2 → 1 → 0
    child0[8] = 1   замыкает: ni = 1 + 7 = 8, и так вечно

Старая проверка это принимает (`1 + 8 ≤ 9`), новая отвергает как `E_CYCLE` на
`i = 1`. Вывод Г27 в силе, а вот его обоснование было бы ложным свидетельством:
тест, написанный по первой редакции, ПРОШЁЛ БЫ по неверной причине — предъявил бы
файл, который отвергается, но не висит, и «зависание» осталось бы недоказанным.
Это и есть первая подпись артефакта в негативном контроле.

### Г28. ВЕРСИИ БЕЗ СВИДЕТЕЛЕЙ РАЗМЕРА И ПРЕДСТАВЛЕНИЯ НЕДОСТАТОЧНО

Номер версии ловит изменения, которые кто-то ПОМНИЛ проставить. Смена `double` на
`float`, смена порядка байт, сборка на другой ABI номер не трогают. Отсюда два
свидетеля в заголовке (`endian`, `canary`) и SoA-раскладка, у которой нет набивки,
а значит нет и зависимости от выравнивания компилятора. **Предсказание:** файл,
записанный сборкой с `float`, отвергается по `canary`, а не читается как мусор.

### Г29. «CBMC НА `hz_oct_load`» БУКВАЛЬНО НЕВЫПОЛНИМ

CBMC не имеет тела `fread`/`FILE*`. Требование пункта 2 исполняется только после
разделения на `hz_oct_hdr_decode` / `hz_oct_load` / `hz_oct_validate`, где первое и
третье чисты и целочисленны. **Оснастка обязана прогонять НАСТОЯЩИЙ `hz_oct_at`
после `validate() == 0`** — иначе доказано свойство формулы, а не свойство
потребителя, ради которого инвариант и заводился.

### Г30. ЧИСЛО КУСКОВ ДОПОЛНЕНИЯ ЗАВИСИТ ОТ ПОРЯДКА ПОЛУПРОСТРАНСТВ

Разбиение внешности (Р-4) даёт до `m` кусков, но сколько из них НЕПУСТЫ — зависит
от порядка `h_1…h_m`. Значит «число кусков» негодно как проверяемая величина, и
фальсификатор Г8-№4 («число кусков и Σ объёмов верны») в этой части надо читать
только про ВНУТРЕННИЙ кусок. **Σ объёмов инвариантна к порядку и остаётся; число
кусков — нет.** Плюс: перестановка полуплоскостей обязана давать ту же Σ и то же
`V_внутр` побитово — дешёвый и сильный тест, которого в Г8 не было.

### Г31. ВОДОНЕПРОНИЦАЕМОСТЬ МОЖЕТ БЫТЬ СЛОМАНА ФЛАГАМИ КОМПИЛЯТОРА, А НЕ КОДОМ

Всё, что написано в Р-4 про побитовое совпадение, верно для строгого IEEE и ложно
при контракции умножения-сложения: `n·v` в двух местах программы может дать разные
последние биты просто из-за разного инлайнинга. Тогда щели появятся, и НИ ОДИН тест
исходника их не объяснит. **Гейт: `#pragma STDC FP_CONTRACT OFF` в `poly3.c`,
запрет `-ffast-math` для этого файла, и проверка при сборке с `-O0` и с `-O2` —
результаты обязаны совпасть ПОБИТОВО.** Это негативный контроль со своей метрикой:
если они совпадают и при намеренно включённой контракции, значит тест не доходит до
чувствительного места.

### Г32. `c > i` НЕ ЛОВИТ ЦИКЛЫ — ИХ ЛОВИТ ДОСТИЖИМОСТЬ. НАЙДЕНО НЕГАТИВНЫМ КОНТРОЛЕМ

Р-2 и Г27 приписывали ацикличность проверке `c ≤ i ⇒ отказ`. **Замер это опроверг.**
Метод: из `src/octree.c` вырезалась ОДНА проверка, и тем же входам задавался вопрос
заново (`/tmp/repro/bite.c`, коды: 0=OK, 11=CYCLE, 14=DEPTH, 15=TWOPARENT,
16=UNREACHABLE).

| вход | как есть | без `c > i` | без глубины |
|---|---|---|---|
| петля `child0[1] = 1` | 11 | **15** | 11 |
| указатель назад (блок 9 из блока 17) | 16 | **16** | 16 |
| цепочка глубины 3 при `log2size = 1` | 14 | 14 | **0 ← ПРИНЯТО** |

Читается так: **без `c > i` не принимается НИЧЕГО нового** — меняется только код
отказа. Причина в порядке прохода: если родитель `p` заявляет блок с индексом
`c < p`, то индексы `c…c+7` были просмотрены РАНЬШЕ `p`, и на них уже сработала
`UNREACHABLE`. Значит в любом ПРИНЯТОМ дереве родитель всегда левее ребёнка, и
цикл невозможен по построению: узлы цикла не получают глубину ни от кого снаружи.

**Следствие, которое важнее самой находки:** завершаемость `hz_oct_leaf` держится
на «индекс спуска СТРОГО РАСТЁТ» (`ni ≥ c > i`), а это свойство ПРИНЯТОГО дерева, и
даёт его достижимость, а не проверка цикла. Проверку оставляем — она стоит одно
сравнение и даёт точный код, — но Г27 и Р-2 переписаны: сказать «здесь ловятся
циклы» значит назвать не тот механизм, а на неверном механизме потом строятся
следующие решения.

**И ОБРАТНОЕ ПО ГЛУБИНЕ: она НЕ избыточна.** Без неё цепочка глубже `log2size`
ПРИНИМАЕТСЯ (0 вместо 14), и это ровно та конфигурация, в которой `size` в
`hz_oct_leaf` проскакивает 1 и обнуляется. Памяти это не рушит — спуск остаётся в
границах, — поэтому CBMC её и не видел, пока в оснастку не была добавлена ОТДЕЛЬНАЯ
геометрическая проверка (см. доклад ниже).

## ДОКЛАД ПО ПУНКТУ 2 ПОРЯДКА РАБОТ (07-27): ФОРМАТ ДЕРЕВА ЗАКРЫТ

**Вывод.** Дыра Г5 закрыта: magic + версия + два свидетеля представления + полная
целочисленная проверка топологии, `28/28` фальсификаторов, CBMC на лестнице из пяти
конфигураций, `valgrind` и санитайзеры чисты, `make test` зелёный. Три вещи по ходу
оказались не такими, как записано в аудите, и все три исправлены в тексте выше:
механизм зависания (Г27), работающая проверка ацикличности (Г32) и форма кода,
которую принимает `gcc -fanalyzer`.

**ЧТО НАПИСАНО.** `src/octree.c/.h`: формат v1 (заголовок 40 байт, дальше три
раздельных массива — SoA), `hz_oct_hdr_decode` и `hz_oct_validate` — чистые и
целочисленные, `hz_oct_load` — тонкая оболочка ввода-вывода над ними.
Из `hz_oct_at` выделен `hz_oct_leaf` (индекс листа, целочисленный спуск): он же —
ключ боковой таблицы пункта 3 (Г4/Г20), и он же то, что CBMC вообще способен
проверить. `tests/test_octfmt.c` — фальсификаторы, `tests/cbmc_octree.c` — оснастка.

**ИЗМЕРЕНО.**

| | результат |
|---|---|
| `test_octfmt` | 28/28, все десять порч отвергнуты СВОИМ кодом |
| годный файл | грузится, совпадает поячеечно, `save→load→save` байт в байт |
| `valgrind` на новом формате | `0 errors`, записи неинициализированного нет |
| ASan+UBSan | чисто |
| CBMC, лестница `HZ_N/HZ_L2` | `1/0`, `9/1`, `9/2`, `17/2`, `25/3` — SUCCESSFUL, `0 of 1623`, от 1 до 27 с |

**НЕГАТИВНЫЕ КОНТРОЛИ — ЧТО ИЗ НИХ ВЫСТРЕЛИЛО.** Три из четырёх сработали, и
каждый сработавший что-то изменил:

1. **набивка (предсказан провал — провалился).** Старый сырой дамп структуры на
   дереве из 2761 узла (то есть после `realloc`): `valgrind` даёт
   `Syscall param write(buf) points to uninitialised byte(s)`, 3 ошибки. Новый
   формат — 0. Это и есть довод за SoA, теперь измеренный, а не заявленный.
   Оговорка из Р-2 подтвердилась: побитовое сравнение двух файлов СЛАБАЯ метрика
   (свежие страницы нулевые), гейтом работает `valgrind`;
2. **зависание (предсказан провал — сначала НЕ провалился).** Файл `child0[i] = i`
   вернулся из `hz_oct_at` нормально, то есть предсказание Г27 было неверно.
   Механизм найден и записан (Г27, поправка): цикл обязан замыкаться ЧЕРЕЗ
   залипший октантный индекс 7. На исправленной конфигурации контроль виснет, как
   и требуется;
3. **вырезанная проверка диапазона (предсказан провал — провалился).** Без
   `c > n − 8` CBMC даёт `pointer outside object bounds` в `hz_oct_leaf`. Это
   доказывает, что оснастка НЕ пуста;
4. **вырезанная проверка цикла (предсказан провал — НЕ провалился).** Отсюда Г32.

**ПЕРВАЯ ПОДПИСЬ АРТЕФАКТА, ПОЙМАННАЯ В ФОРМАЛЬНОМ СЛОЕ.** Пока оснастка проверяла
только память и завершаемость, `VERIFICATION SUCCESSFUL` держался и с вырезанной
проверкой глубины — то есть проверку можно было бы удалить, и ВЕСЬ гейт остался бы
зелёным. Лечение: в оснастку добавлена ОТДЕЛЬНАЯ, независимо выведенная
геометрическая проверка «глубина ≤ `log2size`». После неё контроль без глубины даёт
`VERIFICATION FAILED`, как и положено.

**ЦЕНА ПРОГОНА — ЭТО ПАРАМЕТР, А НЕ ПЕРЕПИСЫВАНИЕ.** Первая версия оснастки брала
`n` и все `child0` полностью недетерминированными; прогон был убит без единой
строки вывода. Символьное `n` делает `malloc(n)` внутри `hz_oct_validate` объектом
символьного размера, и это доминирует над всем остальным. `n` и `log2size` стали
компилируемыми параметрами, `child0` — из объявленного окна, и лестница считается
за секунды. В `scripts/cverify.sh` добавлен `ulimit -v` (`CVERIFY_MEM_KB`, по
умолчанию 16 ГБ): прогон без предела не падает, а зовёт OOM-killer, и это выглядит
ровно как «инструмент промолчал».

**ФОРМА КОДА ПОД `gcc -fanalyzer`.** Чанковая заливка массива, повторённая для трёх
полей в одной функции, заставляет анализатор расшириться и потерять связь
«ёмкость ↔ счётчик» — фантомное переполнение кучи. Минимальный повтор строился до
правки рабочего кода, как требует `CLAUDE.md`, и показал: дело не в обёртке
аллокации (её здесь нет), а в ЧИСЛЕ таких циклов в одной функции. Лечение — по
одному циклу на функцию (`read_child0`, `read_part`, `write_child0`, `write_part`).
Отдельно: CBMC моделирует `double complex` структурой и падает на неявном
преобразовании `double → double complex`, поэтому в `hz_oct_at` стоит явный `CMPLX`,
а оснастка работает с `hz_oct_leaf` и `k2` не трогает вовсе.

**ЧЕГО ЭТОТ ПУНКТ НЕ ДАЁТ.** Проверка гарантирует, что дерево из файла безопасно
обходить; она НИЧЕГО не говорит про осмысленность `k²` и про боковые таблицы —
их ещё нет. `flags` зарезервирован под них и сейчас обязан быть нулём.

### Г33. МЕХАНИЗМ Г31 НЕ РАБОТАЕТ, А ЕЁ ФАЛЬСИФИКАТОР НЕ ЛОВИТ ТО, РАДИ ЧЕГО ЗАВЕДЁН

Г31 предписывала три вещи: `#pragma STDC FP_CONTRACT OFF` в `poly3.c`, запрет
`-ffast-math` и сверку сборок `-O0`/`-O2` на побитовое совпадение. **Замер убил
первую и обесценил третью.**

**Прагма не работает.** GCC её НЕ РЕАЛИЗУЕТ и молча пропускает
(`-Wunknown-pragmas`). Защита была бы декоративной: строка в файле есть,
действия нет. Механизм — только флаг сборки, `-ffp-contract=off`, и он вынесен в
`CFLAGS` проекта.

**Сверка `-O0`/`-O2` проходит и при включённой контракции.** Собрано четырьмя
способами, включая `-mfma -ffp-contract=fast` и `-ffast-math`: `test_poly3`
зелёный во всех, вывод совпадает побитово. Причина в устройстве самого теста, и
она общая для любого теста такого вида: **он сравнивает выход ОДНОГО
скомпилированного кода с самим собой.** Обе «соседние ячейки» считаются одной и
той же функцией, поэтому контракция сдвигает их последние биты ОДИНАКОВО, и
согласие сохраняется. Ломается оно только там, где ОДНО скалярное произведение
скомпилировано по-разному в двух местах — то есть при разном инлайнинге, чего в
тесте нет и не может быть.

**Наблюдаемая метрика поэтому не число, а ИНСТРУКЦИЯ.** Проверено `objdump`:

| сборка `poly3.c` | `vfmadd`/`vfmsub` | `test_poly3` |
|---|---|---|
| `-O2 -mfma -ffp-contract=off` | **0** | зелёный |
| `-O2 -mfma` (флаг убран) | **18** | зелёный |

Отсюда страж `make check-fp`: компилировать с `-mfma` и требовать НОЛЬ
fma-инструкций. Негативный контроль у него свой и он бьёт — снятие флага даёт 18.
Включён в `make test`.

**И отдельно, честно: сегодня на этой машине флаг не меняет ничего наблюдаемого.**
Базовый x86-64 без `-mfma` fma не имеет, а nix глушит `-march=native`
(`NIX_ENFORCE_NO_NATIVE`). То есть Г31 — опасность НА БУДУЩЕЕ (GPU, другой
`-march`, другой компилятор), закрытая дешёвым флагом и стражем, а не измеренная
поломка. Называть её измеренной было бы третьей подписью артефакта: ошибка,
нормированная на результат, которого не было.

## ДОКЛАД ПО ПУНКТУ 4 (07-27): ЯДРО ОТСЕЧЕНИЯ НАПИСАНО

**Вывод.** `src/cut/poly3.c` — общий слой: коробка × полуплоскости → топология
выпуклого многогранника, плюс разбиение внешности на непересекающиеся выпуклые
куски. `37/37` фальсификаторов, ВСЕ ЧЕТЫРЕ негативных контроля выстрелили как
предсказано, `ccheck` CLEAN, ASan/UBSan и `valgrind` чисты, `make test` зелёный.
Пункты 4 и 3 переставлены сознательно (довод в порядке работ).

**ИЗМЕРЕНО** (предсказания напечатаны до результатов, прогон печатает и то и то):

| фальсификатор | результат |
|---|---|
| 1 `V_внутр + ΣV_внешн = V_коробки` | сходится; свип `off` даёт `0 → 32 → 64` монотонно |
| 2a побитовость на общей грани соседей | 4 пары, совпадение ПОБИТОВОЕ |
| 4 вырождения (угол/ребро/грань/вне) | все шесть случаев верны, куска нулевого объёма нет |
| 5 площадь фасета | `3.4641016` против замкнутой `(√3/2)a² = 3.4641016` |
| 6 перестановка полуплоскостей | `V = 18` при всех 6 перестановках, ПОБИТОВО |

**НЕГАТИВНЫЕ КОНТРОЛИ — ЧЕТЫРЕ ИЗ ЧЕТЫРЁХ ВЫСТРЕЛИЛИ:**

- **Г22, замороженное чтение `off`:** разброс ровно `0.0e+00`, тогда как живой
  свип даёт `0 → 64`. Сумма объёмов при этом сходится в обоих случаях — ровно то,
  ради чего контроль и заведён;
- **Г23, счёт в локальных координатах:** те же точки геометрически, побитово
  РАЗОШЛИСЬ. Значит каноническое ребро действительно несёт работу, а тест не
  сравнивает числа, которые совпали бы и так;
- **Г24, нормаль сбита на 1%:** площадь ушла на `0.659%`, то есть `O(1%)`.
  Площадь читает плоскость, а не коробку;
- **Г21, анизотропный кадр:** объём переносится множителем `u₀u₁u₂` точно, а
  площадь того же фасета `3.4641` против `22.7156` (отношение `6.56`, а не `8`).
  Нормаль в кадре `1×1×8` вышла `∝(8,8,1)`, как и должна. Ошибка «нормаль в
  единицах = нормаль в мире» на кубической ячейке не проявилась бы вовсе.

**ЧТО ПРИШЛОСЬ ПОПРАВИТЬ ПО ХОДУ, СВЕРХ Г33:** `cppcheck` поймал копию целой
структуры `*out = tmp` в конце `hz_poly3_cut` — 8 КБ, из которых прописана лишь
начальная часть массивов, то есть копирование неинициализированных хвостов (тот
же класс, что набивка в пункте 2). Лечится не заглушкой, а выбором начального
буфера пинг-понга по чётности `nh`: результат оседает в `out` сам, копии нет.

**ЧЕГО ПУНКТ 4 НЕ ДАЁТ.** Фальсификатор 2b (побитовость у соседей РАЗНЫХ
уровней) здесь не проверяется и проверяться не может — общего ребра у них нет по
построению, это шов Г11.3/Г12 и он в пункте 5 (Г23). Фальсификатор 3 (Σ площадей
по обе стороны общей грани) проверен через 2a и вырождения, но не как отдельная
метрика на паре ячеек с общей ГРАНЬЮ — это к пункту 5 вместе с 2b. Интеграторов
здесь нет и не будет: общий вход у двух линий — `hz_poly3`, и на этом всё.

## ДОКЛАД ПО ПУНКТУ 3 (07-27): ТАБЛИЦЫ ОБЩЕГО СЛОЯ НАПИСАНЫ

**Вывод.** `src/cut/surf.c` — три таблицы (поверхности, фасеты, боковая) плюс
мост к ядру. `60/60` фальсификаторов, оба негативных контроля выстрелили,
`ccheck` CLEAN, ASan/UBSan и `valgrind` чисты, `make test` зелёный. Порядок работ
пройден до конца по пунктам 2, 4, 3; остаются 5 и 6.

**ИЗМЕРЕНО.**

| что проверялось | результат |
|---|---|
| сторона кодируется `~fref` | у ФАСЕТА НОЛЬ обе стороны представимы: `<=` даёт `n_z=+1`, `>=` даёт `n_z=−1` |
| Г3, общая ссылка | точки на общей стене соседей совпали ПОБИТОВО |
| Г20, независимость от хозяина | ключ-узел `hz_oct_leaf` и ключ-ячейка сетки дали `V=32.000000` ПОБИТОВО одинаково |
| курсор против двоичного поиска | совпали на всём диапазоне, курсор прошёл 500 записей ровно один раз |
| Г25, `dmax` не вычислен | ОТКАЗ, и `*err` не тронут |
| сквозной прогон | `V_материала + ΣV_внешности = 64.000000`, метки сторон `77/88` доехали нетронутыми |
| кадр применяется | мировая `z ≤ 2` в кадре `(o_z=10, u_z=8)` стала `8·z ≤ −8` |

**НЕГАТИВНЫЕ КОНТРОЛИ — ОБА ВЫСТРЕЛИЛИ:**

- **Г3, независимая подгонка:** у каждой ячейки СВОЙ фасет, смещение отличается
  на ОДИН ulp — точки на стене разошлись в битах. То есть тест проверяет
  водонепроницаемость, а не воспроизводит её случайно: общая ссылка даёт
  совпадение, независимая — расхождение, и разница видна на одном ulp;
- **Г25, избирательность отказа:** фасеты с `dmax = 0` и `dmax = 1e−4`
  ОБЯЗАНЫ проходить, и проходят (`err = 0` и `err = 1250` по закону
  `0.125(kd)²√(d/W)`). Без этой пары проверку удовлетворяла бы реализация
  `return 1;`.

**ЧТО ВАЖНО В УСТРОЙСТВЕ.** Перевод плоскости в единицы кадра делается ОДИН РАЗ,
внутри `hz_facettab_add_plane`, и в таблице лежит уже результат — именно поэтому
две ячейки не могут разойтись: расходиться нечему. `hid` для ядра — сам `fref`,
поэтому `fsrc` грани в `hz_poly3` говорит, каким фасетом И С КАКОЙ СТОРОНЫ она
порождена, а `hid_flipped = ~fref` уходит в разбиение внешности. Материалов в
общем слое нет: `hz_surf` несёт две непрозрачные метки, и тест проверяет, что
они доехали нетронутыми.

**ЧЕГО ПУНКТ 3 НЕ ДАЁТ.** Фасетизации кривого примитива (сфера, цилиндр) — она в
пункте 5 вместе с иерархией и заполнением `dmax`; до тех пор `dmax` у
производных фасетов хранит «не вычислено», и потребитель обязан отказываться.
Второго источника плоскостей (QEF по объёмным данным, Г9) тоже нет — структура
под него заведена (`surf`, `dmax`), но заполнителя нет.
