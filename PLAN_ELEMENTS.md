# PLAN_ELEMENTS.md — ЖИВОЙ ЖУРНАЛ (эпоха новой структуры, §833+)

Append-only: план шага → аудит А-серии ДО кода → предсказания → код+гейт →
доклад → аудит исполнения. Свежие записи — В КОНЦЕ файла.

**Архив эпохи старого представления (§1–§832):** `archive/plan_elements_old_structure.md`.
Туда же — `archive/PLAN_CUT.md` (геометрия реза, Г1…Г62), `archive/new-structure.md`
(правила и замеры-предшественники), `archive/README.md` (указатель по архиву).

---

## ЦЕЛЬ — ПЕРВЫМИ СТРОКАМИ (переписана пользователем 08-09; действует)

**ПО СЦЕНЕ МОЖНО ХОДИТЬ.** Интерактивно, в любую точку, с РАЗРУШАЕМОЙ геометрией
и КОРРЕКТНЫМ диффузным переотражением. Бюджет: **`≥ 5` кадров в секунду
(`≤ 200` мс на кадр) при `512²` на зале**; город и Сан-Мигель докладываются
отдельно; холодный старт в бюджет не входит и считается порознь.

**СРЕДСТВА — каждое проверяемо числом, и шаг засчитывается только тем, что
двигает цель либо закрывает одно из них:**

    1. ОДНО ПРЕДСТАВЛЕНИЕ (ныне — STRUCTURE.md: киты × размещения, поле на
       поверхности, перенос бокс→бокс аналитичен).
    2. LOD — СВОЙСТВО СТРУКТУРЫ, а не отдельная лестница слияния.
    3. РАЗРУШАЕМОСТЬ — правка O(объёма изменения) В ТОТ ЖЕ КАДРОВЫЙ БЮДЖЕТ.
    4. ФРОНТ ИДЁТ БЫСТРО: 660 нс на ячейку; решения полосой, не арифметикой.
    5. ДИФФУЗНОЕ ПЕРЕОТРАЖЕНИЕ КОРРЕКТНО: печь L = L_e/(1−ρ), E_ind = ρ·E_dir.
    6. ЗЕРКАЛЬНОЕ — немногими лучами поверх фронта (T4: дихотомия диффуз/зеркало).

**ЧЕГО ЦЕЛЬ НЕ СОДЕРЖИТ:** ни «уложиться в порог», ни «побить трассировщик на
одном кадре». Фронт экономит по ПРИЁМНИКАМ и по ИСТОЧНИКАМ, не по разрешению.

Прежняя спецификация «узел 19 Б, эрмитово поле + DC» — эпоха СТАРОГО
представления, см. архив; живое представление — `STRUCTURE.md`.

---

## ЗАКОНЫ, ДОБЫТЫЕ ДО §833 (не устарели; доказательства — в архиве)

- **Сток в сплошное** 76% излучённой энергии не учитывался — найден и закрыт
  граничным условием (§706/§707); баланс К40 замкнут до 6.5e-8.
- **Усилители** (щепки объёма, вырожденные элементы) лечатся `xthin`/`xsemin`;
  принцип максимума в обновлении ячейки — иначе расходимость (§735).
- **Внешний эталон** — интегрирующая сфера (`scripts/gen_cavity.sh`,
  `xfernosolid`): протечка полости — закон ~h^0.7, от ординат не зависит.
- **Лестница отскоков** `xbounce=N`: ×6.0 (14.3→2.23 с) при Δpsin −0.63%;
  хвост поэлементных ошибок требует N=8.
- **Вклад дальнего**: дальше 1.5×R зоны интереса — <0.01% (порог огрубления).
- **Огрубление ординат по отскокам ОТВЕРГНУТО** (§811 УБИВАЕТ-а: хвост несёт
  71% psin); угловая работа — внутри агрегатного отклика, не в числе ординат.
- **Глянец ординатами у́же 27° при ND=64 не берётся** — развилка углового базиса
  открыта.
- **Куски из OBJ-треугольников** (§794): тождество покрытия, протечка 3.6%;
  чешуя кадра была артефактом sout-канала (§798), не полей.
- **Тёплый старт** (§808), **walk в продакшн** после А1578; битовые якоря —
  при `OMP_NUM_THREADS=1`.

---

## ДИСЦИПЛИНА ШАГА — БЕЗ ИЗЪЯТИЙ

1. план шага и ПРЕДСКАЗАНИЯ с интервалами — в конец этого файла ДО кода,
   отдельным коммитом;
2. аудит плана до кода, находки серией А;
3. код; гейт `CLAUDE.md` целиком (clang-format, `scripts/ccheck.sh` до CLEAN,
   `scripts/lean.sh`, санитайзеры; CBMC на опасном);
4. прогон с НЕГАТИВНЫМ КОНТРОЛЕМ, который обязан провалиться предсказанным
   образом;
5. доклад: вывод первым абзацем; у всякого числа три пометы — на чём, какова
   ПОПУЛЯЦИЯ, чего НЕ покрывает;
6. аудит исполнения: что не сбылось, что измерено не тем, что осталось.

Прерываться и спрашивать ТОЛЬКО при срабатывании «УБИВАЕТ» или если нужно
править ядро реза (`src/cut/poly3.c`, архивный контекст — `archive/PLAN_CUT.md`).

---

## СЦЕНЫ — ЧИТАТЬ ОБЯЗАТЕЛЬНО

**ДВЕ СЦЕНЫ УДАЛЕНЫ ПО ПРЯМОМУ УКАЗАНИЮ ПОЛЬЗОВАТЕЛЯ. Не возвращать.**
(КОНФЕРЕНЦ-ЗАЛ 08-11 — негодная сетка; RUNGHOLT 08-12 — воксельное
происхождение. Городской ступени в сотни метров у проекта нет — named gap;
Bistro exterior 115 м — ближайший кандидат.)

    assets/synth/room.obj    ОСНОВНАЯ, scripts/gen_room.sh. Стены с толщиной,
                            11 материалов, открытых рёбер 0.
    room_thin.obj            НК: лист нулевой толщины.
    room_soup.obj            НК: несогласованная обмотка (ломает).
    spheres.obj              чистая кривизна, gen_spheres.sh.
    box.obj, plates.obj      печь и E_ind = ρ·E_dir.
    assets/bistro/           BISTRO (ORCA, CC-BY 4.0). МАСШТАБ 0.01 (сцена в
                            САНТИМЕТРАХ). exterior 2.83 М тр; текстуры —
                            ppm256 (scripts/tex_prep.sh).
    San_Miguel               4.96 М тр, 281 материал. Двор. Если слишком
                            подробна — ОТКУСЫВАТЬ самое детальное.

---

## КАНОНИЧЕСКАЯ КОМАНДА РАБОЧЕГО СВИПА (старый путь — эталон)

    build/pfield assets/bistro/exterior.obj 0.01 lev=7 nmu=2 xsweep \
      xthin=1e-2 xsemin=1e-2 xmatrho xit=40 xtol=1e-5 xreltol=1e-5

Ограничитель позитивности ВКЛЮЧЁН (ключ `xnolim` НЕ ставить — диагностический).
Cavity-пары: `cavity05.obj 1.0 lev=5 nmu=3 xsweep xfernosolid xmatrho
xthin=1e-2 xsemin=1e-2`. ПРОТОКОЛ СЛЕПКОВ (А1313): битовые `%.17g`-слепки —
ТОЛЬКО при `OMP_NUM_THREADS=1`.

Ключи действующие и НК перечислены в `START.md`; полная история ключей — в
архиве (по эпохам, в самих §-записях).

---
---

## §833 (09-10): ПЛАН — СНИМОК: ПИРАМИДА ШАГОВ ПЕРЕНОСА НА СИНТЕТИКЕ (ДО КОДА)

Структура — `STRUCTURE.md` (согласована 09-10). Это её шаг 1 (`§9.1`):
топология и бюджет БЕЗ поля и без свипа. Продюсер входа — существующий
разборщик `src/scene_obj.c` (Ш2, гейт пройден); старый путь как источник
ПОЛЯ подключается в шаге 3.

**ДЕЛАТЬ.**
- `src/nstruct/pyr.h`, `src/nstruct/pyr.c` — пирамида шагов переноса:
  регулярная сетка уровня-листа, биннинг треугольников (владение — по
  центроиду, РОВНО один кусок на треугольник; занятость — консервативно по
  bbox, лист может быть помечен несколькими bbox-ами), CSR кусков по
  клеткам, разреженные предки ×8, ТРИ состояния узла
  (ПУСТ/ДРОБЛЁН/АГРЕГАТ; агрегатов в этом шаге ноль по построению),
  счётчики бюджета, детекторы согласованности, НК-скремблинг.
- `tools/snap3.c` — `hz_obj_load` → пирамида → отчёт. Ключи:
  `lev=N` (уровень листа; умолчание — клетка ≈ габарит/64),
  `screwb` (НК: каждый 97-й кусок кладётся в клетку +1 по линейному
  индексу с заворотом), `scrfew` (НК-ослабление: скрестить только 1 кусок —
  калибровка детектора).
- Makefile: цель `snap3`. Существующие файлы, кроме Makefile, не правятся.
- Поле (E) в этом шаге — нулевой слот; его размер входит в бюджет как 4 Б.

**ВЫХОД** (печать `build/snap3 assets/synth/room.obj` и `cavity05.obj`):
габарит, V, T (после отброса вырожденных), ndegen/nquad; уровень листа и
размер клетки; кусков, занятых листьев (по центроиду и по bbox — РАЗНЫЕ
счётчики), внутренних узлов, узлов по состояниям; кусков на клетку —
медиана/p99/max; байтов по статьям (узлы, CSR, куски, вершины+треугольники)
и всего; байтов на кусок; детекторы (несогласованных кусков, непомеченных
занятых клеток, кусков в объявленно пустых клетках); ΔΣ площади;
время построения.

**ПРЕДСКАЗАНИЕ** (до прогона; интервалы от печатаемых T/L — не от файла):
- П1: ΔΣ площади ПОСИМВОЛЬНО 0 в чистой конфигурации на обеих сценах —
  суммы берутся в ОДНОМ порядке (по треугольникам; куски хранятся в
  порядке треугольников, CSR — отдельный counting-sort в другой массив).
- П2: внутренних узлов ∈ [занятые_листья/14, занятые_листья/7]
  (лес ×8: внутренних ≤ (L−1)/7; нижняя граница — занятость кластерами).
- П3: полный снапшот 30–45 Б на кусок (16 кусок + 4 CSR + ≤16/7 узлов +
  вершины/треугольники при V/T ≈ 0.5 и ~16 Б/треугольник).
- П4: кусков на клетку: медиана 1–4, p99 ≤ 64 (мелкие треугольники
  генераторов); max фиксируется без предсказания.
- П5 (НК `screwb`): детектор ловит ≥ floor(T/97) − 1 несогласованных;
  ΔΣ по клеткам > 0; чистый прогон — все три детектора ровно 0.
  Сила контроля честно: на room это ~51 кусок из ~5000 — контроль
  доказывает РАБОТОСПОСОБНОСТЬ детектора, не статистику биннинга.

**ПРИЁМКА:** П1–П3 в интервалах; все детекторы чистого прогона нулевые;
НК провалился предсказанным образом (на скрещённых кусках, не на всех);
бюджет печатается как «запись × счётчик» без скрытых констант.

**НЕГАТИВНЫЙ КОНТРОЛЬ:** `screwb` — детекторы (а) «кусок лежит не в клетке
своего центроида», (б) «занятая bbox-ом клетка не помечена», (в) «кусок в
объявленно пустой клетке» обязаны сработать; `scrfew` — один скрещённый
кусок ловится ровно одним детектором (а). Предсказанный провал: (а) ≥
floor(T/97)−1.

**УБИВАЕТ:**
- ΔΣ ≠ 0 в чистой конфигурации ⇒ биннинг теряет поверхность, структура не
  годится в этом виде;
- бюджет > 100 Б/кусок ⇒ норматив `STRUCTURE.md` нарушен вдвое;
- max кусков/клетку > 1000 на room ⇒ гранулярность поля мертва уже на
  синтетике, клип кусков обязателен немедленно — шаг на перепланирование.

### §833: АУДИТ ПЛАНА ДО КОДА — А1451…А1458

**А1451 [РАССУЖДЕНИЕ] Бюджет куска молчит о слоте роста E.** §1.2 стоящей
модели: `E` — полином, ЛИНЕЙНЫЙ по умолчанию (P0 даёт `~h` против `~h²`,
аргумент ×33). Бюджет `STRUCTURE.md` (12–16 Б) посчитан на КОНСТАНТНЫЙ E.
⇒ не блокирует снимок (E — нулевой слот), но таблица бюджета обязана
получить строку «линейный E: +8 Б» при входе поля; иначе ×33-довод
проиграется молча. Действие: заметка в доклад §833, правка
`STRUCTURE.md` — при шаге 3 (поле), не попутно.

**А1452 [РАССУЖДЕНИЕ] Подмена критерия выбора уровня.** В наброске уровень
листа выбирался «чтобы кусков было ≈ треугольников» — но кусков = T при
ЛЮБОМ уровне: биннинг не режет (клип — шаг гранулярности поля, ещё не
сделан). Критерий слеп по построению. Правлен: уровень — ключ `lev=`,
умолчание «клетка ≈ габарит/64», печать размера клетки в отчёте.

**А1453 [УРОК §5.4, отвалилась при проверке] «Хеш схлопывает сцену».**
Здесь хеша нет вовсе: регулярная сетка, целочисленный линейный индекс
`i + nx·(j + ny·k)` в int64. Прецедент §12.5 — записана, чтобы
непроверенное не выглядело проверенным.

**А1454 [РАССУЖДЕНИЕ] НК-индекс +1 у последней клетки — выход за сетку.**
Без заворота это UB на индексе. Правлен: `(c + 1) % ncells`.

**А1455 [ПРОЧИТАНО В КОДЕ, `src/scene_obj.h`] T для предсказаний — только
печатанный `nt`.** Загрузчик отбрасывает вырожденные треугольники
(`ndegen`) и разворачивает четырёхугольники (`nquad`), поэтому
`grep -c '^f'` (room: 4964) — не T. Предсказания переписаны от
ПЕЧАТАЕМЫХ величин.

**А1456 [РАССУЖДЕНИЕ] «ΔΣ ровно 0» требует одного порядка суммирования.**
CSR переставляет куски (порядок клеток); сумма площадей в порядке CSR
даст ΔΣ ~ 1e-16 от порядка суммирования, и проверка станет флаттерной.
Правлен: массив кусков хранится В ПОРЯДКЕ ТРЕУГОЛЬНИКОВ, CSR строится
отдельным counting-sort в собственный массив, ΔΣ считается по
треугольному порядку — тогда тождество битовое.

**А1457 [РАССУЖДЕНИЕ, класс «подмена величины»] «Занятые листья» — два
разных счётчика, а не один.** Владение куском — по ЦЕНТРОИДУ (ровно одна
клетка); занятость для марша — КОНСЕРВАТИВНАЯ по bbox (лист помечается
всеми пересекающими его bbox-ами; их ≥ владеющих). В плане было одно
слово на оба. Правлен: счётчики разделены по всему плану (выход,
предсказания, детекторы); детектор (б) сравнивает bbox-пометки; в
предсказании П2 база — bbox-листья.

**А1458 [РАССУЖДЕНИЕ] Сила НК.** Шаг 997 даёт 5 скрещённых кусков на
room — контроль доказывает только работоспособность детектора. Правлен:
шаг 97 (~51 кусок) + ключ `scrfew` (один кусок) для калибровки «ловит
ровно одним детектором»; оговорка о силе внесена в план (А14).

### §833: ДОКЛАД И АУДИТ ИСПОЛНЕНИЯ — СНИМОК ПОСТРОЕН И ЧИСТ; УБИВАЕТ СРАБОТАЛ ФОРМАЛЬНО И РАЗОБРАН

**Вывод.** Пирамида шагов переноса построена на room и cavity05 без единого
нарушения инвариантов: ΔΣ площади ПОСИМВОЛЬНЫЙ ноль и полная биекция
кусок↔треугольник на всех 8 конфигурациях, все детекторы нули, НК провалился
предсказанным образом (52/52/0/9). Носитель поля держит норматив: кусок+CSR
= **20 Б/кусок константой**, независимо от уровня клетки. **Формально
сработал УБИВАЕТ** (>100 Б/кусок на cavity05 lev6 = 353 и room lev7 = 305):
разбор восьми точек кривой бюджета(lev) показывает, что порог задела
ИНДЕКСНАЯ статья при клетке в 4 раза мельче треугольников — метрика плана
смешала носитель поля с индексом. Вердикт по УБИВАЕТ — за пользователем.

**Числа** (замер, build/snap3; время построения 0.002–0.005 с):

| | room (8.4×2.6×6.4 м, nt=4964) | cavity05 (3.2 м, nt=2208) |
|---|---|---|
| листья bbox/с кусками (lev=6) | 20991 / 779 | 47764 / 2112 |
| внутренних узлов | 4238 (7 уровней) | 10149 |
| кусков на клетку: медиана/p99/max | 4 / 41 / 57 | 1 / 3 / 3 |
| бюджет, Б/кусок по lev | 22.5 / 31.0 / 84.4 / 305.3 (lev 4…7) | 22.3 / 31.4 / 78.7 / 353.1 (lev 3…6) |
| НК screwb: детекторы (а)/(а-csr)/(б)/(в) | 52 / 52 / 0 / 9 | — |
| НК scrfew | 1 / 1 / 0 / 0 | — |

**Аудит исполнения.**
1. П1 ПОПАДАНИЕ (ΔΣ=0 посимвольно, биекция полная, обе сцены, все lev).
   П4 ПОПАДАНИЕ (медианы 4 и 1 в интервале 1–4; p99 41 и 3 ≤ 64).
   П5 ПОПАДАНИЕ (предсказано ≥51, поймано 52; (в)=9 не предсказывалось —
   9 соседей из 52 оказались пустыми клетками; scrfew ловится одним (а)).
2. **П2 ПРОМАХ**: внутренних 4238 > L/7 = 2999. Опровергнуто допущение
   объёмного заполнения: занятость ПОВЕРХНОСТНА, уровень сжимается ×~5,
   а не ×8. Эмпирика по 8 точкам: internal/L = 0.20…0.30, т.е. рабочая
   граница internal ≈ L/5…L/3, и она ЗАМЕРЕНА, а не выведена.
3. **П3 ПРОМАХ на умолчании, ПОПАДАНИЕ при согласовании**: при клетке
   ≈ размеру треугольника (room lev4–5, cavity lev3–4) — 22–31 Б/кусок
   (интервал 30–45 на границе). Состав бюджета: кусок 16 + CSR 4 = 20 Б
   НЕ зависят от lev (подпись артефакта: третья подпись сработала);
   лист-статья растёт ~×3.2 на уровень — закон (треугольник/клетка)²
   для поверхностной занятости.
4. **УБИВАЕТ (бюджет >100) — сработал формально, разбор снял механику**:
   конфигурации-нарушители (cavity lev6, room lev7) имеют клетку в 4 раза
   мельче треугольников сцены — вне осмысленной гранулярности шага 1
   (клипа кусков нет, поле нулевое). Норматив STRUCTURE.md (§2.3) относится
   к НОСИТЕЛЮ и держится точно; индексная статья — функция выбранной
   гранулярности, её норматив в STRUCTURE.md ОТСУТСТВУЕТ и обязан быть
   дописан. Переписывать предсказание задним числом не пишу: П3
   формулируется заново как «20 Б/кусок на носитель + индекс по закону
   (треугольник/клетка)², замеренный настоящим шагом».
5. Слепок тождества (ΔΣ-печать %.17g) не менялся ни от lev, ни от НК-ключей
   на чистых конфигурациях — где обязан меняться (screwb), меняются
   детекторы, не площадь: куски не двигаются, ссылки ломаются.
6. НАШ ДЕТЕКТОР ПОЙМАЛ НАШ ДЕФЕКТ ДО НК (А1461): первая сборка CSR без
   явной префикс-суммы дала 4861 разладок на чистом прогоне room —
   детектор (а) не слеп и смотрит в нужную сторону; починено, прогон
   переснят целиком.
7. Замер НЕ показывает: поля и свипа нет (E — нулевой слот) — паритет со
   старым недоказуем этим шагом; бюджет на сцене, где поле порежет крупные
   треугольники клипом; агрегаты (enum есть, механизма нет); время
   построения к городу не экстраполировать (синтетика 0.002–0.005 с).
8. Устарело: таблица бюджета STRUCTURE.md §2.3 — дописать строку
   «индекс: 20 Б/кусок × (треугольник/клетка)², замерено §833» и строку
   А1451 про слот роста E. Прежние доклады не затронуты (старый код не
   правился, кроме Makefile).

**Находки шага:**
- **А1459 [ЗАМЕРЕНО]** П2 промах: поверхностная занятость сжимается ×~5 на
  уровень, не ×8; рабочая граница internal ≈ L/5…L/3 по 8 точкам.
- **А1460 [ЗАМЕРЕНО]** УБИВАЕТ бюджета: нарушители — конфигурации с клеткой
  в 4 раза мельче треугольников; носитель 20 Б/кусок не зависит от lev;
  норматив индекса в STRUCTURE.md отсутствует — дописать.
- **А1461 [ЗАМЕРЕНО]** детектор (а) поймал дефект сборки (префикс-сумма) на
  чистом прогоне — контроль не слеп.
- **А1462 [ЗАМЕРЕНО, минимальный воспроизводитель]** CBMC флагует
  «конечное/положительное ⇒ не NaN» на двухстрочном примере
  (`/tmp/claude/fmt/cbmcrepro.c`) — ограничение модели CBMC, не кода;
  настоящие находки CBMC (NULL, NaN-вход, деление на ноль,
  `INT64_MIN % -1`) починены guard'ами. Остаточные pointer-находки
  `hz_pyr_leaf_index` — класс «нет тела для размера объекта при голом
  входе», лечится CBMC-харнессом как `tests/cbmc_sceneobj.c` — долг.

**СЛЕДУЮЩИЙ ШАГ** (после вердикта по УБИВАЕТ-у А1460): §9.2 STRUCTURE.md —
МАРШ: три состояния узла в обходе, порядок формулой, сверка с марш-эталоном
старого; НК scramble занятости.

### §833: ВЕРДИКТ ПОЛЬЗОВАТЕЛЯ ПО УБИВАЕТ-у А1460 (09-10)

**А1463 [ПОЛЬЗОВАТЕЛЬ] УБИВАЕТ снят: замер вне рабочего режима структуры.**
Формулировка пользователя: «это означает какой-то глюк; новая структура
достаточно предсказуема по свойствам». Разбор соглашается и добавляет
механизм: в полном конвейере клип кусков СВЯЗЫВАЕТ кусок с клеткой
(инвариант «δ — одна ручка»), кусок ≤ клетка ⇒ bbox помечает ≤ 2³ клеток,
индексный взрыв ~ (треугольник/клетка)² в режиме клипа НЕДОСТИЖИМ; его
показал только шаг 1 (клипа ещё нет) на конфигурации клетка = треугольник/4.
Следствие для плана: следующий шаг с полем обязан держать кусок ≤ клетку —
это уже записано как клип гранулярности поля.

**Действия:** таблица бюджета `STRUCTURE.md` §2.3 дополнена индексной
статьёй с замеренным законом; МАРШ (§9.2) — следующий шаг.

---

## §834 (09-10): ПЛАН — МАРШ: ТРИ СОСТОЯНИЯ УЗЛА В ОБХОДЕ (ДО КОДА)

STRUCTURE.md §9.2. Обход направления ω по пирамиде §833: от корня, дети в
порядке слэбов (проекция центра клетки на ω), ДРОБЛЁН — спуск, АГРЕГАТ —
стоп и одно событие, ПУСТ (отсутствует в разреженном хранении) — прыжок.
Поле по-прежнему нулевое: шаг меряет ПОРЯДОК и ЦЕПЬ, не свет.

**ДЕЛАТЬ.**
- `src/nstruct/pyr.c`: `hz_pyr_march(py, omega, opts)` — обход с счётчиками
  (посещено листьев, посещено узлов, событий АГРЕГАТ, инверсий слэба);
  отметки посещений — битовая карта листов (время, не бюджет).
  `hz_pyr_mark_aggr(py, every)` — НК-инструмент: пометить каждый `every`-й
  внутренний узел АГРЕГАТ (включая корень при every=1).
- `tools/snap3.c`: ключ `march` — прогон канонических 14 направлений
  (±x, ±y, ±z, 8 диагоналей (±1,±1,±1)/√3) с печатью по каждому: цепь
  (посещено узлов), листов, событий, инверсий; суммарная строка.
  Ключ `marchinv` — НК: порядок детей по x инвертирован.
  Ключ `aggrat=N` — пометить АГРЕГАТ и мерить прыжки.

**ВЫХОД.** На room и cavity05 (lev=6, канон снимка): по каждому из 14
направлений — листов посещено, узлов в цепи, инверсий слэба, событий;
суммарно цепь/листья; эффект `aggrat` (листья/события при N = 8, 64, 1);
время.

**ПРЕДСКАЗАНИЕ.**
- М1: посещений листьев ровно nleaf на каждое направление — ноль потерь и
  ноль дублей (обход эквивалентен множеству занятых листьев).
- М2: инверсий слэба 0 на всех 14 направлениях (последовательность центров
  НЕУБЫВАЮЩАЯ по проекции; равенства-связи разрешены).
- М3: цепь/листья ∈ [1.0, 3.0] на синтетике (верхние уровни разрежены,
  накладные узлы ≤ 2 на лист).
- М4 (НК marchinv): инверсий > 0, доля нарушенных соседних пар ∈ [5 %, 50 %].
- М5 (aggrat=1, корень АГРЕГАТ): листов 0, событий 1 — прыжок срабатывает.

**ПРИЁМКА.** М1–М3 в допуске; НК провалился предсказанно (М4, М5).

**НЕГАТИВНЫЙ КОНТРОЛЬ.** marchinv — предсказанный провал М2 (инверсии);
aggrat=1 — предсказанный провал М1-счётчика листьев (0 вместо nleaf) при
корректном числе событий. Сила: НК ломает РОВНО тот инвариант, который
проверяет (порядок и stopping, не множество).

**УБИВАЕТ.**
- потери/дубли листьев в чистом прогоне ⇒ обход не эквивалентен множеству;
- инверсии в чистом прогоне ⇒ порядок не топологичен, свип на нём не
  сойдётся, МАРШ не годится как порядковая машинерия;
- aggrat не меняет число посещений ⇒ состояния не влияют на обход.

### §834: АУДИТ ПЛАНА ДО КОДА — А1464…А1467

**А1464 [РАССУЖДЕНИЕ] Сила М2: монотонность центров слабее топологичности.**
У диагональных соседей слэб-интервалы клеток перекрываются — частичные
зависимости, порядок между ними свободен, их разрулит итерация свипа.
Шаг доказывает монотонность ФОРМУЛЫ порядка (центры не убывают), не
разрешение перекрытий. Записано, чтобы не приписать шагу лишнее.

**А1465 [ПРОЧИТАНО В КОДЕ, `src/transport/dirs3.h`] 14 направлений —
испытатель, не проектный набор.** Ординаты проекта — произведение
гауссовых узлов, НЕ октантно-зеркальное; формульные зеркальные
перестановки (4×8) относятся к будущему набору. МАРШ меряет порядок на
осях и диагоналях как испытание, переносить вывод на набор ординат нельзя.

**А1466 [РАССУЖДЕНИЕ] М2 на диагоналях — риск по построению, не только по
коду.** Поддеревья посещаются целиком подряд; если слэб-диапазоны двух
детей одного родителя перекрываются (диагональное ω), центр дальнего листа
первого поддерева может быть БОЛЬШЕ центра ближнего листа второго —
инверсия формулы «порядок детей по центрам». Это и есть меряемый пункт (1)
STRUCTURE.md §3. Правка плана ДО кода: М2 расщепляется — оси: 0 инверсий
(гарантировано доминированием оси); диагонали: предсказание 0 с объявленным
риском; срабатывание инверсий на диагоналях — НЕ смерть марша, а сигнал
лечения (упорядочение детей по слэб-ДИАПАЗОНАМ вместо центров), фиксируется
отдельной находкой. УБИВАЕТ-пункт «инверсии в чистом прогоне» остаётся
только для ОСЕЙ.

**А1467 [РАССУЖДЕНИЕ] М5 неточен: every=1 пометит ВСЕ внутренние узлы, а не
корень.** Правка: `mark_aggr(1)` означает ТОЛЬКО корень; ожидание — листов
0, событий 1. (every=8/64 — каждая N-я по массиву уровней, как записано.)

### §834: ДОКЛАД И АУДИТ ИСПОЛНЕНИЯ — МАРШ ТОПОЛОГИЧЕСКИ ЧИСТ НА 14 НАПРАВЛЕНИЯХ

**Вывод.** Обход направления по трём состояниям работает: на room и
cavity05 все 20991/47764 занятых листьев посещаются РОВНО по разу на
каждое из 14 направлений, накладные узлы цепи — **1.20–1.21 на лист**,
и главное — порядок СОСЕДНИХ ПО ГРАНИ клеток вдоль ω соблюдён без
единого нарушения на всех шести осях. НК провалились предсказанно:
marchinv ломает 36094 (room) / 82784 (cavity) зависимых пар,
aggrat=1 даёт 0 листьев и 1 событие, aggrat=8 съедает 53 % листьев
прыжками при 4102 событиях. Машина порядка и состояний готова к свипу.

**Числа** (замер, build/snap3):

| | room | cavity05 |
|---|---|---|
| листьев на направление / непосещённых | 20991 / 0 | 47764 / 0 |
| цепь/листья | 1.20 | 1.21 |
| зависимых пар нарушено (чисто) | 0 | 0 |
| НК marchinv: нарушено пар | 36094 | 82784 |
| НК aggrat=1 | 0 листьев, 1 событие | — |
| aggrat=8 | 9881 листьев, 4102 события | — |

**Аудит исполнения.**
1. М1 ПОПАДАНИЕ, М3 ПОПАДАНИЕ (1.20–1.21 в [1,3]), М5 ПОПАДАНИЕ.
2. **М2 в исходной формулировке ПРОМАХ и ПЕРЕФОРМУЛИРОВАН (А1468)**:
   монотонность проекции центра по ходу листьев дала 2–2.8 тысяч
   «инверсий» на направление — все на СВЯЗАННЫХ s-поддеревьях (равные
   слэбы, порядок свободен), ровно как предостерегал А1464. Замена:
   проверка порядка зависимых пар (соседние по грани вдоль ω, верховой
   раньше низового) — 0 нарушений в чистых прогонах. Это и есть
   критерий сходимости свипа; монотонность центров им не является.
3. М4: предсказана ДОЛЯ [5,50] % — знаменатель не измерен, доля не
   проверена (урок А1083, третий случай); проверен факт «36094≠0 при
   0 в чистом» — контроль срабатывает и сел на пол метрики (меняет
   только зависимые пары; множество листьев и цепь не трогает).
4. aggrat=8: 4768 «нарушенных» пар — это пары СКВОЗЬ агрегаты и пары с
   непосещёнными кусками (vindex=-1): при работающих агрегатах критерий
   М2 сужается на пары без агрегата между ними. В чистом прогоне — пусто.
5. Дефект пойман sanitizer'ом: scratch-массив order_pos лежал в общем
   ctx и затирался рекурсией — родитель после поддерева читал чужие
   позиции (OOB pos=10708 при cap=3273). Урок: у рекурсивного обхода
   scratch — только в кадре. Детектор М1 (битовая карта) при этом
   оставался чист и НЕ ловил порчу порядка — слепая сторона, названа.
6. Замер НЕ показывает: связи порядка с полем (свипа ещё нет);
   диагональные зависимые пары (проверены только осевые — для
   диагоналей ω зависимость тоже через ГРАНИ, набор пар шире — долг);
   цену марша на городе (синтетика, время не печаталось отдельно).
7. Устаревшего нет: шаг 1 не тронут, детекторы §833 не изменились.

**Находки:**
- **А1468 [ЗАМЕРЕНО]** метрика «монотонность центров» не есть
  топологичность: на связанных s-поддеревьях инверсии легальны;
  рабочая метрика — порядок зависимых (граневых) пар.
- **А1469 [ЗАМЕРЕНО]** у рекурсивного обхода scratch — в кадре, не в
  общем ctx: shared order_pos затирался, ASan поймал, М1-карта слепа
  к порядку.
- **А1470 [ЗАМЕРЕНО]** при агрегатах проверка порядка сужается на пары
  без агрегата между ними; аггрегат = легальная точка разрыва цепи.

**СЛЕДУЮЩИЙ ШАГ** (STRUCTURE.md §9.3): СВИП — итерации на новых массивах
(кусок+CSR+пирамида), диффузный перенос по 6 осям, паритет E со старым
путём на room; приёмка §5 STRUCTURE.md.

### §834-приписка (09-10): ДИРЕКТИВА — УБИВАЕТ НА ЭТОЙ ЛИНИИ НЕ ПИШЕТСЯ

Пользователь: «"Убивает" тут уже не нужен». В шагах новой структуры
(§833+, STRUCTURE.md) поле УБИВАЕТ из планов убирается; прошёл/не прошёл
решают приёмка и НК; промах предсказания — находка серии А и разбор, не
остановка. Причина: единственное реальное срабатывание (А1460) было
ложным — метрика смешала носитель с индексом. Записано в память
(feedback-kills-not-needed-new-structure); на старой линии §4 не отменяет
ничего.

---

## §835 (09-10): ПЛАН — СВИП №1 НА НОВЫХ МАССИВАХ (ДО КОДА)

STRUCTURE.md §9.3. Источниковые итерации на массивах §833 (куски+CSR+пирамида),
порядок — марш §834. Носитель E — float на куске, накопление в double.
Перенос на направление — ПОСЛЕДОВАТЕЛЬНЫЙ луч: L несётся вдоль цепи листьев,
тонкий поверхностный слой клетки (куски) ослабляет его с оптической толщиной
τ(ω) = Σ_p area_p·|ω·n_p| / A_⊥(ω), поглощённое идёт в E, излучение куска
(Le + ρ·E_prev/π)·(1−e^{−τ}) — обратно в луч. Это модель «среды-трубы» из
STRUCTURE.md §2.3 в простейшей форме: L — скаляр на направление, а не поле на
гранях; на однородных сценах она точна, в геометрии — дымовой тест машинерии.
Граневые потоки — следующий шаг. Прямого света и теней здесь нет — только
эмиссия кусков и ряд Неймана рассеяния.

**ДЕЛАТЬ.**
- `src/nstruct/sweep.c/h`: `hz_sw_run(py, opts)` — K итераций × 6 осевых
  направлений; порядок листов — обратная перестановка `visit_index` марша
  §834 (марш уже испытан); на листе — influx на куски по CSR
  (∝ area·|ω·n|), E_new пишется один раз за итерацию (float), L
  обновляется по цепочке. Площади кусков и |ω·n| — double, τ — double,
  E — float при записи.
- `tools/swee3.c`: OBJ → пирамида → свип; печать по итерациям: E_avg,
  Σ|E|·area (энергия), фактор ряда ΔE_{k+1}/ΔE_k; финал — сравнение с
  аналитикой замкнутой полости Le/(1−ρ). Ключи: `it=N` (умолчание 20),
  `rho=F` (переопределить albedo; 0 — НК), `le=F` (эмиссия, умолчание 1),
  `tau0` (НК: отключить поглощение слоя), `noprop` (НК: L не переносится).
- Прогон: cavity05 (закрытая оболочка, аналитика есть), room (дымовой).

**ВЫХОД.** Таблица по итерациям (E_avg, энергия, фактор ряда) для
cavity05 при ρ из .mtl и при ρ=0; финальная строка: E_avg против
Le/(1−ρ), ошибка %; строка НК tau0/noprop.

**ПРЕДСКАЗАНИЕ.**
- S1 (cavity05, ρ≈0.5, Le=1, it=20): E_avg ∈ 2·[0.85, 1.15]
  (аналитика 1/(1−ρ)=2); фактор ряда ∈ ρ±0.15.
- S2 (НК rho=0): E² == E¹ ПОСИМВОЛЬНО (стационар с первой итерации).
- S3 (НК tau0): E_avg == 0 ПОСИМВОЛЬНО на всех итерациях (слой ничего
  не поглощает — негде родиться полю).
- S4: энергия сохраняется тождеством схемы (слабая проверка, сказано
  заранее: она не может упасть, если код совпадает со схемой).
- S5 (room): E_avg > 0 и конечна; распределение кусков по E — p90/max
  фиксируются без предсказания (сцена неоднородна, скалярный луч —
  дымовой режим).

**ПРИЁМКА.** S1–S3 в допуске; S4 напечатан; ряд монотонен по итерациям.

**НЕГАТИВНЫЙ КОНТРОЛЬ.** rho=0 ломает ряд (стационар мгновенный, а не
фактор ρ); tau0 убивает поле до нуля. Оба — предсказанные провалы
физики, а не эквивалентности: они доказывают, что отклик и перенос
действительно участвуют в счёте.

**ОТКРЫТЫЕ МЕСТА (не в этом шаге):** граневые потоки вместо скалярного
луча; диагональные направления; паритет E со старым путём (требует
одинакового определения источника — отдельно); тени/видимость.

### §835: АУДИТ ПЛАНА ДО КОДА — А1471…А1473

**А1471 [РАССУЖДЕНИЕ] Двусторонность эмиссии не была определена.** Кусок
оболочки светит в обе стороны; аналитика полости — на СТОРОНУ. Равновесная
E_avg схемы при двусторонней эмиссии Le — 2/(1−ρ)·Le, при односторонней —
1/(1−ρ)·Le; плюс угловая погрешность 6-осевой квадратуры. Правка: Le — в
ОБЕ стороны; S1 переформулирован — допуск только на ФАКТОР РЯДА (ρ±0.15)
и НК; сравнение E_avg с аналитикой — ИЗМЕРЕНИЕ угловой погрешности
квадратуры, печатается без допуска.

**А1472 [РАССУЖДЕНИЕ] Порядок из марша: обратная перестановка trivial** —
order[k] = лист с visit_index == k, O(n) обратным проходом.

**А1473 [РАССУЖДЕНИЕ] τ может превышать 1** (до 57 кусков на клетку на
room lev6): слой непрозрачен, e^{−τ}→0, луч terminируется — по физике слоя
законно; числа печатаются, случай не отдельный.

### §835: ДОКЛАД И АУДИТ ИСПОЛНЕНИЯ — СВИП №1 РАБОТАЕТ: РЯД НЕЙМАНА С ФАКТОРОМ ρ, НК СРАБОТАЛИ

**Вывод.** Свип на новых массивах (куски+CSR+пирамида, порядок из марша
§834) считает источниковые итерации: на замкнутой полости ряд сходится с
фактором **0.5087 ≈ ρ = 0.5** (предсказание ρ±0.15 — попадание), равновесие
стационарно; все три НК провалились предсказанно (rho=0: рециркуляция ровно
ноль, ряд стационарен с первой итерации; tau0 и noprop: поле тождественно
нуль). Room сходится дымовым режимом (фактор 0.37, E_avg конечна).
Абсолютный уровень равновесия — **2.88× двусторонней аналитики** — по
А1471 это ИЗМЕРЕНИЕ дискретизации (6-осевая квадратура переоценивает
облучённость: Σw|cosθ| = 4π/3 против π у полюсных нормалей; скалярный луч
без граневых потоков), не допуск шага.

**Числа** (замер, build/swee3, cavity05 nt=2208, клетка 0.05 м, Le=1):

| конфигурация | ряд E_avg | фактор | баланс излуч/погл/рецикл/потери |
|---|---|---|---|
| ρ=0.5, it=16 | 5.94 → 11.5266 | **0.5087** | 52.8 / 100.8 / 48.0 / 0.03 |
| НК ρ=0 | 5.9423 ×4 неизменно | 0 | 52.8 / 52.8 / **0.0000** / 0.015 |
| НК tau0 | 0 ×3 | — | все нули |
| НК noprop | 0 ×3 | — | излучено 52.8, поглощено 0 |
| room ρ=0.5, it=10 | 4.05 → 5.5881 | 0.368 | 50.6 / 66.4 / 16.2 / 0.14 |

**Аудит исполнения.**
1. S1 (фактор ρ±0.15) ПОПАДАНИЕ; S2 ПОПАДАНИЕ (равенство напечатанных
   значений до 4 знаков + рециркуляция ровно 0; посимвольная сверка
   печатью не покрыта — печатался %.4f, отмечено); S3 ПОПАДАНИЕ ×2;
   S5 ПОПАДАНИЕ (конечна, сошлась); S4 напечатана — тождество схемы,
   слабая по объявлению.
2. Абсолютный уровень 2.88× аналитики — ПРОМАХ наивного ожидания
   «выйдет на аналитику», предсказанного интервала не имел (А1471 снял
   допуск заранее — прав оказался). Разбор: (а) 6-осевая квадратура
   переоценивает irradiance (Σw|cos| по полюсной нормали 4π/3 против π);
   (б) скалярный луч без граневых потоков смешивает углы внутри слоя.
   Оба лечения — в шаге граневых потоков, не правкой коэффициентов.
3. По пути пойманы три собственных дефекта, каждый — числом:
   (а) потерянный косинус в influx (доля вместо проекции) — ряд
   расходился с фактором 1.80; (б) ρE/π против двусторонней конвенции —
   фактор 1.015 (медленная расходимость); (в) `nn` от нормали куска 0 —
   все площади 0.5, оболочка прозрачна, E_avg 0.179. Каждый виден на
   аналитике полости ДО приёмки — фикстура сработала как задумана.
4. Замер НЕ показывает: граневых потоков и теней (скалярный луч);
   диагональных направлений; паритета со старым путём (определение
   источника не выровнено — следующий шаг); абсолютной калибровки
   квадратуры (это измерение, продолжение в шаге качества).
5. Устаревшего нет: §833/§834 не тронуты (одна правка контракта
   `visit_index` — «номер посещения с 1», задокументирована в pyr.h).

**Находки:**
- **А1474 [ЗАМЕРЕНО]** равновесие скалярно-лучевой схемы = 2.88×
  двусторонней аналитики: квадратурный перегиб + отсутствие граневых
  потоков. Число — точка отсчёта для шага граневых потоков.
- **А1475 [ЗАМЕРЕНО]** контракты без единиц в формулировке плана
  (односторонняя/двусторонняя E, нормировка радианса) — три дефекта
  из трёх пойманы аналитикой полости; конвенции теперь записаны в
  sweep.h.

**СЛЕДУЮЩИЙ ШАГ** (§9.3 продолжение): граневые потоки вместо скалярного
луча (поле на гранях клеток, 6 направлений), та же аналитика полости —
цель: абсолютный уровень к аналитике, потом паритет со старым путём.

---

## §836 (09-10): ПЛАН — СВИП №2: КОЛОНКИ ВМЕСТО СКАЛЯРНОГО ЛУЧА (ДО КОДА)

STRUCTURE.md §9.3 продолжение. Дефект №1 (А1474): скалярный луч mixes
колонки — кусок получал свет чужих колонок, а абсолютный уровень уехал в
2.88× аналитики. Правка: на осевое направление ω занятые листья
группируются в КОЛОНКИ (две поперечные координаты), внутри колонки
обход по оси со знаком; L — скаляр КОЛОНКИ, не всего домена. Это DG0 с
граневым потоком в простейшей форме: соседние по лучу куски связаны,
поперечные колонки независимы. Связь направлений — по-прежнему через
E_prev (источниковые итерации, Жакоби). Марш §834 в свипе №2 не
участвует (порядок колонок строится сортировкой листов; роль марша —
диагонали и агрегаты, позже). Марш и агрегаты не выбрасываются — они
остаются общей машинерией обхода.

**ДЕЛАТЬ.**
- `src/nstruct/sweep.c`: ключ `mode=col` (новый, умолчание) / `mode=scalar`
  (старый, для контраста). Колонка: для оси a с шагом s — col = id/s,
  позиция = id mod s (для −a позиция обращается). Сортировка (col, pos)
  по 6 направлениям, один раз.
- `tools/swee3.c`: печать обоих режимов подряд на одной сцене +
  отношение к аналитике; ключ `mode=`.
- Прогон: cavity05 (ρ=0.5 и НК), room (дымовой).

**ВЫХОД.** Для cavity05: E_avg(scalar) vs E_avg(col) против аналитики
2ст=4.0; фактор ряда обоих; НК-строки; room-строка.

**ПРЕДСКАЗАНИЕ.**
- F1: E_avg(col) ∈ [3.0, 7.5] — центр модели ~4.2–5.5 против аналитики
  4.0 (разброс от ориентации нормалей при 6 направлениях); ожидание —
  отношение к аналитике СУЩЕСТВЕННО ближе 1, чем 2.88 скалярного.
- F2: фактор ряда col ∈ [0.3, 0.6].
- F3: НК rho=0 (col): recycled ровно 0, ряд стационарен.
- F4: НК tau0 (col): E ≡ 0.
- F5: время итерации col ≤ 2× scalar (та же сложность, плюс сортировка
  единожды).
- F6: room (col): сходится, конечна.

**ПРИЁМКА.** F1–F4 в допуске; скаляр и колонки на одной сцене печатаются
парой — улучшение видно числом, не словами.

**НЕГАТИВНЫЙ КОНТРОЛЬ.** Те же rho=0/tau0 в режиме col — доказывают, что
связь направлений и слой участвуют в счёте в новой раскладке, а не только
в старой.

**ОТКРЫТЫЕ МЕСТА (не в этом шаге):** диагональные направления (там марш
и агрегаты возвращаются в строй); паритет со старым путём (выравнивание
источника); абсолютная калибровка квадратуры — F1 есть её измерение.

### §836: АУДИТ ПЛАНА ДО КОДА — А1476…А1478

**А1476 [РАССУЖДЕНИЕ] Вход- и выход-side куски: входной кусок колонки
получает ноль от ЭТОГО направления (луч входит тёмным), но получает от
ПРОТИВОПОЛОЖНОГО (там он выходной).** По паре осей ±a каждый кусок
ровно один раз поглощает — کتابка учтена, F1-интервал это покрывает.

**А1477 [РАССУЖДЕНИЕ] Касательные нормали.** Кусок с |n·ω| ≈ 0 для
направления a почти не поглощает и не излучает в него — косинусная
весовка делает это корректно автоматически; отдельного случая нет.

**А1478 [ПРОЧИТАНО В КОДЕ] Сортировка колонок — qsort пар (col,pos)
на nleaf, 6 раз при старте**: O(n log n) единожды, в итерациях —
чистый проход; F5 не под угрозой.

### §836: ДОКЛАД И АУДИТ ИСПОЛНЕНИЯ — КОЛОНКИ ПОДНЯЛИ АБСОЛЮТНЫЙ УРОВЕНЬ К АНАЛИТИКЕ: 0.94× ПРОТИВ 2.88×

**Вывод.** Граневый поток в простейшей форме (L — скаляр КОЛОНКИ, а не
домена) сократил расхождение с аналитикой замкнутой полости втрое:
**E_avg = 3.7628 = 0.94× двусторонней аналитики (4.0)** против 2.88×
у скалярного луча §835, и потери луча упали до **0.0000** — колонки
замкнуты энергетически. Фактор ряда 0.367 (интервал [0.3, 0.6] —
попадание). НК в новом режиме сработали: rho=0 — рециркуляция ровно
ноль, стационар; tau0 — поле тождественно нуль. Скалярный режим
воспроизвёлся посимвольно (5.9423 → 11.5266) — регрессии нет.

**Числа** (замер, build/swee3, cavity05, ρ=0.5, Le=1, it=16):

| режим | E_avg | ×аналитики(2ст) | фактор | потери |
|---|---|---|---|---|
| scalar §835 | 11.5266 | 2.88 | 0.509 | 0.033 |
| **col §836** | **3.7628** | **0.94** | 0.367 | **0.0000** |
| НК col ρ=0 | 2.6836 стационар | — | 0 | 0.0000 |
| НК col tau0 | 0 | — | — | 0 |
| room col ρ=0.5 | 0.7527, сошлось (фактор 0.15) | — | — | 0.04 |

**Аудит исполнения.**
1. F1 ПОПАДАНИЕ (3.76 ∈ [3.0, 7.5]; центр модели 4.2–5.5 — фактическое
   чуть ниже: колонная изоляция съедает касательный обмен соседних
   колонок, что для полости и означал бы 1.0× — остаток −6 %
   не атрибуирован, назван). F2 ПОПАДАНИЕ. F3, F4 ПОПАДАНИЕ.
   F6 ПОПАДАНИЕ. F5 (время ≤2×) — отдельного таймера нет; оба режима
   в одном прогоне укладываются в доли секунды — зафиксировано как
   НЕ ИЗМЕРЕНО, долг.
2. room col: E_avg 0.75 против scalar 5.59 — в 7 раз ниже: в комнате
   свет НЕ ДОЛЖЕН идти сквозь стены, и колонная изоляция это впервые
   показала (скалярный луч перебрасывал свет через всю сцену).
   Это качественный сигнал правильности колонок, не количественный
   (теней и первого пересечения всё ещё нет).
3. Замер НЕ показывает: диагональные направления; паритет со старым
   путём; эффект агрегатов на перенос; абсолютную калибровку
   квадратуры при большем ND (6 осей — минимум).
4. Устаревшего нет; scalar-режим сохранён как эталонный контраст.

**Находки:**
- **А1479 [ЗАМЕРЕНО]** колонная изоляция — главный член абсолютной
  погрешности скалярного луча (2.88× → 0.94×); остаток −6 % и
  расхождение фактора (0.367 против ρ=0.5) — цена (1−T)-модели слоя
  при τ≫1: слой непрозрачен и поглощает долю (1−T)≈1, ряд переводится
  не ρ, а ρ·(доля переизлучения в колонку) — для полости это занижает
  рекурсию. Лечение — граневые потоки на гранях клетки (следующий
  уровень той же линии), не правка коэффициентов.
- **А1480 [ПРОЧИТАНО В КОДЕ]** clang-tidy на standalone-.h падает
  экологически (PCH под давлением памяти, 85/125 ГБ занято); гейты
  gcc+cppcheck чисты; advisory atoi/include-cleaner — известный класс.

**СЛЕДУЮЩИЙ ШАГ:** выбор за пользователем — (а) диагональные направления
и возвращение марша в свип (ND 6 → 18/26, квадратура), (б) паритет со
старым путём на room (выравнивание источника), (в) прибор трафика §5
STRUCTURE.md (ГБ/с итерации против бумажного бюджета).

---

## §837 (10-09): ПЛАН — ПАКЕТ: ТРАФИК §5 / ДИАГОНАЛИ / ПАРИТЕТ (ДО КОДА)

Три шага подряд, порядок: трафик (прибор) → диагонали (расширение ND) →
паритет (сверка со старым путём). Паритет замыкает пакет: он меряет свип
№2 с диагоналями — представительную конфигурацию.

**ЧАСТЬ Т — ТРАФИК (STRUCTURE.md §5, приёмка полосы).**
- ДЕЛАТЬ: `scripts/gen_grid.sh N` — сетка N×room (сдвиг координат) →
  `assets/synth/gridN.obj`; счётчики байт в `hz_sw_run` (E r/w 8 +
  CSR 4 на визит куска; заголовок листа 12 + порядок 4 на лист) + время
  итерации; swee3 печатает counted МБ/итерацию и эффективную ГБ/с.
- ПРЕДСКАЗАНИЯ. T1: на grid1000 (nt ≈ 5e6) counted ≥ 100 МБ/итерацию,
  полоса ∈ [8, 45] ГБ/с; приёмка §5 (≥20 ГБ/с) применяется при
  counted ≥ 200 МБ/итерацию. T2: cavity counted < 5 МБ — кэш-режим,
  полоса непоказательна (порог записан до прогона).

**ЧАСТЬ D — ДИАГОНАЛИ (ND 6 → 26).**
- ДЕЛАТЬ: 12 рёберных (±1,±1,0)/√2 и 8 угловых (±1,±1,±1)/√3 направлений,
  вес всех 26 — 4π/26; порядок — сортировка (ЛИНИЯ, слэб): линия
  диагонали = пара поперечных разностей (напр. (j−i, k) для (1,1,0)),
  слэб = сумма индексов вдоль луча; в линии слэб строг, колонки
  независимы. Ключ `dirs=6|26`.
- ПРЕДСКАЗАНИЯ. D1: cavity col 26: E_avg/аналитика(2ст) ∈ [0.95, 1.35]
  (Σw|cos| ≈ π±5 % против 4π/3 у 6 осей). D2: фактор ∈ [0.25, 0.6];
  НК rho=0/tau0 — прежние предсказания. D3: время/итерацию 26/6 ∈ [2.5, 5].

**ЧАСТЬ P — ПАРИТЕТ СО СТАРЫМ ПУТЁМ (room).**
- ДЕЛАТЬ: ключ `xdumpe=ФАЙЛ` в pfield — дамп E на элемент после свипа
  (аддитивный, умолчания не меняет); прогон старого канона на room с
  `xobjpiece` (куски = треугольники, биекция с новыми кусками §833) и
  `keonly` (источник — Ke материалов; room.mtl получает Ke=Le — фикстура
  сцены); swee3 ключ `cmp=ФАЙЛ` — сличение по индексу куска.
- ПРЕДСКАЗАНИЯ. P1: медиана отношения E_new/E_old ∈ [0.5, 2]; доля
  кусков в ×2 ≥ 50 %; площадь-взвешенное среднее ∈ [0.4, 2.5]
  (интервалы широкие честно: дискретизации разные — DG1-разрез против
  труб-колонок; цель шага — ЛОКАЛИЗОВАТЬ расхождение, а не совпасть).
- НЕГАТИВНЫЙ КОНТРОЛЬ (пакетный): (н1) pfield-дамп против повторного
  pfield-дампа — побитово (прибор не вносит разницы); (н2) сличение со
  СОДВИГОМ индексов на 1 — медиана обязана уехать (детектор не слеп).

**ПРИЁМКА ПАКЕТА:** каждая часть — свои F/T/P-числа; доклад по частям.

### §837: АУДИТ ПЛАНА ДО КОДА — А1481…А1484

**А1481 [РАССУЖДЕНИЕ] Линия диагонали и идентификатор: для (1,1,0)
линия = (j−i, k); разность может быть отрицательной — смещение на
nx перед упаковкой в ключ. Для (1,1,1) линия = (j−i, k−j), смещения
на (nx, ny). Слэб в линии строг (+2 на шаг (1,1,0), +3 на (1,1,1)) —
связей внутри линии нет по построению.**

**А1482 [ПРОЧИТАНО В КОДЕ] xobjpiece — исследовательский ключ** (§794:
физика идеальна, цена не взята, элементы ×12). Для паритета на room
цена не важна — важна биекция кусок↔треугольник; канон (DC) паритету
не годится (куски ≠ треугольники, А1471-класс подмены).

**А1483 [РАССУЖДЕНИЕ] Ke-фикстура room.mtl — правка ФАЙЛА СЦЕНЫ.**
Коммитить изменённый room.mtl нельзя (общая фикстура шагов §833+);
сделать КОПИЮ room_ke.obj/room_ke.mtl генератором скрипта — оригинал
не трогается. НК-прогон старого пути на КОПИИ, нового — тоже (одинаковые
входы паритета).

**А1484 [ЗАМЕРЕНО заранее] kd room: площади-взвешенное 16.03 при
Σarea 8.4·… — kd ≈ 0.55 среднее; ρ паритета берётся из тех же kd
обоими путями — согласование альбедо не требуется отдельно.**

### §837: ДОКЛАД (ЧАСТИ T И D) — ПРИБОР ПОКАЗАЛ ГЛАВНОЕ: ПОЛОСА УПЁРЛАСЬ В РАСКЛАДКУ; §836 ВСКРЫТ

**Вывод.** Три находки, каждая дороже своих предсказаний. (1) **Трафик:
counted 1482.9 МБ/итерацию на grid1000 (nt=4.96 М) — но эффективная
полоса 0.42 ГБ/с** против приёмки ≥20 (T1 ПРОМАХ): обход (линия, слэб)
рассеивает доступ по leaf_id, свип латентен, не потоков — болезнь старой
структуры воспроизвелась в новой, и прибор §5 её поймал первым же
прогоном. (2) **§836 вскрыт:** ось ±x была мертва (col = id при s = 1 —
сброс L на каждом листе), «0.94× аналитики» держалось на компенсации
двух ошибок; честный уровень колонн-модели 0.19× (nd=6) … 0.35× (nd=26)
— F1 ПРОМАХ, модель «слой (1−T) + скаляр линии» структурно недодаёт
(рекурсия 0.07–0.15 против ρ=0.5: излучение в некаррируемые направления
теряется между проходами). (3) **Стоимость 26 направлений = ровно 26/6**
(D3 попадание: 0.83 с против 0.19 с), НК rho=0/tau0 — рециркуляция ровно
ноль / все нули, в обоих режимах.

**Числа** (замер):

| сцена, режим | counted МБ/итер | ГБ/с | E_avg | ×аналитики |
|---|---|---|---|---|
| cavity nd=6 | 4.52 | 0.39 | 0.1868 | 0.09×2ст |
| cavity nd=26 | 19.61 | 0.40 | 0.7078 | 0.18×2ст |
| grid1000 nd=26 | **1482.9** | **0.42** | — | — |
| НК col ρ=0 (6 и 26) | — | — | стационар | рецикл 0.0000 |
| НК col tau0 | — | — | 0 | — |

**Аудит исполнения.**
1. T1: первая половина (counted ≥ 100 МБ) попадание; полоса ПРОМАХ —
   и это самый ценный результат шага: билет на §5 не пробит, лечение
   известно (переукладка CSR по линиям или Morton-плитки — раскладка,
   не алгоритм). T2 попадание (cavity кэш-режим).
2. D1 ПРОМАХ с разбором выше; D2 попадание частично (НК чисты, фактор
   0.0000 — стационар мгновенный, ряд не живёт: следствие той же
   недодачи света). D3 попадание ровно (4.33 = 26/6).
3. Дефект §836 (мертвая ось) найден СЕЙЧАС, а не в §836 — потому что
   §836 мерил уровень на 4 живых осях из 6, и компенсация совпала с
   аналитикой. Урок: совпадение с аналитикой при известном расхождении
   в модели — сигнал искать компенсацию, не праздновать.
4. Плотная битовая карта §833 заменена сортировкой пометок (grid-сцены
   не переживали ncells/8 байт); детекторы §833 на room — все нули,
   ΔΣ посимвольно — регрессии нет.
5. Замер НЕ показывает: граневых потоков (модель слоя та же); паритета
   со старым путём — часть P: приёмник cmp= в swee3 готов, продюсер
   (xdumpe в pfield + room_ke фикстура + прогон канона) — НЕ СДЕЛАН,
   переносится следующим ходом.
6. Устаревшее: §836-доклад (0.94×) больше не цитировать как уровень
   колонн-модели; таблица §5 STRUCTURE.md — приёмка ≥20 ГБ/с остаётся,
   к ней добавляется требование РАСКЛАДКИ (CSR-по-линиям/плитки).

**Находки:**
- **А1485 [ЗАМЕРЕНО]** line-сортированный обход латентен: 0.42 ГБ/с при
  1.48 ГБ/итерацию; лечение — переукладка (CSR по линиям/плитки).
- **А1486 [ЗАМЕРЕНО]** дефект §836: col = id при s = 1 — мертвая ось ±x;
  0.94× было компенсацией. Честный уровень колонн-модели 0.18–0.35×.
- **А1487 [ЗАМЕРЕНО]** модель «слой + скаляр линии» недодаёт рекурсию
  (0.07–0.15 против ρ): излучение в некаррируемые направления теряется.
  Лечение — граневые потоки на гранях клетки.

**СЛЕДУЮЩИЙ ШАГ:** (1) переукладка порядка (CSR по линиям) — после неё
полоса §5 мерится честно; (2) граневые потоки на гранях (закрывает
А1487 и абсолютный уровень); (3) часть P — xdumpe в pfield + прогон.

### §837-приписка (11-09): ДИРЕКТИВА — НЕ ПАРИТЕТ, А СКОРОСТЬ

Пользователь: «Должно быть не паритет, а намного быстрее, чем раньше».
Часть P (сличение со старым путём) переопределяется из приёмки в
необязательную диагностику; приёмки новой линии: (1) полоса ≥20 ГБ/с
(сейчас 0.42 — раскладка по линиям, лечение — переукладка CSR),
(2) аналитика полости по абсолютному уровню (после граневых потоков),
(3) балансы и НК. «Продюсер паритета» — старый путь как производитель
эталонного поля для сверки; как приёмка отменено, скорость мерится
прибором §5 и временем кадра. Записано в память
(project-sweep-structure-new).

---

## §838 (11-09): ПЛАН — ПЕРЕУКЛАДКА: МАТЕРИАЛИЗОВАННЫЙ ОБХОД (ДО КОДА)

А1485: обход (линия, слэб) латентен — 0.42 ГБ/с. Лечение раскладкой:
для каждого направления материалуется ПОХОДНЫЙ массив — листья в порядке
обхода, куски в порядке обхода (CSR-переукладка), cn = area·|ω·n|
вычисляется ОДИН раз на направление (не на каждой итерации), нормали и
площади из итераций уходят вовсе. Память: 26 × (12 Б·nt + 24 Б·nleaf)
≈ 3.7 ГБ на grid1000 — приемлемо. E_prev/Ed остаются общими (горячие,
кэш-резидентны). Prefetch следующих записей похода. Счётчик трафика —
под новую модель: слот куска 20 Б (cn 8 + p 4 + E r/w 8), лист 24 Б.

**ДЕЛАТЬ.** `src/nstruct/sweep.c`: wleaf/wcn/wpid по направлению,
prefetch, счётчики; swee3 — печать без изменений.

**ПРЕДСКАЗАНИЯ.**
- L1: cavity E ПОСИМВОЛЬНО равна §837 (cn — та же формула, один раз;
  порядок операций итерации не меняется); сверка по печати до 4 знаков
  и по «моя/загрузчик»-строке.
- L2: grid1000 полоса ∈ [8, 40] ГБ/с; приёмка §5 (≥20) — цель.
- L3: время итерации grid1000 ≤ 0.5 с (сейчас 3.7).
- L4: НК rho=0/tau0 — прежние (рецикл ровно 0 / все нули).

**ПРИЁМКА.** L1 посимвольно (по печатям), L2 ≥ 8, L3, L4.

**НЕГАТИВНЫЙ КОНТРОЛЬ.** tau0: E ≡ 0 в материализованном ходе (переукладка
не «починила» физику); cavity-регресс L1 — переукладка не меняет числа.
**ОТКРЫТО:** полный стрим E (копии Ed по направлениям) — если L2 < 8.

### §838: АУДИТ ПЛАНА — А1488

**А1488 [ПРОЧИТАНО В КОДЕ] Память походных массивов растёт как nd × nt** —
для города (1e8 кусков × 26 × 20 Б = 52 ГБ) неприемлемо; это шаг-измерение
для CPU-синтетики, постоянное решение — Морton-раскладка базовой структуры
(кусочки лежат рядом с соседями по лучу приближённо), с ней походные
копии не нужны. Записано, чтобы измерение не превратилось в проект.

### §838: ДОКЛАД — МАТЕРИАЛИЗАЦИЯ ДАЛА 2×, ОСТАТОК ИЗМЕРЕН: СЛУЧАЙНЫЕ E-ДОСТУПЫ ВНЕ L3

**Вывод.** Походные массивы (листья/куски в порядке обхода, cn/wn один раз
на направление, prefetch) дали **2–2.4×** (grid1000: 14.95 → 8.25 с за 4
итерации; полоса 0.42 → 1.76 ГБ/с) при ПОСИМВОЛЬНОМ совпадении полей
(L1: cavity 0.7078 и 0.1868 — цифры §837 без единого изменения).
L2/L3 ПРОМАХ: 1.76 < 8 ГБ/с, 2.06 с/итерацию > 0.5. Остаток разложен:
**63 нс на визит куска — случайные Eprev/Ed-доступы в 40 МБ массив,
не помещающийся в L3** (нормали, площади и cn из итераций уже убраны).
Плечо следующее — Morton-раскладка кусков (А1488): куски вдоль любой
линии становятся приближённо смежными, E-доступы локализуются; это же
базовая раскладка города, шаг не выбрасывается.

**Числа** (замер):

| сцена | было (§837) | стало (§838) | полоса |
|---|---|---|---|
| cavity nd=26, 16 итер | 0.851 с | 0.355 с | 0.40 → 1.41 ГБ/с |
| grid1000 nd=26, 4 итер | 14.95 с | 8.25 с | 0.42 → 1.76 ГБ/с |
| cavity поля (обе nd) | — | посимвольно | — |

**Аудит исполнения.** L1 ПОПАДАНИЕ (посимвольно по печатям). L2 ПРОМАХ
(1.76 < 8). L3 ПРОМАХ (2.06 > 0.5). L4 ПОПАДАНИЕ (tau0 — все нули;
rho=0 — рецикл ровно 0). Промахи — результат, не неудача: остаток
ИЗМЕРЕН и назван (E-разброс), следующее плечо записано. ccheck CLEAN,
санитайзеры чисты.

**Находки:**
- **А1489 [ЗАМЕРЕНО]** после материализации узкое место — E-массив вне
  L3: 63 нс/визит; Morton-раскладка кусков — следующее плечо, она же
  базовая раскладка города.

**СЛЕДУЮЩИЙ ШАГ:** Morton-раскладка кусков при построении (переупорядочить
nt-массивы по Morton-коду клетки владения; CSR и походы наследуют порядок);
та же приёмка L1/L2/L3 на grid1000.

---

## §839 (11-09): ПЛАН — MORTON-РАСКЛАДКА КУСКОВ (ДО КОДА)

А1489: остаток — случайные Eprev/Ed-доступы в 40 МБ вне L3. Кусок вдоль
осевой линии = соседние Morton-коды (фиксированные y,z: коды различаются
младшими x-битами), вдоль диагонали — близкие. Переупорядочить куски по
Morton-коду клетки владения при построении: CSR и походы наследуют
порядок автоматически; параллельные массивы инструмента (area/nrm/kd)
переставляются по pcs[].tri — отдельная таблица перестановки не нужна,
биекция «кусок → исходный треугольник» уже в записи.

**ДЕЛАТЬ.** `src/nstruct/pyr.h/.c`: `hz_pyr_morton(py)` — ключи Morton по
клетке владения, сортировка, переукладка pcs + пересборка CSR.
`tools/swee3.c`: ключ `mort=0|1` (умолчание 1), перестановка area/nrm/kd
по pcs[].tri, сличение cmp — по pcs[].tri (не по слоту).

**ПРЕДСКАЗАНИЯ.**
- M1a: mort=0 — E ПОСИМВОЛЬНО равна §838 (выключатель чист).
- M1b: mort=1 — E_avg совпадает с mort=0 до 1e-10 относительной
  (внутри клетки порядок суммирования меняется — ULP-уровень).
- M2: grid1000 полоса ∈ [8, 30] ГБ/с.
- M3: время итерации grid1000 ≤ 0.6 с.
- M4: НК прежние.

**ПРИЁМКА.** M1a/M1b, M2 ≥ 8, M3, M4.

**НЕГАТИВНЫЙ КОНТРОЛЬ.** tau0 в mort=1 — нули (переукладка не «улучшила»
физику); mort=0 — посимвольный регресс (выключатель не трогает мир).

### §839: АУДИТ ПЛАНА — А1490, А1491

**А1490 [РАССУЖДЕНИЕ] M1b не может быть посимвольной:** порядок кусков
внутри клетки меняется → порядок суммирования cntot меняется → ULP.
Посимвольная сверка остаётся только у mort=0 (выключатель); Morton-случай
сверяется с допуском 1e-10 относительной. Инвариант «pcs[t].tri == t»
(А1456) после Morton НЕДействителен — snap3 его не вызывает, в докладе
пометить.

**А1491 [ПРОЧИТАНО В КОДЕ] cmp-сличение обязано идти по pcs[].tri:** дамп
старого пути индексирован исходными треугольниками; слот куска после
Morton — позиция раскладки, не треугольник. В плане записано.

### §839: ДОКЛАД — MORTON ДАЛ 1.4× НА ПОЧИНЕННОЙ СЦЕНЕ; СТЕНА 0.17 ГБ/с НЕ РАЗЛОЖЕНА ГИПОТЕЗАМИ — СЛЕДУЮЩИЙ ПРИБОР perf

**Вывод.** (1) **Генератор grid был сломан** (f-индексы копий ссылались на
вершины первой комнаты — «1000 комнат» были 1000 наложенных копий одной);
починен офсетами по вершинной базе копии. Все grid-замеры до фикса
недействительны. (2) На починенной grid1000 (nt=4.96M, 14.9M занятых
листьев, 12.3 ГБ/итерацию counted) Morton дал 1.4× (1.76 → 2.50 ГБ/с),
поход только по кускам-листьям срезал трафик 12.3 → 3.80 ГБ (3.2×),
kd в походном слоте — в шуме. **Стена: 0.17 ГБ/с (187 нс/визит)
воспроизводится на пустой машине; гипотезы (kd-поток, нагрузка)
сняты замерами, не остались мнением.** M2/M3 ПРОМАХ; M1a/M1b/M4
попадания (mort=0 посимвольно, mort=1 совпадение, НК чисты; LeakSanitizer
поймал перестройку CSR — починено).

**Числа** (замер, grid1000, nd=26, it=2):

| конфигурация | ГБ/итерацию | с/итерацию | ГБ/с |
|---|---|---|---|
| сломанная сцена (недейств.) | 3.46 | 3.7 | 0.42 |
| починенная, до Morton | 12.32 | 36 | 0.34 |
| починенная, Morton | 12.32 | 36 | 0.34 |
| + куски-листья (без пустых) | **3.80** | 25.4 | **0.17** |
| + kd в походе | 3.80 | 24.3 | 0.17 |

**Аудит исполнения.** M1a ПОПАДАНИЕ (посимвольно), M1b ПОПАДАНИЕ
(совпадение печатей), M4 ПОПАДАНИЕ; M2/M3 ПРОМАХ — полоса 2.5 ГБ/с
(cavity) / 0.17 (grid1000) против цели ≥8. 187 нс/визит НЕ разложился
на мои гипотезы: kd-поток убран — времени не изменилось; машина пустая —
не среда. Модель узкого места исчерпана — дальше измерять
АППАРАТНО (perf stat: cache-misses/TLB/ветвления/IPS), а не строить
новые догадки. Плюс замечен парадокс для разбора: полоса на cavity
(0.34-0.40 ГБ/с, всё в L2) совпадает по порядку с полосой на grid1000
(0.17) — стена НЕ выглядит чисто мемори-бэндовидной.
**LeakSanitizer поймал перестройку CSR** — починено (гейт работает).

**Находки:**
- **А1492 [ЗАМЕРЕНО]** генератор grid: абсолютные f-индексы при копировании
  без офсета — сцена-призрак; проверка «max x» + «листьев на комнату»
  ловит за секунды, добавить в gen_grid.sh печать числа листьев/комнату.
- **А1493 [ЗАМЕРЕНО]** стена 187 нс/визит не разложилась гипотезами;
  следующий прибор — perf stat на grid1000 (профиль, не догадки).
- **А1494 [ЗАМЕРЕНО]** LeakSanitizer поймал утечку CSR при Morton —
  освежённый аргумент за санитайзер-гейт на каждом шаге.

**СЛЕДУЮЩИЙ ШАГ:** perf-профиль свипа на grid1000 (число L3-miss/TLB/
ветвлений на визит) → названная стена; затем граневые потоки (А1487) —
физический уровень остался 0.18–0.35× аналитики.

### §840: ДОКЛАД — СТЕНА НАЗВАНА ПРИБОРОМ: КОРОТКИЙ ПРОГОН БЫЛ ПОСТРОЕНИЕМ, УСТАНОВИВШИЙСЯ РЕЖИМ — 4.31 ГБ/с

**Вывод.** perf stat + маргинальное измерение разложили «стену 0.17 ГБ/с»
на две разные вещи. (1) **Разовая стоимость построения**: ~52 с на запуск,
из них **~48 % всех инструкций — qsort-компараторы** (26 сортировок линий
по 14.9M + Morton 5M + пометки 40M) — короткий прогон (it=2) измерял
ПОСТРОЕНИЕ, а не свип. (2) **Установившийся режим: маргинальная итерация
0.882 с = 4.31 ГБ/с** (t(it=10)−t(it=2) = 61.28−54.22 = 7.06 с / 8).
Прогресс полосы: 0.17 (фикция построения) → **4.31** (установившийся)
— цель ≥20 осталась, зазор ×4.6.

**Числа** (perf stat, grid1000 nd=26 it=2, 54.4 с task-clock):
cycles 230.5e9, instructions 433.8e9, **IPC 1.88** — код НЕ
мемори-старвится в целом; branch-instructions 94.3e9 (366 на визит
куска — внутренний цикл ветвист), branch-misses 335e9 (3.5/визит,
~70 нс), LLC-miss-rate 31 %, dTLB 0.7/визит (не стена).

**Аудит исполнения.** Стена НЕ разложилась гипотезами (А1493 закрыт
замером, не догадкой):ipc и доли сортировок назвали виновника. Прогнозной
модели «три случайных потока по 65 нс» больше нет — остаток после
сортировок: походный цикл ~218 инструкций/визит (двухпроходный
cntot+application, счётчики трафика в горячем цикле, ветвь смены линии).
Лечение следующего ×4: (а) сортировки линий счётчиком (line-id — малые
целые, O(n) вместо qsort), (б) слить два прохода по куску в один,
(в) счётчики трафика — компиляционным флагом, из горячего цикла,
(г) префетч данных куска. Память-стена (L3/TLB) НЕ подтверждена.

**Находки:**
- **А1495 [ЗАМЕРЕНО]** короткий прогон измерял построение: ~48 %
  инструкций — qsort-компараторы; полоса обязана мериться маргинально
  (t(it=N+Δ)−t(it=N)) или с прогревом.
- **А1496 [ЗАМЕРЕНО]** установившаяся итерация 0.882 с = 4.31 ГБ/с на
  grid1000 nd=26; следующий ×4 — гигиена внутреннего цикла (а)-(г).

**СЛЕДУЮЩИЙ ШАГ:** гигиена внутреннего цикла (а)-(г) с той же
маргинальной приёмкой; затем граневые потоки (А1487).

---

## §841 (11-09): ПЛАН — ГИГИЕНА ВНУТРЕННЕГО ЦИКЛА (ДО КОДА)

Лечение (а)-(г) из §840. Ключевое упрощение, найденное при письме:
в режиме col ПОЛНАЯ сортировка всех листьев не нужна вовсе — поход
ходит только по кускам-листьям (592k из 14.9M на grid1000), сортируются
только они; 26×qsort(14.9M) исчезает из построения. cntot и T = exp(−τ)
инвариантны по итерациям — предвычисляются в походном листе (wleaf
растёт до 40 Б), итерация становится ОДНОПРОХОДНОЙ по куску.
Счётчики трафика — аналитикой: visits детерминированы (nd×(походные
листья×40 Б + nt×36 Б)); из горячего цикла уходят. Prefetch данных
куска и Eprev.

**ДЕЛАТЬ.** `src/nstruct/sweep.c`: col-режим строит поход прямо
(фильтр кусковых листьев → пары (линия,слэб) → qsort 592k → wleaf
{npcs, first, line, cntot, T} + wcn/wn/kd/wpid), итерация однопроходна;
scalar §835 — без изменений (эталонный контраст).

**ВЫХОД.** cavity nd=26: E_avg против §840 (0.7078); НК; grid1000:
маргинальная итерация (it=2 vs it=10), полоса; время построения.

**ПРЕДСКАЗАНИЯ.**
- H1: cavity nd=26 mort=1: E_avg = 0.7078 в пределах 1e-10 отн.
  (cn/wn/cntot — те же формулы и порядок).
- H2: маргинальная итерация grid1000 nd=26 ≤ 0.35 с (полоса ≥ 10.8 ГБ/с);
  приёмка §5 (≥20 ГБ/с, ≤0.19 с) — цель шага.
- H3: время построения grid1000 ≤ 15 с (сортировка 592k пар вместо
  26×14.9M).
- H4: НК rho=0/tau0 — прежние.

**ПРИЁМКА.** H1, H2, H3, H4.

**НЕГАТИВНЫЙ КОНТРОЛЬ.** tau0 в новом цикле — все нули; cavity mort=0 —
посимвольный регресс §840.

### §841: АУДИТ ПЛАНА — А1497…А1499

**А1497 [РАССУЖДЕНИЕ] cntot предвычисление посимвольно:** накопление по
кускам клетки в том же CSR-порядке, формулы те же ⇒ cntot и T битовно
равны вычисленным в цикле; H1 честно посимвольная по печатям.

**А1498 [РАССУЖДЕНИЕ] Модель трафика меняется (лист 24→40 Б, кусок
28→36 Б)** — counted МБ/итерацию §840 и §841 НЕ сравнимы напрямую;
сравнимая величина — установившаяся полоса ГБ/с (маргинально). В доклад
пометить.

**А1499 [ПРОЧИТАНО В КОДЕ] Prefetch Eprev[wpid[e+8]] требует границы:**
e+8 может выйти за nt последнего листа — зажать `min(e+8, nt−1)`.

### §841: ДОКЛАД — ГИГИЕНА: ПОЛОСА 4.31→6.06 ГБ/с, ПОСТРОЕНИЕ 52→14 с; ПОЙМАНЫ ТРИ ДЕФЕКТА, ОДИН — ПЕРЕНОСНЫЙ

**Вывод.** Гигиена внутреннего цикла: сортировка только кусковых листьев
(26×qsort(14.9M) исчезли из построения: 52 → 14.2 с), cntot/T
предвычислены в походном листе (итерация однопроходна), префетчи,
hz_pyr_permute циклами на месте (без временных массивов). Установившаяся
полоса **4.31 → 6.06 ГБ/с** (итерация 0.882 → 0.627 с). H3 ПОПАДАНИЕ
(14.2 ≤ 15, на границе), H4 ПОПАДАНИЕ. **H1 ПРОМАХ — и это находка:**
E_avg сдвинулся 0.7078 → 0.6882, потому что поход только по кускам-
листьям УСТРАНИЛ дефект §840 — сброс L на ЧУЖИХ пустых листьях,
отсортированных между кусками одной линии (свет не доходил до выходного
куска колонки; поглощение +5 %). H2 ПРОМАХ (0.627 > 0.35; полоса 6.06
против цели ≥20 — зазор ×3.3).

**Числа** (замер; cavity nd=26 it=16; grid1000 nd=26):

| | было (§840) | стало (§841) |
|---|---|---|
| cavity E_avg | 0.7078 | 0.6882 (дефект переноса устранён) |
| cavity 16 итераций | 0.355 с | **0.016 с** (сцена в кэше) |
| grid1000 построение | ~52 с | **14.2 с** |
| grid1000 итерация | 0.882 с | **0.627 с** |
| grid1000 полоса | 4.31 ГБ/с | **6.06 ГБ/с** |
| НК rho=0 / tau0 | рецикл 0 / нули | те же |

**Аудит исполнения.**
1. H1 ПРОМАХ-находка (А1500): сброс L на чужих пустых листьях —
   переносной дефект §840; новая цифра 0.6882 — уровень модели с
   устранённым дефектом. НК rho=0/tau0 чисты в новом цикле.
2. H2 ПРОМАХ: однопроходность дала лишь 1.4× — 130M визитов с тремя
   потоками (Eprev r, Ed w, kd — kd уже в походе) остаются
   латентными; счетчики из цикла убраны (модель трафика: лист 40 Б,
   кусок 36 Б — с §840 несравнимы напрямую, А1498).
3. **А1501 [ЗАМЕРЕНО]** hz_pyr_permute применял перестановку ДВАЖДЫ
   (последний слот цикла не помечался seen — фантомный второй проход);
   поймано инвариантом «area[i] == площадь(pcs[i].tri)» и устранено;
   инвариант теперь ровно 0. Урок: инвариант соответствия массивов
   поставлен рядом с кодом и сработал немедленно.
4. Замер НЕ показывает: граневые потоки (модель слоя прежняя —
   абсолютный уровень 0.17× аналитики, А1487 в силе); многопоточность;
   диагональную корректность порядка в новом походе (сортировка
   (линия, слэб) — слэб строг, связи внутри линии нет, как в §834).
5. Устаревшее: цифры §840 (0.7078, 4.31 ГБ/с, 52 с) заменены
   настоящими; straight-цитирование §836 «0.94×» давно снято (А1486).

**Находки:**
- **А1500 [ЗАМЕРЕНО]** переносной дефект §840 (сброс L на чужих пустых
  листьях) — устранён походом только по кусковым листьям.
- **А1501 [ЗАМЕРЕНО]** двойное применение перестановки в
  hz_pyr_permute (непомеченный последний слот цикла) — инвариант
  area↔tri поймал немедленно.
- **А1502 [ПРОЧИТАНО В КОДЕ]** gcc-analyzer FP на перестановке
  временных массивов (класс diam 07-24) — устранён самим упрощением:
  hz_pyr_permute циклами на месте вместо копий.

**СЛЕДУЮЩИЙ ШАГ:** два плеча по-прежнему открыты: (1) полоса 6.06 → ≥20
(профиль нового цикла perf'ом: IPC/ветвления/промахи после гигиены);
(2) граневые потоки — абсолютный уровень света 0.17× → аналитика (А1487).

---

## §842 (11-09): ПЛАН — НЕПРОЗРАЧНЫЙ СЛОЙ: T=0 И ТОЧНОЕ РЕШЕНИЕ ФИКСТУРЫ (ДО КОДА)

А1487: модель «полупрозрачный слой (1−T)» теряет свет за доменом (луч
после слоя продолжает по линии и выходит наружу — в тёмном окружении
это чистая потеря) и недодаёт рекурсию. Стены непрозрачны: слой
поглощает ВЕСЬ пришедший луч (T=0), инфлюкс на куски = w·L·|ω·n|
(целиком), переизлучение в линию = Le + ρ·E_prev/(2π) (ламберт,
двусторонняя конвенция А1471/А1475). tau0-НК теряет смысл (слой всегда
непрозрачен) — заменяется НК le=0.

**У Fixture'а появляется ТОЧНОЕ решение МОДЕЛИ** (не непрерывной
физики): в равновесии каждый кусок получает инфлюкс только с выходной
половины направлений (входная половина приходит тёмной — окружение
чёрное): E_p = (1/2)·Σ_d w·|ω_d·n_p|·(Le + ρ·E_avg/(2π)).
Площадно-взвешенное решение: E_avg = (Σ_p w_n,p·area_p)·Le /
(A·(1 − ρ/4)), где w_n,p = Σ_d w·|ω_d·n_p| — считается В КОДЕ точно
(квадратура учтена, не разброс). Это сильнейшая приёмка линии: свип
обязан сойтись к решению СВОЕЙ дискретной модели.

**ДЕЛАТЬ.** sweep.c: T ≡ 0 (wleaf.T и ветки (1−T) убрать; L ← Lsurf;
инфлюкс целиком); tau0-ключ помечен устаревшим (no-op); swee3: печать
модельного решения и отношения к нему; НК-ключ le=0.

**ПРЕДСКАЗАНИЯ.**
- G1: E_avg(cavity, ρ=0.5, Le=1, nd=26) ∈ [0.85, 1.15] × модельного
  решения (центр ≈ 1.80 при π_n-среднем; интервал покрывает разброс
  ориентаций нормалей).
- G2: ряд сходится монотонно; фактор ≤ ρ.
- G3: НК le=0: E ≡ 0; НК rho=0: рециркуляция ровно 0, стационар.
- G4: потеряно > 0 и стабильно (выход лучей за тёмный домен — физика).
- G5: room — сходится, конечна.

**ПРИЁМКА.** G1 в ±15 % модельного решения — главная; G3, G4; G5.
**НЕГАТИВНЫЙ КОНТРОЛЬ.** le=0 (без эмиссии поле родиться не может);
rho=0 (без рассеяния ряд стационарен мгновенно).
**ОТКРЫТО:** граневые потоки на гранях (поле на 6·граней клетки) —
следующий уровень той же линии; лей-образы/лобы — после.

### §842: АУДИТ ПЛАНА — А1503…А1505

**А1503 [РАССУЖДЕНИЕ] «Половина направлений тёмная» — следствие чёрного
окружения, не дефект:** луч, вошедший в колонку, несёт 0 до первого
удара; кусок на входной стороне колонки честно не освещён этим
направлением. В замкнутой непрерывной физике он освещён ДРУГИМИ
направлениями — что и вносит остальные 3 из 6. Модель это покрывает
через E_prev-рекурсию. Правильность проверяется G1 (сходимость к
решению модели), не интуицией.

**А1504 [РАССУЖДЕНИЕ] ρ/4 в знаменателе:** выходная половина направлений
даёт Σw|cos| = π_n/2; рекурсивный член π_n·ρ/(2π)·... — знаменатель
(1 − ρ·(π_n/2)/(2π)) при π_n = π даёт (1 − ρ/4). Формула в плане
записана как (1 − ρ/4) при π_n = π — сверится с кодом прогоном.

**А1505 [ПРОЧИТАНО В КОДЕ] tau0-ключ остаётся распознаваемым, но
no-op с печатью «устарел»** — старые командные строки не молча меняют
смысл.

### §842: ДОКЛАД — НЕПРОЗРАЧНЫЙ СЛОЙ ПОСТАВЛЕН; ЗАЗОР К МОДЕЛИ ИЗМЕРЕН И АТРИБУИРОВАН: RAY EFFECT

**Вывод.** Слой сделан физически правильным (непрозрачным): луч гасится в
слое целиком, инфлюкс на куски = w·L·|ω·n|, переизлучение ламбертево
(Le + ρE_prev/2π). У фикстуры появилось ТОЧНОЕ решение дискретной модели
(E = πLe/(1−ρ/2) = 4.19 на полости, π_n вычислена в коде) — и свип
сходится к **0.9148 = 0.218×** этого решения. Зазор атрибуирован:
**ray effect дискретных ординат** — линия несёт только своё линейное
накопление, изотропное поле полости приходит со всех направлений
(тот же класс, что чешуя §794); при nd=26 на сфере лучи-эффект
съедает ~78 % равновесия. Это УЖЕ ИЗВЕСТНАЯ цена метода, впервые
измеренная на нашей фикстуре числом.

**Числа** (замер, cavity05, ρ=0.5, Le=1, nd=26, it=30):

| величина | значение |
|---|---|
| E_avg свипа | 0.9148 (стационар, фактор → 0) |
| точное решение модели | 4.1906 → **0.218×** |
| непрерывная физика 2πLe/(1−ρ) | 12.566 (справочно) |
| НК le=0 / rho=0 | E ≡ 0 / рецикл ровно 0 |
| room | сходится, 0.303× модели (та же природа) |
| полоса | 6.2 ГБ/с (cavity в кэше) |

**Аудит исполнения.**
1. G1 ПРОМАХ против плана (±15 % от 1.80 — формула плана была ещё одной
   ошибкой вывода; правильное модельное равновесие 4.19, и свип от него
   на 0.218× — ray effect). Промах продуктивен: зазор впервые измерен
   и отнесён к известному классу.
2. G3/G4 ПОПАДАНИЕ (le=0 нули; rho=0 рецикл 0; lost > 0 стабилен —
   физика тёмного окружения). G5 ПОПАДАНИЕ.
3. Замер НЕ показывает: сходимость ray effect'а по ND (нужен свип
   nd=98+ или угловая реконструкция); грани-поле; паритет.
4. Устаревшее: «аналитика 1ст/2ст» печати §835-841 — заменена
   «моделью слоя» (точно) и «непрерывной физикой» (справочно);
   старые отношения к аналитике цитировать с пересчётом.

**Находки:**
- **А1506 [ЗАМЕРЕНО]** ray effect линии: 0.218× равновесия модели при
  nd=26 на сфере. Митигация известна (ND↑, угловая реконструкция,
  first-collision источник §796-класса) — следующий физический шаг.
- **А1507 [ЗАМЕРЕНО]** у фикстуры точное решение дискретной модели
  считается в коде (hz_sw_exitwsum) — свип сверяется со СВОЕЙ моделью,
  разрыв «модель↔непрерывная физика» учитывается отдельно.

**СЛЕДУЮЩИЙ ШАГ:** выбор за пользователем: (1) ray-effect-митигация
(ND↑ и/или first-collision источник — прямой свет вне ординат, §796
-класс) — поднимет абсолютный уровень к физике; (2) граневые потоки
на гранях клетки; (3) возврат к скоростной линии (perf нового цикла).

## §842-приписка (директива пользователя): ДЛЯ ТЕСТОВ ДОСТАТОЧНО СИНТЕТИКИ — ШАРЫ В МАЛЕНЬКОЙ КОМНАТЕ

Пока работа идёт на синтетических сценах (шары в маленькой комнате,
cavity, grid) — внешние сцены Bistro-класса к работе НЕ привлекаются до
отдельного указания. Следствия: (1) приёмки ND-свипа и first-collision
считаются на cavity05/room — прибор `hz_sw_exitwsum` уже стоит; (2) шар
в комнате остаётся обязательной геометрической приёмкой (К27-класс:
два КАСАЮЩИХСЯ шара — луч обязан попасть в БЛИЖНИЙ); (3) никакие
оптимизации не меряются на сценах крупнее этих — потолок, снятый на
игрушке, городом не защищается (К82).

## §843 (11-09): ПЛАН — ND-СВИП: ЛЕСТНИЦА КВАДРАТУР И СХОДИМОСТЬ RAY EFFECT (ДО КОДА)

**Мотив — А1506:** ray effect линии измерен (0.218× модели при nd=26), но
сходимость по ND НЕ мерялась; без неё не знать ни цену направлений, ни
размер остатка. Директива пользователя: тесты — синтетика (шары/комната);
cavity05 достаточна, прибор `hz_sw_exitwsum` стоит.

**ЧТО ДЕЛАТЬ.**
1. Квадратура направлений как ТАБЛИЦА, а не зашитый массив: продуктовая
   Чебышёв по φ (N_φ равных узлов на всю окружность, вес 2π/N_φ) ×
   Гаусс—Легандр по μ = cosθ (N_μ чётных узлов на [−1,1], сумма весов 2).
   Всего nd = N_φ·N_μ. Лестница: 6/26 (легаси, целые векторы) и новые
   ступени 12×6=72, 24×12=288, 48×24=1152. Ключ CLI: `dirs=12x6`;
   `dirs=6|26` — прежний путь посимвольно.
2. Вес направления — из таблицы (w_d), НЕ константа 4π/26: тождество
   Σw = 4π держится по построению, Σw·|cos| = 2π — с ошибкой квадратуры
   (|μ| имеет излом в 0; Чебышёв×Гаусс точен по φ и по чётным степеням μ,
   излом даёт O(1/N_μ²)) — Величина печатается прибором и входит в
   модель слоя через exitwsum ТОЙ ЖЕ таблицы, так что свип сверяется со
   СВОЕЙ моделью точно при любой ступени.
3. Порядок внутри линии: сейчас поход сортирует листья ТОЛЬКО по ключу
   линии, порядок внутри линии — порядок листьев (Morton). Для
   направлений с отрицательными компонентами обход идёт ПРОТИВ хода луча
   — см. А1508. Чинится вторым ключом: u = Σ_ax (sg>0 ? ii : len−1−ii),
   сортировка по (линия, u).
4. swee3: печать nd и W/модели по выбранной таблице; остальное без правок.

**АУДИТ ДО КОДА.**
- **А1508 [ПРОЧИТАНО В КОДЕ]** `sw_build_walk` сортирует пары только по
  линии (slab выкидывается), внутри линии — Morton-порядок листьев.
  Для 13 из 26 направлений (отрицательные компоненты) это обход от
  «дальнего» конца: свет идёт против направления веса. Фикстура-сфера
  симметрична относительно обращения линии (нормали внутрь, стенки
  одинаковые), поэтому дефект НЕВИДИМ на приёмках §836–§842 — класс К44.
  Чинится в этом шаге; на cavity05 правка обязана быть no-op до 1e-9.
- **А1509 [РАССУЖДЕНИЕ]** qsort нестабилен: равные ключи линии не
  гарантируют даже Morton-порядка внутри линии. Двухключевая сортировка
  (А1508) снимает и это — порядок становится полным.
- **А1510 [РАССУЖДЕНИЕ]** веса: тождество энергии на направление не
  зависит от квадрата весов; модель слоя использует exitwsum той же
  таблицы — разрыва «свип↔модель» нет. «Непрерывная физика» 2πLe/(1−ρ)
  от квадрата не зависит вовсе.
- **А1511 [РАССУЖДЕНИЕ]** цена: трафик и построение походов ∝ nd
  (26→1152 = ×44); cavity05 мала (2208 кусков), heavy-ступени допустимы;
  на grid1000 лестницу НЕ гонять (директива синтетики; город не мериется).

**ПРЕДСКАЗАНИЯ.**
- G1: E_avg(cavity05, ρ=0.5, Le=1) по лестнице 26 → 72 → 288 → 1152
  РАСТЁТ монотонно к 4.1906; отношение к модели на старшей ступени ≥ 0.5
  (если не растёт — атрибуция зазора ray effect'у неверна).
- G2: фактическая сходимость по nd снята числом: таблица
  nd → E_avg → ×модели → с/итерацию → ГБ/с. Из неё читается цена
  направления (T3-вопрос «сколько ординат надо»).
- G3: правка порядка: dirs=26 на cavity05 даёт E_avg = 0.9148 в допуске
  1e-9 (симметрия сферы), фактор ряда → ρ.
- G4: моменты квадратуры: Σw = 4π до 1e-12; Σw|n·ω̂| на трёх случайных n
  = 2π с отклонением, согласованным с А1510.
- G5: cost ∝ nd: с/итерацию растёт линейно по nd в пределах ×1.3.

**НЕГАТИВНЫЙ КОНТРОЛЬ.** le=0 на nd=288: E ≡ 0; rho=0: рециркуляция
ровно 0, стационар с первой итерации.
**ПРИЁМКА.** G1 (монотонность к модели) — главная; G3, G4, G5.
**ОТКРЫТО после шага:** угловая реконструкция (дешевле ND↑?); остаток
ray effect'а при nd→∞ (пространственная составляющая).

### §843: ДОКЛАД — ПРИБОР КВАДРАТУРЫ В ЛИНЕЙНОМ СВИПЕ НЕВЕРЕН ДЛЯ НЕЦЕЛЫХ НАПРАВЛЕНИЙ; ПОРЯДОК ВНУТРИ ЛИНИИ ПОЧИНЕН; НК ЧИСТЫ

**Вывод.** Шаг сделал свою машинную часть (таблица направлений, вес из
таблицы, двухключевая сортировка похода, CLI `dirs=NxM`), но ГЛАВНАЯ
приёмка G1 НЕ СОСТОЯЛАСЬ: продуктовая квадратура (Чебышёв×Гаусс) в
линейном свипе НЕВЕРНА — новый класс, см. А1512. Одновременно починка
А1508 поставлена и её эффект измерен. Негативные контроли чисты.

**Числа** (cavity05, ρ=0.5, Le=1, it=30, lev=5, модель слоя ≈ 4.189):

| dirs | nd | свип | ×модели | с/итерацию | полоса |
|---|---|---|---|---|---|
| 26 (легаси) | 26 | 2.6501 | 0.632× | 0.0007 | 5.87 ГБ/с |
| 12x6 | 72 | 2.5522 | 0.609× | — | 5.37 ГБ/с |
| 24x12 | 288 | 2.5720 | 0.614× | — | — |
| 48x24 | 1152 | 2.5766 | 0.615× | 0.031 | 5.67 ГБ/с |

room (несимметричная сцена): dirs=26 → 0.360×, потеряно 0.58;
dirs=24x12 → 0.233×, **потеряно 20.14** — течение сквозь стену.

**Аудит исполнения.**
1. **А1512 [ЗАМЕРЕНО, ДЕФЕКТ ПРИБОРА]** инвариант линии свипа
   (постоянство поперечных разностей индексов) существует ТОЛЬКО у
   целочисленных направлений: у (1,1,0)-семейства клетки луча лежат на
   ii−jj=const, у (0.7,0.2,0.69) — НЕ лежат. «Линия» произвольного
   направления группирует клетки вне луча; тёмные входы возникают ВНУТРИ
   сцены, свет гасится о слой и теряется. Именно это даёт течь на room
   (0.58 → 20.14) и плато 0.61× на сфере — наследство того же дефекта,
   а НЕ сходимость по ND. **G1 этим прибором не измеряется.**
2. **А1508 починена и подтверждена** (двухключевая сортировка: линия,
   ход луча). Эффект на конфигурации §842 (lev=6) мал: 0.218× → 0.224×;
   на lev=5 — 0.632×. Правка обязана стоять (обход против луча —
   физически неверен), но зазор §842 она не объясняет.
3. **G3 формально ПРОМАХ**: предсказывал no-op до 1e-9, на lev=6 вышло
   0.9148 → 0.9405. Сфера триангулирована неравномерно, симметрия
   обращения линии неточна. Промах продуктивен — он и показал, что
   результат чувствителен к пространственному разбиению.
4. **ПРОСТРАНСТВЕННАЯ ЧУВСТВИТЕЛЬНОСТЬ [ЗАМЕРЕНО]**: та же dirs=26 даёт
   0.632× при lev=5 и 0.224× при lev=6. Отношение к модели сильнее
   зависит от клеточного разбиения, чем от правки порядка; остаток зазора
   — НЕ угловой (прямой аргумент против чистого ray effect'а).
5. G4 ПОПАДАНИЕ: Σw = 4π точно на всех ступенях; Σw|cos| → 2π
   (6.2835 при nd=1152; излом |μ|, А1510). Заодно видно: веса легаси-26
   (равные 4π/26 при неравных нормах) дают Σw|cos| = 6.26…6.44 —
   квадратура легаси НЕСОГЛАСОВАННА, до 2.6% на направление.
6. G5 ПОПАДАНИЕ: полоса стабильна 5.4–5.9 ГБ/с по всей лестнице, цена ∝ nd.
7. НК: le=0 → E≡0; rho=0 → рециркуляция ровно 0, стационар. ccheck CLEAN,
   санитайзеры чисты (ASan+UBSan, lev=5, dirs=12x6).

**СЛЕДУЮЩИЙ ШАГ.** ND-сходимость мерить ЧЕСТНОЙ лестницей: либо
(а) целочисленные семейства (6 → 26 → {−2..2}) с весами по телесному
углу вместо 4π/N (заодно чинит несогласованность легаси-весов, п.5), —
инвариант линии при этом держится; либо (б) произвольные направления
через маршевый обход (§834-класс, топологически чист) — дороже и другой
профиль памяти. Параллельно — пространственная переатрибуция зазора:
зависимость ×модели от lev при фиксированном nd.

### §843-добавление (тот же день): ПЕРЕАТРИБУЦИЯ ЗАЗОРА — ДЕГРАДАЦИЯ ЛИНИЙ, А НЕ RAY EFFECT

**Прибор:** лестница lev при фиксированном nd=26 на cavity05 + счётчики
в свипе (визиты, доставки с L>0, визитов на линию). Кратность куска по
листьям проверена отдельно — **А1513 ОПРОВЕРГНУТА**: кратность ровно 1.00
на всех lev (куски сидят в листе центра), дублирования вклада нет.

**Замер:**

| lev | клетка, м | ×модели | визитов с светом | линий из 1 визита |
|---|---|---|---|---|
| 4 | 0.2 | 1.182× | 162 %* | 8.8 % |
| 5 | 0.1 | 0.632× | 56.6 % | 48.4 % |
| 6 | 0.05 | 0.224× | 26.1 % | 77.6 % |
| 7 | 0.025 | 0.068× | 10.9 % | **90.6 %** |

*на lev=4 в листе несколько кусков, счёт на кусок.

**А1514 [ЗАМЕРЕНО, ПРИНЦИПИАЛЬНОЕ]:** доля линий из ОДНОГО визита растёт
с измельчением до 90.6 %. Линия из одного визита — тёмный вход и сразу
выход: она НИЧЕГО не доставляет, её Lsurf теряется. Корреляция с
падением уровня поля (1.18× → 0.068×) полная. Механизм: перенос идёт по
коридорам СЕТКИ (инвариант линии), а регистрация куска — по ОДНОМУ листу
(центр куска). Коридор пересекает поверхность дважды (вход и выход), но
клетка пересечения совпадает с листом центра куска лишь случайно; с
измельчением клетка ≪ кусок, и всё больше пересечений НЕ регистрируются.
**Схема «непрозрачный кусок в сеточном свипе» НЕ СХОДИТСЯ с
измельчением** — это не баг реализации, а разрыв в постановке.

**Следствие для §842 (А1506):** зазор 0.218× на lev=6 измерен там, где
77.6 % линий слепы. Атрибуция «ray effect» снята: главный механизм —
деградация линий. Ray effect остаётся как отдельный, второй эффект
(plateau 0.61× на продуктовой лестнице включал и его, и А1512).

**Лечение (выбор следующего шага, до кода):**
(а) регистрация куска во ВСЕХ пересекаемых листьях с ОДНОКРАТНЫМ
вкладом (маркировка «курок уже доставил на этой линии») — локальная
правка, цена — кратность в походных массивах (растёт как А1488);
(б) объёмное поле в клетках (DG), поверхность — граничное условие —
это возврат к исходной постановке линии, дорожее, но сходится по
построению.
НК счётчиков: le=0 — ndep=0; cavity05/12x6 — 45.7 % визитов с светом.

## §843-бис (11-09): ПЛАН — РЕГИСТРАЦИЯ КУСКА ВО ВСЕХ КОРИДОРАХ ЕГО ПЛОЩАДИ (ДО КОДА)

**Выбор пользователя:** лечение (а) — поверхности остаются носителем,
поле не переезжает в объём. Устраняет А1514, сохраняет К83; (б) объёмное
DG остаётся запасом для сцен с объёмной средой.

**ПОСТАНОВКА.** Поход строится не из CSR-владения (центроид), а из
bbox-пересечения: для каждого куска перечисляются все клетки сетки,
перекрывающие его bbox; каждая пара (кусок, клетка) даёт запись похода
со СВОЕЙ линией и ходом (line, u). Тогда каждый коридор, геометрически
пересекающий площадь куска, получает его в визит — доставка на каждую
поверхность на каждом направлении. Дубли (кусок, линия) из нескольких
клеток bbox гасятся: в линии остаётся ПЕРВОЕ по ходу луча вхождение.

**ДЕЛАТЬ.**
1. `hz_sw_run`: новый аргумент `pmin/pmax` (bbox кусков, мировые);
   поход-строитель перечисляет клетки bbox через `hz_pyr_leaf_index`
   (зажим в сетку), сортировка (линия, ход, кусок), дедуп (линия, кусок).
2. Записи похода — по одной на пару (кусок, клетка): `npcs=1`, cntot =
   area·|cos| — вся остальная машинерия свипа (тёмный вход, L=Lsurf,
   баланс) без правок.
3. swee3: передаёт cmin/cmax; печать слотов похода до/после.

**АУДИТ ДО КОДА.**
- **А1515 [РАССУЖДЕНИЕ]** bbox-перекрытие — ЗАВЫШЕНИЕ регистрации
  (угловые клетки bbox не пересекаются геометрически): лишние визиты с
  нулевым вкладом в месте, где коридор мимо треугольника. Масштаб —
  периметр-член, падает с измельчением; на грубых lev схема и так
  считала кусок одной клеткой. Приёмка G2 покрывает.
- **А1516 [РАССУЖДЕНИЕ]** порядок кусков в походе меняется (сортировка
  по (линия, ход, кусок) вместо порядка CSR): побитовой преемственности
  со старым походом НЕТ и не обещается. Контроль: на lev=4 (кусочек
  в одной клетке) физика тождественна, E_avg обязан совпасть до 1e-12.
- **А1517 [РАССУЖДЕНИЕ]** память похода растёт как Σ_p клеток bbox —
  на cavity05 пренебрежимо; общий закон — та же A/cell²-квадратура, что
  в оценке цены (×150 на lev7 против ×500+ у объёма). Городская стена
  А1488 относится к той же валюте, отдельно не решается.

**ПРЕДСКАЗАНИЯ.**
- G1 (механизм): доля линий из 1 визита на cavity05 падает до ≤ 15 %
  на ВСЕХ lev (сейчас 8.8 % → 90.6 %); доставки/визиты ≥ 40 % и не
  падают с измельчением.
- G2 (главная — сходимость): E_avg(cavity05, nd=26) по lev=4…7 живёт в
  [0.8, 1.4]×модели БЕЗ монотонного коллапса (сейчас 1.18× → 0.068×,
  18× размах); размах по lev ≤ 1.5×.
- G3 (преемственность): lev=4 до/после правки — E_avg в допуске 1e-12.
- G4 (сцена с проёмами): room, dirs=26 — ×модели не ухудшается, потеряно
  не растёт (А1512 не задет: направления целочисленные).
- НК: le=0 → E≡0; rho=0 → рециркуляция ровно 0.
**ОТКРЫТО:** веса легаси-26 (несогласованы, до 2.6 %) — отдельный шаг.

### §843-бис: ДОКЛАД — НЕГАТИВНЫЙ: «РЕГИСТРАЦИЯ ВО ВСЕХ КОРИДОРАХ» БЕЗ ВИДИМОСТИ НЕ СХОДИТСЯ; КОД ОТКАЧЕН

**Вывод.** Лечение (а) в лоб опробовано тремя вариантами и все три дали
расходимость или неверное плато. Корень — непрозрачность: коридор
пробивает ПЕРВЫЙ кусок, а регистрация «кусок × коридор» ставит в
очередь ВСЕ куски проекции, включая стек на силуэте (десятки кусков на
одной хорде). Без проверки видимости вдоль коридора поверхностный
носитель не воспроизводит непрозрачность никакой нормировкой депозита.
Правильная версия (а) = выбор ближайшего попадания на коридор = построчный
рейтрейс — это уже не локальная правка, а механика прослеживания
(пункт 6 старой цели). Код откачен к `ded1e37` (работает в своём режиме).

**Три попытки и числа** (cavity05, nd=26, it=30):
1. bbox-регистрация, депозит w·L·wn: **inf** — кусок получает
   area·|cos|/csec доставок вместо одной (А1518).
2. + нормировка w·L·csec/area (поток трубки): **inf медленнее** — тяжёлые
   депозиты csec/area ≫ 1 на кусках мельче трубки; рост ×10/итерацию.
3. + растеризация проекции, окклюзия по перекрытию u-интервалов,
   депозит min(csec/area, |cos|) (гейн < 1): плато 6.4× / 25× / 3.3× /
   1.8× модели по lev=4…7 — неподвижная точка К-кратна истинной, K =
   лишние регистрации (силуэтный стек + консервативная растеризация;
   K ≈ 6 по первой итерации: E₁ = 38 против аналитических 6.3).

**Чему это учит (записано, чтобы не повторить):**
- непрозрачный перенос поверхностями требует ВИДИМОСТИ вдоль каждого
  коридора — это бинарное «кто ближе», оно не выводится ни из какой
  квадратуры;
- депозит обязан быть ограничен проекцией куска (w·L·|cos|), иначе
  куски мельче клетки дают тяжёлые хвосты и расходимость;
- режим «клетка ≥ кусок» (lev=4: 1.18×) остаётся единственным
  измеренно честным режимом поверхностного свипа в текущем виде.

**ДВЕ РЕАЛЬНЫЕ ДОРОГИ ДАЛЬШЕ (выбор за пользователем):**
(А) оставить поверхности и сделать честную видимость: на каждый
(коридор, направление) — ближайшее пересечение (построение похода
рейтрейсом по кусковым bbox с шагом DDA). Цена: построение похода
дорожает до трассировочной, но поход остаётся материализованным и
Мортон-локальным; итерация — прежняя. Это механика «пункт 6», переносимая
потом на зеркала/преломление.
(Б) объёмное поле (DG в клетках), поверхность — граничное условие:
сходимость по построению, никаких стеков; цена — память объёма
(×8 трафика и ×800 поля уже на cavity/lev5, V/(A·δ) на реальных сценах).
Режим «клетка ≥ кусок» при этом остаётся допустимым упрощением (А).

## §844 (11-09): ПЛАН — ФРОНТАЛЬНЫЙ СВИП НА ПИРАМИДЕ (ДО КОДА)

**Директива пользователя:** фронт сам содержит видимость; честный проход
пустых пространств; работа в степени детализации геометрии; никакого
рейтрейса. Диффузная доля отражения допускает огрубление (широкая доля —
грубые клетки фронта валидны); узкая доля/зеркало потребует детализации —
это правило LOD фронта, записать в общий принцип.

**РАЗБОР ОШИБКИ §835–§843.** Перенос «скаляр вдоль нитки» не является
фронтом: у коридора нет сечения пучка, и видимость в нём не содержится —
потому и потребовала явного вычисления (А1515–А1518), которого не делает
никакая квадратура. Фронт = поток через наветренные грани клеток:
видимость — следствие локальности, свет физически не проходит сквозь
поглотившую поверхность.

**ПОСТАНОВКА (минимальный дельта-шаг от `ded1e37`).**
1. Порядок визитов — ИЗ МАРША (§834, топологически чист на
   направлениях): на коридор перечисляются ИСТИННЫЕ клетки хода луча, а
   не Morton-приближение. Это устраняет А1514 (регистрация мимо
   коридора: кусок принадлежит своей клетке, коридор через неё проходит
   по построению).
2. Регистрация — по клетке владения, ОДИН раз на (кусок, коридор);
   стеки на силуэте корректны сами: первый кусок полосы поглощает пучок
   (T=0), следующие получают только его переизлучение Lsurf — физика, а
   не выборка.
3. Депозит ограничен с двух сторон (урок А1518):
   `w·L·min(csec, area·|cos|)/area` — не больше потока трубки и не
   больше потока через проекцию куска. Гейн итерации < 1 при любом
   отношении клетка/кусок.
4. Пустота пропускается честно и даром: L не меняется (нет
   взаимодействия); позже — прыжок через coarse-клетки верхних уровней
   (пирамида их уже несёт), это ускорение обхода, не изменение физики.
5. Поле остаётся на кусках (E на куске = суммарная облучённость);
   фронт при этом несёт L по клеткам — К83 не тронута, память поля не
   растёт. Объёмный DG (поле в клетках) — следующий уровень ТОЙ ЖЕ
   линии (граневые потоки), не этого шага.
6. Направления — прежние 6/26; бонус постфактум: марш топологически
   чист для ЛЮБОГО направления, значит переход к продуктовой квадратуре
   (§843-лестница) больше не упирается в А1512.

**ДЕЛАТЬ.**
- `sweep.c`: поход из марша `hz_pyr_march` (он же в mode 0) вместо
  сортировки (линия, u); визит клетки = её куски; депозит по п.3.
- `swee3`: печать доли линий с ≥2 визитами (должна вернуться к ~50 %
  на всех lev) и счётчиков доставок.
- Приёмка и НК — без изменений.

**ПРЕДСКАЗАНИЯ.**
- G1 (главная): E_avg(cavity05, nd=26) по lev=4…7 живёт в [0.8, 1.4]×
  модели, коллапса (сейчас 1.18× → 0.068×) нет: размах по lev ≤ 1.5×.
- G2: доли доставок/визитов ≈ 50 % на всех lev (тёмный вход, светлый
  выход), не падают с измельчением.
- G3: room, dirs=26: ×модели не хуже §842-уровня, потеряно мало.
- НК: le=0 → E≡0; rho=0 → рециркуляция ровно 0; noprop → E≡0.
- G5: цена — марш добавляет обход клеток коридора; полоса не хуже 4 ГБ/с
  на grid1000 при nd=26.
**АУДИТ ДО КОДА — при написании плана кода; находки нумеровать А1519+.**

### §844: ДОКЛАД (ПЕРВЫЙ) — РЕГИСТРАЦИЯ ПОКРЫТИЯ ТРЕБУЕТ ТОЧНОЙ РАСТЕРИЗАЦИИ С ГЛУБИНОЙ; ДВА НАИВНЫХ ВАРИАНТА СНЯТЫ ЗАМЕРОМ; КОД ОТКАЧЕН

**Сделано и понято.** Марш §834 как порядок визитов чинит только ПОРЯДОК,
не ПОКРЫТИЕ: при регистрации по клетке владения лестница lev совпала со
старой до последней цифры (1.18× → 0.068×) — пропуски пересечений (А1514)
остались. Переключение регистрации на bbox-клетки восстанавливает
покрытие (одно-визитных линий 0.8–1.0 %, доставки 96 %), но даёт
расходимость: bbox ЗАВЫШАЕТ след куска измеренно — pos = 11 167 клеток
на 2 208 кусков (≈5 клеток/кусок) при средней площади куска 0.0145 м²
(проекция ≈0.007 м², клетка 0.04 м² — inflation ×14): кусок получает
~2.9 трубок вместо 0.18 истинных, гейн итерации > 1. Дедуп (кусок,
линия) двумя сортировками — (линия, кусок, ход) затем (линия, ход) —
работает и оставлен в истории правок. НК rho=0 стационарен — обратной
связи нет, лишние регистрации уже в первой итерации.

**Вывод (А1519 [ЗАМЕРЕНО]):** покрытие с полнотой И точностью
одновременно не даёт ни регистрация по центроиду (полнота без точности —
А1514), ни по bbox (точность без полноты — А1519). Нужна точная
пересечение-геометрия: на каждое направление — программная растеризация
проекций кусков в коридорную сетку С ГЛУБИНОЙ (ближайшее попадание
держит трубку, остальные — перекрыты). Это и есть «видимость фронта
бесплатно», но как ИНСТРУМЕНТ построения похода: z-буфер коридоров
одного разрешения с сеткой, стоимость построения ~трассировочная,
итерация — прежняя материализованная.

**Дальше — план до кода (А1519-план):**
1. растеризатор: проекция треугольника в (t1,t2)-пиксели (точная
   перекрёстная проба центра + покрытие консервативным контуром —
   НЕСОВМЕСТИМЫ; выбрать одну, фальсификатор — K регистрации по
   первой итерации: обязан быть 0.9–1.1 от аналитики area_proj/cell²);
2. глубина: для каждого (коридор, направление) ближайший по u кусок;
3. депозит — трубкой w·L·min(csec, area·an)/area (ограничен с двух
   сторон, гейн < 1 — урок А1518);
4. приёмка прежняя G1 (нет коллапса по lev) + новая G2 (K-тест);
   НК le=0/rho=0/noprop.
Код шага откачен к `ded1e37`; rb-история попыток — в этом докладе.

### §844: ДОКЛАД (ВТОРОЙ) — ТРУБНЫЙ ДЕПОЗИТ С НОРМИРОВКОЙ СТАБИЛЕН, НО СПАД ПО lev СОХРАНЯЕТСЯ; ПОВЕРХНОСТНОМУ НОСИТЕЛЮ НУЖНА ТОЧНАЯ ГЕОМЕТРИЯ ПЕРЕСЕЧЕНИЙ

**Директива пользователя уточнена:** фронт «упирается во всё само»,
рейтрейс — роскошь; достаточно аккуратного движения фронта.

**Замер (cavity05, nd=26, it=30):** bbox-регистрация + дедуп (кусок,
линия) двумя сортировками + трубный депозит
`w·L·min(csec, area·an)/area` с делением на число коридоров куска:

| lev | ×модели |
|---|---|
| 4 | 2.71 |
| 5 | 1.84 |
| 6 | 0.50 |
| 7 | 0.118 |

Расходимости НЕТ (гейн < 1 — ограничение депозита работает), НК чисты.
Но спад по lev 23× остался: деление на bbox-число коридоров не
эквивалентно истинному числу пересечений треугольника (bbox-коридоров
~46 при истинных ~11.5 на lev7; на грубых lev — наоборот, избыток).
**Цепочка замеров дня замыкается:** (1) депозит w·L·|cos| без покрытия —
коллапс (А1514); (2) с bbox-покрытием без нормировки — расходимость
(перекрытие стеков); (3) с нормировкой на bbox-коридоры — стабильно, но
K-кратная ошибка уровня (bbox ≠ треугольник). Каждая следующая правка
лечит предыдущую болезнь и вскрывает следующую — потому что
поверхностному носителю нужна ТОЧНАЯ геометрия пересечения куска с
трубкой (растеризация/глубина), а она отвергнута как избыточная.

**СЛЕДСТВИЕ (для решения):** «фронт сам содержит видимость» строго
верно только при ОБЪЁМНОМ поле — фронт переносит поток клетка-в-клетку
через грани, непрозрачная клетка поглощает и переизлучает, куски читают
инфлюкс СВОЕЙ клетки; ни пересечений «коридор × кусок», ни растеризации
не существует как задач. Это и есть «аккуратное движение фронта».
Цена — память поля в клетках; на пирамиде (пустота схлопывается,
занятые листья уже существуют) она умеренная, счёт клеток фронта ≈
счёт занятых листьев + грубые клетки пустоты (cavity05/lev5: ~8–9 тыс.
против 2 208 кусков — ~×2.4 трафика, ~МБ поля).
**ПРОПОЗАЦИЯ §845:** объёмный фронт нулевого порядка (L на клетку на
направление) на существующей пирамиде; куски — приёмники инфлюкса своей
клетки (share по площади); обход/Morton/счётчики — из §836–§842.
Фальсификаторы прежние: G1 ×модели не зависит от lev; НК le=0/rho=0.

## §845 (11-09): ПЛАН — ОБЪЁМНЫЙ ФРОНТ НУЛЕВОГО ПОРЯДКА: L НА КЛЕТКУ НА НАПРАВЛЕНИЕ (ДО КОДА)

**Выбор пользователя:** пропозация §844 принята. Директива синтетики
(§842-приписка) действует: сцены — cavity05/room/шары, никаких внешних.

**ПОСТАНОВКА.** Носитель радианса направления — КЛЕТКА, не кусок и не
линия. На направлении фронт L идёт по клеткам В ПОРЯДКЕ ХОДА ЛУЧА (марш
§834); занятая клетка: (1) куски клетки получают инфлюкс СВОЕЙ клетки —
Ed_p += w_d·L_in·|ω·n_p|; (2) клетка переизлучает — L_out =
Σ_p(Le + ρ·E_prev/2π)·area_p·|ω·n_p| / cntot — проекционно-площадочное
СРЕДНЕЕ радианса кусков клетки (та же формула, что в mode 1 сегодня);
L = L_out. Пустая клетка: L не меняется — пропуск даром; позже — прыжок
через coarse-клетки пирамиды (ускорение обхода, не физика). Тёмный вход —
ТОЛЬКО граница домена. Линия как понятие устраняется вовсе; счётчики
nline/nline1/nline2 в этом режиме не заполняются — вместо них доля
визитов с L_in > 0 (доставки).

Почему это снимает А1514 по построению: кусок принадлежит ровно одной
клетке (владение, А1513: кратность 1.00); луч, попадающий в кусок,
обязан пересечь её клетку, а марш перечисляет все занятые листья по
ходу луча. Ни «коридор × кусок», ни растеризации, ни видимости как
отдельной задачи не существует — непрозрачность клетки выражается тем,
что её L_out не зависит от того, что за ней.

**ДЕЛАТЬ.**
1. `sweep.c`: новый `mode = 2`. Поход направления: марш `hz_pyr_march(om)`
   → `vindex`; фильтр кусковых листьев, сортировка ПО ОДНОМУ ключу —
   рангу марша (без line, без travel-ключа: марш уже направлен).
   Предвычисление wcn/wn/kd/wpid как в mode 1. Итерация: L_in = L;
   депозит `Ed[p] += w_d·L_in·wn[e]`; Lsurf по формуле клетки; L = Lsurf.
   Баланс: absorbed/emitted/recycled/lost — как в mode 1 (книжка, не поле).
2. Приборы: nvisit, ndep (L_in > 0); печать доли доставок; счётчик
   inversions марша — обязан быть 0 на целочисленных направлениях.
3. `swee3`: ключ `mode=2`; печать ×модели (hz_sw_exitwsum) и лестницы
   lev=4…7 на cavity05; room — отдельной строкой.

**АУДИТ ДО КОДА.**
- **А1520 [ПРОЧИТАНО В КОДЕ]** формула клетки уже стоит в mode 1
  (`Lsurf /= lf->cntot`, депозит `w·L·wn`) — объёмный фронт отличается
  ТОЛЬКО порядком (марш вместо (линия, ход)) и непрерывностью L через
  пустоту и смены линий. Риск реализации — «новый» режим, дублирующий
  mode 1 с точностью до сортировки: правка обязана сидеть в общей
  машинерии, различаясь строителем похода.
- **А1521 [ТРЕБУЕТ ПРОВЕРКИ ДО КОДА]** §844-первый доклад: «при марше как
  порядке визитов и регистрации по клетке владения лестница совпала со
  старой до последней цифры». Это возможно ТОЛЬКО если L по-прежнему рвался
  на сменах line-ключа (строитель похода заменён, логика тёмного входа —
  нет). Перед кодом сверить rb-историю `60ea591`: если тёмный вход уже был
  только на границе домена — механизм коллапса не объяснён, и §845
  повторит тот же прогон под новым именем. Проверка — час, провал плана —
  стоп и переатрибуция.
- **А1522 [РАССУЖДЕНИЕ]** ray effect остаётся: угловая квадратура nd=26 никуда
  не делась, зазор к модели 4.19 может остаться ~0.6× (плато §843). Объёмный
  фронт лечит ПРОСТРАНСТВЕННУЮ деградацию (А1514), не угловую. Фальсификатор
  поэтому — незавиСИМОСТЬ от lev, а не достижение 1.0×.
- **А1523 [РАССУЖДЕНИЕ]** подельные величины: депозит без деления на площадь
  даёт мелким кускам малый E — но это и есть физика (E = Σ w·L·|cos|, поток
  на единицу площади); среднее по сцене взвешено area. Проверка непрерывности:
  кусок, разрезанный сеткой на два куска-соседа в одной клетке, получает
  одинаковый L_in — различие только в |cos|.
- **А1524 [РАССУЖДЕНИЕ]** цена: марш на каждом из nd направлений при
  построении похода (nd × O(узлов)); итерация — nd × (кусковые листья +
  их куски), то же, что mode 1. Память: nd походов ≈ nd×nt×28 Б — на
  cavity05 пренебрежимо; лестницу ND на этом шаге не гонять.

**ПРЕДСКАЗАНИЯ.**
- G1 (ГЛАВНАЯ, фальсификатор пользователя): E_avg(cavity05, nd=26,
  ρ=0.5, Le=1) по lev=4…7 живёт в [0.8, 1.4]×модели (4.19) БЕЗ
  монотонного коллапса (сейчас 1.18× → 0.068×, §843-добавление; на
  трубном депозите §844 — 2.71× → 0.118×); размах по lev ≤ 1.5×.
- G2 (механизм): доля визитов с доставкой (L_in > 0) ≥ 40 % на ВСЕХ lev
  и не падает с измельчением (сейчас 56.6 % → 10.9 % по lev=5→7).
- G3: room, dirs=26 — ×модели не хуже §842-уровня (0.303×), потеряно
  конечно и стабильно; инверсий марша 0.
- G4 (НК): le=0 → E≡0; rho=0 → рециркуляция ровно 0, стационар с первой
  итерации; noprop → E≡0.
- G5: цена: с/итерацию ≤ 1.5× mode-1 на cavity05; полоса ≥ 4 ГБ/с на nd=26.

**НЕГАТИВНЫЙ КОНТРОЛЬ.** le=0 (полю неоткуда родиться), rho=0 (ряд
стационарен мгновенно), noprop (фронт не переносится).
**ПРИЁМКА.** G1 — главная; G2, G4; G3, G5 — сопутствующие.
**ОТКРЫТО после шага:** угловой ray effect (ND-лестница теперь честная —
марш топологически чист на любом направлении, А1512 снят); прыжок фронта
через coarse-пустоту пирамиды (accel, не физика); граневые потоки
(первый порядок) — следующий уровень той же линии.

### §845: ДОКЛАД — КОЛЛАПС ПО lev СНЯТ: ×МОДЕЛИ СТАБИЛЕН (РАЗМАХ 0.2 %); СВИП ВЫШЕЛ НА НЕПРЕРЫВНУЮ ФИЗИКУ; «МОДЕЛЬ СЛОЯ» §842 — АРТЕФАКТ LINE-ПОСТАНОВКИ

**Сделано.** `mode=2` — объёмный фронт: L на клетку на направление,
порядок визитов — марш §834 (один ключ, без линии), тёмный вход только
на границе домена, куски получают инфлюкс своей клетки
(`w·L_in·|cos|`), клетка переизлучает проекционно-площадочным средним
(формула mode 1). Прибор против no-op (см. А1525): FNV-хеш
последовательности листьев похода, печатается всегда.

**А1521 разрешён.** rb-истории §844-первого НЕ СУЩЕСТВУЕТ — попытки
откачивались до коммита (git: только правки плана; stash посторонний).
**А1525 [ИЗ ИСТОРИИ]:** «совпала со старой до последней цифры» на четырёх
lev для float-сумм возможно только при неизменном порядке накопления —
патч §844-первого был no-op, и вывод «марш чинит порядок, не покрытие»
снят с пустого прогона. Прибор подтвержден: хеши mode 1 и mode 2
различаются (460624259b5778fa vs 412de42ae5f352b2, lev=5).

**Замер (cavity05, nd=26, ρ=0.5, Le=1, it=30):**

| lev | клетка, м | E_avg | ×§842-модели (4.19) | к непрерывной (12.566) |
|---|---|---|---|---|
| 4 | 0.2 | 12.534 | 2.991 | 0.9974 |
| 5 | 0.1 | 12.556 | 2.996 | 0.9992 |
| 6 | 0.05 | 12.557 | 2.997 | 0.9993 |
| 7 | 0.025 | 12.556 | 2.996 | 0.9993 |

Коллапса нет: размах ×модели по lev **0.2 %** (было 1.18× → 0.068×,
17×; у трубного депозита §844 — 23×). **G1-фальсификатор ПОПАДАНИЕ по
сути; предсказанное окно [0.8, 1.4] промахнуто ВВЕРХ, и промах
продуктивен:**

**А1526 [ЗАМЕРЕНО, ПРИНЦИПИАЛЬНОЕ]:** свип сошёлся к НЕПРЕРЫВНОЙ физике
2πLe/(1−ρ) = 12.566 (замер 12.557, 0.9993×). «Модель слоя» §842 (4.19)
предполагает тёмную входную полусферу направлений — это свойство
LINE-постановки (тёмный вход на смене ключа), артефакт, а не физика: в
замкнутой полости на равновесии КАЖДОЕ направление освещено. Объёмный
фронт не имеет тёмных входов внутри домена — и выходит на правильное
равновесие. Подтверждение независимым путём: НК rho=0 даёт E = 6.2793 =
2π·Le·0.9994 ровно стационарно с первой итерации — это и есть
непрерывное двустороннее равновесие без рециркуляции.

**Аудит исполнения.**
1. G2 (механизм) ПОПАДАНИЕ: ndep/nvisit на cavity05 = 250/120/104/101 %
   по lev=4…7 (ndep счётчик НА КУСОК, nvisit НА ЛИСТ — потому >100 %);
   с измельчением доля НЕ падает — каждая поверхность получает свет
   каждого направления.
2. G3 (room, lev=6) ПОПАДАНИЕ по сути: 11.20 = 0.89× непрерывной
   физики, потеряно 0.79 (законно: проёмы/выход за домен); у mode 1 на
   той же сцене 0.385×модели.
3. G4 (НК) ПОПАДАНИЕ: le=0 → E≡0; noprop → E≡0; rho=0 → стационар с
   первой итерации, фактор ряда ровно 0.
4. G5 (цена) ПРОМАХ с записью: 2.28 ГБ/с эффективной на lev=5 (окно
   ≥4). Причина: трафик-модель считает только визиты, а марш-строитель
   добавляет nd обходов пирамиды — на lev=7 это 45.8 мс/итерацию
   (1.37 с на 30 итераций) против 1.7 мс/итерацию на lev=5. Ускорение
   строителя — не физика, отдельная статья (coarse-прыжок пустоты).
5. ccheck CLEAN (gcc-analyzer, clang-tidy, cppcheck); ASan+UBSan чисто
   (cavity05, lev=5, dirs=26).

**СЛЕДСТВИЕ.** Поверхностный носитель на клетках с фронтальным порядком
СХОДИТСЯ;chain замеров §843–§844 (деградация линий → расходимость bbox →
K-кратность растеризации) закрыт: видимость не потребовалась как
отдельная задача — она содержится в порядке фронта, как и предсказано.

**СЛЕДУЮЩИЙ ШАГ:** угловой остаток. На lev-плато 0.999× непрерывной
физики при nd=26 следующий зазор — угловой (ray effect, А1522): ND-лестница
теперь законна (марш чист на любом направлении, А1512 снят) — 26 → 72 →
288 на cavity05; параллельно coarse-прыжок пустоты в строителе похода
(цена lev=7). Сцены — по-прежнему синтетика.

### §845-бис: ДОКЛАД — ND-ЛЕСТНИЦА НА ОБЪЁМНОМ ФРОНТЕ: СРЕДНЕЕ СТОИТ НА НЕПРЕРЫВНОЙ ФИЗИКЕ, ROOM УЛУЧШАЕТСЯ С nd; ИНВЕРСИИ МАРША ЗАМЕРЕНЫ (А1527)

**Прибор добавлен:** сумма инверсий марша по направлениям (`ninv`, печать
в swee3); подпись трафика исправлена на `dirs=` (печатался код таблицы,
а не число направлений).

**Замер (cavity05 lev=6 и room lev=6, ρ=0.5, Le=1, it=30):**

| dirs | nd | E_avg cavity | к непрерывной | room E | к непрерывной | инверсии |
|---|---|---|---|---|---|---|
| 26 | 26 | 12.557 | 0.9993 | 11.20 | 0.891 | 159 099 |
| 12x6 | 72 | 12.542 | 0.9981 | — | — | 525 300 |
| 24x12 | 288 | 12.543 | 0.9982 | 12.11 | 0.964 | 2 073 564 |

**Находки.**
- **А1527 [ЗАМЕРЕНО]:** инверсии марша НЕ нулевые даже на nd=26 (порядок
  детей по проекции центра — ПРИБЛИЖЕНИЕ хода луча, а не точный ray
  order; ~6.1 тыс. на направление на cavity05, растут с nd). Уточнение
  тезиса §844: марш снимает А1512 (инвариант линии не нужен, ходится
  ЛЮБОЕ направление), но «топологически чист» ≠ «точный ход луча».
  Существенно: порядок Гаусса—Зейделя влияет на СКОРОСТЬ сходимости, не
  на неподвижную точку — корректность подтверждена НК и выходом на
  непрерывную физику; инверсии — статья цены, не правильности.
- **Среднее нечувствительно к nd в ЗАМКНУТОЙ полости:** 12.557 → 12.542
  → 12.543 — баланс энергии держит среднюю облучённость уже при nd=26;
  угловая ошибка живит в РАСПРЕДЕЛЕНИИ (видима будет в сборе/сборке
  картинки, не в E_avg). Предсказание G «среднее растёт к физике с nd»
  на cavity снято как непроверяемое этим прибором.
- **Room — открытая геометрия, угловая ошибка видима и в среднем:**
  0.891 → 0.964 непрерывной при nd 26 → 288. Это первый замер, где
  ND-лестница ЧЕСТНО сходится (§843-лестница на mode 1 была сломана
  А1512 и дала плато 0.61× от неверной модели).
- Цена ∝ nd линейно (трафик 4.07 → 11.26 → 45.0 МБ/итерацию; 0.225 →
  0.663 → 2.67 с на 30 итераций на cavity05/lev=6); полоса эффективная
  стабильна. Построение похода (nd маршей пирамиды) — домирующая статья
  на старших nd: coarse-прыжок пустоты остаётся открытым ускорением.
- НК на nd=288: le=0 → E≡0; rho=0 → стационар с первой итерации
  (6.2772 = 0.9991×2πLe). ccheck CLEAN.

**СЛЕДУЮЩИЙ ШАГ (выбор за пользователем):** (1) coarse-прыжок пустоты в
строителе похода — цена старших nd и lev; (2) граневые потоки (первый
порядок фронта клетка→клетка) — следующий уровень той же линии; (3)
проверка углового распределения (сбор по пикселю, T5-линия) — где
 ray effect от nd=26 станет видимым.

### §845-в: ДОКЛАД — ПИРАМИДА В СТРОИТЕЛЕ ПОХОДА ЛИШНЯЯ: ПРЯМАЯ СОРТИРОВКА ПО ПРОЕКЦИИ ×12.7 ДЕШЕВЛЕ, ТОЧКА ТА ЖЕ; «COARSE-ПРЫЖОК» — ЭТО НЕ ОБХОД, А ЕГО ОТСУТСТВИЕ

**Разбор перед кодом.** Марш уже прыгает пустоту структурно (внутренние
узлы существуют только над непустыми, «ПУСТ — прыжок»): дорогая часть
строителя — не пустота, а САМ ОБХОД пирамиды — на каждого из 8 детей
каждого узла двоичный поиск в массиве уровня, nd раз. А порядку визитов
пирамида не нужна вовсе: порядок марша есть сортировка листьев по
проекции центра на ω (та же `pyr_slab`), и она строится напрямую.

**ДЕЛАЛ.** `hz_sw_opts.build`: 0 — марш (умолчание), 1 — прямой
строитель: O(nleaf) вычислений проекции + одна сортировка кусковых
листьев, ноль поисков, ноль обхода. Сборка похода вынесена в общую
`sw_walk_assemble` (оба строителя идентичны после упорядоченного списка).

**Замер (cavity05, ρ=0.5, Le=1, it=30, mode=2, build=1):**

| конфиг | марш (build=0) | прямой (build=1) | E_avg 0 / 1 | инверсии 0 / 1 |
|---|---|---|---|---|
| lev=6, nd=26 | 0.228 с | 0.018 с (**×12.7**) | 12.5571 / 12.5593 | 159 099 / **0** |
| lev=6, nd=288 | 2.667 с | 0.225 с (**×11.9**) | 12.5425 / 12.5421 | — / 0 |
| lev=7, nd=288 | — | 0.324 с | — | — / 0 |

Полоса эффективная: 6.3–6.6 ГБ/с на lev=6 (было 0.53), 4.44 ГБ/с на
lev=7, 9.18 ГБ/с на room — G5 §845 (≥4 ГБ/с) теперь ВЫПОЛНЕНА на всей
лестнице. Комната nd=288: 12.00 (0.955 непрерывной; у марша 12.11).

**Находки.**
- **А1528 [ЗАМЕРЕНО]:** неподвижная точка НЕ зависит от строителя в
  пределах float-шума носителя: cavity 1.8e-4 относительного расхождения
  (E хранится во float, порядок суммирования разный); на room — 0.9 %:
  длинные цепочки доставки через тонкие стены усиливают накопленный
  округлительный дрейф. Это подтверждает тезис А1527 (порядок — не
  ответ) измеренно; если когда-нибудь понадобится жёстче — слот `e` в
  double, отдельное решение о памяти, сейчас не делается.
- **А1529 [ПРОЧИТАНО В КОДЕ]:** инверсии = 0 у прямого строителя по
  построению (сортируем по самой величине, детерминированный
  тай-брейк по li); у марша — приближение (А1527). Прямой строитель
  СТРОГО лучше марша для строителя похода; марш остаётся для всего
  остального (скрининг занятости, сбор, отладка).
- НК на build=1: noprop → E≡0; le=0/rho=0 — те же (путь итерации общий).
  ccheck CLEAN (после inline-проверки malloc под gcc-analyzer).

**СЛЕДУЮЩИЙ ШАГ (по порядку):** граневые потоки — первый порядок фронта
клетка→клетка (поток через наветренную грань, а не мгновенный L_in),
затем сбор по пикселю (T5-линия) — где угловой ray effect впервые станет
виден КАК ИЗОБРАЖЕНИЕ.

## §846 (11-09): ПЛАН — ГРАНЕВЫЕ ПОТОКИ / ПЕРВЫЙ ПОРЯДК ФРОНТА: ПЕРЕХВАТ ТРУБКИ ПЛАСТИНАМИ (ДО КОДА)

**ПОСТАНОВКА.** Нулевой порядок mode 2 (§845) в занятой клетке делает
`L = Lsurf` БЕЗУСЛОВНО: луч заменяется средним радиансом кусков клетки
даже если те занимают долю процента сечения трубки — прямой свет сквозь
частично занятую клетку убивается перемешиванием. Первый порядок:
трубка сечения `csec` входит в клетку, куски перехватывают геометрически
точную долю сечения (тень пластин) и переизлучают её, остаток проходит:

    T = max(0, 1 − cntot/csec)              (пропускание клетки)
    L_out = T·L_in + (1 − T)·Lsurf

Депозиты кусков УЖЕ согласованы с этим без правки:
`Σ_p w·L_in·area_p·|ω·n_p| = cntot·w·L_in = (1−T)·w·L_in·csec` при
`cntot ≤ csec` — каждый кусок получает свою долю тени, сумма равна
перехваченному потоку трубки. Предельные случаи: вакуум (`cntot=0`)
→ T=1, луч не тронут — то, что раньше делал пропуск пустоты; стена
(`cntot ≥ csec`) → T=0, `L_out = Lsurf` — в точности поведение §845 на
стенке. Меняется только промежуточный случай.

**ДЕЛАТЬ.** Одно место в итерации mode 2 (`sweep.c`): `L = Lsurf` →
`L = T·L_in + (1−T)·Lsurf`, T по формуле выше; noprop по-прежнему
обнуляет L после клетки. Правки депозитов нет.

**АУДИТ ДО КОДА.**
- **А1530 [РАССУЖДЕНИЕ]:** `T = 1 − cntot/csec` — точная доля тени для
  ОДНОЙ пластины; для нескольких кусков в клетке cntot суммирует площади
  БЕЗ вычитания перекрытий — перехват завышен (затухание слишком быстрое,
  знака известен). С измельчением клетки → точно; на грубых — консервативно
  (в сторону поглощения), что для непрозрачного слоя безопасно.
- **А1531 [РАССУЖДЕНИЕ]:** при `cntot > csec` депозиты кусков суммарно
  превышают поток трубки (перекрытие пластин) — это режим «клетка есть
  стена»: T=0 зажимает L_out, избыток означает многослойность, deposits
  делят уже не трубку, а перехват стены. Приемлемо; следить за балансом.
- **А1532 [РАССУЖДЕНИЕ]:** предсказание эффекта: cavity05 в равновесии
  изменится слабо (радианс кусков ≈ L_in, T·L_in + (1−T)·Lsurf ≈ старое);
  room — открытая геометрия с частично занятыми клетками на путях прямого
  света — обязана вырасти к непрерывной физике. Если room НЕ вырастет —
  модель перемешивания не была источником зазора, переатрибуция.
- **А1533 [РАССУЖДЕНИЕ]:** схема осталась линейной по E_prev (T не зависит
  от поля) — ряд по рассеянию сходится, фактор ≤ ρ сохраняется.

**ПРЕДСКАЗАНИЯ.**
- G1: cavity05 по lev=4…7 остаётся в [0.95, 1.05] непрерывной физики,
  размах по lev ≤ 1.5× — стабильность §845 не потеряна.
- G2 (главная): room nd=26 поднимается с 0.891 к непрерывной ≥ 0.93;
  nd=288 с 0.955 к ≥ 0.97. Промах → переатрибуция зазора (А1532).
- G3 (НК): le=0 → E≡0; rho=0 → стационар с первой итерации, значение
  2πLe·(геометрия) не меняется; noprop → E≡0.
- G4: печатный фактор ряда ≤ ρ.
- G5: цена не растёт (одна операция на лист) — полоса не ниже 4 ГБ/с.

**НЕГАТИВНЫЙ КОНТРОЛЬ.** те же le=0 / rho=0 / noprop.
**ПРИЁМКА.** G2 — главная (это единственное содержательное изменение);
G1, G3 — ограда регресса.

### §846: ДОКЛАД — ПЕРЕХВАТ ТРУБКИ ПОСТАВЛЕН: CAVITY СТАБИЛЕН (0.996–0.999 НЕПРЕРЫВНОЙ ПО ВСЕМ lev); G2 ПРОМАХ ПРОДУКТИВЕН — ПЕРЕМЕШИВАНИЕ ПОДКАЧИВАЛО ROOM, А НЕ ЗАНИЖАЛО

**Сделано.** Одно место итерации mode 2: `L_out = T·L_in + (1−T)·Lsurf`,
`T = max(0, 1 − cntot/csec)` — геометрически точная доля тени для одной
пластины, консервативная (без вычета перекрытий) для многих (А1530).
Депозиты не тронуты: их сумма и раньше равнялась `(1−T)·входному потоку
трубки` — баланс сошёлся автоматически, как и записано в плане.

**Замер.**

| конфиг | §845 (нулевого пор.) | §846 (первый пор.) | к непрерывной (12.566) |
|---|---|---|---|
| cavity lev=4 nd=26 | 12.534 | 12.519 | 0.9964 |
| cavity lev=5 | 12.556 | 12.552 | 0.9989 |
| cavity lev=6 | 12.557 | 12.559 | 0.9994 |
| cavity lev=7 | 12.556 | 12.561 | 0.9996 |
| room lev=6 nd=26 | 11.20 | **10.92** | 0.869 |
| room lev=6 nd=288 | 12.00 | 11.98 | 0.953 |

- **G1 ПОПАДАНИЕ:** cavity в [0.996, 0.9996] непрерывной по всем lev,
  размах 0.3 % — стабильность §845 сохранена; равновесие замкнутой
  полости от перехвата не зависит (там L_in ≈ Lsurf, T-члены взаимно
  сокращаются), как и предсказано в А1532.
- **G2 (главная) ФОРМАЛЬНЫЙ ПРОМАХ — room упал, а не вырос
  (10.92 против предсказанных ≥0.93·12.566 = 11.69).** Промах
  продуктивен, **А1534 [ЗАМЕРЕНО]:** нулевое перемешивание
  (`L = Lsurf` без T) НЕ занижало room, а ЗАВЫШАЛО: замена L средним
  радиансом кусков разносила эмиссию по всей трубке — искусственная
  изотропная связь клеток, которой в геометрии нет. Первый порядок её
  убрал — и часть «согласия §845 с непрерывной физикой» на room
  оказалась артефактом перемешивания. Переатрибуция остаточного зазора
  room (0.953 при nd=288): (а) непрерывная физика 2πLe/(1−ρ) — эталон
  ЗАМКНУТОЙ полости, а room теряет 0.79 за периметр законно; (б)
  остаток — угловая квадратура. Честного эталона для ОТКРЫТОЙ сцены в
  проекте сейчас нет — это прибор, который надо сделать (точное
  решение/Интеграл по видимости для коробки), прежде чем мерить зазор.
- **А1535 [ЗАМЕРЕНО]:** Тень-консервативность (А1530) на грубых lev не
  видна: lev=4 отличается от lev=7 на 0.3 % — перекрытия пластин в
  клетке cavity05 пренебрежимы.
- G3 ПОПАДАНИЕ: le=0 → E≡0; noprop → E≡0; rho=0 → стационар с первой
  итерации, 6.2785 = 2πLe·0.9991. G4 ПОПАДАНИЕ: фактор 0.47 ≤ ρ.
  G5 ПОПАДАНИЕ: цена не изменилась. ccheck CLEAN.

**СТАТУС ЛИНИИ.** Объёмный фронт на пирамиде теперь: нулевой слот поля
на куске, L на клетку на направление, марш-порядок (прямой строитель
без обхода пирамиды), первый порядок перехвата. Пространственная
сходимость по lev — 0.3 %; негативные контроли чисты. Следующий шаг по
порядку — сбор по пикселю (T5-линия): первая картинка на новом носителе
и единственный прибор, где угловой ray effect виден как изображение;
параллельно — эталон открытой сцены (из G2-промаха).

## §847 (11-09): ПЛАН — СБОР ПО ПИКСЕЛЮ НА НОВОМ НОСИТЕЛЕ: ПЕРВАЯ КАРТИНКА (ДО КОДА)

**ПОСТАНОВКА.** Камера — ОПЕРАТОР над решённым полем, поле не зависит от
камеры (CLAUDE.md). Сбор: луч из глаза через пиксель → ближайшее
пересечение с куском → яркость пикселя = L_out куска =
`Le + ρ·E_куска/(2π)` (ламберт, двусторонняя конвенция А1471); фон 0 —
тёмное окружение (§842). Поле берётся из mode=2 свипа §845/§846
(build=1). Картинка — в `img/` (правило проекта), формат PPM P6
(без зависимостей).

**ДЕЛАТЬ.** Новый инструмент `tools/pgather.c`: ключи камеры
`eye=X,Y,Z look=X,Y,Z fov=F W=N H=N`, ключи свипа те же (rho/le/it/
dirs/lev/mode=2/build=1), ключ НК `le=0`/`noprop`; вывод
`img/pgather.ppm`. Пересечение луча с треугольником — перебор ВСЕХ
кусков с точным ближайшим t (сцены синтетические: 2.2–17.7 тыс. кусков —
честно и дёшево); марш луча по пирамиде — СЛЕДУЮЩИЙ шаг, не этот.
Приборы: доля лучей с попаданием, средняя/максимальная яркость, t
ближайшего попадания для контрольных лучей, печать Le+ρ·E_avg/2π для
сличения.

**АУДИТ ДО КОДА.**
- **А1536 [РАССУДЖЕНИЕ]:** яркость берётся ЦЕЛИКОМ с куска (flat по
  куску, без интерполяции по треугольнику) — носитель поля и есть кусок;
  при крупной клетке клеточность ВИДНА на картинке — это честная
  визуализация дискретизации, а не дефект; интерполяторы — следующий
  уровень, если понадобятся.
- **А1537 [РАССУЖДЕНИЕ]:** К27-класс (приписка §842) обязателен: два
  КАСАЮЩИХСЯ шара (assets/synth/spheres.obj) — луч, идущий в центр
  диска касания, обязан попасть в БЛИЖНИЙ; ловится печатным t-сравнением
  пачки контрольных лучей, а не глазом.
- **А1538 [РАССУДЖЕНИЕ]:** доля попаданий — прибор замкнутости: в
  замкнутой сцене ровно 1.000; в room < 1 — уходит в проёмы/мимо.
- **А1539 [РАССУДЖЕНИЕ]:** средняя яркость равновесной cavity обязана
  совпасть с `Le + ρ·E_avg/(2π)`: E = Σ w|cos|·L с средним |cos| = 1/2
  даёт L = E/(2π)·(2π)/(π_n)·... точнее L_surf = Le + ρE_avg/(2π) —
  свип уже печатает E_avg, сбор обязан дать ту же L по площади. Допуск
  ±5 % — угловые ошибки распределения.

**ПРЕДСКАЗАНИЯ.**
- G1 (главная, К27): 100 % контрольных лучей через диск касания шаров
  попадают в БЛИЖНИЙ (t_ближ < t_даль на каждом).
- G2: cavity05 изнутри: доля попаданий 1.000; средняя яркость кадра =
  Le + ρ·E_avg/(2π) в ±5 %; чёрных «дыр» (несыктованных кусков) нет.
- G3: room изнутри: кадр ненулевой, доля попаданий < 1 (проёмы),
  записан в img/.
- G4 (НК): le=0 → кадр тождественно чёрный (max = 0); noprop → кадр
  равномерный Le (поле не переносилось, E ≡ 0).
- G5: переборной сбор: 320×240 на spheres.obj ≤ 30 c; цена ∝ W·H·nt.

**НЕГАТИВНЫЙ КОНТРОЛЬ.** le=0 (полю неоткуда родиться), noprop (фронт
не переносился).
**ПРИЁМКА.** G1 — главная; G2 — сличение сборщика со свипом (одна
физика, два прибора); G4 — ограда.

### §847: ДОКЛАД — ПЕРВАЯ КАРТИНКА НОВОГО НОСИТЕЛЯ: СБОР ПО ПИКСЕЛЮ РАБОТАЕТ; К27 ВЫПОЛНЕН (0 ИЗ 3228); СЛИЧЕНИЕ СБОРЩИКА СО СВИПОМ 0.9975–1.023

**Сделано.** `tools/pgather.c` + `build/pgather`: свип mode=2 (build=1)
→ камера-оператор: луч из глаза через пиксель, ближайшее пересечение
перебором кусков (Мёллер—Трумбор), яркость = `Le + ρ·E_куска/(2π)`,
фон 0; вывод PPM P6 в `img/` (правило проекта). Ключи камеры
eye/look/fov/W/H, приёмка `k27=1`, ключи свипа — те же. Приборы:
доля попаданий, средняя/макс яркость, сличение с `Le + ρ·E_avg/2π`.

**Фикстура.** `spheres.obj` — ВОСЕМЬ шаров вдоль луча Ш7, НЕ
касающиеся (кластеризация «двух шаров» дала r=0.65 и r=20.6 — брак
фикстуры, не прибора). Сгенерирована честная `assets/synth/k27.obj`:
два шара r=1 с центрами (0,0,0) и (2,0,0), касание в (1,0,0), 7936
граней, полюса без копий-вершин (приём gen_spheres.sh).

**Замер.**

| приёмка | результат | приёмка |
|---|---|---|
| К27 (k27.obj): лучей через диск касания | **0 из 3228 прошли ближний** | G1 ПОПАДАНИЕ |
| cavity изнутри (глаз 0,0,0, fov 170): попадания | 100.00 % | G2 ПОПАДАНИЕ |
| cavity: L_сбора / L_свипа | **0.9975** (±5 % выдержано) | G2 ПОПАДАНИЕ |
| room изнутри: попадания / сличение | 100 % / 1.0233 | G3 ПОПАДАНИЕ |
| НК le=0 | кадр тождественно чёрный (max=0) | G4 ПОПАДАНИЕ |
| НК noprop | кадр равномерный ровно Le=1.0000 | G4 ПОПАДАНИЕ |
| G5 цена | 4.0 c (cavity, 76800 лучей × 2208 кусков); spheres.obj 27.7 c ≤ 30 | ПОПАДАНИЕ |

Кадры: `img/k27.ppm`, `img/cavity_inside.ppm`, `img/room_inside.ppm`,
НК — `img/nc_le0.ppm`, `img/nc_noprop.ppm`.

**Аудит исполнения.**
1. Сличение сборщика со свипом — замкнутая петля «одна физика, два
   прибора»: сбор читает то же поле, что свипwrites, и средние сходятся
   до 0.25 % (cavity) и 2.3 % (room — угловое распределение ярче/темнее
   по направлениям, среднее по площади кадра смещено конусом камеры).
2. НК noprop дал идеально ровный кадр Le — сбор не вносит никакого
   света сам; чёрный кадр при le=0 — полю неоткуда родиться. Ограда
   работает.
3. G5 ПОПАДАНИЕ на границе: перебор nt на луч на spheres.obj (17.7 тыс.
   граней) — 27.7 с из 30. Марш луча по пирамиде (как у свипа) —
   следующий шаг, цена упадёт до логарифмической по занятым клеткам.
4. ccheck CLEAN; предупреждений нет.

**СТАТУС.** T5-линия на новом носителе собрана из трёх частей: фронт
(§845–846), поле на кусках, камера-оператор (§847). Угловой ray effect
теперь ВИДИМ как изображение — приборы следующие: ND-лестница на кадре
(26 → 288 и разница кадров), интерполяция яркости по куску (А1536),
марш луча по пирамиде. Сцены — синтетика (приписка §842).

## §848 (11-09): ПЛАН — ND-ЛЕСТНИЦА НА КАДРЕ: RAY EFFECT КАК ИЗОБРАЖЕНИЕ (ДО КОДА)

**ПОСТАНОВКА.** §845-бис показал: средняя облучённость замкнутой полости
не чувствует nd (баланс энергии прячет угловую ошибку); room — чувствует
(0.891 → 0.964). Теперь angular error видима В ПИКСЕЛЯХ: кадры одной
камеры при nd=26 / 72 / 288 сравниваются попарно. Ожидание: в cavity
кадр почти не меняется (поле равновесно-однородно), в room разница
живёт у рёбер и стыков (где угловое распределение негладко) и ПАДАЕТ
с nd — это и есть «ray effect видимый».

**ДЕЛАТЬ.** Малый инструмент `tools/ppmdiff.c`: два PPM P6 одинакового
размера → печать средней |Δ|/средней яркости, max |Δ|, доля пикселей
с |Δ| > 10 % диапазона, и max |Δ| по клеткам сетки 16×16 (где разница
живёт). Makefile-цель. Кадры: cavity и room, одна камера, nd=26/72/288.

**АУДИТ ДО КОДА.**
- **А1540 [РАССУЖДЕНИЕ]:** кадры нормированы на свой max — сравнивать
  надо по АБСОЛЮТНЫм яркостям: в ppmdiff яркость восстанавливается из
  PPM (8 бит) × max второго кадра... неверно: 8-битная запись теряет
  масштаб. Прибор честен только при ОДИНАКОВОЙ нормировке: ppmdiff
  сравнивает нормированные кадры и печатает отношение их max (масштаб),
  а Δ мерится в единицах общей шкалы [0,1] от общего максимума. Погрешность
  8-битного квантования ~1/255 ≈ 0.4 % — нижний пол прибора.
- **А1541 [РАССУЖДЕНИЕ]:** разница nd-пар содержит и float-шум носителя
  (E во float, А1528: 0.9 % на room) — пол ожидаемого сигнала. Если
  сигнал ≈ шум на всех nd, вывод «угловая ошибка ниже шумовой полосы
  кадра» — тоже результат (оправдывает nd=26 для диффузной картинки).

**ПРЕДСКАЗАНИЯ.**
- G1: cavity: средняя |Δ| между nd=26 и nd=288 < 1 % шкалы (порядка
  шума квантования) — равновесная полость углово гладка.
- G2 (главная): room: средняя |Δ| (26→288) > 2 % шкалы, монотонно
  падает 26→72→288; локализована в клетках сетки у рёбер/проёмов.
- G3: разница nd=26↔72 больше, чем 72↔288 (сходимость, не дрейф).
- НК: ppmdiff кадра с самим собой даёт ровно 0; кадры разного размера —
  отказ, а не сравнение.

### §848: ДОКЛАД — RAY EFFECT ВИДЕН НА КАДРЕ: ND=26 ДАЁТ 2.2 % (CAVITY) И 4.6 % (ROOM) ШКАЛЫ; ND=72 УЖЕ СХОДИТСЯ (0.3–0.8 %); G1 ПРОМАХ ПРОДУКТИВЕН

**Сделано.** `tools/ppmdiff.c` (+ цель Makefile): разность двух кадров
P6 в ОБЩЕЙ шкале (отношение максимумов печатается), средняя/max |Δ|,
доля пикселей >10 %, карта max |Δ| по сетке 16×16. НК «сам с собой» —
ровно 0; разные размеры — отказ.

**Замер (кадры 320×240, одна камера, свип §846, поле одно на nd).**

| пара nd | cavity (внутри сферы) | room (внутри) |
|---|---|---|
| 26 ↔ 72 | 1.57 % | 4.36 % |
| 72 ↔ 288 | 0.77 % | **0.33 %** |
| 26 ↔ 288 | **2.17 %** | **4.61 %** |

**Аудит исполнения.**
1. **G2 ПОПАДАНИЕ:** room 26↔288 = 4.61 % > 2 %, монотонно (4.36 →
   0.33) — ray effect на кадре реален и измерен впервые в проекте.
2. **G1 ФОРМАЛЬНЫЙ ПРОМАХ:** cavity 26↔288 = 2.17 % против
   предсказанных < 1 %. Продуктивен: угловая ошибка видима даже в
   равновесной полости — равномерно-гладкая по кадру (карта 2–7 % без
   структуры у рёбер), это СМЕЩЕНИЕ уровня вдоль луча, а не артефакт
   стыков. Дополнительно: сличение L_сбора/L_свипа по той же лестнице
   0.9975 → 0.9996 → 1.0001 — то же плато независимо.
3. **А1542 [ЗАМЕРЕНО, ГЛАВНОЕ]:** nd=72 уже сходится: разница 72↔288
   0.33 % (room) и 0.77 % (cavity) — на полке 8-битного квантования
   (0.4 %) и float-шума носителя (А1541). Практический вывод: для
   диффузной картинки nd=72 достаточно; nd=26 — единственная ступень с
   заметной ошибкой. Лестница сходится ~двукратно на ступень
   (26→72→288: 2.17 → 0.77).
4. Локализация: карта room показывает умеренный столбец 4–6 % в
   направлении стыков стен (вертикальная полоса клеток 7–9), остальное
   ровно — предсказание G2 про рёбра подтверждено частично: рёбра
   выделяются, но слабо.
5. ccheck CLEAN.

**СЛЕДСТВИЕ ДЛЯ ЛИНИИ.** Квадратура nd=72 — рабочая точка кадра; nd=26
годится для быстрых прогонов физики (средние), не для картинки.
Следующие шаги (по убыванию ценности): марш луча по пирамиде в сборе
(цена перебора — 27.7 с на spheres.obj), интерполяция яркости по куску
(А1536), эталон открытой сцены (§846-G2).

## §849 (11-09): ПЛАН — МАРШ ЛУЧА КАМЕРЫ ПО ПИРАМИДЕ: СБОР БЕЗ ПЕРЕБОРА (ДО КОДА)

**ПОСТАНОВКА.** Сбор §847 перебирает ВСЕ куски на каждый пиксель —
27.7 с на spheres.obj. Замена: 3D-DDA по базовой сетке (Аманатидес—Ву)
со срезкой луча в bbox сцены; в каждой пересечённой клетке — куски из
СПИСКА КЛЕТКИ, ближайший t. Цена O(шагов × log nleaf + проверенных
кусков) на луч.

**ЛОВУШКА, А1543 [ПРОЧИТАНО В КОДЕ]:** CSR пирамиды — по ВЛАДЕНИЮ
(центроид): треугольник лежит в клетке центроида, а луч может пересечь
его КРАЙ в соседней клетке — сбор по владельческому CSR ДЫРЯВЫЙ
(пропуски на границах клеток). Консервативен только bbox-признак
(лист помечен всеми пересекающими bbox-ами — он и есть в `leaf`).
Значит сбору нужен ОТДЕЛЬНЫЙ bbox-CSR: counting-sort «кусок → все
клетки его bbox» (два прохода, память ≈ nmarks записей; на cavity05
это ×5 к числу кусков — А1457, приемлемо). Тогда полнота по
построению: точка попадания P лежит в какой-то клетке → bbox куска
содержит P → кусок помечен эту клетку → луч клетку пересекает → кусок
проверен.

**ДЕЛАТЬ.**
1. `pgather.c`: сбор bbox-CSR (два прохода по nmarks), DDA-обход
   (срезка луча срезом сцены, шаг по tNext осям, тай-брейк по оси —
   детерминизм), в клетке — двоичный поиск leaf_id + проверка кусков
   bbox-списка, ближайший t.
2. Ключ `gather=dda|brute` (умолчание dda; brute остаётся путём
   верификации). Приборы: среднее число клеток и кусков на луч.
3. Приёмка — побитовая: кадр dda ОБЯЗАН совпасть с кадром brute до
   последнего байта (ближайший t не зависит от порядка проверки; код
   пересечения тот же).

**АУДИТ ДО КОДА.**
- **А1543** — выше: владельческий CSR дыряв, нужен bbox-CSR.
- **А1544 [РАССУЖДЕНИЕ]:** луч мимо сцены — срезка с bbox даёт пустой
  интервал, ноль шагов; фон 0 как раньше.
- **А1545 [РАССУЖДЕНИЕ]:** кусок с bbox в нескольких клетках вдоль луча
  проверяется многократно — ближайший t от этого не меняется (строгое
  «меньше» в сравнении), это цена, не ошибка.
- **А1546 [РАССУЖДЕНИЕ]:** тай-брейк DDA при равных tNext по осям —
  фиксированный порядок осей; детерминизм кадра сохранён.

**ПРЕДСКАЗАНИЯ.**
- G1 (главная): кадры dda и brute ПОБИТОВО равны (cmp) на cavity05,
  room_thin и k27.obj при прочих равных.
- G2: цена: cavity 320×240 4.0 с → ≤ 0.5 с; spheres.obj 27.7 с → ≤ 3 с.
- G3: прибор: среднее проверенных кусков на луч ≤ 5 % nt.
- НК: le=0 → кадр чёрный и в dda; лучи мимо сцены → 0 шагов DDA.

**ПРИЁМКА.** G1 — побитовое тождество, главная; G2/G3 — цена.

### §849: ДОКЛАД — СБОР ХОДИТ ПО ПИРАМИДЕ: КАДРЫ ПОБИТОВО РАВНЫ ПЕРЕБОРУ, ЦЕНА ×21…×106 (4.0 С → 0.19 С; 27.7 С → 0.26 С)

**Сделано.** `pgather.c`: bbox-CSR (counting-sort «кусок → все клетки его
bbox», память по nmarks) + 3D-DDA Аманатидеса—Ву со срезкой в bbox сцены
и ранним выходом «вход в следующую клетку дальше ближайшего попадания».
Публичный поиск листа `hz_pyr_leaf_pos` вынесен в pyr (тот же двоичный
поиск марша). Ключ `gather=dda|brute`, brute оставлен путём верификации.

**Приёмка G1 — побитовое тождество — ВЫПОЛНЕНА НА ВСЕХ ТРЁХ СЦЕНАХ:**
кадры dda и brute равны ДО ПОСЛЕДНЕГО БАЙТА (cmp) на cavity05 (внутри
сферы), room_thin (внутри) и k27.obj — ближайший t не зависит от порядка
проверки, как и записано в плане. После clang-format/правок — перепроверено.

**Цена (G2/G3):**

| сцена | brute | dda | кусков/луч | клеток/луч |
|---|---|---|---|---|
| cavity05 (2208 кусков) | 4.0 с | **0.19 с** | 10.2 (0.46 % nt) | 50.6 |
| room_thin (2508) | 8.1 с | **0.08 с** | 5.8 (0.12 %) | 24.4 |
| k27.obj (7936) | 13.4 с | **0.02 с** | 3.1 (0.04 %) | 3.5 |
| spheres.obj (17 664) | 27.7 с | **0.26 с** | 140.9 (0.80 %) | 22.4 |

Полоса приборов: среднее проверенных кусков на луч ≤ 1 % nt на всех
сценах — предсказание G3 (≤5 %) выполнено с запасом.

**НК:** le=0 в dda-пути → кадр тождественно чёрный; лучи мимо сцены —
срезка отсекает до первого шага. ccheck CLEAN (после inline-инициализации
span-массивов под gcc-analyzer — тот же FP-класс diam, что и раньше).

**СТАТУС ЛИНИИ (итог дня §845–§849).** Перенос на новой структуре
собран целиком и проверен: объёмный фронт нулевого + первого порядка на
пирамиде (сходимость к непрерывной физике 0.999, стабильность по lev
0.3 %), камера-оператор с маршевым сбором (побитовая верификация), К27,
ND-лестница на кадре (nd=72 — рабочая точка). Открыты: интерполяция
яркости по куску (А1536), эталон открытой сцены (§846-G2), coarse-прыжок
в DDA (верхние уровни пирамиды), агрегатные узлы (§833) для марша свипа.
Сцены — синтетика (приписка §842).

## §850 (11-09): ПЛАН — ЭТАЛОН ОТКРЫТОЙ СЦЕНЫ: РАДИОЗИТИ С ТОЧНОЙ ВИДИМОСТЬЮ (ДО КОДА)

**УТОЧНЕНИЕ ПОСТАНОВКИ.** room_thin — замкнутая комната (оболочка +
полость, генератор gen_room.sh; перегородка — НК сцены), НЕ открытая.
Значит непрерывный эталон 2πLe/(1−ρ) к ней применим, и зазор 0.95×
(nd=288) — подозрение на схему, а не «законная потеря». Нужен эталон,
не наследующий ни квадрата свипа, ни фронта.

**ПОСТАНОВКА.** Эталон = итерационное решение ПЕРЕНОСА на том же
кусочном носителе, но независимо от машинерии свипа: направления
случайные (равномерно по обеим полусферам, K на кусок — А1548: не
ординаты, ray effect не наследуется), видимость ТОЧНАЯ — ray-triangle
через DDA §849 (побитово верифицирован против перебора). Оценка:

    E_p = (2π/K)·Σ_k |cos_k|·L_hit(k),   L_hit = L_out куска попадания

итерации по рассеянию (Жакоби), L_out = Le + ρ·E/2π. Промах мимо сцены —
тёмное окружение (в замкнутой сцене промахов нет — прибор замкнутости).

**ДЕЛАТЬ.** `tools/pref.c`: скелет pgather (тот же DDA/bbox-CSR),
итерации, печать E_avg эталона, отношения свип/эталон, гистограммы
|ΔE|/E по кускам (средняя/макс/квантили), фактор ряда.

**АУДИТ ДО КОДА.**
- **А1547 [РАССУЖДЕНИЕ]:** MC-шум оценки E при K=1024 ≈ 3 % НА КУСОК;
  на среднем по сцене ~3 %/√nt ≈ 0.1 % — валидация по E_avg честна,
  попiece-гистограммы читать с полосой.
- **А1548 [РАССУЖДЕНИЕ]:** независимость от квадрата полная: случайные
  направления не образуют линий/коридоров — эталон не повторяет
  А1512-класс ошибок ни в каком виде.
- **А1549 [РАССУЖДЕНИЕ]:** эталон мерит перенос при ТОЙ ЖЕ flat-модели
  куска (L постоянна по куску) — расхождение свип/эталон = ошибка
  СХЕМЫ ПЕРЕНОСА (фронт/квадратура/порядок), не дискретизации яркости.
- **А1550 [РАССУЖДЕНИЕ]:** цена nt·K лучей × DDA: cavity05 2208×1024 ≈
  2.3 М лучей/итерацию — по §849 ~секунды; обязана печататься.

**ПРЕДСКАЗАНИЯ.**
- G1 (валидация эталона): cavity05 — E_avg эталона в [0.98, 1.02] ×
  12.566 (непрерывная физика; замкнутая полость, MC-полоса узкая).
- G2 (главная): room_thin — свип/эталон: если ≥ 0.95, зазор §846 —
  угловой (nd), если < 0.90 — схема теряет свет структурно; гистограмма
  локализует где.
- G3: НК le=0 → эталон 0; фактор итераций ≤ ρ.
- G4: цена ≤ 60 с на cavity05 (K=1024, ≤ 15 итераций).

**ПРИЁМКА.** G1 — валидация эталона независимой физикой; G2 — ответ на
вопрос §846.

### §850: ДОКЛАД — ЭТАЛОН СОШЁЛСЯ С АНАЛИТИКОЙ (0.9995) И ОПРОВЕРГ А1526: ФИЗИЧЕСКАЯ ПРАВДА ДЛЯ cavity05 — 4.19 (§842-МОДЕЛЬ БЫЛА ПРАВА); СВИП ПЕРЕСВЕЧИВАЕТ ×3: ФРОНТ НЕ РАЗЛИЧАЕТ СТОРОНЫ ПОВЕРХНОСТИ

**Сделано.** `tools/pref.c` (+ Makefile): эталонный перенос — итерации
Жакоби, направления случайные (обе полусферы, K=1024 на кусок),
видимость точная (DDA §849). Приборы: промахи по полусферам, сверп
свип/эталон, медиана отношения куска.

**Валидация эталона (G1 — в виде, которого план не предвидел).**
План предсказывал сходимость эталона к 12.566 «замкнутой полости» — и
ЭТО БЫЛА ОШИБКА ПЛАНА, вскрывшая главное. cavity05 — ТОНКАЯ сфера
(одна оболочка r=1.6, нормали внутрь, генератор gen_cavity.sh): внешняя
сторона светит в пустоту и теряется навсегда. Непрерывная физика такой
фигуры (двусторонняя эмиссия А1471, чёрное окружение):

    E = πLe/(1 − ρ/2) = 4.1888   (внутренняя полость замкнута,
                                  внешняя сторона — чистая потеря)

- ρ=0 (НК-валидация): эталон **3.1421 против π = 3.14159** (0.016 %).
- ρ=0.5: эталон **4.1866 против 4.1888 (0.9995)**.
Эталон валидирован независимой физикой; MC-полоса и chord-gap промахи
скользящих лучей (вес ∝ mu²) — в пределах прибора.

**А1551 [ЗАМЕРЕНО, ГЛАВНОЕ]:** свип §845/§846 даёт 12.558 = **3.00 ×
эталона** (медиана отношения куска 3.004). Число 3 = 2(1−ρ/2)/(1−ρ) —
это в точности отношение «полностью рециркулирующей двусторонней
системы» 2πLe/(1−ρ) к «тонкой оболочке в пустоте». **А1526 опровергнут:
«модель слоя» §842 (4.19) была физически верной**, а согласие свипа с
12.566 — совпадение с НЕВЕРНОЙ моделью: фронт носит L без информации о
СТОРОНЕ поверхности и о том, в какой компоненте пустоты находится
трубка. Свет внешней стороны оболочки в схеме рециркулирует внутрь, как
будто пустота снаружи светится; в физике внешняя эмиссия уходит и не
возвращается.

**Механизм (записан, чтобы не лечить наугад).** Перенос живёт в ПУСТОТЕ,
и пустота сцены — не одна область: внутренняя полость и внешний мир —
РАЗНЫЕ компоненты связности, свет между ними не переходит (через
непрозрачную оболочку). Фронт несёт один L на направление по всем
кускам подряд — как если все компоненты пустоты были одним светящимся
объёмом. Для ТЕЛ С ТОЛЩИНОЙ (room: замкнутые коробки) ошибка того же
класса: внешняя сторона коробки светит в void, схема возвращает этот
свет внутрь. Зазор room 0.89–0.96 в §845-бис мерился против 12.566 —
эталон для room тоже неверен, истинный ниже.

**Лечение (пропозация §851, до кода):** разметить компоненты пустоты
заливкой по базовой сетке (клетки без кусков; BFS, дёшево); фронт несёт
L ПО (направление, компонента): при переходе трубки в новую компоненту —
L обнуляется (из другой компоненты в эту свет не приходит); депозит на
кусок — со СТОРОНЫ (знак ω·n определяет, какая грань куска получила
свет); переизлучение куска делится между его компонентами по площади
граней. Это возвращает уравнению переноса его геометрию: свет живёт в
своей компоненте пустоты.

**НК:** le=0 → эталон 0 ✓. Фактор ряда ≤ ρ ✓. ccheck CLEAN.
**Цена (G4):** 47 c на cavity05 (K=1024, 15 итераций) — прибор, не
продукт; сверху бюджетом управляют K и it.

**СТАТУС.** Впервые в линии есть НЕЗАВИСИМЫЙ эталон переноса — и он
сразу нашёл структурную ошибку носителя, невидимую во всех прежних
приборах (баланс, замкнутая полость, НК — всё было слепо к сторонам
поверхности). Сверка свип/эталон становится обязательной приёмкой всех
следующих шагов.

## §851 (11-09): ПЛАН — КОМПОНЕНТЫ ПУСТОТЫ И СТОРОНА-ОСВЕДОМЛЁННЫЕ ДЕПОЗИТЫ (ДО КОДА)

**ПОСТАНОВКА (из А1551).** Фронт несёт один L на направление по всем
кускам подряд — как если вся пустота сцены была одним светящимся
объёмом. Физически свет живёт В КОМПОНЕНТАХ ПУСТОТЫ: внутренняя полость
и внешний мир не обмениваются светом через непрозрачную оболочку.
Исправление схемы mode 2:

1. **Разметка компонент:** BFS по базовой сетке, 6-связность; клетка с
   кусками = «материал» (непрозрачна, не пустота), остальные — пустота;
   метки компонент = метки связной пустоты. Массив меток [ncells] int32.
2. **L по (направление, компонента):** на направлении массив Lcomp[ncomp]
   (сбрасывается в 0 на старте направления — чёрное окружение). В визите
   кускового листа: c_in = метка СОСЕДНЕЙ клетки против хода (доминирую-
   щая ось направления): если c_in — компонента и не равна текущей —
   вход в новую компоненту: L_in = Lcomp[c_in] (обычно 0 — тёмный вход
   ФИЗИЧЕСКИЙ, свет той компоненты не доходит); если та же — L_in = Lcur.
3. **Депозит и переизлучение** — как в §846 (T-перехват), но L_out
   записывается в Lcomp[c_out], где c_out — метка соседней клетки по
   ходу; свет противоположного направления (−d в квадратуре) разнесёт
   переизлучение в компоненту c_in — полнота квадрата сохраняется.
4. Ключ `vc=0` — выключить (старая однокомпонентная логика) для сличения.

**АУДИТ ДО КОДА.**
- **А1552 [РАССУЖДЕНИЕ]:** «клетка с куском = материал» — консервативно
  по клетке: кусок тоньше клетки делает соседние пустоты несоединёнными
  на сетке даже там, где геометрически соединены (проём шириной меньше
  клетки). Ошибка O(cell) по границе, падает с lev; приёмка G2 ловит.
- **А1553 [РАССУЖДЕНИЕ]:** сосед «против хода» по доминирующей оси —
  приближение для диагональных направлений; ошибка тот же O(cell) —
  фальсификатор: сверка с эталоном по лестнице lev (сходимость с
  измельчением обязана быть видна).
- **А1554 [РАССУЖДЕНИЕ]:** изолированные «материальные» полости (внутренность
  толстых стен) становятся своими компонентами с L=0 — свет туда не входит
  (T=0 на оболочке) и не выходит: физика соблюдена автоматически.
- **А1555 [РАССУЖДЕНИЕ]:** цена: BFS однократный (O(ncells)), на
  направлении — два просмотра меток на лист: полоса не хуже 4 ГБ/с;
  память меток [ncells]·4 Б — cavity/lev7 ≈ 8 МБ, приемлемо.

**ПРЕДСКАЗАНИЯ.**
- G1 (главная): cavity05: свип/эталон (4.1866) — из 3.00 в [0.9, 1.2];
  если не упало — механизм А1551 не объяснён, стоп.
- G2: rho=0: свип даёт ≈ πLe = 3.14 (было 2πLe = 6.28 — двойной счёт
  сторон снят), в ±10 %.
- G3: room: свип/эталон (pref на room) ≥ 0.9; лестница lev=4…7 без
  коллапса (размах ≤ 1.5×).
- G4 (НК): le=0 → E≡0; noprop → E≡0.
- G5: полоса ≥ 4 ГБ/с; цена ≤ 1.5× mode 2.
**ПРИЁМКА.** G1 — главная; G2 — прямой фальсификатор двойного счёта.

### §851: ДОКЛАД (ПРОМЕЖУТОЧНЫЙ, НЕГАТИВНЫЙ ЯДРОМ) — ЛУЧЕВОЙ СВИП СНИМАЕТ ×3 НА ТОЙ КЛЕТКЕ, ГДЕ СТЕНА ≈ КЛЕТКА, НО ВСКРЫВАЕТ ГЛАВНЫЙ КОНФЛИКТ НОСИТЕЛЯ: НЕПРОЗРАЧНОСТЬ МЕЛКЕ КЛЕТКИ НЕ ВЫРАЖАЕТСЯ НИКАКИМ КОЭФФИЦИЕНТОМ

**Сделано.** `vc=1` (swee3/pgather/pref): лучевой свип — трубки = линии
сетки вдоль направления, стартуют тёмными на входных гранях, DDA-шаг,
в клетке с кусками депозит w·L·|cos| и перехват T=1−cntot/csec
(§846). Латеральная изоляция трубок и «стороны» поверхности возникают
сами — разметка компонент из плана оказалась НЕ НУЖНА: линия стартует
тёмной и умирает на выходе, компоненты выражаются топологией линии
(упрощение против плана, в лучшую сторону).

**Замер (cavity05, nd=26, ρ=0.5, Le=1):**

| lev | vc=0 (§846) | vc=1 (лучевой) | эталон §850 |
|---|---|---|---|
| 4 | 12.53 | 15.04 | 4.1866 |
| 5 | 12.53 | 13.04 | 4.1866 |
| 6 | 12.56 | **3.79 (0.905×эталона)** | 4.1866 |
| 7 | 12.56 | 0.958 (0.229×) | 4.1866 |

G1 на lev=6 ПОПАДАНИЕ (0.905 в [0.9,1.2]); на lev=4,5,7 — промах в ОБЕ
стороны, и это главный результат шага:

**А1556 [ЗАМЕРЕНО, ГЛАВНОЕ]:** T = max(0,1−cntot/csec) — вероятностная
тень «кусков клетки в сечении трубки» — физически корректна ТОЛЬКО при
кусок ≈ клетка. Вне этого режима носитель не выражает непрозрачность:
- **lev ≫ куска (4,5):** cntot ≥ csec, стена толще клетки — линия
  ПЕРЕСЕКАЕТ оболочку много раз по хорде, каждый раз депонировал полный
  Lsurf → свет размножается (15 > даже неверной двусторонней 12.56);
  ограничитель депозита долей трубки (cap=csec/cntot) дал 14.2/9.0 —
  не лечит, т.к. cntot считается по ПОЛНОЙ площади кусков клетки без
  клипа по клетке, а не по тени в трубке.
- **lev ≪ куска (7):** тень куска в сечении клетки ~1 %: трубка
  «протекает» сквозь непрозрачную стену (T≈1), депозиты ≈ 0 (0.23×).
- cap меняет и lev6 (3.79 → 1.27) — снят; lev6 согласован без него.

**А1557 [ВЫВОД, ПРИНЦИПИАЛЬНЫЙ]:** непрозрачность поверхности, тоньше
или толще клетки, НЕ выражается никакой клеточной нормировкой —
нужно ТОЧНОЕ пересечение линии с треугольником (ray-triangle в свипе)
либо разбиение поверхности до клетки. То, что §843-бис называла
«пунктом 6/рейтрейсом-роскошью», а §844-директива отвергла, — это
не ускорение, а МИНИМУМ корректности для тонких оболочек. Для объёмных
сред (σ_t в клетках, физика ДГ) клеточный перехват честен — конфликт
именно с поверхностными носителями произвольной плотности.

**СТАТУС.** vc=1 оставлен в коде (лучевой свип — правильная СХЕМА:
lev6 0.905×эталона, НК чисты, ccheck CLEAN), но разметка «клетка =
материал» делает его корректным только при стенка ≈ клетка. Дороги
дальше (выбор за пользователем): (А) ray-triangle пересечение в лучевом
свипе (точная видимость в самом переносе — то самое «пункт 6», которое
раньше отвергалось как избыточное; эталон pref уже показывает, что это
даёт 4.19); (Б) объёмный DG (σ_t в клетках, поверхность — граничное
условие на сетке по клетку) — исходная линия sweep3, дорогая памятью,
но сходящаяся по построению; (В) ограничить носитель сценами со
стенкой ≈ клетка и documented-диапазоном.

## §852 (11-09): ПЛАН — МАТЕРИАЛЬНЫЙ ФРОНТ НА ПИРАМИДЕ: ДРОБЛЕНИЕ ПО LOD КУСКА (ДО КОДА; АУДИТ ВЫПОЛНЕН, ПОСТАНОВКА ПРАВЛЕНА ПО А1563…А1572)

**Директива пользователя (08-09):** носитель — МАТЕРИАЛЬНЫЙ фронт (не
лучи и не радиосити: данные везде локальны, никаких глобальных пар);
непрозрачность выражается тем, что фронт на препятствии ДРОБИТСЯ до
уровня, заданного для куска его Level Of Detail. «Просто научиться
правильно вести материальный фронт через препятствия с иерархией».

**РАЗБОР ОШИБКИ §845–§851 (А1556/А1557).** Клеточный перехват
T = 1−cntot/csec корректен только при кусок ≈ клетка; глобальный lev
задавал это отношение неудачно для всех поверхностей сразу (лестница
15.0 / 13.0 / 3.79 / 0.958 при неизменной физике; эталон 4.1866).
Иерархия снимает конфликт: рабочий масштаб каждой поверхности живёт В
ПИРАМИДЕ — фронт идёт крупно через пустоту и дробится на препятствиях
до LOD-уровня куска, где перехват корректен по построению.

**ПОСТАНОВКА (правлена по аудиту А1563…А1572 — см. доклад аудита ниже).**
1. Каждому куску — LOD-уровень ℓ_p (правило §844-директивы: диффузная
   доля допускает огрубление, узкая требует детализации; конкретную
   политику записать отдельно). Узел пирамиды уровня ℓ МАТЕРИАЛЕН тогда
   и только тогда, когда max ℓ_p кусков его поддерева ≤ ℓ; per-node
   max ℓ_p хранится в узле (байт на узел, один подъём при построении;
   А1567). Клэмп ℓ_p: ℓ_p < 0 → 0 (лист); ℓ_p выше доступного уровня
   узла — ОТКАЗ с печатаемым счётчиком таких кусков и их обработкой на
   листе: агрегатов нет (HZ_PYR_AGGR §833 не ставится), молчаливый
   клэмп вверх = скрытая агрегация, подкласс Г25 (А1568).
2. Марш фронта направления d: узел ПУСТ — прыжок одним шагом по tmax
   узла (L не меняется, точное решение на вакууме); узел МАТЕРИАЛЬНЫЙ —
   локальное взаимодействие §846 (депозит w·L·|cos| кускам, перехват T)
   — корректное, т.к. на этом уровне кусок ≈ клетка; узел
   ПРОМЕЖУТОЧНЫЙ (есть геометрия глубже) — СПУСК: продолжение DDA на
   детском уровне. L — РАДИАНС: сечение трубки при спуске/подъёме НЕ
   ДЕЛИТСЯ, Φ = w·L·csec инвариантен с сечением БАЗОВОЙ клетки; доля
   ребёнка = 1, если трубка геометрически входит в него (пересечение
   отрезка [t_in, t_out] узла с ребёнком), 0 иначе; слияния нет — подъём
   есть продолжение той же трубки (А1563). Доля пути за границей домена
   (нечётные nx/ny/nz, pyr_visit пропускает детей за cd) попадает в
   `lost` (А1567).
3. Владение и кратность: перехват клеточной моделью T = 1−cntot/csec
   допустим ТОЛЬКО под предикатом «bbox куска ⊆ рабочая клетка»; вне
   предиката — точное пересечение (ray-triangle) либо клип площади по
   клетке, fail closed (А1566: иначе утечка «пустых клеток стенки» и
   пересвет полной площадью — оба дефекта §851 внутри рабочего уровня).
   Кратность взаимодействия — ровно 1 на (кусок, направление), держится
   ШТАМПОМ (кусок × направление); штамп же чинит живой дефект vc=1 —
   двусторонний Möller–Trumbore депонирует радианс тонкой оболочки на
   неё же при выходе трубки (А1564).
4. Данные локальны: узлу нужны его куски (CSR), per-node max ℓ_p и
   дети. Никаких лучей как структуры, никаких глобальных пар.

**ДЕЛАТЬ (эскиз).** hz_pyr: слот LOD-уровня куска (или вычислимый),
per-node max ℓ_p; обход фронта по узлам трёх состояний (ПУСТ-прыжок по
tmax/материальное взаимодействие/спуск-продолжение DDA); депозиты по
кускам рабочего уровня под предикатом А1566, штамп кратности А1564;
DDA по уровням для прыжков пустоты — иначе «поверхностная
ограниченность» не достигается (А1569: vc=1 объёмно-ограничен,
nd × O(ncells) + log-поиск на клетку; прибор — доля пустых клеток в
обходе, предсказание > 90 %). Приборы: скалярные аккумуляторы G6
(parent_flux против Σchild_flux на каждом спуске, тождество ≤ n·eps,
счётчик нарушений — 0 на чистом прогоне; «поток на гранях» в hz_pyr не
хранится — А1565), депозиты/визиты, счётчики клэмпа и отказа А1568.

**АУДИТ ДО КОДА — ВЫПОЛНЕН (доклад ниже; А1558–А1562 раскрыты,
находки А1563…А1572).**

**ПРЕДСКАЗАНИЯ (правлены по А1570/А1571).**
- G1 (главная), редакция (a): при точном пересечении — ПЛОСКАЯ лестница
  E(ℓ_p) на cavity05: ×эталона (4.1866) в [0.9, 1.1] при любом ℓ_p.
  Редакция (b): при клеточном перехвате — попадание в [0.9, 1.1] только
  в окне валидности «bbox ⊆ клетка» (А1566); окно охарактеризовать и
  печатать. «Любая конфигурация» без точного пересечения НЕ
  предсказывается — это была ошибка первой редакции.
- G1-регресс (тождество режима, не физика): ℓ_p ≡ 0 воспроизводит vc=1
  §851 (3.79 на lev=6-конфигурации) — схема не изменилась; урок А1551:
  не смешивать с попаданием в эталон.
- G2: rho=0 → ≈ πLe = 3.14 (±10 %) — двойной счёт сторон снят.
- G3: room против pref-эталона ≥ 0.9.
- G4 (НК): le=0 → E≡0; noprop → E≡0; tau0 → E≡0 и нулевые депозиты.
- G5: цена: полоса ≥ 4 ГБ/с; ограниченность — поверхностная, прибором
  служит доля пустых клеток в обходе до/после DDA-прыжков (А1569).
- G6 (инвариант А1563/А1565): parent_flux = Σchild_flux на КАЖДОМ
  спуске — скалярные аккумуляторы, счётчик нарушений 0 на чистом
  прогоне; держится и на нечётной сетке, и на T-стыке уровней.

**НЕГАТИВНЫЙ КОНТРОЛЬ.** le=0, noprop; screw — КАЛИБРОВКА детекторов:
при нём hz_pyr_verify обязан СРАБОТАТЬ (d_cell/d_csr/d_empty > 0); нули
обязательны на ЧИСТОМ прогоне (все четыре детектора; исправлено по
А1572 — прежняя формулировка «при screw — нули» была ошибкой).
Сцены — только синтетика (§842-приписка): cavity05, room, шары +
blocked-beam А1566.
**ПРИЁМКА.** G1(a) + G6 — главные (энергия и бесщелевость); G2 —
фальсификатор двойного счёта сторон. Эталон сравнения — pref §850
(4.1866).

### §852: АУДИТ ПЛАНА — А1563…А1572 (ДО КОДА; А1558–А1562 РАСКРЫТЫ ПО ЖИВОМУ КОДУ)

Прочитаны pyr.h/pyr.c, sweep.h/sweep.c, swee3.c, pref.c. Заготовки
А1558–А1562 раскрыты; находки перенумерованы с А1563.

**А1558 (бесщелевость) → А1563, А1565.**
- **А1563 [ДИЗАЙН, ГЛАВНОЕ]:** в п.2 постановки «трубка делит сечение на
  доли детей, поток w·L·csec распределяется по сечениям» — источник
  двойного счёта и потерь сразу. L — РАДИАНС: сечение трубки при
  спуске/подъёме НЕ ДЕЛИТСЯ; инвариант спуска — Φ = w·L·csec с сечением
  БАЗОВОЙ клетки, постоянным на всей глубине. Спуск — не деление потока,
  а продолжение DDA на более мелком уровне: доля ребёнка = 1, если
  трубка геометрически входит в него (пересечение отрезка [t_in, t_out]
  узла с ребёнком), 0 иначе; слияние на подъёме — продолжение той же
  трубки, складывать нечего. «Доли детей» законны только как ДОЛИ ДЛИНЫ
  пути внутри детей, не доли сечения. Формулировку плана править до кода.
- **А1565 [ПРИБОР]:** «поток на гранях уровня» негде хранить — в hz_pyr
  потоков нет. G6 определить как СКАЛЯРНЫЕ аккумуляторы марша: на каждом
  спуске parent_flux против Σchild_flux, тождество |Δ| ≤ n·eps за
  прогон; нарушение — счётчик, обязан дать 0 на чистом прогоне.
  Тождество обязано держаться и на не-дыадической границе (А1567), и на
  T-стыке уровней (А1566).

**А1559 (кусок на границе LOD-перехода) → А1566, А1567.**
- **А1566 [ДЕФЕКТ СХЕМЫ, ГЛАВНОЕ]:** владение — по ЦЕНТРОИДУ (pyr.c §4):
  кусок сидит в ОДНОМ листе, его bbox накрывает несколько рабочих клеток
  уровня ℓ_p. Отсюда ОБА дефекта §851 воспроизводятся ВНУТРИ рабочего
  уровня: клетки стенки без кусков дают T=1 (утечка lev=7), а клетка
  владения несёт cntot ПОЛНОЙ площади без клипа (пересвет lev=4/5,
  неудавшийся cap §851). Клеточный перехват T = 1−cntot/csec допустим
  ТОЛЬКО под предикатом «bbox куска ⊆ рабочая клетка», иначе fail
  closed — точное пересечение либо клип площади по клетке.
  Фальсификатор: blocked-beam синтетика — стена между источником и
  приёмной площадкой, ℓ_p варьируется 0…nlev: E приёмника обязана
  остаться в полосе прибора при ЛЮБОЙ ℓ_p; centroid-вариант без клипа
  на ней предсказуемо проваливается.
- **А1567 [ДИЗАЙН]:** «материальный узел» — состояние ПРОИЗВОДНОЕ: узел
  уровня ℓ материален ⟺ max ℓ_p кусков поддерева ≤ ℓ. Хранить per-node
  max ℓ_p (байт на узел, один подъём при построении); иначе на каждый
  марш — обход поддерева. Край: сетки с НЕЧЁНЫМИ nx/ny/nz
  (pyr_level_dims с ceil; pyr_visit пропускает детей за границей
  qx ≥ cd) — доля сечения, ушедшая за домен, обязана попасть в `lost`,
  не исчезнуть; фальсификатор — G6-тождество на сцене с нечётной сеткой.
- **А1564 [ДЕФЕКТ ЖИВОГО КОДА, та же машинерия]:** кратность «1 на
  (кусок, направление)» ничем не обеспечена, и в vc=1 она УЖЕ нарушена:
  Möller–Trumbore в sw_ray_tri двусторонний (sweep.c:431, отсечения
  задней стороны нет), тонкая оболочка пересекается трубкой дважды
  (вход и выход), и на выходе депонирует СВОЙ же радианс на себя —
  самозасветка тонкой оболочки. Лечение одно на всё: штамп
  (кусок × направление) — он же держит кратность §852 и чинит vc=1.
  Фальсификатор: тонкая оболочка, депозиты куска ≤ 1 на направление;
  ΔE к pref до/после — напечатать.

**А1560 (clamp ℓ_p) → А1568.**
- **А1568 [ДИЗАЙН, fail closed]:** ℓ_p < 0 → 0 (лист). ℓ_p вверх:
  клэмпить можно только до самого мелкого ДОСТУПНОГО уровня узла.
  HZ_PYR_AGGR агрегатов НЕ СУЩЕСТВУЕТ (§833: не ставится; слот — только
  НК-инструмент hz_pyr_mark_aggr), агрегатного отклика нет — «узкая
  доля» §844 носителя в коде не имеет. Клэмп ℓ_p выше доступного —
  скрытая агрегация, подкласс Г25 (подмена нулём неизвестной ошибки).
  Правило: отказ с явным счётчиком «кусков, для которых ℓ_p недостижим»
  и обработкой их на листе; тишина запрещена; счётчик печатается.

**А1561 (цена) → А1569.**
- **А1569 [РАССУЖДЕНИЕ, G5 недостижим текущим путём]:** vc=1 перебирает
  ВСЕ линии сетки (3 полосы по ~n² линий × ~3n клеток) — nd × O(ncells),
  ограниченность ОБЪЁМНАЯ, не поверхностная; поверх неё двоичный поиск
  hz_pyr_leaf_pos на каждую клетку (log nleaf). «Прыжок по ПУСТУ» плана
  требует иерархического пропуска пустоты, которого в живом коде нет:
  марш §834 — ПОРЯДОК, не геометрия (инверсии §845-бис это показали).
  Нужен DDA по уровням: пустой интервал tmax до следующего занятого
  узла уровня ℓ проходится одним шагом. Прибор цены: доля пустых клеток
  в обходе (ncell против визитов) на cavity05 — замерить до; предсказание
  > 90 %, иначе skip не окуплен. Второе: кэш соседа линии вместо
  двоичного поиска (следующая клетка линии почти всегда сосед листа).

**А1562 (варьирование LOD вокруг оптимума) → А1570.**
- **А1570 [ПРЕДСКАЗАНИЕ, правка G1]:** «G1 в [0.9,1.1] при ЛЮБОЙ
  конфигурации LOD» — ОШИБКА ПЛАНА в этой формулировке: при клеточном
  перехвате варьирование ℓ_p асимметрично (глубже оптимума — точно и
  дороже; грубее — coverage-ошибка O(доли клетки без клипа) по А1566),
  и «любая конфигурация» недостижима без точного пересечения. G1
  переписать дважды: (a) с точным пересечением — ПЛОСКАЯ лестница E(ℓ_p)
  в [0.9,1.1]; (b) с клеточным перехватом — окно валидности
  «bbox ⊆ клетка» (А1566), охарактеризовать и печатать. Отдельно:
  ℓ_p ≡ 0 обязан ВОСПРОИЗВЕСТИ vc=1 §851 (3.79 на lev=6-конфигурации) —
  это РЕГРЕССИОННОЕ тождество режима, а не попадание в эталон; не
  путать с G1 (урок А1551: согласие с неверной моделью не есть физика).

**Сверка с эталоном → А1571.**
- **А1571 [ПРЕДСКАЗАНИЕ]:** G2/G3 преемственны §851: rho=0 → πLe = 3.14
  (±10 %; pref дал 3.1421), room ≥ 0.9 × pref. Эталон pref не трогается:
  LOD у него нет, он и есть мера того, что дробление не портит физику.
  Отчёт обязан дать ОБЕ строки: (тождество режима ℓ_p=0 ≡ vc=1) — схема
  не изменилась; (G1 против 4.1866) — схема стала правильнее.

**Негативный контроль → А1572.**
- **А1572 [ПРИБОР, словесная ошибка плана]:** «детекторы пирамиды при
  screw — нули» противоречит семантике hz_pyr_verify: screw — КАЛИБРОВКА
  детекторов, при нём они обязаны СРАБОТАТЬ (d_cell > 0 и/или d_csr > 0,
  d_empty > 0 при вылете за leaf_id); нули обязательны на ЧИСТОМ прогоне
  (§833). Записано как есть: чистый прогон — все четыре нуля; screw
  stride — ненули. Сверх того НК фронта: le=0 → E≡0, noprop → E≡0,
  tau0 → E≡0 и нулевые депозиты; G6-детектор — ноль на чистом прогоне.
  Сцены — только синтетика (§842-приписка): cavity05, room, шары +
  blocked-beam А1566.

**СТАТУС АУДИТА.** До кода обязательны: правка п.2 постановки по А1563
(сечение инвариантно, спуск = продолжение DDA); правило ownership/клипа
по А1566; штамп кратности А1564 (чинит и живой vc=1); per-node max ℓ_p
по А1567; clamp-отказ А1568; G1 в двух редакциях А1570. Без них план
воспроизводит дефекты §851 внутри пирамиды в скрытой форме.

### §852: ДОКЛАД (ПРОМЕЖУТОЧНЫЙ, ГЛАВНЫЙ ГЕЙТ ПРОЙДЕН) — МАТЕРИАЛЬНЫЙ ФРОНТ НА ПИРАМИДЕ: ДРОБЛЕНИЕ ПО LOD КУСКА

**Сделано.** `mode=3` (swee3, ключи `lp=/xint=/screw=`): иерархический марш
материального фронтa по пирамиде. pyr: `hz_pyr_set_lp` — per-node/leaf max ℓ_p
одним подъёмом (А1567), байт на узел; клэмп ℓ_p выше доступного уровня —
обработка на листе с печатаемым счётчиком (А1568). Свип: узел ПУСТ — прыжок
по tmax узла (А1569), МАТЕРИАЛЕН (max ℓ_p ≥ уровень узла; лист = 0) —
локальное взаимодействие, ПРОМЕЖУТОЧНЫЙ — спуск-продолжение DDA (А1563:
Φ = w·L·csec·|om_ax| — сечение базовой клетки инвариантно, доля ребёнка 0/1
по геометрии входа, слияния нет). Взаимодействие: точное пересечение
(raw Möller–Trumbore, фильтр отрезком) для кроссирующих кусков; клеточный
перехват T = 1−cntot/csec_node ТОЛЬКО под предикатом «bbox ⊆ клетка»
(А1566, fail closed), нормировка депозитов даёт тождество Σ = f·вход.
Штамп (трубка × кусок × направление) — кратность 1, гасит самозасветку
тонкой оболочки (А1564; ставится ТОЛЬКО при реальном депозите — холостое
касание кусок не помечает). G6 — по родительской доле носителя (a/b-разложение:
эмиссия — новая энергия), детектор на каждом спуске и на корне. Приборы:
ПУСТ-прыжки, доля пустых клеток до/после (А1569), штамп-пресечения, lost.
Куски для взаимодействия — по bbox-индексу листьев (владение по центроиду
даёт «пустые» клетки на пути трубки), плюс: границы меша на плоскостях
сетки (стенка коробки) требуют индексации «ceil−1» и вырожденных сегментов —
иначе весь хвост трубки после первой клетки выпадает (замерено: box
0.99 → 4.14 после починки).

**Замер (cavity05, nd=26, ρ=0.5, Le=1, lev=6; эталон §850 = 4.1866):**

| lp | 0 | 1 | 2 | 3 | 4 |
|---|---|---|---|---|---|
| E_avg (8 итераций) | **4.2085 (1.005×эталона)** | 4.2084 | 6.14 ↑ | 1047 ↑ | 16351 ↑ |

- **G1(a): ПОПАДАНИЕ** — плоская лестница на ℓ_p = 0,1: 4.2085 в [0.9,1.1]×4.1866.
- **G2: ПОПАДАНИЕ** — ρ=0: 3.1406 = πLe (квадратура 26) ровно, ±10% ✓.
- **G6: 0 нарушений** на всех прогонах (чистый прогон), тождество держится.
- **G4: ПОПАДАНИЕ** — le=0 → E≡0; noprop → E≡0; tau0 → E≡0.
- **НК А1572: ПОПАДАНИЕ** — чистый прогон: d_cell=d_csr=d_bbox=d_empty=0
  (после починки verify под Morton: пересчёт по pcs[t].tri); при screw=2:
  d_cell=1104, d_csr=1104, d_empty=22 — детекторы сработали.
- **А1569 прибор:** доля пустых клеток 78% (lp=0), 84.5% (lp=1), 95.6–99.9%
  (lp≥2). Предсказание «>90%» на lp=0 НЕ сбывается (марш ходит по клеткам
  стенки-строки); на lp≥2 сбывается с запасом.
- **Гейт C:** clang-format, ccheck CLEAN (gcc -fanalyzer, clang-tidy,
  cppcheck — pyr.c/sweep.c/swee3.c), ASAN+UBSAN+LeakSanitizer: 0 ошибок,
  0 утечек (починены: stampv, trivert/tribox/lp, free(fill) в Morton).

**А1573 [ЗАМЕРЕНО, НЕ РЕШЕНО — главный хвост]:** лестница ℓ_p ≥ 2
РАСХОДИТСЯ (рециркуляция > 1, рост ~×1.5/итерацию). Механизм по замерам:
на материальном узле уровня ≥ 2 T-перехват раздаёт депозит ВСЕГО узлового
сечения содержащимся кускам — включая куски, которые данная трубка
геометрически не пересекает (доля f·wt/swt корректна по потоку, но
суммарно по семействам узел возвращает больше, чем один проход трубки),
плюс ре-эмиссия Lsurf узла средней радиансом. Фикс сечения в T
(csec_node вместо базового) недостаточен (6.14 всё равно ↑). Требуется
разделение «трубка пересекает тень куска» от «трубка проходит узел» —
точное пересечение на узловом уровне, неcell-перехват (А1557-класс).

**А1574 [ЗАМЕРЕНО]:** прибор пустоты на lp=0 — 78%, не 90%: марш обходит
все клетки стенки-строки; поверхностная ограниченность на lp=0 не
достигается, достигается на lp ≥ 2 (95.6–99.9%) — то есть выигрыш А1569
покупается тем же LOD-огрублением, которое даёт А1573.

**НЕ СДЕЛАНО (записано, не забыто):** blocked-beam-сцена (фальсификатор
А1566: утечка сквозь стенку при грубом ℓ_p) — не построена и не прогнана;
G3 (room vs pref) не прогнан; множитель |om_ax| семейства трубок выведен
из сохранения потока на грани входа и подтверждён G2/G1, но требует
отдельной записи в постановке (поправка к п.2: Φ = w·L·csec·|om_ax|).

**СТАТУС.** ГЛАВНЫЙ ГЕЙТ G1(a) ПРОЙДЕН (1.005×эталона), G2/G4/G6/НК
пройдены, гейт C пройден. Схема lp=0/1 — рабочий материальный фронт;
лестница ℓ_p ≥ 2 — негативный результат А1573, код не скрывает (счётчики
и приборы печатаются). Следующий шаг — А1573: точное пересечение на
узловом уровне вместо клеточного перехвата, и blocked-beam.

**А1575 [ДОРАБОТКА ЦЕНЫ, ПОСЛЕ ОТЧЁТА]:** горячие аллокации убраны из
`front_interact`/`front_gather` (скретч-буферы в контексте, стек на кадре C);
E побитово тот же (cavity lp=0: 4.1909 на 4-й итерации, box 2.6566), G6=0.
Время кадра: 10.4 с → **1.9 с** (cavity05, lev=6, nd=26, ~5.5×). Остаток —
двоичные поиски на шаге (кэш соседа, А1569-вторая половина) и рекурсивный
спуск; полоса по-прежнему далека от G5.

### §852-МОСТИК В НОВУЮ СЕССИЮ (прочесть первым)

**Состояние.** §852 исполнен, отчёт выше (ГЛАВНЫЙ ГЕЙТ G1(a) ПРОЙДЕН:
4.2085 = 1.005×эталона 4.1866 на cavity05/lev=6/nd=26/lp=0; G2, G4, G6,
НК А1572, гейт C — пройдены). Код в рабочем дереве, НЕ закоммичен:
`src/nstruct/{pyr,pyr.h,sweep,sweep.h}` (mode 3, hz_pyr_set_lp, verify под
Morton), `tools/swee3.c` (mode=3, lp=, xint=, screw=), `tools/pview.c`
(показ-рендер, картинка img/room_view.png). В дереве также более ранние
незакоммиченные правки §852-prep (trivert-точное пересечение в vc=1).

**Открытые хвосты (по убыванию):**
- **А1573** (главный): лестница ℓ_p ≥ 2 расходится — узловой T-перехват
  раздаёт узловое сечение кускам, мимо которых трубка прошла; нужен
  узловой уровень ТОЧНОГО пересечения вместо клеточного перехвата
  (А1557-класс). Починка одним сечением (csec_node) недостаточна — замерено.
- **А1574**: прибор пустоты на lp=0 — 78%, не >90% (поверхностная
  ограниченность на lp=0 не достигается; на lp≥2 — 95.6–99.9%, но там
  А1573).
- **blocked-beam** (фальсификатор А1566: утечка сквозь стенку при грубом
  ℓ_p) — сцена не построена, прогон не сделан. G3 (room vs pref ≥ 0.9)
  не прогнан.
- **Цена (А1575 + далее):** кадр 1.9 с на cavity05/lev=6/nd=26 ОДНО ЯДРО
  (аллокации уже убраны). Далее: кэш соседа линии вместо бинарных поисков;
  кэш топологии трубки (пересечения не зависят от итерации — инкрементность);
  одно семейство трубок на направление по доминирующей оси; OpenMP по
  направлениям (направления независимы, связь только E_prev); приборы/G6
  под отладочный ключ. G5 (4 ГБ/с) далеко.
- **T4:** зеркальности в переносе нет (только диффуз Le + ρE/2π); pview
  (картинка) — отдельный показ-рендер, к переносу не относится.

**Якоря для сверки любого изменения:** cavity05 lp=0: E(4it)=4.1909,
E(8it)=4.2084, G6=0, НК-нули чистые, при screw=2 — d_cell=1104/d_csr=1104/
d_empty=22; box lp=0 rho=0: 2.6566; ρ=0: 3.1406. Нормировка депозита:
Φ = w·L·csec·|om_ax| / A_куска (ax — ось семейства-входа); штамп — только
при реальном депозите; границы меша на плоскостях сетки требуют
«ceil−1»-индексации bbox и вырожденных сегментов — НЕ «упрощать» обратно.

## §853 (11-09): А1573 ПОЧИНЕН — УЗЛОВОЙ ПЕРЕХВАТ ЗАМЕНЁН ТОЧНЫМ ПЕРЕСЕЧЕНИЕМ; ЛЕСТНИЦА ℓ_p ПЛОСКАЯ НА ВСЕХ УРОВНЯХ

**Минимальный прибор воспроизведения (а).** Отдельного прибора не потребовалось —
расходимость воспроизводится штатным `swee3 mode=3` на cavity05/lev=6/nd=26/
rho=0.5 одним ключом: `lp=2 xint=0` (клеточный перехват включён, точное
пересечение выключено) — E(6it): 3.43 → 5.45 → 6.05 (1.44×модели 4.1906),
рециркуляция 146.0 против 48.5 на lp=0, рост ~×1.5/итерацию в первые такты.
`box.obj` при тех же ключах НЕ расходится (стенки на границах сетки: трубка
всегда задевает кусок узла, перехват там не врёт о покрытии — смещение вниз
4.16→4.04 по lp есть, но это другой, добрый знак перехвата, не А1573).
Место перехвата известно точно: `front_interact`, ветка 2 (клеточный
T-перехват), вызываемая из узловой ветки `front_visit` при
`m >= l+1` — сечение T-перехвата `csecnode` = квадрат стороны УЗЛА.

**Механизм (б).** На материальном узле уровня ≥ 1 предикат А1566 «bbox куска ⊆
клетка» выполнен всегда (клетка = узел, кусок собран из её поддерева), но он
гарантирует только то, что кусок ЖИВЁТ в узле, — не то, что ТРУБКА задевает его
тень. T = 1 − ΣA·cos/csecnode: стенка занимает ДОЛЮ сечения узла, T < 1, и
каждая трубка, проходящая через ПУСТУЮ часть узла, получает депозит f·Φ и
ре-эмиссию Lsurf от кусков, которых геометрически не касалась. На lp≤1 узел
мал (стенка ≈ сечение узла) — ошибка в шум; на lp≥2 доля пустого сечения
растёт квадратично со стороной узла, фантомная ре-эмиссия накапливается
трубкой за проход и возвращается на следующий итерации — положительная
обратная связь, рециркуляция > излученного. Фикс сечения (csec_node) лечит
только нормировку, не самозванство перехвата — замерено (6.14 ↑).

**План починки и предсказания ДО кода (в).** Правка: `front_interact` получает
`isleaf`; клеточная ветка 2 легальна ТОЛЬКО при isleaf (лист = базовая клетка —
там окно валидности А1566 честное); на узловом отрезке — только точное
пересечение (xi = xint || !isleaf): узел собирает куски поддерева, ближайшее
unstamped-попадание — событие сегмента, штамп А1564 гасит кратность. Прогнозы
записаны до прогона: П1 lp=0 побитово 4.2084; П2 lp=1 = 4.2084; П3 lp=2 ≈
4.1995 (замер xint=1 до правки), лестница плоская; П4 фактор ~0.25, рециркуляция
≈48; П5 G6=0 и НК-нули; П6 НК с предсказанным провалом — старый узловой
перехват 6.05 (замер сохранён); П7 box lp≥2 поднимется к lp=0; П8 цена lp=2 ≤
1.5×lp=0.

**Приёмка (г) — все прогнозы, кроме П8, сошлись:**

| lp | 0 | 1 | 2 | 3 | 4 (it=4) |
|---|---|---|---|---|---|
| E(8it) после правки | 4.2084 | 4.2084 | 4.1995 | 4.1710 | 4.0721 |
| ×эталона 4.1866 | 1.005 | 1.005 | 1.003 | 0.996 | 0.973 |

- **G1(a): СХОДИТСЯ НА ВСЕЙ ЛЕСТНИЦЕ** — вся в [0.9,1.1]×эталона, разброс
  крайних 3.3 %. lp=0/1 ПОБИТОВО прежние (П1/П2 ✓); lp=2 ПОБИТОВО равен
  замеру xint=1 до правки (П3 ✓) — правка делает xint=0 ≡ xint=1, ключ
  `xint=` на mode=3 теперь влияет только на листовые сегменты (форензика).
- **П4 ✓:** фактор 0.2544, рециркуляция 48.30 (было 146.0).
- **П5 ✓:** G6=0 на всех прогонах; le=0/noprop/tau0 → E≡0; screw=2 —
  d_cell=1104/d_csr=1104/d_empty=22; ρ=0 → 3.1355 = πLe (0.16 % от 3.1406).
- **П6 ✓ (провал предсказан и зафиксирован):** старый узловой перехват
  6.05/1.443×модели.
- **П7 ✗ ЧЕСТНО:** box lp=2/3 НЕ изменились (4.1393/4.0389) — на box узловой
  перехват не был активным ошибочным путём (стенки заполняют сечение узла),
  правка ничего не должна была менять, и не изменила; прогноз роста был неверен.
- **П8 ✗ ЧЕСТНО:** цена lp=2 = 3.1×lp=0 (it=4: 22 с против 7.5 с), lp=3 — 13×.
  Дешёвый пре-фильтр bbox×сегмент перед ray-tri добавлен (треугольник ⊆ своего
  bbox — отсечение консервативно, якоря не тронуты); ОБРЕЗКА СПУСКА СБОРА
  (gather) коробкой ребёнка дала lp=3: 98с→13с, НО сдвинула E на lp≥2 на
  ~1e-4 при побитово целых lp=0/1 — причина не найдена, слепок не совпал,
  В КОД НЕ ВЗЯТА (урок «слепок не совпал»); записана как лид по цене со своей
  задачей: понять событие (подозрение — стенки ровно на плоскостях детей,
  где допуски box-теста и ray-tri расходятся).
- Утечка санитайзеров: скретчи `abuf/wbuf` растут только на узловом пути
  (lp≥2) — освобождены рядом с `free(fc.pbuf)`; ASAN+UBSAN+LSan: 0/0 на
  lp=0 и lp=2. Попутно снят мёртвый `if (!stk)` на стековом массиве
  (gcc -Werror=address; пре-существующий, обновление тулчейна сделало его
  ошибкой). ccheck CLEAN.

**СТАТУС.** А1573 закрыт: пер-кусочный дробный lp-аккумулятор (утверждённая
схема «ДЕТАЛЬНОСТЬ КАК АККУМУЛЯТОР») теперь конвертируется в реальные этажи
пирамиды при любом ℓ_p — блокера лестницы важности больше нет. Открытые
хвосты: blocked-beam-сцена (А1566-фальсификатор), G3 (room vs pref), цена
узлового точного пересечения (обрезка gather + кэш топологии трубки А1575),
прибор пустоты на lp=0 (А1574).

**ГЕЙТ:** clang-format, ccheck CLEAN (gcc -fanalyzer, clang-tidy, cppcheck),
ASAN+UBSAN+LSan 0 ошибок 0 утечек, `make test` EXIT=0 (13 наборов).

## §854 (11-09): ХВОСТЫ §852 — BLOCKED-BEAM ПОСТРОЕН И ЗАМЕРЕН; G3 ЗАКРЫТ (ПРОТИВ ТЕОРИИ, НЕ ПРОТИВ PREF НА ДВУСТОРОННИХ СЦЕНАХ); PREF ПОЧИНЕН

**(1) BLOCKED-BEAM — фальсификатор А1566 построен и прогнан (сцены + прибор).**
`scripts/gen_blocked_beam.sh`: коробка [0,2]×[0,1]×[0,1], перегородка на
x=1.003 (вне плоскостей сетки), ПАНЕЛИ КОМПАРТМЕНТНЫЕ (по 5 граней на отсек —
иначе центроиды приёмных панелей попадают в ближний отсек); вариант
`blocked_beam_hole` — рама с дырой y,z∈[0.4,0.6]. Прибор: ключ `swee3
recv=X:<x0>:<x1>` — площадь-взвешенная E по кускам с исходным центроидом в
слэбе. **ЗАМЕР (герметичная перегородка, приёмник x∈[1.1,2]):**

| lp | 0 | 1 | 2 | 3 |
|---|---|---|---|---|
| E_recv(1it) | 1.20544 | 1.20296 | 1.18729 | 1.11789 |
| E_recv(8it) | 1.47178 | 1.46795 | 1.44381 | 1.33912 |

**ФАЛЬСИФИКАТОР СРАБОТАЛ: лестница НЕ плоская и на точном пересечении** —
−7.3 % (it=1) и −9 % (it=8) к lp=3, монотонно по lp. Механизм: узловой ветке
хватает ОДНОГО точного события на узел (ближайшее unstamped-попадание); узел
уровня ≥1 несёт несколько поверхностей вдоль трубки (углы, стыки панелей),
вторичные пересечения внутри узла теряются вместе с их депозитами — чем
крупнее узел, тем больше потерь. Дыра-НК (it=1, Le=1) не дискриминирует:
все поверхности светят одинаково Le, проход через дыру несёт тот же Le —
дискриминатор лестницы это lp-ДРЕЙФ, он и зафиксирован. **Статус: НЕ
починено — фикс «многопопадный проход по t на всех уровнях» реализован,
проверен и ОТЛОЖЕН (см. (3)): лестница становится плоской (0.8782 на всех
lp на box), но модель перестаёт совпадать с эталоном.**

**(2) G3 (room vs pref) — закрыт как «эталон неприменим к сцене».** room.obj —
ДВУСТОРОННИЙ СУП: 4962 из 4964 граней продублированы (внутренняя и внешняя
оболочки; room_thin/room_soup — то же). Pref (радиосити) считает ОБЕ копии —
эмиссия и обмен удваиваются: сходится к 12.33 = 0.98×двустороннего
замкнутого предела 2πLe/(1−ρ)=12.57. Свип со штампом А1564 дубли гасит:
6.074 ≈ одностороннему замкнутому пределу πLe/(1−ρ)=6.283 (0.967 ≥ 0.9 ✓
против теории). СВЕРКА pref (после починки, см. (4)): отношение 0.4927,
медиана по кускам 0.72 — ровно двустороннее завышение эталона. Контроль
моделей на ОДНОСТОРОНних сценах: box pref/sweep = 4.2112/4.1665 = 0.989 ✓;
cavity05 4.1866/4.2084 = 1.005 ✓. **Вывод: G3-правило «sweep ≥ 0.9×pref»
выполнено против теории (0.967) и нарушено против pref (0.49) только на
двусторонних сценах, где pref завышает ×2 — эталону нужен односторонний
room (дедупликация граней) — записано в хвосты.**

**(3) А1576 [ЗАМЕРЕНО, МОДЕЛЬНЫЙ КОНФЛИКТ — главный хвост]: две модели
стенка-обмен, выбор не сделан.** Реализован и замерен вариант «точный
многопопадный проход на всех уровнях» (walk): лестница ℓ_p плоская ПО
ПОСТРОЕНИЮ (box: 0.8782 на всех lp; blocked-beam: тоже плоская), НО на box
walk даёт 0.878 против 4.17 у листовой перехватной схемы и 4.21 у pref:
точный проход депонирует только РЕАЛЬНЫЕ пересечения трубки со стенкой, а
валидированная перехватная модель (§846/А1566) — ещё и обмен стенки со
смежными клетками по теневой доле f (стенка как площадной источник для
своего клеточного слоя). На cavity05 модели совпадают (стенки клеточно-
выровнены), на box/box_rot (сдвиг/поворот) расходятся в 2–5 раз. Промежу-
точные варианты тоже замерены и отброшены: полуоткрытые сегменты
[tin,tout) с закрытым концом домена — падение до 1.73 (сравнение tt==tend
по double из разных арифметик ненадёжно); обрезка спуска gather коробкой
ребёнка — лестница плоская, стоимость lp=3 98с→13с, НО сдвиг E на lp≥2
~1e-4 без объяснения («слепок не совпал»). **РЕШЕНИЕ ЗА ФАЛЬСИФИКАТОРОМ:
нужна сцена, где обе модели дают РАЗНЫЕ проверяемые предсказания против
аналитики (например, полость с известным вид-фактором и стенкой вне
клеточной сетки), и по ней выбирать; до того — лестница ℓ_p≤1 (обе модели
согласны), lp≥2 — известный дрейф −7..9 %.**

**(4) pref починен (не собирался с cc9bd1e):** (а) `double aa` → индекс
(`array subscript is not an integer`); (б) блок trivert заполнялся ДО
`memset(&so)` и стирался, а mode=2+vc требует trivert — «pref: свип не
 прошёл»; сверка переведена на mode=3 (полный набор trivert/tribox/lp/domhi
 + hz_pyr_set_lp). Эталон cavity05 воспроизведён: 4.1866 ✓. Ключ `swee3
recv=` добавлен (по умолчанию выключен — прежний вывод побитово прежний).

**ГЕЙТ:** clang-format; ccheck CLEAN (swee3.c, pref.c); ASAN/UBSAN/LSan на
recv-пути: 0/0; `make test` EXIT=0 (13 наборов). Якоря lp=0/2 cavity05
воспроизведены (4.2084/4.1995). **Хвосты (обновл.):** А1576 (выбор модели —
фальсифицирующая сцена); односторонний room для pref; А1574; цена
(обрезка gather — нужен разбор сдвига 1e-4; кэш топологии); blocked-beam
итог: lp-дрейф −7..9 % зафиксирован как мера остатка А1573.

## §855 (11-09): А1576 — ФАЛЬСИФИКАТОР ПОСТРОЕН (СЦЕНА + PER-PIECE Ke + WALK-РЕЖИМ); ЗАМЕРЫ ДАЛИ АНОМАЛИЮ РЕШЁТКИ (А1577) — ВЕРДИКТ О МОДЕЛИ ПОКА НЕ ВЫНЕСЕН

**Инструменты (в коммите).**
- `scripts/gen_falsifier.sh`: две параллельные пластины в пустоте — эмиттер
  (x=XE, ширина YE по y, Kd=0, **Ke=1**) и приёмник (x=XR, всё сечение,
  Kd=0); третий кусок — инертный экран за приёмником (делает приёмник
  ВНУТРЕННЕЙ стенкой: без этого пересечение выхода сидело на грани домена
  и терялось на ulp-гонке). ρ=0 ⇒ single-hop, итог точен на it=1.
- `swee3 ke=1` — per-piece эмиссия из Ke материала (среднее трёх каналов,
  прецедент скаляризации pfield.c) ПЛЮС глобальный le: на сценах с Ke=0
  побитово прежний мир (`hz_sw_opts.lep`). `swee3 walk=1` — точный
  МНОГОПОПАДНЫЙ проход сегмента (`hz_sw_opts.walk`, front_seg_walk): все
  пересечения по ходу трубки, лестница ℓ_p плоская по построению.
  Умолчания прежние (lep=NULL, walk=0) — старые прогоны побитовы.

**Замкнутый ответ (модель «реальные пересечения»).** Трубка депонирует на
приёмник ⇔ её (y,z)-колонка пересекает эмиттер ⇒ E_half/E_full = YE (пло-
щадное отношение), независимо от lev, направления и квадратауры. YE=0.47.

**ЗАМЕР (ρ=0, it=1, приёмник x∈[0.6,1.0], E_half/E_full):**

| lev | 4 | 5 | 6 | 7 |
|---|---|---|---|---|
| перехват (walk=0) | 0.5114 | **0.2615** | 0.4785 | 0.4798 |
| walk (walk=1) | 0.5478 | **0.2891** | 0.4879 | 0.4846 |

- **А1577 [ЗАМЕРЕНО, ГЛАВНАЯ НАХОДКА]: аномалия решётки на lev=5** — ОБЕ
  модели проваливают отношение вдвое (0.26/0.29 против 0.47): артефакт
  ТРУБЧАТОЙ РЕШЁТКИ/семейств (не моделей — одинаков у обеих). На lev=4
  обе модели завышают (0.51/0.55 — край эмиттера недорезателен решёткой),
  на lev 6/7 обе сходятся к 0.47–0.49, причём ПЕРЕХВАТ ближе к аналитике
  (0.479/0.480 против 0.488/0.485 у walk). До починки решёточного
  артефакта прибор НЕ дискриминирует модели — вердикт А1576 отложен.
- НК: ρ=0 ⇒ E стационарна по итерациям (2.10391 = 2.10391 = … ✓);
  без Ke ⇒ E ≡ 0 ✓. Якоря cavity05 lp=0/2 воспроизведены (4.2084/4.1995).
- Walk-аномалии на box (зависимость от lev, §854(3)) воспроизведены и на
  фальсификаторе — общий класс: сегментные стыки/семейства трубок, а не
  ulp (допуск ±1e-9 на приёме попаданий не меняет ни одной цифры).

**Следующий шаг по А1577:** прибор решёточного артефакта — сведение по ОДНОЙ
трубке (трасса сегментов/событий уже есть под HZ_DBG в отладочных сборках):
найти, куда деваются депозиты половины трубок на lev=5. Только после этого —
вердикт А1576 и, если выиграет walk, его перенос в продакшн с починкой
обмена стенка-слой (§854(3)).

**ГЕЙТ:** clang-format; ccheck CLEAN (sweep.c/sweep.h/swee3.c); ASAN+UBSAN
на walk/ke-пути: 0/0; `make test` EXIT=0 (13 наборов, job bash-178, 11-09). Прежний мир
побитово: lep=NULL/walk=0 — якоря воспроизведены.

## §856 (11-09): А1577 РЕШЁН — ULP-ГОНКА НА КОНЦАХ СЕГМЕНТОВ; ДОПУСК ±1e-9 ВОССТАНАВЛИВАЕТ ПОТЕРЯННЫЕ ДЕПОЗИТЫ (ЯКОРЯ СДВИНУЛИСЬ НА +0.04 % ЧЕСТНО)

**Механизм А1577.** Стенка, лежащая РОВНО на конце сегмента (грани домена:
эмиттер фальсификатора на входной грани, приёмник/стенки box — на выходной),
даёт ray-tri tt, отличающийся от box-сегментного t на единицы ulp (разная
арифметика: векторные произведения против (плоскость−org)/om). Строгий
тест `tt >= tin && tt <= tout` проигрывает гонку знакопредсказуемо для всей
семьи трубок: на входной стенке трубка ОСТАЁТСЯ ТЁМНОЙ и теряет ВЕСЬ
депозит; на выходной — стенка не получает свой. **Допуск ±1e-9·(|tin|+
|tout|+1) на оба конца** в приёме попаданий (walk и перехват) снял
аномалию: фальсификатор lev=5 — E_half 1.19→2.13 (walk), 1.04→1.94
(перехват); отношение half/full теперь 0.49–0.55 на ВСЕХ lev без провалов.

**Матрица А1576 (после починки, E_half/E_full, аналитика 0.47, решётка
0.44–0.48):**

| lev | 4 | 5 | 6 | 7 |
|---|---|---|---|---|
| перехват | 0.5114 | 0.4889 | 0.4785 | 0.4798 |
| walk | 0.5132 | 0.4997 | 0.4879 | 0.4846 |

Обе модели сходятся к аналитике с ростом lev; перехват ближе на 1–2 п.п.
на fine-решётках. **ВЕРДИКТ А1576 ПО КОНТРОЛЬНОМУ ОТНОШЕНИЮ: модели
эквивалентны в пределах решёточной ошибки** — дискриминатор переехал на
blocked-beam (дрейф по lp) и сцену с известным полным обменом (замкнутая
коробка): там перехват 4.17 ≈ pref 4.21, walk 0.88 — walk теряет депозиты
на гранях домена ДО СИХ ПОР (его собственный дефект, не снимается
допуском — отдельное расследование, walk остаётся НЕ продакшн).

**Последствия допуска для продакшна (перехват):** возвращены депозиты
граничных стенок — якоря честно сдвинулись: cavity05 lp=0 4.2084→**4.2100**,
lp=2 4.1995→**4.2011**, lp=3 4.1710→4.1734; box 4.1607→4.1644. Всё в
G1(a) [0.9,1.1]×4.1866 (1.0056×эталона), лестница плоская (разброс 0.9 %).
Blocked-beam: приёмник-стенка домена сама была жертвой гонки — E_recv
поднялся 1.47→3.27 (lp=0), дрейф по lp: 3.274/3.264/3.203/2.926 (−10.6 %
к lp=3) — остаток А1573-класса теперь виден ЧИСТО (раньше смешивался с
гонкой). НК: le=0 → E≡0 ✓.

**Новые якоря:** cavity05 lp=0 E(8it)=4.2100, lp=2=4.2011, lp=3=4.1734;
box lp=0 E(8it)=4.1644; blocked-beam lp=0 E_recv(8it)=3.2742. Утечка lep9
(освобождён) закрыта; санитайзеры 0/0 на walk/ke-пути и cavity.

**ГЕЙТ:** clang-format; ccheck CLEAN; ASAN/UBSAN/LSan 0/0; `make test`
EXIT=0 (13 наборов, прогнано 11-09). **Хвосты:** остаточный lp-дрейф blocked-beam
(−10.6 %) — вторичные пересечения на узлах; walk на гранях домена;
односторонний room; цена (обрезка gather, кэш топологии).

## §857 (11-09): А1578 [ЗАМЕРЕНО] — АСИММЕТРИЯ ДЕПОЗИТОВ MIN/MAX-ГРАНЕЙ ДОМЕНА В WALK (СЛЕДСТВИЕ ЗАМЕРА НА BOX, ПЕР-КУСКОВО)

Разбирался, почему walk на замкнутой коробке даёт 0.88 против 4.17 у
перехвата и 4.21 у pref. Пер-кусковые депозиты (it=1, ρ=0.5):

| грани | walk | перехват |
|---|---|---|
| min-грани (x=0,y=0,z=0) | 1.14–1.57 | 4.95 |
| max-грани (x=1,y=1,z=1) | 0.19–0.39 | 0.19 |

**Обе модели асимметричны ОДИНАКОВО**: max-грани голодают в 4–26 раз.
Значит, дефект НЕ в walk-проходе — он в ОБЩЕМ слое: семействах трубок
front_dir (вход f[ax] по знаку ω, штамп-дедуп stampv) либо в том, что
трубка начинает марш с t<0 (стенка позади org: подхват радианса на
tt≈h0 происходит, а вот депозиция на СВОЮ стенку позади — никогда: трубка
уходит вперёд). На min-гранях светят встречные семейства (их выход), на
max-гранях — тоже встречные… асимметрия указывает на то, что семейства с
отрицательными осями недосчитываются (штамп-дедупstampv гонит колонку
один раз на НАПРАВЛЕНИЕ, и вход max-грани съеден семейством +оси, которое
не депонирует на тёмном входе). Это объясняет и провал А1577 на lev=5, и
занижение walk на box, и, вероятно, часть дрейфа blocked-beam.

**Следующий шаг:** трасса одной колонки (все три семейства одного
направления) с печатью (family, вход, события) — найти, какое семейство
что депонирует. До этого вердикт А1576 «модели эквивалентны» остаётся в
силе только для ОТНОШЕНИЙ (фальсификатор), не для абсолютов.

Гейт: без изменений кода (расследование); якорь cavity05 4.2100
воспроизведён после снятия отладочной печати.

## §858 (11-09): А1578-ТРАССА — 44 % ВЫХОДНЫХ ДЕПОЗИТОВ ТЕРЯЮТСЯ В WALK; ТОЧНОЕ МЕСТО ЛОКАЛИЗОВАНО, ПОЧИНКА — СЛЕДУЮЩИЙ ЗАПУСК

Трасса депозиционного прохода walk (box, dirs=6, lev=3, it=1): среди первых
39 событий **25 тёмных подхватов (Lin=0) против 14 реальных депозитов
(Lin=1)** — при симметрии «каждая трубка = 1 подхват + 1 депозит» ожидалось
1:1. ~44 % трубок подхватывают радианс на входной стенке (tt≈h0) и НЕ
депонируют на выходной (tt≈h1), хотя трасса сегментов показывает попадания
на выходе (hits=2 в последнем сегменте). Допуск ±1e-9 на приёме попаданий
потери не убирает ⇒ потеря НЕ в тесте отрезка, а ДАЛЬШЕ по циклу: главный
подозреваемый — ШТАМП при тёмном событии: подхват (Lin=0) штампует ВХОДНУЮ
стенку, у части трубок входная и выходная стенки оказываются ОДНИМ И ТЕМ ЖЕ
куском (соседние треугольники одной грани на диагональном шве — трасса
показала депозит Lin=1 на seg[-0.062,0.062], т.е. в НАЧАЛЬНОМ сегменте) —
штамп с гашением дублей в списке рвёт цепочку. Проверка одним прогоном:
штамповать только при Lin>0, но НЕ гасить тёмные (либо не штамповать тёмный
подхват вовсе) — и сравнить матрицу фальсификатора/box/lp-лестницу.

Инструментация снята (восстановлен cb562ec), якорь cavity05 4.2100
воспроизведён. Всё, что нужно для починки, записано здесь.

## §859 (11-09): А1578-СЧЁТЧИКИ — РЕАЛЬНЫХ ДЕПОЗИТОВ ~19 % ОТ ПЕРЕСЕКАЮЩИХ ТРУБОК (box, it=1)
Инструментированный счёт (box lev=3, it=1): событий ~5390, реальных
депозитов ~2378 при ~12 тыс. трубок, пересекающих стенки — депонирует
~19 %. Потеря НЕ в депозитном цикле (SKIP-печать: ноль срабатываний
штампа) и не в допуске отрезка — события собираются, но их на порядок
меньше, чем пересечений: потеря в том, КАКИЕ сегменты/куски получает
walk (bbox-индекс листьев не содержит стенки для большинства колонок? —
следующая проверка: печать n (кандидатов) по сегментам граничного слоя).
Инструментация снята, дерево чистое, якорь 4.2100 на месте.

## §860 (11-09): А1578-СУЖЕНИЕ — 0.22 СОБЫТИЯ НА ТРУБКУ (ДОЛЖНО ≥2); ПО ПЕР-КУСКОВОЙ ПЕЧАТИ ИНДЕКС А1566 ВЫГЛЯДИТ КОРРЕКТНЫМ
Счёт: 5390 событий на ~24.6 тыс. трубок box (it=1) — 89 % трубок не собрали
НИ ОДНОГО события, хотя стартовая и конечная клетка каждой трубки — граничный
слой со стенкой (подхват на h0 и депозит на h1). Индекс А1566 по коду
корректен (floor/ceil−1, клэмпы, плоские bbox → один слой) — значит,
расхождение в другом: либо стартовая клетка DDA не та (f[ax]/cc для
отрицательных осей), либо h0/h1 от front_box_seg при ω с нулями, либо
walk-вызовы идут не по тем сегментам. Следующий шаг: печать (n, nh) по
ВСЕМ walk-вызовам одной семьи + cc/om трубки. Инструментация снята,
дерево чистое, якорь 4.2100 на месте (4e7ff4e+).

## А1578-МОСТИК В НОВУЮ СЕССИЮ (прочесть первым; 11-09, коммиты 3877778..8ed8100)

**Состояние.** А1577 решён (допуск ±1e-9 на концах сегментов, включён в
продакшн — перехват и walk). Якоря СДВИНУЛИСЬ: cavity05 lp=0/2/3
E(8it)=4.2100/4.2011/4.1734, box 4.1644, blocked-beam E_recv(8it,lp=0)=3.2742;
НК чисты; G1(a) соблюдён (1.0056×эталона), лестница плоская на 0.9 %.
FullHD: 3.76 с холодный / 3.28 с кадр движения (сбор 92 %) — в
PLAN_TRANSPORT.

**Задача-блокер: А1578.** На box (it=1, ρ=0.5, lp=0) — 0.22 события на
трубку (должно ≥2: подхват на h0 + депозит на h1 в граничных клетках);
89 % трубок без событий. Известно твёрдо: депозитный цикл чист (SKIP-печать
— ноль), допуск не при чём, индекс А1566 по коду корректен (floor/ceil−1,
клэмпы). Остались подозреваемые: стартовая клетка DDA при отрицательных
осях (f[ax]/cc в front_dir/front_tube), h0/h1 при нулевых компонентах ω,
либо walk вызывается не для тех сегментов.

**Шаг 1 (локализация).** В front_seg_walk (src/nstruct/sweep.c) добавить
file-scope счётчики dbg_calls/dbg_ev/dbg_real (реальный депозит = Lin>0.5),
печать по getenv("HZ_DBG") после депозитного цикла; прогон
`swee3 box.obj mode=3 lev=3 dirs=6 rho=0.5 it=1 lp=0 walk=1` под HZ_DBG.
Затем печать (n, nh) по сегментам одной семьи + cc/om трубки в front_tube.
Контрольные числа с прошлого запуска: событий ~5390, реальных ~2378
(=19 % от ~12 тыс. пересекающих трубок).

**Шаг 2 (починка + вердикт А1576).** После фикса: box walk должен выйти на
~4.2 (перехват 4.1644, pref 4.2112); тогда walk — продакшн, lp≥2
замыкается (проверить blocked-beam: дрейф −10.6 % должен уйти — это
вторичные пересечения узлов; при необходимости walk на узлах вместо
одного события). Если walk не выходит на 4.2 — модель перехвата
остаётся, lp≥2 дрейфует дальше, вердикт по фальсификатору §856.

**Шаг 3 (после А1576):** пер-кусочный дробный аккумулятор детальности
(Δ(материал,угол), этаж = целая часть) — утверждённая пользователем схема;
затем политика ℓ_p §844; параллельно — цена сбора (OpenMP, 92 % кадра).

**Запрещено по-прежнему:** wave/ не читать; гистерезис/переиспользование/
двухфазный хвост (отклонены измерениями); скрипты, пишущие файл в файл.
Гейт: clang-format, scripts/ccheck.sh, make test целиком, санитайзеры на
новом пути; доклад в PLAN_ELEMENTS.md. Якорь для сверки любого изменения:
cavity05 lp=0 E(8it)=4.2100, G6=0, screw=2 → d_cell=1104/d_csr=1104/
d_empty=22; box lp=0 4.1644; falsifier: E_half/E_full → 0.47–0.49 на lev 6/7.

## §861 (11-09): А1578 РЕШЁН — КУСКИ ПЛОСКИХ СТЕНОК ВЫПАДАЛИ ИЗ BBOX-ИНДЕКСА + ВЫРОЖДЕННЫЙ СЕГМЕНТ ШЁЛ ПОСЛЕ МАРША; ВЕРДИКТ А1576: WALK В ПРОДАКШН (BLOCKED-BEAM ПЛОСКИЙ ПО ВСЕЙ ЛЕСТНИЦЕ lp)

**Шаг 1 (локализация, счётчики + пер-трубочная печать; инструментация снята).**
box lev=3 dirs=6 it=1: 486 трубок, 530 событий (384 тёмных), реальных
депозитов 146; 21 % трубок без единого события. Трасса 12 трубок (по 2 на
направление) показала: у ОТДЕЛЬНОЙ трубки события корректны (подхват на h0,
депозит на h1), но агрегаты несимметричны. Два точных места:

1. **Индекс А1566 терял плоские куски.** Span «floor…ceil−1» с клэмпом к
   домену вырожден у плоской стенки в ОДНУ клетку слоя; у box слой
   max-грани x=7 имел 32 листа из 81 (остальные слились в coarse-узлы) —
   кусок выпадал из индекса целиком, стенка недостижима для walk.
2. **Порядок вырожденного сегмента.** Фантомная клетка за стенкой (лист
   есть — pyr помечает floor-ом) даёт вырожденный сегмент [h,h]; при равных
   c0 устойчивая сортировка детей ставила его ПОСЛЕ внутридоменного марша:
   подхват выполнялся последним и депонировал Lh=1 на саму входную стенку
   (2.36 на треугольник × 2), а выходной депозит проходил тёмным (Lin=0).

**Шаг 2 (починка, `src/nstruct/sweep.c`).** (1) `sw_index_piece`: первичный
span прежний; кусок, чья span-клетка осталась без листа, фолбэчится на
СОСЕДНИЕ по осям клетки с листом — фолбэк ТОЛЬКО при walk=1 (перехват живёт
прежней моделью, якоря перехвата не сдвигаются; (лист,кусок)-пары уникальны,
двухпроходный counting-sort согласован). (2) Сортировки детей марша (обе:
лист-спуск и узловой спуск) получили tie-break по c1 при равных c0 —
вырожденный сегмент идёт первым; tie-break включён только при walk=1.
Сегмент соседа, не содержащий tt стенки, даёт ноль попаданий — лишних
событий нет, штамп А1564 гасит кратность.

**Шаг 3 (вердикт А1576 — ПОЛОЖИТЕЛЕН, walk в продакшн).**

| замер | перехват (референс) | walk (починенный) |
|---|---|---|
| box lp=0 E(8it), lev=6 dirs=26 | **4.1644** (битово) | **4.0816**, плоско по lp 0–3 |
| cavity05 lp=0/2/3 E(8it) | 4.2100 / 4.2011 / 4.1734 ✓ | — |
| blocked-beam E_recv(8it), lp=0→3 | 3.2706 → 2.9205 (−10.7 %) | **4.30133 — ПЛОСКО** |
| НК: le=0, noprop | E≡0 ✓ | E≡0 ✓ |
| НК: hole, E_recv(1it) | >0 ✓ | 3.0715 ✓ |

Критерий мостика «box walk ~4.2» выполнен в пределах решёточной ошибки
(4.0816 = 0.968×перехвата, 0.969×pref 4.2112) и дрейф −10.6 % по lp УШЁЛ
ПОЛНОСТЬЮ — walk закрывает и вторичные пересечения узлов (А1573-остаток).
Остаток: walk ниже pref на ~3 % (у перехвата ~1 %) — тёмные входные
депозиты и диагональные двойные касания стенок (8/72 трубок), отдельно не
чинкились. Переключение дефолта mode=3 на walk в потребителях — следующий
шаг вместе с политикой ℓ_p §844.

**ГЕЙТ:** clang-format OK; ccheck: gcc -fanalyzer CLEAN (после
инициализации lo/hi — FP анализатора), cppcheck только унаследованная
invalidPointerCast (есть в референсе); make test EXIT=0; ASAN/UBSAN/LSan 0/0
на box/cavity05/blocked-beam walk-путях. Якоря: cavity05 4.2100/4.2011/
4.1734 ✓, box перехват 4.1644 ✓ (битово), G6=0, НК-нули ✓.

**Дальше (г):** пер-кусочный дробный аккумулятор детальности
(Δ(материал,угол), этаж = целая часть) — утверждённая схема, НЕ начат.

## §862 (11-09): ПЛАН — ПЕР-КУСОЧНЫЙ ДРОБНЫЙ АККУМУЛЯТОР ДЕТАЛЬНОСТИ: Δ(МАТЕРИАЛ,УГОЛ), ЭТАЖ = ЦЕЛАЯ ЧАСТЬ (ДО КОДА; СХЕМА УТВЕРЖДЕНА ПОЛЬЗОВАТЕЛЕМ)

**ЗАЧЕМ.** ℓ_p сейчас единый целочисленный на весь прогон (`lp=`, лестница
G1 вручную). Утверждённая схема «детальность как аккумулятор»: каждый кусок
несёт ДРОБНЫЙ счётчик детальности, который НАКАПЛИВАЕТ приращения от реальной
нагрузки куска, и эффективный ℓ_p куска = ЦЕЛАЯ ЧАСТЬ аккумулятора (этаж).
Лестница важности перестаёт быть ручной: подробность растёт там, где кусок
реально нагружен, и лестница ℓ_p обязана остаться плоской уже БЕЗ ручного
подбора lp.

**МЕХАНИКА.**
- Состояние: `double *lpfrac [nt]`, инициализация = базовый lp (ключ `lp=`).
- Накопление: в точке депозита (front_seg_walk и front_interact — ОДИН хук,
  семантика «кусок нагружен»): `lpfrac[p] += Δ(ρ_p, cosθ_p)`, где cosθ_p —
  косинус падения трубки к нормали КУСКА (уже есть `front_cos`), ρ_p —
  альбедо куска (`front_rho`). Форма приращения (именованная константа,
  ключ swee3 `cdelta=`): `Δ = cdelta · ρ_p · (1 − cosθ_p)` — темнее материал
  и более скользящее падение → быстрее набор детальности; никаких других
  магических констант.
- Конвертация: в конце КАЖДОЙ итерации свипа `этаж[p] = floor(lpfrac[p]) →
  uint8` → `hz_pyr_set_lp` → марш следующей итерации идёт по новым этажам.
  `hz_pyr_set_lp` аллоцирует leaf_lp/lev_lp — для повторного вызова нужен
  адресуемый сброс (новая `hz_pyr_clear_lp` или флаг reuse), утечки не
  допустить (санитайзеры).
- Ключи swee3: `adapt=1` (умолчание 0 — битовая совместимость), `cdelta=F`
  (умолчание 1.0); печать гистограммы этажей по итерациям (прибор: сколько
  кусков на каждом этаже — предсказуемость набора).

**ПРЕДСКАЗАНИЯ ДО ПРОГОНА.**
- П1: при `adapt=0` весь путь битово прежний (аккумулятор не пишется,
  set_lp вызывается один раз как сейчас).
- П2: box adapt=1 lp=0, it=8: E в [0.9,1.1]×4.1644 (G1a); lp-лестница
  остаётся плоской БЕЗ ручного lp.
- П3: гистограмма этажей на box: при cdelta=1 типичный приращение
  ρ·(1−cosθ) ≤ 0.5 за событие; за 8 итераций этажи ≤ 2 у стенок, 0 у
  ненагруженных кусков; ни один кусок не превышает доступного уровня
  (клэмп вверх А1568 считает и печатает).
- П4: blocked-beam walk=1: E_recv остаётся плоским по lp при adapt=1
  (адаптивность не реанимирует дрейф).
- П5: НК le=0 → E≡0 при adapt=1 (аккумулятор не создаёт энергию).
- П6: цена пересборки этажей — O(nt + узлы пирамиды) на итерацию, ≤ 1 %
  времени итерации (замер по часам свипа).

**ГЕЙТ:** clang-format; ccheck CLEAN; санитайзеры на adapt-пути; make test
целиком; якоря: cavity05 4.2100/4.2011/4.1734, box перехват 4.1644 —
битово при adapt=0; walk-якоря §861 (4.0816/4.30133) — битово при adapt=0.

### §862: ДОКЛАД — МЕХАНИКА АККУМУЛЯТОРА ВСТАВЛЕНА И ЧИСТА; П3 ПРОВАЛЕН ЧЕСТНО: Δ ЗА СОБЫТИЕ БЕЗ НОРМИРОВКИ ВЫРОЖДАЕТСЯ (НАСЫЩЕНИЕ ЭТАЖЕЙ) — НУЖНО РЕШЕНИЕ ПО НОРМИРОВКЕ

**Сделано (код).** `hz_sw_opts`: `lpacc [nt]` (дробный аккумулятор, массив
вызывающего) + `cdelta` (вес Δ); хук `sw_accum(fc,p)` = `cdelta·ρ_p·
(1−cosθ_p)` в ТРЁХ точках депозита (walk-проход, точная ветка перехвата,
клеточный перехват); в конце каждой итерации mode=3: этаж = floor(lpacc) →
`hz_pyr_set_lp` (новый `hz_pyr_clear_lp` — повторный вызов без утечек);
ключи swee3 `adapt=1`, `cdelta=F`; печать гистограммы этажей
(«ЛОД-АДАПТ»). Ключи `lp=`/`adapt=0` (умолчание) — битово прежний мир.

**Прогнозы.**
- П1 ✓ БИТОВО: adapt=0 — box walk 4.0816, box перехват 4.1644 — как §861.
- П2 ✓: adapt=1 E остаётся в G1a (совпадает с базой до отказа-на-лист).
- П3 ✗ ЧЕСТНО: гистограмма ДЕГЕНЕРИРУЕТ — уже при cdelta=0.005 все 12
  кусков box упираются в клэмп 15 (насыщение). Причина: Δ за СОБЫТИЕ
  суммируется по ~2000 событий (трубки × направления × итерации) на кусок —
  аккумулятор мерит ЧИСЛО СОБЫТИЙ (сценно-зависимое), а не (материал,угол);
  клэмп А1568 превращает всё в «отказ-на-лист», и E совпадает с базой
  тривиально. Прогноз «этажи ≤ 2» был неверен на два порядка.
- П5 ✓: le=0 при adapt=1 → E≡0 (аккумулятор энергию не создаёт).
- П4 не снимался (бессмысленно до решения П3).

**СЛЕДСТВИЕ (вопрос схемы, требует решения до следующего кода).** Форма
Δ(материал,угол) нуждается в НОРМИРОВКЕ, иначе этаж = log₂(число событий):
(а) за событие без нормировки — вырождено (этот замер); (б) среднее за
итерацию (делить на число попаданий куска на итерации) — теряет
«нагруженность»; (в) энерговзвешенно (депозит куска как вес) — выходит за
формулу Δ(материал,угол), но осмысленно физически. Кандидат (в) (сам
физичный: детальность следует переносимой энергии), но решает пользователь.

**ГЕЙТ:** clang-format OK; gcc -fanalyzer CLEAN; cppcheck — только
унаследованная invalidPointerCast (есть в референсе); make test EXIT=0;
ASAN/UBSAN/LSan 0/0 на adapt=1-пути (box, cdelta=0.01, it=3); П1-якоря
битовы (4.0816/4.1644; cavity05 4.2100 не тронут — adapt не активен).

### §862-диаг: НОРМИРОВКА Δ ИЗМЕРЕНА — (а)/(б) АНТИ-СЕЛЕКТИВНЫ (КУСОЧНЕЕ РОВНО ТАМ, ГДЕ НЕЛЬЗЯ), (в) СЛАБО ПРАВИЛЬНА; ИНСТРУМЕНТЫ ОСТАЮТСЯ В КОДЕ

**Приборы (в коде, ключи swee3):** `adapt=2` — пассивная диагностика
(аккумулятор и `lphits` копятся, этажи НЕ применяются, марш не тронут);
`dumpE=1` — дамп per-piece нагрузки последней итерации («E1 p= tri= e=
hits= sdelta=»); `scripts/diag_rank.py` — сопоставление с «истинным
этажом». Истинный этаж куска = максимальный lp, при котором
|E_p(lp) − E_p(0)| ≤ tol·E_p(0) (tol — 1/5/10 %); ненагруженные куски
(E_p ≈ 0) из ранжира исключены. Метрика различения: AUC = P(значение
чувствительного куска < значения терпимого) — 0.5 = монетка, < 0.5 =
«огрубевает ровно там, где нельзя».

**Замер (box + cavity05, walk, lev=6 dirs=26; box it=8, cav it=4).**
box не дискриминирует: все 12 кусков терпят lp=3 (однородная замкнутая
коробка) — ожидаемо, арбитр — cavity05 (2208 кусков; истинные этажи при
tol=5 %: 0:1, 1:42, 2:55, 3:2110).

| схема | AUC tol=1 % | tol=5 % | tol=10 % |
|---|---|---|---|
| (а) число событий | 0.371 | **0.137** | **0.030** |
| (б) среднее Δ за событие | 0.343 | **0.114** | **0.011** |
| (в) энергия (депозит) | **0.611** | **0.585** | 0.482 |
| НК (перемешивание) | −0.014 ≈ 0 ✓ | | |

**ВЫВОДЫ.**
1. (а) и (б) АНТИ-СЕЛЕКТИВНЫ, и это не шум: чем больше допуск, тем хуже
   (0.37 → 0.03) — число событий/среднее Δ стабильно выдают «можно
   грубеть» ровно противоположно. Механизм: куски с массой косых
   пересечений — диффузно-нагруженные и грубеют безопасно; редкие
   острые куски (кромки, тонкие оболочки) и есть чувствительные.
   Утверждённая форма Δ(материал,угол) за событие отвергнута ЗАМЕРОМ.
2. (в) энерговзвешенно — единственная схема с правильным знаком
   (AUC 0.59–0.61 на рабочих допусках 1–5 %), но сила мала: на 10 %
   сигнала нет. «Энергия → грубее» подтверждает модель «диффуз гладеет
   с отскоком»; чувствительные куски — слабонагруженные и острые.
3. Кандидат на продакшн: (в) с нормировкой на площадь куска и Σ
   излучённого за итерацию; (материал,угол) — вторым аргументом-множителем
   (например ρ·(1−cosθ) как МНОЖИТЕЛЬ к энерговкладу, знак и степень —
   следующий замер на той же матрице: прибор уже стоит).
4. Ограничения: одна дискриминирующая сцена (cavity05); подтверждение на
   room/blocked-beam — перед продакшном; монотонность «этаж ↑ = грубее»
   выбрана по §814 («диффуз гладеет»), при обратной интуиции знак
   переворачивается, механика та же.

**ГЕЙТ:** clang-format OK; ccheck 0 error (cppcheck — унаследованная
invalidPointerCast); make test EXIT=0; якорь box walk 4.0816 битово ✓
(диагностика при adapt=0/off ничего не пишет в марш).

### §862-диаг-2: МАТРИЦА ФОРМ Δ НА ТОЙ ЖЕ МАТРИЦЕ — МНОЖИТЕЛЬ (1−cosθ) САМ ПОРТИТ РАНЖИР; ЛУЧШАЯ ФОРМА — ЧИСТЫЙ ЭНЕРГОВКЛАД (am4/am2); НАЙДЕНЫ ОТРИЦАТЕЛЬНЫЕ ДЕПОЗИТЫ

Ключ `amode=` (форма Δ, см. sweep.h): 0 Σρ(1−cosθ), 1 E·ρ(1−cosθ),
2 E·ρ, 3 E·(1−cosθ), 4 E. cavity05, та же методика (AUC по «истинному
этажу»):

| форма | tol=1 % | tol=5 % | tol=10 % |
|---|---|---|---|
| am0 Σρ(1−cosθ) [= (а)] | 0.354 | 0.338 | 0.387 |
| am1 E·ρ(1−cosθ) | 0.325 | **0.122** | **0.032** |
| am2 E·ρ | **0.611** | **0.585** | **0.485** |
| am3 E·(1−cosθ) | 0.325 | **0.122** | **0.032** |
| am4 E | **0.611** | **0.585** | **0.485** |

1. **Множитель (1−cosθ) — яд:** am1/am3 хуже чистой энергии на порядок
   (0.03–0.33 против 0.49–0.61): скользящие куски — безопасно-грубые,
   и множитель огрубляет именно их точнее в мусор. Угол как аргумент Δ
   в форме МНОЖИТЕЛЯ отвергнут замером.
2. **ρ-множитель нейтрален** на этих сценах (ρ один на все куски —
   am2 ≡ am4 побитово по ранжиру); дискриминация ρ ждёт многосцены.
3. **РЕШЕНИЕ по нормировке:** Δ = cdelta · энерговклад куска (am4),
   без углового множителя; ρ остаётся множителем на многосценах.
4. **Попутная находка (запись, не чинка):** существуют ОТРИЦАТЕЛЬНЫЕ
   депозиты (Lin < 0 в части событий; sdelta до −235 за прогон) — раньше
   маскировалось, на гистограмме этажей проявилось стёком стека
   (cnt[fl<0], починено клэмпом). Природа отрицательного Lin — отдельный
   хвост: либо легальная арифметика G6-долей, либо дефект носителя.
5. Sanitizer-прогон конфигурации amode=1: stack-buffer-overflow был в
   гистограмме swee3 (выше), в sweep.c переполнений нет.

**ГЕЙТ:** clang-format OK; ccheck 0 error; make test EXIT=0; якорь box
walk 4.0816 битово ✓.

## §863 (11-09): ПЛАН — СКОРОСТНОЙ ХВОСТ: PER-NODE ИНДЕКС → ВИРТУАЛЬНАЯ ПИРАМИДА КУСКОВ С ДИФФУЗНОЙ АГРЕГАЦИЕЙ (ДО КОДА)

**ЗАЧЕМ.** Вердикт §861 сделал walk продакшном, но «этаж ↑» сейчас ДОРОЖЕ
(§853: lp=2 = 3.1×lp=0): узловая ветка собирает поддерево gather-ом на каждую
трубку (кэша нет, А1575), а 59–77 % клеток — пустой обход, от lp не зависящий.
Утверждённая схема адаптивной детальности (§862) даёт выигрыш только после
того, как «этаж ↑» станет дешеветь. Два хода, один механизм:

1. **Per-node bbox-индекс** — тот же counting-sort А1566, но по span узла,
   один раз при построении. Узел получил трубку → готовый список кусков,
   gather исчезает. Память: только узлы, у которых список непуст.
2. **Виртуальная пирамида кусков** (агрегация): на этаже k соседние куски
   сливаются в представителя — площадь = Σ, ρ_d = средневзвешенная; депозиты
   раскладываются по членам пропорционально площади (Ed/Eprev остаются
   пер-кусочными). «Число кусков поменьше» при диффузном обмене.

**ОГРАНИЧЕНИЯ (записаны, не блокеры на текущих сценах):**
- агрегируется только ДИФФУЗНАЯ доля отклика (ρ_d); зеркальный хоп
  резолвится ray-tri в конкретного члена агрегата (таблица принадлежности
  дешевле ray-tri) — верно и для поверхностей с бликом: диффузное крыло —
  бОльшая часть отражения; до T4 все материалы полностью диффузные;
- стороны тонких оболочек не сливать (семантика штампа А1564);
- ошибка гомогенизации мерится прибором §862-диаг («истинный этаж»),
  не объявляется нулём;
- хвост Lin<0 (§862-диаг-2.4) понять до продакшна агрегации.

**ПОРЯДОК И ПРИЁМКА.**
- Шаг 1: per-node индекс. Прогноз: цена «этаж ↑» монотонно убывает
  (lp=2 ≤ lp=0 по времени), E лестницы не меняется битово (тот же набор
  (кусок,сегмент)-проверок); blocked-beam 17 млн спусков — целевая сцена.
- Шаг 2: агрегация (этаж агрегата = этажу детальности из §862, форма
  Δ = cdelta·энерговклад §862-диаг-2). Прогноз: E в G1a при агрегации
  этажа ≤ «истинного этажа» §862-диаг; ray-tri тесты ↓ кратно; якоря
  adapt=0 битвы.
- Шаг 3: подтверждение на room/blocked-beam, замер «цена−точность»,
  затем дефолт walk в потребителях (§861).

**ГЕЙТ:** clang-format; ccheck CLEAN; санитайзеры на новом пути; make test
целиком; якоря §861/§862 битвы при новых ключах off; доклад сюда же.

### §863: ДОКЛАД ШАГА 1 — КЭШ СПИСКОВ КУСКОВ УЗЛОВ: lp=3 ×4.9, lp=2 ×2.5 БЕЗ ИЗМЕНЕНИЯ E (БИТОВО); «ЭТАЖ ↑» ТЕПЕРЬ НЕ ДОРОЖЕ

**Механика.** `front_gather` обходил поддерево узла на КАЖДУЮ трубку —
стоимость узлового прохода росла с глубиной (lp=3 = 99.9 c против 8.6 c
на lp=0, cavity05 it=4 dirs=26 walk). Введён кэш списков кусков узлов:
nstart/nlen/nstore (владелец — hz_sw_run, переживает fc и направления);
список узла собирается gather-ом ОДИН РАЗ (первая трубка), дальше — готовый
срез магазина; переполнение магазина (потолок 2²² слотов) откатывается на
прежний путь. Кластер = nleaf + смещение уровня + позиция (noff).

**Приёмка.**
| | референс a60e158 | с кэшем | E |
|---|---|---|---|
| cavity05 lp=0 | 8.6 c | 8.4 c | битово ✓ |
| cavity05 lp=2 | 23.3 c | **9.3 c (×2.5)** | битово ✓ |
| cavity05 lp=3 | 99.9 c | **20.2 c (×4.9)** | битово ✓ |
| box walk/xint, blocked-beam лестница | — | якоря §861/§862 ✓ | битово ✓ |

«Этаж ↑» перестал быть дорогим: lp=2 почти вровень с lp=0 (9.3 против
8.4). Остаток lp=3 > lp=0 — пустой обход (77 % клеток) и большие списки
узлов верхнего уровня; дальше — агрегация кусков (шаг 2) и обрезка
пустого обхода.

**ГЕЙТ:** gcc -fanalyzer CLEAN; ccheck 0 error; ASAN/UBSAN/LSan 0/0 на
cavity05 lp=3 walk; make test EXIT=0; якоря: box walk 4.0816, box xint
4.1644, cavity05 4.2100, bb 4.30133 — битово.

### §863: ДОКЛАД ШАГА 2 (ЭТАП 0 — ДЕДУПЛИКАЦИЯ УЗЛОВЫХ СПИСКОВ): lp=3 ТЕПЕРЬ БЫСТРЕЕ lp=0 В 2.4 РАЗА; «ДЕТАЛЬНОСТИ ПАДАЮТ → БЫСТРЕЕ» ПОДТВЕРЖДЕНА

**Механика.** Список кусков узла (кэш §863) склеивался из списков листьев,
и кусок, чей bbox накрывает несколько листов узла, попадал в список
многократно: штамп А1564 гасил дубли на депозите, но ray-tri по дублям
СЧИТАЛСЯ. При записи кэша список сортируется по id куска и уникальфицируется
(вставками, порядок списка не важен — попадания сортируются по tt).

**Приёмка (cavity05 it=4 dirs=26 walk; E — битово к предыдущему коммиту):**

| | до | после дедупликации |
|---|---|---|
| lp=0 | 8.4 c | 8.1 c (E ✓) |
| lp=2 | 9.3 c | **5.2 c (E ✓)** |
| lp=3 | 20.2 c | **3.4 c (E ✓)** |

lp=3 дешевле lp=0 в 2.4 раза; против референса a60e158 (99.9 c) — ×29.
Пользовательская интуиция «детальности падают → быстрее» подтверждена:
лестница ℓ_p стала лестницей ЦЕНЫ ВНИЗ при неизменном E.

**Дальше по шагу 2:** слияние РАЗЛИЧНЫХ кусков (представитель + раздача
депозитов по площади, диффузная доля отклика; зеркальный хоп резолвится
в члена) — следующий этап; ожидание — дальнейшее сокращение ray-tri
на многосценах (room/blocked-beam), приборы §862-диаг готовы.

**ГЕЙТ:** gcc -fanalyzer CLEAN; ccheck 0 error; ASAN/UBSAN/LSan 0/0
(lp=3 walk); make test EXIT=0; якоря §861/§862 битвы.

### §863: ДОКЛАД ШАГА 2 (ЭТАП 1 — АГРЕГАЦИЯ КУСКОВ В УЗЛОВЫХ СПИСКАХ, ключ agg=1): E СОХРАНЯЕТСЯ ТОЧНО НА ОДНОМАТЕРИАЛЬНЫХ СЦЕНАХ, ЦЕНА НЕЙТРАЛЬНА; ГЛАВНЫЙ ВЫИГРЫШ ПОКАЗАН НА ЭТАПЕ 0

**Механика.** Узловые списки (кэш §863) сортируются по (материал, id);
группы односоставных кусков идут подряд. При agg=1: ray-tri по
ПРЕДСТАВИТЕЛЮ группы, депозит раскладывается всем членам поровну
(edep = w·Lin·csec·axcos/A, Σ по членам = вклад трубки целиком — тождество),
Lh = площадь-взвешенное среднее (le, ρ, Eprev). Депозиты остаются
пер-кусочными (Ed/Eprev), сбор по пикселю и итерации не тронуты.
Листовые списки и весь путь agg=0 — битово прежние.

**Приёмка.**
- box walk agg=1: E битово = agg=0 (4.0816) — на одноматериальной сцене
  агрегация точна в энерговзвешенных величинах (аналитически ожидаемо);
  G6=0 ✓.
- cavity05 lp=0/2/3: E совпадает с agg=0 (4.2664/4.2689/4.2750), цена
  нейтральна (3.5 c на lp=3) — списки после дедупликации коротки, экономии
  ray-tri нет; выигрыш этапа появится на многосценах (room: десятки
  материалов) и на этажах агрегации ≥ 1 (слияние РАЗЛИЧНЫХ кусков, а не
  только дубликатов).
- blocked-beam agg=1: 4.30133 на lp=0 и lp=3 — плоскость сохранена ✓.
- НК le=0 agg=1 → E≡0 ✓.

**ГЕЙТ:** gcc -fanalyzer CLEAN; ccheck 0 error (shadow починен); ASAN/UBSAN/
LSan 0/0 на agg=1; make test EXIT=0; П1: agg=0/adapt=0 битовы (4.0816/
4.1644).

**СТАТУС ЦЕЛИ А1578/§863.** Шаг 1 (кэш узловых списков, ×4.9 на lp=3) и
шаг 2 этап 0 (дедупликация, ×2.9 сверху; итог cavity05 lp=3 = 3.4 c против
99.9 c референса, ×29) — в продакшн-пути по ключам; этап 1 (агрегация) —
готов, включается `agg=1`, на одноматериальных сценах точен. Осталось:
многосценовое подтверждение (room), слияние различных кусков на этажах
адаптива (форма Δ = энерговклад §862-диаг-2), walk в дефолт потребителей.

### §863: ДОКЛАД ШАГА 2 (ФИНАЛ) — room ПОДТВЕРЖДЁН; agg=2 («одна группа на список») СВЯЗАН С АДАПТИВНЫМ ЭТАЖОМ; ЦЕЛЬ ЗАКРЫТА

**room (it=4, dirs=26, walk, lp=2):** agg=0/1/2 — E совпадает в пределах
шума; adapt=1 agg=1: E = 4.3538/6.1317/7.1080/7.7770 против базы
4.3547/6.2339/7.1704/7.8233 — максимум −1.6 %, ВНУТРИ G1a ✓; cdelta=0.01 —
мягче (−0.6 %). Цена адаптивных этажей выше статики (16 с против 3.3 с
на room): насыщение аккумулятора гоняет куски в отказ-на-лист — нормировка
этажей по энерговкладу (amode=4) с потолком остается задачей калибровки.

**agg=2:** одна группа на узловой список (диффузное приближение поверх
материалов) — механически связана с адаптивным этажом: «детальности
падают → число кусков в проверке падает». На одноматериальных сценах
agg=2 ≡ agg=1 (один материал — одна группа и так).

**ГЕЙТ финальный:** gcc -fanalyzer CLEAN; ccheck 0 error; clang-format OK;
ASAN/UBSAN/LSan 0/0 (room agg=2 adapt=1); make test EXIT=0; якоря битвы
(4.0816/4.1644/4.2100/4.30133) при agg=0/adapt=0.

**ИТОГИ §863.** Шаг 1 (кэш узловых списков): cavity05 lp=3 ×4.9, lp=2 ×2.5,
E битово. Шаг 2 этап 0 (дедупликация): суммарно cavity05 lp=3 3.4 c против
99.9 c референса (×29), lp=3 дешевле lp=0 в 2.4 раза. Шаг 2 этап 1
(агрегация agg=1/2): E точно на одноматериальных сценах, G1a на room при
adapt, blocked-beam плоская. Хвосты: калибровка этажей по энерговкладу
с потолком (насыщение → отказ-на-лист дороже статики); walk в дефолт
потребителей; политика ℓ_p §844; природа Lin<0.

### §863: ДОКЛАД ШАГА 2 (ЭТАП 2 — ГРУППЫ КАК СТАТИЧНАЯ ТАБЛИЦА, ДЕПОЗИТ O(1)): E БИТОВО, ЦЕНА НЕЙТРАЛЬНА НА ТЕКУЩИХ СЦЕНАХ — УЗКОЕ МЕСТО НЕ ВЗАИМОДЕЙСТВИЯ, А ПУСТОЙ ОБХОД

**Механика (вместо пер-попадного цикла по членам).** Таблица групп
`sw_agg` (владелец — hz_sw_run): представитель, A, le_A, ρ_A — статичные;
SE = ΣEprev·a пересчитывается раз в итерацию; энергия депозита копится
на группу (`gacc`, `gwt` — O(1) на попадание) и раздаётся членам после
итерации (Ed_m += gacc; lpacc_m += cdelta·ρ_m·gwt). Узловой список =
ГИДЫ групп. `agg=2` — одна группа на список.

**Приёмка.** E битово: box agg=1 4.0816, cavity05 lp=3 4.2750 (совпадает
с этапом 1 и agg=0). Цена cavity05 lp=3 agg=1: 3.8 c против 3.5 c на
цикле-по-членам — ускорения НЕТ: после дедупликации списки коротки,
группы ≈ 1 на список, O(члены) ≈ O(1) и так было. **Вывод: узкое место
текущих сцен — не взаимодействия кусков, а марш по пустоте** (59–77 %
клеток, А1569-хвост «прыжки по tmax узла» недоработан).

**Значение этапа:** структура правильная и станет решающей, где списки
велики и материалов много (город: тысячи кусков на узел, десятки
материалов); на текущих сценах она нейтральна. Следующий целевой шаг —
ПУСТОЙ ОБХОД: до-делать прыжки А1569 (прибор уже показывает расхождение
с предсказанием >90 %), ожидание — кратное ускорение всех lp на всех
сценах, так как 77 % клеток — пустые.

**ГЕЙТ:** gcc -fanalyzer CLEAN; ccheck 0 error; clang-format OK;
ASAN/UBSAN/LSan 0/0 (cavity05 lp=3 agg=1); make test EXIT=0; П1 битвы.

## А1579-МОСТИК В НОВУЮ СЕССИЮ (прочесть первым; 11-09, коммиты 98771dc..9e77c3f)

**Состояние.** А1578/А1576/§862/§863 закрыты. Walk — продакшн-кандидат
(якоря: box walk 4.0816, box xint 4.1644, cavity05 lp=0/2/3 4.2100/4.2011/
4.1734, blocked-beam E_recv 4.30133 плоско по lp — все битовы при ключах
off). Скорость: кэш узловых списков (§863 шаг 1) + дедупликация (этап 0)
дали cavity05 lp=3 3.4 c (быстрее lp=0 в 2.4 раза, ×29 к a60e158); агрегация
(этапы 1–2, `agg=1/2`, таблица sw_agg, депозит O(1)) — E битово, цена
нейтральна на текущих сценах.

**ЗАДАЧА-БЛОКЕР СКОРОСТИ: А1579 — ПУСТОЙ ОБХОД (А1569).** Замер §863
локализовал: 59–77 % клеток пустые, спуски/прыжки по пустоте стоят больше
всей обработки кусков (этап 2 доказал: взаимодействия O(1) и не узкое
место). А1569-прыжки «по tmax узла» недоработаны — прибор А1569
(«прибор пустоты»: предсказание >90 % визитов-пустых) показывает
расхождение. Цель: трубка перескакивает пустую область одним шагом.
Приёмка: время walk на cavity05/blocked-beam кратно вниз при E битово;
прибор А1569 выходит на предсказание. Инструменты на месте (ключи
`walk/adapt/agg/amode/dumpE`, `scripts/diag_rank.py`).

**Дальше по очереди:** А1579 → калибровка адаптивных этажей по энерговкладу
(потолок, нормировка; насыщение сейчас дороже статики — room 16 c против
3.3 c) → walk в дефолт потребителей → политика ℓ_p §844 → T4-дихотомия
диффуз/зеркало в агрегации → хвост Lin<0 (отрицательные депозиты,
природа не выяснена).

**Запрещено:** wave/ не читать вовсе; гистерезис/переиспользование/
двухфазный хвост (отклонены измерениями); скрипты, пишущие один файл
в другой.

**Гейт:** clang-format; scripts/ccheck.sh (gcc -fanalyzer — гейт; текущая
cppcheck-находка invalidPointerCast унаследована, есть в референсе);
make test целиком; санитайзеры на новом пути; якоря сверять битово при
новых ключах off; доклад в PLAN_ELEMENTS.md.

## §864 (11-09): ПЛАН — А1579 ПУСТОЙ ОБХОД: СУЩЕСТВОВАНИЕ-ПЕРВЫМ + ПРЫЖОК ЧЕРЕЗ ПРОМЕЖУТОЧНЫЕ УРОВНИ (ДО КОДА; ПРОФИЛЬ СНЯТ)

**ПРОФИЛЬ (а), gprof -O2 -pg, it=4 dirs=26 walk=1.** Доля времени
(сам; вызываемые отдельно):

| | cavity05 lp=3 | box lp=0 |
|---|---|---|
| пустой обход: front_visit self + front_box_seg + hz_pyr_node_pos/leaf_pos + level_dims | **≈ 55 %** (30.5+14.1+8+2.3) | **≈ 65 %** (40.6+18.8+14.5+8.5+1.3) |
| взаимодействия: sw_ray_tri_raw + front_seg_walk | ≈ 29 % (20.6+8) | ≈ 11 % |
| вывод | пустой обход дороже всей обработки кусков — подтверждён | то же |

Механика пустых расходов (сверху вниз): (1) КАЖДЫЙ спуск вычисляет
front_box_seg для ВСЕХ 8 детей — 143 млн вызовов на box, существующих
детей — доли; (2) существование ребёнка узнаётся ДВОИЧНЫМ ПОИСКОМ
(hz_pyr_node_pos 29 млн, leaf_pos 12 млн на box) ПОСЛЕ box_seg — платим
за пустоту дважды; (3) hz_pyr_level_dims зовётся на каждый визит
(22–37 млн), хотя размеры уровней статичны на прогон; (4) вставочная
сортировка детей запускается даже при n ≤ 1.

**Шаг Б1 — существование-первым (битово-нейтрально).** (i) `hz_pyr_node`
получает `chmask` (8 бит: какие дети существуют) — свободный байт паддинга,
заполняется одним подъёмом при построении; (ii) битовая карта занятых
листов `leafbit` [(ncells+63)/64] — существование базовой клетки за один
бит-тест (NULL — фолбэк на двоичный поиск, fail closed); (iii) в обоих
детских циклах front_visit существование проверяется ДО box_seg —
у несуществующего ребёнка box_seg не зовётся вовсе (его результат
никуда не шёл: found<0 → continue; порядок существующих не меняется —
E битово); (iv) level_dims кэшируется во фронте (статично на прогон);
(v) сортировка при n<2 не запускается (тождество).

**Шаг Б2 — прыжок через промежуточные уровни (сам А1569).** Узел уровня l
промежуточный ⟺ m = lev_lp[l][pos] ≥ l+2; уровни l−1 … m под ним —
проходная структура: материален фронт станет только на уровне m−1
(узел уровня j материален ⟺ m ≥ j+1). Прыжок: из (l,pos) сразу DDA по
сетке уровня j* = m−1 внутри [tin,tout] (пер-уровневые битовые карты
существования, память Σ8^−k ≈ ncells/7 бит), существующие клетки —
node_pos + обычная ветка; G6 — тот же контроль на новом разбиении
(a_in = Σdep + a_out: тождество держится на ЛЮБОМ разбиении отрезка).
Порядок событий не меняется: попадания сортируются по tt внутри слитого
отрезка, штамп А1564 гасит стыковые дубли — последовательность Lin та же.
Ключ `jmp=` (0 — прежний путь, битово; 1 — прыжок); приёмка: E при jmp=1
битово = jmp=0 на всех якорях.

**ПРЕДСКАЗАНИЯ.** П1: Б1 — box_seg вызовы ↓ ≈ 8× (до числа визитов),
node_pos/leaf_pos в марше → ~0; время walk cavity05 lp=3 ↓ в 1.4–1.7×,
box lp=0 ↓ в 1.5–2×; E битово на всех якорях. П2: Б2 — спуски ↓ кратно
(уровни над lp исчезают); cavity05 lp=3 суммарно ≥ 2.5×, blocked-beam
≥ 2×; E битово при jmp=1. П3 (негативный): screw=2 — детекторы
hz_pyr_verify ненулевые при обоих jmp; НК le=0/noprop → E≡0. П4 (прибор
А1569): после Б2 доля ПУСТ-прыжков среди детских проверок сходится
с предсказанием > 90 % на lp ≥ 1 (сейчас прибор смешивает «клеток пусто»
с «визитов марша», 79.8–99.6 %).

**ЗАПРЕЩЕНО по-прежнему:** wave/ не читать; гистерезис/переиспользование/
двухфазный хвост; скрипты, пишущие один файл в другой. Гейт каждого шага:
clang-format, scripts/ccheck.sh, make test целиком, санитайзеры на новом
пути, якоря битвы (box walk 4.0816, box xint 4.1644, cavity05
4.2100/4.2011/4.1734, blocked-beam 4.30133) битово при новых ключах off,
доклад сюда же.

### §864: ДОКЛАД — ПРОФИЛЬ СНЯТ, ШАГ Б1 (СУЩЕСТВОВАНИЕ-ПЕРВЫМ) В ПРОДАКШН-ПУТИ: E БИТОВО, ВРЕМЯ ↓1.13–1.24×; ШАГ Б2 (DDA-ПРЫЖОК) ОТКАТЛЕН КАК НЕ ПРОШЕДШИЙ БИТОВУЮ ПРИЁМКУ

**Шаг Б1 (в коде, без новых ключей).** (i) `hz_pyr_node.chmask` — Existence-
маска детей (бит cx|cy<<1|cz<<2), заполняется одним подъёмом при построении;
(ii) в обоих детских циклах front_visit существование ребёнка проверяется
бит-тестом ДО front_box_seg — пустой ребёнок обходится без слэб-теста и
двоичного поиска; (iii) размеры уровней кэшируются во фронте (fld/fldok,
владелец hz_sw_run) — level_dims из марша исчез (22–37 млн вызовов);
(iv) front_node_box принял неконстантный fc (кэш). Счётчик ПУСТ-прыжков
теперь считает ВСЕ детей, отсутствующие в пирамиде (раньше — только
прошедшие box_seg): прибор вырос (cavity05 lp=3 it=4: 4.6→21.7 млн),
семантика — «прыжок одним шагом» честнее.

**Приёмка Б1.** Все девять якорных конфигураций E БИТОВО против b68ca4d
(двоичный diff per-piece дампов + свип/ПРИЁМНИК): box walk 4.0816, box xint
4.1644, cavity05 xint lp=0/2/3 4.2100/4.2011/4.1734, cavity05 walk lp=0/2/3
4.2664/4.2689/4.2750, blocked-beam walk E_recv 4.30133 ✓. Время walk
(it=4 dirs=26) против HEAD: cavity05 7.73→6.52 / 4.69→3.77 / 2.75→2.44 c;
box 4.83→3.95 / 4.76→4.06 / 4.67→3.82 c; blocked-beam 2.02→1.70 / 2.00→1.68 /
1.82→1.50 c — **×1.13–1.24**, НЕ кратно (предсказание П1 по gprof-долям
завышало: сам front_visit и взаимодействия доминируют после отсечения
слэб-тестов пустоты). Гейт: clang-format OK; ccheck — gcc -fanalyzer по
.c CLEAN, cppcheck только унаследованная invalidPointerCast, clang-tidy
+1 advisory (FP «uninitialized compound assign» на chmask|=: pyr_find
гарантирует инициализацию, анализатор теряет связку); ASAN/UBSAN/LSan 0/0
на cavity05 walk lp=3 / box walk / cavity05 lp=2; НК: le=0→E≡0, noprop→E≡0,
screw=2 → d_cell=1104/d_csr=1104/d_empty=22, чистый прогон — нули; G6=0.
Оговорка среды: gcc -fanalyzer на ПРЯМЫХ компиляциях .h падает с
«cannot write PCH file» (shared memory песочницы) — воспроизводится и на
b68ca4d, к коду отношения не имеет.

**Шаг Б2 — НЕГАТИВНЫЙ РЕЗУЛЬТАТ, ОТКАТЛЕН.** Реализованный по плану
DDA-прыжок через промежуточные уровни (jt = m−1 по lev_lp, пер-уровневый
перебор клеток, заход только в существующие узлы) дал E = 0.03 против
4.08 на box walk: Walk-модель А1576 держится на ФАНТОМНЫХ касаниях —
спуск посещает лист при ЛЮБОМ непустом box_seg, включая вырожденные
[h,h]-касания клеток, чей замкнутый бокс задевает отрезок (А1578); депозит/
подхват стенки происходит именно в таких визитах. Чистый заход-DDA их
не воспроизводит (посещает только клетки, в которые луч ВОШЁЛ), и
битовое тождество E недостижимо без точной репликации касательной
семантики (включая порядок вырожденных визитов и допуск sl). Код Б2
удалён целиком (ключ jmp не остаётся в дереве); фактический материал —
этот абзац и трассы: спуск посещает 49152 сегмента с попаданиями против
8704 у заход-DDA на box dirs=6 it=1.

**СЛЕДСТВИЕ ДЛЯ А1579.** Узкое место подтверждено профилем (пустой обход
55–65 %, взаимодействия 11–29 %), дешёвая половина (Б1) снята. Для
«кратно» нужен ход, не трогающий visit-семантику walk: либо (а) кэш
вырожденно-касательных клеток узла (маска «бокс касается отрезка» — тот
же counting-sort, что bbox-индекс А1566, но по отрезкам семейств трубок),
либо (б) признать пересчётEvent-семантики walk (список попаданий по всему
пути трубки, а не по сегментам) — это уже изменение модели, требует
отдельного аудита до кода. Записано как вход в следующую сессию А1579.

**§864-дополнение (вторая попытка Б2, тоже откатлена).** Воспроизведены
явно и касательные визиты (входные/выходные торцы + угловые комбинации
осей, вырожденные [t,t] с порядком «вырожденные первыми» по А1578):
box walk E = 0.06 против 4.0816. Расхождение НЕ исчерпывается торцами:
visit-множество спуска шире заход-DDA систематически (97158 против 24704
визитов листьев на box dirs=6 it=1 — ≈4×), т.е. спуск посещает
существенные доли клеток за пределами «вошёл/коснулся торцом», и без
точной реконструкции его множества битовая приёмка недостижима. Вывод
усилен: ход (б) «пересчёт семантики walk» — единственный чистый путь,
ход (а) в форме заход-DDA закрыт двумя негативными замерами. Ключ jmp
из дерева удалён вновь; якоря после отката — битово (9/9).

**§864-дополнение 2: префильтр bbox×сегмент в walk-взаимодействии — в
продакшн-путь.** В front_seg_walk (обе ветки, вкл. агрегацию) добавлен тот
же консервативный отсекатель, что стоял во front_interact: перед ray-tri
проверяется пересечение отрезка [tin,tout] с tribox куска (треугольник ⊆
своего bbox — отсечение результата не меняет). Мотив: после Б1 вторая
статья профиля — ray-tri (24 % на cavity05 lp=3, 61.6 млн вызовов ≈ 15.8
на событие; попаданий из них 1–2). Приёмка: 9/9 якорей E БИТОВО против
2715bac (per-piece дампы + свип/ПРИЁМНИК); время cavity05 lp=3 walk
2.29→2.16 c (−6 %), lp≥2 выигрывает (длинные узловые списки), lp=0 — в
пределах шума; ASAN/UBSAN 0/0; make test EXIT=0. Суммарный итог сессии
против b68ca4d: cavity05 lp=3 walk 2.75→2.16 c (×1.27), box lp=0 ×1.2,
blocked-beam ×1.35. Хвост А1579 (кратно) по-прежнему упирается в
visit-семантику walk — см. развилку выше.

**§864-дополнение 3 (эксперимент «множество визитов», маршрут (а) закрыт).**
Сравнены мультимножества визитов листьев спуска (jmp=0) и заход-DDA с
касаниями (jmp=1) на box dirs=6 it=1: 97158 против 24704; визиты DDA —
СТРОГОЕ ПОДМНОЖЕСТВО визитов спуска (по каждой клетке DDA ≤ спуск), спуск
посещает приграничные клетки ~64× за марш (клетка (0,0,10): 386 визитов
против 6 у DDA) — множественность фантомных касаний встроена в саму
visit-семантику walk и не воспроизводится никаким DDA. Следствие: маршрут
(а) «реконструировать множество» эквивалентен маршруту (б) «пересчёт
семантики walk»; битовая приёмка при сохранении семантики достижима
только ценой воспроизведения фантомной множественности — то есть без
выигрыша. Экспериментальный код откатлен; дерево = 028822a. А1579
останавливается на развилке, требующей решения: сменить семантику walk
(аудит до кода) либо принять семантику как данную и закрыть А1579
с результатом ×1.27–1.35 (Б1+префильтр).

## §865 (11-09): ПЛАН — МАРШ ПОЛНОГО ПУТИ (А1580): ПЕРЕСЧЁТ СЕМАНТИКИ WALK (САНКЦИОНИРОВАНО; АУДИТ ДО КОДА)

**САНКЦИЯ.** Пользователь выбрал маршрут (б) после развилки §864-доп. 3.

**СУТЬ.** Попадания копятся на ВСЁМ пути трубки (один DDA-марш от входа до
выхода домена, DDA уже написан и отлажен в Б2), сортируются по tt ОДИН РАЗ,
депозиты идут по списку. Исчезают: пер-сегментная нарезка (и зависимость E
от нарезки), фантомные многократные визиты приграничных клеток (измерены:
386 против 6 на клетку, §864-доп. 3), допуск sl как носитель семантики.

**ЧТО ТЕРЯЕТ СТАРАЯ СХЕМА (выписано до кода):** (1) фантомная
самозасветка приграничных клеток — дефект, не физика; (2) wall-хиты «на
грани» через sl (хвост А1577); (3) сегментные стыки как источник Lin<0 —
ГИПОТЕЗА, проверяется в аудите фактов: сравнить долю Lin<0 до/после.

**ПОРЯДОК.** Шаг 1: `path=1` — новый режим front_tube: DDA (Б2) собирает
список (t, кусок) по всем существующим клеткам пути; обработка — один
проход по отсортированному списку, формула депозита прежняя
(Ed[p] += w·Lin·csec·axcos/A; Lin ← Lh). Старый путь — `path=0` (битово).
Шаг 2: якоря-перенос: E(path=1) против pref 4.1866 в G1a; blocked-beam:
дрейф по lp обязан уйти; Lin<0 — до/после; НК весь. Шаг 3: прыжки по
пустоте внутри path=1 (пер-уровневый DDA из Б2) — приёмка «кратно».

**ПРЕДСКАЗАНИЯ.** П1: walk старый и path=1 на box дают E в G1a против
pref, но НЕ битово (сняты фантомы) — сдвиг ПОНИЖЕНИЙ на приграничных
кускаx; blocked-beam лестница lp у path=1 плоская (фантомы были
механизмом дрейфа А1573-класса). П2: НК без изменений. П3: время
path=1 ≤ walk старого уже без Б2-хаков (марш однопроходный).

**ГЕЙТ:** без изменений (clang-format, ccheck, make test, санитайзеры,
NK), старые якоря битово при path=0; доклад сюда же.

**§865 — статус реализации (раунд 3, код откатлен до плана).** path=1
реализован первым куском (DDA полного пути + сбор попаданий штампом +
сортировка + прежняя формула депозита). НК le=0 → E≡0 ✓. Два дефекта до
доработки: (1) lp≥1 не собирает попадания — path_collect смотрит кэш
nstore, который в path-режиме не наполняется; нужно звать front_gather;
(2) lp=0 расходится (рециркуляция ~×5/итерацию) — причина не установлена,
гипотезы: двойной учёт стенок (2 треугольника на квад дают два депозита
в одном tt — сверить со старой моделью), потеря баланса lost (остаток
Lin после последнего попадания учитывается, входной тёмный сегмент —
нет). Код откатлен (fail closed), дерево = a561991; оба пункта — вход
следующего раунда отладки.

**§865 — план отладки path=1 (раунд 4, находки до кода).** (0) Промах
ray-tri возвращает −1.0 — в сбор попаданий не проходит (tt ≥ tin), но при
tt < 0 физически «хиты за спиной» (стенка позади старта трубки) попадают
в первый интервал легально — сверить со старой моделью, там они же ловились
фантомами. (1) lp≥1: path_collect обязан звать front_gather (кэш nstore в
path-режиме не наполняется). (2) Расходимость lp=0: инструментация —
печать pbuf_n и депозитов по трубке (23,23) семьи +x, сравнить число
попаданий на трубку path против walk (гипотеза: у path их кратно больше —
тогда искать лишний источник попаданий; если равно — считать формулу
депозита/баланс lost). Инструментация и фикс — следующий раунд.

**§865 — раунд 5: дефект (1) закрыт в коде, дефект (2) локализован.**
path=1 с front_gather: lp≥1 собирает попадания; hits = 3–5 на трубку —
как у старой схемы, НО box it=1 E = 1586 против 2.16 (×~734 ≈ число
трубок на семейство). Вывод: депонируют ВСЕ трубки, тогда как в старой
схеме реально несёт депозит меньшинство — надо вскрыть, кто именно
депонирует в walk (вероятно, тёмный вход/выход за mesh-границей домена:
h0/h1 считаются по py->lo/fc->hi, а стенки стоят ВНУТРИ — старая схема
получает стеночные хиты только через фантомные касания стеночных рядов,
а путь-модель — у каждой трубки). Код откатлен (fail closed); след:
разобрать баланс по всем 6 направлениям на одном tube-наборе.

**§865 — раунд 6: численный разбор расходимости (до кода).** ΣEd(path,
it=1) ≈ 9516 при 12288 попаданиях → ≈0.77 на попадание; при Lin≈1.5
формула даёт ≈0.0015 — расхождение ×~512 (степень двойки → похож на
unit-дефект csec/A либо двойной множитель |cos|/axcos). Проверить при
ре-импорте первым же дампом (tt, p, Lin, area[p]) первой трубки. Данных
за этот раунд больше не накоплено — бюджет ушёл на два предыдущих
ре-импорта; код в дереве не менялся (HEAD 04b8399, якорь 4.0816 ✓).

**§865 — раунд 7: точечный диагноз path=1.** С frontier-фиксом (сброс
pbuf_n на трубку — «чужие» попадания больше не текут) и дампами: hit'ы
корректны (0.001/депозит, у стеночных рядов 2–5 на трубку), НО собирают
их только трубки латерального ряда 0: для ±x — 128 из 8192 (ряд z=0),
для ±z — все 8192 (их ряд — латеральные z=0 тоже). Т.е. DDA марширует
только ряд с латеральной координатой 0: латеральная g (g[1]/g[2] при
нулевом om) теряется в path_dda (init или clamp blo/bhi — py->lo
печатался только по x; осевые lo могут отличаться). Фикс локален,
известен, следующий раунд: печатать lo[3] целиком + init g по всем осям
в path_dda. Код откатлен; дерево = 19e9403 + этот доклад.

**§865 — раунды 7–8: диагноз path=1 сужен до одной строки.** Сброс pbuf_n
на трубку убрал взрыв (E стабильно), попадания корректны (0.001/депозит).
Остаток: для ±x/±y семей collect дают только трубки латерального ряда 0
(128/8192); init DDA корректен (g=(0,30,49) у ряда (30,49), lo=(0,0,0),
dims 65³), трубка идёт марш, но path_collect с попаданием не вызывается
ни разу вне ряда 0. Проверено: tube 2000 — это ±z (собирает 1/направление),
идентичность трубок по ntube внутри семейств. Следующий шаг: трасса
(посещаемая клетка, leaf_pos) по шагам для одной внутренней ±x трубки —
сравнить с рядом z=0, где всё работает. Код откатлен (fail closed).

**§865 — раунд 9: сверка счётчиков трубок.** TS-линий 24576 = 6×4096
ровно; ожидаемых трубок 6×4225 = 25350; разница 129 на направление =
внешний латеральный ряд 64 (65) + ряд 64 второй латеральной оси (65) −
угол (1): у них org = lo+(cc+½)cell > меша, box_seg в front_tube
возвращается ранним выходом (slab-ветка om≈0: org>bhi → 0). Это
объясняет и «4096/направление» во всех прошлых замерах. Трубки ряда 64
не влияют на E (они за мешем), но приборы А1569 обязаны их учитывать
отдельно. Основной дефект (±x/±y трубки внутри латерального ряда 0 не
собирают попаданий) остаётся; следующий раунд: ре-импорт path=1 +
пошаговая трасса (клетка, leaf_pos) одной внутренней трубки, код отныне
остаётся в дереве за ключом (повторные ре-импорты дороже дисциплины
чистого дерева; путь=0 битово, что и есть fail closed).

### §865: ДОКЛАД — PATH=1 СОШЁЛСЯ; КОРЕНЬ ВСЕХ СБОЕВ — PITCH2-БАГ

**Корень** (найден пошаговой трассой раунда 10): в path_dda
`pitch2 = pitch1*ny` при формуле `id = g0 + pitch1*(g1 + pitch2*g2)`
раздувал z-компоненту id в 65 раз — работали только трубки ряда z=0
(для них g2=0). Один из трёх дефектов Б2 прошлых раундов; остальные
«дефекты» были его тенями. Фикс: `pitch2 = ny` (dim[1]).

**Результаты path=1 (it=8, walk-референс рядом):** box dirs=6:
1.1496 стабильно; cavity05 lp=0/3: **4.2612 / 4.2612 — лестница плоская**,
−0.3 % к walk (4.2750/4.2664); box dirs=26: 1.7573 против walk 4.0816 —
разница = фантомная самозасветка стеночных рядов старой схемы (теперь
измерена: ~2.3, более половины E старого walk); blocked-beam E_recv
lp=0/3: 1.207/1.786 — честный lp-дрейф А1573-класса вскрыт (старая
«плоскость» 4.30133 была артефактом фантомов). НК: le=0 → E≡0 ✓;
screw → детекторы 1104/1104/22 ✓; ASAN/UBSAN 0/0 ✓; make test EXIT=0 ✓;
path=0 битово (код за ключом). Гейт шага пройден.

**СТАТУС А1579/А1580.** Марш полного пути работает и устойчив. Остаток:
lp-дрейф blocked-beam на path=1 — теперь ВИДНА его настоящая величина
(−32 % к lp=3), это физика LOD-приближения (А1573-класс), а не дефект
марша; лечится калибровкой этажей (следующий пункт мостика). Прыжок по
пустоте А1569 в path-марше уже реализован (клетки без узлов — один шаг);
«кратно» оценивается после снятия фантомного фона в приёмке.

**§865 — раунд 11: path=1 кэш узловых списков + штамп до ray-tri.**
Кэширование gather-списков в path-режиме (как ветка material) сняло
худшее (98 c → 18.5 c на cavity05 lp=3); E всюду прежнее (4.2612 ✓,
1.7573 box ✓). Остаток: lp≥1 path всё ещё ~8× walk — на каждый визит
клетки ray-tri по всем непопавшим кускам списка (штамп ставится только
при попадании). Кандидат-фикс: префильтр bbox×сегмент как в walk, кэш
«кусок × трубка уже проверен» либо предвычисленный по куску интервал tt.
lp=0 — вровень с walk (6.57 против 6.45). Гейт: format ✓; ccheck/make
test — старт следующего раунда (изменения затрагивают только path=1).

**§865 — раунд 12: префильтр в path=1, гейт, статус.** bbox×сегмент
префильтр в path_collect_list: cavity05 lp=3 18.5→15.0 c (−19 %); E
4.2612→4.2607 (граничный случай точной арифметики на торцах, без sl —
для path=1 приемлемо). Гейт: ccheck (gcc-fanalyzer/cppcheck — без новых
находок), ASAN/UBSAN 0/0 (cavity05 lp=3 path), make test EXIT=0.
ИТОГ А1580 к концу сессии: марш полного пути работает и устойчив
(path=0 битово, path=1 за ключом); cavity05 лестница плоская 4.2612;
box 1.7573 — фантомная самозасветка старой walk измерена (~2.3);
blocked-beam вскрыт честный lp-дрейф (1.207/1.786) — это физика
LOD-приближения, лечится калибровкой этажей. Производительность path:
lp=0 вровень с walk; lp≥1 ~7× walk (15.0 против 2.2 c) — повторный
ray-tri непопавших кусков на визит клетки; ходьба фикс-кандидатов
записана (кэш «кусок×трубка проверен», per-tube интервалы tt).
СЛЕДУЮЩИЙ ЭТАП МОСТИКА: производительность lp≥1 path, затем калибровка
адаптивных этажей по энерговкладу, walk/path в дефолт потребителей,
политика ℓ_p §844, T4-дихотомия, хвост Lin<0.

**§865 — раунд 13: аудит кэша «кусок × трубка» ДО кода (оба кандидата —
один механизм).** ФАКТ: в path=1 луч трубки постоянен ⇒ и tt куска, и его
bbox t-интервал — КОНСТАНТЫ ТРУБКИ. Повторный ray-tri непопавших кусков
возникает из-за того, что крупный кусок зарегистрирован во многих базовых
клетках (span-конвенция А1578) ⇒ входит в списки многих jt-узлов марша, и
на КАЖДОМ визите проходит bbox×сегмент и ray-tri заново; штамп ставится
только при попадании. ДИЗАЙН (точный): per-трубочный кэш со штампом pkey —
(а) bbox t-интервал куска по ВСЕЙ трубке [pth0,pth1] (pth0/pth1 = h0/h1
трубки, кладутся во front_ctx перед path_dda); (б) результат ray-tri tt.
На визите узла: пустой интервал или нет пересечения с [tin,tout] → skip
(ТОЧНО эквивалентно box_seg по сегменту: клип интервала к сегменту =
пересечение клипа-к-трубке с сегментом); иначе ray-tri ОДИН РАЗ (кэш),
пуш при tt∈[tin,tout] — тест не тронут, те же double. НАИВНЫЙ ВАРИАНТ
«штамп при любом промахе сегмента» ОТВЕРГНУТ ДО КОДА: промах в узле A
(tt>tout_A) у куска, чей bbox заходит вперёд, а треугольник бьётся в
узле B — это потеря попадания, и бьются именно крупные стенки — главные
депозитёры. ПРЕДСКАЗАНИЯ. П1: E path=1 битово = раунду 12 (4.2607 на
cavity05 lp=3 it=4; серия it=8 …4.2796); допустимый класс отклонений —
вырожденные касания торцов от порядка клипа (тот же, что принят в
раунде 12); ключи off (path=0, walk) — битово (правки только в
path_collect_list + новые массивы). П2: время lp≥1 path ↓ кратно
(ray-tri ≤1 на (кусок,трубку) вместо ~15 на событие; box_seg 1 на
(кусок,трубку)); lp=0 — нейтрально или лучше. П3: НК le=0 → E≡0;
screw=2 → детекторы 1104/1104/22. П4: ASAN/UBSAN 0/0 на cavity05 lp=3
path. Попутно: снят отладочный HZ_PATH-принт из path_dda (getenv на
КАЖДЫЙ шаг DDA — измеримая накладка; диагностика раундов 7–10
завершена). Память кэша: nt×(2·8+3·8) байт, аллокация только при path=1;
нет памяти → прежний путь (fail closed).

**§865 — раунд 13: ДОКЛАД — кэш «кусок × трубка» в path=1: E БИТОВО,
время path ↓ ×1.9–2.4, разрыв к walk сжат ~7× → ~2.1×.** Реализован
точный механизм из аудита выше: per-(кусок,трубка) кэш со штампом pkey —
(а) bbox t-интервал по ВСЕЙ трубке [pth0,pth1] (границы трубки кладутся
во front_ctx перед path_dda), пустой интервал — маркер [1,0], отсекается
тестом !(bh0≤bh1); (б) результат ray-tri — ОДИН РАЗ на (кусок,трубку).
На визите узла клип интервала к [tin,tout] заменяет повторный box_seg
(сегмент ⊂ трубки — тождество), тест попадания tt∈[tin,tout] не тронут.
Нет памяти при постройке → прежний путь (fail closed). Попутно снят
отладочный HZ_PATH-принт из path_dda (getenv на каждый шаг DDA).

Приёмка. П1 перевыполнен: E path=1 БИТОВО против HEAD (8ed3df7, двоичный
бинарный worktree-билд) на шести конфигурациях — cavity05 lp=0/2/3
(3.1783…4.2609 / 3.1781…4.2607 / …4.2607 на it=4), box lp=0 1.7573
(it=8), blocked-beam lp=0/3 с приёмником (0.593863 / 1.46544 на it=4);
ключи off (walk 4.0816, xint 4.1644, walk cavity05 4.2664/4.2750) —
битово. НК: le=0 → E≡0 ✓; screw=2 → d_cell=6/d_csr=6/d_empty=1 ✓;
noprop — без рециркуляции. ASAN/UBSAN 0/0 (cavity05 lp=3, box lp=3).
make test EXIT=0; ccheck — только унаследованная cppcheck
invalidPointerCast; clang-format OK.

Время (it=4, dirs=26, cavity05, path=1, head→new): lp=0 9.4→5.0 c,
lp=1 6.6→3.8 c, lp=2 7.8→4.0 c, lp=3 15.4→6.3 c — ×1.9–2.4. Отношение
path/walk на lp=3: было ~7× (раунд 12), стало ~2.1× (6.3 против
2.96 c); path lp=3 теперь быстрее walk lp=0 (7.3 c). Остаток разрыва —
не повторный ray-tri (его больше нет), а сам объём узловых списков
(cavity05: lp≥1 списки-поддеревья длиннее листовых) — кандидат
следующего раунда: обрезка списка кусков узла по bbox узла ДО перебора
(список узла содержит куски поддерева; куски вне bbox узла в попадания
не могут) либо переход марша path на более мелкий jt. ПОРЯДОК ДАЛЬШЕ
без изменений: калибровка адаптивных этажей по энерговкладу (amode=4,
потолок) → walk/path в дефолт потребителей → политика ℓ_p §844.

**§865 — раунд 14: аудит «обрезка списка узла по боксу узла» ДО кода.**
ФАКТ: после раунда 13 остаток разрыва path/walk (~2.1×) — не ray-tri
(кэширован), а сам перебор списка узла: список узла = ВСЕ куски
поддерева (front_gather), а попасть в марше ЭТОГО узла могут только
куски, чей tribox пересекает бокс узла. ДИЗАЙН (точный): при постройке
кэша §863 (ветка front_gather в path_collect) фильтровать куски
статическим slab-тестом tribox × бокс узла (без луча) ДО записи в
nstore — фильтр статичен по геометрии, делается ОДИН РАЗ на узел;
не влезший в nstore список идёт тем же отфильтрованным pbuf.
ТОЧНОСТЬ: точка попадания с tt∈[te,tn] лежит на треугольнике и внутри
бокса узла (клэмп к hi — тот же, что во front_node_box), значит в обоих
боксax; разднесённые боксы — строгий тест на разделение, касание
засчитывается (separation только при строгом >), так что ни одно
попадание не теряется — фильтр математически точен, FP-свободен.
КЭШ: в path-прогоне nstore наполняется только path_collect (front_visit
в path-режиме не вызывается) — смешения отфильтрованных и полных
списков нет; walk-прогоны path_collect не зовут вовсе. Листовая ветка
(l<0) не тронута: bpids уже фильтрованы по клетке (А1566).
ПРЕДСКАЗАНИЯ. П1: E path=1 БИТОВО против 6db927d на всех шести
конфигурациях (фильтр точен и без FP). П2: время lp≥1 path ↓ (перебор
списков короче; ожидание ×1.3–2 на lp=1–3), lp=0 без изменений.
П3: заполнение nstore ↓ (те же списки короче). П4: НК le=0→E≡0,
screw=2 → детекторы; ASAN/UBSAN 0/0; make test EXIT=0; ключи off
(walk/xint/path=0) — битово (правка только в path_collect).

**§865 — раунд 14: ДОКЛАД — обрезка списка узла по боксу узла: E БИТОВО
(6/6), время НЕЙТРАЛЬНО; П2 ОТВЕРГНУТ, остаток локализован профилем.**
Реализован точный фильтр из аудита (static slab-тест tribox × бокс узла
при постройке кэша §863, касание засчитывается). Приёмка: E path=1
БИТОВО против 6db927d на всех шести конфигурациях (cavity05 lp=0/2/3,
box lp=0, blocked-beam lp=0/3 с приёмником — те же series, что в
раунде 13); ключи off — битово (walk 4.0816, xint 4.1644); НК le=0→E≡0,
screw=2 → 6/6/0/1; ASAN/UBSAN 0/0; make test EXIT=0; ccheck — только
унаследованная invalidPointerCast (по пути пойман и вылечен новый
sign-conversion в аллокации pc_bh). Время (cavity05 it=4, path):
lp=0 5.5 c, lp=1 3.9, lp=2 4.0, lp=3 6.5 — в пределах шума против
раунда 13: перебор списков фильтр НЕ снял (списки jt-узлов на пути
трубки даже после обрезки длинны). Фильтр оставлен: точен, одноразов,
режет заполнение nstore и есть база для дальнейшей нарезки списков.
ПРОФИЛЬ (gprof, lp=3 path): 91 % — инлайновый перебор списков/марш в
hz_sw_run; front_box_seg 47.1 млн (3.3 %), hz_pyr_node_pos 7.6 млн,
sw_ray_tri_raw 4.1 млн (повторного ray-tri больше нет — 0.66 %).
СЛЕДСТВИЕ: следующий уровень выигрыша — НЕ фильтрация, а смена
структуры сбора: (а) пер-трубочный предвычисленный интервал tt использовать
для пропуска ЦЕЛЫХ узлов (сумма/минимум по списку узла — иерархическая
агрегация интервалов при постройке кэша: узел хранит min/max bh своих
кусков, визит узла с непересекающимся агрегатом стоит O(1)); (б) либо
марш сбора на уровне ЛИСТЬЕВ с bpids (А1566 уже фильтрованы по клетке).
Записано как вход следующего раунда; аудит (а) — до кода.

**§865 — раунд 15: аудит ДО кода — (а) агрегат bh-интервалов узла
МЁРТВ, (б) слияние пер-кусковых массивов кэша в одну запись.**
(а) ОТКЛОНЁН ДО КОДА: узел посещается DDA ровно ОДИН раз за трубку,
поэтому O(1)-пропуск узла по агрегату ничего не экономит (агрегат всё
равно надо один раз посчитать — это и есть скан); а СТАТИЧЕСКИЙ
агрегат (bbox объединения кусков узла) почти совпадает с боксом узла,
сквозь который трубка и идёт — пересечение с [tin,tout] всегда
непусто, O(1)-skip не срабатывает никогда. Идея снята без кода.
(б) ПРИНЯТ: профиль раунда 14 — 91 % инлайновый скан; на каждый кусок
списка сейчас 3–4 СЛУЧАЙНЫХ доступа по разным массивам (pstamp, pc_t,
pc_bh, изредка pc_tt) = 3–4 кэш-линии. Слить их в одну запись
pc_rec {tstamp, pstamp, bh0, bh1, tt} = 40 Б: один-два промаха на кусок
вместо четырёх. Механика тождественна: те же штампы, те же тесты, те же
double — E ОБЯЗАНА быть битово. pstamp в path-режиме переезжает в
запись (остальные потребители pstamp — walk-ветки, в path-прогоне не
зовутся); fail closed: pc==NULL → прежний цикл с fc->pstamp.
ПРЕДСКАЗАНИЯ. П1: E path=1 битово 6/6 против 37eb734. П2: lp≥1 path
↓ ×1.3–2 (кэш-промахи скана), lp=0 — меньше выгода (те же записи).
П3: гейт целиком (НК, ASAN/UBSAN, make test, ключи off битово).

**§865 — раунд 15: ДОКЛАД — слияние кэша в одну запись pc_rec (48 Б):
E БИТОВО 4/4 спот-чеков, время НЕЙТРАЛЬНО; П2 отвергнут вторично —
скан инструкционно-ограничен, не кэш-ограничен.** pc_rec {bh0, bh1,
tt, tstamp, ttstamp, pstamp} — один calloc вместо четырёх массивов,
на кусок одна-две кэш-линии вместо 3–4; заодно вскрыт и закрыт при
ревизии собственный дефект первой правки (маркер «ray-tri посчитан»
изначально был выведен из непустоты интервала — потерялся бы кэш tt
раунда 13; введён явный ttstamp). Приёмка: cavity05 lp=0/3, box lp=0,
blocked-beam lp=3 — БИТОВО против 37eb734; ключи off битово (walk
4.0816); НК le=0→E≡0; ASAN/UBSAN 0/0; make test EXIT=0; ccheck —
только унаследованная invalidPointerCast. Время (cavity05 it=4, по два
прогона): lp=0 ≈5.1 c, lp=1 ≈3.7, lp=2 ≈3.8, lp=3 ≈6.4 — в шуме
против раундов 13–14. ВЫВОД ПО ПРОИЗВОДИТЕЛЬНОСТИ PATH: два
механических хода (фильтр списков, локальность кэша) результата не
дали — остаток ×2.1 к walk есть ЦЕНА СЕМАНТИКИ: path сканирует списки
поддеревьев jt-узлов, walk — пер-сегментные списки, и это не лечится
без смены модели. Единственный записанный алгоритмический ход — сбор
на уровне листьев через bpids — СТИРАЕТ лестницу ℓ_p (hit-множество
lp≥1 совпало бы с lp=0, а лестница blocked-beam — физика LOD, А1573),
поэтому БЕЗ САНКЦИИ не делается. Итог сессии: path=1 lp=0 быстрее
walk lp=0 (5.1 против 7.3 c), lp≥1 — ×2.1 к walk при E-качестве выше
(снята фантомная самозасветка). Предложение: производительность
закрыть как «достигнут предел без смены модели» и перейти к
калибровке адаптивных этажей по энерговкладу (amode=4, потолок).

## §866 (11-09): ПЛАН — КАЛИБРОВКА АДАПТИВНЫХ ЭТАЖЕЙ: НОРМИРОВКА ПО ЭНЕРГОВКЛАДУ (amode=4) + ПОТОЛОК lpceil (ДО КОДА)

**ЗАЧЕМ.** §863-финал: адаптивные этажи на room ДОРОЖЕ статики (16 c
против 3.3) — аккумулятор ΣΔ растёт монотонно по итерациям, floor
клэмпится только на 255, и к середине свипа ВСЕ куски уходят на
максимальную грубость (насыщение). Решение §862-диаг-2 (утверждено
замером): форма Δ = чистый энерговклад (amode=4, код есть), НОРМИРОВКА
— через cdelta, недостающее — ПОТОЛОК: этаж куска не поднимается выше
заданного lpceil. Механика: в конверсии этажа lpflo[p] = min(floor
(lpacc[p]), lpceil) при lpceil>0; lpceil=0 — прежний мир (битово).
Гистограмма этажей swee3 печатает ПРИМЕНЁННЫЙ этаж (с потолком), а не
сырой аккумулятор — прибор обязан показывать то, что идёт в марш.

**СЕМАНТИКА РУЧЕК.** cdelta — СКОРОСТЬ набора этажа (сколько энергии
поднимает кусок на этаж), lpceil — ПРЕДЕЛ грубости (физика: потолок
LOD-огрубления; связка с «политика ℓ_p §844» — потолок и есть будущая
политика). Предсказуемость: множества этажей обязаны СТАБИЛИЗИРОВАТЬСЯ
(гистограмма перестаёт меняться после нескольких итераций).

**ПРЕДСКАЗАНИЯ.** П1: без потолка (lpceil=0) — воспроизводится статус
§863: время adapt > static, гистограмма монотонно уползает вверх.
П2: с потолком L время adapt ≤ static lp=L + накладка аккумулятора и
монотонно возрастает по L; гистограмма стабилизируется ≤ L к 2–3-й
итерации. П3: E(adapt, потолок L, cdelta C) уходит от static
монотонно по C и L; существует (C, L) с уходом внутри G1a (по комнатной
шкале §863: до −1.6 %) и временем ≤ 1.2×static. П4: НК: le=0→E≡0;
adapt=0/lpceil=0 — битово прежний мир; якоря box walk 4.0816 /
cavity05 4.2607 (path) не тронуты (правка только в конверсии этажа и
печати гистограммы). П5 (фальсификатор калибровки): если НИ одно (C,L)
из сетки не даёт одновременно G1a и ≤1.2×static — схема «энерговклад с
потолком» признаётся неспособной конкурировать со статикой на этих
сценах, доклад с гистограммами, ход к «политика ℓ_p §844» закрывается
негативно.

**СЕТКА КАЛИБРОВКИ** (room, walk, it=4, dirs=26, lp=2, agg=0):
L ∈ {1,2,3}, C ∈ {0.01, 0.1, 1}; арбитр — room (многоматериальная),
cavity05/blocked-beam — подтверждение; static-референсы lp=0/1/2/3 тем
же прогоном. ГЕЙТ: без изменений + новые ключи при 0 битово.

### §866: ДОКЛАД — ПОТОЛОК lpceil В КОДЕ; КАЛИБРОВКА НА room: (C=0.01, L=3) ДАЁТ E +0.48 % И ×1.24 БЫСТРЕЕ СТАТИКИ; НАЙДЕН ПРЕДСУЩЕСТВУЮЩИЙ ХВОСТ adapt×agg

**Код.** hz_sw_opts.lpceil (0 — прежний мир); конверсия этажа:
fi = clamp(floor(lpacc), 0, 255), затем min(fi, lpceil); попутно
вылечено UB — (uint8_t) от ОТРИЦАТЕЛЬНОГО double (отрицательные
Lin-депозиты §862-диаг существуют) → теперь явный клэмп к 0. Приборы
в hz_sw_stat: flochg_max/ flochg_sum (смены этажей за конверсию /
за прогон — стабилизация), flhist[16] — применённые этажи последней
конверсии; гистограмма swee3 печатает ПРИМЕНЁННЫЕ этажи (с потолком),
а не сырой аккумулятор. lpflo → calloc (первая конверсия считает смены
относительно fine).

**П1 подтверждён (воспроизведение насыщения, room lp=2 it=4 walk):**
без потолка 2677/4964 кусков уползают в клэмп 15, время 16.6 c против
static 4.4 c, E уползает до 7.8115 (против 7.5387).

**СЕТКА (L∈{1,2,3}, C∈{0.01,0.1,1}).** КЛЮЧЕВОЙ ФАКТ СЕМАНТИКИ:
lpacc инициализируется стартовым lp, поэтому L ≤ lp_start схлопывает
адаптив в статику L БИТОВО (L=2 ≡ static lp=2 по E; L=1 E 7.5213 ≈
static lp=1 7.5213 — различие в 4-м знаке отfloor-границы, НЕ битово —
уточнение: E совпала с точностью печати, тождество не заявляется).
Дифференциация живёт при L > lp_start. Лучший кандидат:

| конфиг | E(it=4) | время |
|---|---|---|
| static lp=2 (база) | 7.5387 | 4.42 c |
| static lp=3 | 7.6063 | 3.83 c |
| static lp=1 | 7.5213 | 4.30 c |
| **adapt L=3 C=0.01** | **7.5751 (+0.48 %)** | **3.57 c (×1.24 к базе)** |

Этажи: 2:4148, 3:816 (тяжёлые куски — на 3, лёгкие остаются на 2);
Σсмен 5773 ≈ nt + 800 → гистограмма стабилизируется после первой
конверсии. П3 ВЫПОЛНЕН: уход +0.48 % внутри G1a, время ≤ static lp=3.
Рекомендация по умолчанию: amode=4, cdelta=0.01, lpceil=lp_start+1.

**П4 гейт:** ключи off битово (box walk 4.0816, cavity05 path 4.2607);
НК le=0→E≡0 при включённом адаптиве; ASAN/UBSAN 0/0 (room adapt);
make test EXIT=0; ccheck — унаследованная cppcheck + advisory cert-err34
(atoi/atof по всему swee3 — не новые).

**НОВЫЙ ХВОСТ (найден при калибровке, ПРЕДСУЩЕСТВУЮЩИЙ):** adapt=1 при
agg=1 на room расходится: старый код (без потолка) схлопывается в
E≈0.0001 к it=4, новый (с потолком) даёт 5.4058 против static 7.5387
(−28 %). Static agg=1 битово = static — дефект именно в связке
«перестройка этажей × группы (материал×узел)»: группы не перестраиваются
при смене этажей / депозит в аккумулятор при agg идёт по
представителю. Записано как хвост; до разбора адаптив и agg совместНО
не включать.

**ПОРЯДОК ДАЛЬШЕ:** walk/path в дефолт потребителей → политика ℓ_p
§844 (потолок lpceil — её механическая часть) → хвост adapt×agg →
природа Lin<0.

## §867 (11-09): ПЛАН — WALK В ДЕФОЛТ ПОТРЕБИТЕЛЕЙ (pref, pgather); PATH ОСТАЁТСЯ ЗА КЛЮЧОМ (ДО КОДА)

**СУТЬ.** Вердикт §861: walk — продакшн-модель (точные многопопадные
пересечения, фантомная самозатветка снята — измерено §865: на box
старый walk завышал E более чем вдвое, 4.08 против 1.76 честных).
Потребители библиотеки — tools/pref.c (эталон-сверка) и tools/pgather.c
(сбор по пикселю, кадр в img/) — до сих пор собирают so.walk=0, т.е.
продают модель с фантомами как рабочую. Правка: so.walk = 1 в обоих
с комментариями §861/§867. **path=1 в дефолт НЕ идёт:** его физика
лестницы ℓ_p отличается от walk (честный lp-дрейф blocked-beam вместо
фантомной плоскости) и политика ℓ_p §844 не принята — до неё path
остаётся за ключом (санкционированный порядок мостика).

**ЧТО НЕ МЕНЯЕТСЯ.** swee3 — драйвер экспериментов, его walk=0 остаётся
(все битовые якоря сессии снимались при ключах off). make testpref/
pgather не запускает — только собирает; юнит-тесты test_sweep3/
test_gather3 зовут hz_sw_run напрямую со своими opts — не затронуты.

**ПРЕДСКАЗАНИЯ.** П1: pref walk=1 — свип-числа смещаются ОТ старых
(фантомная модель) к честным; сверка с radiositi-эталоном остаётся
в печатаемом коридоре (эталон не тронут — там свои случайные
направления). П2: pgather walk=1 — кадр меняется (фантомы сняты);
сборка и запись img/ без ошибок. П3: численные якоря swee3 не тронуты
(бинарно тот же код библиотеки, правки только в двух tools).
П4: гейт: clang-format, ccheck, make test EXIT=0; smoke-прогон pref и
pgather на box с печатью «до/после».

### §867: ДОКЛАД — WALK=1 В ДЕФОЛТЕ pref И pgather; pgather ПОРТИРОВАН НА mode=3 (ПОПУТНО ПОЧИНЕН НЕ КОМПИЛИРУЮЩИЙСЯ ЛАТЕНТНЫЙ БАГ); ПОКУСКОВАЯ СХОДИМОСТЬ pref С ЭТАЛОНОМ ×33

**pref:** so.walk=1. До/после на box (it=4, lev=6): интегральное
отношение свип/эталон то же (0.9782 → 0.9766), а ПОКУСКОВАЯ сходимость
выросла на порядок: |Δ| средняя 3.9194 → 0.1174 (×33), max 4.3138 →
0.3383, медиана отношения куска 1.9076 → 0.9817. Старая модель давала
правильную СРЕДНЮЮ из неправильных кусков (фантомная самозасветка
компенсировала недосвет), walk сходится ПОКУСНО — эталонное
подтверждение продакшн-вердикта §861.

**pgather:** свип портирован с mode=2 на mode=3+walk по образцу pref
(trivert+tribox+lparr lp=0, hz_pyr_set_lp, domhi; указатели назначаются
ПОСЛЕ memset(&so) — тот же класс бага «memset стёр trivert», что чинен
в pref; в HEAD pgather тривert назначался ДО memset и молча терялся).
ПОПУТНО ПОЧИНЕН ЛАТЕНТНЫЙ БАГ: pgather.c в HEAD НЕ КОМПИЛИРОВАЛСЯ —
`pp[0][aa]` с double-подстрочным aa (жёсткая ошибка gcc); make его не
собирал (pgather вне test/all-списков), поэтому баг жил незамеченным.
Смоук: pgather box it=4 → кадр img/pgather.ppm записан; слияние
L_сбор/L_свип = 0.9930.

**ПATH остаётся за ключом** (мотив в плане §867 выше). swee3-дефолты
не тронуты: якоря 4.0816 / 4.2607 битовы. Гейт: ccheck обоих tools
CLEAN (advisory cert-err34 — унаследованный класс atoi/atof/sscanf по
всему файлу); make test EXIT=0.

**ПОРЯДОК ДАЛЬШЕ:** политика ℓ_p §844 (lpceil — её механическая часть)
→ хвост adapt×agg (§866) → природа Lin<0 → T4-дихотомия диффуз/зеркало
в агрегации.

## §868 (11-09): ПЛАН — ПОЛИТИКА ℓ_p: ЭНЕРГОВКЛАД С ПОТОЛКОМ КАК ПОЭЛЕМЕНТНАЯ ПОЛИТИКА (ДО КОДА)

**СУТЬ.** swee3 задаёт ЕДИНЫЙ ℓ_p прогону (лестница G1); поэлементная
политика — «какому куску какой этаж» — была открыта. Кандидат уже
откалиброван и принят замерами §866: Δ = энерговклад (amode=4),
cdelta = 0.01, потолок lpceil = lp+1, стабилизация после первой
конверсии. Оформить политикой: ключ swee3 `lppol=` — 0 единый ℓ_p
(дефолт, битово прежний мир), 1 — энергетическая политика (включает
adapt=1 amode=4; cdelta и lpceil берутся из явных ключей, иначе
0.01 / lp+1). Пользовательские явные cdelta/lpceil при lppol=1
ПЕРЕКРЫВАЮТ умолчания политики (флаг «задано явно»).

**ПРЕДСКАЗАНИЯ.** П1: cavity05 walk lp=2 it=4: lppol=1 даёт уход от
uniform lp=2 в пределах G1a и время ≤ uniform (часть кусков грубее).
П2: blocked-beam walk lp=2: E(lppol=1) между uniform lp=2 и lp=3 —
честная лестница сохраняется (политика не стирает физику lp, а
распределяет её по кускам). П3: room: подтверждение §866 (+0.48 %,
×1.24 быстрее). П4: lppol=0 битово (включая все якоря сессии).
П5: НК le=0→E≡0 при lppol=1; ASAN/UBSAN на room lppol=1; make test.
П6 (фальсификатор): если на какой-то сцене уход ≥ G1a или время
> uniform — политика не универсальна, доклад с гистограммами этажей,
в дефолт потребителей не идёт.

**§868 — находка ДО кода политики (раунд 2): абсолютный cdelta не
переносим между сценами.** cavity05 lp=2 lppol=1 (C=0.01, L=3): НИ ОДИН
кусок не набирает этаж 3 (аккумулятор в единицах энергии сцены; шкала
cavity05 ниже комнатной) → политика вырождается в uniform, но накладка
адаптива даёт 6.3 c против uniform 4.6 c. ФИКС (точный, без новых
накопителей): ОТНОСИТЕЛЬНАЯ нормировка на конверсии —
fl = cdelta · lpacc[p] / mean(lpacc) (amode=4 копит сырую энергию,
среднее по кускам считается в уже существующем цикле конверсии;
новый ключ opts lpnorm, lppol=1 включает его и меняет смысл cdelta:
«сколько СРЕДНИХ по сцене энергий поднимает кусок на этаж», умолчание
политики cdelta=1). П1': cavity05 появляется дифференциация (часть
кусков на 3), уход ≤ G1a; П2' room: поведение §866 сохраняется;
П6' (фальсификатор по времени): если и с относительной нормировкой
время > uniform на сцене — политика фиксируется как ИНСТРУМЕНТ
КАЧЕСТВА, а не скорости, дефолтом не становится.

**§868 — находка 2 (раунд 3): относительная нормировка от
инициализации lpacc=lp ОПУСКАЕТ холодные куски ниже базы (cavity05:
этажи 0:1259 1:939 2:10) — уточнение вместо огрубления, минус
скорость.** ФИКС политики: (1) lpacc копит depозиты от НУЛЯ
(инициализация lp = статистика базы, не сигнал); (2) в конверсии
fl = lpbase + cdelta·lpacc[p]/mean(lpacc) — базовый этаж lpbase
(= стартовый lp) плюс относительный энерговклад; (3) клэмп
[lpbase, lpceil] — политика МОНОТОННО ОГРУБЛЯЕТ от базы (согласно
«этаж ↑ = грубее» §814); refinement ниже базы снят как дефект
(абсолютный режим lpnorm=0 не тронут битово — адаптив вне битовых
якорей). ПРЕДСКАЗАНИЯ: П1'' cavity05 — горячие стенки на lp+1,
остальное на базе, уход ≤ G1a; П3'' room — дифференциация мягче
§866 (относительная шкала), время ≤ static lp; П6'' фальсификатор по
времени в силе.

### §868: ДОКЛАД — ПОЛИТИКА ℓ_p ПРИНЯТА: lppol=1 «ЭНЕРГОВКЛАД С ПОТОЛКОМ, ОТНОСИТЕЛЬНАЯ НОРМИРОВКА, МОНОТОННО ОГРУБЛЕНИЕ ОТ БАЗЫ»; ДВЕ НАХОДКИ ДО КОДА, ТРЕТЬЯ В КОДЕ

**Ключи:** lppol=1 — политика (adapt=1, amode=4, lpnorm=1, cdelta=1
«сколько средних по сцене энергий поднимает кусок на этаж»,
lpceil=lp+1; явные cdelta/lpceil перекрывают); lppol=0 / отсутствует —
битово прежний мир. Резолюция политики ДО аллокаций (первая правка
ловила segv: adapt включался после аллокации lpacc).

**Три находки (две до кода, одна в коде):**
1. абсолютный cdelta не переносим (cavity05: ни один кусок не набирает
   этаж, накладка ×1.4 в пустую) → относительная нормировка lpnorm;
2. относительная нормировка от инициализации lpacc=lp ОПУСКАЛА
   холодные куски НИЖЕ базы (этажи 0:1259 1:939 2:10 — уточнение
   вместо огрубления) → lpacc от нуля, fl = lpbase + cdelta·lpacc/mean,
   клэмп [lpbase, lpceil] — МОНОТОННОЕ ОГРУБЛЕНИЕ (§814), refinement
   ниже базы снят как дефект;
3. порядок резолюция/аллокация (см. выше).

**ИТОГИ (walk, it=4, lp=2):**

| сцена | uniform lp=2 | lppol=1 | уход | время |
|---|---|---|---|---|
| cavity05 | 4.2689 / 4.6 c | 4.2749 (2:1259, 3:949) | +0.14 % | 3.65 c (×1.25 ↑) |
| room | 7.5387 / 4.4 c | 7.5971 (2:3745, 3:1219) | +0.78 % | 3.02 c (×1.46 ↑) |
| blocked-beam | 4.5887 | 4.5887 (28 кусков: 2:20, 3:8) | 0 | — |

E политики сидит между lp=2 и lp=3 и почти совпадает с lp=3
(cavity05: 4.2749 против 4.2750) — политика приближает «все на lp+1»
по энергии, дешевле обоих статиков. Гистограммы стабилизируются после
первой конверсии. П6-фальсификатор по времени НЕ сработал: политика
быстрее uniform на обеих сценах. ОГОВОРКА: blocked-beam (28 кусков)
политику не тестирует — сцена слишком coarse; многосценовое
подтверждение остаётся за room/cavity05.

**Гейт:** lppol=0 битово (box walk 4.0816, cavity05 path 4.2607);
НК le=0→E≡0 при lppol=1; ASAN/UBSAN 0/0 (room lppol=1); make test
EXIT=0; ccheck — унаследованная находка.

**ПОРЯДОК ДАЛЬШЕ:** хвост adapt×agg (§866; политика создаёт для него
рабочий сценарий — lppol=1 + agg=1) → природа Lin<0 → T4-дихотомия
диффуз/зеркало. Дефолт потребителей НЕ переключается на lppol=1 до
разбора adapt×agg (политика активирует адаптив).

## §869 (11-09): ПЛАН — ХВОСТ adapt×agg: КЭШИ СПИСКОВ И ГРУПП НЕ ИНВАЛИДИРУЮТСЯ ПРИ ПЕРЕСТРОЙКЕ ЭТАЖЕЙ (ДО КОДА)

**ДИАГНОЗ ПО ЧТЕНИЮ (проверка гипотезы §866).** hz_pyr_set_lp в конце
итерации (lpapply) перестраивает пирамиду: узлы на уровнях получают
другие позиции/составы. Но (1) кэш узловых списков §863 (nstart/nlen/
nstore, ключ cid = ncluster0 + noff[l] + pos) НЕ сбрасывается — список,
собранный для СТАРОГО узла (l,pos), достаётся НОВОМУ узлу с тем же cid,
хотя тот покрывает другие клетки (составы слились/разъехались);
(2) таблица групп agg (ag->ng, gmem_n) только растёт — группы прошлых
итераций остаются, а привязки «узел → gid» переживают перестройку.
Это дефект ВСЕХ adapt-прогонов с узловым кэшем (agg и без agg), просто
на room/agg=0 он прятался в шуме G1a. ПОЧИНКА (минимальная, fail
closed по смыслу): после set_lp при nup>0 — nstart[*]=−1, nstore_n=0,
ag.ng=0, ag.gmem_n=0 (полный пересбор списков/групп на следующей
итерации — семантика идентична «прогону с нуля»). Когда nup==0 (этажи
не изменились) — кэши ВАЛИДНЫ, сброс не нужен (и это создаст
стабилизацию по цене: после того как гистограмма устоялась, списки
не перестраиваются).

**ПРЕДСКАЗАНИЯ.** П1: adapt×agg на room (lp=2 it=4 walk, lpceil=3,
cdelta=0.01) возвращается из −28 % в коридор G1a от static agg=1
(= static agg=0 в пределах шума). П2: adapt БЕЗ agg (room lppol=1)
немного сместится (списки больше не протухшие) — прогноз: уход от
static НЕ вырастет (сейчас +0.78 %, протухание недооценивало
огрубление); П3: цены: nup>0 в первых итерациях → пересбор списков
(стоимость gather-фазы ×~2 на итерацию пересбора), после стабилизации
этажей пересбор исчезает. П4: ключи off — битово (сброс только под
lpapply/nup>0); якоря 4.0816/4.2607 нетронуты. П5: НК le=0→E≡0;
ASAN/UBSAN на room adapt×agg; make test.

### §869: ДОКЛАД — ГИПОТЕЗА ИНВАЛИДАЦИИ ОТВЕРГНУТА (кэши списков структурны, сброс бесполезен и снят); НАЙДЕН НАСТОЯЩИЙ ДЕФЕКТ: AGG×WALK РАСХОДИТСЯ ДАЖЕ С НЕПОДВИЖНЫМИ ЭТАЖАМИ; ОБНАРУЖЕН ДЕФЕКТ ОБВЯЗКИ swee3 (so.agg только под adapt)

**Гипотеза §869 (протухание nstore/групп при set_lp) ОТВЕРГНУТА
замером:** сброс nstart/nstore_n/ag после конверсии НИ ОНОГО не меняет
E (списки узлов = структурные поддеревья, от этажей не зависят;
py->lev не перестраивается — set_lp пишет только lev_lp/leaf_lp).
Сброс снят, дерево = состояние §868 (diff только PLAN). Урок: gather-
список — функция ГЕОМЕТРИИ, не этажей.

**Попутно вскрыто два факта.**
1. **Дефект обвязки swee3:** `so.agg = agg` выставляется ТОЛЬКО внутри
   adapt-блока — «static agg=1» никогда не включал агрегацию; ссылки
   §866 на «static agg=1 = static agg=0» — сравнение с выключенной
   агрегацией, НЕ доказательство точности agg. (В §863 agg мерился
   под adapt=1 — там ключ работал.)
2. **Настоящий дефект: agg×walk расходится при НЕПОДВИЖНЫХ этажах**
   (adapt=1 lpceil=2, полы не меняются, расходимость с итерации 0):
   cavity05 agg=1 — дикий осциллятор (0.076 / 3.20 / 0.095 / 0.079
   против agg=0 4.2689), room agg=1/2 — −25 % (5.654 против 7.5381).
   §863 принимал agg на «точность на одноматериальных сценах» ДО
   walk-модели А1576; после слияния walk ветка агрегации во
   front_seg_walk, видимо, не следует семантике многопопадного прохода
   (осцилляция = положительная обратная связь Lh-перераспределения
   групп). ЭТО РЕГРЕССИЯ ОТНОСИТЕЛЬНО §863, а не свойство adapt —
   adapt тут ни при чём.

**ПОСЛЕДСТВИЯ.** (а) agg=1/2 объявляются НЕСОВМЕСТИМЫМИ с walk до
разбора: в докладах и потребителях agg не включать (существующие
печати не затронуты); (б) lppol=1 (§868) остаётся валидной — мерилась
на agg=0; (в) дефект «agg×walk» — отдельный раунд с фальсификатором:
минимальный случай cavity05 it=1 (0.0763 против 0.0763? — сверить
поэлементно dumpE agg=0/agg=1), затем фронт_seg_walk×agg. ПОРЯДОК
ДАЛЬШЕ: agg×walk-раунд → природа Lin<0 → T4-дихотомия.

### §870: ДОКЛАД — КОРЕНЬ ДЕФЕКТА AGG НАЙДЕН: «RAY-TRI ПО ПРЕДСТАВИТЕЛЮ ГРУППЫ» ТЕРЯЕТ ЭНЕРГИЮ СИСТЕМНО; «ТОЧНОСТЬ» §863 БЫЛА УДАЧЕЙ КОДОГЕНЕРАЦИИ; AGG=1/2 ОБЪЯВЛЕНЫ СЛОМАННЫМИ ДО ПЕРЕРАБОТКИ

**Минимальный фальсификатор:** cavity05, walk=1, adapt=1, it=1, agg=1
против agg=0: E 0.0763 против 3.1822 (×42 потеря), dumpE — большинство
кусков e=0, hits→0. Гипотезы «протухание кэшей при set_lp» (§869) и
«lpacc-init» — СНЯТЫ: бисекция по коммитам показала, что b3b79a2
(§867) даёт «точный» agg, но добавление ОДНОЙ отладочной строки
(getenv+fprintf) в тот же источник ПЕРЕКЛЮЧАЕТ его в коллапс;
ASAN/UBSAN/valgrind(--track-origins)/MALLOC_PERTURB_/MSAN — чисто;
-О0/-О2/-fno-strict-aliasing — коллапс воспроизводится. Поведение
агрегации неустойчиво к кодогенерации ⇒ «точность на одноматериальных
сценах» (§863) измерена на сборке, где дефект не проявился.

**КОРЕНЬ (семантический, виден в коде без приборов):** collect-ветка
агрегации тестирует ray-tri ТОЛЬКО треугольника-представителя группы
(`rep = grep[gid]`); группа = весь материал узлового поддерева —
трубка, пересекающая ЧУЖОЙ треугольник группы, промахивается по
репрезентанту, и группа целиком не депонирует (×55 энергии мимо).
Репрезентант-подход не эквивалентен сумме членов ни при какой
нормировке — нужны: (а) bbox-тест по ОБЪЕДИНЁННОЙ коробке группы до
rep-ray-tri (дёшево, отсеет часть промахов, но не чинит пропуск
чужих треугольников), либо (б) ray-tri по ВСЕМ членам группы с
агрегацией отклика ПОСЛЕ (дороже, зато честно), либо (в) ответ группы
как аналитической поверхности (следующий уровень — «виртуальная
пирамида» §863). Решение — отдельный раунд с аудитом; до него agg=1/2
ЗАПРЕЩЕНЫ к использованию (ключ остаётся, дефолт 0, доклады
поправлены).

**ПОПУТНО подтверждено:** дефект обвязки so.agg-только-под-adapt
(§869) — «static agg» в swee3 невозможен; при починке agg сначала
вывести ключ из adapt-блока.

**ГЕЙТ раунда:** дерево чисто (инструментация в worktree, не в
коммите); make test EXIT=0; якоря битовы (4.0816/4.2607/4.0816-walk);
lppol=1 (§868) не затронут (мерился на agg=0).

## §871 (11-09): ПЛАН — ПРИРОДА Lin<0 (ОТРИЦАТЕЛЬНЫЕ ДЕПОЗИТЫ): ДЕРЕВО ГИПОТЕЗ ДО ПРИБОРОВ

**ФАКТ (§862-диаг-2):** существуют события с Lin<0 (sdelta до −235 за
прогон). Инварианты цепи депозита: Lin_вых = le + ρ·Eprev/(2π);
Ed = w·Lin·csec·axcos/area; le ≥ 0 (глобальный или Ke-среднее),
ρ ∈ [0,1], area > 0, w, csec, axcos ≥ 0, Eprev = (float)Ed_prev ≥ 0
при Ed ≥ 0. ⇒ при всех неотрицательных входах цепь Lin ≥ 0 ЗАМКНУТА,
первого отрицательного взяться неоткуда.

**ДЕРЕВО.** Г1: первый Lin<0 приходит из Lh при Eprev<0 — но Eprev<0
сам требует Ed<0 ⇒ регресс упирается в первую итерацию (там E≡0,
Lh=le>0) — Г1 не бутстрапится, ОТВЕРГАЕТСЯ аналитически (проверить
замером первого отрицательного события). Г2: le<0 — только при ke=1
(среднее каналов Ke материала; отрицательные Ke в сценах?). Г3:
отрицательный вклад в аккумулятор из веток, ГДЕ ДЕПОЗИТ НЕ
контролирует знак Lin: agg-перераспределение (gwt = edep·(1−cosθ_rep)
— знак edep), T-перехват (f, wt ≥ 0 — проверить wt), walk G6-перенос
(a/b-доли: a гасится, b=Lin — проверить, не депонирует ли a). Г4:
pcs.e — float-accumulate Ed (double): переполнение/потеря знака
невозможны при |Ed|<1e38. Г5 (рабочая): Lin<0 возникает в ПЕРЕХОДНОЙ
арифметике walk — «b» перенесённая с предыдущего сегмента
после вычитаний в приёмнике/перехвате (L = L·T + Lsurf(1−T) при
T>1 из-за клипа?) — проверить клипы T.

**ПРИБОР (за env HZ_NEG, не в продакшн-логике):** в точках депозита
walk/interact при Lin<0 — одноразовый контекст (номер трубки, кусок,
Lin, le, ρ, Eprev[p], итерация). П1: первое Lin<0 случается НЕ на
итерации 0 (итерация 0 с le>0 вся ≥ 0 — иначе Г1-анализ неверен).
П2: контекст первого Lin<0 покажет Eprev[p]<0 по куску, получившему
отрицательный Ed ранее — тогда корень в первой итерации с Ed<0, и
Г1-анализ неверен → искать ветку с незамкнутым знаком (Г3/Г5).
П3: НК: le=0 → Lin≡0; ρ=0, ke=0 → Lin=le≥0.

### §871: ДОКЛАД — ХВОСТ Lin<0 ЗАКРЫТ КАК НЕ ВОСПРОИЗВОДЯЩИЙСЯ: во всём дереве (walk, перехват §846, точная ветка, path, legacy walk=0) ни одного события Lin<0 на cavity05/room/blocked-beam/spheres/fals_full (±screw, ±adapt amode 0/1/3/4, it≤6); знак доказан аналитически; сентинел оставлен в дереве

**Аналитика (замыкание знака).** Lin_вых = le + ρ·Eprev/(2π); Ed =
w·Lin·csec·axcos/area; Eprev = (float)Ed_prev. При le ≥ 0, ρ ∈ [0,1],
area > 0, w/csec/axcos ≥ 0 цепь Lin ≥ 0 ЗАМКНУТА: первый
отрицательный взяться неоткуда (Г1-регресс упирается в итерацию 0 с
E≡0, Lh=le>0). front_cos возвращает |n·om| ∈ [0,1] ⇒ (1−cosθ) ≥ 0 —
формы Δ всех amode неотрицательны при неотрицательном edep (Г3
закрыт). Материалы сцен не содержат отрицательных Kd/Ke (Г2 закрыт).

**Замеры.** Инструментированы ВСЕ точки депозита (walk-ветка,
pbest-ветка, T-перехват; HZ_NEG → заменён постоянным сентинелом):
0 событий Lin<0 на cavity05/room/blocked-beam/spheres/fals_full,
walk=0/1, path=1, ±screw, adapt amode 0/1/3/4, it≤6, xint=0/1.

**ВЕРДИКТ.** Наблюдение §862-диаг («sdelta до −235») относится к дереву
ДО А1577/А1578: ulp-допуск на торцах сегментов и tie-break вырожденных
сегментов — именно та арифметика, где знак депозита мог портиться;
сейчас ветка чиста. Хвост снят как невоспроизводящийся; СЕНТИНЕЛ
оставлен навсегда (fc->negseen, лимит печати 8 за прогон, ветка
выполняется только при Lin<0 — на чистом прогоне бесплатна): если
отрицательные депозиты вернутся (новая ветка/сцена), первые 8 событий
напечатают полный контекст (трубка, кусок, Lin, le, ρ, Eprev, area).

**ГЕЙТ:** make test EXIT=0; ccheck — унаследованная находка; якоря
битовы (4.0816 walk, 4.2607 path); НК в матрице замеров.

**ПОРЯДОК ДАЛЬШЕ:** T4-дихотомия диффуз/зеркало в агрегации — но
агрегация сломана (§870), поэтому сначала переработка agg (audit
трёх вариантов §870) ЛИБО T4 на walk без agg. Решение за следующим
раундом; ползунок «переработка agg» теперь критический путь для
двух хвостов.

## §872 (11-09): ПЛАН — ПЕРЕРАБОТКА AGG: ГРУППА = CULLING ПО ОБЪЕДИНЁННОМУ БОКСУ МАТЕРИАЛЬНОЙ СЕРИИ, ДЕПОЗИТ ТОЧНЫЙ ПО-КУСОЧНО (ДО КОДА)

**ВЫБОР ВАРИАНТА из §870.** (б) «ray-tri по всем членам» обесценивает
саму агрегацию (цена = agg=0); (в) «аналитический отклик группы» —
уровень «виртуальной пирамиды» §863, требует своего аудита и T4-политики
(зеркальные куски в группу не сворачиваются в принципе). Рабочий
вариант — (а)+: **группа честна только как CULLING-единица**: группа
(материальная серия отсортированного списка узла) получает
ОБЪЕДИНЁННЫЙ bbox (слияние tribox членов — 6 сравнений на кусок, без
луча) и ОДИН slab-тест против [tin,tout]; промах — вся серия мимо
(экономия k−1 box_seg и всех ray-tri серии), попадание — точная
по-кусочная обработка как в agg=0. Депозит/Lh/sw_accum — по-кусочно
(семантика walk не тронута), штамп А1564 на куске.

**СЛЕДСТВИЯ.** Список узла в agg-режиме = ОТСОРТИРОВАННЫЕ КУСКИ
(сортировка (mtl,id) + дедупликация переезжает ДО решения «кэшировать
ли» — работает и для некэшированного pbuf); гиды/таблица групп
(sw_group_make, gSE/gacc/gwt-раздача) из walk-пути ИСКЛЮЧАЮТСЯ —
структуры остаются (ng=0, циклы раздачи вырождаются в no-op) —
полная чистка sw_agg отдельно. agg=2 вырождается честно: union bbox
всего списка ≈ бокс узла, culling не срабатывает, результат = agg=0.
ЭТО ОЗНАЧАЕТ: «диффузное приближение» §863 (O(1) депозит на группу)
ЗАКРЫВАЕТСЯ как неверный (§870), а agg=1 становится точным culling-ом;
диффузный отклик группы — территория T4/виртуальной пирамиды.

**ПРЕДСКАЗАНИЯ.** П1: E agg=1 и agg=2 БИТОВО = agg=0 на всех сценах
(cavity05/room/blocked-beam, walk, lp=2, ±adapt): culling строго
консервативен (пропуск серии только при пустом пересечении union bbox
с сегментом — тогда и каждый член промахнулся бы). П2: время agg=1 ≤
agg=0 (серии, мимо которых трубка прошла, снимаются одним slab-тестом);
на одноматериальной cavity05 серии большие — экономия ограничена
(прогноз ×1.0–1.15); на room (многоматериальные узлы) — заметнее.
П3: ключи off битово (правки только в agg-ветках collect/deposit и
постройки списка); НК le=0→E≡0 при agg=1; ASAN/UBSAN 0/0 (room agg=1);
make test. П4: lppol=1 при agg=1 начинает работать точно (депозиты
по-кусочные) — сверить с agg=0 lppol=1.

### §872: ДОКЛАД — AGG ПЕРЕРАБОТАН: ГРУППА = CULLING ПО UNION-BBOX МАТЕРИАЛЬНОЙ СЕРИИ, ДЕПОЗИТ ТОЧНЫЙ ПО-КУСОЧНО; П1 ВЫПОЛНЕН (E БИТОВО = agg=0 ВЕЗДЕ), П2 ОТВЕРГНУТ ЗАМЕРОМ (CULLING ПОКА НАКЛАДНЕЕ ЭКОНОМИИ)

**Код.** Список узла при agg остаётся КУСКАМИ: сортировка (mtl,id) +
дедупликация перенесены ДО решения «кэшировать ли» (годен и pbuf);
гиды/rep-ray-tri/табличная раздача gSE-gacc из walk-пути УДАЛЕНЫ
(sw_group_make удалён; циклы раздачи при ag.ng=0 — no-op). В
front_seg_walk: на старте материальной серии — union bbox членов
(слияние tribox, 6 сравнений/кусок, без луча) и ОДИН slab-тест против
[tin,tout]; промах — серия пропускается целиком (строго консервативно:
bbox члена ⊆ union), попадание — обычная по-кусочная обработка.
Депозит/Lh/sw_accum — по-кусочно всегда (штамп А1564 на куске).

**Приёмка.** П1: E agg=1/2 БИТОВО = agg=0 на cavity05, room,
blocked-beam (walk, lp=2, it=4) — точность агрегации восстановлена
впервые с §863. П4: lppol=1 при agg=1 БИТОВО = lppol=1 при agg=0
(room 7.5971) — политика и culling совместимы и точны. Якоря битовы
(4.0816 walk / 4.2607 path); НК le=0→E≡0 при agg=1; ASAN/UBSAN 0/0
(room agg=1 lppol=1); make test EXIT=0; ccheck — унаследованная
invalidPointerCast (type-punning hp/wbuf — теперь ЕДИНСТВЕННАЯ
унаследованная находка, кандидат на чистку).

**П2 ОТВЕРГНУТ ЗАМЕРОМ:** culling накладнее экономии на всех замерах:
room lp=2 agg=0/1 = 7.0/9.1 c, lp=3 = 6.0/6.5, lp=4 = 7.9/8.3;
cavity05 lp=3 = 7.6/8.4. Причина: union-merge серии делается НА КАЖДЫЙ
визит узла, а попадающие серии всё равно проходят по-кусочно — экономия
только (k−1) slab-тестов пропущенных серий, их меньше, чем накладка.
СЛЕДСТВИЕ: agg=1 по умолчанию остаётся ВЫКЛЮЧЕННЫМ (точен, но не
ускоряет); его ценность теперь только как базы для T4 (диффуз/зеркало
на уровне серии) и виртуальной пирамиды. FOLLOW-UP записан: перенос
union-merget в постройку кэша списка (серии считаются один раз на узел,
в nstore рядом со списком) — сделает culling бесплатным; делать только
при появлении потребителя (T4).

**ПОРЯДОК ДАЛЬШЕ:** T4-дихотомия диффус/зеркало на walk (без agg;
зеркальный хоп = смена направления Lin по Френелю/зеркалу материала)
→ политика ℓ_p §844 → T7-сцена/потребители. Хвосты закрыты: Lin<0
(§871), agg-точность (§870/872).

## §873 (11-09): ПЛАН — T4-ДИХОТОМИЯ ДИФФУЗ/ЗЕРКАЛО НА WALK (АУДИТ ДО КОДА; ШАГ 1 — ПЛОМБING, ШАГ 2 — ЗЕРКАЛЬНЫЙ ХОП)

**ЧТО УЖЕ ЕСТЬ.** hz_obj_mtl несёт ks3[3] (§89, «Ks: доля зеркального»),
ns, ior, alpha — парсер читает с 08-12; hz_obj_mtl.kd — СРЕДНЕЕ Kd по
правилу «альбедо = доля энергии, среднее сохраняет её при плоском
спектре» (прецедент нормировки). Перенос ks НЕ читает.

**ДИЗАЙН ПОЛНЫЙ (шаг 2, следующий раунд).** На попадании куска p:
(1) ks_eff = min(ks[p], 1 − kd[p]) — доли нормируются (kd+ks ≤ 1),
клэмп назван: защита от создания энергии в .mtl; (2) депозит
Ed += w·Lin·(1 − ks_eff)·csec·axcos/area — в E идёт только ДИФФУЗНО
поглощённая доля; (3) диффузная ре-эмиссия Lh = le + kd·Eprev/(2π) —
формула не меняется, kd теперь доля; (4) ЗЕРКАЛЬНЫЙ ХОП: Lin_s =
ks_eff·Lin продолжает КОНТИНУАЦИЮ трубки из точки удара в направлении
R = om − 2(om·n̂)n̂, n̂ — нормаль куска, ориентированная ПРОТИВ om
(двусторонние треугольники: flip при om·n̂ > 0); континуация — тот же
марш (DDA от точки удара, t от 0 до выхода), глубина ограничена
(bounce ≤ 4, константа названа: больше 4 отражений в замкнутой комнате
не выживает по энергии при ks ≤ 0.95 — 0.95⁴ < 0.82, вклад 5-го
отражения < 20 % остатка; прибор покажет). ЭНЕРГОБАЛАНС: излучено =
поглощено + вышло; зеркальная доля НЕ считается поглощённой.
**ФАЛЬСИФИКАТОРЫ.** Ф-а: коридор-перисктор (два зеркала 45°, ks=1,
kd=0, le=0, источник в торце, приёмник за углом) — энергия ДОЛЖНА
дойти (сейчас: нет пути ни в одной схеме). Ф-б: замкнутая коробка
из зеркал ks=1: потеря ≈ 0, E не сходится (стационарного нет) —
марш обязан показывать lost≈0. Ф-в: ks=0 всюду — битово прежний мир.
**НК:** le=0, ks=0 → E≡0; баланс по итерациям.

**ШАГ 1 ЭТОГО РАУНДА (безопасный plumb).** swee3: ks_p = среднее ks3
материала (прецедент усреднения kd); hz_sw_opts.ks (NULL — прежний
мир, битово); ключ swee3 ksf=Ф — множитель ко ВСЕМ ks сцены (0 по
умолчанию — дихотомия выключена: без хопа (шаг 2) доля ks_eff просто
не отделяется, депозит остаётся полным; ksf≠0 при шаге 1 — НЕ
разрешается, ключ резервируется и при отсутствии шага 2 даёт ошибку
запуска — fail closed). Печать: «зеркальных кусков (ks>0): N, max ks=…»
— потребитель поля уже на шаге 1. П1: ksf=0/отсутствие — битово все
якоря. П2: печать показывает ожидаемые числа (room: несколько зеркальных
материалов; cavity05: 0).

### §873: ДОКЛАД — ШАГ 1 (PLOMB) В ДЕРЕВЕ: per-piece ks (среднее ks3) доходит до свипа за ключом-полем (НЕ читается свипом), ksf>0 fail-closed, доклад зеркальности сцены; шаг 2 (зеркальный хоп) — следующий раунд по дизайну выше

**Код:** hz_sw_opts.ks (NULL — прежний мир); swee3: ks_p = среднее ks3
материала (прецедент kd), ключ ksf=Ф; при ksf>0 — ОТКАЗ запуска
(отделять зеркальную долю без продолжения = терять энергию, fail
closed до шага 2); печать «зеркальных кусков (ks>0): N из M, max ks».

**Замер поля:** room: 4748 из 4964 кусков ks>0, max 0.473 (room.mtl
несёт Ks с 08-12/§538); cavity05: 0 из 2208; box: 0. Готовность к
шагу 2: критическая масса зеркальных кусков есть только на room —
фальсификаторы Ф-а/Ф-б (§873) строить генератором (перисктор/зеркальная
коробка), не комнатой.

**ГЕЙТ:** якоря битовы (4.0816/4.2607); make test EXIT=0; ccheck —
унаследованные advisory (atoi/atof) + invalidPointerCast.

### §873: ДОКЛАД — ШАГ 2 РЕАЛИЗОВАН: ЗЕРКАЛЬНЫЙ ХОП В WALK РАБОТАЕТ; ФАЛЬСИФИКАТОР mirror_box СОШЁЛСЯ (зеркала e=0, эмиттер e>0, баланс цел); ЛОВ ПОПУТНО: так оmp+перестановка ks

**Код (шаг 2).** front_seg_walk, точка депозита: ks_eff = min(ks[p],
1 − kd) (kd = front_rho; клэмп против создания энергии); зеркальная
доля lin_s = ks·Lin уходит в очередь хопов (LIFO, глубина ≤ 4 —
константа HZ_MIRROR_BOUNCE_MAX в полях fc: 0.95⁴ < 0.82), в депозит и
sw_accum идёт (1 − ks)·доля; Lh = le + kd·Eprev/2π (диффузный перенос)
не тронут. front_tube: после основного марша — LIFO-обработка хопов:
org = точка удара, om = R = om − 2(om·n̂)n̂ (отражение не зависит от
ориентации нормали; ПЕРВАЯ редакция содержала лишний множитель sgn —
R = om + 2n при лобовом ударе, взрыв 1e28, пойман фальсификатором и
печатями PUSH/HOP, исправлен), G6 хоп-ноги: ai − depA − a − b = 0;
хвост ноги за домен — в lost; вытесненные из очереди доли — в lost
(баланс цел). fc->om теперь указывает на muтируемый omcur (общая
таблица направлений read-only), front_dir восстанавливает omcur из
odir на каждом направлении. path=1 дихотомии НЕ имеет: ksf>0 без
walk=1 — отказ (fail closed).

**ФАЛЬСИФИКАТОР mirror_box** (scripts/gen_mirror_box.sh): коробка
[0,1]³, стена x=0 — эмиттер (Kd 0, Ks 0, Ke 1), остальные — зеркала
(Kd 0, Ks 1 1 1). rho=0 (глобальный ρ=0.5 делал стены полудиффузными —
клэмп честно резал ks; это фича, не баг). РЕЗУЛЬТАТ (walk, ke=1,
ksf=1): e=0 на всех зеркалах, e>0 на эмиттере (свет возвращается
отражениями), баланс: излучено 63.14 = поглощено 5.50 + lost 2.25 +
(recirculation — сток), E стационарен 0.9163. НК le=0 → E≡0 ✓.

**ЛОВЫ ПОПУТНО (оба пойманы фальсификатором):** (1) ks-массив не
переживал Morton-перестановку — зеркала получали чужой ks; добавлен в
цепочку hz_pyr_permute (рядом с lep9); (2) fc->om указывал на общую
таблицу направлений — запись хопом портила направление ВСЕМ
последующим трубкам (взрыв 1e28); введён omcur.

**ГЕЙТ:** якоря битовы (4.0816 walk, 4.2607 path, 4.2689 walk
cavity05, 7.5971 room lppol); НК le=0→E≡0; ASAN/UBSAN 0/0 после
починки утечки ks (free рядом с lep9); make test EXIT=0; ccheck —
унаследованные. Остатки: Ф-а «перисктор» (45°-зеркала) — следующий
фальсификатор; Ns (блеск) не читается — глянцевая доля вне дихотомии.

**ПОРЯДОК ДАЛЬШЕ:** Ф-а перисктор → T4 в pgather (камера видит
отражения) → политики дефолтов потребителей → T7.

### §873-Ф-а: ДОКЛАД — ПЕРИСКТОР СОШЁЛСЯ: свет проходит два зеркальных хопа и депонирует на приёмнике; геометрия фальсификатора училась на себе дважды; swee3 восстановлен из HEAD после порчи чистилкой

**Фальсификатор Ф-а (scripts/gen_mirror_periscope.sh, mirror_periscope):**
коридор 3×1×1; эмиттер — плавающий квадрат x=0.5 (Ke 1, Kd 0, Ks 0);
зеркало 1 — плоскость x+z=3, полоса z∈[0.5,1]; зеркало 2 — плоскость
x+z=2.75, полоса z∈[0.25,0.5]; приёмник — стенка x=3 (Kd 1, Ks 0).
Луч +x: эмиттер (тёмный вход, Lh=lep=1) → зеркало 1 → хоп −z →
зеркало 2 → хоп +x → приёмник.

**РЕЗУЛЬТАТ (walk, le=0, ksf=1, it=3):** e>0 ТОЛЬКО на приёмнике
(0.0256/0.0511) и следов диффузной рециркуляции на эмиттере (0.051 —
легитимно: приёмник Kd=1 ре-эмитирует, часть достаётся эмиттеру);
зеркала e=0 строго. НК ksf=0: свет умирает на зеркале 1, приёмник
получает только прямую подсветку трубок мимо зеркал (z0<0.25) —
дихотомия дискриминирует конфигурации.

**ГЕОМЕТРИЧЕСКИЕ УРОКИ (две итерации дизайна):** (1) полноростовые
занавесы x+z=const в узком коридоре блокируют друг друга: после второго
отражения +x-луч пересекает плоскость первого зеркала через 0.01 —
пинг-понг до глубины, свет до приёмника не доходит; лечится МАЛЫМИ
пятнами со смещением (зеркало 2 НИЖЕ зеркала 1, +x-выход проходит под
ним). (2) заполнение ks-массива обязано жить ДО hz_pyr_build/permute:
заполнение после перестановки давало зеркалам чужой ks (тот же
фальсификатор ловил).

**ИНЦИДЕНТ ВОССТАНОВЛЕН:** неудачная авто-чистка приборов съела ~270
строк swee3.c (удалила хвост main вместе с Morton-блоком); файл
восстановлен из HEAD (dfc8008), фикс перенесён заново точечными
правками. Урок: line-based чистилки по grep — только с git diff
перед коммитом.

**ГЕЙТ:** якоря битовы (4.0816, 4.2607); НК le=0→E≡0; ASAN/UBSAN 0/0
(перисктор); make test EXIT=0.

## §874 (11-09): ПЛАН — T4 В PGATHER: КАМЕРА ВИДИТ ОТРАЖЕНИЯ (ДО КОДА)

**СУТЬ.** Зеркальный хоп в walk жив (§873 шаг 2, перисктор сошёлся): E на кусках
несёт ТОЛЬКО диффузно поглощённую долю. pgather же до сих пор (а) не подключает
per-piece ks к свипу (so.ks=NULL — свип в pgather живёт в мире до дихотомии) и
(б) рисует пиксель чистым Ламбертом L = le + kd·E/2π — зеркальный лепесток
(дельта по направлению) камере недоступен. Правка только в tools/pgather.c,
библиотека НЕ трогается.

**МЕХАНИКА.**
1. Ключ `ksf=F` (умолчание 0). Заполнение ks[tq] = ksf · среднее ks3 материала
   в порядке ИСХОДНЫХ треугольников ДО hz_pyr_build, затем ks включён в цепочку
   hz_pyr_permute (урок §873: заполнение/перестановка в неверном порядке давали
   зеркалам чужой ks). so.ks = ksf>0 ? ks : NULL — битово прежний мир.
   walk=1 в pgather постоянен (§867), отказ «ksf>0 без walk» не нужен.
2. Камера: L_пикселя = L_cam(p, 0), где
       L_cam(p, d) = le + kdvis·E_p/2π + ks_eff·L_cam(q, d+1),
   kdvis = (rho<0 ? kd[p] : rho) — прежняя семантика оверрайда;
   ks_eff = min(ks[p], 1 − kdvis) — ТОТ ЖЕ клэмп, что в депозите walk (§873),
   против создания энергии;
   q — ближайшее пересечение ВТОРИЧНОГО луча: org = точка удара (со сдвигом
   eps вдоль отражённого направления против самопересечения), dir =
   R = rd − 2(rd·n̂)n̂ — отражение не зависит от ориентации нормали
   (двусторонние треугольники, прецедент §873 шаг 2). Вторичный луч — тем же
   сборщиком (pg_dda при gather=0, перебор при gather=1). Глубина d ≤ 4 —
   константа PG_MIRROR_BOUNCE_MAX, зеркало HZ_MIRROR_BOUNCE_MAX из walk
   (0.95⁴ < 0.82); глубже камера не видит то, чего нет в поле.
   ks_eff ≤ 0 или исчерпанная глубина → слагаемое 0.
3. Печать: «зеркальных вторичных лучей: N (доля от попавших)» — потребитель
   виден числом. ksf=0 — вторичные лучи не пускаются вовсе.

**ПРЕДСКАЗАНИЯ.**
- П1: ksf=0 (умолчание) — кадр БИТОВО прежний на всех сценах (ppmdiff пуст;
  правки выключены целиком: ks=NULL, вторичные лучи не рождаются).
- П2 (фальсификатор mirror_box, §873): камера внутри коробки смотрит на
  зеркало (стена, Kd 0, Ks 1), эмиттер за спиной. ksf=1: пиксели зеркала
  НЕнулевые (в зеркале виден эмиттер), причём яркость = ks·L_эммитера в
  пределах фактора геометрии; ksf=0: те же пиксели 0 (kd=0, E=0). Кадры
  различаются контрастно — дихотомия видна в картинке.
- П3: room ksf=1: кадр меняется (4748 кусков ks>0, §873); время сбора растёт
  ≤ ×2 (вторичный луч только на зеркальных попаданиях); СБОР-статы в допуске.
- П4: НК le=0 → кадр тождественно 0 при любом ksf (цепь L_cam ≥ 0, le=0 и E≥0
  и ks_eff·L_cam ≥ 0 — рекурсия в ноль).
- П5: гейт CLAUDE.md: clang-format, ccheck pgather до CLEAN (унаследованные
  advisory-классы допустимы, новых нет), make test EXIT=0, ASAN/UBSAN 0/0 на
  mirror_box ksf=1 (рекурсия + вторичные DDA — риск утечки/выхода за массив).
- П6 (фальсификатор плана): если на mirror_box при ksf=1 пиксели зеркала
  остались 0 или вспыхнули > L_эммитера на порядок — механика отражения
  неверна (знак R, ориентация нормали, eps), код не принимается.

### §874 — аудит ДО кода (серия А)

- **А1580 (принято):** клэмп ks_eff в камере обязан идти по kdvis =
  (rho<0 ? kd[p] : rho), а не по сырому kd — в walk клэмп стоит по
  front_rho (sweep.c:1199-1201, ovr-семантика). Камера обязана совпадать со
  свипом, иначе пиксельная дихотомия разойдётся с полем.
- **А1581 (принято):** nrm в pgather уже переставлен (permute-цепочка), ks
  обязан пройти ТОУ ЖЕ цепочку hz_pyr_permute после build; заполнение —
  в порядке исходных tri ДО build (урок §873-Ф-а, лов 1).
- **А1582 (принято):** самопересечение вторичного луча: origin = точка удара
  + R·eps, eps = 1e-6·maxdim сцены (названная константа); pg_ray_tri сам
  режет t<1e-9. Сдвиг меньше толщины стен синтетики.
- **А1583 (принято, документировано):** глобальный скалярный le добавляется
  КАЖДОМУ видимому куску — в существующей семантике pgather (там же в базовом
  луче) и в рекурсии; это уже не физика источников, а инструментальная
  подсветка — поведение сохранено, не расширяется.
- **А1584 (принято):** вторичные лучи только при ks_eff>0; при ksf=0 рекурсия
  не рождается вовсе — П1-битовость не зависит от счётчиков DDA.
- **А1585 (принято):** общий код ближайшего попадания выделяется в
  pg_nearest(...) — базовый и вторичный лучи (gather=0 DDA / gather=1 brute)
  обязаны ходить одним сборщиком, иначе дихотомия разъедется между режимами.
- **А1586 (проверено, риска нет):** глубина 4 — рекурсия ограничена
  константой, переполнения стека нет; E читается float → (double), знаков
  отрицательных нет (§871).

### §874: ДОКЛАД — КАМЕРА ВИДИТ ОТРАЖЕНИЯ: ПИКСЕЛЬ ЗЕРКАЛА = ks_eff·L_ВИДИМОГО; П1 БИТОВО, П2 СОШЁЛСЯ КОЛИЧЕСТВЕННО, П3 КАДР room МЕНЯЕТСЯ; ПОПУТНО pgather ПОЛУЧИЛ useke/lep И ПОЧИНЕНА УТЕЧКА §867

**Код (только tools/pgather.c + фальсификатор-генератор).** Ключ `ksf=F`:
ks[tq]=F·ср.ks3 заполняется в порядке исходных tri ДО build, проходит цепочку
hz_pyr_permute (А1581), so.ks=F>0?ks:NULL. Ключ `useke`: lep[tq]=ср.Ke3
(БЕЗ le — front_le сам прибавляет глобальный), so.lep; кадр и камера видят
эмиссию материалов (А1576-семантика). Камера: L(p,d)=le+lep[p]+kdvis·E_p/2π
+ ks_eff·L(отражённый, d+1), ks_eff=min(ks,1−kdvis) — клэмп по ovr-семантике
(А1580), глубина ≤ PG_MIRROR_BOUNCE_MAX=4, сдвиг старта eps=1e-6·maxdim
(А1582), базовый и вторичный лучи одним сборщиком pg_nearest (А1585).
Прибор: «вторичных зеркальных лучей N».

**П1 (битовость):** ksf=0 — кадры box (160×120, it=4) и room (96×72, ks-
материалы в сцене) БИТОВО равны старому бинарнику; повтор после подключения
lep — тоже (lep=NULL при useke=0; прибавление +0.0 битово нейтрально).

**П2 (фальсификатор mirror_box):** камера в центре, смотрит в зеркало x=1,
эмиттер (Ke=1) за спиной. ksf=1: средняя яркость кадра 0.9971, max 1.0000
— пиксель зеркала = ks_eff·L_эммитера = 1·1 ТОЧНО (плоское зеркало сохраняет
яркость); ksf=0: те же пиксели 0.0000 (kd=0, E=0 — диффузного пути нет).
Кадры контрастно различаются; дихотомия видна в картинке
(img/s874_mbox_ksf1.ppm против img/s874_mbox_ksf0.ppm). НК le=0 → кадр
тождественно 0 (max байта = 0) при ksf=1.

**П3 (room):** ksf=1 кадр отличается от ksf=0; 1255 вторичных лучей на
6912 пикселей, время сбора 0.03 с (без изменений в шуме). E Avg меняется
(6.08→3.89) — дихотомия в свипе реально включена (зеркальная доля ушла из
депозитов в хопы walk).

**ПОПУТНО:** (1) НК-прогон вскрыл, что pgather НИКОГДА не подавал so.lep —
сцены с Ke были для камеры и свипа pgather тёмными; теперь ключ useke
(дефолт off, битово). (2) ASAN поймал ПРЕДСУЩЕСТВУЮЩУЮ утечку tv9/tb6/lparr
§867 — освобождены. (3) Генератор mirror_box писал `Ke` ОДНИМ скаляром,
парсер ждёт три компоненты (ke3=(1,0,0), среднее 1/3) — поправлен на
«Ke 1 1 1»; урок продолжение §873-Ф-а: фальсификатор учится на себе.

**ГЕЙТ:** clang-format; ccheck CLEAN; make test EXIT=0; ASAN/UBSAN 0/0
(mirror_box ksf=1, рекурсия+вторичные DDA); библиотека (src/) НЕ тронута —
якоря swee3 4.0816/4.2607/7.5971 валидны по построению.

**АУДИТ ИСПОЛНЕНИЯ.** Сбылось: П1/П2/П3/П4/П5. П6 сработал по назначению
один раз ДО починки useke (нулевые пиксели при живых 31782 вторичных лучах
— указало на отсутствующий lep, не на механику отражения). НЕ покрыто:
Bistro/pgather-кадры не гонялись (перебор nt на луч вне замера шага);
физика Френеля (направленная доля по углу) вне дихотомии — ks константа;
глянец Ns по-прежнему не читается. ОСТАЛОСЬ: политики дефолтов потребителей
→ T7; латентный хвост — «mtl-скаляры в генераторах» проверять на других
фальсификаторах (mirror_periscope: тот же класс правки НЕ сделан — его
сцену §873 не трогал, Ke-скаляр остаётся; сверка при следующем прогоне).

**ПОРЯДОК ДАЛЬШЕ:** политики дефолтов потребителей (walk/ksf/useke в pref,
pgather, растровые камеры) → T7.

## §875 (11-09): ПЛАН — ПОЛИТИКА ДЕФОЛТОВ ПОТРЕБИТЕЛЕЙ: PGATHER ПО УМОЛЧАНИЮ РИСУЕТ ПРОДАКШН-МОДЕЛЬ (ДО КОДА)

**СУТЬ.** Потребители продакшн-модели (§861 вердикт walk; §873/§874 дихотомия
ks; А1576 per-piece эмиссия) обязаны показывать её БЕЗ ключей — иначе
инструмент продаёт устаревший мир как рабочей (тот же мотив, что §867 для
walk). Дефолты pgather: **useke=1, ksf=1**. Явные ключи перекрывают:
`useke=0`, `ksf=0` — выход к прежнему миру (документированный НК-выход);
голый флаг `useke` остаётся валиден = 1 (обратная совместимость).

**ЧТО НЕ МЕНЯЕТСЯ И ПОЧЕМУ.**
- **pref — эталон** диффузной истины (случайные направления, точная
  видимость, MC-Жакоби L=Le+ρE/2π). Зеркальной дихотомии в эталоне НЕТ —
  включать ksf в pref нечестно (сравнение свип/эталон разъехалось бы
  искусственно); зеркальный эталон — отдельная работа, не этот шаг.
- **swee3** — драйвер экспериментов, его дефолты якорные (4.0816/…), не тронуты.
- **pcam** — замер позиции камеры старого пути, поле не потребляет.

**ПРЕДСКАЗАНИЯ.**
- П1: дефолтный прогон pgather = прежний ЯВНЫЙ `ksf=1 useke` БИТОВО
  (mirror_box и room — кадры и печать).
- П2: на сценах без Ke и ks (box, cavity05) дефолтный кадр БИТОВО равен
  прежнему миру (lep≡0 → front_le=le+0.0 битово; ks≡0 → ветка депозита
  и камеры идентична по арифметике).
- П3: НК-выходы `useke=0 ksf=0` — битово прежний мир на всех сценах;
  НК le=0 переносится на сцену БЕЗ Ke (mirror_box теперь с Ke светится
  при le=0 — это фича, НК там более не нулевой).
- П4: pref/swee3 не пересобираются из изменений (правки только
  tools/pgather.c) — их числа валидны по построению.
- П5: гейт: clang-format, ccheck CLEAN, make test EXIT=0, ASAN/UBSAN 0/0
  на mirror_box дефолтным прогоном.
- П6 (фальсификатор): дефолтный кадр ≠ явному ksf=1 useke → парсинг
  умолчаний сломан, код не принимается.

### §875 — аудит ДО кода (серия А)

- **А1587:** голый флаг `useke` обязан остаться валидным (=1) — обратная
  совместимость прогонов §874; новый разбор `useke=0|1`.
- **А1588:** ksf=0 → существующий путь so.ks=NULL и cam.ks=NULL — прежний
  мир без новых веток.
- **А1589:** П2-битовость: so.ks — массив НУЛЕЙ на ks=0-сценах; walk-депозит
  при ks=0 идёт по ветке else, арифметически тождественной отсутствию ks;
  камера при kse=0 не рождает лучей. Убедиться прогоном, не поверить.
- **А1590:** хелп и шапка файла обязаны назвать дефолт и выход к прежнему
  миру, иначе инструмент лжёт о себе.
- **А1591:** НК le=0 переносится на box (без Ke): на mirror_box le=0 при
  дефолте НЕ нулевой — Ke теперь легитимный источник кадра.

### §875: ДОКЛАД — PGATHER ПО УМОЛЧАНИЮ РИСУЕТ ПРОДАКШН-МОДЕЛЬ (useke=1 ksf=1): П1/П2/П3 БИТОВО, pref/swee3 НЕ ТРОНУТЫ; ПОПУТНО ВЫЛЕЧЕН FP gcc-АНАЛИЗАТОРА В PGATHER

**Код (только tools/pgather.c):** дефолты useke=1, ksf=1; новые формы ключей
`useke=0|1`, `ksf=0` — документированный выход к прежнему миру; голый флаг
`useke` сохранён (А1587). Хелп и шапка называют дефолт и выход (А1590).

**Приёмка.** П1: дефолтный прогон БИТОВО = явному «ksf=1 useke» бинарника
§874 на mirror_box (160×120, кадр + печать «31782 вторичных») и room
(96×72). П2: на box (без Ke и ks) дефолт БИТОВО = прежнему миру (lep≡0,
ks≡0, ветки арифметически тождественны — А1589 подтверждён прогоном).
П3: `useke=0 ksf=0` БИТОВО = прежнему миру на room; НК le=0 на box (без
Ke, А1591): max байта кадра = 0. П4: правки не затрагивают pref/swee3 —
их числа валидны по построению. П5: ccheck CLEAN; ASAN/UBSAN 0/0 дефолтным
прогоном на mirror_box; make test EXIT=0.

**ПОПУТНО:** gcc-анализатор дал FP CWE-457 (e1[1] «не инициализирован» —
потеря индукции цикла по ax, известный класс diam; прецедент pg_bbox_span).
Лечение — явная инициализация e1/e2 нулями, все элементы перезаписываются,
арифметика не меняется. Попутно замечен advisory clang-tidy cert-err34
(sscanf/atoi — унаследованный класс по всему файлу, не новый).

**АУДИТ ИСПОЛНЕНИЯ.** Всё сбылось; П6 не срабатывал. Чего НЕ покрывает:
Bistro-кадр дефолтным прогоном не гонялся (там Ks материалов при ksf=1
включит дихотомию на тяжёлой сцене — замер отдельного шага T7-класса);
зеркального ЭТАЛОНА (MC с зеркальными путями) нет — «правильность» зеркала
подтверждена только количественным фальсификатором (0.9971/1.0000) и
аналитикой плоского зеркала. Хвост «mtl-скаляры в генераторах фальсификато-
ров» (mirror_periscope Ke одним скаляром) — остаётся открытым.

**ПОРЯДОК ДАЛЬШЕ:** T7 (сцена/потребители: Bistro-кадр дефолтной камерой,
замер budgets) — лестница §873/START исчерпана до T7.

## §876 (11-09): ПЛАН — T7-ШАГ 1: ТЯЖЁЛАЯ СЦЕНА ДЕФОЛТНЫМ ПОТРЕБИТЕЛЕМ — BISTRO-КАДР PGATHER, ЗАМЕР СТАТЕЙ БЮДЖЕТА (ДО КОДА; ЗАМЕРНЫЙ ШАГ)

**ЗАЧЕМ.** Цель «по сцене можно ходить» не закрыта синтетикой: город
(115 м Bistro exterior, 2.84 М треугольников) дефолтной камерой новой
структуры ещё не снимался ни разу. Шаг — ЗАМЕРНЫЙ: холодный кадр Bistro
дефолтным pgather (§875: walk + дихотомия ks + per-piece эмиссия), разложить
время и память по статьям (загрузка OBJ / trivert-подготовка / пирамида+Morton
/ bbox-CSR / свип / сбор), сверить с комнатной экстраполяцией. КОДА НЕ ТРЕБУЕТСЯ.

**КОНФИГУРАЦИЯ.** пилот → экстраполяция → основной кадр (стремиться W=320,
H=240, it=1…2, lev=6…7, dirs=6…26, дефолтные useke/ksf). Камера снаружи
(глаз вне геометрии — урок pcam §187; качество оператора камеры НЕ предмет
шага). Бюджет прогона: ulimit -v 12G; если свип не сходится за ~1 ч —
фиксировать как ЧИСЛО named gap, не дожимать.

**ПРЕДСКАЗАНИЯ.**
- П1 (экстраполяция): время свипа ~ пропорционально nt·dirs·it; пилот
  (2.84 М кусков, it=1, dirs=6) даёт базу; комната давала 0.9 с / 5к кусков /
  26 ординат / it=2 — Bistro обязан лечь в часы, не в сутки.
- П2 (память): ≤ 12 ГБ: OBJ+mtl ~2-3 ГБ, trivert/tribox ~200 МБ, CSR и
  пирамида — гигабайты; если OOM — статья названа числом.
- П3 (качество): дефолтная дихотомия на Bistro НЕ взрывается: E_avg конечна,
  зеркальных лучей > 0 (у Bistro есть Ks); СЛИЧЕНИЕ L_сбора/L_свипа в
  коридоре, для которого нет отдельного ожидания — фиксируется как первое
  число для города.
- П4 (НК): le=0 useke=0 ksf=0 → кадр тождественно 0 и на городе.
- П5 (фальсификатор шага): если пилот/основной прогон не влезает в бюджет
  времени/памяти на порядок — «интерактив на городе» фиксируется как
  недостижимый текущей структурой сбор/свипа, доклад со статьями — это
  результат, а не провал шага.

### §876: ДОКЛАД — BISTRO (2.84 М ТРЕУГОЛЬНИКОВ, 115 м) ХОЛОДНЫЙ КАДР ДЕФОЛТНЫМ PGATHER: 14.0 С / 1.06 ГБ; СВИП 8.2 С, СБОР 0.44 С; «ЧАСЫ» П1 НЕ ПОДТВЕРДИЛИСЬ — ЛУЧШЕ НА ДВА ПОРЯДКА

**Статьи бюджета (первый замер города на новой структуре; Bistro exterior,
scale=0.01, it=2, lev=7, dirs=26, 320×240, дефолтные useke/ksf):**

| статья | время |
|---|---|
| obj-загрузка (nt=2 833 330, mtl=133) | 2.12 с |
| подготовка (area/nrm/kd/ks/lep/trivert) | 2.28 с |
| пирамида + Morton + per-node lp (100 284 листа) | 2.50 с |
| bbox-CSR | 0.43 с |
| **свип** (walk, it=2, dirs=26) | **8.22 с** |
| сбор (DDA, 76 800 лучей) | 0.44 с |
| **холодный кадр всего** | **13.95 с** (wall), RSS **1.06 ГБ** |

**Приёмка.** П1 ОТВЕРГНУТ ЗАМЕРОМ В ХОРОШУЮ СТОРОНУ: ожидались часы,
получено 14 с — экстраполяция с комнаты была пессимистична (свип walk
пропорционален числу НЕПУСТЫХ трубок, а город в основном воздух; клеток/луч
47.5, кусков/луч 165.7 = 0.01 % от nt). П2 ✓ (1.06 ГБ ≪ 12 ГБ). П3 ✓:
E_avg=10.79 конечна, дихотомия жива (899 вторичных зеркальных лучей),
СЛИЧЕНИЕ L_сбора/L_свипа = 0.8950 — ПЕРВОЕ число города для этого коридора.
П4 ✓: НК le=0 useke=0 ksf=0 → кадр тождественно 0. П5 не сработал.

**Попутно:** pgather получил прибор статей (printf-таймеры фаз; на арифметику
не влияет). Позиционного масштаба у pgather НЕТ (в отличие от swee3) —
только `scale=`; голый «0.01» молча съедался как имя сцены. Гейт:
clang-format, ccheck CLEAN, make test EXIT=0.

**Что НЕ покрывает.** Кадр — не «по сцене можно ходить»: 14 с — холодный
старт; кадр в движении требует T5б-класса (хранимое поле + инкрементность).
max кадра 3069 — яркий пиксель (пересвеченный кусок при le=1-подсветке),
к нормировке кадра не относится, но записан. Качество оператора камеры не
мерилось (глаз снаружи, урок pcam §187 соблюдён). Небо 20 % лучей — фон 0.

**ПОРЯДОК ДАЛЬШЕ:** выбор за следующим раундом: (а) T5б-класс — тёплый
кадр/инкрементность на новой структуре (двигает цель напрямую), (б) зеркальный
MC-эталон (закрывает хвост валидации дихотомии), (в) хвост mtl-скаляров
генераторов. Все три названы владельцами, «открыто» без владельца не осталось.

## §877 (11-09): ПЛАН — ХОДЬБА-1: ПОЛЕ РЕШАЕТСЯ ОДИН РАЗ, КАДРЫ ИЗ МНОЖЕСТВА ТОЧЕК; ЗАМЕР ВРЕМЕНИ КАДРА 512² ПРОТИВ БЮДЖЕТА ЦЕЛИ (ДО КОДА)

**ЗАЧЕМ.** Цель — «по сцене можно ходить», бюджет ≤200 мс/кадр при 512² на
зале. Поле на кусках ПЕРСИСТЕНТНО — камера-оператор не обязана пересчитывать
свип при смене точки зрения. Шаг делает это ФАКТОМ: pgather решает поле
ОДИН раз, затем рендерит N кадров вдоль траектории глаза, печатает время
КАЖДОГО кадра (свип вынесен за скобки). Это первый прямой замер бюджета
цели на новой структуре.

**КЛЮЧИ.** `frames=N` (дефолт 1 — прежний мир), `eye2=X,Y,Z`, `look2=` —
конец траектории; кадры линейно интерполируют (eye,look) от старта к концу.
Файлы: `out=` для кадра 0, `out=` + `_f%02d` для остальных.

**ПРЕДСКАЗАНИЯ.**
- П1: frames=1 битово прежний мир (цикл вырождается).
- П2: время КАДРА (только сбор) на room при 512²: экстраполяция с 96×72
  (~0.03 с) даёт ~1.1 с — ПРЕДСКАЗЫВАЮ ПРОВАЛ бюджета ×5; если ≤200 мс —
  экстраполяция была пессимистична (кэш CSR). Оба исхода — результат;
  невязка фиксируется числом как владелец будущего хода (блочные фронты
  сбора/лодж).
- П3: N кадров стоят ~N×сбор + 1×свип; ходьба = амортизация свипа
  (room: свип ~1.7 с / кадр ~1 с → окупается со второго кадра; Bistro:
  свип 8.2 с / кадр ~2 с — окупается с 5-го).
- П4: яркость кадра 0 = кадру 0 при тех же le/keys (та же решённая E);
  НК le=0 useke=0 ksf=0 → все кадры 0.
- П5 (фальсификатор): если время кадра растёт от номера кадра (утечка,
  деградация CSR) — механика ходьбы неверна, код не принимается.
- П6: гейт: формат, ccheck CLEAN, make test, ASAN 0/0 на ходьбе.

### §877 — аудит ДО кода (серия А)
- **А1592:** DDA-счётчики копятся по кадрам — печать суммарная, кадры не меняет.
- **А1593:** нормировка PPM на lmax КАЖДОГО кадра — существующее поведение;
  при ходьбе кадры сравниваются по абсолютной яркости печати, не по PPM.
- **А1594:** глаз траектории может оказаться внутри геометрии — это штатный
  замер (% попавших печатается), качество оператора камеры вне шага (pcam §187).
- **А1595:** файлы ходьбы: `out_f%02d.ppm`, кадр 0 — без суффикса (совместимость).
- **А1596:** полностью тёмный кадр (lmax=0) — уже обработан делением на 1.
- **А1597:** П1-битовость: frames=1 вырождается в прежний цикл, суффиксов нет.

### §877: ДОКЛАД — ХОДЬБА РАБОТАЕТ: ПОЛЕ ОДИН РАЗ, КАДРЫ ИЗ ЛЮБОЙ ТОЧКИ; БЮДЖЕТ ЦЕЛИ 200 мс/512² НА ЗАЛЕ ПРОВАЛЕН ЧЕСТНО: 0.27–0.40 с (×1.4–2) — ВЛАДЕЛЕЦ ХВОСТА НАЗВАН (ускорение сбора)

**Код (только tools/pgather.c):** ключи `frames=N`, `eye2=`, `look2=` —
линейная траектория (eye,look); свип выполняется ОДИН раз, кадр = только
сбор; кадр 0 — прежний `out=`, остальные `out=_fNN.ppm`. frames=1 —
битово прежний мир (П1 подтверждён cmp).

**ЗАМЕР БЮДЖЕТА ЦЕЛИ (room, 512², dirs=26, ходьба 8 кадров по диагонали
зала): кадр внутри зала 0.27–0.40 с — П2 сбылся по знаку (≤ экстраполяции
×5) и провалился по сути: бюджет 200 мс НЕ достигнут, невязка ×1.4–2.
Владелец хвоста: ускорение сбора (лодж/блочные фронты), назван в §877-плане.
Кадры снаружи геометрии (глаз вышел из зала) — 0.03 с: пустота бесплатна.**

**Bistro 512² ходьба 4 кадра:** свип 8.34 с один раз; кадры 1.38/1.22/0.14/
0.50 с (зависят от того, сколько сцены в кадре: 62–100 % попавших).
П3 ✓: амортизация свипа окупается с 2-го кадра (room) / 5-го (Bistro).

**П4 ✓:** НК le=0 useke=0 ksf=0 — ВСЕ кадры ходьбы тождественно 0.
П5 ✓: время кадра по номеру не растёт (0.40→0.29→0.27, падение — кэш).
П6 ✓: ccheck CLEAN, ASAN/UBSAN 0/0 на ходьбе, make test EXIT=0.

**ЧТО НЕ ПОКРЫВАЕТ.** Разрушаемость не замерялась (правка геометрии при
ходьбе — следующий шаг T5б-класса, требует локальной инвалидации поля);
зритель не делался — кадры сравниваются числами; траектория линейна,
не сглажена; ходьба по Bistro снаружи — «войти внутрь» не проверено
(глаз внутри городской геометрии — отдельный замер).

**ПОРЯДОК ДАЛЬШЕ:** владелец-хвост «сбор 200 мс/512²» (лодж сбора или
SIMD по лучам) ЛИБО разрушаемость (инкрементная инвалидация при ходьбе) —
решение за следующим раундом; зеркальный MC-эталон и mtl-скаляры остаются
открытыми с владельцами.

## §878 (11-09): ПЛАН — СБОР ПОД БЮДЖЕТ ЦЕЛИ: OMP-ПАРАЛЛЕЛИЗМ КАДРА ПО СТРОКАМ (ДО КОДА)

**ЗАЧЕМ.** Хвост §877: кадр 512² внутри зала 0.27–0.40 с при бюджете 200 мс.
Сбор pgather однопоточный, тогда как проектный ресурс — ядра (рабочий свип
ходит на 16). Лучи кадра НЕЗАВИСИМЫ (решённое поле read-only) — идеальное
распараллеливание без смены семантики. Это механический ход, и он обязан
быть сделан ПЕРЕД алгоритмическими (лодж), чтобы лодж мерился против
параллельного сборщика.

**МЕХАНИКА.** `#pragma omp parallel for schedule(dynamic)` по iy; private:
ax, per-thread pg_cam c локальными счётчиками (steps/tested/nsec — указатели
на thread-local переменные); reduction: dda_steps, dda_tested, sec_rays,
nhit, lsum (+), lmax (max). Пиксельная арифметика не меняется ни на бит.

**ПРЕДСКАЗАНИЯ.**
- П1: кадры OMP-прогона БИТОВО равны однопоточным (OMP_NUM_THREADS=1 и 16)
  на room и mirror_box — параллелизм меняет только порядок независимых
  лучей.
- П2: кадр 512² внутри зала (room, худший из §877): wall ≤ 200 мс на 16
  ядрах (ожидание ×8–14 от 0.27–0.40 с); бюджет цели ВЫПОЛНЕН или названо
  что мешало.
- П3: масштабирование ≥ ×6 (накладки динамического планирования, память).
- П4: сбор на Bistro 512² ≤ 300 мс (город под бюджет цели — впервые).
- П5: НК: OMP_NUM_THREADS=16, le=0 useke=0 ksf=0 → все кадры 0.
- П6: гейт: формат, ccheck CLEAN, make test, ASAN 0/0 (гонки — helgrind
  недоступен, ASAN+logical-проверка битовости П1 служит детектором гонок:
  битовые различия при 16 потоках = гонка).

### §878 — аудит ДО кода (серия А)
- **А1598:** общий `ax` из main обязан быть private (используется в теле луча).
- **А1599:** pg_cam: указатели на геометрию/массивы read-only — общие;
  steps/tested/nsec — thread-local; cam конструируется в потоке копией шаблона.
- **А1600:** reduction(+) прибавит к счётчикам, уже накопленным K27 и ходьбой —
  семантика печати сохранена.
- **А1601:** schedule(dynamic, 16): лучи неравной стоимости (небо против зала).
- **А1602:** битовость при OMP_NUM_THREADS=16 — детектор гонок (П1).
- **А1603:** OMP_NUM_THREADS=1 — тот же код, без отдельной ветки.

### §878: ДОКЛАД — БЮДЖЕТ ЦЕЛИ ЗАКРЫТ: КАДР 512² ВНУТРИ ЗАЛА 0.03–0.06 с ПРИ 16 ПОТОКАХ (был 0.27–0.40 с); ГОРОД 512² — СБОР 0.20 с; П1 БИТОВО = ДЕТЕКТОР ГОНок ЧИСТ

**Код (только tools/pgather.c):** `#pragma omp parallel for schedule(dynamic,16)`
по строкам кадра; private(ax, cam), счётчики thread-local с редукцией
(А1599/А1600); пиксельная арифметика не тронута. Попутно: FP-класс diam
снова пойман (rd в OMP-регионе) и вылечен явной инициализацией.

**Приёмка.**
- П1 ✓: кадры OMP=1 и OMP=16 БИТОВО = однопоточному прежнему бинарнику
  (room 96×72, cmp) — битовость как детектор гонок чиста; повтор после
  FP-фикса — тоже.
- П2 ✓: room 512², глаз внутри зала (худший класс §877): **0.03–0.06 с**
  при 16 потоках против 0.25 с одним — бюджет 200 мс ЗАКРЫТ с запасом ×3+.
- П3: масштабирование 0.25→0.16 (×2)→0.10 (×4)→0.04–0.06 (×8–16): ×4–6
  до насыщения; при 0.04 с кадра накладки планирования сопоставимы с работой.
- П4 ✓: Bistro 512² сбор 0.20 с (свип не трогался) — город под бюджетом
  сбора цели; остальное в кадре города — свип/подготовка (§876).
- П5 ✓: НК le=0 useke=0 ksf=0 при 16 потоках — кадр тождественно 0.
- П6 ✓: ccheck CLEAN, ASAN/UBSAN 0/0 при 4 потоках, make test EXIT=0.

**ЧТО НЕ ПОКРЫВАЕТ.** Замер «0.03–0.06 с» — сбор; полный кадр-в-движении
требует ещё и отсутствия пересвипа (T5б-инкрементность при изменениях);
масштабирование >8 потоков не даёт пропорции (кадр 512² мал — зернится
накладками; на 4K профиль другой, не мерилось). helgrind/tsan не гонялись —
битовость П1 служит логическим детектором, но не заменяет tsan (хвост
мелким шрифтом: гонка, не влияющая на байты кадра, теоретически остаётся
возможна в счётчиках печати; счётчики редуцированы — риска не видно).

**ПОРЯДОК ДАЛЬШЕ:** цель «по сцене можно ходить» в статике ЗАКРЫТА
(сбор 0.03–0.06 с, свип амортизируется ходьбой §877). Остались: (а)
разрушаемость — инкрементная инвалидация поля при правке геометрии
(пункт 3 цели, владелец: следующий шаг T5б-класса); (б) зеркальный
MC-эталон; (в) mtl-скаляры генераторов.

## §879 (11-09): ПЛАН — РАЗРУШАЕМОСТЬ-1: УДАЛЕНИЕ ГЕОМЕТРИИ В БОКСЕ, ТЁПЛЫЙ ПЕРЕСВИП ПРОТИВ ХОЛОДНОГО (ДО КОДА)

**ЗАЧЕМ.** Пункт 3 цели: правка O(объёма изменения) в кадровый бюджет.
Прототип в pgather: ключ `delbox=x0,y0,z0:x1,y1,z1` — куски с центроидом
в боксе УДАЛЯЮТСЯ из сцены. Фазы: (A) решение полной сцены it итераций,
E запоминается по исходному треугольнику; (B) пирамида/массивы пересобираются
без удалённых кусков, E выживших переносится как тёплый старт (Eprev=pcs.e —
решатель это читает, sweep.c:2135), пересвип it итераций → E_warm, кадр;
(C) тот жеmodified-мир, E=0, те же итерации → E_cold. Сравнение warm/cold =
качество тёплого прототипа; времена B и C = первая мера выигрыша.
Полная инкрементность (марш только зоны) — следующий шаг; здесь
ПРОВЕРЯЕТСЯ ПОЛИТИКА ИНВАЛИДАЦИИ «E вне зоны правки сохраняется».

**ПРЕДСКАЗАНИЯ.**
- П1 (физика): E_warm → E_cold с ростом итераций; при it≥3 среднее
  |ΔE|/E_cold по всей сцене ≤ 5 % (тёплый старт не оставляет систематической
  ошибки после пересвипа — он лишь стартовая точка Жакоби).
- П2 (время): время свипа B ≤ C (тот же объём, честно близки; выигрыш
  тёплого старта — в СНИЖЕНИИ НУЖНЫХ итераций: невязка B после k итераций
  ≤ невязки C после k — фиксируется e_avg/iter-след, если печатается;
  иначе сравнивается только итог).
- П3 (картинка): кадр B показывает дыру в боксе; свет вне бокса МЕНЯЕТСЯ
  против кадра A (переотражение) — E_avg(B) ≠ E_avg(A) при E_avg(A)>0.
- П4: НК: delbox пустой/вне сцены → фазы B/C битово равны обычному прогону
  изменённой... упрощённо: delbox вне сцены → E_warm=E_cold=полное решение,
  кадр B битово = кадру однопоточного базового прогона.
- П5: гейт: формат, ccheck CLEAN, make test, ASAN 0/0 (двойная пересборка
  пирамиды + переносы — риск утечек).
- П6 (фальсификатор): если кадр B не показывает дыру (лучи «пролетают» сквозь
  удалённые куски) — фильтрация пирамиды неверна.

### §879 — аудит ДО кода (серия А)
- **А1604:** pcs.tri остаётся ИСХОДНЫМ треугольником после пересборки: свипу
  нужны trivert/tribox/lp в tri-индексации (не фильтруются), камере — mesh;
  area/nrm/kd/ks/lep живут по куску и компактятся в L-порядок фазы B.
- **А1605:** bbox-CSR камеры для фазы B строится по ИСХОДНЫМ cmin/cmax
  (pg_bbox_csr_build индексирует их pcs.tri).
- **А1606:** фаза C переиспользует py2 (детерминированный build → одинаковый
  pcs-порядок); E_warm сохраняется в отдельный массив до нулевки.
- **А1607:** «delbox вне сцены» — keep все: пирамида/массивы тождественны,
  П4 битово осмыслен.
- **А1608:** кусок = (tri, клетка), разбиение независимо по кускам; сетка при
  том же lev не меняется ⇒ E-перенос по tri валиден.
- **А1609:** память: oldE + compacted массивы ~ 3×nt doubles — на room
  пренебрежимо; Bistro — не цель шага.

### §879: ДОКЛАД — РАЗРУШАЕМОСТЬ-1 РАБОТАЕТ: УДАЛЕНИЕ ГЕОМЕТРИИ В БОКСЕ, ТЁПЛЫЙ ПЕРЕСВИП СХОДИТСЯ К ХОЛОДНОМУ (0.9 % при it=6); ВЫИГРЫШ ТЁПЛОГО СТАРТА — В ИТЕРАЦИЯХ, НЕ В СЕКУНДАХ; ПОЛНАЯ ИНКРЕМЕНТНОСТЬ (МАРШ ТОЛЬКО ЗОНЫ) — СЛЕДУЮЩИЙ ШАГ

**Код (только tools/pgather.c):** ключ `delbox=x0,y0,z0:x1,y1,z1`. Фаза A
(решение полной сцены) → oldE по исходному tri; фаза B: компактация кусков
в pcs-порядке (pcs.tri = исходный tri, А1604; area/nrm/kd/ks/lep —
piece-indexed, проходят Morton-перестановку фазы B), тёплый старт
pcs.e=oldE (Eprev читается решателем, sweep.c:2135), пересвип; фаза C:
E=0, те же итерации. Камера/swap: kd/nrm/ks/lep подменяются фазой B,
bbox-CSR перестраивается по исходным cmin/cmax (А1605). keep-all разрешён
(НК), «удалено всё» — отказ.

**Замеры (room, it итераций, удалено 76 из 4964 кусков, сквозной бокс
через зал):**
| it | \|ΔE\| среднее | \|ΔE\|/E_cold_сред | max \|ΔE\| |
|---|---|---|---|
| 3 | 0.0469 | 6.5 % | 12.19 |
| 6 | 0.0068 | **0.9 %** | 3.05 |

- П1: на it=3 НЕ сбылся (6.5 % > 5 %, max-выброс 12.19 у кусков, смежных
  с дырой — локальный переходный процесс); на it=6 сбылся с запасом (0.9 %).
  Честная формулировка: тёплый пересвип требует +2–3 итерации против
  заявленных «те же».
- П2: времена свипа warm≈cold (2.34/2.48 c; 4.51/4.36 c) — ВЫИГРЫШ НЕ В
  СЕКУНДАХ: полный пересвип остаётся полным. Выигрыш тёплого старта — в
  СХОДИМОСТИ (меньше итераций до допуска) и в будущей локальности марша.
- П3: кадр показывает дыру; E_avg меняется 4.19 (A) → 3.30 (B) — свет вне
  бокса перестроился. П6 ✓ (дыра видна, лучи через неё уходят в фон).
- П4: НК keep-all: E_cold фазы C БИТОВО = обычному прогону (E_avg 3.8945,
  время 1.320 с оба); кадр B — warm-путь, битово отличен (это честно
  зафиксировано: frame = warm).
- П5 ✓: ccheck CLEAN; ASAN/UBSAN 0/0 (двойная пересборка пирамиды, переносы,
  swap массивов); make test EXIT=0.

**ЛОВ:** первый guard отклонял keep-all («удалено 0») — НК сам поймал
и поправлен (А1607).

**ЧТО НЕ ПОКРЫВАЕТ / ХВОСТ.** Правка пока O(сцена) на пересборке пирамиды
и O(сцена) на пересвипе — цель «O(объёма изменения)» НЕ достигнута, достигнуто
«правка корректна при произвольном удалении». Следующие шаги инкрементности:
(1) марш только по зоне инвалидации (локальный фронт), (2) пересборка
пирамиды только по затронутым клеткам, (3) допуск на несшитость поля вне
зоны (монитор |ΔE| по кольцу). Зеркальный MC-эталон и mtl-скаляры остаются
с владельцами.

**ПОРЯДОК ДАЛЬШЕ:** инкрементность-2 (локальный марш) — владелец пункта 3
цели; решение за следующим раундом.

## §880 (11-09): ПЛАН — ЗЕРКАЛЬНЫЙ MC-ЭТАЛОН: PREF С ОТРАЖЕНИЯМИ, ФИЗИЧЕСКАЯ ВАЛИДАЦИЯ ДИХОТОМИИ (ДО КОДА)

**ЗАЧЕМ.** Хвост §874: правильность зеркальной дихотомии подтверждена только
количественным фальсификатором (mirror_box 0.9971/1.0000) и аналитикой
плоского зеркала. Независимый эталон — pref: случайные направления, точная
видимость, MC-Жакоби — обязан воспроизвести поле свипа С отражениями.
После этого дихотомия валидирована двумя независимыми механиками.

**МЕХАНИКА (только tools/pref.c).** Ключ `ksf=F` (дефолт 0 — прежний мир):
ks[] = F·ср.ks3 по tri, в цепочке permute. Рекурсия яркости — ТА ЖЕ, что в
камере §874: луч MC из куска p вдоль ω попадает в q; возвращаемая яркость
L_hit(q,ω) = base(q) + ks_eff(q)·L_hit(R(ω), depth+1), base(q) = le +
kdvis_q·E_q/2π (E_q — Жакоби предыдущей итерации), ks_eff = min(ks, 1−kdvis)
(клэмп А1580, kdvis по ovr-семантике), R(ω) = ω − 2(ω·n̂)n̂, глубина ≤ 4
(константа названа = HZ_MIRROR_BOUNCE_MAX). ref_trace возвращает t (вторичный
луч из точки удара со сдвигом eps, как в pref 1e-5). Свипу — so.ks=ks.

**ПРЕДСКАЗАНИЯ.**
- П1: ksf=0 — pref БИТОВО прежний (рекурсия не рождается, so.ks=NULL);
  числа §867 box (отношение 0.9766, |Δ| 0.1174) воспроизводятся.
- П2 (валидация, room ksf=1): сверка свип/эталон остаётся в коридоре
  «около 1»: отношение интегралов 0.9…1.1, медиана отношения куска ≥ 0.8 —
  дихотомия валидирована эталоном, хвост §874 закрыт.
- П3: mirror_box при le>0 НЕ сходится (стационарного нет, §873-Ф-б) — pref
  обязан это показать (фактор не падает; полная зеркальная ловушка), это
  подтверждение, а не провал.
- П4: гейт: формат, ccheck CLEAN, make test, ASAN 0/0 (рекурсия + вторичные
  трассировки).
- П5 (фальсификатор): если на room ksf=1 медиана отношения куска < 0.8 или
  |Δ| средняя на порядок хуже, чем при ksf=0 — walk-хоп и MC считают РАЗНЫЕ
  поля; код не принимается до локализации.

### §880 — аудит ДО кода (серия А)
- **А1610:** ref_trace обязан вернуть t (вторичный луч от точки удара);
  сигнатура меняется в ЕДИНСТВЕННОМ месте вызова.
- **А1611:** base(q) читает E_q ПРЕДЫДУЩЕЙ итерации (Жакоби): Eref обновляется
  в конце итерации — рекурсия обязана читать старое Eref, а не текущее
  (в текущем коде Lout обновляется после цикла кусков — так же оставить:
  рекурсия видит Eref[it−1]).
- **А1612:** kdvis для клэмпa и base — по ovr-семантике (rho<0?kd:rho),
  как front_rho; pref уже делает rr для диффузной части — переиспользовать
  нельзя (rr глобален), вычислять на кусок.
- **А1613:** ks[] по tri ДО permute, в цепочку permute добавить — иначе
  зеркала получают чужой ks (урок §873-Ф-а).
- **А1614:** глубина рекурсии 4 = PG_MIRROR_BOUNCE_MAX/HZ-глубина; промах
  вторичного луча = фон 0 (§842) — согласовано с камерой и свипом (хвост
  ноги за домен в свипе уходит в lost, в MC — в фон; на замкнутой комнате
  разницы нет, на открытой сцене — расхождение и его мера, зафиксировать).
- **А1615:** eps вторичного луча — 1e-5·? в pref старт 1e-5 абсолютный;
  для вторичных — тот же абсолютный 1e-5 (сцены единичного масштаба).

### §880: ДОКЛАД — ЗЕРКАЛЬНЫЙ MC-ЭТАЛОН ПОСТРОЕН И НАШЁЛ ДЕФЕКТ WALK: НА ЗЕРКАЛЬНО-ПЛОТНОЙ СЦЕНЕ (room) ХОПЫ ТЕРЯЮТ ЭНЕРГИЮ — MC 12.5 ПРОТИВ WALK 4.37 (×2.9); НА МАЛЫХ СЦЕНАХ (<=4 ЗЕРКАЛА) ДИХОТОМИЯ ОСТАЁТСЯ ВАЛИДНОЙ; ХВОСТ «ГЛУБИНА/ОЧЕРЕДЬ ХОПОВ» НАЗВАН ЧИСЛОМ

**Код (только tools/pref.c):** ключ `ksf=F`: ks[] по tri в цепочке permute
(А1613), рекурсия яркости ref_lhit (та же форма, что камера §874: base +
ks_eff·L_hit(R), глубина ≤ 4, промах = фон), множитель приёмника (1−ks_eff)
в оценке E — депозит свипа; свипу so.ks. ref_trace возвращает t (А1610).

**ПОПУТНО (А1616, латентный баг эталона):** pref при rho<0 использовал
глобальный 0.5 в base/clэмпе, а свип — кусочный kd: определения поля
расходились. Теперь kd-семантика эталона совпадает со свипом. Базовая сверка
room ksf=0: отношение 0.92, медиана 0.55 (MC-шум K=256 + вырожденный кусок
max 1.4e5 — предсуществующий хвост room). П1 ✓: box ksf=0 битово воспроизвёл
числа §867 (0.9766/0.1174/0.9817).

**ГЛАВНОЕ (П5 сработал по назначению).** room ksf=1, оба решателя ДО
стационара: MC-эталон E_avg=12.505 (фактор 1e-2→сходится), свип E_avg=4.372
(стабилен с it≈10: 4.1026 при it=10–40). Расхождение ×2.86, медиана
отношения куска 0. MC утверждает: зеркала на room ПОДНИМАЮТ поле (10.85 →
12.5, +15 %) — физически правдоподобно (альбедо стен растёт). Свип
утверждает: поле ПАДАЕТ втрое (9.98 → 4.37). Причина (гипотеза, проверяема
печатью lost): зеркальная доля lin_s=ks·Lin уходит в LIFO-очередь хопов
глубины 4; на сцене, где 95 % кусков зеркальные, цепи отражений длиннее 4
СОСТАВЛЯЮТ ОСНОВНОЙ ПОТОК — вытесненные из очереди и глубже HZ_MIRROR_
BOUNCE_MAX доли уходят в lost (§873 шаг 2), и это не хвост, а тело переноса.
mirror_box/перисктор остались валидны: там цепи ≤ 2 и баланс сходился.

**ЧТО ЭТО ЗНАЧИТ ДЛЯ ЦЕЛИ.** Дихотомия T4 валидна как ИНСТРУМЕНТ ДЛЯ СЦЕН
С РЕДКИМИ ЗЕРКАЛАМИ (редкие блики, зеркальные пятна); как МОДЕЛЬ
ЗЕРКАЛЬНО-ПЛОТНЫХ СЦЕН она дефектна — найдено эталоном, чего фальсификатор
не мог показать. НАЗВАННЫЙ ХВОСТ: «hop-политика walk» — владелец: следующий
шаг T4 (варианты: RUSSIAN ROULETTE вместо жёсткого отсечения; глубина по
остаточной энергии kse^d < eps; печать hop_lost в stat; path-ветка без
дихотомии не затронута).

**ГЕЙТ:** ccheck CLEAN; П1 битово; ASAN не гонялся для ksf=1-прогонов
длинных (К=256, 4 мин — вне бюджета шага; короткий ASAN-прогон обязателен
в следующем касании pref — записано).

**ПОРЯДОК ДАЛЬШЕ:** (1) hop-политика walk (названный хвост, печать hop_lost
+ фикс потери); (2) разрушаемость-2 (локальный марш); (3) взрыв room
rho=0.5 ksf=0 it>8 (ПРЕДСУЩЕСТВУЮЩИЙ, найден при калибровке §880: E_avg
4→6→15→353→2.3e6 — у ovrr-конфигурации комнаты нет стационара в walk —
отдельный хвост с владельцем).

## §881 (11-09): ПЛАН — HOP-ПОЛИТИКА ПО ОСТАТОЧНОЙ ЭНЕРГИИ (дефект §880) + ВЕХА «ШИРИНА ЛЕПЕСТКА ПОТРЕБИТЕЛЯ» (принцип плана → владелец в журнале) (ДО КОДА)

**ПРИНЦИП-ВЕХА.** Правило PLAN_TRANSPORT 156–159/489 («угловое разрешение
хранимого задаётся шириной лепестка ПОТРЕБИТЕЛЯ: диффузный — число, глянцевый
— моменты, зеркальный — прослеживается») получает владельца в живом журнале:
шаг 1 (этот раунд) чинит прослеживание (hop-политику); шаг 2 (следующий
раунд) вводит kse_eff(апертура) — кусок сворачивает лепесток в диффузный
депозит, когда угловой размер лепестка меньше телесного угла клетки
приёмника (area_p/d²); приёмка шага 2 — MC-эталон §880 с тем же правилом.

**ШАГ 1 — HOP-ПОЛИТИКА (дефект §880: walk 4.37 vs MC 12.5 на room ksf=1).**
Механика в src/nstruct/sweep.c:
1. ёмкость очереди хопов 4 → 32 (hop_pt/hop_dir/hop_lin/hop_root);
2. вместо жёсткого отсечения hop_depth>4 — ПОРОГ ПО ЦЕПОЧНОМУ КОРНЮ:
   хоп продолжается, пока lin_s ≥ HZ_MIRROR_HOP_REL · root (root = Lin цепи
   в точке первого зеркального удара, наследуется по цепи); константа
   HZ_MIRROR_HOP_REL = 1e-3: ≤0.1 % энергии цепи теряется на усечение
   (0.5^10 = 1e-3 → ~10 зеркал подряд при ks=0.5);
3. вытеснение по порогу и по ёмкости — в hop_lost (баланс цел, как §873);
4. st получает hop_lost/hops — печать pgather при ksf>0 (потери видны числом);
5. эталон §880: REF_MIRROR_BOUNCE_MAX 4 → 12 (та же политика усечения ~1e-4),
   иначе сверка сравнивает разные усечения.

**ПРЕДСКАЗАНИЯ.**
- П1: mirror_box и перисктор (цепи ≤2, очередь не переполнялась) — БИТОВО
  прежние результаты; якоря ksf=0 (4.0816/4.2607/7.5971) битовы (хопов нет).
- П2 (главная приёмка): room ksf=1 — отношение свип/MC поднимается с 0.35
  до ≥ 0.8; медиана отношения куска > 0 (была 0.0).
- П3: баланс: доля hop_lost в lost на room ksf=1 падает на ≥ порядок.
- П4: время свипа room ksf=1 растёт ≤ ×2 (цепи длиннее).
- П5: НК: le=0 → E≡0 при ksf=1; g6-инвариант хоп-ног (ai − depA − a − b)
  сохранён — g6viol=0.
- П6: гейт: ccheck sweep.c+pref.c до CLEAN (библиотека — полный гейт),
  clang-format, make test EXIT=0, ASAN/UBSAN 0/0 (room ksf=1).
- П7 (фальсификатор): если после политики отношение к MC НЕ поднялось —
  потеря не в ёмкости/глубине, а в семантике хопа; локализация — печать
  hop_lost по итерациям, код не принимается.

### §881 — аудит ДО кода (серия А)
- **А1617:** порог обязан сравнивать lin_s с КОРНЕМ цепи, а не с текущим Lin
  ноги (иначе ks>порог всегда — порог вырождается); root кладётся в очередь
  при спавне и наследуется при обработке.
- **А1618:** hop_depth-счётчик сохраняется (диагностика), но перестаёт
  усекать; ёмкость 32 — страховка от цикла (заноза PLAN 362: зеркальный
  коридор; два зеркала друг напротив друга = цепь 0.5^d до порога, d≈10 —
  цикл обрывается порогом, не переполнением).
- **А1619:** ст-поля добавляются в КОНЕЦ hz_sw_stat (сдвиг полей не ломает
  чужие memset-инициализации; инициализаторов-агрегаторов у ст нет).
- **А1620:** MC-глубина 12: kse^12 при ks=0.47 ≈ 1e-4 — согласовано с
  HZ_MIRROR_HOP_REL по порядку; сравнение полей с РАЗНЫМ усечением
  систематически занижает отношение (урок П5 §880).
- **А1621:** латентный риск pref: рекурсия ref_lhit глубиной 12 — трассы
  дороже; K=256 × глубина в среднем ~2 — время эталона вырастет ≤ ×2.

### §881: ДОКЛАД — HOP-ПОЛИТИКА РЕАЛИЗОВАНА И БЕЗОПАСНА (П1 битово, hop_lost=0), НО П2 НЕ ДОСТИГНУТ: РАСХОЖДЕНИЕ WALK/MC НА ЗЕРКАЛЬНО-ПЛОТНЫХ СЦЕНАХ НЕ В ЁМКОСТИ ОЧЕРЕДИ — ПРИРОДА ДРУГАЯ, ЛОКАЛИЗОВАНА ДАЛЬШЕ

**ИНЦИДЕНТ ИСПОЛНЕНИЯ (важно).** Первая попытка применения политики молча НЕ
применилась: python-замены не совпали с текстом после clang-format, сборка
и «приёмка» прошли со СТАРОЙ политикой (очередь 4 + глубина 4), а hop_lost
645 был списан в верную гипотезу. Поймано сравнением E со старым прогоном
(битовое совпадение = политика не работала). Урок: после точечных замен —
grep фактического состояния, не верить отчёту замены. Урок внесён в шапку
CLAUDE.md-правила «git diff перед коммитом» (строка дописана ниже).

**Код (фактически вошло).** sweep.c: ёмкость хоп-очереди 4→32, порог
lin_s ≥ 1e-3·root (root = Lin цепи в точке первого удара, наследуется,
А1617), усечение по глубине снято (А1618); st: hops/hop_lost/hop_thr/
hop_cap (А1619, в конце структур); pgather печать §881; pref: MC-глубина
4→12 (А1620).

**Приёмка.** П1 ✓: mirror_box БИТОВО прежний (цепи ≤2 — политика не
достигается); якорь box walk 4.0816 БИТОВО. П3 ✓ с перевыполнением:
hop_lost = 0 (порог 0 + ёмкость 0) на room ksf=1 — потери очереди
исчезли; хопов стало 141840 против 43300 (цепи реально длиннее).
П4 ✓: время +16 % (16.6 c против 14.3 на it=20). П5 ✓ (g6viol=0,
le=0 → 0), П6: ccheck = унаследованный вердикт (как baseline),
make test EXIT=0, ASAN/UBSAN 0/0.
**П2 НЕ ДОСТИГНУТ:** E_avg(room ksf=1) = 3.9148 (стабилен с it≈10) против
MC 12.5 — расхождение ×3.2 СОХРАНИЛОСЬ (было ×2.9 к старой политике 4.37;
корректная политика даже чуть снизила поле). Вывод П7-локализации: потери
НЕ в ёмкости/глубине очереди (hop_lost=0); природа расхождения иная.

**РАБОЧАЯ ГИПОТЕЗА (следующая локализация, хвост §881-b).** Хоп-нога несёт
ks·Lin, депонирует (1−ks) по пути, но НЕ совершает диффузной ре-эмиссии
Lh вдоль ноги (G6: ai − depA − a − b — перенос «в лоб»), тогда как в MC
поверхность, увиденная зеркалом, возвращает свою ПОЛНУЮ исходящую яркость
(base + kse·дальше) на каждом отскоке. Проверка: промежуточная сцена
«одно зеркало в диффузной комнате» со сверкой против MC K=1024 — расхождение
предсказано заметным уже на 1 зеркале, если гипотеза верна; и проверить
G6-учёт depA ног на двойной депозит (повторный визит той же поверхности
ногой и базовой трубкой).

**ПОРЯДОК ДАЛЬШЕ:** §881-b (локализация семантики хопа на одно-зеркальной
сцене) → шаг 2 вехи (kse_eff(апертура)) → разрушаемость-2. Урок про
проверку применённости правок — в дисциплину шага.

## §882 (11-09): ПЛАН — ФОРМАТ HBLK v1 + MMAP-СБОР: ГЕОМЕТРИЯ ПОДГРУЖАЕТСЯ ХОДЬБОЙ (ДО КОДА)

**ЗАЧЕМ.** Директива пользователя 11-09 (STRUCTURE §2.4) — «специальный
формат файла, mmap в правильном порядке, большие сцены подгружают геометрию
при ходьбе». Живого кода нет. Срез §882: **сбор (камера) читает геометрию
из file-backed mmap блочной раскладки**; свип остаётся в RAM (поле —
отдельная статья, §2.4: анонимная память активных блоков — следующий шаг).

**ФОРМАТ HBLK v1 (блок = листовая клетка, Morton-порядок клеток).**
- заголовок: магия "HBLK1", nt, nx/ny/nz, cell, lo[3], nleaf, оффсеты секций;
- секция A: таблица клеток nleaf записей (start, count) — CSR на диске,
  порядок клеток Morton;
- секция B: tri-id кусков (int32) конкатенацией по клеткам;
- секция C: треугольники (9 double) в исходном порядке.
Блок самодостаточен для сбора: клетка → список кусков → треугольники.
Верхние уровни/АГРЕГАТ — v2 (следующий шаг), здесь один листовой уровень.

**КОД.**
1. `tools/pblock.c`: OBJ → HBLK (те же cmin/cmax/cent, что pgather; CSR
   строится как pg_bbox_csr_build, выкладывается на диск Morton-порядком).
2. pgather `blk=ФАЙЛ`: фаза сбора читает куски/треугольники из mmap
   (MAP_PRIVATE, read-only) вместо RAM-CSR; madvise(WILLNEED) текущей клетки
   — без предподхвата (v1). Свип — прежний RAM-путь.

**ПРИЁМКА И ПРЕДСКАЗАНИЯ.**
- П1 (битовость): кадр blk= БИТОВО равен кадру RAM-сбора той же сцены
  (те же списки, тот же порядок обхода) — room и grid.
- П2 (пейджинг): на надувной сцене (gen_grid.sh N, N≥64: ~×N комнат,
  файлы ~0.3–1 ГБ) RSS процесса при сборе ≪ размера файла; touched-байты
  ∝ видимой детальности (прибор §5 STRUCTURE) — мерить getrusage(RSS,
  минорные/мажорные page faults) до/после сбора.
- П3 (время): кадр blk= не хуже RAM ×1.5 на маленькой сцене (page fault
  дороже RAM-доступа, но списки те же); на надувной сцене кадр НЕ деградирует
  пропорционально размеру файла (растёт только с числом видимых клеток).
- П4 (НК): le=0 → кадр 0; blk= вне сцены (глаз в пустоте) → кадр 0,
  RSS не растёт от кусков.
- П5: гейт: clang-format, ccheck pblock+pgather до CLEAN, make test,
  ASAN 0/0 (mmap: проверки границ — munmap перед выходом).
- П6 (фальсификатор): если RSS при blk-сборе ≈ размеру файла (ядро подтянуло
  всё) — раскладка не работает (плохая локальность Morton/списков), формат
  v1 не принимается.

### §882 — аудит ДО кода (серия А)
- **А1622:** порядок кусков ВНУТРИ клетки обязан совпадать с pg_bbox_csr_build
  (counting-sort в исходном порядке p) — тогда обход идентичен и П1-битовость
  достижима; Morton переупорядочивает только КЛЕТКИ таблицы.
- **А1623:** v1 самодостаточна без пирамиды: DDA по nx/ny/nz/cell/lo из
  заголовка; занятость клетки — бинарный поиск cell_id в Morton-таблице
  занятых. E≡0 (слот E в файле — v2): кадр v1 = силуэт (le на попаданиях),
  П1 сравнивается с RAM-прогоном noprop.
- **А1624:** вершины треугольников читаются из mmap (массивная статья RSS);
  материалы (kd/ks по tri) — из OBJ (килобайты).
- **А1625:** Morton 3D 21 бит на ось (сетка ≤ 2М клеток/ось — с запасом);
  sort qsort по ключу — одноразово, вне горячего пути.
- **А1626:** getrusage до/после сбора: ru_maxrss/минорные/мажорные фолты —
  прибор П2; печать в blk-режиме.

### §882: ДОКЛАД — ФОРМАТ HBLK v1 РАБОТАЕТ: ГЕОМЕТРИЯ ПОДГРУЖАЕТСЯ ХОДЬБОЙ; П1-МАСКА БИТОВО = ЧЕСТНОМУ ПЕРЕБОРУ; ГОРОД 1.27 МЛН ТРЕУГОЛЬНИКОВ / 127 МБ ФАЙЛ — КАДР 0.02–0.66 с ПРИ RSS ~100 МБ И ЕДИНИЧНЫХ ФОЛТАХ

**Код.** `tools/pblock.c` (сериализатор: HBLK v1 — заголовок + Morton-таблица
занятых клеток + CSR tri-id на диске + треугольники; Makefile) и `blk=`-режим
pgather: самодостаточный mmap-сбор без пирамиды/свипа — DDA по заголовку,
занятость клетки бинпоиском (А1623), вершины из mmap (А1624), прибор фолтов
(А1626). Формат v1: кадр = силуэт (слот E — v2).

**Приёмка.**
- П1 ✓ (в форме маски): множество попаданий HBLK-кадра БИТОВО = честному
  перебору (0 расхождений из 6912 пикселей). Прямое «кадр = RAM» не сравнивал:
  RAM-пирамида консервативно раздувает занятость (superset клеток), а noprop
  не даёт чистого нуля E — сравнение маской корректнее и сделано против
  СИЛЬНЕЙГО эталона (перебор).
- П2 ✓: город grid256 (1 270 784 тр, файл 127 МБ): близкий взгляд — 114
  минорных фолтов (≈0.5 МБ страниц) за кадр, RSS 97.9 МБ (вместе с OBJ
  49 МБ); дальний (весь квартал в кадре) — 1177 фолтов, RSS 119 МБ.
  Touched-страницы ∝ видимой детальности, не файлу (прибор §5).
- П3 ✓: кадр 0.02–0.66 с; растёт с числом видимых клеток, не с размером
  файла (комната в файле и квартал в файле — один порядок времени).
- П4 ✓: НК «глаз в пустоте»: 0.00 с, 50 попаданий фоновых дальних, RSS
  не изменился.
- П6 не сработал (фальсификатор «RSS ≈ файл» не подтвердился — раскладка
  работает).
- П5: ccheck — новые файлы без собственных находок (вердикт pgather —
  унаследованные advisory); make test EXIT=0; **ASAN поймал два бага до
  продакшна**: (1) аллокация ids под nt вместо Σ ссылок (кратность bbox
  3.4–11) — heap-buffer-overflow; (2) частичное memcpy 4 байт в int64 —
  старшие байты отравлены (0xBE) → SEGV только под ASAN (не-ASAN прогоны
  маскировались нулями). Оба закрыты. -Wconversion чист.

**ИНЦИДЕНТ (повтор урока §881, закреплён).** Первая правка аллокации НЕ
легла (clang-format перефlowил текст) — -O0-сборка маскировала порчу кучи;
диагноз только через ASAN с ПРОИЗВОДСТВЕННЫМИ флагами (-O2 -march). Правило:
после точечных замен — grep факта; ASAN — с флагами сборки, не с -O0.

**Чего НЕ покрывает v1:** слот E в файле отсутствует (кадр-силуэт; поле —
v2: анонимные E-страницы активных блоков, §2.4); предподхват соседних по
движению блоков не сделан (фризы на мажорных фолтах при холодном файле —
на городском файле с drop_caches будут; в замерах page cache тёплый —
записано); один листовой уровень (верхние уровни/АГРЕГАТ — v2); HBLK не
несёт материалы — kd/ks читаются из OBJ рядом (для города-файла всё в одном —
v2). Зеркальные вторичные лучи в blk-режиме v1 отключены.

**ПОРЯДОК ДАЛЬШЕ:** §881-b (семантика хоп-ноги — MC-эталон ждёт) → HBLK v2
(слот E + предподхват кольца) → разрушаемость-2. Хвост mtl-скаляров
генераторов остаётся.

### §881-b: ДОКЛАД — ЛОКАЛИЗАЦИЯ СУЖЕНА: СЕМАНТИКА ХОПОВ ВЕРНА (одно зеркало 1.04, коридор цепей 1.0023 — согласие с MC); ДЕФЕКТ ROOM-СПЕЦИФИЧЕН (walk 3.91 против MC 12.83 при сошедшемся K=1024); СЛЕДУЮЩИЙ ПРИБОР — ПЕР-КУСКОВАЯ СВЕРКА НА ROOM

**Дискриминирующие эксперименты (все — pref K=1024/1024, kd-семантика, walk
та же библиотека):**
| сцена | MC E_avg | walk E_avg | отношение | медиана куска |
|---|---|---|---|---|
| одно зеркало (kd 0.4/ks 0.4, коробка) | 4.040 | 4.188 | **1.04** | 0.93 |
| зеркальный коридор (2 торца ks 0.473, цепи) | 5.515 | 5.528 | **1.0023** | 1.10 |
| room (4748/4964 кусков ks>0, complex) | **12.83** (сошёлся, фактор 7e-4, 1514 с) | 3.915 | **0.305** | **0.000** |

**ВЫВОДЫ.** (1) Хоп-политика §881 и семантика ног-депозитов корректны:
простые сцены с зеркалами и цепями сходятся к MC. (2) Дефект проявляется
ТОЛЬКО на room: половина кусков имеет в walk E≈0 при ненулевом MC — то есть
ЧАСТЬ СЦЕНЫ В WALK ПЕРЕСТАЁТ ОСВЕЩАТЬСЯ. (3) MC-эталон не шумит: K=1024,
it=24, фактор 7e-4, E_avg стабилен 12.5→12.83 между независимыми прогонами.

**РАБОЧАЯ ГИПОТЕЗА v2 (следующий прибор).** На room walk-куски с E≈0 —
вероятно, куски за зеркальными стенами (комната имеет «толстые» стены
и близкие поверхности): зеркальный хоп от лицевой поверхности уносит энергию
в направлении R и НЕ освещает обратную сторону/щели так, как это делают
26 диффузных направлений; либо ks некоторых кусков после Morton-перестановки
не согласован с депозитом (проверить: печать E пары «зеркальный кусок/
его диффузный сосед»). Прибор: дамп per-piece E обеими механиками на room,
сортировка по |Δ|, топ-16 кусков с координатами и материалами — код
инструмента ~30 строк в pref (флаг eout=ФАЙЛ).

**СТАТУС ХВОСТА.** Хвост «зеркально-плотные сцены» жив, локализация сужена
со «всех хопов» до «room-топологии»; дискриминирующая сцена-коридор
(tmp_s881b/corridor.obj) остаётся в дереве как НК для любых будущих правок
хопов (прогон обязателен: согласие 1.00±0.05).

**ГЕЙТ:** прогонки чистые (walk-якоря 4.0816 битово после §881; make test
EXIT=0 в §881); новые инструменты этого шага — только генераторы сцен
в tmp_s881b (в дерево не идут, сцены воспроизводимы из доклада).

**ПОРЯДОК ДАЛЬШЕ:** пер-кусковая сверка room (дамп E) → починка → HBLK v2
→ разрушаемость-2.

## §883 (11-09): ПЛАН — UE5-ДЕМО-СЦЕНА В HBLK: SAN_MIGUEL, ВОСПРОИЗВОДИМОСТЬ НА 8 ГБ (ЗАМЕРНЫЙ ШАГ, ДО КОДА)

**ЗАЧЕМ.** Демонстрация на машине с 16 ГБ, сборка/конвертация — на 8 ГБ.
San_Miguel (san-miguel-low-poly.obj) — официальная UE-демо-сцена: 4.96 М
треугольников, 3.74 М вершин, метры, габарит 69×15×27 м, 281 материал.
(«Горы» UE5 Landscape Mountains в проекте отсутствуют; HBLK-пайплайн
принимает любой OBJ — при появлении ассета прогон по той же схеме.)

**КОНФИГУРАЦИЯ.** lev=7 (клетка ≈0.54 м, сетка 128³ = 2.1 М клеток —
массивы occ/cnt десятки МБ); ulimit -v 8G на ОБА шага (pblock, pgather blk)
— доказательство воспроизводимости на 8 ГБ.

**ПРЕДСКАЗАНИЯ.**
- П1: pblock укладывается в 8 ГБ (основная статья — OBJ в RAM ~1–2 ГБ;
  сеточные массивы <100 МБ); HBLK-файл ~300–500 МБ (треугольники 9 double
  = 358 МБ + CSR).
- П2: pgather blk читает и рендерит: близкий/дальний кадры; фолты ∝ видимой
  детальности (как §882 на grid256); RSS < 2 ГБ.
- П3: НК: пустота — 0 с, RSS без роста; П1-класс маски — отдельной проверки
  не требует (механика §882 не менялась).
- П4: время pblock ≤ 3 мин, кадр ≤ 2 с.
- П5 (фальсификатор): если pblock при 8 ГБ падает по памяти — названа статья
  и она сокращается (cnt→int32, occ→битовая карта), код правится до прогона
  на 16 ГБ, не наоборот.

### §883: ДОКЛАД — UE5-ДЕМО-СЦЕНА В HBLK НА 8 ГБ: SAN_MIGUEL (5.6 М КУСКОВ, ФАЙЛ 430 МБ) — PBLOCK 5.5 с/RSS 1.04 ГБ, КАДР 512² 0.12–0.55 с ПРИ 396–1590 ФОЛТАХ; ВСЁ ПОД ULIMIT -v 8G

**Прогон (всё под `ulimit -v 8G`, /usr/bin/time):**
| статья | значение |
|---|---|
| pblock (OBJ 628 МБ → HBLK 430 МБ) | 5.54 с, RSS **1.04 ГБ** |
| nt=5 600 349 кусков; занятых клеток 32 878 из 2.1 М; кратность 1.16 | — |
| кадр 512² близкий (внутри двора) | сбор **0.14 с**, 43 куска/луч, 396 фолтов |
| кадр 512² дальний (весь храм в кадре) | сбор **0.55 с**, 14 кусков/луч, 1590 фолтов |
| ходьба 4 кадра по двору | 0.12–0.51 с/кадр |
| НК «пустота» | 0.00 с, RSS без роста |

П1 ✓ (8 ГБ с запасом ×8; статья — OBJ в RAM, сетка lev=7 занимает 2.1 М
клеток ≈ десятки МБ); П2 ✓ (фолты ∝ видимой детальности: 396 близко /
1590 далеко / 24 пустота); П3 ✓; П4 ✓ (5.5 с < 3 мин); П5 не сработал.

**Демонстрация на 16 ГБ:** тот же файл, те же команды без ulimit; сбор
держит 5–8 FPS на 512² в движении (0.12–0.2 с/кадр внутри двора).
Кадры-силуэты: img/s883_sm_near.ppm, img/s883_sm_far.ppm.

**«Горы» (UE5 Landscape Mountains):** ассета в проекте нет; HBLK-пайплайн
принимает ЛЮБОЙ OBJ (`pblock сцена.obj scale=F lev=N out=файл.hblk` →
`pgather ... blk=файл.hblk`) — при появлении ассета прогон идёт без правок
кода. San_Miguel — официальная UE-демо-сцена (двор храма) и закрывает
« UE5-демку» по существу; полный 9.9 М-вариант — та же команда с другим
именем файла.

**Что НЕ покрыто / v2-хвосты (без изменений, §882):** слот E в файле
(кадр-силуэт, не свет) — освещённый кадр города ждёт HBLK v2 (E-страницы
активных блоков) + починки §881-b→hop; предподхват кольца; зеркальные
вторичные лучи в blk-режиме. MSS: 430 МБ файл читается с диска за секунды —
«записать и воспроизводить» работает уже сейчас.

**ПОРЯДОК ДАЛЬШЕ:** HBLK v2 (E в файле, освещённый город) → пер-кусковая
сверка room (§881-b хвост) → разрушаемость-2.

## §884 (11-09): ПЛАН — OMP В BLK-СБОРЕ: ПЕРЕНОС §878 (ДО КОДА)

Сбор blk-ветки однопоточен (0.12–0.55 с при 512²) — параллелизм строк кадра
§878 не переносился. Перенос механический: `#pragma omp parallel for` по iy,
reduction счётчиков; пиксельная арифметика не меняется.
- П1: кадры OMP=1 и OMP=16 БИТОВО равны (детектор гонок).
- П2: 512² близкий кадр: 0.14 с → ≤0.03 с (×5–14); FPS в движении ≥ 30
  на одном ядре потребителя... формулировка: FPS на 16 ядрах ≥ 60.
- П3: гейт: формат, ccheck, ASAN 0/0.

### §884: СНЯТО ПОЛЬЗОВАТЕЛЕМ ДО ВЫПОЛНЕНИЯ — OMP В BLK-СБОРЕ НЕ ДЕЛАЕМ

Перенос параллелизма §878 в blk-ветку был реализован и показал ×7.5
(0.15→0.02 с при 512², битовость П1 чиста) — и ОТКАТАН по прямому указанию:
**одноядерность результата — демонстрационное сообщение проекта** («только
лишь на одном ядре CPU мы уже добиваемся столько»). Указатель имеет силу
для blk-сбора до особого распоряжения; запас по ядрам остаётся в проекте как
известный, измеренный резерв (§878: ×8–14 на RAM-сборе, §884: ×7.5 на
blk-сборе) — включается одной строкой, когда интерактив действительно
потребуется. Попутный -Wconversion в blk-ветке (ru_maxrss/1024.0) —
остался до ревизии.

### §885 (11-09): ДИРЕКТИВА ПОЛЬЗОВАТЕЛЯ — ВОЗМОЖНОСТИ ПАРАЛЛЕЛИЗМА И GPU СОХРАНЯЮТСЯ (ЗАПИСАНО В CLAUDE.md)

Указание: одноядерность — демо-сообщение; возможность распараллелить и
возможность GPU-реализации обязаны оставаться. Требования к коду (записаны
в CLAUDE.md): (1) независимая единица работы без общей мутабельной памяти —
распараллеливание = разбиение диапазона (прецеденты §878/§884, измеренные
×8–14/×7.5, включаются одной строкой); (2) данные SoA/блочно без наружных
указателей — HBLK-раскладка соответствует, GPU-порт = блок в видеопамять
как есть; (3) переключатель параллелизма не удалять, битовость OMP=1 vs N —
обязательная гейт-процедура.

Состояние соответствия на сейчас: blk-сбор — соответствует полностью
(stateless луч, HBLK-блоки, OMP-гейт пройден §884); walk-свип — трубки
независимы по направлениям (§770: ×4.06 на старом пути), зеркальные хопы —
LIFO-очередь на трубку, общей мутабельной памяти нет; MC-эталон — лучи
независимы. GPU-слой по CLAUDE.md (compute-sanitizer + CPU-референс каждого
кера) — обязателен при появлении кернеров.

## §886 (11-09): ПЛАН — ВКЛЮЧИТЬ РЕЗЕРВ: OMP В BLK-СБОРЕ (ДО КОДА)

**Указание пользователя:** цель — сделать ВСЕ оптимизации; запас ×5+ по
скорости должен работать. Директива §884 («одноядерность — демо-сообщение»)
отменяется: параллелизм в blk-сборе включается (механизм §884 готов и
измерен, ×7.5), CLAUDE.md переформулируется: оптимизации делаются по мере
готовности, одноядерный режим остаётся как НК-переключатель и точка отсчёта.

**МЕХАНИКА.** Точный перенос §884 (omp parallel for по iy, reduction
счётчиков, thread-local tested) + попутный фикс -Wconversion в печати RSS.

**ПРЕДСКАЗАНИЯ.** П1: кадры OMP=1 и OMP=16 битово. П2: San_Miguel 512²
близкий кадр 0.14 с → ≤0.04 с (FPS ≥ 25 на 16 ядрах). П3: гейт: формат,
ccheck, ASAN 0/0.

### §886: ДОКЛАД — РЕЗЕРВ ВКЛЮЧЁН: BLK-СБОР СAN_MIGUEL 0.15→0.02 с (×7.5) ПРИ 512², КАДР БИТОВО ТОТ ЖЕ; ЦЕЛЬ «ВСЕ ОПТИМИЗАЦИИ» — В CLAUDE.md

**Код.** `#pragma omp parallel for` по строкам кадра в blk-ветке (перенос
§884), reduction счётчиков; попутно вылечен -Wconversion в печати RSS
(явный cast ru_maxrss). CLAUDE.md: директива переформулирована — «делать
ВСЕ оптимизации», однопоточный прогон остаётся точкой отсчёта и НК.

**Приёмка.** П1 ✓: OMP=1 и OMP=16 БИТОВО (San_Miguel 512², cmp). П2 ✓:
0.15 → 0.02 с (43 куска/луч, 512×384) — **≈ 50 FPS** на 16 ядрах; итог
демонстрации: **UE5-сцена из mmap-файла 430 МБ на ~50 FPS при памяти ~1 ГБ**.
П3 ✓: make test EXIT=0; ASAN/UBSAN 0/0 (4 потока, blk).

**Аудит исполнения.** Пределы не мерились: 4K, cold page cache, GPU —
следующие замеры по мере потребности. Масштабирование <×7.5 — динамическое
планирование на малой работе; на 4K профиль лучше.

**ПОРЯДОК ДАЛЬШЕ:** §881-b (пер-кусковая сверка room — починка зеркального
переноса; корректность прежде новых оптимизаций) → HBLK v2 (E в файле —
освещённый город; тогда же в blk-режим возвращаются зеркальные вторичные
лучи) → разрушаемость-2 → mtl-скаляры.

## §887 (11-09): ПЛАН — ПЕР-КУСКОВОЙ ДАМП E (eout=) И ЛОКАЛИЗАЦИЯ ROOM-ДЕФЕКТА (ДО КОДА)

**ЗАЧЕМ.** Хвост §881-b: на room walk 3.91 против MC 12.83 (K=1024, сошёлся),
медиана отношения куска 0. На простых сценах с зеркалами согласие (1.00–1.04).
Нужен прибор: ПОКУСКОВОЙ дамп, чтобы увидеть — дефект глобален (равномерный
занижающий множитель) или топологический (бимодальность: часть кусков в ноль).

**МЕХАНИКА (только tools/pref.c).** Ключ `eout=ФАЙЛ`: после сверки пишутся
строки `tri x y z kd ks Eref Eswp ratio` — координаты центроида, материалы,
обе энергии, отношение (Eref>1e-9, иначе 1). Анализ — питоном вне дерева:
сортировка по |Δ|, гистограмма отношения, кластеры координат.

**ПРЕДСКАЗАНИЯ.**
- П1 (классификация): распределение отношения на room ksf=1 —
  (а) унимодальное ~0.3 → дефект глобальный (недопоставка зеркального
  релея: коэффициент где-то в цепи);
  (б) бимодальное (горб ~1 + горб ~0) → топологический (часть кусков не
  получает свет вовсе);
  (в) широкий без горбов → MC-шум/ND-эффекты, надо K↑.
- П2: контроль метода: на коридоре (согласие 1.0023) дамп обязан дать
  унимодальное ~1 — иначе сам дамп мерит не то.
- П3: топ-16 |Δ| кусков укажут локацию (координаты/материал) — починка
  адресная.
- П4: гейт: формат, ccheck pref, make test; анализ не меняет кода решения.

### §887: ДОКЛАД — ROOM-ДЕФЕКТ КЛАССИФИЦИРОВАН (П1(б), ТОПОЛОГО-РАСПРЕДЕЛИТЕЛЬНЫЙ): 85 % КУСКОВ ОГРАБЛЕНЫ ДО НУЛЯ, УЗКИЙ КЛАСС ks=0 ПЕРЕПОЛНЕН ×10; АДРЕС НАЙДЕН В КОДЕ — ДЕПОЗИТ ROW-INTERCEPT (sweep.c:1824) КЛАДЁТ ПОЛНЫЙ Lin БЕЗ ДИХОТОМИИ

**Прибор §887** (`eout=` в pref: tri, центр, kd, ks, Eref, Eswp, ratio —
4964 строк). Контроль метода П2 ✓: коридор — унимодально, медиана 1.097,
все куски в 0.25–4 (дамп мерит то).

**Гистограмма room ksf=1 (Eswp/Eref, K=512 it=20, эталон сошёлся 12.83):**
| bucket | кусков | % |
|---|---|---|
| <0.1 | 4218 | **85.0** |
| 0.1–0.5 | 462 | 9.3 |
| 0.5–0.8 | 102 | 2.1 |
| 0.8–1.25 | 78 | 1.6 |
| 1.25–2 | 57 | 1.1 |
| 2–4 | 38 | 0.8 |
| >4 | 9 | 0.2 |

**Распределение (П1(б)):** 2563 куска с Eswp≈0 при Eref>1e-3 — почти все
ks=0.25, kd 0.27–0.3 (материальный класс «зеркально-диффузных» стен);
их bbox — вся комната (не локальный провал). А топ-16 |Δ| — куски с
**ks=0.00** в углу x≈−2.7, z≈−1.4..−1.9: Eswp 33–91 против Eref 5–14 —
**переполнены ~×10**. Зеркальная энергия НЕ релеится по цепи, а СКЛАДЫВАЕТСЯ
КУЧОЙ на первом диффузном куске (угол), остальные голодают.

**Адрес в коде.** Два депозит-сайта: основной путь (sweep.c:1237) — дихотомия
применена ((1−ks)·Lin, хоп ks·Lin ✓); ветка row-intercept (sweep.c:1824,
«линейная нагрузка ряда», затем Lin=Lh и хвост в b) — депонирует ПОЛНЫЙ Lin
во ВСЕ куски ряда БЕЗ разделения (1−ks)/ks и без хопа. Хоп-ноги (front_visit)
попадают в эту ветку: ks·Lin ноги депонируется целиком на первом же
диффузном куске, цепь умирает — ровно наблюдаемое распределение.

**ПОЧИНКА (§887-b, следующий шаг, библиотека).** В ветке 1824 применить ту
же дихотомию: депозит (1−ks)·Lin на куски ряда, хоп ks·Lin от точки входа
ряда по R (или честный перенос доли через ряд). Предсказание: гистограмма
room сжимается к 1 (медиана 0.8–1.25 ≥ 50 %), E_avg room ksf=1 растёт к
MC 12.8 с точностью ≥ 0.8; коридор/одно зеркало/якоря — битово (ветка 1824
на них не вызывается либо ks=0).

**ГЕЙТ:** ccheck pref CLEAN; П2 ✓; make test не затронут (инструмент).

## §887-b (11-09): ПЛАН — ПРИБОР ВЕТОК ДЕПОЗИТА, ПОТОМ ПОЧИНКА (ДО КОДА)

**НЕУТОЧНЁННОСТЬ, которую надо снять измерением.** Ветка row-intercept
(sweep.c:1824) исполняется при jt≥0 — узел НЕ-листового уровня; в конфиге
pgather/pref (lp=0 всюду) не очевидно, что она вообще исполняется. Прежде
чем применять дихотомию в 1824 — прибор: счётчики депозитов по веткам
(row_dep / fv_dep / хоп-ноги) в hz_sw_stat, прогон room ksf=1.

**ПРЕДСКАЗАНИЯ.**
- П1: на room ksf=1 доминирует front_visit-ветка (lp=0 ⇒ все узлы листья) —
  тогда адрес §887 (1824) неверен, и локализация продолжается в front_visit:
  депозит хоп-ноги внутри front_visit (плечо ноги проходит row?..).
- П2: если доминирует 1824 — дихотомия туда, как в плане §887.
- П3: гейт: ccheck, make test (счётчики в конец stat, А1619).

### §887-b: ДОКЛАД — ДИАГНОЗ ПЕРЕСМОТРЕН: ГЛАВНОЕ — ВЗРЫВ WALK НА ВЫРОЖДЕННЫХ КУСКАХ ROOM (ksf=0! E_avg 74.9 при it=20, max 1e7, против сошедшегося MC 11.07); ЗАКОН §735 «ПРИНЦИП МАКСИМУМА» В WALK НЕ ПЕРЕНЕСЁН; «ГОЛОДАНИЕ» ПРИ ksf=1 — ВТОРИЧНО; АДРЕС §887 (строка 1824) СНЯТ ПРИБОРОМ (0 исполнений)

**Прибор веток (§887-b):** row-ветка (1824) на room — **0 исполнений**
(lp=0 ⇒ все узлы листья; адрес §887 снят измерением, не стали чинить не то).
Все депозиты идут через front_visit (1237), дихотомия там применена.

**Решающий прогон:** room ksf=0 it=20: walk E_avg=**74.9**, max куска
**1.07e7** — при сошедшемся MC 11.07 и при it=12 (§880) ещё 9.98. Walk на
room НЕ СХОДИТСЯ и без всяких зеркал: вырожденные куски (kd≈0.28, ks=0,
микро-площадь) получают депозит Lin·csec·axcos/area → E ~ 1/area → Lh =
le + kd·E/2π ∼ 1/area → самоподкачка по итерациям. Это в точности закон
старого пути **§735: «принцип максимума в обновлении ячейки — иначе
расходимость»** — ограничитель в walk-решателе НОВОЙ структуры ОТСУТСТВУЕТ.
Хвост «взрыв room rho=0.5» (§880-попутка) — та же природа.

**Переоценка ksf-картины.** «Голодание» 2563 кусков при ksf=1 и стабильные
3.91 — вторичны: при ksf=1 часть самоподкачки уходит в зеркальное реле
(поле стабилизируется, но на неверном уровне); при ksf=0 самоподкачка
взрывается. Честная формулировка: на room walk-стационара НЕТ в обоих
режимах; MC-эталон (11.07 / 12.83) — единственная опора.

**ПОЧИНКА (§887-c, следующий шаг, библиотека).** Ограничитель максимума в
walk: Lh на куске ≤ Lh_max·(1+eps) с Lh_max = max по сцене на прошлой
итерации (форма из §735 старого пути; константа названа), либо клэмп
депозита по соседству. Предсказание: room ksf=0 сходится к MC 11.07
(отношение ≥ 0.9 при it=20); room ksf=1 пересходится и поднимается от 3.91;
коридор/одно зеркало/box — битово (вырожденных кусков нет).
Лов: ограничитель НЕ должен ломать легитимные яркие куски (emitter) —
граница по фактической сцене, не глобальная константа.

**ГЕЙТ:** ccheck (счётчики — конец stat, А1619); make test прогнан
(EXIT=0); PG-печати §887-b в pgather.

## §887-c (11-09): ПЛАН — ОГРАНИЧИТЕЛЬ ДЕПОЗИТА «МИНИМАЛЬНАЯ ДОЛЯ РЯДА» (форма §735) (ДО КОДА)

**МЕХАНИКА.** Корень взрыва: депозит Lin·csec·axcos/**area[p]** на вырожденном
куске (area→0) даёт E ~ 1/area и самоподкачку через Lh. Фикс: знаменатель
депозита D = max(area[p], DEP_MIN_SHARE·cell²), DEP_MIN_SHARE = 1/16
(кусок не может принять более 16 долей «среднего по клетке»; константа
названа, из геометрии клетки csec = cell²). Для обычных кусков D == area —
арифметика идентична битов. Применяется во ВСЕХ депозит-сайтах деления на
fc->area[p] (1047/1053/1200/1234/1236/1238/1240/1825/1831); Lh-кэмп не нужен —
корень снят.

**ПРЕДСКАЗАНИЯ.**
- П1: room ksf=0 it=20 сходится: E_avg в 0.9–1.2 от MC 11.07 (было 74.9).
- П2: room ksf=1 it=20: E_avg растёт от 3.91 к MC 12.83; сверка ≥ 0.8;
  гистограмма §887 сжимается (медиана > 0.5).
- П3: якоря box 4.0816 / cavity 4.2607 / 4.2689 — БИТОВО (кусков ниже
  порога нет — проверить прогоном, не поверить).
- П4: якорь room lppol 7.5971 ИЗМЕНИТСЯ легитимно: старое число было
  взрыв-загрязнено (замер на it, где взрыв уже шёл) — переустановка якоря
  с ЯВНОЙ пометкой в докладе.
- П5: коридор/одно зеркало — согласие 1.00±0.05 сохраняется.
- П6: НК le=0 → E≡0; гейт полный (ccheck, ASAN, make test).

### §887-c: ДОКЛАД — ОГРАНИЧИТЕЛЬ ЧАСТИЧЕН: ВЗРЫВ ОСТАЛСЯ (ΣE walk0 = 26.3 МЛН, E_avg ЕГО МАСКИРОВАЛ); В WALK1 СТЕНЫ ПОЛУЧАЮТ РОВНО 0 (4688 кусков); §735 НУЖЕН НА УРОВНЕ Lh, НЕ ЗНАМЕНАТЕЛЯ; ЛОВ ЛОЖНОГО НК (пустые слепки) — ЗАФИКСИРОВАН

**Что сделано.** Ограничитель «минимальная доля ряда» (D = max(area,
DEP_MIN_SHARE·cell²), DEP_MIN_SHARE=1/16) применён в 8 депозит-сайтах.
Якорь box walk — битово (ЧЕСТНОЙ проверкой: md5 полных числовых слепков;
первая проверка оказалась ложной — оба файла пустые из-за неверного ключа
xit=4 вместо it=4; лов зафиксирован: НК обязан сравнивать НЕПУСТЫЕ
артефакты).

**П1 НЕ ДОСТИГНУТ в заявленной форме:** room ksf=0 it=20 E_avg=9.36→10.91
(it=40) «сошлось» к MC 11.07 — но по-кусочно ΣE = 26 316 354: взрыв живёт
на стенах (три 3782/3783: E≈1.07e7), E_avg его маскировал. Вывод:
знаменательный ограничитель душит 1/area-сингулярность, но НЕ взрыв:
межстенный релейный козффициент усиления (kd·D⁻¹·csec-цепь) остаётся ≥1
на кусках нормального размера. Форма §735 нужна НА УРОВНЕ Lh (принцип
максимума радианса: Lh ≤ Lh_max_прошлой_итерации ·(1+eps), константа
названа) — §887-d.

**П2 НЕ ДОСТИГНУТ:** walk ksf=1 стабилен 3.9142 против MC 12.83; по-кусочно
Σ стен (ks=0.25, n=4688): walk0 26.3М (взрыв) → walk1 **1697, из них
большинство РОВНО 0** — в ksf=1 стены не депонируют ВООБЩЕ (это не
ограбление релеем: ноль депозита при любом Lin>0 невозможен в схеме
(1−ks)·Lin — значит Lin в стены не приходит, либо ветка не исполняется).
Следующий прибор §887-d: трассировка одного куска (tri 3782) по итерациям
при ksf=1 — Lin/депозит/ветка на каждый визит (env-флаг, ~20 строк).

**Положительное:** П3 ✓ box битово (честно); П5 ✓ коридор 1.0023 / одно
зеркало 1.0365 (дихотомия на простых сценах верна и после ограничителя);
П6 НК прогнан; make test EXIT=0; ccheck = унаследованный вердикт.

**СТАТУС.** Зеркальная тема room: два слоя — (а) взрыв walk (независим от
зеркал, ksf=0), (б) нулевые депозиты стен при ksf=1 (новая, точная
формулировка §887-d). Ограничитель-знаменатель ОСТАВЛЯЕТСЯ (снимает
1/area-сингулярность, безвреден битово на здоровых сценах).

## §887-d (11-09): ПЛАН — ТРАССИРОВКА КУСКА (HZ_TRACE_TRI) И РЕШЕНИЕ О НУЛЕВЫХ ДЕПОЗИТАХ; ПОТОМ Lh-ОГРАНИЧИТЕЛЬ (ДО КОДА)

**СТАДИЯ 1 — ТРАССИРОВКА (этот раунд).** Env-флаг HZ_TRACE_TRI=<tri> (читается
однократно, вне горячего пути): в депозит-сайтах front_visit (1234/1238) и
в хоп-спавне (1206) — печать в stderr контекста (Lin, ks, депозит, ветка)
для указанного куска. Кусок-детектив: 3782 (kd 0.28, ks 0.25 в w1; в walk0
взрывался до 1e7).
- П1: ksf=1 трассировка покажет один из трёх диагнозов: (а) визитов куска
  НЕТ — трубки/ветка не доходят (проблема выше по конвейеру); (б) визиты
  есть, Lin≈0 — энергия не приходит (реле душит подачу); (в) визиты с
  Lin>0, а депозит 0 — код депозита.
- П2: ksf=0 трассировка того же куска — эталонная карта (взрывной профиль
  Lin к 1e7).
- П3: по диагнозу — Lh-ограничитель (max-принцип радианса) и/или точечная
  починка; предсказания починки — в докладе стадии 1.

**СТАДИЯ 2 — Lh-ОГРАНИЧИТЕЛЬ (следующий раунд, informed стадией 1).** Форма:
Lh ≤ (1+LH_EPS)·Lh_max_прошлой_итерации, LH_EPS названная константа;
карманы: max по сцене на прошлой итерации; НК: box/corridor битово (не
должен активироваться на здоровых сценах), room ksf=0 ΣE ≤ 1.5×E_avg-масштаба.

### §887-d: ДОКЛАД — ТРАССИРОВКА НАШЛА ТРЕТИЙ ДЕПОЗИТ-САЙТ БЕЗ ДИХОТОМИИ: PBEST «ТОЧНАЯ ВЕТКА» (sweep.c ~1017-1044) — ПОЛНЫЙ Lin НА ПЕРВОМ ЖЕ УДАРЕ И ТЕРМИНАЦИЯ ТРУБКИ БЕЗ ХОПА; ЭТО ГЛАВНЫЙ ПУТЬ DEPOSIT'А WALK

**Трассировка (HZ_TRACE_TRI).** Стена 3013 (ks=0.25, центр 0.64/0.44/−1.59,
фронт в зал): при ksf=1 за 2 итерации ×26 направлений — всего 2 события,
оба хоп-ноги с Lin ~ 2.5e-10; основные трубки (десятки пересечений этой
стены) НЕ депонируют в неё вовсе. При этом глобально свет течёт:
депозитов с Lin>0.5 — 451 532, с Lin≤0.5 — 203 930 (= тёмные входы трубок,
26×трубки×итерации — сходится точно). Свет уходит НЕ туда.

**Источник.** front_visit имеет ПУТЬ pbest («точная ветка», ближайшее
точное пересечение): депозит полного Lin на pbest, `depA += ai`,
`*a=0; *b=Lh; goto done` — трубка ТЕРМИНИРУЕТСЯ. В этой ветке НЕТ
дихотомии §873 (ни (1−ks)-депозита, ни хопа). Симптомы сходятся: hit через
pbest → зеркало обрабатывается как диффузный кусок с полным депозитом
и без релея; релеят только хиты FV-ветки (nh-список) — отсюда согласие на
простых сценах (их хиты попали в FV) и провал на room (стены идут через
pbest). Классификация из §887 (ks=0 углы переполнены, ks=0.25 ограблены) —
распределение между двумя ветками депозитов.

**ПОЧИНКА §887-e (следующий шаг, библиотека, аккуратно).** В pbest-ветке
применить дихотомию: депозит (1−ks)·Lin; при ks_eff>0 — spawn хопа
(ks·Lin, R, корень Lin) как в FV-ветке; трубку терминировать (диффузная
часть погашена, релей — хопом), семантика G6 сохраняется (depA += ai при
ai = вход ноги... уточнить по месту: depA учитывает только диффузную часть —
инвариант ai = depA + a + b пересчитать). Предсказания: room ksf=1 стены
поднимаются к MC (сверка ≥ 0.8, Σ по классу ks=0.25 ≥ 0.5×MC); room ksf=0
не меняется (ks=0 — ветвь else, битово); коридор/одно зеркало — сверка
остаётся 1.0±0.05 (их текущее согласие через FV не трогается; pbest-хиты
там, если есть, СЕЙЧАС переполняют — сверка может даже улучшиться); box
якорь — битово (ks=0). Риск: двойной учёт с FV на одном сегменте — штамп
A1564 гасит (кусок × трубка один раз).

**ГЕЙТ стадии:** трассировка добавлена (HZ_TRACE_TRI, здоровый прогон
бесплатен), lin_pos/lin_zero в st; ccheck = унаследованный; make test не
затронут (прибор).

## §888 (11-09): ПЛАН — ДЕМОНСТРАЦИОННЫЕ КАДРЫ В img/ (справедливый упрёк: снаружи не видно ничего)

Кадры держались в tmp-каталогах и были силуэтами v1. Шаг-демонстрация:
освещённые кадры (полный свип + сбор) трёх сцен → img/ в PNG (конвертер
stdlib-zlib, без зависимостей). Без правок решателя.
- П1: room 512² le=1 it=8 (ksf=0): кадр с тенями и переотражением
  (яркость ≥ 2 уровней: прямой свет + отскок).
- П2: mirror_box ksf=1: в зеркале виден эмиттер (пиксель = отражение).
- П3: San_Miguel RAM-путём (свип по 5.6 М кусков, ulimit 8G): освещённый
  кадр UE5-сцены.
- П4: НК le=0 той же камеры — чёрный.

### §888: ДОКЛАД — ДЕМОНСТРАЦИЯ В img/: ЛАМПА+ТЕНИ (room_lamp), ЗЕРКАЛО (mirror_box), ОСВЕЩЁННЫЙ SAN_MIGUEL; ЧЕСТНЫЕ ОГРАНИЧЕНИЯ НАЗВАНЫ

**Кадры (img/, PNG через stdlib-конвертер ppm2png.py):**
- `s888_room_lamp.png` — комната с лампой под потолком (Ke 20, сцена
  assets/synth/room_lamp.{obj,mtl}: комнатный зал + квад-лампа): световой
  клин на полу, колонна в контровом свете, теневые зоны — локальный свет
  и переотражение считаются и видны.
- `s888_mirror.png` — mirror_box ksf=1: в зеркальной стене виден эмиттер
  (яркость 0.9988, эмиттер за спиной — отражение), НК без дихотомии — 0.
- `s888_sm_lit.png` — San_Miguel (UE5-демо), le=3 dirs=6 it=1: освещённый
  кадр 384², яркость 5.3±, max 131 (свип 174 с RAM; dirs=26 — фон-таймаут,
  отдельный прогон).
- `s888_room_le0.png` — НК: le=0 — кадр тождественно чёрный.
- `s888_room_le0.png` (дубль НК) — контр-демонстрация: глобальный le освещает всё
  равномерно (теней нет, mean=max) — почему нужен ЛОКАЛЬНЫЙ источник.

**Ловы демонстрации (записаны):** mtllib из копии сцены указывал на старый
mtl — материал лампы молча не читался (E≡0); пойман сравнением E_avg с
нулевым. Композиция кадра room_lamp слабая (камера внутри загромождённого
интерьера) — работает физика, не арт; красивый ракурс — после HBLK v2.

**ГЕЙТ:** ccheck не затронут (конвертер — stdlib-python, вне C-гейта);
make test не затронут.

## §889 (11-09): ПЛАН — ЦВЕТ И ГАММА: RGB-РЕНДЕР В PGATHER (3 СВИПА ПО КАНАЛАМ), ДО КОДА

**ЗАЧЕМ.** Кадры монохромны и тёмны: kd3/ke3 в материалах ЦВЕТНЫЕ, а решатель
скалярный. Классический приём радиосити: прогон переноса 3 раза с альбедо
канала (kd_c = kd3[c], эмиссия lep_c = ke3[c]) — цвет через три скалярных
решения; камера собирает L_c поканально; тонмапинг = экспозиция по
99-процентилю + гамма 1/2.2. Решатель НЕ трогается.

**МЕХАНИКА (только tools/pgather.c).** Ключ `rgb=1`: три массива kd_c/lep_c
(по tri, до permute, permute-цепочка ×3), три hz_sw_run с общими остальными
opts, E_r/g/b сохраняются; сбор — L_c = le + lep_c[p] + kdvis_c·E_c/2π +
(зеркальный член по каналам — v2, в этой версии зеркальные кадры остаются
монохромными); PPM пишется цветом (P6 уже 3 канала), тонмапинг
v = (max(0,L/exp))^(1/2.2), exp = 99-перцентиль L_max-канала.

**ПРЕДСКАЗАНИЯ.**
- П1: le=0 → чёрный (НК). П2: room_lamp: стены приобретают цвет материала,
  лампа тёплая (Ke 20/18/14 — теплее), тени цветные (не серые).
- П3: стоимость ×3 свипа (room_lamp ~30 с, San_Miguel dirs=6 ~9 мин).
- П4: ksf=0 — битово относительно одноканальной логики (каналы независимы,
  r-канал с kd_r == прежний kd при монохромных kd3? НЕТ: kd_r = kd3[r] ≠ kd —
  битовый НК не заявляется; заявляется НК le=0 и контроль «серый мир»:
  сцена с kd3=(v,v,v) обязана дать кадр, битово равный одноканальному прогону
  с rho=v).
- П5: гейт: ccheck, make test не затронут, ASAN быстрый.

### §889: ДОКЛАД — RGB-РЕНДЕР РАБОТАЕТ: КАНАЛЬНЫЕ СВИПЫ (kd_c=kd3[c], lep_c=ke3[c]) + СРЕДНЕКЛЮЧЕВАЯ ЭКСПОЗИЦИЯ + ГАММА; КОМНАТА С ЛАМПОЙ СТАЛА ЦВЕТНОЙ

**Код (только tools/pgather.c).** Ключ `rgb=1`: три скалярных свипа
(альбедо/эмиссия канала), канальные массивы проходят morton-перестановку
(py->perm допускает многократный вызов), камера копит поканальную яркость;
тонмапинг: экспозиция = средняя ключевая яркость (перцентиль душил тени —
первый кадр был почти чёрным), гамма 1/2.2. rgb+ksf>0 в v1 — отказ
(зеркальный член поканально — v2).

**Приёмка.** Комната с лампой: тёплый свет, цветные стены (kd3 читаются),
колонна. П4 (серый мир) — не прогонялся формально (kd3 комнат не серые);
НК le=0 → чёрный действует. П3 ✓: 3 свипа = 21 с. ccheck = унаследованный
вердикт; make test не затронут (pgather вне тестов по сборке).

**ЛОВЫ (повторяющийся класс, закрепить в дисциплину!).** Два раза подряд
python-замены НЕ применялись из-за расхождения текста после clang-format
(§881-инцидент повторился): сборка проходила, «RGB»-кадр рендерился СЕРЫМ
от синего канала. Лечение: перед любым «применилось» — grep факта + один
смотровой кадр глазами (read_image), а не только успешный rc.

**Ограничения v1:** rgb не совмещается с ksf>0 и delbox; зеркальный
поканальный член — v2; экспозиция глобальная (нет локальной адаптации).

**ПОРЯДОК ДАЛЬШЕ:** §887-e (дихотомия в pbest-ветке — зеркальная
корректность) → HBLK v2 (E в файле: цветной освещённый город из mmap) →
разрушаемость-2.

## §890 (11-09): ПЛАН — ГРАДИЕНТЫ СВЕТА: ДЕТАЛЬНОСТЬ СЕТКИ + ЭКСПОЗИЦИЯ (ДО КОДА)

Кадр «16 цветов» = плоские фасетки: E на куске, кусок = tri ∩ клетка, клетка
lev=6 огромна. Свет ЕСТЬ (гистограммы это показали), но не ВИДЕН. Две ручки:
1. **lev↑** — клетка мельче → спад освещённости по полу становится видимым
   (это и есть лестница детальности структуры). Комната lev=6 → 4966 кусков;
   lev=9 → ~×8.
2. **ключ expm=F** — множитель экспозиции (тонмапинг по среднему клипует
   яркое в белое; expm<1 открывает градиент светового пятна).
- П1: кадр lev=9 expm=0.25: виден плавный спад света по полу (градиент,
  а не 2 плоскости) и мягкая тень стола/колонны.
- П2: стоимость: время свипа ≤ ×12 против lev=6 (20 с → ≤4 мин).
- П3: НК le=0 чёрный; гейт ccheck.

### §890: ДОКЛАД — ГРАДИЕНТ НЕВОЗМОЖЕН БЕЗ КЛИПА: КУСОК = ТРЕУГОЛЬНИК ЦЕЛИКОМ (nt не зависит от lev — клип tri ∩ клетка НЕ реализован, это недостающий элемент STRUCTURE §2.3); expm добавлен и работает

**Факт.** lev=6→9: nt = 4966 в обоих (hz_pyr_build снапшота держит pcs 1:1
с треугольниками) — свет считается НА ТРЕУГОЛЬНИКЕ, крупный полигон = один
цвет, что бы ни делала сетка. Кадр expm=0.22: большие плоскости стали
различимы по яркости (стены/пол/угол), но внутри полигона — плоско.

**ЭКСПРОМТ (экспозиция):** добавлен ключ expm=F (множитель средней ключевой
экспозиции) — работает: expm=0.22 открыл различие плоскостей. ccheck =
унаследованный.

**Следствие для плана (названный хвост с владельцем — следующий структурный
шаг):** «КУСОК = (TRI, КЛЕТКА)» — настоящий клип треугольника клетками при
hz_pyr_build: тогда E живёт на кусочке, крупные треугольники (пол!) получают
градиент, и это же носитель для HBLK v2/АГРЕГАТА. Это ровно строка «кусок =
(tri, клетка, E, материал), 12–16 Б» из STRUCTURE §2.3, не перенесённая в
снапшот. Оценка: правка в pyr_build + место в pblock/pgather — средний шаг.
Экспозиция expm — остаётся в инструменте.

**ПОРЯДОК ДАЛЬШЕ:** (а) клип куска (градиенты + честная структура) →
§887-e (дихотомия pbest — зеркальная корректность) → HBLK v2 (цветной
освещённый город из файла) → разрушаемость-2.

## §891 (11-09): ПЛАН — КАДРЫ С ПОЛУТОНАМИ: КРИВИЗНА (СФЕРЫ) И ДВОР SAN_MIGUEL, 768² (ЗАМЕРНЫЙ)

Полутона показывает КРИВИЗНА (мелкие фасетки сфер = плавный спад по нормали)
и мелкополигональный двор San_Miguel; большие плоскости комнаты заведомо
плоские (§890 — клип не реализован). Разрешение 768².
- П1: сферы + лампа сверху (rgb, 768²): на сферах виден плавный спад
  света/полутона и межсферные тени.
- П2: San_Miguel, двор сверху, rgb, 768², dirs=12 it=1: цветные стены с
  перепадами освещённости.
- П3: время: сферы ≤ 5 мин; SM ≤ 15 мин.

### §891: ДОКЛАД — ПОЛУТОНА ПОКАЗАНЫ: ПЕРСПЕКТИВНЫЙ РЯД СФЕР С ФАСЕТОЧНЫМ ОСВЕЩЕНИЕМ (img/s891_spheres_sky.png, 768², RGB, le=2); ПЛЮС ЛОВЫ ДЕМО-СЦЕН

**Кадр:** [s891_spheres_sky.ppm|png] — diag-ряд сфер (глобальный le=2 =
равномерная «небесная» засветка, rgb-каналы, dirs=26, it=14, expm=0.6):
на кривизне сфер полутона ВИДНЫ — фасетки с разной освещённостью (|cos|
нормали + межсферное затенение), тёплый оттенок (kd3 неравномерны),
перспектива, затухание к горизонту. Это и есть непрерывное поле на мелких
кусках — то, чего лишены крупные плоскости (§890: клип tri∩клетка).

**Ловы демо-сцен (два, оба — «молча нулевой свет»):**
1. Гигантский эмиссивный треугольник (16 м) ломает перенос: E_avg=0.003 —
   депозиты исчезают (нужно расследование отдельно, если понадобится
   большой софтбокс; для демо — малые лампы).
2. Копия сцены без mtllib/с «чужим» mtl — материалы молча нулевые
   (повтор §888-лова). Демо-сцены обязаны проверяться печатью E_avg > 0
   ДО рендера кадра.
3. Сферы выстроены ДИАГОНАЛЬЮ (−2,−2)→(28,16) — камера «в лоб» видит 5 %
   геометрии; ракурс вдоль диагонали даёт 65 %.

### §891-2: ДОКЛАД — RAY EFFECT ПОДТВЕРЖДЁН ПАРОЙ КАДРОВ И УПРАВЛЯЕМ: dirs=26 ШАХМАТКА (ΣE 4.8М — взрывной хвост на сферах), dirs=96 ГЛАДКО (E_avg 10.2 против MC-класса); СТОИМОСТЬ — ВРЕМЯ СВИПА ×4

**Контрольная пара** (та же камера/экспозиция expm=3, 768²):
- dirs=26: шахматка (соседние фасетки в разнобой), ΣE по сцене — взрывной
  хвост (E_avg 447 завышен вырожденными; max пикселя 79165);
- dirs=96 (12×08, гаусс по μ): фасетки СГЛАЖЕНЫ, мягкие градиенты по сфере,
  E_avg=10.19 стабилен; свип 241 с против 43 с (×5.6).
- Диагноз §888 «текстура» снят: это квадратурный шум дискретных ординат,
  управляется числом ординат; депозит-доля (линейная нагрузка) — следующий
  слой сглаживания (T4).
- Важный побочный факт:dirs=96 ПОДАВИЛ взрывной хвост на сферах (E_avg 447 →
  10.2) — депозит-пересчёт «весь поток клеточного сечения задетому куску»
  был одним из двигателей расходимости; подтверждает диагноз §887-c.

## §892 (11-09): ПЛАН — ЛИНЕЙНАЯ НАГРУЗКА КЛЕТКИ ВМЕСТО ЛОТЕРЕИ ТРЕУГОЛЬНИКОВ (ДО КОДА)

**МЕХАНИКА.** В front_seg_walk депозит-цикл заменяется линейной нагрузкой
(T4): ВСЕ куски списка клетки получают Ed += w_d·Lin·csec·axcos·(1−ks_p)/Σarea
(Σarea — сумма площадей кусков клетки); зеркало — ОДИН хоп от ближайшего
точного удара (pbest по существующему ray-tri списку: точка, R, ks·Lin);
трубка продолжает b = Lh(pbest). Ray-tri остаётся только для геометрии хопа.
Это убирает лотерею (ray effect на уровне кусков) и 1/area-сингулярность
заменяется Σarea (закрывает §887-c по построению).

**ПРЕДСКАЗАНИЯ.**
- П1: сферы dirs=26: шахматка уходит — соседние фасетки в пределах ±20 %;
  кадр гладкий при низких dirs.
- П2: room ksf=0: ΣE без взрыва, E_avg стабилен; сверка с MC 11.07 ≥ 0.8.
- П3: room ksf=1: стены оживают, сверка с MC 12.83 ≥ 0.8 (починка §887-e
  включена сюда — pbest-релей заменён хопом от pbest).
- П4: коридор/одно зеркало: сверка 1.0±0.05.
- П5: НК: le=0 чёрный; tau0 без взаимодействия; якорь cavity-класса может
  измениться (модель депозита поменялась — фиксировать новые значения ЯВНО).
- П6: гейт: ccheck, make test (g6viol при линейной нагрузке может считать —
  проверить и задокументировать), ASAN.

### §892: ДОКЛАД — ЛИНЕЙНАЯ НАГРУЗКА РАБОТАЕТ КАК СПРОЕКТИРОВАНА, НО: ШАХМАТКА УШЛА — И СВЕТОТЕНЬ ТОЖЕ (равномерное поле: deposit не зависит от нормали куска); СРЕДНИЙ ПУТЬ — УГЛОВОЙ ОТКЛИК (А1361), НЕ ЛОТЕРЕЯ И НЕ РАВНОМЕРНОСТЬ

**Факт (сферы, dirs=26, le=2):** кадр — ровная заливка (mean=max=2.0):
линейная нагрузка делит поток клетки поровну на куски — |cos| нормали
КУСКА исчез из депозита (axcos — косинус трубки к ОСИ клетки), затенение
пропало. Шахматки нет, взрыва нет (E стабильно), но и изображения света нет.

**ДИАГНОЗ ОБЩЕЙ КАРТИНЫ (три точки, все измерены):**
| вариант депозита | шахматка | светотень | взрыв |
|---|---|---|---|
| лотерея треугольников (до §892) | да | да (шумная) | да |
| линейная нагрузка (§892) | нет | НЕТ | нет |
| MC-эталон (истина) | нет | да | нет |

Средний путь — не выбор между двумя крайностями, а **угловой отклик на
куске**: депозит с зависимостью от нормали куска (axcos→|ω·n̂| куска,
взвешивание по доле площади), т.е. ровно А1361/агрегат из STRUCTURE §2.3,
либо депозит по факту перекрытия с весом нормали. Это §892-b.

**Что оставить из §892:** линейная нагрузка как НИЖНЯЯ опора (нет
лотереи/взрыва — пригодна для «ambient»-модели и как НК-пол), код остаётся
за флагом... флага нет: ветка стала депозитом walk. Решение: оставить
(тесты сходятся, взрыв закрыт), светотень вернуть в §892-b весом нормали —
минимальная правка той же формулы (axcos → |ω·n̂_p| по куску в сумме).
**ГЕЙТ:** ccheck = унаследованный; make test EXIT=0 (test_bounce сошёлся —
полусферная модель с линейной нагрузкой даёт физически правильный интеграл).

## §892-b (11-09): ПЛАН — ВЕС НОРМАЛИ В ЛИНЕЙНОЙ НАГРУЗКЕ (ДО КОДА)

Формула §892 равномерна — затенение исчезло. Восстановить |cos| нормали
куска весом: w_p = area_p·|ω·n̂_p|; Ed_p += w_d·Lin·csec·|ω·n̂_p|·(1−ks_p)/Σw,
Σw = Σ area_p·|ω·n̂_p|. Соседние фасетки имеют БЛИЗКИЕ нормали → близкие
веса → плавные градиенты без лотереи (лотерея была в «попал/не попал»
ray-tri, а не в весах). Σw=0 (трубка скользит вдоль всех поверхностей
клетки) → депозит 0, трубка проходит. Lh-продолжение — как в §892.
- П1: сферы dirs=26: светотень ВОЗВРАЩАЕТСЯ без шахматки (соседние фасетки
  ±близкие веса); кадр «graded sphere» как у dirs=96, но при dirs=26.
- П2: room ksf=0 it=20: E_avg стабилен (взрыв не возвращается: Σw знаменатель).
- П3: НК le=0 чёрный; tau0 без взаимодействия; make test.
- П4: гейт: ccheck, ASAN быстрый.

### §892-b: ДОКЛАД — ВЕС НОРМАЛИ РЕАЛИЗОВАН; НАЙДЕН И ПОЧИНЕН БАГ «ТЁМНАЯ ТРУБКА НЕ ЗАЖИГАЕТСЯ» (b=Lh только при Lin>0 — эмиссия не подхватывалась, E≡0); ПОСЛЕ Зажигания — ВЗРЫВ (E_avg 1.5e5): Lh-ОГРАНИЧИТЕЛЬ ТЕПЕРЬ ПРИОРИТЕТ №1

**Сделано.** Вес нормали в линейной нагрузке (§892-b: w_p = area_p·|ω·n̂_p|,
Ed_p += w_d·Lin·csec·an·(1−ks_p)/Σw, Σw=0 → трубка скользит мимо).
Формула восстанавливает затенение без лотереи (соседние фасетки — близкие
веса). Лов (третий раз тот же класс): правки не применялись из-за рефлоу —
grep факта обязателен.

**Найден и починен фундаментальный баг:** в §892-ветке зажигание трубки
требовало Lin>0 — тёмная трубка (вход) НИКОГДА не подхватывала эмиссию
(b=Lin=0 навсегда) → E≡0 на всех сценах. Фикс: b = Lh_last при pbest≥0
безусловно (эмиссия зажигает). Результат: зажигание работает — и
немедленно взрыв (E_avg=1.5e5 на spheres, le=2, it=14): конкурирующие
требования «эмиссия зажигает» и «стабильность» упираются в ОТСУТСТВИЕ
ограничителя радианса.

**СТАТУС.** §887-d стадия 2 (Lh ≤ (1+LH_EPS)·Lh_max прошлой итерации,
константа названа) — ПРИОРИТЕТ №1: без него не работает НИ одна модель
депозита (лотерея взрывалась, линейная нагрузка взрывается при зажигании).
П1 (светотень без шахматки) не верифицируем до ограничителя.

**ГЕЙТ:** ccheck = унаследованный; make test прогнан в §892; текущее
состояние дерева — рабочее, но поле на сценах с эмиссией нестабильно
(это задокументированный известный дефект, не регрессия от §887-с:
ограничитель-знаменатель остаётся).

## §893 (11-09): ПЛАН — Lh-ОГРАНИЧИТЕЛЬ (ПРИНЦИП МАКСИМУМА РАДИАНСА, §887-d ст.2) (ДО КОДА)

**МЕХАНИКА (src/nstruct/sweep.c).** На каждой итерации до обхода направлений:
Lmax = max_p (le_p + rho_p·Eprev[p]/2π) — максимум радианса прошлой итерации
(из того же цикла, где Eprev читается из pcs — без нового прохода). Кап
итерации: lh_cap = LH_GROWTH · Lmax, LH_GROWTH = 1.25 (константа: рост не
быстрее +25 %/итерацию; физическая сходимость при альбедо<1 даёт запас).
Во всех точках вычисления Lh (pbest-ветка, FV-цикл, §892 Lh_last):
Lh = min(Lh, lh_cap); fc.lh_max_seen = max(...) за направление; после
front_dir — свёртка в lh_seen итерации. Память/стоимость: O(1).

**СООТВЕТСТВИЕ §735:** старый закон «принцип максимума в обновлении ячейки —
иначе расходимость» — та же форма: новое значение радианса не превышает
максимум входов/эмиссии более чем наnamed growth.

**ПРЕДСКАЗАНИЯ.**
- П1: spheres_sky le=2 ksf=0 rgb (взрыв 1.5e5 в §892-b): E_avg стабилен
  и ≤ ~50·le; кадр существует (полутона, не белая заливка).
- П2: room ksf=0 it=20/40: ΣE стабилен (взрыв 26М §887-c подавлен),
  E_avg в коридоре MC 11.07 (0.8–1.2).
- П3: room ksf=1: E_avg растёт от 3.91 к 12.8 (0.8–1.2).
- П4: коридор/одно зеркало: сверка 1.0±0.05.
- П5: box: битово, ПОКА ограничитель не активен; если активен — новый
  якорь фиксируется ЯВНО (модель депозита уже менялась в §892).
- П6: гейт: ccheck, make test, ASAN быстрый.

### §893: ДОКЛАД — ОГРАНИЧИТЕЛЬ «РОСТ ×1.25» ЧАСТИЧЕН: room ksf=0 СТАБИЛИЗИРОВАНА И СОШЛАСЬ К MC (10.397 vs 11.07, ΣE стабилен), НО spheres-взрыв (149891) НЕ ЗАДУШЕН — кап следует за взрывом, а в депозите §892 через sw вернулась 1/area-сингулярность

**Код вошёл:** Lh-ограничитель «рост ×1.25/итерацию» (LH_GROWTH) в трёх
точках Lh + pbest-продолжение (найден непокрытый 4-й сайт — строка 1048);
fc.lh_cap/lh_seen, свёртка по направлениям.

**Приёмка.** П2 ✓ частично: room ksf=0 it=20/40: **10.3973/10.3974 —
стабильно и ≈MC 11.07 (0.94)** — впервые walk ksf=0 на room сошёлся без
взрывного дрейфа. П3 ✗: room ksf=1: 4.4369 стабильно — стены по-прежнему
голодны (до MC 12.83 далеко). П1 ✗: spheres_sky E_avg=149891.9735 —
БИТОВО то же, что до ограничителя: кап «×1.25 от текущего максимума»
следует за взрывом, не душит его.

**ДИАГНОЗ (для §893-b).** Две незакрытые сингулярности:
1. Депозит §892: Ed += Lin·csec·an·(1−ks)/sw, sw = Σ area·an — на клетке
   из ОДНОГО куска sw = area·an → /area — 1/area вернулась (ан-фактор
   усугубляет). Фикс: знаменатель снова через front_depden-класс
   (max(sw, DEP_MIN_SHARE·cell²·an-масштаб)).
2. Кап должен быть ПРИНЦИПОМ МАКСИМУМА, а не «ростом»: повторное излучение
   куска ≤ max(собственной эмиссии, макс пришедшего на него радианса
   прошлой итерации): Lh_p ≤ max(le_p, Linmax_prev[p]), Linmax[p] —
   пер-кусковый массив (память nt·8×2). Это §735 в точной форме.

**ГЕЙТ:** ccheck = унаследованный; make test EXIT=0; clang-format.
Якоря: box — прогон отложен до §893-b (модель меняется дважды).

## §893-b (11-09): ПЛАН — ПРИНЦИП МАКСИМУМА ПЕР-КУСКОВО + ФИКС ЗНАМЕНАТЕЛЯ SW (ДО КОДА)

1. **Пер-кусковый кап радианса:** массивы Linmax_prev/cur[nt]; на каждом
   депозите Linmax_cur[p] = max(..., Lin); Lh куска ограничивается
   max(front_le(p), Linmax_prev[p]) — «повторное излучение не ярче
   максимума пришедшего» (§735 в точной форме). Это убирает «свечение
   изнутри» (взрывные вырожденные куски).
2. **Знаменатель sw:** sw_eff = max(sw, FRONT_DEP_MIN_SHARE·cell²) — убирает
   1/area на однокусковых клетках (это убирает «пестроту» от пересчёта).
- П1: spheres_sky le=2: E_avg конечен и стабилен (≤ ~50), кадр — однотонные
  сферы с градиентом, без пестроты и внутреннего свечения.
- П2: room ksf=1: E_avg поднимается к MC 12.83 (0.8–1.2).
- П3: room ksf=0: остаётся ≈ MC 11.07.
- П4: НК le=0 чёрный; гейт ccheck/make test/ASAN.

### §893-b: ДОКЛАД — ПРИНЦИП МАКСИМУМА ПЕР-КУСКОВО + ФИКС ЗНАМЕНАТЕЛЯ: ВНУТРЕННЕЕ СВЕЧЕНИЕ УБРАНО (max 41 против 471052), СФЕРЫ — СВЕТЛО-СЕРЫЕ С ГРАДИЕНТОМ БЕЗ ВЗРЫВА; expm ДОКУМЕНТИРОВАН КАК РУЧКА ЭКСПОЗИЦИИ

**Код (src/nstruct/sweep.c).** (1) Пер-кусковый максимум пришедшего радианса
Linmax_prev/cur[nt] (только mode 3; swap на итерации, memset cur);
депозиты пишут Lin; Lh куска капится max(front_le(p), Linmax_prev[p]) —
«повторное излучение не ярче максимума пришедшего» (§735 в точной форме) —
в двух сайтах (Lh_last §892, FV-цикл). (2) Знаменатель депозита
sw_den = max(sw, FRONT_DEP_MIN_SHARE·cell²) — 1/area однокусковых клеток
закрыта. fc получил Linmax_prev/cur (прокид из hz_sw_run).
(3) expm=F — ручка экспозиции (§890), задокументирована: нормировка на
среднюю ключевую ×expm; expm≈5 для сцен с яркостью ~10–40.

**Приёмка на spheres_sky le=2 (было: E_avg 149891, max пикселя 471052 —
внутреннее свечение + пестрота):**
| метрика | §892-b (взрыв) | §893-b |
|---|---|---|
| E_avg | 149891 | 51.4 |
| max пикселя кадра | 471052 | 41.4 |
| вид | белая заливка | серые сферы с градиентом |
- П1 ✓ (по сути): однотонные сферы с градиентом, без пестроты и свечения;
  остаточная фасеточность — ray effect при dirs=26 (лечится dirs=96, §891-2).
- П2/П3 (room): прогон следующего раунда (модель депозита изменилась —
  room-сверки с MC пересчитываются заново; это следующий шаг).
- П4: le=0 НК, make test EXIT=0; ccheck = унаследованный + maybe-uninitialized
  FP (Linmax вне mode 3 — NULL, free(NULL) легален, комментарии в коде).

**ПОРЯДОК ДАЛЬШЕ:** room-сверки с MC на новой модели (ksf=0/1 — стены/взрыв)
→ клип tri∩клетка (градиенты на плоскостях) → dirs=96 дефолт → HBLK v2.

## §894 (11-09): ПЛАН — ROOM-СВЕРКИ С MC НА МОДЕЛИ §893-b (ЗАМЕРНЫЙ)

Модель депозита сменилась (линейная нагрузка с весом нормали + Lh-кап) —
room-числа пересчитываются заново против MC (K=512, it=20, ~11 мин/прогон).
- П1: ksf=1: walk E_avg поднимается к MC ≈12.8 (0.8–1.2); класс стен
  ks=0.25 оживает (Σ по классу ≥ 0.5×MC).
- П2: ksf=0: E_avg в 0.8–1.25 от MC ≈11.07.
- П3: НК le=0 чёрный; гейт make test.

### §894: ДОКЛАД — ROOM-СВЕРКИ НА МОДЕЛИ §893-b: ksf=1 ПОДНЯЛСЯ 0.31→0.56 (стены оживают, П1 не добит), ksf=0 0.81 ✓; ОСТАТОК — НЕДОСТАЧА РЕЛЕЯ ПРИ МНОГОЗЕРКАЛЬНЫХ ЦЕПЯХ, локализация — связь хоп↔линейная нагрузка

| конфиг | walk | MC | отношение | было (§893) |
|---|---|---|---|---|
| room ksf=1 | 7.17 | 12.83 | **0.559** | 0.305 |
| room ksf=0 | 8.94 | 11.07 | **0.807** ✓ | 0.92* |

*ksg=0 «было» — другой ритм итераций/эксперимента; сравнение в таблице —
единый протокол it=20 K=512.

- П1 ✗ формально (0.56 < 0.8), НО динамика: 0.305 → 0.559; класс стен
  ks=0.25 ожил (медиана куска 0.276 против 0.000). Медиана всё ещё <1 —
  стены недополучают.
- П2 ✓: ksf=0 в коридоре 0.8–1.25.
- Остаточная недостача ksf=1 = зеркальное реле недодаёт при ЦЕПЯХ:
  гипотеза — хоп-нога при линейной нагрузке депонирует (1−ks) на всей
  клетке, но хоп продолжается только ks·Lin от ОДНОЙ точки (pbest);
  многозеркальные клетки теряют релей от остальных кусков клетки.
  §894-b: хоп на каждый зеркальный кусок клетки (с корнем-порогом) —
  счётчик хопов покажет рост; либо пересмотр продолжения.

**ГЕЙТ:** make test EXIT=0 не затронут; дампы в tmp_s888.
**ПОРЯДОК:** §894-b (хоп на зеркальный кусок клетки) — финальный слой
зеркальной корректности; параллельно стоит пересчитать rooms-якоря (модель
депозита сменилась — старые числовые якоря room недействительны, box/cavity
проверить битово при ближайшем касании swee3).

## §894-b (11-09): ПЛАН — ХОП НА КАЖДЫЙ ЗЕРКАЛЬНЫЙ КУСОК КЛЕТКИ, С ДОЛЕЙ ПОТОКА (ДО КОДА)

Сейчас хоп продолжается только от pbest — при линейной нагрузке зеркальных
кусков в клетке несколько, релей остальных теряется (остаток §894: 0.56).
Механика: в цикле по точным ударам (ht/hp) — для каждого куска с
ks_eff>0 хоп: точка org+om·t, R по нормали, lin = ks·(w_p/sw)·Lin, где
w_p = area_p·an_p — ДОЛЯ потока клетки этого куска (иначе многозеркальная
клетка умножает энергию). Порог от корня, ёмкость 32, lost — баланс.
- П1: room ksf=1: отношение 0.56 → ≥0.8; Σ стен ks=0.25 ≥ 0.5×MC.
- П2: коридор/одно зеркало: не меняются битово (один кусок-зеркало на
  клетку → w_p/sw = 1 → хоп идентичен прежнему).
- П3: room ksf=0: битово (хопов нет).
- П4: hop_lost ≈ 0, хопов больше (прибор); гейт ccheck/make test/ASAN.

### §894-b: ДОКЛАД — ОТКАТ ЭКСПЕРИМЕНТА И ТОЧНЫЙ ДИАГНОЗ: ПЕРЕ-ОСВЕЩЕНИЕ (коридор 1.46) ДАЁТ САМА ЛИНЕЙНАЯ НАГРУЗКА НА НОГАХ (не хоп-правка: откат не убрал), МЕХАНИКА — НОГА ДЕПОНИРУЕТ ПОЛНЫЙ поток в каждой клетке пути без декремента (N клеток = N× энергия); §894-c — СЕМАНТИКА НОГ

**Хроника.** (1) §894-b хоп-эксперимент: коридор 1.00→1.47, одно зеркало
1.04→1.97. (2) Откат хоп-правки: коридор остался 1.4633 → источник не в
хопах. (3) Линейная нагрузка §892 депонирует (1−ks)·долю в КАЖДОЙ клетке,
которую пересекает нога, и НЕ уменьшает поток ноги (b = Lh_last полный) —
нога через N клеток кладёт N× поток. Старый pbest-хоп был редким/коротким —
эффект был слабее, но существовал.

**§894-c — правильная семантика ног (дизайн, следующий шаг).** Два типа
трубок с разными депозит-правилами:
- ОСНОВНАЯ трубка (решает поле): линейная нагрузка клетки (§892) — её
  пере-эмиссия и есть источник поля (коридор 1.46 объясняется и этим —
  сверить с MC-погрешностью: MC-эталон в §891-2 давал согласие ДО §892 —
  значит основной вклад порчи внесли ноги);
- НОГА (релей): депонирует только в ТОЧНОЙ поверхности удара (пер-хит,
  как до §892), поток декрементируется на депозит, релей — ks·остатка.
Реализация: флаг fc->in_leg; front_seg_walk по флагу выбирает депозит
(линейная нагрузка / пер-хит).
- П1: коридор: сверка возврат к 1.00±0.05. П2: одно зеркало 1.04±0.05.
- П3: room ksf=1: ≥ 0.56 (не хуже; ожидание выше — ноги перестанут
  пере-свет).

## §894-c (11-09): ПЛАН — ДВЕ СЕМАНТИКИ ТРУБОК: ОСНОВНАЯ = ЛИНЕЙНАЯ НАГРУЗКА, НОГА = ПЕР-ХИТ С ДЕКРЕМЕНТОМ (ДО КОДА)

fc->in_leg: основная трубка депонирует линейной нагрузкой клетки (её
пере-эмиссия — источник поля); НОГА депонирует пер-хит в точных
поверхностях (депозит (1−ks)·Lin·csec·axcos/D, Lin = Lh, релей ks·Lin),
поток декрементируется депозитом — N клеток больше не множат энергию.
- П1: коридор 1.00±0.05, одно зеркало 1.04±0.05 (ноги перестанут пере-свет).
- П2: room ksf=1 ≥ 0.56 (ожидают выше); ksf=0 битово прежнему (хопов нет —
  in_leg не наступает).
- П3: гейт: ccheck, make test, ASAN быстрый.

### §894-c: ДОКЛАД — КОНТРОЛЬ БЕЗ ЗЕРКАЛ: +44% ДАЁТ САМА ЛИНЕЙНАЯ НАГРУЗКА (коридор без зеркал walk/MC = 1.443); in_leg-семантика реализована, но дефект — ПЕРЕРАСПРЕДЕЛЕНИЕ ПОТОКА КЛЕТКИ КУСКАМ, КОТОРЫХ ТРУБКА НЕ КАСАЛАСЬ (та же расходимость, что А1573/T-перехват)

**Контроль:** коридор (зеркала → Kd 0.9/Ks 0): walk 8.12 против MC 5.62 =
**1.443**. Вывод: +44% пере-освещения НЕ связано с зеркалами — линейная
нагрузка (§892) депонирует полный поток сечения трубки ВСЕМ кускам клетки,
включая куски, мимо которых трубка прошла (трубка — линия, клетка — объём).
На зеркальном коридоре это умножилось релеем до 1.46 (совпадение чисел —
та же природа). Зеркальная тема §887-e/§894-b в этом свете: их «0.56»
измерено на фоне +44% систематики — сначала система координат, потом зеркало.

**Что реализовано в этом шаге (остаётся):** fc->in_leg — ноги депонируют
пер-хит с декрементом (старая семантика), основная трубка — линейная
нагрузка. ccheck = унаследованный, make test EXIT=0.

**§894-d — следующий шаг (дизайн).** Депозит куску — только по ФАКТИЧЕСКОМУ
перекрытию отрезка трубки с куском (bbox ∩ сегмент → доля пересечения ×
площадь), а не «полный поток всем кускам клетки»: перекрытие уже считается
(front_box_seg в фильтре), вес = длина перекрытия × |an| —Deposit_p =
Lin·csec·an·(overlap_len/cell)·(1−ks). Это промежуточное между
«лотереей точек» и «всем клеткам»: непрерывно по геометрии (шахматка ↓),
не раздаёт мимо-прошедшим (пере-свет ↓). Проверка — тот же контроль
без зеркал: отношение к MC → 1.0.

## §894-d (11-09): ПЛАН — ДЕПОЗИТ ПО ФАКТИЧЕСКОМУ ПЕРЕКРЫТИЮ (ДО КОДА)

Основная трубка: каждому куску клетки с непустым bbox∩сегмент — доля
f_p = (bh1−bh0)/(tout−tin); депозит Ed_p += w_d·Lin·csec·an_p·f_p·(1−ks_p)/
front_depden(fc,p). Зеркальный релей — как в §894 (хоп от ближайшего
ray-tri удара, ks·Lin). Ноги — пер-хит (§894-c, без изменений).
- П1: контроль без зеркал: отношение walk/MC → 1.0±0.15 (было 1.443).
- П2: room ksf=1: ≥ 0.56, ожидание выше; ksf=0: 0.8–1.25.
- П3: коридор/одно зеркало с зеркалами — сверка улучшается против 1.46/1.97.
- П4: НК le=0 чёрный; гейт ccheck/make test.

### §894-d: ДОКЛАД — ЭКСПЕРИМЕНТ НЕ УДАЛСЯ: overlap-депозит даёт E≡0 (причина не найдена за отладку), ОТКАТ К СОСТОЯНИЮ §894-c; факты для будущей попытки сохранены

**Что пробовалось.** Депозит по фактическому перекрытию (f_p = длина
bbox∩сегмента / сегмент, вес an_p, (1−ks_p), знаменатель front_depden):
E_avg = 0.0000 на corridor_nomir — депозиты не происходят. Отладка:
front_seg_walk ВЫЗЫВАЕТСЯ (FSW ENTER, Lin=1 на 2+ сегментах), но OV-события
не регистрируются даже трассировкой по 9 tri-id.

**Откат** к состоянию §894-c (git checkout sweep.c): make test EXIT=0,
компиляция чистая. Текущее поведение: коридор 1.46 (пере-свет линейной
нагрузки на ногах), room ksf=1 0.56, ksf=0 0.81 — известные дефекты с
владельцами.

**Сохранённые факты для §894-d-2:**
1. front_box_seg(tribox, org, om, tin, tout, ...) в hit-коллекторе РАБОТАЕТ
   (ht/hp заполняются — FV-события были в старом коде).
2. В overlap-пассе тот же вызов не давал событий — подозрение: tin/tout в
   front_seg_walk — ОТРЕЗКИ ВНУТРИ ЛИСТА (короткие), а параметризация
   front_box_seg — относительно org/om ГЛОБАЛЬНО; bh0/bh1 могут быть вне
   [tin,tout] при другом масштабе/сдвиге параметра → f_p ≤ 0 везде.
   Проверить прежде чем писать: печать bh0/bh1/tin/tout на первом куске.
3. Ошибка моя: сразу писать код на предположении о параметризации, вместо
  minute-печати фактических bh0/bh1 (закон: сначала печать, потом код).

**СТАТУС ДЕРЕВА.** Рабочее: walk = линейная нагрузка (main) + пер-хит ноги
(in_leg), Lh-ограничитель ×1.25, Linmax-кап. Известные дефекты: коридорный
пере-свет 1.46 (линейная нагрузка), room ksf=1 0.56, старые room-якоря
недействительны. Все — с владельцами (§894-d-2).

### §894-e: ДОКЛАД — ПЕР-ХИТ СХЕМА ВОССТАНОВЛЕНА ДЛЯ ВСЕХ ТРУБОК: ПРОСТЫЕ СЦЕНЫ СНОВА СОГЛАСНЫ С MC (коридор 0.998, одно зеркало 1.010, контроль без зеркал 1.099); ROOM ksf=1 ОСТАЁТСЯ 0.31 — ДЕФЕКТ ROOM-СПЕЦИФИЧЕН И ИЗМЕРЕН УЖЕ НА ЧИСТОЙ ПЕР-ХИТ МОДЕЛИ

**Код (sweep.c).** Удалены: in_leg-ветвление (§894-c) и линейная нагрузка
(§892). front_seg_walk: пер-хит депозит для ЛЮБОЙ трубки — дихотомия
§873 (депозит (1−ks), хоп ks·Lin, Lin=Lh), Lh-кап §893, Linmax-кап §893-b.

**Замеры (единый протокол it=20, K=512):**
| конфиг | walk | MC | отношение |
|---|---|---|---|
| коридор зеркальный | 5.50 | 5.52 | **0.998** |
| одно зеркало | 4.08 | 4.04 | **1.010** |
| коридор без зеркал (контроль) | 6.18 | 5.62 | 1.099 |
| room ksf=0 | 9.36 | 11.07 | 0.846 |
| room ksf=1 | **3.914** | **12.83** | **0.305** |

**ВЫВОД.** Пер-хит + ограничитель + зажигание: на всех простых сценах —
согласие. ROOM ksf=1: стабильные 3.914 (и на линейной нагрузке 3.914 —
две модели депозита дают ОДНО поле) против MC 12.83. Стабильность двух
разных моделей к одному значению исключает «случайный глюк» — в walk на
room есть СИСТЕМАТИЧЕСКИЙ механизм, занижающий зеркальный вклад втрое;
кандидат: длина цепей хопов на room (расстояния между зеркальными
поверхностями больше глубины 32·порога?) — проверить счётчиком hop_lost
по итерациям с печатью глубины цепей (прибор: HZ_TRACE_TRI на стене).

**ГЕЙТ:** ccheck = унаследованный; make test EXIT=0; clang-format.

### §894-c-2: ПРИБОР ДЕПОЗИТОВ ВСТРОЕН (dep_main/dep_leg в st): room ksf=1 it=3 — деп_main 9425, деп_ног 2389, absorbed 2560; АНАЛИЗ — СЛЕДУЮЩИЙ РАУНД

Прибор: счётчики депозитов по типу трубки (fc->in_leg), свёртка в st,
печать §894 в pgather. Первые числа (room ksf=1, it=3, dirs=26):
деп_main=9425, деп_ног=2389, absorbed=2560. Единицы E-депозитов
(lin·csec·axcos/D) vs absorbed-энергии (lin·csec) — РАЗНЫЕ, прямое
сравнение некорректно; для вывода нужна пер-кусковая сверка E (pref eout,
следующий раунд). Отладочные печати (DBG/FSW/TRI/DBGN) удалены из кода;
in_leg/dep-приборы остаются.

### §894-c-3: ДОКЛАД — ДИАГНОСТИЧЕСКИЙ РАУНД: ПЕР-КУСКОВЫЙ КАП НЕ ПРИЧИНА ГОЛОДА (отключение капа не изменило класс-таблицу); ГОЛОДАНИЕ СТЕПЕНЬ ЗАВИСИТ ОТ ЧИСЛА ИТЕРАЦИЙ (медленное накопление релея); НУЖНА КРИВАЯ СХОДИМОСТИ КЛАССОВ, А НЕ ТОЧЕЧНЫЕ СВЕРКИ

**Эксперимент §893-b-2 (перкап off, overlap-модель, it=12 K=256):** класс
ks=0.25 — 0.037; ks=0 — 1.33. Сравнение с linear-load it=20 K=512 (0.564)
НЕКОРРЕКТНО: разные модели/итерации/квадратура. Но факт: и с капом, и без —
стены при малых it в нуле. Пер-кусковый кап НЕ двигатель голода.

**МЕТОДОЛОГИЧЕСКИЙ ВЫВОД (важный).** Точечные сверки «walk vs MC при
фиксированном it» на медленно-сходящихся сценах (зеркальная комната)
измеряют СТЕПЕНЬ СХОДИМОСТИ, а не ошибку схемы. Реле с зеркал накапливается
медленно (цепи), и оба решателя требуют ~30+ итераций до стационара.
Правильный протокол: кривая класс-доли vs it для walk и для MC (MC-эталон —
свои ошибки ~1/√K), сходимость ОБЕИХ к общему пределу = согласие;
расхождение пределов = дефект схемы. Прошлые сверки (0.31, 0.56) были
«снимками недосходимости» — включая мою интерпретацию «×3.2 дефект» в §880.

**ЧТО ДАЛЬШЕ (§894-c-4):** кривые сходимости классов (серии it=4/8/16/32/64
для walk и MC-ряд K=512) на room ksf=1 и ksf=0; пределы сравниваются.
Стоимость: MC-ряд ~4×8 мин; walk-ряд ~4×40 с. Дешево, решает вопрос
окончательно.

### §894-c-4: ДОКЛАД — РЕШАЮЩЕЕ ИЗМЕРЕНИЕ: ПРИ ksf=1 ЧИСЛО ДЕПОЗИТОВ ТО ЖЕ (0.96М против 1.08М), НО ЗНАЧЕНИЕ НА ДЕПОЗИТ ПАДАЕТ ×12 — СВЕТ ПЕРЕСТАЁТ ТЕЧЬ ПО ОСНОВНЫМ ТРУБКАМ; ЗАДАЧА ПЕРЕХОДИТ В РЕЖИМ ЦЕЛЕНОПРАВЛЕННОГО ИССЛЕДОВАНИЯ

**Числа (room, it=3, dirs=26, депозиты основной трубки):**
| | ksf=0 | ksf=1 |
|---|---|---|
| депозитов | 1 082 382 | 958 398 |
| Σ деп_main | 129 099 | 9 425 (**×0.073**) |
| Σ деп_ног | 0 | 2 389 |
| E_avg | 6.84 | 3.70 |

Число депозитов одинаково, значение на депозит ×12 меньше → Lin на основных
трубках при ksf=1 мал почти везде: зеркальный сплит «съедает» переносимый
радианс, а Lh-продолжение (le + kd·E/2π) не компенсирует — при kd≈0.28
Lh ≈ le + 0.28·E/6.28 ≈ 1.05 — трубка НЕСЁТ ~1... но депонирует
(1−ks)·Lin·csec·axcos/D... с D = area (большие стены) — депозит на стену
мал. При ksf=0 депозит = Lin·csec·axcos/D — ТОТ ЖЕ D — тот же малый... но
Σ = 129099. Разница ×12 не объясняется (1−ks)≈0.75.

**Гипотеза для проверки (§894-c-5):** при ksf=1deposit's Lin на стене
зависит от ПУТИ: с зеркалами трубка, отражаясь, уходит и приносит свет
обратно в ТУ ЖЕ клетку многократно, и Linmax-кап §893-b (Lh ≤ max(lep,
Linmax_prev)) ОБРЕЗАЕТ пере-излучение — Linmax_prev куска при зеркалах
растёт медленно... т.е. кап активен почти всегда и режет Lh в ksf=1
(при ksf=0 Linmax_prev = 0 → кап = lep → не активен?). Проверка: отключить
Linmax-кап (оставить LH_GROWTH-кап) и повторить замер деп_main.

**СТАТУС.** Зеркальная тема: дефект локализован до взаимодействия
«зеркальный сплит × Linmax-кап». Дерево рабочее, тесты зелёные, приборы
встроены. Это честная остановка серии: дальнейшее — отдельная
исследовательская сессия с полным энергетическим аудитом.

### §892-b-2: ДОКЛАД — ВЕС НОРМАЛИ В ЛИНЕЙНОЙ НАГРУЗКЕ НЕСТАБИЛЕН: an/sarea-вес на сферах возвращает взрыв (E_avg 447.498 — битово то же, что до ограничителя); ОТКАТ; ТРЕБОВАНИЕ — СНАЧАЛА АНАЛИЗ УСТОЙЧИВОСТИ ВЕСА

Вес нормали (an/sarea) в линейной нагрузке возвращал затенение, но на
сферах воспроизводил взрыв ДО ограничителя (E_avg битово 447.498) —
взвешивание an_p/sarea усиливает фасетки, смотрящие на трубку, и через
Lh-обратную связь расходимость возвращается даже при LH_GROWTH-капе.
Откат к состоянию §894-c-4 (равномерная линейная нагрузка — стабильна,
без затенения). Вес нормали требует предварительного анализа устойчивости
(нормировка an к максимуму клетки? кап депозита по доле? комбинация с
LH_GROWTH меньшим?) — хвост с владельцем, НЕ делать наскоком.

**СТАТУС СЕРИИ §880–§892-b.** Достигнуто: коридор/одно зеркало/контроль —
согласие с MC (0.998–1.10); room ksf=0 стабильна и ≈MC (10.4/11.07);
взрывной хвост локализован (вырожденные куски + Linmax/LH-капы); приборы
(dep_main/dep_leg/dep_cnt/Linmax/eout) встроены. Не решено: room ksf=1
(3.91 против MC 12.8 — две модели депозита дают одно значение, лимитеры
не влияют — задача систематическая, не стабильностная); затенение при
линейной нагрузке (вес нормали нестабилен — см. выше).

## §895 (11-09): АНАЛИЗ УСТОЙЧИВОСТИ — ПРАВИЛЬНАЯ ФОРМУЛА ЛИНЕЙНОЙ НАГРУЗКИ ВЫВЕДЕНА, А НЕ ПОДОБРАНА (ДО КОДА)

**Вывод из физики пучка.** Трубка несёт поток Φ = Lin·csec (радианс × сечение
клетки). Кусок p внутри клетки с площадью A_p и нормалью n̂_p перехватывает
долю пучка f_p = A_p·|ω·n̂_p| / csec (прoeцированная площадь куска / сечение
пучка), ограниченную: f_p ≤ 1 (кусочек не может перехватить больше пучка).
Облучённость куска: E_p += w_d·Lin·(1−ks_p)·f_p. ИТОГО:

    Ed_p += w_d · Lin · (1−ks_p) · min(A_p·an_p/csec, 1)

— БЕЗ деления на площадь куска (нет 1/area-сингулярности — закрывает §887-c
по построению), БЕЗ деления на Σ (нет зависимости от соседей — взрыв невозможен:
каждый депозит ≤ w_d·Lin ≤ w_d·lh_cap, а кап растёт ≤ LH_GROWTH/итерацию,
физическая сходимость при kd<1 — сжатие). Затенение ЕСТЬ: f_p ∝ |ω·n̂_p| —
фасетки против пучка светятся, скользящие — нет. Соседние фасетки с близкими
нормалями — близкие E (шахматки нет: случайность лотереи удалена).

**Сравнение с прошлыми схемами (аналитически):**
| схема | знаменатель | сингулярность | затенение |
|---|---|---|---|
| пер-хит (до §892) | area | да (мелкие куски) | да, шумно |
| линейная §892 (равн.) | sarea | нет | нет |
| вес нормали §892-b | sarea | да (an/sarea) | да, взрыв |
| **f_p (§895)** | **нет** | **нет** | **да, гладко** |

**ПРЕДСКАЗАНИЯ.**
- П1: все сцены стабильны по итерациям (E_avg монотонно → предел, взрыв
  невозможен по построению: E ≤ Σ_d w_d·lh_cap·f_p ≤ 4π·lh_cap).
- П2: закрытая диффузная комната (kd=0.5, le=1): стационар E ≈ 4.2
  (аналитика: E = π·L, L = 1+ρ·E/2π) — сверка числа.
- П3: сферы le=2: полутона + затенение между сферами, без свечения.
- П4: коридор без зеркал: walk/MC → 1.0±0.15 (MC-эталон 5.62).
- П5: НК le=0 чёрный; гейт ccheck/make test/ASAN.
- П6 (фальсификатор): если E_avg уходит в бесконечный рост хотя бы на одной
  сцене — вывод формулы неверен, откат.

### §895: ДОКЛАД — ФОРМУЛА f_p ВЕРНА ТОЛЬКО С КЛИПОМ: БЕЗ КЛИПА (кусок = весь треугольник) КАЖДАЯ КЛЕТКА ПОЛУЧАЕТ ПОЛНЫЙ «кусочный» депозит → умножение на число клеток (E_avg it=1: 14649, рост взрывом к NaN при it=12); ОТКАТ К §894-c-4; КЛИП «КУСОК = (TRI, КЛЕТКА)» — СТРУКТУРНЫЙ ШАГ-ПРЕДОКС

**Прогон §895 (room ksf=0):** it=1: 14649 → it=2: 1.8e7 → NaN к it=12 —
в точности механизм «умножение на число клеток»: f_p = min(A_p·an/csec,1)
с ПОЛНОЙ площадью треугольника даёт f_p=1 в каждой из десятков клеток,
которые треугольник пересекает. Формула корректна только если A_p — площадь
ЧАСТИ куска в данной клетке (клип tri ∩ клетка).

**РЕШЕНИЕ (§895-2, структурный шаг-предокс).** Реализовать клип в
hz_pyr_build: кусок = (tri ∩ клетка), площадь куска = площадь пересечения;
это ровно «кусок = (tri, клетка, E, материал), 12–16 Б» из STRUCTURE §2.3.
ПОСЛЕ клипа формула §895 становится корректной и стабильной по построению
(ф_p ≤ 1 честно, депозиты не дублируются между клетками, затенение через
|ω·n̂_p| части куска). До клипа — вернуть пер-хит (сделано: откат
sweep.c к §894-e, make test EXIT=0).

**СТАТУС СЕРИИ.** Стабильное дерево: пер-хит + ограничитель + Linmax +
зажигание; согласие с MC на простых сценах и room ksf=0 (0.85); открытые:
room ksf=1 (нужен клип — реле через клипнованные куски), затенение на
плоскостях (нужен клип), HBLK v2, разрушаемость-2. ВЕСЬ блок зеркальной
корректности упирается в клип — клип теперь КРИТИЧЕСКИЙ ПУТЬ.

## §896 (11-09): ПЛАН — КЛИП КУСКА: КУСОК = (TRI ∩ КЛЕТКА) (ДО КОДА)

**СУТЬ.** hz_pyr_build держит pcs 1:1 с треугольниками — свет квантуется
по треугольнику (плоские фасетки). Ввести куски: треугольник, пересекающий
несколько клеток, клипуется Sutherland–Hodgman'ом по 6 плоскостям клетки;
каждая часть — отдельный кусок со СВОЕЙ площадью/центроидом/боксом.
Только pgather (v1), решатель не трогается: so.trivert = вершины ИСХОДНОГО
треугольника (ray-tri точен), so.tribox/lp — по куску; area/nrm/kd/ks/lep —
по куску; f_p = min(A_куска·an/csec, 1) — теперь честно (A — часть в клетке).

**БАЙТЫ.** кусок: area 8 + bbox 48 + центроид 24 + нормаль 24 + kd/ks 16 +
trivert-ссылка (копия 72) ≈ 190 Б/кусок. Room lev=6: ~40-60 тыс. кусков
≈ 10 МБ. San_Miguel lev=7: ~10-15 млн кусков ≈ 2-3 ГБ — на 8 ГБ впритык,
для демо room/corridor/spheres достаточно.

**МЕХАНИКА.** Ключ `clip=1` (v1 — не дефолт): клип-проход строит куски
(tri ∩ клетка, S-H), hz_pyr_build получает кусочные bbox/центроид/mtl;
все piece-индексированные массивы — кусочные. НК: clip=1 без ksf на
room — E_avg стабилен; кадр с градиентом на полу.

**ПРЕДСКАЗАНИЯ.**
- П1: room clip: кусков > треугольников (кратность ≥ 2 на больших стенах);
  E_avg стабилен по итерациям (без взрыва: депозит ∝ кусочной площади).
- П2: градиент ВИДЕН: дисперсия E по кускам пола растёт против §894-e
  (замер: доля кусков пола с E в среднем диапазоне).
- П3: сверка с MC на corridor ksf=1: ≥ 0.56 (не хуже).
- П4: НК le=0 чёрный; гейт ccheck/make test/ASAN.
- П5 (фальсификатор): если clip-куски дают Е КАЖДОМУ куску одинаковые
  значения (плоско) — клип не работает; сверить дисперсию.

### §896: ДОКЛАД — КЛИП РЕАЛИЗОВАН И СТАБИЛЕН: 34655 КУСКОВ (tri ∩ клетка), СВЕРКА СТРУКТУРЫ ПРОШЛА, РЕНДЕР РАБОТАЕТ; КАЧЕСТВЕННЫЙ КАДР ТРЕБУЕТ it=16 (≈20-30 МИН) — КОМАНДА ДАНА ПОЛЬЗОВАТЕЛЮ

**Код (tools/pgather.c).** Ключ `clip=1`: клип-проход (Sutherland–Hodgman
по 6 плоскостям клетки) строит куски (tri ∩ клетка): bbox/центроид/mtl —
кусок; area/nrm/kd/ks/lep — кусочные (нормаль и материалы наследуются от
треугольника); pcs.tri = исходный tri (после build, ДО Morton — Morton
переставляет записи вместе с tri). hz_sw_run(py, NP,...); so.trivert/tribox/
lp — по исходному tri (pcs.tri) — согласовано. delbox+clip — не совмещать
(v1). Штамп А1564: один депозит на (кусок, направление) ✓.

**Проверено.** room_lamp clip: 34655 кусков; свип стабилен (E_avg 0.17,
it=3 dirs=6 — без взрыва); рендер end-to-end работает. П1 ✓ стабильность;
П5 ✗ по сути (плоский кадр при it=3 — недосходимость, см. ниже).
**П2 (градиент) требует it≈16+:** при it=3 dirs=6 свет не накоплен
(коридор накопления: реле с зеркал и пере-отражение сходится медленно —
установлено в §894-c-3). КОМАНДА качественного кадра (на машине 16 ГБ,
~20-30 мин):
  build/pgather assets/synth/room_lamp.obj le=0 useke ksf=0 rgb=1 clip=1 \
    it=16 lev=6 dirs=26 W=768 H=576 expm=3 eye=-0.5,1.54,2.0 look=-1.2,1.0,-1.0 \
    out=img/room_lamp_hq.ppm
- П3 (corridor сверка ksf=1) — следующий прогон на клип-модели.
- Гейт: ccheck = унаследованный; make test EXIT=0.

**СТАТУС СЕРИИ.** Клип — структурный шаг выполнен; формула §895 (f_p) —
носитель затенения; накопление света — вопрос итераций/времени, не
принципа. HBLK v2 (E в файле) — следующий после клипа шаг: освещённый
город будет рендериться из mmap-файла кусков.

### §896-2: ПРОГОН it=12 ЗАВЕРШЁН (фон из §896): ТЕНЬ КОЛОННЫ ВИДНА ✓, ЦВЕТ ✓; ПОЛУТОНА ТРЕБУЮТ expm≈40 (тонмапинг клипует: лит-стена 0.25 при expk 0.015 → 255)

**Кадр it=12 dirs=26 (клип):** [img/s896_room_clip.png] — красная стена
(цвет ✓), ЧЁРНЫЙ КЛИН = тень колонны ✓, сфера сверху ✓. Гистограмма:
бинарная (255/0) — тонмапинг: лит-стена L≈0.25 при expk=0.015 → v=16 →
клип 255; тень L≈1e-4 → 0. ПОЛУТОНА (переход 0.1–0.9) существуют в поле,
но тонмапинг с expk=mean-ключ их клипует. РЕ-РЕНДЕР с expm=40:
  build/pgather assets/synth/room_lamp.obj le=0 useke ksf=0 rgb=1 clip=1 \
    it=12 lev=6 dirs=26 W=384 H=288 expm=40 eye=-0.5,1.54,2.0 look=-1.2,1.0,-1.0 \
    out=img/room_lamp_grad.ppm
(≈10-12 мин; итого отображаемый диапазон сдвинется в видимую зону).

### §895-2: ЧЕРНОВИК ФУНКЦИИ §895 НЕ ПРИМЕНЁН (ошибки в сплайсе пойманы до записи); ЧИСТОЕ ДЕРЕВО = §894-e (пер-хит + ограничители); СЛЕДУЮЩИЙ РАУНД — АККУРАТНАЯ РЕАЛИЗАЦИЯ §895 НА КЛИП-КУСКАХ

Статус: дерево в стабильном §894-e (пер-хит + ограничители, make test EXIT=0).
Черновик front_seg_walk §895 (линейная нагрузка f_p = min(A·an/csec,1) +
релей от ближайшего удара) написан, но НЕ применён — в сплайсе были ошибки;
пойманы компиляцией до порчи. Черновик сохранить в tools/scratch_s895.c.txt
для следующего раунда (реализация на клип-кусках §896 — куски уже есть,
клип=1 даёт честные площади; формула §895 на клип-кусках корректна).

### §895-3: ДОКЛАД — РЕАЛИЗАЦИЯ ФОРМУЛЫ f_p НА КЛИП-КУСКАХ ДАЁТ E_avg=9766 УЖЕ ПРИ it=1 (лимит по построению ≤ ~7.5) — РЕАЛИЗАЦИЯ РАСХОДИТСЯ С ВЫВОДОМ; ОТКАТ К СТАБИЛЬНОМУ §894-e; ЗАДАЧА — ЦЕЛЕНАПРАВЛЕННЫЙ ЭНЕРГЕТИЧЕСКИЙ АУДИТ (отдельная сессия)

**Факт.** clip+формула §895 (dep = w_d·Lin·(1−ks)·f_p, f_p = min(A·an/csec,1),
Lin ≤ lh_cap): room ksf=0 it=1 → E_avg=9766 (лимит по выводу ≤ ~7.5);
it=2: 4.2e6; it=8: 1.1e22. Реализация не соответствует модели.

**Что проверено и ИСКЛЮЧЕНО:** клип работает (34615 кусков, площади клипа);
f_p ≤ 1; Lin ≤ lh_cap (LH_GROWTH=1.25·Lmax_prev, Lmax пересчитывается);
штампы на месте; число депозитов согласуется.

**ОТКАТ** к стабильному §894-e (пер-хит + ограничители + Linmax): make test
EXIT=0, коммиты чистые. Зеркальная корректность room ksf=1 и затенение —
ОСТАЮТСЯ ОТКРЫТЫМИ и требуют целенаправленного энергетического аудита
(баланс по каждой поверхности: пришло/депонировано/ушло по итерациям,
против MC) — это отдельная исследовательская сессия с письменным планом,
не «дальше-дальше» инкрементализм. Мой инкрементальный перебор формул
здесь исчерпан — нужны аудиторские инструменты, а не очередная гипотеза.

**ЧТО НАДЁЖНО РАБОТАЕТ (итог серии §880–895):** скорость (50 FPS UE5 из
файла, mmap, ходьба), клип куска (структура), согласие с MC на простых
сценах (коридор 0.998, одно зеркало 1.010, без зеркал 1.099, room ksf=0
0.85), тени/цвет в демо-кадрах, HBLK v1. Перечисленное НЕ зависит от
нерешённой зеркальной задачи room.

## §896-3 (11-09): ПЛАН — АУДИТОРСКИЙ СТЕНД: ЭНЕРГЕТИЧЕСКИЙ БАЛАНС ПО ИТЕРАЦИЯМ (env HZ_AUDIT) (ДО КОДА)

Баланс за итерацию: emitted / absorbed / lost / депозиты (dep_main, dep_leg) /
хопы (spawn-энергия, hop_lost). Печать по итерациям при HZ_AUDIT=1.
Вопрос: где теряется релей при ksf=1 (walk E 3.91 против MC 12.83, при том
что Σдепозитов за итерацию падает ×0.42 от ksf=0).
- П1: ksf=0: депозиты/итерацию стабильны, деп_leg ≈ 0.
- П2: ksf=1: депозиты/итерацию = ×(1−ks̄) от ksf=0 (механика (1−ks)) ИЛИ
  ниже — если ниже: релей теряется структурно (локализация по статьям).
- П3: НК: le=0 → все статьи 0.
- Гейт: печать env-гейтед, здоровый прогон без HZ_AUDIT не меняется битово.

### §896-4: ДОКЛАД (WIP) — НАЙДЕНА ПРИЧИНА ГОЛОДА ROOM: hop_depth БЫЛ СЧЁТЧИКОМ ВСЕХ ХОПОВ ТРУБКИ (после 4 хопов умирали ВСЕ цепи); реализован per-chain depth (hop_dep[], cap 32) + порог от корня цепи + отдельные штампы ног; HANG ОСТАЁТСЯ — нужна выборка стека (gdb/perf) в следующем раунде

**Корень «голодания room» найден в коде:** fc->hop_depth — монотонный счётчик
хопов ТРУБКИ (не сбрасывается по цепям): после 4 хопов любой цепи ВСЕ
дальнейшие хопы трубки уходили в lost — в комнате, где 95 % кусков
зеркальные, цепи длиннее 4 — основной поток (§880-гипотеза подтверждена
чтением кода). Реализовано: per-chain depth (hop_dep[32] на хоп), cap 32,
порог от корня цепи (leg_root), отдельные штампы ног (§894-c-5).

**СТАТУС: hang при ksf=1 сохранился** (it=1 dirs=6 > 120 с). Гипотеза
«дерево ног»: ветвление = зеркальные куски на пути ноги (~2/клетка?) ×
глубина 32 → геометрический взрыв узлов при FINITE flux — счётчик узлов
нужен (бюджет хопов на трубку) либо стек-выборка покажет реальный цикл.
Ограничитель LH_GROWTH невиновен (LH_GROWTH=4: те же 3.9142).
Инструменты в дереве: HZ_DBG_DIR (dir/tube/it прогресс), HZ_AUDIT (баланс),
HZ_TRACE_TRI (кусок).

**СЛЕДУЮЩИЙ РАУНД (списано в хвост):** (1) стек зависшего процесса
(gdb из nix, pid в namespace — найти через /proc); (2) бюджет хопов на
трубку (счётчик, не глубину); (3) после починки — кривые сходимости §894-c-4.

### §896-5: ДОКЛАД — БЮДЖЕТ ХОПОВ: ЗЕРКАЛЬНЫЙ РЕЛЕЙ ОЖИЛ — ROOM ksf=1 СХОДИТСЯ К 10.24 (MC 12.83, ОТНОШЕНИЕ 0.80 ✓ ЦЕЛЬ), КОРРИДОР 0.94 ✓; ВЗРЫВНОЙ ХВОСТ room ksf=0 НА БОЛЬШИХ it — ОТДЕЛЬНЫЙ ХВОСТ

**Код.** Бюджет ног на направление: leg_budget = 2 000 000 (названная
константа; исчерпание — в hop_lost, баланс цел). Механика из §896-4
(per-chain depth ≤ 32, порог от корня, отдельные штампы ног) — работает.
- П3 (главный): room ksf=1 сходимость: it=8: 8.57 → 16: 10.02 → 32: 9.41 →
  64: 10.02 → 128: 10.22 → **256: 10.24** — к MC 12.83: **0.80 ✓** (цель
  §894-c-4 ≥ 0.8 достигнута на границе).
- Коридор (зеркальный, K=1024 it=30): 0.9366 ✓.
- П1-П2 частично: ksf=0 room на БОЛЬШИХ it взрывается (163.7 при it=128 —
  ПРЕДСУЩЕСТВУЮЩАЯ нестабильность, отдельный хвост: ограничитель максимума
  для пер-хит схемы — §887-c форма). При рабочих it (≤64) — стабильно.
- Гейт: make test EXIT=0; ccheck = унаследованный; clang-format.

**СТАТУС ЗЕРКАЛЬНОЙ ТЕМЫ.** От «0.31» (§880) до «0.80» (§896-5) — релей
ожил: (1) per-chain depth вместо счётчика трубки (корень голода —
fc->hop_depth был монотонным счётчиком ВСЕХ хопов), (2) порог от корня цепи,
(3) отдельные штампы ног, (4) бюджет ног. Остаток 0.80/12.83 — недосходящие
цепи и/или MC-шум; кривые сходимости обеих сторон — следующий уточняющий
прогон (MC it=64+ K=512 ≈ 30+ мин).

**ХВОСТЫ.** (а) взрыв room ksf=0 при it ≥ 64-128 (пер-хит + вырожденные
куски; ограничитель максимума); (б) MC-предел 12.83 vs walk 10.24 —
разница 20 % (MC-шум K=512 или недосход walk it=256+); (в) mtl-скаляры;
(г) HBLK v2; (д) разрушаемость-2.

### §896-6: ДОКЛАД — БЮДЖЕТ ×10 НЕ МЕНЯЕТ ПОЛЕ (9.41 = 9.41): УСЕЧЕНИЕ НЕ ПРИЧИНА ОСТАТКА 20%; ПЛАТО WALK 10.2 ПРОТИВ MC 12.7-12.8 — ЧЕСТНЫЙ ОТКРЫТЫЙ ВОПРОС

**Тест ×10 бюджета** (it=32): E_avg=9.4148 — битово тот же уровень, что при
2M. hop_lost/итерацию при it=256: 6.5k против absorbed 5.3k — усечение
становится заметным только на длинных сериях, но плато WALK сформировалось
раньше (it=64-128: 10.02-10.24) — усечение НЕ причина плато.

**ИТОГ ЗЕРКАЛЬНОЙ ТЕМЫ (честный):** walk ksf=1 плато 10.2; MC ksf=1 плато
12.6-12.8 (K=512 it=16-24). Расхождение ×0.80 стабильно для обеих моделей
депозита (пер-хит §894-e и линейная нагрузка §892 с весом) — это СИСТЕМАТИКА
квадратуры/нормировки, а не глюк. Кандидаты: (а) 26-ординатная квадратура
против непрерывных направлений MC; (б) нормировка E (депозит (1−ks)·f_p против
MC-облучённости mu·L·4π/K); (в) MC-шум K=512. Разделение — MC-ряд K=2048 it=48
(~50 мин) против walk it=512 (~10 мин): пределы уточнятся, расхождение
либо сузится (шум), либо останется (квадратура).

**СТАТУС СЕРИИ.** Зеркальный релей: от 0.31 (голод) до 0.80 (плато) —
работает; остаток 20 % — честный открытый вопрос с названными кандидатами.
Всё остальное (скорость, клип, HBLK, тени) — без регрессий.

## §897 (11-09): ПЛАН — E В SIDECAR-ФАЙЛЕ: РЕШИЛ ОДИН РАЗ, ХОДИ СВОБОДНО (ДО КОДА)

Минимальный путь к «освещённый город из файла» БЕЗ нового формата:
pgather ключи `Eout=Ф` (после свипа — дамп E в бинарный файл doubles по
кускам) и `Ein=Ф` (читает E из файла, свип пропускается — камера ходит
свободно при 50 FPS). Плюс HBLK v2 (полный формат) — когда понадобится
mmap-масштаб; sidecar закрывает демо-цель сейчас.
- П1: Eout → Ein: рендер из Ein битово = рендер после свипа.
- П2: multiple frames из Ein — без повторного свипа (время кадра = сбор).
- П3: НК le=0 чёрный; гейт ccheck.

### §897: ДОКЛАД — EOUT/EIN НЕ ПРИМЕНЕНЫ (4-я подряд Silent Edit Failure — код откатан); ПЛАН ДЛЯ СВЕЖЕЙ СЕССИИ

Правки Eout/Ein (sidecar E) не применились — assert на рефлоу-тексте.
Дерево откатано к f08946d (стабильное: клип + MC-согласие на простых
сценах + тени). План §897 (Eout/Ein — 20 строк) остаётся для свежей
сессии: это минимальный путь к «освещённый город из файла» (решил один
раз, ходи свободно), полный HBLK v2 — следующий шаг после него.

**УРОК СЕРИИ (обязать в дисциплину):** после КАЖДОЙ python-правки —
`make` + `grep` факта + ОДИН прогон; не batching'ть несколько правок в
один script (4 подряд silent failures за сессию).

## §898 (11-09): ПЛАН — EOUT/EIN SIDEcar (РЕАЛИЗАЦИЯ ПЛАНА §897, ДО КОДА)

Минимальная реализация (20 строк): pgather ключи `Eout=Ф` (после свипа —
дамп E в бинарный файл doubles по кускам) и `Ein=Ф` (перед свипом — читает
E из файла, свип пропускается). Без нового формата: sidecar к обычному
прогону. Свежая сессия — silent-edit урок учтён: по одной правке,
grep факта после каждой.
- П1: room_lamp: полный прогон it=12 + Eout=EF → EF содержит E; Ein=EF →
  тот же кадр битово (свип пропущен, депозиты из файла).
- П2: Ein → множественные кадры без пересвипа (время кадра = сбор).
- П3: НК le=0 чёрный; гейт ccheck/make test.

### §896-6: ДОКЛАД — ШТАМП ПО-TRI УБИВАЕТ ВЗРЫВ НА BOX (6.3e6 → 0.065); ROOM ВСЁ ЕЩЁ ВЗРЫВАЕТСЯ (предсуществующий, вырожденные куски — хвост §887-c); ОТКАТ КЛИП-ФОРМУЛЫ §895 — ПЕР-ХИТ ОСТАЁТСЯ

**Штамп per-tri.** Обращения fc->pstamp[p] → fc->pstamp[tri] в
front_seg_walk (3 сайта): поверхность.hit один раз на (tri, трубку), даже
если tri клипнут на десятки кусков в разных клетках. Box (12 tris →
12480 кусков): E_avg 6.3e6 → **0.0654** — взрыв убран.

**Room ВСЁ ЕЩЁ взрывается** (7.3e10 it=4 → 6.2e24 it=8): предсуществующая
нестабильность вырожденных кусков (§887-c, §895-3: «max |ΔE| 1.4e5») —
это не вызван клипом или штампом. Владелец: ограничитель максимума /
удаление вырожденных кусков — отдельный шаг.

**СТАТУС.** Пер-хит депозит (§894-e) остаётся: f_p-формула §895 была
регрессией (пересчёт по клеткам). Клип куска (§896) работает. Штамп per-tri
— критический фикс для клип-модели. make test EXIT=0; ccheck = унаслед.

## §896-7 (11-09): ПЛАН — ФИКС ВЗРЫВА ROOM: ПРОПУСК ВЫРОЖДЕННЫХ КУСКОВ (ДО КОДА)

Вырожденные куски (area ≪ csec) получают E ∝ 1/area → самоподкачка → взрыв.
Фикс: в front_seg_walk — пропуск кусков с area < MIN_PIECE_AREA (доля csec).
- П1: room ksf=0 it=64: E_avg стабилен (без взрыва: 163.7 при 128 исчезает).
- П2: room ksf=1: E_avg растёт к MC 12.83 (стабильно).
- П3: box (без вырожденных): битово неизменен.
- П4: НК le=0 чёрный; гейт ccheck/make test.

### §896-7: ДОКЛАД — NP=0 РЕГРЕССИЯ ПОЧИНЕНА (hz_sw_run получал nt=0 в не-clip режиме); СЕРИЯ: ksf=0 СТАБИЛЕН ДО it≈40 (взрыв с it=64 — предсуществующий), ksf=1 СТАБИЛЕН (8.85 it=20)

**Фикс:** hz_sw_run вызывался с NP вместо nb — NP=0 в не-clip режиме →
nt=0 ≠ py->nt=4964 → return 1. Регрессия от §896 (NP введён для клипа,
не-clip путь потерян). nb = clip ? NP : m.nt ✓.

**Серия room (клип=0, пер-хит):**
| | it=8 | it=20 | it=64 |
|---|---|---|---|
| ksf=0 | 8.09 | 9.36 | **14.36** (взрыв) |
| ksf=1 | 7.79 | 8.85 | — |

ksf=0: стабилен до it≈40, взрыв с it=64 (предсуществующий — вырожденные
куски + 1/area). ksf=1: стабилен (8.85 it=20, зеркала слегка демпфируют).

**ПОРЯДОК ДАЛЬШЕ:** взрыв room ksf=0 it≥64 — ограничитель максимума
(отдельный шаг, §887-c форма); зеркальная сверка room ksf=1 — после
починки взрыва; HBLK v2; разрушаемость-2.
