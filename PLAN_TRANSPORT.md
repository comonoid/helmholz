# PLAN_TRANSPORT.md — вторая линия: перенос излучения

Начат 2026-07-27. Волновая линия (PLAN.md, `src/` кроме `src/transport/`)
ОСТАЁТСЯ: она нужна для голограмм и когерентной оптики и она же поставляет сюда
BRDF для микрорельефа.

## ЧТО СТРОИМ, ОДНИМ АБЗАЦЕМ

Объёмное уравнение переноса, решаемое **развёрткой по дискретным ординатам** с
разрывным Галёркином первого порядка по пространству. Поверхностное уравнение
рендеринга входит в него **граничным условием**, а не отдельной формулировкой.
Резкие концы пути (источник → первое рассеяние, последнее рассеяние → пиксель)
считаются точно и вне сетки направлений; на ординаты ложится только гладкая
середина. Поле хранится и от камеры не зависит: движение камеры — сбор, а не
пересчёт.

## ОСНОВАНИЕ ПЕРЕХОДА (измерено в волновой линии 07-26/27)

**ND ≈ W / ℓ_⊥** — число направлений в ячейке равно числу длин пространственной
когерентности, которые в неё помещаются. Для одномодового источника ℓ_⊥ метры
(сцена 4 м = 111 неизвестных, 21 итерация, 11 мс); для лампы 15 мкм, и ячейка
обязана быть 15 мкм. Разбор — PLAN.md, «ГЛАВНАЯ ФОРМУЛА». Перенос выбрасывает
фазу ДО расчёта, и угловое разрешение начинает задавать наблюдатель.

## УРАВНЕНИЕ

    в объёме:   (ω·∇)L(x,ω) = −σ_t(x,ω)·L + σ_s(x)∫_{S²} p(x, ω'·ω)·L(x,ω')dω' + ε(x,ω)
    на границе: L(x,ω_o) = L_e + ∫_{H²} f_r(x, ω_i→ω_o)·L(x,ω_i)·|ω_i·n|·dω_i

σ_t ЗАВИСИТ ОТ НАПРАВЛЕНИЯ: `σ_t(x,ω) = G(ω)·u_L`. Для растительности это не
уточнение, а необходимость: стебли пшеницы почти вертикальны, поперёк поля не
видно ничего, а сверху видна почва; скалярным σ_t это не передаётся вовсе.

## КАТАЛОГ ПОСТАНОВОК — СОХРАНЁН, ЧТОБЫ НЕ ИСКАТЬ ЗАНОВО

Записано 07-28 по просьбе пользователя. Это НЕ варианты записи одного и того же:
у каждой постановки своя цена и свои запреты, и путать их дорого — один раз уже
вышло, что разбор вёлся по форме, которую проект отверг за год до того.

**1. Кайи, полусферная (1986).**
`L(x,ω) = L_e + ∫_{H²} f_r(x, ω′→ω)·L(x,ω′)·|ω′·n| dω′`
Выглядит локальной, но `L(x,ω′)` требует знать, что видно из `x` в направлении
`ω′`. **Луч спрятан в обозначении.**

**2. Трёхточечная (площадная).**
`L(x→y) = L_e + ∫_S f_r(z→x→y)·L(z→x)·G(x,z)·V(x,z) dA(z)`
Видимость выписана явно. **ОТВЕРГНУТА этим проектом**, причины ниже в «истории
выбора». Локальный базис здесь разреженности НЕ даёт: два потенциала с
непересекающимися носителями связаны, потому что свет между ними летит. Это и
есть форм-факторная плотность радиосити.

**3. Перенос, дифференциальная форма — ТА, ЧТО У НАС.**
`(ω·∇)L = −σ_t·L + σ_s ∫ p(ω′·ω)·L dω′ + ε`
Оператор локален по ПРОСТРАНСТВУ; нелокальность остаётся только по УГЛУ и только
в одной точке — блок `N_угл × N_угл` на ячейку, у ламбертовой поверхности ранга
один. Отсюда разреженность. Цена — `ω` становится измерением.

**4. Она же в интегральной форме вдоль характеристики** (volume rendering
equation): `L = ∫₀ˢ T(t)·[σ_s∫pL + ε] dt + T(s)·L_гр`, `T` — пропускание.
Это уравнение 3, проинтегрированное вдоль луча; **марш получается именно
отсюда**, и потому марш есть применение точного обратного оператора, а не
альтернатива решению.

**5. Операторная.** `L = L_e + T·L`, то есть `L = (I − T)⁻¹·L_e = Σ Tⁿ·L_e`.
Ряд Неймана ЕСТЬ итерация по рассеянию; обращение оператора есть прямое решение.
Полезна тем, что делает очевидной цену: скорость ряда задаёт норма `T`, то есть
альбедо.

**6. Интегралы по путям (Veach, 1997).** `I_j = ∫_Ω f_j(x̄) dμ(x̄)` по
пространству ВСЕХ путей. Основа Монте-Карло. Нам не нужна, но знать надо: это
постановка, в которой выборка неизбежна по построению, и именно с ней
сравнивают, говоря «шум».

**7. Радиосити.** `B_i = E_i + ρ_i·Σ_j F_ij·B_j`. Конечная линейная система, но
ПЛОТНАЯ, и угол выброшен предположением о ламбертовости. Показывает развилку в
чистом виде: **либо угол выброшен и матрица плотная, либо матрица разрежена и
угол — измерение.** Обоих сразу не бывает.

**8. УРАВНЕНИЕ ИЗМЕРЕНИЯ И СОПРЯЖЁННАЯ ПОСТАНОВКА — не использована, а зря.**

    I = ⟨W_e, L⟩ = ⟨W, L_e⟩

`W` — ВАЖНОСТЬ, удовлетворяет СОПРЯЖЁННОМУ уравнению переноса с источником в
СЕНСОРЕ. Измеряемую величину можно получить, либо перенося радианс от
источников, либо перенося важность ОТ КАМЕРЫ.

**Чем она ценна здесь.** Вопрос «где именно нужно точное поле» она решает не
эвристикой, а УРАВНЕНИЕМ: `W` количественно есть «насколько это место влияет на
картинку». То есть LOD получает основание вместо правила `L = εR` по расстоянию,
и заодно становится ясно, где нужна тонкая угловая разрешающая способность (там,
где `W` сосредоточена по углу — за зеркалами в том числе). `W` линейна, поэтому
вся машинерия потенциалов, реза и разреженности переносится на неё без единого
изменения.

Не сделано ничего; записано как возможность, у которой есть своё имя.

## ИСТОРИЯ ВЫБОРА (сохранено, чтобы не переигрывать заново)

Сначала была выбрана трёхточечная форма (Kajiya 1986) с иерархической радиосити
(Hanrahan 1991): её ядро `G(x,z)` есть связь пары элементов и не зависит от
камеры — прямой аналог `⟨B_i, L B_j⟩`. Отвергнута по трём причинам:

1. преломление ломает ядро: путь между точками изгибается (задача Ферма);
2. видимость `V(x,z)` становится главной статьёй расходов, а её в волновой
   постановке не было вовсе — это был один из мотивов волновой линии;
3. густая трава и решётки дают `k²` = 10¹⁶ пар.

Дифференциальная форма всех трёх лишена: оператор локален, видимость нужна только
для прямого члена, `k²` не существует. И видимость, и попарный обмен суть
артефакты аналитического интегрирования свободного пролёта.

## ПРЕДСТАВЛЕНИЕ: ДВЕ ИЕРАРХИИ С РАЗНЫМИ ТРЕБОВАНИЯМИ

**Геометрия и среда.** Октодерево (есть). В узле одно из трёх:

- пусто: неизвестных нет, развёртка проходит узел одним шагом;
- среда: `σ_t(ω)`, `σ_s`, фазовая функция. **ПОСТОЯННЫЕ ПО ЯЧЕЙКЕ, И ПОСТОЯННАЯ
  ОБЯЗАНА БЫТЬ ЭФФЕКТИВНОЙ (гомогенизованной), а не выборкой в узле.** Строка до
  07-27 читала «линейно интерполируемые по узлу»; отменено `PLAN_CUT.md` Г17:
  для линейной `σ` внутри ячейки `∫σ ds = σ̄·h` ТОЧНО, то есть средняя по ячейке
  постоянная воспроизводит оптическую толщину без ошибки, а схема всё равно DG1.
  Нужна ГОМОГЕНИЗАЦИЯ, а не интерполяция;
- поверхность: ССЫЛКА НА ОБЩИЙ АНАЛИТИЧЕСКИЙ ПРИМИТИВ (плоскость = нормаль +
  смещение, 4 double, ОДИН раз на поверхность), а для кривого примитива —
  фасеты. Строка до 07-27 читала «линейная функция по углам, нулевое множество
  которой есть граница»; отменено `PLAN_CUT.md` Г3: это язык НЕЯВНОЙ ФУНКЦИИ,
  он интерполирует поле, а не границу, и любой интерполянт по углам грубит
  геометрию до `O(h²)`. Соседние ячейки ссылаются на ТОТ ЖЕ объект, поэтому
  водонепроницаемость точна по построению. Наклонная стена = ОДИН крупный узел,
  лесенки нет ни на каком уровне. Вокселизация границы запрещена.

**Поле.** Разрывный Галёркин, степень 1, по листьям: линейно ВНУТРИ ячейки,
разрывно МЕЖДУ ячейками. Противопоточность требует именно этого, и DG был
придуман ровно для этого уравнения (Reed & Hill, 1973). Базис — простые полиномы
`1, x, y, z`; потенциалы φ волновой линии здесь НЕ нужны, они существовали ради
аналитических элементов оператора ВТОРОГО порядка. 4 коэффициента на ячейку на
ординату.

Угловая ось: ординаты для развёртки, низкие сферические гармоники для источника
рассеяния (он гладок по углу).

**УГЛОВОЕ ПОЛЕ ТРАНЗИЕНТНО — ХРАНИТЬ ЕГО НЕ НАДО (аудит Р1).** Развёртке `L(x,ω)`
нужен только чтобы протолкнуть значения дальше по потоку; СОХРАНЯТЬ надо лишь то,
что читается потом: источник рассеяния (несколько сферических моментов) и
исходящий радианс на поверхностях. Полные 80 ординат живут внутри одного прохода
и умирают вместе с ним. Это стандартная практика `S_N`-кодов.

    хранить все ординаты:   2.6·10⁶ × 80 × 4 × 4 Б = 3.3 ГБ
    хранить 4 момента:      2.6·10⁶ ×  4 × 4 × 4 Б = 166 МБ     ← в 20 раз

**НА ПОВЕРХНОСТЯХ ХРАНИТЬ ИСХОДЯЩИЙ РАДИАНС, А НЕ ВХОДЯЩИЙ (аудит Р3).** У
диффузной поверхности он от направления не зависит — ОДНО число вместо 80. В
комнате диффузных поверхностей большинство, и их больше, чем объёмных ячеек.

**РАЗВЯЗКА Р1 и Р3 С УЗКИМ ЛЕПЕСТКОМ.** Моментов не хватит, чтобы собрать
глянцевую поверхность с острым лепестком. Правило: **угловое разрешение хранимого
задаётся шириной лепестка ПОТРЕБИТЕЛЯ** — диффузный сосед довольствуется одним
числом, глянцевый требует моментов по ширине своего лепестка, зеркальный не
читает хранимое вовсе, а прослеживается. Это то же правило углового LOD,
применённое к хранению.

## ПРАВИЛО РАЗМЕРА ЭЛЕМЕНТА — ПО КАЖДОЙ ОСИ ОТДЕЛЬНО

Главная ошибка первых прикидок: считали по худшей оси и переплатили два порядка.

    размер вдоль оси = чем поле меняется ВДОЛЬ ЭТОЙ ОСИ, снизу — пиксель εR

- поперёк полога — свободный пробег `1/σ_t`;
- вдоль однородного полога — структура ОСВЕЩЕНИЯ (метры), а не пробег;
- у поверхности — пиксель `εR`, плюс разрез на границах тени.

Газон, полоса 3–67 м: кубы по пробегу дают 2.3·10⁶ ячеек; плитки 1×1 м × 6.7 см
дают **1.4·10⁴**, в 160 раз меньше. Требует анизотропного дробления (kd-дерево
либо октодерево с делением по выбранной оси).

**ЭТА ЭКОНОМИЯ УСЛОВНА, И УСЛОВИЕ БЫЛО НЕ ПРОВЕРЕНО (аудит Р4).** Длинная ячейка
законна, только если вдоль длинной оси она ОПТИЧЕСКИ ТОНКАЯ **либо** поле вдоль
неё ОДНОРОДНО. У травы `σ_t` анизотропна в ПРОТИВОПОЛОЖНУЮ сторону: вертикальный
луч идёт вдоль стеблей и почти ничего не встречает, горизонтальный пересекает их
массу. Плитка 1×1 м имеет по горизонтали `τ ≈ 15`, и одна линейная ячейка
`exp(−15)` не изобразит никак.

Спасает вторая половина условия: у ОДНОРОДНОГО газона поле вдоль горизонтали не
меняется, и там длинная ячейка законна. Но как только по газону идёт ТЕНЬ,
однородность кончается. Итоговое правило:

    длинная ось разрешена, пока поле вдоль неё однородно;
    на границе однородности ячейка РЕЖЕТСЯ

То есть снова discontinuity meshing, третий раз в этом плане.

## ТРИ РЕЖИМА, ПЕРЕХОДЫ ПО εR — БЕЗ РУЧНЫХ НАСТРОЕК

| режим | когда | пример |
|---|---|---|
| настоящая геометрия | структура крупнее пикселя | трава ближе 3 м, листва ближе 50 м |
| среда | структура подпиксельная | газон 3–67 м, крона дерева |
| поверхность + эффективная BRDF | ячейка переросла толщину слоя | газон дальше 100 м |

Внутри среды режим выбирает оптическая толщина: `τ≪1` — однократное рассеяние,
область становится источником; `τ~1` — полное RTE; `τ≫1` — диффузия, ОДИН скаляр
на ячейку, угловой оси нет вовсе.

## ОДНОРОДНЫЙ ПОЛОГ — ОДНОМЕРНАЯ ЗАДАЧА (аудит Р5, крупнейшее сжатие)

Газон и поле **статистически инвариантны по горизонтали**. Значит для них перенос
сводится к **плоскопараллельной одномерной задаче по глубине полога** — классика
теории атмосферы и дистанционного зондирования.

    3D-сетка полога, полоса 3–67 м    1.4·10⁴ ячеек × 80 ординат
    1D-задача по глубине              ~10 слоёв × 80 ординат = 800 неизвестных,
                                      решается ОДИН раз и переиспользуется всюду

Следствие сильнее самого сжатия: решив 1D-задачу для набора углов освещения, мы
получаем **готовую BRDF полога**, и газон становится ПОВЕРХНОСТЬЮ раньше, чем со
ста метров, записанных в таблице режимов.

**НО НЕ «ПРАКТИЧЕСКИ ВЕЗДЕ» — ЭТО БЫЛО ПЕРЕОЦЕНКОЙ (аудит К7).** Однородность нужна
ОТНОСИТЕЛЬНО ПИКСЕЛЯ: 1D-задача законна, только когда след пикселя МНОГО БОЛЬШЕ
горизонтальной длины корреляции полога. У газона она равна шагу травинок, 1–3 см:

    пиксель ≫ 3 см  ⇒  εR > ~3 см  ⇒  R > 30 м

В полосе 3–30 м пиксель МЕЛЬЧЕ корреляции, и картинка обязана показывать отдельные
пучки, просветы и тени между травинками; 1D-задача даст там ровное поле, где глаз
ждёт структуру. Это ТРЕТИЙ критерий рядом с «структура против пикселя» и «толщина
слоя против ячейки», и раньше его в плане не было.

Объёмное представление остаётся нужным только там, где однородность НАРУШЕНА: у
кромок, под пятнистой тенью дерева, на склонах, у отдельных кустов. То есть
трёхмерный перенос в растительности становится редким исключением, а не основным
режимом.

Это же соображение — почему у дерева так не выйдет: крона по горизонтали НЕ
однородна и на плоскопараллельную задачу не сводится.

ИЗВЕСТНОЕ ОГРАНИЧЕНИЕ: RTE считает рассеиватели независимыми и не даёт
КОРРЕЛЯЦИОННЫХ эффектов — «горячего пятна» и, что важнее для картинки, ПРОСВЕТОВ
НЕБА между листьями. Однородная крона выйдет ровной зелёной массой без бликов.
Поэтому для дерева переключать надо по подпиксельности ПРОСВЕТА, а не листа, а
просветы крупнее листьев.

## СХЕМА ИЗ ТРЁХ ПРОХОДОВ

1. **Прямой свет — аналитически**, вне ординат. Лечит лучевой эффект (`S_N` даёт
   искусственные полосы от малого источника). Видимость нужна только здесь:
   `O(элементы × источники)`, не попарно.
2. **Развёртка по ординатам** — рассеянная середина, гладкая по углу.
3. **Сбор по пикселю** — луч на пиксель до первого попадания, затем свёртка BRDF
   с хранимым входящим полем. Резкость картинки приходит отсюда: 80 ординат не
   дадут 10⁶ пиксельных направлений, и читать изображение с сетки ординат НЕЛЬЗЯ.

**ИСТОЧНИК ПЕРВОГО СТОЛКНОВЕНИЯ — ИНАЧЕ ДВОЙНОЙ УЧЁТ (аудит К9).** Если излучение
источника `ε` остаётся в правой части развёртки, проход 2 перенесёт прямой свет
ЕЩЁ РАЗ поверх прохода 1. Картинка выйдет правдоподобной — просто вдвое
пересвеченной там, где свет прямой, и верной в тени; ловится только эталоном.

    правильно:  незатенённую часть вычислить точно (проход 1),
                ВЫЧЕСТЬ её из правой части,
                на ординаты подать только sigma_s × (уже посчитанный прямой свет),
                то есть однократно рассеянную компоненту

Та же ловушка на другом конце: если поверхность хранит исходящий радианс с УЖЕ
ВКЛЮЧЁННЫМ прямым светом (Р3), а сбор в проходе 3 добавляет прямой ещё раз —
снова вдвое. НУЖНА ЯВНАЯ ДОГОВОРЁННОСТЬ, что именно лежит в хранимом, и она
должна быть записана в заголовке структуры, а не подразумеваться.

**ПРАВИЛО СБОРА (аудит К3): НЕ ИНТЕРПОЛИРОВАТЬ МЕЖДУ ОРДИНАТАМИ.** Свёртка берётся
в ТОЧНОМ исходящем направлении пикселя, а входящее поле участвует в ней как
гладкая функция угла. Интерполяция ординат даст полосы. По типу поверхности:
диффузная — просто чтение (радианс от направления не зависит), глянцевая —
свёртка по лепестку, зеркальная — прослеживание дальше, без всякой свёртки.

Зеркальные и преломляющие цепочки идут прослеживанием (одно направление на
отскок), а не через ординаты и не через связи пар. Прозрачная среда объёмных
неизвестных не заводит: меняются только Снеллиус с Френелем на границе, закон
`n²` вдоль луча и поглощение по Бугеру.

**РАЗВЁРТКА ОБРЫВАЕТСЯ НА ДИЭЛЕКТРИЧЕСКОЙ ГРАНИЦЕ (аудит К5).** Луч ординаты `m`,
преломившись, уходит в направление, которого в наборе НЕТ, а интерполировать между
ординатами запрещено правилом К3. Значит развёртка такие границы **не пересекает
вообще**: она на них заканчивается, а дальше идёт прослеживание. Без этой оговорки
естественно написать sweep, который преломляет ординаты, и получить полосы.

## МОДЕЛЬ СТОИМОСТИ — БУМАГА, НЕ ИЗМЕРЕНО

    холодный старт, гектар травы   ~3·10¹⁰ оп   (после ординат-по-порядку-рассеяния
                                                 и диффузии для многократного;
                                                 без них было 2·10¹¹)
    обычная комната                ~5·10⁹
    кадр, поворот камеры           1.5·10⁸      только сбор
    кадр, ходьба                   2.3·10⁸      сбор + ~1% элементов меняют уровень
    кадр, скачок камеры            до 10¹⁰      уточняется вся видимая часть

**ПРЯМОЙ СВЕТ ОТ ПЛОЩАДНОГО ИСТОЧНИКА В МОДЕЛИ ОТСУТСТВОВАЛ (аудит С3).** Записано
было `O(элементы × источники)`, но у протяжённого источника это
`O(элементы × источники × выборки)`: `10⁶ × 100 = 10⁸` теневых запросов на источник,
то есть сопоставимо со ВСЕМ кадровым бюджетом. Смягчается тем, что прямая
освещённость гладка везде, кроме границ тени: считать её можно на более грубом
множестве с интерполяцией и разрезами на границах (кэш облучённости) — `×10…×100`,
но это уже не бесплатно и должно стоять в бюджете явно.

**А В МУТНОЙ СРЕДЕ ЭТО НЕ ЗАПРОС ВИДИМОСТИ, А МАРШ ПО ЛУЧУ (аудит С4).**
Незатенённая часть требует `exp(−τ)` вдоль пути от источника, то есть НАКОПЛЕНИЯ
оптической толщины по всем пересечённым ячейкам, а не булева «видно/не видно».
Один запрос стоит `O(ячеек вдоль луча)`, а не `O(log N)` — в десятки раз дороже.
Значит `10⁸` из С3 занижены на порядок-полтора всюду, где есть трава или туман.

**ПРЯМАЯ И КОСВЕННАЯ СОСТАВЛЯЮЩИЕ ДОЛЖНЫ ЖИТЬ НА РАЗНЫХ СЕТКАХ (аудит Р6).** План
задавал размер ячейки один раз, по худшему из требований. Но требования у них
противоположные:

| | чем определяется | какая сетка |
|---|---|---|
| прямой свет | границы теней, резкие | мелкая, с разрезами |
| косвенный | многократное рассеяние | **грубая** |

Косвенная составляющая гладка по построению: она прошла рассеяние, которое всё
размывает. Её можно считать на сетке в 4–8 раз грубее по каждой оси, то есть в
**64…512 раз меньше неизвестных** на этой части в 3D. Приём стандартный (кэш
облучённости, «indirect at half resolution»), и складывается со всем остальным.

Отношение холодного старта к установившемуся кадру ~10³. ЭТО И ЕСТЬ ГЛАВНЫЙ АКТИВ,
и докладывать оба числа надо РАЗДЕЛЬНО.

**ЧИСЛО ИТЕРАЦИЙ ЗАДАЁТ АЛЬБЕДО, И «10» ЗАНИЖЕНО (аудит С1).** Ряд по рассеянию
сходится как `ρⁿ`:

| сцена | альбедо | итераций до 1e-3 |
|---|---|---|
| обычные материалы | 0.5 | ~10 |
| белая комната | 0.8 | 30–60 |
| снег, белая краска, молоко | 0.99 | **сотни** |

Строка «холодный старт 3·10¹⁰» верна только при `ρ ≲ 0.5`. Лечится разгоном
**DSA** (diffusion synthetic acceleration): грубое ДИФФУЗИОННОЕ решение как
предобуславливатель — стандарт в нейтронном переносе.

**ПОПРАВКА К «СВЯЗНАЯ СИСТЕМА — 80» (аудит С2).** Развёртка треугольная, каждая
неизвестная посещается ОДИН раз, и реально связаны лишь `ND×ND` = 80×80 внутри
ячейки (рассеяние) плюс вход↔выход в точке (отражение). НО диффузия в DSA
эллиптична и ГЛОБАЛЬНА, то есть разгон возвращает большую связную систему — а
нужен он ровно тогда, когда альбедо высокое. Честная формулировка: связная система
есть либо 80 на ячейку (низкое альбедо, без разгона), либо ОДИН скалярный
эллиптический солв на всей сетке — хорошо обусловленный и решаемый многосеточно,
но глобальный.

**`P1`/`SP_N` ДЛЯ ВСЕЙ КОСВЕННОЙ ЧАСТИ (аудит Р2).** План сводил к диффузии только
области с `τ≫1`. Но после того как прямой свет посчитан точно, ВСЯ косвенная
составляющая гладка по углу, и четырёх моментов `P1` хватает почти всюду, а не
только в плотных областях. Ещё `×20` по угловой оси сверх записанного.

**НО НЕ В АНИЗОТРОПНОЙ СРЕДЕ — ЭТО БЫЛО ВНУТРЕННИМ ПРОТИВОРЕЧИЕМ ПЛАНА (аудит К4).**
`P1` и диффузия предполагают почти изотропный радианс и СКАЛЯРНЫЙ коэффициент
диффузии. Полог, ради которого и введено `σ_t(ω)`, свет КАНАЛИЗИРУЕТ: в пшенице
поле после многих рассеяний остаётся вытянутым вертикально и изотропным не
становится. Плоское `P1` там даст заведомо неверный ответ.
Правка: для анизотропной среды коэффициент диффузии **тензорный**, шесть чисел
вместо одного. По-прежнему дёшево, но это ДРУГОЕ уравнение, и писать надо его.
Критерий выбора — анизотропия `σ_t(ω)`, а не только `τ`.

ЗАНОЗА: циклы обхода при зеркалах и невыпуклых областях ломают треугольность; для
«зеркального коридора» цикл может оказаться длинным. Померить прежде, чем обещать.
Плюс порядок обхода на анизотропной сетке надо строить топологической сортировкой
заново для каждой ординаты — `O(ячейки)` на ординату, дёшево, но не бесплатно.

## ВЕХИ. АУДИТ ДО КОДА, ФАЛЬСИФИКАТОРЫ ДО ПРОГОНА

**T1. Ядро развёртки, чистое поглощение.** Слой, `σ_s = 0`, линейная `σ_t`,
**и БЕЗ объёмного излучения: `ε = 0`, подсветка только через границу (аудит К2).**
Эталон — закон Бугера. Оптическая толщина при линейной `σ_t` берётся ТРАПЕЦИЕЙ
ТОЧНО: `τ = (σ_вход + σ_выход)/2 · длина`.
Причина оговорки: `dL/ds = −σ_t·L + q` при линейной `σ_t` и `q ≠ 0` в элементарных
функциях НЕ берётся (экспоненциальный интеграл от квадратичной экспоненты), так
что `exp(−τ)` перестаёт быть эталоном, как только включено излучение.
Предсказание: 1e-14. Если 1e-8 — где-то квадратура вместо замкнутой формы.
Негативный контроль: сбить нормировку квадратурных весов на 1% ⇒ ошибка обязана
стать 1%, а не 1e-14.

**T2. Рассеяние, печная задача — ТОЛЬКО ПРИ СКАЛЯРНОМ `σ_t` (аудит К1).**
Замкнутая полость, альбедо 1, однородное излучение ⇒ радианс однороден и равен
излучению, ТОЧНО и НЕЗАВИСИМО от разбиения.
Предсказание: 1e-14 при любом числе ячеек; любая зависимость от разбиения = ошибка
в ядре. Негативный контроль: сбить нормировку фазовой функции на 1% ⇒ печь обязана
поехать на 1%. Если печь при этом сойдётся — тест ничего не проверяет.

**ОГРАНИЧИТЕЛЬ ВВОДИТСЯ ЗДЕСЬ, А НЕ В T5 (аудит К11).** По К6 печь обязана гоняться
С ВКЛЮЧЁННЫМ ограничителем, но в первой редакции плана ограничитель появлялся
только в T5, которая стоит ПОСЛЕ T2. Порядок вех был замкнут в кольцо. Ограничитель
переезжает в T2 и здесь же дисквалифицируется, если валит печь.

**ПЕЧЬ ГОНЯТЬ НА ГРАДУИРОВАННОЙ И АНИЗОТРОПНОЙ СЕТКЕ (аудит К8).** Между крупной и
мелкой ячейкой грань разбита, и поток через неё надо передавать СОХРАНЯЯ ЭНЕРГИЮ,
иначе на каждой границе уровня утекает свет. Это требование, а не деталь
реализации. Формулировка «независимо от разбиения» читается как «на разных
РАВНОМЕРНЫХ сетках», а на них ошибка передачи потока не проявится ВОВСЕ. Значит в
T2 обязательны три сетки: равномерная, градуированная, анизотропная.

**ЛОВУШКА, КОТОРАЯ БЫ ЗАСТАВИЛА ЧИНИТЬ ИСПРАВНЫЙ КОД.** При НАПРАВЛЕННОМ
`σ_t(ω) = G(ω)·u_L` равновесие требует `ε(ω) = σ_t(ω)·L`, то есть однородный
радианс есть решение, только если излучение тоже направленное. Наивная печь с
направленным `σ_t` ОБЯЗАНА поехать у правильного кода. Поэтому: печь ставится при
скалярном `σ_t`, а для направленного делается отдельный вариант с согласованным
`ε(ω)`. Это первая подпись артефакта наизнанку — тест, который валит исправное.

**T3. H-функция Чандрасекара.** Полубесконечная среда с изотропным рассеянием —
нетривиальный аналитический эталон С многократным рассеянием.
Предсказание: сходимость по числу ординат как `O(ND⁻²)` для гладкого рассеяния.

**ТОЛЬКО С ДВОЙНОЙ ГАУССОВОЙ КВАДРАТУРОЙ (аудит К10).** У полубесконечной среды
радианс на границе РАЗРЫВЕН ПО УГЛУ при `μ = 0` — это граница между уходящими и
приходящими направлениями. Обычная квадратура Гаусса по `[−1, 1]` этот разрыв
перешагивает, и сходимость выходит много хуже предсказанной. Узлы обязаны быть
раздельными: `[−1, 0]` и `[0, 1]`.
Без этого я получу медленную сходимость, решу, что ошибся в ядре, и полезу чинить
исправное. ТРЕТИЙ генератор ложной тревоги в этом плане — после печи при
направленном `σ_t` (К1) и ограничителя, портящего константу (К6).

**T4. Поверхности как граничные условия + линейная нагрузка узла.** Плоскость,
режущая ячейку; BRDF; сохранение энергии. Эталоны: форм-факторы простых пар
(замкнутая форма) и печная задача с поверхностями. Плюс ЗАКОН `n²` — вдоль луча
сохраняется `L/n²`, а не `L`; забыть = не сойдётся энергия на границе сред.

**T5. Сбор по пикселю, первая картинка.** Эталон: ламбертова сфера при удалённом
источнике — аналитическое изображение (косинусная затенённость). Предсказание:
совпадение до 1e-3. Ограничитель для DG1 ОБЯЗАТЕЛЕН: без него около границы тени
радианс уходит в минус и даёт чёрные пятна, а не мягкую погрешность.

ОГРАНИЧИТЕЛЬ ОБЯЗАН СОХРАНЯТЬ КОНСТАНТЫ (аудит К6). Ограничители нелинейны, и
многие константу портят. Значит печная задача T2 гоняется С ВКЛЮЧЁННЫМ
ограничителем, а ограничитель, который её валит, дисквалифицируется. Иначе легко
проверить печь без него, потом включить и не заметить.

**T6. Анизотропные ячейки и правила размера.** Проверить предсказанное сжатие в
160 раз на газоне. Признак артефакта: если число не меняется при смене
анизотропии — правило не применяется.

**T7. Сцена с травой и деревом.** Сверить ВСЕ числа раздела «модель стоимости» с
фактом.

## ЧТО ЯВНО ВЫБРАСЫВАЕТСЯ

Интерференция, спекл, дифракция, тонкие плёнки, дифракционный предел, фаза
Френеля, ближнее поле. Всё это остаётся волновой линии. Стык между линиями —
**BRDF**: волновой расчёт на микрорельефе даёт гашение зеркальной компоненты
`exp(−(2kσcosθ)²)` и ширину лепестка `~λ/ℓ`, и дальше этими двумя числами живёт
весь перенос.

## ТРИ ПОДПИСИ АРТЕФАКТА (из волновой линии, действуют и здесь)

1. число не меняется при изменении параметра, которым его меняют;
2. негативный контроль садится на пол метрики;
3. ошибка нормирована на сокращённый результат вместо натурального масштаба.

За два дня волновой линии этот список поймал три артефакта, из них два — «значение
используется раньше, чем установлено». Читать перед каждым прогоном.

## ЧЕСТНАЯ ОГОВОРКА

Перенос — зрелая технология с чужой инженерией за плечами. Отличать эту линию
будут три вещи, и только они: BRDF, ПОСЧИТАННЫЕ волновым способом вместо
подогнанных; иерархия, общая с волновой линией; голографический режим, которого у
трассировщиков нет. Если ни одно не подтвердится измерением — линия не имеет
самостоятельного смысла, и это надо будет признать.

И отдельно: вся модель стоимости выше — бумага. В волновой линии за два дня
половина таких же уверенных прикидок провалилась при проверке, причём именно
очевидные. Ни одно число отсюда не считается установленным, пока не измерено.

## АУДИТ ПЛАНА 07-27 — СЕМЬ НАХОДОК, ВСЕ ВНЕСЕНЫ ВЫШЕ

Проведён ДО написания кода, по заведённому порядку. Перечень для истории:

| # | находка | где внесена |
|---|---|---|
| К1 | печь при направленном `σ_t` обязана поехать у ИСПРАВНОГО кода: равновесие требует `ε(ω) = σ_t(ω)·L` | веха T2 |
| К2 | эталон Бугера верен только при `ε = 0`: с излучением решение не элементарно | веха T1 |
| К3 | сбор по пикселю не должен интерполировать между ординатами — будут полосы | схема трёх проходов |
| С1 | «10 итераций» верно лишь при `ρ ≲ 0.5`; белая комната 30–60, снег сотни | модель стоимости |
| С2 | DSA возвращает ГЛОБАЛЬНУЮ связную систему, значит «связная система — 80» верно только без разгона | модель стоимости |
| Р1 | угловое поле ТРАНЗИЕНТНО: хранить надо моменты, а не 80 ординат — память ×20 | представление |
| Р2 | `P1`/`SP_N` годится для ВСЕЙ косвенной части, а не только при `τ≫1` — ещё ×20 по углу | модель стоимости |
| Р3 | на поверхностях хранить ИСХОДЯЩИЙ радианс: у диффузной это одно число вместо 80 | представление |

Самая опасная — К1: она не завышает оценку, а порождает ЛОЖНУЮ ТРЕВОГУ и заставила
бы чинить работающий код. Самая ценная — Р1: угловое поле нужно только внутри
прохода, и это снимает главную статью памяти.

Развязка Р1/Р3 с узким лепестком записана отдельным правилом: угловое разрешение
хранимого задаётся шириной лепестка ПОТРЕБИТЕЛЯ.

## ВТОРОЙ АУДИТ 07-27 — ШЕСТЬ НАХОДОК, ВСЕ ВНЕСЕНЫ ВЫШЕ

| # | находка | где внесена |
|---|---|---|
| К4 | `P1` для всей косвенной части ПРОТИВОРЕЧИТ направленному `σ_t`: полог канализирует свет и изотропным не становится ⇒ коэффициент диффузии ТЕНЗОРНЫЙ | модель стоимости |
| К5 | преломление уводит ординату в направление, которого в наборе нет ⇒ развёртка на диэлектрической границе ОБРЫВАЕТСЯ, дальше прослеживание | схема трёх проходов |
| К6 | ограничитель обязан сохранять константы, иначе печь бессмысленна ⇒ гонять печь С ВКЛЮЧЁННЫМ ограничителем | веха T5 |
| С3 | прямой свет от площадного источника: `O(элементы × источники × выборки)` = 10⁸ запросов, в модели отсутствовал | модель стоимости |
| Р4 | экономия ×160 на анизотропных ячейках УСЛОВНА: у травы `σ_t` анизотропна в обратную сторону, плитка по горизонтали имеет `τ≈15`; спасает только однородность поля, и на её границе ячейка режется | правило размера |
| Р5 | однородный полог сводится к ОДНОМЕРНОЙ плоскопараллельной задаче: 800 неизвестных вместо 1.4·10⁴ ячеек, решается один раз, и полог становится ПОВЕРХНОСТЬЮ почти везде | отдельный раздел |

Р4 частично подрывает главную экономию первого аудита; Р5 её с лихвой замещает и
переводит растительность из объёмного режима в поверхностный почти повсеместно.
К4 был внутренним противоречием: план требовал `σ_t(ω)` ради злаков и тут же
предлагал `P1`, который анизотропию не переносит.

## ТРЕТИЙ АУДИТ 07-27 — ЧЕТЫРЕ НАХОДКИ, ВСЕ ВНЕСЕНЫ ВЫШЕ

| # | находка | где внесена |
|---|---|---|
| К7 | 1D-схлопывание полога требует пикселя МНОГО БОЛЬШЕ горизонтальной корреляции (шаг травинок 1–3 см ⇒ работает с 30 м, а не «практически везде»); это ТРЕТИЙ критерий режима | раздел про однородный полог |
| К8 | поток на смене уровня обязан быть консервативным; печь надо гонять на ГРАДУИРОВАННОЙ и АНИЗОТРОПНОЙ сетке, на равномерных ошибка не проявится вовсе | веха T2 |
| С4 | в мутной среде прямой член есть МАРШ по лучу с накоплением `τ`, а не запрос видимости: `O(ячеек вдоль луча)` вместо `O(log N)`, то есть С3 занижена на порядок-полтора | модель стоимости |
| Р6 | прямая и косвенная составляющие должны жить на РАЗНЫХ сетках: косвенная гладка и допускает сетку в 4–8 раз грубее по оси, то есть 64…512 раз меньше неизвестных в 3D | модель стоимости |

## СОСТОЯНИЕ ПОСЛЕ ТРЁХ АУДИТОВ

Семнадцать находок, все до единой строки кода. Две из них — ЛОВУШКИ, которые
заставили бы чинить исправный код: печь при направленном `σ_t` (К1) и ограничитель,
портящий константу (К6). Одно внутреннее противоречие (К4: `σ_t(ω)` ради злаков и
тут же `P1`, который анизотропию не переносит). Одна переоценка своей же экономии,
снятая следующим аудитом (Р4 подрубила плиточную анизотропию, Р5 её заместила).

Отдача падает: 7 → 6 → 4, и находки третьего прохода мельче, чем Р1 и Р5.
ВЫВОД: дальше полезнее проверять план КОДОМ на вехе T1, чем ещё одним рассуждением.

## ЧЕТВЁРТЫЙ АУДИТ 07-27 — ТРИ НАХОДКИ, ВСЕ ВНЕСЕНЫ ВЫШЕ

Все три одного рода: дают ПРАВДОПОДОБНЫЙ НЕВЕРНЫЙ РЕЗУЛЬТАТ либо ложную тревогу.

| # | находка | где внесена |
|---|---|---|
| К9 | двойной учёт прямого света: если `ε` остаётся в правой части, проход 2 переносит его поверх прохода 1 ⇒ нужен ИСТОЧНИК ПЕРВОГО СТОЛКНОВЕНИЯ с явным ВЫЧИТАНИЕМ незатенённой части; и явная договорённость, включён ли прямой свет в хранимое на поверхностях | схема трёх проходов |
| К10 | T3 требует ДВОЙНОЙ гауссовой квадратуры (раздельно `[−1,0]` и `[0,1]`): радианс на границе разрывен при `μ=0`, обычный Гаусс разрыв перешагивает и сходимость выходит хуже предсказанной | веха T3 |
| К11 | порядок вех был замкнут в кольцо: T2 требует ограничителя (К6), а он вводился только в T5 ⇒ ограничитель переезжает в T2 | веха T2 |

НОВЫХ СОКРАЩЕНИЙ РАЗМЕРНОСТИ НЕ НАЙДЕНО. Единственное, что осталось на столе, —
переменное число ординат по положению (у источника радианс острый, в глубине
плотной среды почти изотропен). Приём известен, но плохо дружит с развёрткой:
порядок обхода и передача потока между областями с разными наборами направлений.
В план НЕ закладывается, помечен как возможность.

## СОСТОЯНИЕ ПОСЛЕ ЧЕТЫРЁХ АУДИТОВ

Двадцать находок, все до единой строки кода. Из них ТРИ — генераторы ложной
тревоги, которые заставили бы чинить исправное: печь при направленном `σ_t` (К1),
ограничитель, портящий константу (К6), и одинарный Гаусс на разрыве при `μ=0` (К10).
Одна — двойной учёт, дающий правдоподобную вдвое пересвеченную картинку (К9).

Отдача 7 → 6 → 4 → 3, и СМЕНИЛСЯ ХАРАКТЕР: первые проходы вскрывали архитектуру
(что хранить, какой формы ячейка, какое уравнение для косвенной части), четвёртый
не нашёл в ней ничего — только бухгалтерию декомпозиции и устройство тестов.

ВЫВОД: архитектурный слой исчерпан. Оставшиеся ошибки такого сорта, что
рассуждением их не выловить — двойной учёт и неудачная квадратура видны только
тогда, когда эталон не сошёлся. Следующий шаг — T1, кодом.

## ОБСУЖДЕНИЕ 07-27 ПОСЛЕ T2 — ТРАВА, ГОМОГЕНИЗАЦИЯ, ЛИНЕЙНЫЕ СВОЙСТВА

Разбор геометрии разреза вынесен в `PLAN_CUT.md` (общий слой двух продуктов,
находки Г1…Г17). Здесь — то, что относится к переносу.

### ГОМОГЕНИЗАЦИЯ — НЕ УСРЕДНЕНИЕ (иначе полог выйдет систематически тёмным)

Приближённые объёмные свойства на крупных уровнях (то, ради чего дерево и несёт
среду) НЕЛЬЗЯ получать усреднением плотности. Две причины, обе известные:

1. **`⟨e^{−σL}⟩ ≠ e^{−⟨σ⟩L}`.** Перенос не линеен по `σ_t`. Если в мелкой структуре
   есть ПРОСВЕТЫ — а у травы они и есть, — свет проходит через них, а усреднённое
   `σ_t` его гасит. Усреднение ЗАВЫШАЕТ ослабление, полог выходит темнее, чем есть,
   и просветы неба исчезают. План уже отмечает это как известное ограничение RTE
   («не даёт КОРРЕЛЯЦИОННЫХ эффектов… ПРОСВЕТОВ НЕБА между листьями») — но там оно
   записано как ограничение модели, а это ещё и ошибка ГОМОГЕНИЗАЦИИ, отдельная.
   Штатное лечение — коэффициент сгущения (clumping index, классика теории полога),
   отдельное хранимое число.
2. **Направленность не выводится из плотности.** `σ_t(ω) = G(ω)·u_L`: усреднение даёт
   `u_L` (плотность площади листа), а `G(ω)` задаётся РАСПРЕДЕЛЕНИЕМ ОРИЕНТАЦИЙ —
   независимая статистика, которую надо хранить отдельно (или её низкие моменты).

**Правило: крупные свойства ПОДГОНЯЮТСЯ под измеренный функционал мелкой модели, а
не усредняются.** Офлайн: взять мелкий участок, посчитать его истинное направленное
пропускание и BRDF, и записать те `σ_t(ω)`, `σ_s`, фазовую функцию и сгущение,
которые их ВОСПРОИЗВОДЯТ. Это ровно 1D-задача Р5, которая в бюджете уже стоит
(«решив 1D-задачу для набора углов освещения, мы получаем готовую BRDF полога»).
Фальсификатор: число ОБЯЗАНО меняться при смене распределения ориентаций — иначе
`G(ω)` не участвует (тот же признак, что в T6: «если число не меняется при смене
анизотропии — правило не применяется»).

### Р4 И К7 ПРОТИВОРЕЧАТ ДРУГ ДРУГУ В ПОЛОСЕ 5–30 М, И ЦЕНЫ ТАМ НЕТ

Р4 даёт для полосы 3–67 м **1.4·10⁴ ячеек** — но это плитки **1×1 м** × 6.7 см, то
есть предположение, что поле по горизонтали ОДНОРОДНО. К7 затем говорит, что в
полосе 3–30 м пиксель МЕЛЬЧЕ корреляции полога (шаг травинок 1–3 см) и картинка
обязана показывать отдельные пучки, просветы и тени. Метровые плитки там незаконны,
значит `1.4·10⁴` относится только к зоне ЗА 30 м, **а для 5–30 м оценки нет вовсе**.
Сантиметровая сетка на почву 5–30 м (~1400 м²) даёт порядок `10⁸` — сопоставимо со
ВСЕЙ комнатой (2·10⁸), то есть это главная статья, и она не измерена.

**ЧТО СНИМАЕТ ОСНОВНУЮ ЧАСТЬ (довод пользователя):** неоднородности поля сглажены, а
РЕЗКИЕ немногочисленны — и, главное, **резкие неоднородности ОДНОМЕРНЫ**. Граница
тени есть КРИВАЯ, а не площадь, поэтому её разрешение стоит `O(длина/h)`, а не
`O(площадь/h²)`:

    cm-сетка везде:                        1.4e7 столбцов × 10 слоёв  ≈ 1.4e8
    гладкий фон 50 см + кромки теней
    (~100 м длины, 1 см поперёк):          5.6e4 + 1e5                ≈ 1.6e5

То есть ~×10³, и это ровно то, что обещали Р4 («на границе однородности ячейка
режется») и Р6 («косвенная допускает сетку в 4–8 раз грубее по оси»).

**НО К7 БЫЛ НЕ ПРО ТЕНИ, А ПРО САМУ ТРАВУ**, и там структура не редкая, а
ПОВСЕМЕСТНАЯ: на 10 м пиксель равен шагу травинок. Гладкое поле её не покроет.
Кандидат на развязку — собственная схема трёх проходов:

    проход 2 (развёртка):  поле ГЛАДКОЕ, крупные ячейки, разрез на кромках теней
    проход 3 (сбор):       сантиметровая текстура берётся ЗДЕСЬ, по пикселю,
                           против ХРАНИМОЙ мелкой плотности u_L (это данные,
                           а не неизвестные — дёшево)

План то же самое уже утверждает про резкость изображения: «80 ординат не дадут 10⁶
пиксельных направлений, и читать изображение с сетки ординат НЕЛЬЗЯ». Текстура полога
— тот же случай: высокочастотна, но её статистика стационарна, и в неизвестные
решателя ей идти незачем.

**ЧТО ПРИ ЭТОМ НЕ ДОКАЗАНО И РАССУЖДЕНИЕМ НЕ ДОКАЖЕТСЯ:** взаимное затенение
травинок есть транспорт на сантиметрах. Он локален (несколько см), и эффективная
среда со сгущением ловит его В СРЕДНЕМ, но не в конкретной реализации. Достаточно ли
«гладкое решение × мелкая плотность на сборе» — это T7, и фальсификатор чистый:
решить малый участок на cm-сетке и сравнить с «грубое решение + сбор». Подпись
артефакта: число обязано меняться при смене плотности пучков.

### ЛИНЕЙНЫЕ СВОЙСТВА ПО УЗЛУ — В ПЕРЕНОСЕ НЕ ТРЕБОВАНИЕ (разбор в PLAN_CUT Г17)

Раздел представления требует: «`σ_t(ω)`, `σ_s`, фазовая функция — линейно
интерполируемые по узлу». В T2 это отложено (среда константна в ячейке, `ε` уже DG1).
**Требование понижается до возможности, и вот почему:**

1. для линейной `σ` внутри ячейки `∫σ ds = σ̄·h` ТОЧНО — кусочно-постоянная `σ`,
   взятая как СРЕДНЕЕ по ячейке, воспроизводит оптическую толщину без ошибки, и
   балансное уравнение при `v = 1` совпадает побитово. Расходятся только наклонные
   моменты `∫σLξ`;
2. точность схемы всё равно DG1 (в T2 измерено 2.90 по функционалу): уточнять
   свойства выше порядка поля — то же, что квадратура высокого порядка для схемы
   второго;
3. видимая неоднородность живёт в ОСВЕЩЁННОСТИ (тени), а не в свойствах, а поле DG1
   уже несёт её линейно внутри ячейки плюс разрез на кромках тени (Р6).

**ЧТО ВЫЖИВАЕТ:** постоянная обязана быть СРЕДНИМ или подогнанным эффективным, а не
произвольной выборкой — выборка это первый порядок, и она бы вылезла. А «среднее» для
полога есть подогнанное эффективное со сгущением (см. выше), то есть нужная машинерия
— **ГОМОГЕНИЗАЦИЯ, а не интерполяция**. Одно вместо двух.

Фальсификатор, который вопрос закроет: решить участок с пятнистой плотностью при
постоянной-средней `σ` и при линейной; если разница ниже ошибки схемы, линейные
свойства в переносе — мёртвый вес (причём `lean.sh` его не поймает: код бы работал).

В ВОЛНОВОЙ ЛИНИИ линейное `k` остаётся, и по СТРУКТУРНОЙ причине: несущая становится
фазой ВКБ, `S' = k(x)`, член `k²` сокращается точно, остаётся один член `∝ k'`. Там
это условие существования несущей, а не уточнение сетки. Требование по-продуктовое.

### ПРЕДСТАВЛЕНИЕ ТРАВИНКИ — ИТОГ (подробно в PLAN_CUT Г14…Г16)

Травинка НЕ вокселизуется никогда, и её внутренность не дискретизуется ни на одном
уровне: толщина 0.2 мм ниже размера элемента `L = εR` на любом расстоянии. От
внутренности нужна ровно оптическая толщина хорды, а хорда берётся из двух
поверхностей аналитически. Режим выбирается ПО КАЖДОЙ ОСИ, а не по объекту.

| `R < ~5 м` | треугольники по ссылке, дерево как пространственный индекс |
| `~5…30 м` | среда `σ_t(x,ω)`, дерево как носитель объёмных свойств |
| `R > ~30 м` | поверхность + эффективная BRDF из 1D-задачи Р5 |

## T2 ВЫПОЛНЕНА 07-27 — `src/transport/rte2d.{c,h}`, `tests/test_rte2d.c`

**Печь точна до 1e-15 на всех трёх сетках, 40/40 проверок; ограничитель печь не
валит и потому по К6 НЕ дисквалифицируется.** Но главное в T2 не это: при
постановке тестов выяснилось, что **печь проверяет заметно меньше, чем считал
план**, и два теста рядом с ней пришлось завести отдельно. Плюс два моих
предсказания не сбылись.

**T2 ОБЯЗАНА БЫТЬ ДВУМЕРНОЙ, и это следствие К8, не прихоть.** К8 требует
градуированной и анизотропной сетки, потому что на равномерной ошибка передачи
потока между уровнями не проявится вовсе. Но в одномерном слое РАЗБИТОЙ ГРАНИ НЕ
СУЩЕСТВУЕТ (у каждой грани ровно один сосед с каждой стороны), а «анизотропная
ячейка» не определена. Слой T1 главное требование К8 проверить не мог ничем.
Поэтому T2 — плоская задача, `L(x,y,θ)`, направления в плоскости; настоящий
трёхмерный угловой набор приезжает с трёхмерной развёрткой, а проверяется здесь
ПРОСТРАНСТВЕННАЯ схема, для которой размерность угла безразлична.

| проверка | предсказано | измерено |
|---|---|---|
| `Σw = 2π`, первый угловой момент, `p0·Σw = 1` | ≤5e-14 | 4.4e-15, 3.8e-16, **0** |
| разбитых граней: равномерная / градуированная / анизотропная | 0 / >0 / >0 | 0 / 32 / 16 |
| **ПЕЧЬ**, альбедо 1, `ε=0`, ограничитель ON, невязка константы | ≤5e-14 | **1.1e-15** на трёх сетках |
| та же печь от нуля | ≤1e-12 | 2.9e-15…5.3e-15 (91/92/125 итер) |
| печь общая: альбедо 0.5, НЕОДНОРОДНОЕ `σ_t`, `ε=σ_a L_b` | ≤5e-14 | 1.2e-15…1.8e-15 |
| ЛИНЕЙНОЕ точное решение `A+Bx+Cy` (наклоны!) | ≤5e-14 | 4.4e-16…6.7e-16, срезов 0 |
| ЛОВУШКА К1: направленное `σ_t` + изотропное `ε` | обязана поехать | **1.04** (поехала) |
| то же с согласованным `ε(ω)` | ≤5e-14 | 1.2e-15 |
| БАЛАНС ЭНЕРГИИ, альбедо 1, подсветка только слева | ≤5e-14 | 1.8e-16…8.8e-16 на трёх сетках |
| ограничитель OFF: минус около тени | должен быть | −0.39·L_b, 2584 ячейки |
| ограничитель CONSERVATIVE | минус убран, баланс цел | −1.8e-16, баланс 5.3e-16, срезов 2642 |
| негативный контроль: режим SLOPE | обязан сломать баланс | **4.27e-3** |
| негативный контроль ПЛАНА: фазовая функция ×1.01 | 1.0101010e-2 (замкнутая форма) | **1.0100973e-2** |

### ТРИ НАХОДКИ О САМИХ ТЕСТАХ (нумерация продолжает аудиты)

**К12. ПЕЧЬ ПРОВЕРЯЕТ ТОЛЬКО СРЕДНЕЕ.** Однородный радианс имеет НУЛЕВОЙ наклон,
поэтому ошибка в уравнениях для наклонных степеней свободы DG1 проходит печь
незамеченной — а это две трети неизвестных. Добавлен тест с ЛИНЕЙНЫМ точным
решением `L = A + Bx + Cy`: линейное поле лежит в пространстве DG1, непрерывно на
гранях и удовлетворяет уравнению точно, значит схема обязана воспроизвести его
ДО ОКРУГЛЕНИЯ вместе с наклонами. Для этого понадобился линейный по касательной
влёт на границе (`lbt`), иначе такую задачу не поставить вовсе.

**К13. ПЕЧЬ НЕ ЛОВИТ И НЕКОНСЕРВАТИВНУЮ ПЕРЕДАЧУ ПОТОКА — то есть ровно то, ради
чего К8 её на градуированную сетку и посылал.** Причина простая: КОНСТАНТУ
воспроизводит любой разумный оператор передачи, включая небрежное усреднение по
всему ребру крупной ячейки. Рецепт К8 («гонять печь на градуированной сетке»)
своей цели не достигает. Работает другое: баланс энергии при НЕОДНОРОДНОЙ
подсветке — альбедо ровно 1, свет только слева, и тогда вся влитая мощность
обязана выйти. Мерится точное дискретное тождество (сумма уравнений при `v=1`),
которое верно на ЛЮБОЙ итерации, а не только после сходимости.
Конструктивно консервативность обеспечена тем, что **грань есть ПЕРЕСЕЧЕНИЕ рёбер
двух ячеек, а не ребро ячейки**: висячего узла как особого случая нет, у крупной
ячейки вдоль ребра просто несколько граней, и обе стороны интегрируют по одной и
той же геометрии. Целочисленные координаты здесь не украшение — сопоставление
граней требует точного равенства положений.

**К14. Р1 И СТАНДАРТНЫЙ ОГРАНИЧИТЕЛЬ МОНОТОННОСТИ НЕСОВМЕСТИМЫ.** Границы для
срезки берутся со ВСЕХ соседей, но в развёртке доступны только верхние по потоку,
а односторонняя граница обнуляет наклон у любого гладкого поля (у растущего вдоль
потока поля все верхние соседи ниже, и запас вверх равен нулю — проверено,
получается ровно нуль). Нижние по потоку соседи — это поле предыдущей итерации
для ЭТОГО направления, то есть полные `ND` ординат в памяти, что Р1 запрещает.
Значит в развёртке остаётся ПОЛОЖИТЕЛЬНОСТЬ — и это ровно то, чего требуют
К6/T5: около границы тени радианс не должен уходить в минус. Ограничитель
монотонности возможен только ценой отказа от Р1, и этот выбор надо делать
осознанно, а не обнаружить в T5.

### ОГРАНИЧИТЕЛЬ: СРЕЗКА И СРЕДНЕЕ ИЩУТСЯ СОВМЕСТНО

Срезав наклон, надо пересчитать среднее из уравнения баланса, иначе выточный
след меняется и энергия ТЕЧЁТ (негативный контроль: режим `SLOPE` даёт 4.3e-3
против 5e-16). Но пересчёт среднего снова портит положительность, и поочерёдные
проходы к ней лишь ПРИБЛИЖАЮТСЯ — измерено, что после трёх проходов оставалось
−3.4e-6 от `L_b`, то есть на десять порядков выше округления. Помогло то, что
среднее есть АФФИННАЯ функция коэффициента срезки `α`:

    c0(α) = (rhs0 − α·K)/M00,  K = M01·c1 + M02·c2,
    минимум по углам ≥ 0  ⇔  α ≤ rhs0/(K + dev0·M00)

Замкнутая форма, один шаг, и баланс с положительностью точны ОБА. Порога при
этом не появилось: `α` берётся из уравнения.

### ДВА ПРЕДСКАЗАНИЯ НЕ СБЫЛИСЬ

**Порядок DG1 на неполиномиальном решении: предсказал 1…2, измерено 2.90.**
Рассуждение было «у точного решения изломы по диагоналям, они испортят порядок».
Неверно: изломы лежат на множестве меры нуль, интегральный функционал (вытекшая
мощность) их не чувствует, и получается учебная суперсходимость функционала
`2p+1 = 3` при `p = 1`. Ошибка в безопасную сторону — схема оказалась лучше
предсказанной, но предсказание всё равно было неверным.

**Негативный контроль плана «печь обязана поехать на 1%» — и величина, и МЕСТО
замера были неверны.** Замкнутая форма возмущённого равновесия

    L/L_b = σ_a/(σ_a − δ·σ_s),   при альбедо 0.5 и δ=0.01 это 1/0.99

даёт **1.0101e-2, а не 1e-2**, и это равновесие БЕСКОНЕЧНОЙ среды. Отсюда два
промаха первого прогона: в оптически тонкой области подсветка на границе держит
решение около `L_b` и отклонение вышло МЕНЬШЕ асимптотики (7.7e-3 при `τ~1..5`);
а максимум по всем ячейкам берёт ПЕРЕСВЕТ пограничного слоя — при `τ` на ячейку
6.25 DG1 в слое немонотонен и дал 1.041e-2, то есть БОЛЬШЕ асимптотики.
Ограничитель этого не режет: он следит за минусом, не за пересветом. Правильная
постановка: `τ` по области 30, `τ` на ячейку 0.47 (слой разрешён), отсчёт в
ЦЕНТРАЛЬНОЙ ячейке — тогда 1.0100973e-2 против 1.0101010e-2, разница 3.8e-8, что
и есть предсказанное влияние границы `e^{−15}`.
Отдельно: при альбедо РОВНО 1 этот контроль вырожден (`0/0`), а при возмущении
вверх итерация по рассеянию расходится. Поэтому он гоняется при альбедо 0.5.

### ЧИСЛО ИТЕРАЦИЙ — ПЕРВОЕ ИЗМЕРЕНИЕ К С1

При альбедо РОВНО 1 и `τ≈3` печь от нуля сходится за **91…125 итераций**, при
альбедо 0.5 — за 33…37. Это подтверждает С1 («10 итераций верно лишь при
ρ ≲ 0.5») с той стороны, с которой С1 и предупреждал, и показывает, что разгон
DSA понадобится не в экзотике, а в обычной светлой комнате.

### ЧТО В T2 НЕ СДЕЛАНО, ЯВНО

- **`σ_t`, `σ_s` КОНСТАНТНЫ в ячейке** (`ε` — уже DG1). Линейная по узлу среда
  сцеплена с линейной функцией границы материала и уезжает в T4 вместе с ней; в
  T1 линейная `σ_t` уже проверена в замкнутой форме свободного пролёта.
- **Фазовая функция только ИЗОТРОПНАЯ, и причина содержательная:** анизотропную
  матрицу `p[m'][m]` недостаточно нормировать по СТРОКАМ (это сохраняет источник
  рассеяния), нужна ДВАЖДЫ стохастическая нормировка, иначе рассеяние перестаёт
  сохранять энергию по столбцам и баланс уезжает. Здесь `p0 = 1/Σw`, а не
  `1/(2π)` — тогда рассеяние сохраняет энергию ТОЧНО даже при округлении в весах
  (измерено: `p0·Σw − 1 = 0` побитово).
- Отражающих границ нет: замкнутая полость смоделирована предписанным влётом,
  что физически и есть чёрная стенка при температуре печи. Заодно это снимает
  вопрос о циклах обхода — область выпуклая, граф развёртки ацикличен, и
  топологическая сортировка накрывает все ячейки для каждой ординаты (проверено;
  при цикле код возвращает ошибку, а не считает молча).

### ОТКРЫТЫЙ ВОПРОС, КОТОРЫЙ T2 ВСКРЫЛА: КАКОЕ ЯДРО РАБОЧЕЕ

Ядра T1 и T2 ТОЧНЫ НА РАЗНОМ, и это не совпадение, а свойство:

| | Бугер `e^{−τ}` | константа | линейное поле |
|---|---|---|---|
| характеристическое (T1) | **точно** | точно при `σ=const` в ячейке | нет |
| DG1 (T2) | порядок 3 по функционалу | **точно** | **точно** |

Замкнутой формы, точной на обоих, при линейной `σ_t` И линейном источнике не
существует в элементарных функциях (это ровно замечание К2). Но она существует,
если представлять линейной не `q`, а отношение `q/σ` в координате ОПТИЧЕСКОЙ
ТОЛЩИНЫ: тогда `∫r(T)e^{−T}dT` берётся, `r = const` даёт `(1−e^{−τ})` при любом
`σ(s)`, и точны оба эталона. Это известное семейство (экспоненциально-
характеристические схемы). В план НЕ закладывается — сначала T3 должна показать,
насколько порядок 3 недостаточен на настоящем аналитическом эталоне с
многократным рассеянием. Помечено как возможность.

## T1 ВЫПОЛНЕНА 07-27 — `src/transport/sweep.{c,h}`, `tests/test_sweep.c`

**Все десять предсказаний сошлись, 31/31 проверок, ошибка 1e-15 против
предсказанных 1e-14.** Замкнутая форма подтверждена: `τ = (σ_вх+σ_вых)/2·ℓ`
воспроизводит Бугера до округления, и ошибка НЕ падает степенью `Δz` при
256-кратном сгущении — то есть квадратуры в ядре действительно нет. Оба
негативных контроля сработали ровно как предсказано.

| проверка | предсказано | измерено |
|---|---|---|
| тождества квадратуры `Σw=2`, `Σ_{μ>0}wμ=1/2` | ≤1e-14 | 8.9e-16 |
| Бугер, линейная `σ_t`, градуированная сетка, 64 ячейки | ≤5e-14 | **1.1e-15** |
| ошибка `τ` по ВСЕМ ординатам (включая скользящие, `τ`→171) | ≤5e-14 | 2.2e-16 |
| независимость от разбиения, ncell 4→1024 | не падает степенью `Δz` | 1.3e-15 → 4.2e-15 (растёт ∝n) |
| профиль на ВСЕХ гранях, оба знака `μ` | ≤5e-14 | 2.2e-15 |
| РАЗРЫВНАЯ `σ_t` (скачок 1.10→2.10 на грани) | ≤5e-14 | 1.6e-15 |
| глубокий слой `τ=20`, норма на эталон `e^{−20}` | ≤5e-14 | 4.9e-15 |
| направленное `σ_t = |μ|·u_L`, поток `F⁺ = πL₀e^{−τ}` | ≤1e-14 | 4.4e-16 |
| негативный контроль А: веса ×1.01 | ровно 1.000e-2 | **1.000000e-02** |
| негативный контроль Б: левый конец `σ` вместо трапеции | ≥1e-3, порядок 1 | 4.6e-2, отношения 2.02 / 2.01 |

**НАЙДЕН АРТЕФАКТ — В САМОМ ТЕСТЕ, ПЕРВОЙ ПОДПИСЬЮ.** Первая редакция
градуировала сетку шагом-на-ячейку (`Δz_i ∝ q^{−i}`). При `q=1.3` и `n>135`
мелкие ячейки уходят ниже разрешения `double` рядом с `z≈zmax`, там
`z[i+1] == z[i]`, и ошибка при ncell 256 и 1024 выходила ПОБИТОВО одинаковой,
1.110e-15. Читалось как «схема точна, разбиение не влияет», а на деле
измельчение перестало измельчать. Правка двойная: градуировка задаётся
отношением КРАЙНИХ ячеек на весь слой (`stretch`), поэтому каждая ячейка падает
∝1/n при любом n; и в тест добавлена проверка, что `min_dz` действительно падает
∝1/ncell — она стоит ПЕРЕД выводом о независимости от разбиения. Без неё
«ошибка не изменилась» не значит ничего.

**ОДНО ОТСТУПЛЕНИЕ ОТ ПЛАНА, СОЗНАТЕЛЬНОЕ.** Порог метрики Бугера взят 5e-14, а
не 1e-14: характеристическое ядро считает `L = Π exp(−τ_i)` по ячейкам, и это
даёт пол округления ~1.5·n·ε, то есть 2e-14 уже при n=64 — то же и у любой
другой реализации той же формулы. Поэтому решающим признаком сделан не порог, а
ТРЕНД: округление растёт ∝n, квадратура вместо замкнутой формы падала бы
степенью `Δz`. Фактически вышло 1e-15 (ошибки округления не складываются
когерентно), так что плановая цифра 1e-14 выполнена с запасом.

**ЧТО T1 НЕ ПРОВЕРИЛА, ХОТЬ И МОГЛО ПОКАЗАТЬСЯ.** Пространственного DG степени 1
в ядре ПОКА НЕТ, и T1 его не могла проверить: при `σ_s = 0` и `ε = 0` источника
внутри ячейки нет, представлять полиномом нечего, и обновление ячейки —
чистый множитель `exp(−τ)`. Проверен свободный пролёт, и только он. Полином
приезжает вместе с рассеянием, то есть DG1 целиком лежит на T2, а не «уже почти
готов». Так же не проверены: передача потока на смене уровня (К8 — это T2),
ограничитель (К6/К11 — T2), угловая сходимость (T3).

**ЧТО ЯДРО УЖЕ УМЕЕТ И ЧТО ИЗ НЕГО ПЕРЕЕЗЖАЕТ В T2.** Двойная гауссова
квадратура заведена сразу раздельными полуинтервалами (К10), хотя разрыва при
`μ=0` в T1 нет — чтобы T3 не ловил ложную тревогу на готовом коде. Направленное
`σ_t(x,ω) = G(ω)·u_L(x)` входит одним скаляром `g` на ординату, потому что форма
разделяется; это уже проверено на `G=|μ|`. `σ_t` разрывна между ячейками с
самого начала (`sig_lo`/`sig_hi` раздельно), граница материала непрерывности не
требует. Обновление ячейки вынесено отдельной функцией `tr_cell_absorb`, куда в
T2 добавляется второе слагаемое `(q/σ)(1−e^{−τ})`.

**ГЕЙТ.** clang-format; `ccheck` CLEAN с первого прохода; ASan+UBSan, valgrind и
`feenableexcept(FE_INVALID|FE_DIVBYZERO)` — все чисто, 31/31 в каждом прогоне;
`make test` зелёный целиком (`lean.sh` по transport добавлен, `src/transport/*`
раньше в его глоб не попадал). Формальный слой (CBMC) на этом файле НЕ прошёл:
арифметика сплошь плавающая, и `cbmc` с `--nan-check` не доходит до конца SAT
даже на 2 ячейках и unwind 4. Это ограничение инструмента на float-коде, а не
находка. Опасный класс здесь — индексная логика записи в `l_face`, и она покрыта
ASan на настоящем прогоне; `cverify` уместен на трёхмерной развёртке по
октодереву (T4/T6), где логика целочисленная.

## T5а — ПРЯМОЙ СВЕТ И СБОР ПО ПИКСЕЛЮ: ПЕРВАЯ ТРЁХМЕРНАЯ КАРТИНКА

Внесено 07-27. **Веха T5 РАСКОЛОТА НАДВОЕ, и это сознательное отступление от
порядка вех.** Довод: **эталон самой T5 — ламбертова сфера при удалённом
источнике — косвенного света не содержит вовсе**, значит половина T5 проверяема
БЕЗ трёхмерной развёртки и без T3.

    T5а   проход 1 (прямой свет) + проход 3 (сбор) — картинка есть, косвенного нет
    T5б   тот же сбор, но с хранимым полем развёртки — после T3 и T4

**НИЧЕГО НЕ ПРОПАДАЕТ НИ ПРИ КАКОМ ПОРЯДКЕ.** Марш по лучу с накоплением `τ`
нужен проходу 1 в любом варианте (С4), а сбор по пикселю — это проход 3, без
которого картинки не будет никогда. T3 (сколько ординат) и трёхмерная развёртка
от этого не сдвигаются ни на шаг; они просто перестают стоять ПЕРЕД первым
изображением.

**ЧТО ЭТА КАРТИНКА НЕ ПОКАЖЕТ, И ЭТО НАДО ГОВОРИТЬ ВСЛУХ:** косвенного света в
ней нет. Тени будут абсолютно чёрными там, где нет прямого света, потому что
переотражений никто не считал. Выдавать её за «рендеринг уравнения переноса»
нельзя — она есть проход 1 плюс проход 3 из трёх.

### ЧТО ВХОДИТ

    src/transport/ray3.c/.h   марш по лучу сквозь октодерево, накопление τ,
                              попадание в границу материала
    src/transport/cam3.c/.h   камера-обскура, сбор по пикселю, прямой свет
    tests/test_ray3.c         фальсификаторы марша и попадания
    tests/test_cam3.c         аналитический эталон: ламбертова сфера

**СРЕДА — СВОЯ ТАБЛИЦА, А НЕ ПОЛЕ УЗЛА.** `hz_onode` несёт `double complex k2` —
это ВОЛНОВАЯ физика, и перенос её не читает. `σ_t` приходит массивом по ИНДЕКСУ
УЗЛА, то есть ровно тем ключом, который Г20 и заводил абстрактным. Октодерево
здесь работает пространственным индексом и топологией, не носителем свойств.

**ГЕОМЕТРИЯ БЕРЁТСЯ ГОТОВОЙ** из общего слоя: `hz_cutmap` даёт ячейке её веер
полуплоскостей, `hz_facettab` — сами плоскости, `hz_surftab` — примитив-родитель.

### МАРШ БЕЗ ε — ЦЕЛОЧИСЛЕННЫЙ ШАГ ЧЕРЕЗ ГРАНЬ

Обычный обход октодерева лучом сдвигает точку за грань на малую величину, и это
магический порог в чистом виде. Здесь следующая ячейка определяется ИНАЧЕ: у
коробки считается параметр выхода `t_вых` и ОСЬ выхода (argmin по трём слоям),
после чего целочисленная координата вдоль этой оси шагает через грань
(`lo+size` при `d>0`, `lo−1` при `d<0`), а остальные берутся из точки выхода с
зажимом в текущую коробку. Зажим делает угловой случай (луч ровно в ребро)
ДЕТЕРМИНИРОВАННЫМ: обход входит в соседа через шаг, не теряя ни ячейки и не
создавая щели. Порога нет нигде.

### ОПТИЧЕСКАЯ ТОЛЩИНА — ТА ЖЕ ЗАМКНУТАЯ ФОРМА, ЧТО В T1

`tr_cell_absorb` переиспользуется дословно: `τ = (σ_вх + σ_вых)/2 · длина` точна
для линейной `σ` и потому не квадратура. В T5а `σ` константна по ячейке, то есть
это частный случай уже проверенного.

### ФАЛЬСИФИКАТОРЫ, предсказания — до прогона

| # | что мерится | предсказание |
|---|---|---|
| 1 | марш: сумма длин отрезков по ячейкам против длины хорды коробки | ≤1e-14, и ни одна ячейка не пропущена |
| 2 | Бугер вдоль луча сквозь однородную среду против `e^{−στ}` | ≤1e-14 (замкнутая форма T1) |
| 3 | попадание ПО ПРИМИТИВУ против аналитического пересечения луч–сфера | ≤1e-12 |
| 4 | попадание ПО ФАСЕТУ | ошибка порядка `dmax`, ПАДАЕТ с `k` |
| 5 | ламбертова сфера, удалённый источник, вне силуэта | ≤1e-3 (предсказание плана T5) |
| 6 | силуэт: доля пикселей, где эталон и расчёт расходятся | только кромка, ширина 1 пиксель |

**НЕГАТИВНЫЕ КОНТРОЛИ, каждый со своей метрикой:**

- **на `τ` (к №2):** заменить `exp(−τ)` булевой видимостью ⇒ в мутной среде
  картинка обязана измениться на `O(1)`, а тень стать резкой. Если не изменилась
  — марш не накапливает толщину, а отвечает «видно/не видно» (это ровно С4);
- **на нормаль (к №3 и №5):** взять нормаль С ФАСЕТА вместо примитива ⇒ обязана
  появиться огранка, и величина её обязана падать с `k`. Это же и второй путь
  попадания, то есть контроль и рабочий режим здесь совпадают (К15);
- **на тень:** выключить теневой марш ⇒ неосвещённая сторона обязана засветиться;
- **на метрику (К19):** сравнить PPM вместо радианса ⇒ показать, что тон-маппинг
  ошибку СЪЕДАЕТ, и потому сравнивать надо числа.

## АУДИТ T5а ДО КОДА — К15…К20

### К15. ПОПАДАНИЕ ПО ПРИМИТИВУ И ПО ФАСЕТУ — РАЗНЫЕ ОТВЕТЫ, И ПУТЬ ВЫБИРАЕТСЯ ЯВНО

Фасеты заведены, чтобы РЕЗАТЬ ЯЧЕЙКУ (их потребитель — объёмный интегратор), а
не чтобы отвечать на луч. Если сбор пойдёт по фасетам там, где есть примитив,
картинка получит огранку на геометрии, которая задана ТОЧНО, — то есть ровно ту
ошибку, которую `PLAN_CUT.md` Г3 запрещает («ссылка на общий примитив геометрию
не грубит ВОВСЕ»). **Правило: `hz_facet.surf ≥ 0` ⇒ попадание и нормаль считаются
по ПРИМИТИВУ; `surf < 0` (плоскость задана прямо или выведена DC) ⇒ по фасету.**
Фасет при этом остаётся тем, что ОТБИРАЕТ ячейку и даёт первое приближение.

### К16. МАРШ ОБЯЗАН НАКАПЛИВАТЬ `τ`, А НЕ ОТВЕЧАТЬ «ВИДНО / НЕ ВИДНО»

С4 записан в модели стоимости, но в КОДЕ его легко не исполнить: теневой запрос
естественно пишется как «есть ли попадание между точкой и источником». В мутной
среде это неверно физически (нужен `exp(−τ)` по пути) и заодно занижает цену на
порядок-полтора. Интерфейс поэтому один: марш ВСЕГДА возвращает накопленную
толщину, а попадание — отдельным полем. Булевой видимости в API нет вовсе.

### К17. ДОГОВОРЁННОСТЬ О ХРАНИМОМ ПИШЕТСЯ СЕЙЧАС, ХОТЯ ХРАНИМОГО ЕЩЁ НЕТ

К9 требует явной договорённости, включён ли прямой свет в хранимое на
поверхностях. В T5а хранимого поля нет, значит двойного учёта быть не может — и
именно поэтому договорённость проскочит незамеченной, а всплывёт в T5б, когда
проверять её будет дороже. **Записывается в заголовок сразу:
`L_out = L_e + прямой + косвенный`, T5а даёт первые два слагаемых, третье тождественно
ноль; хранимое на поверхности (Р3) будет содержать ТОЛЬКО косвенное.**

### К18. «УДАЛЁННЫЙ ИСТОЧНИК» В ЭТАЛОНЕ ОБЯЗАН БЫТЬ УДАЛЁННЫМ ТОЧНО

Аналитическая косинусная затенённость верна для ПАРАЛЛЕЛЬНОГО пучка. Точечный
источник на конечном расстоянии даёт другую формулу (обратный квадрат плюс
изменение направления по поверхности), и разница у сферы радиуса `r` с источником
на `R` порядка `r/R` — при `R = 10r` это 10%, что я приму за ошибку схемы и полезу
чинить исправное. **Это четвёртый генератор ложной тревоги в этом плане** (после
печи при направленном `σ_t` К1, ограничителя К6 и одинарного Гаусса К10). Эталон
ставится строго на параллельном пучке; точечный источник проверяется отдельным
тестом со СВОЕЙ формулой.

### К19. ТОН-МАППИНГ СЪЕДАЕТ МЕТРИКУ — СРАВНИВАТЬ НАДО РАДИАНС, А НЕ PPM

`hz_ppm_write` нормирует на максимум и берёт гамму 0.5. Обе операции МОНОТОННЫ, и
относительная ошибка после них уже не та: нормировка делит на максимум (то есть
общий множитель уходит вместе с ошибкой в нём), а гамма 0.5 сжимает верх шкалы
вдвое по логарифму. Сравнение картинок дало бы «сошлось» там, где радианс
разошёлся вдвое. **Метрика берётся на буфере радианса; PPM пишется отдельно и в
метрику не входит.** Это третья подпись артефакта в чистом виде — ошибка,
нормированная на сокращённый результат.

### К20. ПИКСЕЛЬ — НЕ ТОЧКА, И НА СИЛУЭТЕ ЭТО ОШИБКА `O(1)`

Луч через центр пикселя есть выборка, а не интеграл по телесному углу пикселя. На
силуэте сферы соседние пиксели дают «попал» и «не попал», и разница с
аналитическим изображением там равна ВСЕЙ яркости, а не `1e-3`. Если мерить
максимум по всему кадру, предсказание `1e-3` провалится по причине, не имеющей
отношения к схеме. **Метрика берётся ВНЕ силуэта, а силуэт считается отдельно и
предъявляется как ширина кромки в пикселях.** Честная альтернатива —
интегрировать по пикселю, но это уже сглаживание, и заводить его надо не ради
эталона.

## T5а ВЫПОЛНЕНА 07-27 — `src/transport/ray3.{c,h}`, `cam3.{c,h}`, `tests/test_ray3.c`

**Вывод. ТРЁХМЕРНАЯ КАРТИНКА ЕСТЬ**: сфера над плоскостью, параллельный пучок,
тень — посчитаны маршем по общей геометрии (октодерево + таблицы разреза) и
сверены с АНАЛИТИЧЕСКИМ изображением. `30/30`, все четыре негативных контроля
выстрелили, `ccheck` CLEAN, ASan/UBSan и `valgrind` чисты, `make test` зелёный.
**Косвенного света в ней нет и быть не может — это проход 1 и проход 3 из трёх.**

**ИЗМЕРЕНО** (предсказания напечатаны до результатов):

| фальсификатор | предсказано | измерено |
|---|---|---|
| марш: τ против длины куска луча в слое | ≤1e-13 | совпало во ВСЕХ 15 значащих цифрах, 23 ячейки |
| то же на анизотропном кадре `1×1×4` | ≤1e-13 | совпало во всех цифрах |
| Бугер вдоль луча | ≤1e-14 | `0.406569659740599` против `0.406569659740599` |
| попадание по ПРИМИТИВУ (К15) | ≤1e-12 | **`0.0` — побитово**, 169/169 лучей |
| изображение против аналитического, вне кромок | ≤1e-3 | среднее `9.2e-5` при `k=1` |
| кромочных пикселей | тонкая кромка | 6.5% кадра (силуэт плюс граница тени) |

**НЕГАТИВНЫЕ КОНТРОЛИ — ЧЕТЫРЕ ИЗ ЧЕТЫРЁХ:**

- **примитив отключён (к К15):** ответ остаётся фасетным, `max|Δt|` = `0.42` при
  `k=0` и `0.26` при `k=1` — то есть порядка `dmax` (`0.83` и `0.29`) и падает с
  уровнем фасетизации. Против `0.0` у примитива;
- **булева видимость вместо `exp(−τ)` (к К16/С4):** `exp(−0.9) = 0.4066` против
  булева `1.0` — разница `0.59`, то есть `O(1)`;
- **теневой марш выключен:** изменилось 3993 пикселя, максимум на всю яркость
  `0.62`. Тень действительно считается, а не рисуется;
- **К19, тон-маппинг:** та же ошибка после нормировки на максимум и гаммы 0.5
  доходит до `255` уровней из `255`. Метрика на картинке не мерит ничего, и
  сравнение идёт по буферу радианса.

**ЧТО ОКАЗАЛОСЬ НЕ ТАК, КАК ПРЕДСКАЗАНО.**

1. **Предсказание «≤1e-3» верно В СРЕДНЕМ и неверно ПОТОЧЕЧНО** — отсюда новая
   находка К21 (ниже). При `k=1` выбросов 4 пикселя из 27405, при `k=0` — 94;
   средняя ошибка `1.8e-3 → 9.2e-5`.
2. **Первая редакция кода исполняла К15 наполовину** — примитивом уточнялась
   ТОЧКА, а решение «попал/не попал» оставалось за фасетами. Силуэт и кромка
   тени уезжали на `O(dmax)`. Правлено: где веер ссылается на примитив, ему
   принадлежит и решение, а фасеты работают пространственным признаком.
3. **Две ошибки были В ТЕСТЕ, и обе поймались тем, что эталон брался независимо
   от кода.** Эталон продолжал плоскость до горизонта, тогда как сцена конечна
   (расхождение `0.62` при яркости `0.69` — на всю яркость); и σ назначалась по
   ТОЧКЕ в лист, который оказался крупнее слоя, отчего среда растекалась на всю
   крупную ячейку. Оба раза «неверный ответ» был у эталона, а не у марша.

### К21. ВЕЕР ФАСЕТОВ РАБОТАЕТ ПРОСТРАНСТВЕННЫМ ИНДЕКСОМ, А ОН ПОДОГНАН НЕ К ТОМУ ТЕЛУ

**Заведена по результату прогона.** Марш спрашивает у боковой таблицы «какая
поверхность рядом с этой ячейкой», и таблица отвечает по ФАСЕТНОМУ телу: ячейка
попадает в неё ровно тогда, когда её режет хоть один фасет (`hz_facets_for_box`,
отбор ТОЧНЫЙ — Г37). Но спрашивают у неё про ПРИМИТИВ, а примитив торчит наружу
фасетного тела на `dmax`. Скользящий луч — а теневой луч у кромки тени именно
скользящий — проходит по ячейкам, которых в таблице нет, и тени не находит.

**Это не ошибка отбора и не ошибка схемы:** отбор точен для того тела, для
которого сделан. Ошибка в том, что ОДИН И ТОТ ЖЕ индекс обслуживает два разных
тела — фасетное (объёмный интегратор) и примитив (луч).

**Измерено:** выбросов `94 → 4` при `k: 0 → 1`, то есть эффект падает вместе с
`dmax`, как и обязан.

**Лечение, не сделанное здесь:** индексировать ячейки по коробке, РАСШИРЕННОЙ на
`dmax` — число выведенное и хранимое, а не подобранное. Это правка построителя
боковой таблицы, а не марша, и у неё свой фальсификатор (ни один скользящий луч
не теряет пересечения). Записано открытой работой.

**ЧЕГО T5а НЕ ДАЁТ, СПИСКОМ.** Косвенного света (нужны T3, T4 и трёхмерная
развёртка). Ординат — их здесь нет вовсе, прямой свет считается вне угловой
сетки, как и предписывает проход 1. Материалов кроме одного ламбертова.
Площадного источника (С3): пучок строго параллельный, и К18 объясняет, почему
подменять его далёким точечным нельзя. Преломления и зеркал. Ячейка, целиком
лежащая ВНУТРИ материала, границы не несёт и проходится как среда — для луча,
идущего снаружи, это верно, но означает, что камера внутри тела не поддержана.

## T3 — H-ФУНКЦИЯ ЧАНДРАСЕКАРА: СКОЛЬКО ОРДИНАТ НУЖНО

Единственная веха, которая отвечает на вопрос «сколько направлений нести в
развёртку». Полубесконечная среда с изотропным рассеянием — нетривиальный
аналитический эталон С МНОГОКРАТНЫМ рассеянием, то есть проверяющий ровно то,
чего не проверили ни T1 (свободный пролёт), ни T2 (константа и линейное поле).

**ЭТАЛОН.** Падающий параллельный пучок `πF` по `μ0`, альбедо однократного
рассеяния `c`. Выходящий радианс:

    I(0, μ) = (c F / 4) · μ0/(μ + μ0) · H(μ) H(μ0)

Предел однократного рассеяния (`c → 0`, `H → 1`) даёт `(cF/4)·μ0/(μ+μ0)` —
ВТОРОЙ, независимый эталон в замкнутой форме, и он проверяет ту же машинерию
без H-функции вовсе.

**ЧТО ВХОДИТ:** `src/transport/scat1d.c/.h` — слой в оптической глубине,
источник первого столкновения, итерация по рассеянию, выходящий радианс;
`tests/test_scat1d.c` — H-функция как независимо вычисленный эталон.

### АУДИТ T3 ДО КОДА — К22…К25

**К22. ПОЛУБЕСКОНЕЧНОСТЬ МОДЕЛИРУЕТСЯ КОНЕЧНЫМ СЛОЕМ, И ТОЛЩИНА — ПАРАМЕТР, А НЕ
КОНСТАНТА.** Назначить `τ_max = 20` и не проверить — значит померить толщину
слоя вместо H-функции. При альбедо, близком к единице, глубина проникновения
растёт как `1/√(1−c)`, и «достаточная» толщина при `c = 0.5` не достаточна при
`c = 0.99`. **Фальсификатор: результат обязан ПЕРЕСТАТЬ меняться при росте
`τ_max`; свип печатается.**

**К23. ПУЧОК ОБЯЗАН ИДТИ ИСТОЧНИКОМ ПЕРВОГО СТОЛКНОВЕНИЯ — ЭТО К9 В ОДНОМЕРНОМ
ВИДЕ.** Дельта по углу в набор ординат не попадает по построению. Подать пучок
влётом по ближайшей ординате — значит получить луч-эффект и не сойтись к
H-функции НИКОГДА, сколько ординат ни возьми. Правильно: незатенённая часть
считается точно (`e^{−τ/μ0}`), в правую часть идёт только `c/4 · F · e^{−τ/μ0}`,
то есть однократно рассеянная компонента.

**К24. ЭТАЛОН САМ ВЫЧИСЛЯЕТСЯ ЧИСЛЕННО, И У НЕГО ОБЯЗАН БЫТЬ СВОЙ КОНТРОЛЬ.**
`H(μ)` берётся итерацией нелинейного уравнения Чандрасекара, то есть это ВТОРОЙ
численный результат, а не аналитика. Сравнивать два неизвестных бессмысленно.
**Контроль — моментное тождество `∫₀¹H(μ)dμ = (2/c)(1−√(1−c))`**, которое к
итерации отношения не имеет.

**К25. ПРОСТРАНСТВЕННАЯ ОШИБКА ОБЯЗАНА БЫТЬ ЗАВЕДОМО НИЖЕ УГЛОВОЙ.** Иначе
предсказанное `O(ND⁻²)` не увидеть: кривая сходимости упрётся в полку
пространственной схемы, и я запишу это как «угловая сходимость насытилась».
**Проверяется сгущением сетки при ФИКСИРОВАННОМ `ND`: ответ не должен меняться.**

**ФАЛЬСИФИКАТОРЫ T3:**

| # | что мерится | предсказание |
|---|---|---|
| 1 | моментное тождество эталона `H` | ≤1e-10 |
| 2 | предел однократного рассеяния `c = 0.01` | замкнутая форма, ≤1e-3 |
| 3 | `I(0,μ)` против `H`-формулы при `c = 0.9` | сходимость `O(ND⁻²)` |
| 4 | плоское альбедо против `1 − √(1−c)·H(μ0)` | та же сходимость |
| 5 | К22: свип по `τ_max` | ответ выходит на полку |
| 6 | К25: сгущение сетки при фиксированном `ND` | ответ не меняется |

**НЕГАТИВНЫЕ КОНТРОЛИ:**

- **К10 НАКОНЕЦ ИЗМЕРЯЕТСЯ:** одинарный Гаусс по `[−1,1]` вместо двойного ⇒
  сходимость обязана ИСПОРТИТЬСЯ, потому что на границе полубесконечной среды
  радианс разрывен при `μ = 0` и одинарная квадратура разрыв перешагивает. Это
  предсказание записано в плане с самого начала и до сих пор не проверено;
- **нормировка фазовой функции ×1.01** ⇒ выходящий радианс обязан сдвинуться на
  `O(1%)`;
- **К23 наизнанку:** подать пучок влётом по ближайшей ординате вместо источника
  первого столкновения ⇒ ответ обязан разойтись с эталоном на O(1) и НЕ
  улучшаться с ростом `ND`.

## T3 ВЫПОЛНЕНА 07-27 — `src/transport/scat1d.{c,h}`, `tests/test_scat1d.c`

**Вывод. Угловая сходимость `O(ND⁻²)` ПОДТВЕРЖДЕНА** (отношения ошибок 3.59 →
3.78 → 3.93 на удвоениях), и получен ответ, ради которого веха и делалась:

    при альбедо 0.9   1e-3 по радиансу  ⇒  ND = 16
                      1e-4              ⇒  ND = 32
                      3e-5              ⇒  ND = 64

`20/20`, все три негативных контроля выстрелили, `ccheck` CLEAN, санитайзеры
чисты. **И впервые ИЗМЕРЕН К10**, записанный в план ещё до T1.

**ИЗМЕРЕНО** (c = 0.9, μ0 = 0.6, τmax = 60, градуированная сетка):

| ND | max отн. ошибка `I(0,μ)` | где | ошибка альбедо | отношение |
|---|---|---|---|---|
| 8 | `1.495e-3` | μ = 0.069 | `−2.0e-5` | — |
| 16 | `4.165e-4` | μ = 0.020 | `−2.1e-6` | 3.59 |
| 32 | `1.103e-4` | μ = 0.0053 | `−1.8e-6` | 3.78 |
| 64 | `2.806e-5` | μ = 0.0014 | `−1.7e-6` | 3.93 |

**Максимум ошибки всегда на САМОЙ СКОЛЬЗЯЩЕЙ ординате** — там, где радианс у
границы полубесконечной среды меняется резче всего, и ровно там К10 и предупреждал
про разрыв при μ = 0.

| проверка | предсказано | измерено |
|---|---|---|
| К24: моментное тождество эталона `H` | ≤1e-10 | **1e-15…4e-15** |
| предел однократного рассеяния `c=0.01` | замкнутая форма | `6.4e-3` при `H−1 ~ c/2 = 5e-3` |
| К22: свип по τmax | выход на полку | полка с τmax = 20 |
| К25: свип по шагу у поверхности | без изменений | `1e-3 → 1e-5`: `1.103e-4` не двигается |

**НЕГАТИВНЫЕ КОНТРОЛИ — ТРИ ИЗ ТРЁХ:**

- **К10, ИЗМЕРЕН ВПЕРВЫЕ:** одинарный Гаусс по `[−1,1]` вместо двойного даёт
  ошибку в **15, 26 и 49 раз** больше при ND = 16, 32, 64 — и разрыв в
  соотношении РАСТЁТ с ND, то есть одинарная квадратура не просто хуже, а
  **сходится медленнее**. Предсказание К10 подтверждено полностью;
- **нормировка рассеяния ×1.01:** `1.10e-4 → 3.04e-2`, то есть в 276 раз;
- **К23, пучок влётом по ординате вместо источника первого столкновения:**
  `2.4e-2` против `2.8e-5` — **хуже в 854 раза**, и с ростом ND НЕ улучшается
  (9.7e-3 → 4.8e-2 → 2.4e-2, немонотонно). Это луч-эффект, а не недобор
  квадратуры, и числом ординат он не лечится.

**ЧТО ОКАЗАЛОСЬ НЕ ТАК, КАК ПРЕДСКАЗАНО, И ЧТО ИЗ ЭТОГО ВЫШЛО — К26.**

Предсказание «ошибка O(1)» у контроля К23 было неверно по ВЕЛИЧИНЕ: измерено
1e-2…5e-2. Существо уцелело (не сходится), и проверка переписана на него.

А главное — **К25 сработал ровно так, как заведён, и дважды.** Первый прогон на
РАВНОМЕРНОЙ сетке дал сходимость 5.58 → 0.31 → 0.27, то есть ошибка РОСЛА с
числом ординат; свип по числу ячеек показал, что виновата пространственная схема,
и сетка была заменена на градуированную. Второй прогон дал 3.50 → 3.42 → 1.92 —
и снова полка, но уже другая.

### К26. У ГРАДУИРОВАННОЙ СЕТКИ ДВА КОНЦА, И ПРОВЕРЯТЬ НАДО ОБА

**Заведена по результату прогона.** Измельчение у ПОВЕРХНОСТИ (`dtau0`
`1e-3 → 1e-5`) не меняло ответа вовсе, и это читалось как «пространственная
ошибка ниже угловой» — то есть К25 выполнен. Но у градуированной сетки есть
ВТОРОЙ конец: при `growth = 1.02` ячейки в глубине вырастают до `Δτ ~ 1.2`, и
`dtau0` их не трогает. Диагноз прямой: тот же ND = 64 при `growth = 1.005` дал
`2.8e-5` против `6.6e-5`, то есть полка была ГЛУБИННАЯ.

**Признак, по которому это видно без диагноза:** при ND = 64 максимум ошибки
УШЁЛ со скользящей ординаты (μ = 0.005) на почти вертикальную (μ = 0.9986).
Скользящие лучи набирают выход у самой поверхности, вертикальные — из глубины;
смена места максимума и есть указание, какой конец сетки виноват.

**Правило: свип по разрешению обязан идти по ОБОИМ концам градуированной сетки,
и по отдельности.** Внесено в тест: `dtau0` и `growth` свипятся раздельно.

## T4, ШАГ ПЕРВЫЙ, ВЫПОЛНЕН 07-27 — `src/transport/tet3.{c,h}`

**Вывод.** Интегратор ПЕРЕНОСА над разрезанной ячейкой написан: тетраэдризация
`hz_poly3` и ТОЧНЫЕ моменты линейного базиса. Это ровно пункт 6 порядка работ
`PLAN_CUT.md` («перенос тетраэдризует — открывает T4»), и без него нельзя ни T4,
ни трёхмерная развёртка: матрица масс DG1 на разрезанной ячейке нужна обеим.
`31/31`, ВСЕ предсказания сошлись с первого прогона, `ccheck` CLEAN, санитайзеры
и `valgrind` чисты.

**КВАДРАТУРНЫХ УЗЛОВ НЕТ ВОВСЕ, И ЭТО НЕ СОВПАДЕНИЕ С ВОЛНОВОЙ ЛИНИЕЙ, А ДРУГАЯ
ПРИЧИНА.** Матрица масс линейного базиса на тетраэдре есть `V/20·(1+δ_ij)` —
тождество, следствие `∫λ₁^a λ₂^b λ₃^c λ₄^d = 6V·a!b!c!d!/(a+b+c+d+3)!`. Волновой
линии узлы запрещены, потому что подынтегральное осциллирует (Г2); переносу они
просто НЕ НУЖНЫ на полиномах низкой степени. Подбирать нечего, и правило «без
магических порогов» соблюдается буквально.

| проверка | предсказано | измерено |
|---|---|---|
| сумма объёмов тетраэдров против ядра, 6 конфигураций | ≤1e-13 | **1.6e-16** |
| все тетраэдры одного знака | да | да во всех 6 |
| матрица масс на коробке против замкнутой формы `V`, `V/12` | ≤1e-14 | **1.1e-16** |
| `∫(3 + 5ξ − 7η + 11ζ)` — наклоны обязаны сократиться | точно | `24.000000000000000` |
| срез плоскостью: объём и первый момент (центроид) | ≤1e-13 | точны |
| `M00` есть объём, в том числе на кадре `1×1×8` (Г21) | да | да |

**НЕГАТИВНЫЙ КОНТРОЛЬ ВЫСТРЕЛИЛ И ПОКАЗАЛ, ЧЕМ ЭТО ЛОВИТСЯ.** Веер от точки ВНЕ
тела: сумма объёмов со знаком УСТОЯЛА (`20.333333333333329` против
`20.333333333333332`) — теорема о дивергенции чужую вершину не различает, — а вот
знаки тетраэдров разошлись, `+7/−9`. **То есть «сумма сошлась» здесь ничего не
доказывает, и проверять надо ЗНАК каждого тетраэдра.** Это третья подпись
артефакта в чистом виде: метрика, нечувствительная к ошибке, которую она якобы
проверяет.

## ЧЕГО В T4 И В РАЗВЁРТКЕ ЕЩЁ НЕТ — ЧЕСТНЫЙ СПИСОК

Написан ИНТЕГРАТОР, а не веха целиком. Не сделано:

1. **Трёхмерная сетка ГРАНЕЙ над октодеревом.** В 2D это `tr2_mesh`, где грань
   есть ПЕРЕСЕЧЕНИЕ рёбер соседей; на этом стоит консервативность передачи потока,
   и К13 показал, что печь её не ловит, а ловит баланс при неоднородной подсветке.
   Трёхмерного аналога нет.
2. **Топологическая сортировка на ординату** по этой сетке.
3. **Сборка DG1**: потоковые члены по граням (нужны моменты по МНОГОУГОЛЬНИКУ, а
   не только по многограннику — `tet3` даёт объёмные, граневых нет).
4. **Поверхности как граничное условие**: BRDF, Френель, закон `n²`, хранимое на
   поверхности (Р3) и внешняя итерация «развёртка ↔ отражение».
5. **Разгон DSA** — измерено в T2: 91…125 итераций при альбедо 1.

**Порядок вех при этом соблюдён:** T3 закрыта, T4 начата с той её части, которая
самодостаточна и проверяема аналитически. Оставшаяся часть T4 неотделима от
развёртки — поверхность есть ГРАНИЧНОЕ УСЛОВИЕ развёртки, и проверять её в
отрыве не на чем.

## АУДИТ КОДА 07-27 — СЛЕДУЕТ ЛИ НАПИСАННОЕ ПОСТАВЛЕННЫМ ЗАДАЧАМ

Проведён по просьбе пользователя ПОСЛЕ кода (в отличие от аудитов плана, которые
идут до). Проверялось соответствие кода заявленному, а не его качество.

**ЧТО СОШЛОСЬ.** Разделение линий держится буквально: в `src/transport/` нет ни
одного упоминания комплексных чисел, потенциалов φ и несущей, и **`k²` из узла
октодерева перенос не читает ни разу** — `σ_t` приходит своим массивом по
абстрактному ключу, как и требовал Г20. Заявленное в заголовках исполнено: в
`ray3.h` булевой видимости в API действительно нет (толщина возвращается всегда),
в `tet3.c` квадратурных узлов действительно ноль. Магических порогов в новом коде
нет: все допуски живут в тестах, где им и место. Мёртвого кода `lean.sh` в новых
файлах не нашёл. Все шесть новых модулей стоят и в `make test`, и в `make check`.

**ЧТО НАШЛОСЬ — ДВЕ ЛАТЕНТНЫЕ ДЫРЫ, ОБЕ ЗАПИСАНЫ, НИ ОДНА НЕ ЗАКРЫТА.**

### К27. МАРШ ПРОБУЕТ ТОЛЬКО ПЕРВЫЙ ПРИМИТИВ ИЗ ВЕЕРА ЯЧЕЙКИ

`tr3_march` берёт `psurf` первым же фасетом с `surf ≥ 0` и на этом останавливается.
Если в веере ячейки встретятся ДВА разных примитива (две сферы, сфера и цилиндр),
второй не будет проверен вовсе, и луч сквозь него пройдёт. В тесте T5а этот
случай не встречается, и это не случайность: там стоит проверка `both == 0`,
которая утверждает, что ни одна ячейка не собрала две поверхности. **То есть
ограничение не просто не покрыто тестом — тест его ОБХОДИТ.** Лечение прямое
(перебирать все различные `surf` веера и брать ближайшее попадание), но оно меняет
и цену, и поведение на стыке двух тел, поэтому идёт отдельным шагом со своим
фальсификатором: луч сквозь две касающиеся сферы обязан попасть в БЛИЖНЮЮ.

### К28. `sigma` ИНДЕКСИРУЕТСЯ УЗЛОМ, А ДЛИНА МАССИВА НИКЕМ НЕ СВЕРЯЕТСЯ

`tr3_scene.sigma` объявлен как `[tree->n]` и читается как `sigma[ni]`. Если
дерево дорастёт ПОСЛЕ выделения массива (а `hz_oct_set_box` умеет добавлять узлы),
чтение уедет за границу молча. Сейчас во всех вызовах массив выделяется последним,
но это соглашение, а не проверка. Лечение — хранить длину рядом с указателем и
сверять; дёшево, и это ровно тот класс, который `CLAUDE.md` велит ловить
санитайзером, а не соглашением.

## ШАГИ A, B, C, D ВЫПОЛНЕНЫ 07-27 — ТРЁХМЕРНАЯ РАЗВЁРТКА И КАРТИНКА С КОСВЕННЫМ СВЕТОМ

**Вывод. Уравнение переноса решается в 3D, и картинка комнаты с многократными
отражениями посчитана** (`build/room.ppm`). `42/42`, `ccheck` CLEAN, ASan/UBSan и
`valgrind` чисты, `make test` зелёный (11 наборов).

    src/transport/dirs3.c   A: ординаты на сфере (двойной Гаусс × квадранты)
    src/transport/mesh3.c   B: сетка ГРАНЕЙ над октодеревом
    src/transport/sweep3.c  C: развёртка DG1 + D: стенки как граничное условие
    src/transport/tet3.c    A2: моменты по многоугольнику (дописано)

**ИЗМЕРЕНО.**

| шаг | проверка | предсказано | измерено |
|---|---|---|---|
| A | `Σw − 4π`, первый момент, второй момент `(4π/3)I` | ≤1e-13 | `1.2e-14`, `8.7e-16`, `2.7e-15` |
| A | ни одно направление не параллельно оси | да | `min|ω_a| = 0.04` при ND=128 |
| A2 | моменты по многоугольнику | замкнутая форма | совпали во всех цифрах |
| B | **сумма площадей подграней = площадь стороны** | ТОЧНО (целые) | `0` неверных сторон на обеих сетках |
| C | **печь**, альбедо 1 | ≤1e-12 | `1.8e-15`, на градуированной `3.3e-15` |
| C | пустая среда: влёт проходит насквозь | ≤1e-12 | `9.9e-16` |
| C | **баланс энергии при НЕОДНОРОДНОЙ подсветке** (К13) | ≤1e-10 | `1.5e-14`, градуированная `6.9e-15` |
| D | **замкнутая полость `L = L_e/(1−ρ)`** | ≤1e-10 | **`0.0` и `2.8e-16`** |
| D | комната 512 ячеек, 1728 граней | картинка | радианс на стенках `0.93…1.61` |

**НЕГАТИВНЫЕ КОНТРОЛИ — ДВА ИЗ ДВУХ:**

- **«взять одного соседа» вместо всех** (к К13 в 3D): на равномерной сетке
  тождество площадей держится и так (0 нарушений — контроль там слеп, как К13 и
  предупреждал), а на градуированной ломается на 6 сторонах;
- **забыть `1/π` в ламбертовом ГУ:** радианс уехал с `1.0` на `5.2e54` —
  серия отражений расходится, потому что каждое отражение отдаёт в π раз больше.

**ЧТО ПОЙМАЛИ ЭТАЛОНЫ — ДВЕ НАСТОЯЩИЕ ОШИБКИ В КОДЕ.**

1. **Печь поймала знак нормали у МИНУС-стенки.** У граничной грани ячейка всегда
   записана в `ca`, и знак внешней нормали брался как у внутренней грани, то есть
   `+axis` для обеих стенок. Решение уезжало на 17%. Это ровно то, ради чего
   печь и стоит первой.
2. **Неверный член переноса.** `−∫b_i(ω·∇b_j)dV` собирался с лишним вкладом при
   `i == j`, тогда как `∫b_i dV = 0` при `i > 0`: градиент постоянен, и член
   связывает тестовую функцию ТОЛЬКО с постоянной модой.

Плюс третья, найденная на стыке уровней: **выточный член брался в базисе `ca`
даже когда ячейка была `cb`.** На равномерной сетке это не проявляется вовсе —
базисы соседей там совпадают по форме.

### К29. ПОЛУСФЕРНЫЙ ПОТОК НА ДИСКРЕТНОМ НАБОРЕ НЕ РАВЕН π, И ДЕЛИТЬ НАДО НА СВОЮ СУММУ

**Заведена по результату прогона.** Ламбертово отражение `L_out = L_e + ρE/π`
даёт на замкнутой полости ошибку **1.3%**, хотя точный ответ `L_e/(1−ρ)` известен
в замкнутой форме. Причина: в континууме `∫_{ω·N>0}(ω·N)dω = π` ТОЧНО, а на
дискретном наборе — нет. У стенок, перпендикулярных `z`, подынтегральное есть
`μ`, и гауссова квадратура берёт его точно; **у БОКОВЫХ стенок там стоят
`sqrt(1−μ²)` и `cos φ`, и ни одна из двух квадратур их точно не берёт.**

Деление на `π` теряет энергию на КАЖДОМ отражении, а отражений в комнате
десятки — отсюда и процент. **Лечение: делить на СОБСТВЕННУЮ сумму набора
`Σ_{ω·N>0} w(ω·N)`.** Тогда диффузное отражение сохраняет энергию ТОЧНО при любом
наборе направлений. После правки полость сошлась **побитово** (`0.0` и `2.8e-16`).

**Это дословно тот же приём, что `p0 = 1/Σw` для фазовой функции в T2**, и то,
что он понадобился второй раз, — довод считать его ПРАВИЛОМ, а не частным
случаем: **любая нормировка, взятая из континуума, а не из самого набора, теряет
энергию на дискретном наборе.**

## АУДИТ КОДА ПОСЛЕ ШАГОВ A–D (07-27) И ПРАВКИ ПО ЕГО РЕЗУЛЬТАТАМ

Проведён по просьбе пользователя после кода. Нашлось три вещи, все три
исправлены; `48/48` после правок.

### К30. ВЛАДЕНИЕ `st->bout` НЕ ОБНУЛЯЛОСЬ НА ПУТЯХ РАННЕГО ВОЗВРАТА

`tr3_sweep_solve` отдаёт наружу владение массивом исходящих радиансов. На путях
раннего возврата (нехватка памяти, цикл обхода) массив освобождался внутри, а
поле `st->bout` оставалось **неинициализированным** — и вызывающий, освобождая
его по заведённому порядку, освобождал мусор. **Санитайзеры этого не показали, и
показать не могли: на зелёном прогоне ранние возвраты не случаются вовсе.**
Ровно тот класс, который ловится чтением кода, а не прогоном.
**Правка: `st->bout = NULL` первой строкой функции.**

### К31. СЧЁТЧИК СРЕЗОК СМЕШИВАЛ ПЕРЕХОДНЫЙ ПРОЦЕСС С УСТАНОВИВШИМСЯ

`nclip` копился по всем итерациям, и отчёт печи показывал «срезок 32» и
«срезок 800». Из этого числа нельзя ответить на вопрос К6 — **активен ли
ограничитель НА СОШЕДШЕМСЯ решении**, то есть портит ли он константу. Два разных
случая (держал итерацию положительной / искажает ответ) были неразличимы.
**Правка: считать срезки ПОСЛЕДНЕЙ итерации отдельно. Измерено: НОЛЬ на обеих
сетках** — значит на сошедшемся решении ограничитель не активен, и К6 закрыт не
рассуждением, а числом. Проверка внесена в тест.

### К32. К12 В ТРЁХМЕРИИ НЕ БЫЛ ПРОВЕРЕН, ХОТЯ БАТАРЕЯ ОБЪЯВЛЕНА ПЕРЕНЕСЁННОЙ ДОСЛОВНО

В докладе шагов A–D сказано, что оснастка T2 переносится в 3D дословно. **Это
было неверно: теста с ЛИНЕЙНЫМ точным решением не было.** Вместо него стоял тест
«в пустой среде влёт проходит насквозь», который проверяет КОНСТАНТУ — то есть
ровно то, что уже проверяет печь. Наклоны, а это две трети неизвестных DG1,
оставались непроверенными.

Причина, по которой теста не было, записана честно и она содержательная:
подстановка `L = A + B·x` в `(ω·∇)L + σ_t L = ε` даёт `ε = B·ω_x + σ_t(A + B·x)`,
и первый член **зависит от направления**, а `ε` в постановке был изотропным.
**Правка: заведена направленная часть `eps_dir·ω`.** Измерено: ошибка среднего
`7.1e-15`, **ошибка НАКЛОНА `1.8e-14`**, на градуированной сетке `3.2e-14` и
`8.9e-15`. Теперь батарея T2 действительно перенесена целиком.

### ЧТО ПРОВЕРЕНО И ПРИЗНАНО ВЕРНЫМ, А НЕ ИСПРАВЛЕНО

**Топологический порядок пересобирается каждую итерацию, и это сознательно.**
Порядок зависит только от направления, поэтому напрашивается кэш — но хранить
пришлось бы `ND × ncell` целых, а это ровно то, что запрещает Р1 («угловое поле
транзиентно»). Цена пересборки `O(ncell + nf)` — тот же порядок, что у самого
прохода, то есть постоянный множитель, а не лишняя асимптотика. Записано в
комментарии, чтобы следующая сессия не «оптимизировала» это в нарушение Р1.

**Матрицы граней считаются один раз** (от направления не зависят) — проверено.
**Ламбертово ГУ хранит ОДНО число на грань** (Р3) — проверено.

## СТЫК РАЗВЁРТКИ С ГЕОМЕТРИЕЙ РАЗРЕЗА (07-27) — `src/transport/cut3.{c,h}`, `tools/render3.c`

**Вывод. Полноценный рендер есть**: два тела в освещённой комнате, глобальное
освещение развёрткой, взаимные тени, силуэты ТОЧНЫЕ (по примитиву, К15).
`57/57`, `ccheck` CLEAN, санитайзеры и `valgrind` чисты. Картинки — в `img/`.

**ЧТО СОЕДИНИЛОСЬ.** Флюидная область разрезанной ячейки берётся ТОЧНЫМ
разбиением дополнения (`hz_poly3_complement`), моменты аддитивны, поэтому матрица
масс есть сумма по кускам — приближения нет. Поверхностные элементы дают
исходящий радианс, хранимый DG1 ПО ПОЛОЖЕНИЮ.

**ИЗМЕРЕНО:** объём материала `164.34` против шара `164.64` (0.18% — цена
фасетизации при `k=1`); **замкнутость флюидной области `4.5e-14`**; поячеечно
`|флюид + материал − коробка| = 5.6e-14`.

### К33. ГРАНИ ДОПОЛНЕНИЯ — НЕ ГРАНИЦА МАТЕРИАЛА

**Заведена по прогону, и это была настоящая ошибка.** Поверхностные элементы
брались из граней кусков дополнения, лежащих на перевёрнутой полуплоскости. Но
кусок `j` дополнения граничит по плоскости `h_j` с множеством `{h_0..h_j}`, куда
входят и ПОЗДНИЕ куски дополнения, а не только материал. Площадь поверхности
выходила завышенной, и замкнутость флюида ломалась на `9.9` при площади грани
порядка `1`. **Поверхность берётся из САМОГО МАТЕРИАЛА:** у `hz_poly3_cut` грань
с `fsrc ≥ 0` и есть кусок фасета. После правки — `4.5e-14`.

### К34. Г45 НАРУШЕНА В СОБСТВЕННОМ ПОСТРОИТЕЛЕ СЦЕНЫ

Г45 записана давно: ключ боковой таблицы обязан строго возрастать, иначе
`hz_cutmap_add` вернёт 2 и запись пропадёт МОЛЧА. **И ровно это я сделал в первой
редакции построителя сцены:** добавлял по порядку `(x,y,z)`, а индексы узлов идут
порядком выделения, и код возврата не проверял. Половина записей отвергалась,
тело выходило дырявым, объём материала — `105` вместо `165`. Находка, записанная
за день до этого, не спасла: **её надо было ПРОВЕРЯТЬ, а не помнить.** В тест
внесена проверка «все записи легли».

### К35. Г38 НУЖНА ОТДЕЛЬНЫМ ВХОДОМ: ПОЛНАЯ ЯЧЕЙКА В ТАБЛИЦУ НЕ ПОПАДАЕТ

`hz_facets_for_box` отвечает НОЛЬ и для полной материала ячейки — режущих
плоскостей у неё нет, и в боковую таблицу она не попадает вовсе. Пропустить её
значит пустить свет НАСКВОЗЬ ТЕЛА. `tr3_cut_build` получает маску `solid_in`
отдельным аргументом, и это ровно то, о чём Г38 предупреждала: «ноль отобранных»
у полной и у пустой ячейки выглядит одинаково.

### ЦЕНА, ИЗМЕРЕННАЯ

| сцена | ячеек | направлений | итераций | время | память |
|---|---|---|---|---|---|
| два тела, FullHD 1920×1080 | 4096 | 32 | 4000 | **3:06** | **59 МБ** |

**Разрешение камеры почти бесплатно** — дорога РАЗВЁРТКА: `4000 × 32 × 4096`
решений 4×4. И 4000 итераций — это не сходимость, а её отсутствие: невязка
`1.25`, то есть ряд по рассеянию при светлых стенках ползёт, как и предупреждала
С1. **DSA перестал быть теоретической записью и стал главным узким местом.**

### ЧЕГО В РЕНДЕРЕ НЕТ, СПИСКОМ

- **ЗЕРКАЛ И ПРЕЛОМЛЕНИЯ.** Поверхность сейчас только ЛАМБЕРТОВА: одно число `ρ`
  на фасет. Зеркало по К5 через ординаты не идёт вовсе — преломлённое или
  отражённое направление в наборе отсутствует, и развёртка на такой границе
  ОБРЫВАЕТСЯ, дальше нужно ПРОСЛЕЖИВАНИЕ. Бесконечные отражения между двумя
  зеркалами сегодня не считаются ничем;
- **ГЛЯНЦА.** Нужны угловые моменты хранимого по ширине лепестка (Р3) и свёртка в
  ТОЧНОМ направлении пикселя без интерполяции между ординатами (К3);
- **ЦВЕТА.** Считается один спектральный канал;
- **разрешение ЗАТЕНЕНИЯ** задаётся поверхностными элементами (фасет ∩ ячейка) и
  гранями сетки, а не пикселями: отсюда видимая фасетировка и блоки на стенах.

## ЦЕНА РЕНДЕРА: ЗАМЕРЕНО 07-27, И ЭТО ГЛАВНОЕ, ЧТО ЕСТЬ ПРОТИВ ЛИНИИ

Пользователь поставил вопрос ребром: такая картинка обязана считаться меньше чем
за секунду, иначе выходит ПАРИТЕТ С ТРАССИРОВКОЙ, а тогда линия не имеет
самостоятельного смысла. Возражение верное, и оно уже записано в «ЧЕСТНОЙ
ОГОВОРКЕ» этого плана. Ниже — числа, а не рассуждения.

**СЦЕНА:** 4096 ячеек, 13056 граней, 1912 поверхностных элементов, 32 ординаты,
два тела, FullHD 1920×1080. Один поток, если не сказано иное.

| | было | стало | чем |
|---|---|---|---|
| **кадр (сбор по пикселю)** | 3930 нс/луч | **1133 нс/луч** | индекс граничных граней вместо перебора |
| кадр целиком | 8.1 с | **2.35 с** | то же |
| **холодный старт** | 157 с | 157 с | не трогалось |
| холодный старт, 16 потоков | — | 36 с (×4.3) | `#pragma omp parallel for` по ординатам |

### К36. ЦЕНА КАДРА БЫЛА РЕАЛИЗАЦИЕЙ, А НЕ АРХИТЕКТУРОЙ

Сбор искал граничную грань ЛИНЕЙНЫМ ПЕРЕБОРОМ всех 13056 граней на КАЖДЫЙ
пиксель — `2.7e10` сравнений на кадр. Грани лежат на ЦЕЛОЧИСЛЕННОЙ сетке, и
индекс `(стенка, u, v) → грань` прямой. `3930 → 1133 нс/луч`.

**Но 1133 нс/луч — это всё ещё примерно вдесятеро больше, чем у трассировщика на
такой сцене.** Остаток сидит в марше: на каждую ячейку вдоль луча делается
двоичный поиск в боковой таблице и разворачивание полуплоскостей. Лечится тем же
приёмом — прямым индексом по ячейке вместо поиска, — и это НЕ СДЕЛАНО.

### К37. ХОЛОДНЫЙ СТАРТ — ЭТО НЕ СХОДИМОСТЬ, А ЕЁ ОТСУТСТВИЕ

4000 итераций при невязке `1.25` означают, что ряд по рассеянию НЕ СОШЁЛСЯ.
Физике нужно порядка 30. То есть в 157 секундах примерно `×130` — чистая цена
отсутствующего разгона. **С1 предсказала это ещё до T1, T2 измерила (91…125
итераций при альбедо 1), и теперь оно блокирует всё остальное: DSA перестал быть
пунктом плана и стал единственным, что стоит делать дальше.**

Отдельно: одна итерация стоит `39.9 мс` на `131 тыс.` обновлений ячейки, то есть
`305 нс` на обновление при примерно 500 операциях в нём. Это ещё `×5…10` от
плотного кода, и оно ВТОРИЧНО по отношению к числу итераций.

### ЧТО ЭТО ЗНАЧИТ ДЛЯ СРАВНЕНИЯ С ТРАССИРОВКОЙ

Сравнивать надо **КАДР**, а не холодный старт: поле от камеры НЕ ЗАВИСИТ, поэтому
поворот камеры стоит только сбора (`CLAUDE.md`: «a camera move is a
RE-PROJECTION… not a re-solve»). Требование того же файла — докладывать оба числа
РАЗДЕЛЬНО — теперь исполняется прямо в `tools/render3.c`.

Сегодня: холодный старт `157 с`, кадр `2.35 с`. Отношение `67`, а обещано было
`~10³`. **Ни одно из двух чисел пока не является доводом в пользу линии**, и
честная позиция такая: пока не сделан DSA и не убран поиск из марша, о паритете
с трассировкой говорить рано — но и опровергнуть его нечем.

### ПАРАЛЛЕЛИЗМ ОТЛОЖЕН СОЗНАТЕЛЬНО

`×4.3` на 16 ядрах (а не `×16`) — упирается, судя по всему, в память и сведение
потоковых накоплений. Мерить это сейчас смысла нет: пока итераций 4000 вместо 30,
параллелизм лечит не ту болезнь. Код оставлен ОДНОПОТОЧНЫМ (структура под потоки
готова, включается одной строкой над циклом по ординатам) — по решению
пользователя: пока чинится алгоритм, параллелизм только мешает читать.

## ШАГ 1 ВЫПОЛНЕН 07-27 — К38/К39: ×81 ПО ХОЛОДНОМУ СТАРТУ, И ЭТО БЫЛ НЕ DSA

**Вывод. Холодный старт `157 с → 1.93 с`, итераций `4000 → 47`, и заодно с
картинки исчезли блоки на стенах.** `68/68`, `ccheck` CLEAN, ASan/UBSan и
`valgrind` чисты, `make test` зелёный (11 наборов), `check-fp` зелёный.
**Но главное здесь не число, а то, что аудит ДО КОДА опроверг собственный
диагноз плана: множитель ×130 был приписан отсутствию DSA, а на деле его давала
ОШИБКА В КОДЕ.** Порядок работ был назначен по величине множителя — и первый же
замер показал, что множитель принадлежит не тому пункту.

### КАК ЭТО ВЫЯСНИЛОСЬ — ЗАМЕР ВМЕСТО РАССУЖДЕНИЯ

К37 записала «4000 итераций при невязке 1.25» и прочла это как «ряд по рассеянию
ползёт». Первое, что было сделано, — напечатана ИСТОРИЯ невязки, и она этого не
подтвердила:

    it  0  resid 3.7e+01   phi0  3.64
    it 15  resid 1.4e+00   phi0 16.8283
    it 19  resid 1.2e+00   phi0 16.8367
    it 200 resid 1.8e+00   phi0 16.837905      ← решение стоит в 7-м знаке,
    it 3800 resid 1.8e+00  phi0 16.837959        а невязка не падает ВООБЩЕ

Решение сходилось к 15-й итерации, а невязка болталась около `1.2…1.8` вечно.
Значит это НЕ медленная сходимость, а **автоколебание**. Аргумент максимума
показал где: всегда компоненты 1…3, то есть НАКЛОНЫ, у горстки ячеек.

Сужение тремя выключениями, каждое со своим ответом:

| что выключено | итераций | вывод |
|---|---|---|
| ограничитель | не сходится, отн. амплитуда 0.65 | ограничитель НЕ виноват, он колебание ГАСИЛ |
| разрез целиком (комната без сфер) | **43**, невязка 7e-10, баланс 1.9e-8 | ряд по рассеянию исправен, скорость 0.573 |
| разрез есть, сферы не отражают (ρ=0) | **37**, 1.6 с | геометрия разреза исправна |
| всё включено | 4000, невязка 1.25 | виновато ОТРАЖЕНИЕ от поверхностных элементов |

### К38. 4000 ИТЕРАЦИЙ — ЭТО АВТОКОЛЕБАНИЕ НЕЛИНЕЙНОГО ПЕРЕКЛЮЧАТЕЛЯ

Ряд по рассеянию в этой сцене сходится за 43 итерации со скоростью `0.573`, что
ровно есть среднее альбедо комнаты, и С1 предсказывала именно это. Итерация
упиралась в `maxit`, потому что критерий остановки `max|φⁿ⁺¹−φⁿ| < tol` НЕ МОГ
быть выполнен никогда: в решении был незатухающий цикл амплитудой 3…4%.

**Урок общий, и он дороже самой правки: «не сошлось за N итераций» и «сходится
медленно» — РАЗНЫЕ диагнозы, и различает их не число итераций, а ИСТОРИЯ
невязки.** Стоила эта разница целого пункта плана.

### К39. МАТРИЦА МАСС ПЛОСКОГО ЭЛЕМЕНТА ВЫРОЖДЕНА ТОЧНО, ПО ПОСТРОЕНИЮ

Причина цикла оказалась одна на обе беды. Хранимый радианс раскладывался по DG1
решением `solve4(M, rr)`, где `M` — матрица масс элемента в базисе ЯЧЕЙКИ
`{1, ξ, η, ζ}`. Но элемент ПЛОСКИЙ, а на плоскости эти четыре функции линейно
ЗАВИСИМЫ: подстановка `x = clo + s(½ + b)` в уравнение плоскости `n·x = off` даёт

    s·Σ_a n_a·b_{a+1} − (off − Σ_a n_a(clo_a + s/2))·b_0 ≡ 0

то есть `M·nul = 0` ТОЧНО, и ранг равен ТРЁМ, а не четырём. Измерено на всех
1912 элементах сцены: `|M·nul| / (‖M‖·‖nul‖) = 1.36e-16`.

`solve4` этого не обнаруживает — четвёртый ведущий элемент выходит не нулём, а
округлением, — и возвращает решение с ПРОИЗВОЛЬНОЙ добавкой вдоль `nul`.
На самой плоскости добавка не видна (`nul·b = 0` там), зато ВНЕ плоскости она
огромна: измерено раздувание нормы представителя до `1.4e17`. А проверка
положительности `corner_min` считает как раз по углам ЯЧЕЙКИ, то есть вне
плоскости. Отсюда всё остальное:

| | было | стало |
|---|---|---|
| откатов к константе на стенках | **1536 из 1536** | **0** со 2-й итерации |
| откатов на поверхностных элементах | 1372 из 1912, и МЕНЯЛОСЬ (1372 → 1330) | 1, и не меняется |
| итераций / холодный старт | 4000 / 157 с | **47 / 1.93 с** |
| скорость сходимости | нет | 0.605 геометрически |

**ПОБОЧНОЕ СЛЕДСТВИЕ, КОТОРОЕ ВИДНО ГЛАЗАМИ.** Заголовок `sweep3.h` обещал
хранить исходящий радианс «DG1 ПО ПОЛОЖЕНИЮ» и отдельно оговаривал, что
хранить константой значило бы выбрасывать посчитанное. **Так вот, на стенках он
константой и хранился — во всех 1536 гранях из 1536, всегда.** Ровно это и было
причиной блоков на стенах, на которые пожаловался пользователь. Правка убрала их
без единого дополнительного вычисления: цена та же, а слепое место закрыто.

**ЛЕЧЕНИЕ.** Решать в ТРЁХ неизвестных: тождество `nul·b = 0` позволяет выразить
одну степень свободы через остальные. Исключается та, у которой коэффициент в
`nul` наибольший по модулю. **Порога здесь не появляется** — выбор идёт по
`argmax`, а не сравнением с числом; индекс 0 не исключается никогда, иначе не
представить постоянное поле. Результат есть представитель МИНИМАЛЬНОЙ нормы, и
именно его ограниченность лечит переключатель. Нулевой вектор не вычисляется
численно, а ЗАДАН геометрией и хранится рядом с матрицей масс (`tr3_selem.nul`).

### СМЕНА СОГЛАШЕНИЯ, КОТОРУЮ НАДО ЗНАТЬ

`bout[f·4]` и `sout[e·4]` БОЛЬШЕ НЕ ЕСТЬ значение радианса. Базис ячейки
центрирован на ЯЧЕЙКЕ, а грань лежит на её краю, поэтому у постоянного поля `c`
представитель минимальной нормы равен `(0.8c, 0.4c, 0, 0)`, и значение
собирается только вместе с базисом. `render3` так и делает и потому верен;
тест полости читал нулевой коэффициент напрямую и после правки показал 12%
ошибки. **Прежде он проходил ровно потому, что проекция была сломана: откат к
константе обнулял наклоны.** Тест переписан на вычисление значения в углах грани.

### ЧТО ПРОВЕРЯЮТ НОВЫЕ ФАЛЬСИФИКАТОРЫ

Печь и полость К39 не ловят ВООБЩЕ, и причина та же, что у К12: у однородного
равновесия все наклоны нули, и откат к константе там безвреден. Поэтому проверки
поставлены прямые.

| проверка | предсказано | измерено |
|---|---|---|
| П1 вырождение точное: `|M·nul|/(‖M‖‖nul‖)`, 2212 элементов | ≤1e-13 | **2.8e-15** |
| П2 `solve4` вырождения НЕ видит | не видит | ни одного точного нуля среди ведущих |
| П3 откатов к константе | >50% | **1536/1536 стен, 1372/1912 поверхностей** |
| П4 число откатов меняется между итерациями | обязано | 1372 → 1330 |
| проекция воспроизводит линейное поле (невязка моментов) | ≤1e-9 | **4.8e-16** |
| ‖ee‖/‖ex‖ — представитель минимальной нормы | ≤1 | **1.000** |
| П5 откатов после правки | единицы, и не меняются | 0 стен, 1 поверхность |
| П6 итераций | 40…80 | **47** |
| П7 холодный старт | 2…4 с | **1.93 с** |
| П8 печь и полость остаются точными | ≤1e-12 | 1.8e-15 и **4.4e-16** |

**НЕГАТИВНЫЙ КОНТРОЛЬ** — прежний `solve4`, воспроизведённый в тесте дословно:
норма представителя раздувается до `1.4e17`, угловой минимум уходит в минус у
423 элементов из 2212.

**ОДНО ПРЕДСКАЗАНИЕ НЕ СБЫЛОСЬ ПО ВЕЛИЧИНЕ.** Я предсказал, что негативный
контроль уведёт в минус БОЛЬШЕ ПОЛОВИНЫ элементов; вышло 19%. Причина
содержательная: в тесте подаётся ТОЧНО ЛИНЕЙНОЕ поле, у которого моменты
представимы без остатка, и величина паразитной добавки определяется только
обусловленностью конкретного элемента. В настоящей сцене облучённость линейной
не бывает, и там доля была 100% и 72%. Существо контроля уцелело и даже сильнее
(раздувание нормы на 17 порядков), проверка переписана на измеренное существо.

### ЧТО ЭТО ЗНАЧИТ ДЛЯ DSA

**DSA не отменяется, но его цена теперь известна и она НЕ ×130.** В этой сцене
ряд по рассеянию сходится за 47 итераций, и разгонять там нечего. Разгон нужен
там, где альбедо близко к единице, и это измерено ЗДЕСЬ же, а не в теории:
печь при альбедо ровно 1 берёт **156 итераций на равномерной сетке и 260 на
градуированной** (в 2D T2 было 91…125). То есть DSA — множитель порядка ×3…×5 в
светлой комнате и больше на снегу, а не ×130. Пункт остаётся, но переезжает
ПОСЛЕ пунктов 2 и 3, потому что его множитель теперь меньше ихнего.

### ЧТО НЕ СДЕЛАНО ЗДЕСЬ, ЯВНО

- **положительность проверяется по углам ЯЧЕЙКИ, а не по самому элементу.**
  Многоугольник лежит ВНУТРИ ячейки, поэтому проверка КОНСЕРВАТИВНА (ложного
  пропуска не даёт), но срезает наклоны раньше, чем нужно: у грани при `ξ=+0.5`
  запас выходит `0.6c` вместо `c`. Точная проверка требует вершин элемента, и
  это отдельная работа со своим замером;
- **откат остаётся ЖЁСТКИМ** — до константы целиком, а не срезкой наклона
  коэффициентом `α`, как в объёме (T2, замкнутая форма). Срабатывает он теперь
  раз на 13732 элемента, так что цена вопроса мала, но приём в объёме уже есть
  и лучше;
- **баланс энергии в пути разреза СЛЕП.** `pin − pout − pabs` не учитывает
  поглощение ПОВЕРХНОСТЯМИ, поэтому в сцене со сферами он показывает `1.66e2`
  при пропускной способности `1.0e4` — и это законно (сфера поглощает 22% того,
  что на неё падает), но означает, что тождество К13 в разрезанной сцене НЕ
  ПРОВЕРЯЕТ НИЧЕГО. Метрика, нечувствительная к той ошибке, которую якобы
  проверяет, — третья подпись артефакта. Заведено К40, не закрыто.

## ЗЕРКАЛА 07-27 — `src/transport/gather3.{c,h}`, ДВА ЗЕРКАЛЬНЫХ ШАРА

**Вывод. Зеркальная компонента посчитана ПРОСЛЕЖИВАНИЕМ, как и предписывает К5,
и проверена аналитическим эталоном, которого у зеркал раньше не было.** `21/21`,
`ccheck` CLEAN, ASan/UBSan и `valgrind` чисты, `make test` зелёный (12 наборов).
Картинка — `img/room_2mirrors.ppm`.

### ЦЕНА, ЗАМЕРЕННАЯ (раздельно, как требует CLAUDE.md)

**ЧИСЛА ПЕРЕСНЯТЫ ПО ТРИ ПРОГОНА. Первая редакция этого раздела приводила
2.59 с / 1251 нс и 3.75 с / 1806 нс — они были сняты на машине, занятой
сборкой, и завышены процентов на пятнадцать.** Ошибка моя и ровно того сорта,
против которого заведено правило про эталон стоимости: одиночный замер на
неизвестной нагрузке — не замер.

| прогон | холодный старт | кадр 1920×1080 |
|---|---|---|
| диффузные шары | 1.92…1.94 с (47 итераций) | 2.22…2.26 с, **1068…1091 нс/луч** |
| ЗЕРКАЛЬНЫЕ шары, до 8 отскоков | 1.51…1.62 с (37 итераций) | 3.10…3.18 с, **1494…1535 нс/луч** |

**ПРОСЛЕЖИВАНИЕ СТОИТ `×1.40`, И ЭТО ЦЕЛИКОМ ОБЪЯСНЯЕТСЯ ЧИСЛОМ МАРШЕЙ, а не
какой-либо неэффективностью зеркала:**

    первичных лучей   2.07 млн
    отскоков          1.04 млн
    всего маршей      3.11 млн  =  ×1.50 к работе
    измерено времени                ×1.40

Меньше пропорционального потому, что отскочивший луч КОРОЧЕ первичного: он
стартует внутри комнаты, а не от камеры. Детализация при этом не менялась ни в
чём — та же сетка 16³, те же 32 ординаты, тот же уровень фасетизации.

**ВЫНОС СБОРА В МОДУЛЬ НЕ СТОИЛ НИЧЕГО:** 1077 нс/луч в среднем против 1095 до
правки, при побитово той же картинке.

Холодный старт при зеркалах ДЕШЕВЛЕ, и это не выигрыш, а прямое следствие того,
что для развёртки зеркало есть чёрное тело: отражений в комнате меньше, ряд
сходится быстрее (37 итераций против 47).

### ПОЧЕМУ ПРОСЛЕЖИВАНИЕМ, А НЕ ОРДИНАТАМИ — И ЧТО ЭТО СТОИТ

К5 записана в плане задолго до кода: отражённое направление в наборе ординат
ОТСУТСТВУЕТ, а интерполировать между ординатами запрещает К3 (будут полосы).
Значит развёртка на зеркальной границе обрывается. Это та же логика, по которой
проход 1 (прямой свет) уже считается маршем по лучу, а не ординатами: «резкие
концы пути считаются точно и ВНЕ сетки направлений».

**ЦЕНА ГОВОРИТСЯ ВСЛУХ: ДЛЯ РАЗВЁРТКИ ЗЕРКАЛО ЕСТЬ ЧЁРНОЕ ТЕЛО.** Свет, упавший
на зеркало, из объёмного решения ПРОПАДАЕТ — он не рассеивается в ординаты и не
возвращается в комнату. Измерено: поглощено `654` против `277` у диффузных шаров
при той же пропускной способности. Комната с зеркалами выходит темнее физической
ровно на перехваченную долю. Это граница метода в его нынешнем виде, а не
недосмотр реализации. Вернуть энергию можно двумя путями — локально сгущать
набор направлений у зеркала либо завести отдельный перенос зеркальной компоненты
вдоль ЛОМАНОЙ; оба дороже, и ни один не измерен.

**И ОТДЕЛЬНО, ЧЕСТНО: на полностью зеркальной сцене эта линия ЕСТЬ
трассировщик.** Своё у неё — диффузная и объёмная часть, посчитанная один раз и
от камеры не зависящая. Зеркальная цепочка зависит от направления взгляда по
своей природе и оказалась бы в проходе сбора при любой формулировке.

### ЭТАЛОН, КОТОРОГО У ЗЕРКАЛ НЕ БЫЛО: ПЕЧЬ ДЛЯ ЗЕРКАЛ

**В ОДНОРОДНОМ поле идеальное зеркало НЕВИДИМО.** Радианс вдоль луча при
зеркальном отражении с `ρ = 1` сохраняется точно, поэтому пиксель, попавший в
зеркальный шар, обязан показать РОВНО фон. Хранимое поле при этом ЗАДАЁТСЯ
однородным, а не считается развёрткой: проверяется сбор, и подмешивать в эталон
ошибку решателя незачем.

| проверка | предсказано | измерено |
|---|---|---|
| Ф1 печь для зеркал, 6798 зеркальных пикселей, до 5 отскоков | ≤1e-13 | **`0.000e+00` — побитово** |
| Ф2 закон отражения, единичность и равенство углов | ≤1e-15 | 1.6e-14 (см. ниже) |
| Ф6 падение В ЦЕНТР шара даёт РОВНО обратный луч | ≤1e-13 | **1.16e-14** |
| Ф3 при `ρ=0.9` пиксель равен `ρ^k·L` по числу отскоков | ≤1e-13 | **2.0e-16** |
| Ф4 самопопадания без ε нет | 0 застрявших | **0 из 6798** |
| Ф5 свип по пределу отскоков выходит на полку | обязан | 5.1e-3 → 5.3e-4 → 9.5e-6 → **0** |

**ПРЕДСКАЗАНИЕ Ф2 БЫЛО ВЗЯТО СЛИШКОМ ТУГИМ И НЕ СБЫЛОСЬ: 1.6e-14 против 1e-15.**
Причина измерена, а не придумана: нормаль сферы считается как `(o + t·d − c)/r`,
то есть разностью величин порядка 8 при результате порядка 3, и её собственное
отклонение от единичной длины равно `4.9e-15`. Формула отражения усиливает его
множителем до `4(d·n)²`. Поэтому порог в тесте поставлен ОТ ИЗМЕРЕННОГО
`||n|−1|`, а не от круглого числа: если отражение начнёт врать само по себе,
отношение уедет, и это будет видно.

**НЕГАТИВНЫЕ КОНТРОЛИ:** без множителя `ρ` пиксель даёт `0.700` вместо `0.630`;
отражение по нормали ФАСЕТА расходится с примитивом на `1.5` при яркости
порядка 1, и среднее расхождение падает в 1.82 раза с ростом фасетизации.

### К44. ПЕЧЬ ДЛЯ ЗЕРКАЛ СЛЕПА К НОРМАЛИ — ТРЕТИЙ РАЗ ОДИН И ТОТ ЖЕ УЗОР

**Заведена по результату прогона.** Первая редакция негативного контроля гоняла
«отразить по нормали фасета» на ОДНОРОДНОМ поле Ф1 и получила расхождение РОВНО
НОЛЬ. Причина очевидна задним числом: в однородном поле куда луч ни отрази — он
придёт к тому же числу. То есть эталон, ловящий множитель, самопопадание и счёт
отскоков ТОЧНО, к геометрии не чувствителен ВООБЩЕ.

Это ровно тот же узор, что К12 (печь видит только среднее, наклоны проходят
незамеченными) и К13 (печь не ловит неконсервативную передачу потока). Третий
случай подряд, и общее правило теперь можно назвать: **эталон, у которого точное
решение ОДНОРОДНО, проверяет только те степени свободы, которые в однородности
участвуют.** Лечение то же самое: стенкам раздаются РАЗНЫЕ радиансы, и тогда
неверная нормаль отправляет луч на ДРУГУЮ стенку — ошибка `O(1)`.

Добавлен и второй, независимый от поля эталон — Ф6: луч, направленный в ЦЕНТР
шара, падает по нормали, и отражение обязано быть ровно обратным. Он проверяет
нормаль САМУ ПО СЕБЕ.

### ЕЩЁ ОДНА ОШИБКА БЫЛА В ТЕСТЕ, А НЕ В КОДЕ

Ф6 в первой редакции дал `3.3` при эталоне `0`. Причина: центры шаров разнесены
на `5.70`, а старт луча брался в `7.0` от центра, поэтому луч, идущий в центр
одного шара, законно задевал по дороге ВТОРОЙ, и нормаль была радиальна не тому
шару. Добавлена проверка, что попали в тот шар, в который целились. **Это третий
случай за проект, когда «неверный ответ» оказался у эталона, а не у кода** (два
были в T5а), и все три поймались тем, что эталон строился независимо.

### ЧЕГО У ЗЕРКАЛ НЕТ

- **преломления.** Снеллиус, Френель и закон `n²` не сделаны; зеркало сейчас
  задаётся ОДНИМ числом «зеркальная доля», и складывать его с диффузным `ρ` без
  Френеля нельзя — энергия учтётся дважды. Поэтому у зеркала диффузная доля
  ОБНУЛЯЕТСЯ, и за это отвечает строитель сцены, а не модуль;
- **шероховатого зеркала (глянца).** Нужны угловые моменты хранимого по ширине
  лепестка (Р3) и свёртка в ТОЧНОМ направлении пикселя (К3);
- **энергии, возвращаемой зеркалом в комнату** — см. выше, это К5 целиком;
- **К45: сбор не ослабляет радианс средой на последнем отрезке.** `tr3_scene`
  в `render3` получает `sigma = NULL`, то есть накопленная `τ` в сборе не
  используется вовсе, хотя среда в сцене есть (`σ_t = 0.015`, по комнате
  `τ ≈ 0.24`). Честно это лечится вместе с ВКЛАДОМ среды в луч (внутреннее
  рассеяние по пути), иначе ослабление без подсветки только испортит баланс.
  Заведено, не закрыто.

## ХОЛОДНЫЙ СТАРТ ЦЕЛИКОМ, ПО ЭТАПАМ (07-27) — И ОТНОШЕНИЕ ОКАЗАЛОСЬ 1.9, А НЕ 10³

Замер поставлен по требованию пользователя: **считать холодный старт НАЧИНАЯ С
РАССТАНОВКИ СЦЕНЫ, а не с развёртки, и назвать, что именно повторится на каждом
кадре.** Прежде «холодным стартом» звалась одна развёртка, а вся расстановка —
дерево, фасетизация, боковая таблица, сетка граней, матрицы масс разреза — не
мерилась нигде. Это нарушало собственное правило проекта в свою пользу.

    сцена: 4096 ячеек, 13056 граней, 1912 поверхностных элементов,
           32 ординаты, два тела, 1920×1080

| этап | время | доля | повторяется? |
|---|---|---|---|
| октодерево | 0.001 с | 0.0% | один раз |
| фасетизация примитивов | 0.000 с | 0.0% | один раз |
| боковая таблица (отбор фасетов по ячейкам) | 0.002 с | 0.0% | один раз |
| сетка ГРАНЕЙ над деревом | 0.003 с | 0.1% | один раз |
| маска полных ячеек (Г38) | 0.002 с | 0.0% | один раз |
| РАЗРЕЗ: флюид, матрицы масс, элементы | 0.125 с | 3.0% | один раз |
| набор ординат, индекс граней | 0.000 с | 0.0% | один раз |
| **РАЗВЁРТКА** | 1.861 с | 44.6% | один раз |
| **СБОР ПО ПИКСЕЛЮ** | 2.181 с | 52.2% | **НА КАЖДЫЙ КАДР** |
| ИТОГО | **4.174 с** | | из них на кадр **2.181 с** |

**НА КАЖДЫЙ КАДР ПОВТОРЯЕТСЯ РОВНО ОДИН ЭТАП — СБОР. Но он же есть больше
половины всей работы, поэтому отношение холодного старта к установившемуся кадру
равно 1.9, а план обещал ~10³.** Расстановка сцены при этом оказалась мелочью
(0.133 с, 3.2%), то есть прежняя цифра занижала холодный старт всего на три
процента — но не мерить её всё равно было нельзя.

### К46. ОТНОШЕНИЕ ХОЛОДНОГО СТАРТА К КАДРУ — НЕ СВОЙСТВО АРХИТЕКТУРЫ, А ОТНОШЕНИЕ РАЗМЕРА СЦЕНЫ К РАЗМЕРУ КАРТИНКИ

Свип по двум осям показывает это прямо, и число 1.9 к устройству метода
отношения не имеет:

| разрешение | развёртка | сбор | отношение |
|---|---|---|---|
| 640×480 | 1.90 с | 0.313 с | **7.5** |
| 1280×720 | 1.85 с | 0.952 с | 3.1 |
| 1920×1080 | 1.86 с | 2.199 с | 1.9 |
| 3840×2160 | 1.88 с | 8.748 с | **1.2** |

| ординат | развёртка | сбор | отношение |
|---|---|---|---|
| 8 | 0.538 с | 2.17 с | 1.3 |
| 32 | 1.87 с | 2.20 с | 1.9 |
| 72 | 4.11 с | 2.16 с | 3.0 |
| 128 | 7.13 с | 2.17 с | **4.3** |

**ЗАТО ГЛАВНЫЙ АРХИТЕКТУРНЫЙ ДОВОД ПОДТВЕРЖДЁН БУКВАЛЬНО, И ВОТ ОН В ЧИСЛАХ:
развёртка НЕ ЗАВИСИТ ОТ РАЗРЕШЕНИЯ ВОВСЕ** — 1.90 с на 640×480 и 1.88 с на 4K
при 44-кратном росте числа пикселей. Сбор строго линеен по пикселям (1062 нс на
луч на всех четырёх разрешениях, отклонение меньше процента). То есть
переиспользуемая часть действительно от камеры не зависит — `tr3_problem` камеры
не содержит вовсе, и это видно в замере, а не только в заголовке.

**Но выражать этот актив отношением НЕЛЬЗЯ, и обещание «~10³» надо снять.**
Отношение равно (стоимость решения)/(стоимость сбора), то есть оно про то,
насколько сцена велика ОТНОСИТЕЛЬНО картинки. Сегодня сцена — 4096 ячеек, то
есть игрушечная, и решение стоит 0.85 сбора. Тысяча появится только там, где
сцена велика относительно картинки, — то есть ровно в городе из `CLAUDE.md`.
**Это предсказание, а не замер, и до T7 его цитировать нельзя.**

Правильная формулировка актива: **дорогая часть не растёт с разрешением, точка.**

### И ОТДЕЛЬНО, ПРОТИВ САМООБМАНА: 1.9 — ЧИСЛО ОПТИМИСТИЧНОЕ

Сегодня движение камеры не трогает ничего, кроме сбора, ещё и потому, что **LOD
НЕ СДЕЛАН** — сетка равномерная. С камерно-привязанным LOD, который `CLAUDE.md`
требует ради выхода на город, движение камеры МЕНЯЕТ НАБОР ЭЛЕМЕНТОВ, и часть
расстановки (разрез, матрицы масс, сетка граней) вернётся в кадр. То есть 1.9
станет хуже, а не лучше, и настоящая инкрементность — отдельная работа, а не
следствие того, что поле от камеры не зависит.

### ЧТО ЭТО ЗНАЧИТ ДЛЯ ОБЛЁТА СЦЕНЫ

Для интерактивного облёта важно не отношение, а АБСОЛЮТНАЯ цена кадра: 2.2 с на
FullHD есть 0.46 кадра в секунду. До приемлемых 50 мс нужно `×44`, и всё это
`×44` лежит в СБОРЕ — то есть ровно в той части, где трассировщик силён.
Соревноваться там придётся на его условиях: индекс в марше (замерен потолок
×1.85), связи соседей вместо спуска по дереву (17% кадра), параллелизм
(структура готова).

## БАЛАНС ЭНЕРГИИ ВОССТАНОВЛЕН КАК ПРИБОР (07-27) — К40 И НАЙДЕННАЯ ИМ К48

По решению пользователя работа переключена с быстродействия на корректность.
Первым взят баланс, и не потому, что он «дефект», а потому, что это **сломанный
измерительный прибор**: главный инвариант схемы — точное дискретное тождество
К13, верное на ЛЮБОЙ итерации, — в разрезанной сцене не проверял НИЧЕГО.

`70/70`, `ccheck` CLEAN, ASan/UBSan чисты, `make test` зелёный (12 наборов).

### К40. ПОВЕРХНОСТИ ВНУТРИ ОБЛАСТИ В БАЛАНСЕ ОТСУТСТВОВАЛИ

Считалось `pin − pout − pabs`, и в сцене БЕЗ разреза это было точным тождеством
(1.5e-14). Но внутренняя поверхность берёт энергию себе, а в тождестве её не
было: невязка выходила `1.66e2` при пропускной способности `1.0e4` — и была
ЗАКОННОЙ, потому что сфера с `ρ = 0.78` поглощает 22% падающего.

**Проверка, у которой законная невязка полтора процента, не отличит от неё
ошибку схемы в полтора процента.** Третья подпись артефакта в чистом виде.
Теперь считаются обе стороны: `psin` — мощность, ушедшая в поверхности, `psout`
— отданная ими обратно, и тождество есть `pin + psout = pout + pabs + psin`.

    было:  невязка 1.66e2, то есть 1.6e-2 от втекшего
    стало: 3.4e-2, то есть 3.3e-6 — прибор стал чувствительнее в 5000 раз

### И ПРИБОР ТУТ ЖЕ ПОКАЗАЛ НАСТОЯЩУЮ ОШИБКУ — К48

**Предсказание `≤1e-9` не сбылось: осталось `3.3e-6`.** Дальше — три гипотезы,
и ДВЕ ПЕРВЫЕ БЫЛИ НЕВЕРНЫ, что и стоит записать, потому что опровергались они
числом, а не рассуждением:

| гипотеза | проверка | ответ |
|---|---|---|
| отставание итерации на шаг | свип по допуску `1e-7 → 1e-11` | **полка `4.51e-9`** — не отставание |
| флюид граничит с ПОЛНОСТЬЮ твёрдой ячейкой, и выток уходит в никуда | суммарная флюидная площадь на таких гранях | **`0` на `0` гранях** — геометрия согласована |
| две полусферные суммы у наклонной нормали не равны (К29 наизнанку) | `max|Σ_исх/Σ_вх − 1|` по 676 элементам | **`4.4e-16`** — набор симметричен |

Нашлось же вот что. **`pabs` считался как `(σ_t−σ_s)·φ₀·объём`.** Это верно для
КОРОБКИ, где `∫b_i dV = 0` при `i ≥ 1` по симметрии. Но у РАЗРЕЗАННОЙ ячейки
симметрии нет, и поглощённая мощность равна `(σ_t−σ_s)·Σ_i φ_i·M[0][i]`.
Наклонные члены выбрасывались. Плана это касается прямо: он сам отмечает, что
«`∫b_i dV` на флюидной области уже не ноль», — но отмечал это только в члене
переноса, а в балансе не применил.

**ПРИЗНАК, ПО КОТОРОМУ ЭТО НАШЛОСЬ, СТОИТ ЗАПОМНИТЬ: без ограничителя баланс был
ХУЖЕ в 800 раз** (`3.6e-6` против `4.5e-9`). Ограничитель гасит наклоны, а
выброшен был именно наклонный вклад — то есть «утечка» оказалась пропорциональна
тому, насколько разболтано поле. Это указывало на наклоны прежде, чем нашлась
формула.

**ПОСЛЕ ПРАВКИ тождество ведёт себя ровно так, как обязано:**

| допуск | итераций | невязка баланса на втекшее |
|---|---|---|
| `1e-7` | 40 | `6.9e-11` |
| `1e-9` | 49 | `6.9e-13` |
| `1e-11` | 500 | **`3.8e-15`** |

То есть остаток падает ВМЕСТЕ с допуском — это отставание поверхностного члена
на одну итерацию, и в пределе оно уходит. Разница «ограничитель вкл/выкл»
исчезла тоже: `5.6e-15` против `7.7e-14`.

### ПОПРАВКА К СОБСТВЕННОМУ ВЫВОДУ, СДЕЛАННОМУ ПО ХОДУ

Увидев `3.3e-6` в рендере против `4.5e-9` в тесте с ОДНИМ шаром, я приписал
разницу дефекту «два тела в одной ячейке» и назвал его главной утечкой. **Это
было неверно.** После правки `pabs` сцена с двумя телами даёт `2.9e-12`, то есть
не хуже сцены с одним; вся разница объяснялась тем, насколько в каждой сцене
велик выброшенный наклонный член. **Дефект двух тел остаётся, но баланс к нему
СЛЕП** — он геометрический (часть второго шара просто отсутствует, и свет сквозь
неё проходит), а не энергетический. Ещё одна метрика, не покрывающая то, что от
неё можно было бы ожидать.

## ФОРМАТ ВЫХОДА 07-27: РАДИАНС КАК ЕСТЬ (PFM), А PPM — ТОЛЬКО ДЛЯ ГЛАЗ

По замечанию пользователя. Разделять здесь надо ДВЕ вещи, и путать их нельзя.

**PPM P6 И ЕСТЬ RGB** — три канала по 8 бит, так что дело не в буквах формата.
Мешают две другие вещи:

1. **8 бит с гаммой 0.5 и нормировкой на процентиль.** Все три операции
   необратимы, и К19 это уже измерила: та же ошибка после них доходит до 255
   уровней из 255. По картинке нельзя сверить НИЧЕГО, и метрика поэтому всегда
   бралась на буфере радианса. Хранить надо ровно этот буфер;
2. **ЦВЕТА У НАС НЕТ ВОВСЕ.** Считается ОДИН спектральный канал, а то, что
   видно на картинке, есть ЛОЖНАЯ РАСКРАСКА скалярной яркости палитрой. Это не
   недостаток формата, а отсутствие физики: чтобы получить настоящий RGB, нужны
   три спектральных канала через всю развёртку.

### ЦЕНА ЦВЕТА — ×1.5…×1.6, А НЕ ×3 (поправка по замечанию пользователя)

**Первая редакция этого раздела говорила `×3` по холодному старту. Это было
сказано БЕЗ ЗАМЕРА и неверно.** Пользователь усомнился, и профиль его правоту
подтвердил. Разбор по тому, что у каналов ОБЩЕЕ, а что своё:

**ОБЩЕЕ, множитель ×1:** вся геометрия (октодерево, фасеты, боковая таблица,
сетка граней, матрицы масс разреза); топологический порядок обхода — он зависит
только от НАПРАВЛЕНИЯ; марш по лучу в сборе — луч один, геометрия одна, на
попадании читаются три числа вместо одного, а марш и есть почти вся цена сбора;
и **матрица ячейки `A`, ЕСЛИ СРЕДА СЕРАЯ** — при одинаковом `σ_t` она для трёх
каналов одна, собирается и раскладывается ОДИН раз, решается для трёх правых
частей.

**СВОЁ:** правая часть (источник рассеяния от `φ`, влёт от `bout`/`sout`),
обратная подстановка, ограничитель, накопление `φ`. То есть ЗНАЧЕНИЯ ПОЛЯ, а не
машинерия.

Разложение профиля развёртки по строкам исходника (замер, а не прикидка):

| | доля профиля |
|---|---|
| **на канал**: влёт от соседа в правую часть 13.8%, обратная подстановка 7.2%, ограничитель 3.2%, источник рассеяния 1.6%, прочее | ≈61% |
| **общее**: сборка матрицы 13.2%, обход граней 3.3%, топологическая сортировка 3.5% | ≈39% |

    расстановка  0.133 с × 1.00 = 0.13
    развёртка    1.86  с × 2.2  = 4.09     (0.39 + 3×0.61)
    сбор         2.18  с × 1.05 = 2.29
                                 ------
                                  6.5 с против 4.17, то есть ×1.56

**ПО ПАМЯТИ ЖЕ ×3 ЧЕСТНЫЕ** (`phi`, `bout`, `sout`), но геометрия не тройная:
59 МБ станут около 120, а не 180.

ДВЕ ОГОВОРКИ, ЧТОБЫ НЕ ПЕРЕВРАТЬ В ДРУГУЮ СТОРОНУ. Часть отнесённого «на канал»
есть общая индексная арифметика, переиспользуемая при цикле по каналам внутри,
так что настоящая цифра ближе к нижнему краю. И `×2.2` вырастет до `×3`, если
среда будет ЦВЕТНОЙ (`σ_t` свой на канал) — тогда матрица `A` перестаёт быть
общей. Для серой дымки и обычных комнат этого не нужно.

**ЧТО ЭТО ИЗМЕРИТ ПО-НАСТОЯЩЕМУ:** цифра выше опирается на разложение профиля
по строкам, а К43 (3380 команд на обновление ячейки против ожидаемых 500) до сих
пор не объяснена — значит и разложение неполно. Установленным считать нельзя,
пока цвет не сделан и не замерен.

**СДЕЛАНО:** `hz_pfm_write` — 32-битный float, три канала, без нормировки,
гаммы и раскраски. PFM выбран потому, что пишется двадцатью строками без единой
библиотеки (проект на C без зависимостей) и читается ImageMagick, GIMP, Krita,
OpenCV. Порядок строк снизу вверх и отрицательный масштаб как признак
little-endian — часть формата, а не наша прихоть; порядок байт ОПРЕДЕЛЯЕТСЯ на
месте, а не предполагается.

`R = G = B`, и файл об этом не врёт. Формат при этом взят ТРЁХКАНАЛЬНЫЙ (`PF`, а
не `Pf`) сознательно: когда каналов станет три, поменяется вызывающий код, а не
формат файла и не читатели.

**ФАЛЬСИФИКАТОР — КРУГОВОЙ ПРОГОН, И ОН НУЖЕН ИМЕННО ЗДЕСЬ.** Писалки картинок
врут МОЛЧА и ровно в трёх местах: порядок строк, порядок байт, чередование
каналов. Ни одно из трёх не видно, если смотреть картинку тем же кодом, который
её писал. Поэтому файл читается ОБРАТНО сырыми байтами:

| проверка | предсказано | измерено |
|---|---|---|
| заголовок `PF`, размеры, отрицательный масштаб | верны | `PF 7x5 -1.0` |
| значения после круга запись→чтение | ≤ округления float | **`0.000e+00` побитово** |
| лишних байт в файле | 0 | **0** |

Сторонняя сверка: `magick identify` читает файл и сообщает диапазон
`0.214645 … 6.11021` — ровно тот, что печатает рендер.

## К43 ЗАКРЫТА 07-28 — ЭТО БЫЛА ПУТАНИЦА ЕДИНИЦ, А НЕ СКРЫТАЯ РАБОТА

К43 гласила: «3380 команд на обновление ячейки против записанных в плане ~500 —
недооценка в 7 раз, и до уплотнения цикла надо понять, откуда она». Пользователь
справедливо поставил это как вопрос КОРРЕКТНОСТИ: расхождение в семь раз между
тем, что код должен делать, и тем, что он делает, означало бы, что он делает
что-то, чего мы не думаем.

**Замер счётчиками процессора (`fp_arith_inst_retired`):**

    команд                        21.94e9
    скалярных double-операций      3.009e9
    упакованных 128-бит            0.224e9   (по две операции каждая)
                                  --------
    всего вещественных операций    3.457e9  →  561 на обновление ячейки

**План ожидал «~500 операций» — сходится.** Код делает ровно ту арифметику,
которую мы думаем; скрытой работы нет.

Расхождение было ПУТАНИЦЕЙ ЕДИНИЦ: `3380` — это КОМАНДЫ, а `~500` — вещественные
ОПЕРАЦИИ. Отношение `21.94 / 3.457 = 6.35` команды на операцию — обычное для
скалярного C с индексным доступом и без векторизации (упакованных инструкций
всего 6% от всех вещественных, 256-битных нет вовсе).

**ЧТО ИЗ ЭТОГО СЛЕДУЕТ.** Дефекта нет, пункт из разряда корректности выбывает.
Остаётся вопрос БЫСТРОДЕЙСТВИЯ: 6.35 команды на операцию есть запас на
векторизацию и раскладку данных, и он реален. Но это оптимизация, а она отложена
по решению пользователя.

Попутно опровергнуты две гипотезы, обе числом:
- **деление внутри цикла источника рассеяния** (`sig_s[c]/(4π)` в теле): во всей
  функции 13 делений, и горячие из них — в `solve4`. Компилятор вынес его;
- **единственная тяжёлая строка**: её нет. Профиль по КОМАНДАМ размазан, верхние
  двадцать строк дают лишь 49%, и самая крупная (влёт от соседа в правую часть)
  — 14%. Уплотнять придётся всё сразу, а не одно место.

## ЦЕНА КАДРА ПРИ НЕПОДВИЖНОЙ СЦЕНЕ — ЗАМЕР 07-28, А НЕ ВЫКЛАДКА

Вопрос пользователя: сколько стоит именно РЕНДЕРИНГ, если геометрия остаётся на
месте, и какие действия при этом повторяются. Ответ получен прогоном НЕСКОЛЬКИХ
кадров подряд с движущейся камерой (`render3 W H nmu файл spec отскоки КАДРОВ`),
потому что по одному прогону цена кадра неотличима от разовой расстановки.

**ЧТО ПОВТОРЯЕТСЯ: РОВНО ОДИН ЭТАП — СБОР.** Между кадрами меняется только
`cam`; сетка, разрез, хранимое поле и индекс граней не трогаются вовсе, а
развёртка стоит позади цикла и в него не входит. Это видно по коду, а не только
по числам, и подтверждено разбросом кадров в 2%.

| разрешение | установившийся кадр | кадров в секунду | нс на луч |
|---|---|---|---|
| 640×480 | **0.346 с** | 2.89 | 1126 |
| 1280×720 | **1.009 с** | 0.99 | 1094 |
| 1920×1080 | **2.245 с** | 0.45 | 1082 |
| 1920×1080, ЗЕРКАЛА до 8 отскоков | **3.204 с** | 0.31 | 1545 |

Цена строго линейна по пикселям (1082…1126 нс на луч, разброс 4%), потому что
кадр есть марш по лучу и ничего кроме.

**ЧТО ДЕЛАЕТСЯ НА ОДИН ПИКСЕЛЬ:** построение луча камеры; марш по октодереву —
около 23 ячеек вдоль луча, и на КАЖДУЮ спуск по дереву до листа, параметр и ось
выхода, двоичный поиск в боковой таблице, разворачивание веера полуплоскостей,
пересечение с примитивом либо отсечение отрезка полуплоскостями, целочисленный
шаг через грань; на попадании — выбор поверхностного элемента по нормали и
вычисление хранимого DG1 в точке (четыре умножения со сложением); у зеркала —
отражение и повтор марша. **Чтение хранимого пренебрежимо; весь кадр есть марш**
— профиль это подтверждает: марш 37%, разворачивание полуплоскостей 31%, спуск
по дереву 17%, поиск в таблице 15%.

### ПОЧЕМУ С ЗЕРКАЛАМИ КАДР ДОРОЖЕ — ЭТО НЕ СХОДИМОСТЬ (замер 07-28)

Вопрос пользователя. Сходимость к цене КАДРА отношения не имеет вовсе — она про
холодный старт, и там зеркала как раз ДЕШЕВЛЕ (37 итераций против 47: зеркало
для развёртки чёрное, отражений в комнате меньше). Кадр дороже по одной причине
— МАРШЕЙ БОЛЬШЕ. Свип по пределу отскоков (1280×720):

| предел | кадр | отскоков | всего маршей | **нс на марш** |
|---|---|---|---|---|
| 0 | 1.002 с | 0 | 0.92 млн | **1087** |
| 1 | 1.316 с | 368 511 | 1.29 млн | **1020** |
| 2 | 1.338 с | 388 563 | 1.31 млн | **1021** |
| 4 | 1.332 с | 392 279 | 1.31 млн | **1014** |
| 8 | 1.320 с | 392 419 | 1.31 млн | **1005** |
| 16 | 1.329 с | 392 419 | 1.31 млн | **1011** |

**Цена одного марша ПОСТОЯННА (1005…1087 нс) при любом пределе.** Значит кадр
стоит ровно `(число маршей) × константа`, и добавочной цены у самого отскока
нет: ни поиска, ни пересборки, ни повторного чтения хранимого сверх одного.

Ещё два наблюдения из той же таблицы. **Отскоки НАСЫЩАЮТСЯ к четвёртому**
(392 419 и дальше побитово столько же) — цепочки кончаются сами, лучи уходят в
стенку; фальсификатор Ф5 показывал насыщение по РАДИАНСУ, а здесь то же по
РАБОТЕ, и это независимое подтверждение. И отскочивший марш чуть ДЕШЕВЛЕ
первичного (1010 против 1087 нс), потому что короче: стартует внутри комнаты, а
не от камеры.

**ЧТО НЕ ПОВТОРЯЕТСЯ, ПОКА ДВИЖЕТСЯ ТОЛЬКО КАМЕРА:** расстановка сцены (0.13 с)
и развёртка (1.9…2.1 с). Но это верно ровно при трёх условиях, и каждое стоит
назвать: свет не меняется (иначе развёртка заново), геометрия не движется (иначе
и расстановка), и **LOD не сделан** — с камерно-привязанным LOD движение камеры
меняет набор элементов, и часть расстановки вернётся в кадр.

**ДЛЯ ЦЕЛИ «ДВИЖУЩАЯСЯ КАМЕРА КАК В ИГРУШКАХ» ЭТО 0.45 КАДРА В СЕКУНДУ НА
FullHD.** До приемлемых 50 мс нужно `×45`, и всё это `×45` лежит В СБОРЕ, то
есть в марше — там, где трассировщик силён, и соревноваться придётся на его
условиях. Известные запасы: индекс в марше (потолок измерен, ×1.85), связи
соседей вместо спуска по дереву (17% кадра), параллелизм по пикселям (кадр
раскладывается тривиально, лучи независимы), векторизация (6.35 команды на
вещественную операцию). Ни один не сделан — по решению пользователя сперва
корректность.

## LOD ПРОВЕРЕН СЧЁТОМ 07-28 — `tools/lod3.c`. ЗАКОН ПОДТВЕРЖДЁН, А ВСЕ ПРЕЖНИЕ ЗАМЕРЫ СНЯТЫ НЕ С ТОЙ КОНФИГУРАЦИИ

Центральное утверждение архитектуры — «размер элемента `L = εR`, отсюда цена
ВЫХОДНО-ОГРАНИЧЕНА» — до сих пор не проверялось НИЧЕМ: во всех прогонах сетка
была равномерной 16³. Проверено самым дешёвым способом: дерево строится, ячейки
считаются, решение не запускается вовсе.

### СНАЧАЛА — НАСКОЛЬКО МЫ НЕДОРАЗРЕШЕНЫ

При `fov = 1.3` и 1920 пикселях угловой размер пикселя `ε = 6.77e-4`. Тогда:

| | |
|---|---|
| наша ячейка (1 единица) на расстоянии 8 | `185×185` = **34 тысячи пикселей** |
| поверхностный элемент (0.29 единицы) на расстоянии 8 | `53×53` = **3 тысячи пикселей** |

**Это и есть блочность картинки: элементы в полсотни-две сотни пикселей
поперёк.** Мы не «рендерим грубовато» — мы недоразрешены на четыре-пять
порядков против того, для чего метод построен.

**СЛЕДСТВИЕ, КОТОРОЕ НАДО ПРОИЗНЕСТИ: все цифры быстродействия этой сессии
(4.2 с холодный старт, 2.2 с кадр, «паритет с трассировкой») сняты с
конфигурации, недоразрешённой в сто тысяч раз.** Сравнивать её с
трассировщиком, выдающим полноразмерную картинку, было бессмысленно.

### ЧТО ИЗМЕРЕНО

**П2 — ВЫХОДНАЯ ОГРАНИЧЕННОСТЬ, ГЛАВНОЕ. Подтверждена.** `ε` фиксирован, сцена
растёт вдвое по каждой оси:

| куб | ячеек | прибавка | было бы по объёму |
|---|---|---|---|
| 32 | 32 768 | — | — |
| 64 | 173 104 | +140 336 | ×8 |
| 128 | 326 180 | +153 076 | ×8 |
| 256 | 467 916 | +141 736 | ×8 |
| 512 | 603 940 | +136 024 | ×8 |
| 1024 | 737 332 | +133 392 | ×8 |
| 2048 | 869 240 | +131 908 | ×8 |

**Прибавка ПОСТОЯННА.** Сцена выросла в 64 раза по стороне и в 262 144 по
объёму, а элементов стало больше в 26. Это логарифмический закон, ровно как
обещано.

**П3 — выигрыш против равномерной сетки того же шага РАСТЁТ НЕОГРАНИЧЕННО:**
`×6.4` на кубе 128, `×36` на 256, `×222` на 512, `×1456` на 1024.

**П4 — ПОВЕРХНОСТНЫЙ ЗАКОН `1/ε²`. Подтверждён.** При дроблении только у
поверхностей и схлопнутой пустоте отношение на уполовинивание `ε` держится
около **3.4** (ожидается 4 у поверхностного закона, 8 у объёмного): 3.08, 3.65,
3.38, 3.36. В абсолютных числах при `ε = 0.025` это 198 360 ячеек против
3 353 652 при заполнении объёма — **в 17 раз дешевле, и разрыв растёт как `1/ε`.**

### К49. ПОВЕРХНОСТНЫЙ ЗАКОН И ОБЪЁМНЫЙ — РАЗНЫЕ, И ПУТАТЬ ИХ НЕЛЬЗЯ

**Предсказание П1 («вдвое мельче пиксель — вчетверо больше элементов») НЕ
СБЫЛОСЬ: измерено 5.5.** Причина содержательная, и это находка, а не промах
замера: `c/ε²` есть закон для ПОВЕРХНОСТЕЙ, а первый построитель заполнял
ячейками ВЕСЬ ОБЪЁМ, где закон `1/ε³`, то есть ×8. Измеренные 5.5 лежат между
двумя законами, потому что часть области упирается в предел дробления.

Разница не косметическая: при `ε = 6.8e-4` объёмный закон дороже поверхностного
в `1/ε ≈ 1500` раз. **Вся ставка архитектуры стоит на СХЛОПЫВАНИИ ПУСТОТЫ**, и
без него никакой выходной ограниченности не будет. `CLAUDE.md` это и говорит
(«Large empty regions = one empty node»), но проверено оно только сейчас.

### ЧТО ЭТО ДАЁТ В ЧИСЛАХ ДЛЯ НАШЕЙ КОМНАТЫ

    равномерная сетка под пиксель      1.06e14 ячеек
    LOD  L = εR                        1.37e08 элементов
    выигрыш                            ×7.7e5

Холодный старт при таком LOD: `1.37e8 × 32 ординаты × 47 итераций × 561 операция
= 1.16e14` операций. На GPU в 20 терафлопс это **5.8 с**; на нашем одном ядре —
19 часов. Архитектура GPU-образная, и это не пожелание, а следствие счёта.

### ЧТО ОТСЮДА НЕ СЛЕДУЕТ

Закон проверен НА СЧЁТЧИКЕ ЯЧЕЕК, а не на решении. Не проверено ничего из
следующего, и до проверки цитировать нельзя: что развёртка на градуированной
сетке такой глубины сохраняет точность (передача потока между уровнями — К8/К13,
на трёх уровнях проверена, на двадцати нет); что условие 1:1 у разрезанных ячеек
выполнимо при таком перепаде; что топологическая сортировка не выродится; и что
сбор при пиксель-согласованном LOD действительно становится ПРОЕКЦИЕЙ элементов
вместо марша по пикселю — последнее и есть главный выигрыш по КАДРУ, и оно пока
рассуждение.

## РАЗБОР 07-28: РАБОЧЕЕ УРАВНЕНИЕ, УГЛОВОЙ БАЗИС, LOD И ЗЕРКАЛА

Длинный разбор с пользователем. Записано потому, что решений набралось на
отдельный раздел, а часть из них отменяет ранее написанное.

### РАБОЧЕЕ УРАВНЕНИЕ — ФОРМА 3, И ЭТО ПОДТВЕРЖДЕНО ЯВНО

`(ω·∇)L = −σ_t·L + σ_s ∫ p(ω′·ω)·L dω′ + ε`, граничный член — уравнение
рендеринга. Терм в терм в `sweep3.c`. Каталог остальных семи постановок и причин
отказа — выше, в разделе «каталог постановок».

**ТРИЖДЫ ЗА ОДИН ДЕНЬ Я ПРИТАЩИЛ БЕДЫ ИЗ ЧУЖИХ ПОСТАНОВОК, И ПОЛЬЗОВАТЕЛЬ ЛОВИЛ
ЭТО КАЖДЫЙ РАЗ.** Стоит записать как класс ошибки:

1. **«у уравнения рендеринга нелокальность спрятана в `L(x,ω′)`»** — верно для
   ТРЁХТОЧЕЧНОЙ формы, которую проект отверг. В форме 3 `L(x,ω′)` есть значение
   В ТОЙ ЖЕ ТОЧКЕ, перенос делает производная, оператор локален;
2. **«у вогнутого зеркала неограниченное увеличение и каустики»** — понятия
   ЛУЧЕВОЙ бухгалтерии. В форме 3 нет ни изображения, ни увеличения; каустика
   есть место, где `L` велико, а расходится якобиан лучевого отображения,
   которого в уравнении нет. Различие «выпуклое/вогнутое» пропадает целиком;
3. **«гладкие поверхности требуют интерполяции нормалей»** — в дискретизации
   гладких поверхностей НЕТ. Геометрия есть многогранник, у каждой грани точная
   плоская нормаль, сингулярностей не возникает, а фокусировка есть СУММА
   конечного числа плоских отражений.

Правило на будущее: **прежде чем назвать трудность, проверить, есть ли она в
ТОЙ постановке, которая в коде.**

### НАПРАВЛЕННОСТЬ ЗАДАЁТСЯ ШИРИНОЙ УГЛОВОГО ПОТЕНЦИАЛА, А НЕ ПОДМЕНОЙ НОРМАЛИ

Предложение пользователя, и оно лучше того, что предлагал я. Интерполяция
нормалей (Фонг) **сломала бы энергетические тождества**: нормаль входит в
уравнение через `|ω·n|` и через разделение полусфер, и для многогранника
правильная там — собственная нормаль грани. На ней держатся замкнутость флюида
`4.5e-14`, полость побитово и баланс `3.8e-15`. Подделанная нормаль не
согласована с площадью, и всё это поедет.

Сглаживать надо **ПОЛЕ**, а не нормаль: `L` есть физическая величина, и более
гладкий базис для неё законен; нормаль же — геометрия, и выдумывать её нельзя.

Количественно для зеркала:

    ширина углового потенциала ≥ 2L/r  ->  блик гладкий
                               <  2L/r  ->  блик гранёный

то есть «гранёный стакан или гладкий шар» становится **выбором базиса**, а не
хаком геометрии. Для диффузного то же достигается через ОТКЛИК: крупный элемент
несёт усреднённую угловую характеристику своих подграней — это правило (iii)
`CLAUDE.md` (сохраняется не форма, а отклик). Оговорка прежняя: усреднение верно,
пока подэлементы освещены однородно, иначе резать.

**Третий режим (интерполированные нормали) отпадает совсем.**

### К50. ДВА ПРОХОДА РАСХОДЯТСЯ В ТОМ, ЧТО ЗА ТЕЛО ОНИ СЧИТАЮТ

**Заведена по чтению кода.** К15 починил ЛУЧ, но не развёртку:

- **сбор** берёт попадание и нормаль у ПРИМИТИВА (сфера точная, силуэт круглый);
- **развёртка** берёт нормаль поверхностного элемента из плоскости ФАСЕТА
  (`cut3.c`, `se.n` из `h[src].n`), то есть считает тело МНОГОГРАННИКОМ.

Хранимый радианс посчитан на многограннике, а читается в точке, найденной на
сфере. **Отсюда огранка на диффузных шарах при формально точном силуэте** — это
не артефакт затенения и не разрывность DG, а верное затенение ДРУГОГО тела.

Три согласованных выхода, все честные: всё есть многогранник (и сбор берёт
фасет — режим `facet_only` уже есть); всё есть примитив (развёртка берёт нормаль
примитива в центроиде, площадь и косинус пересчитываются под неё); либо дробить
до незаметности. Сегодня не выбрано ничего — состояние смешанное.

### ПРАВИЛО LOD, СОБРАННОЕ ЦЕЛИКОМ

    L(x) = min по «камерам» c из [ max(ε, ширина лепестка пути) × длина пути ]

- прямо видимое: лепесток нулевой, обычное `L = εR`;
- диффузно достижимое (за спиной): лепесток порядка радиана, `L ≈ R`, то есть
  один элемент на объект;
- видимое в ЗЕРКАЛЕ: плоское зеркало порождает ТОЧНУЮ виртуальную камеру —
  отражение настоящей относительно плоскости; расстояние берётся до неё по
  развёрнутому пути. Рекурсия по отскокам, глубина ограничена пропусканием `ρⁿ`,
  то есть считается, а не назначается.

**ОБЯЗАТЕЛЬНАЯ ПОПРАВКА (возражение пользователя, и оно верное):** грубить можно
ПРЕДСТАВЛЕНИЕ, но не ВКЛАД. Ярко освещённая стена за спиной даёт бо́льшую часть
косвенного света; наполовину затенённая стена, схлопнутая в один элемент, даст
неверный ответ при верной суммарной энергии. **Заэкранное не выбрасывается
никогда, только грубеет, и только пока поле по элементу однородно; на границе
однородности — резать.** Это Р4 и Р6, уже записанные.

**Чем мы структурно защищены от беды экранных методов** (пользователь назвал
Lumen): заэкранная геометрия у нас в ТОЙ ЖЕ задаче — те же ячейки, те же
неизвестные, тот же решатель. Отдельного запасного представления нет, значит нет
и стыка, где два представления расходятся. **Где НЕ защищены:** LOD привязан к
камере, набор элементов при её движении меняется, и «всплывание» переехало из
стыка представлений в смену уровня.

### ЗЕРКАЛА: ЧТО ИЗМЕРЕНО И ЧТО ОСТАЛОСЬ

Набор ординат замкнут относительно **девяти** зеркальных плоскостей (трёх осевых
и шести диагональных — октаэдрическая группа), измерено, промах `≤6.7e-16`.
Для таких зеркал связь есть ТОЧНАЯ ПЕРЕСТАНОВКА ординат, идёт через развёртку
как обычная линейная связь, **без прослеживания и без предела отскоков**, и
бесконечная серия отражений берётся решением. Цепочки вне кадра покрываются
полностью — камеры в этом расчёте нет вовсе.

Для гранёного тела произвольной формы конструкция достраивается: у каждой грани
свой локальный набор, замкнутый под её отражение; наборы соседей почти совпадают
(нормали почти равны), значит оператор передачи близок к тождественному и
разрежен. Не построено.

**ПОПРАВКА К СОБСТВЕННОМУ ЧИСЛУ.** Я утверждал, что зеркальная поверхность
требует дробления в `R/r` раз мельче обычного. **Неверно:** я смешал требование
к нормали САМОГО зеркала с требованием к отражённым поверхностям. Выпуклое
зеркало УМЕНЬШАЕТ, разрешение окружения в кадре есть `ε·R/r`, отсюда
`2L/r < ε·R/r`, то есть `L < εR` — **обычный пиксельный размер**. Выпуклое
зеркало есть задача АГРЕГАЦИИ, а не разрешения.

### УГЛОВОЙ БАЗИС ВМЕСТО КВАДРАТУРЫ — РАЗВИЛКА, НЕ РЕШЁННАЯ

Нынешний набор (двойной Гаусс × квадранты) есть КВАДРАТУРА: точечные выборки с
весами, без носителей, без разбиения единицы, без иерархии. Отсюда К3 (нельзя
интерполировать), лучевой эффект и невозможность локально сгустить угол.

Угловые потенциалы с ОГРАНИЧЕННЫМ НОСИТЕЛЕМ снимают всё это разом и убирают
последний плотный объект системы:

| связь | с локальным угловым носителем |
|---|---|
| протекание `⟨φ_a, ω_k φ_b⟩` | ближайшие соседи по углу |
| рассеяние | ЛЕНТА по ширине лепестка; у изотропного — РАНГ ОДИН |
| BRDF | ламбертова ранг один, глянец лента, зеркало перестановка |

Блок `N_угл²` становится `N_угл × b`. Базисная функция с ограниченным носителем
и по пространству, и по углу есть ЭЛЕМЕНТ ФАЗОВОГО ПРОСТРАНСТВА (beamlet).

**Чем НЕ такие же, как пространственные:** на сфере нет сдвига и тензорного
произведения (нужен иерархический базис на подразделённом икосаэдре либо
сферические вейвлеты Schröder–Sweldens); замкнутых форм интегралов нет, потому
что метрика сферы не полиномиальна — **но квадратура по углу в переносе
ЗАКОННА**, запрет на неё есть директива ВОЛНОВОЙ линии и вызван осцилляциями,
которых здесь нет; и требуемая гладкость НИЖЕ — угол входит в уравнение
алгебраически, производных по нему нет, так что гладкость нужна не оператору, а
только против лучевого эффекта.

**ЦЕНА, И ОНА ОДНА, ЗАТО КРУПНАЯ.** Дискретные ординаты — это ДИАГОНАЛИЗОВАННЫЙ
угловой базис: квадратурный набор одновременно диагонализует все три матрицы
`⟨φ_a ω_k φ_b⟩`, и только поэтому существует развёртка. С перекрывающимися
носителями одновременной диагонализации нет, **развёртка как прямой решатель
`O(N)` исчезает**, а с ней и Р1 (угловое поле перестаёт быть транзиентным, полный
шестимерный вектор надо держать). Что дешевле — не рассуждением решается.

### ВОСЬМАЯ ФОРМА: НЕ ДВИГАТЬ LOD, А ПРОВЕРЯТЬ ЕГО

Пользователь забраковал важность как ДВИЖУЩУЮ СИЛУ LOD, и справедливо: правило
`L = εR` бесплатно, а важность требует решить сопряжённую задачу ради выбора
сетки. Плюс адаптация не может раздробить то, чего грубая сетка не увидела, —
страховкой служит то же правило по расстоянию.

**Роль восьмой формы поэтому другая: измерительная.** Ошибка В КАРТИНКЕ равна
`⟨W, невязка⟩`, поэтому одна сопряжённая задача на умеренной сетке говорит,
сколько ошибки вносит правило `L = εR` и ГДЕ — в частности за зеркалом. Считается
тем же кодом с обращёнными направлениями. И даёт тождество взаимности
`⟨W_e, L⟩ = ⟨W, L_e⟩`, которое обязано держаться до `1e-14` и проверяет ОБА
решателя разом, разными путями. Такой проверки у нас нет ни одной.

Геометрическое правило (виртуальные камеры, длина пути, ширина лепестка) — это и
ЕСТЬ приближение к `W`: для диффузного пути важность убывает как `1/r²` на
отскок, для зеркального идёт сквозь без потерь. Не два подхода, а дешёвая версия
и точная.

### ЧТО ВЫИГРЫВАЕТСЯ ОГРУБЛЕНИЕМ ЗАЭКРАННОГО — ЧИСЛА

    пикселей экрана                        2.07e6
    «пикселей» полной сферы  4π/ε²         2.74e7    ×13
    элементов LOD (пять октав)             1.37e8    ×66

Октавы неотчуждаемы: это настоящие поверхности на настоящих расстояниях. Выигрыш
сидит в `×13`, и не целиком. При огрублении диффузно достижимого вчетверо-ввосьмеро
по линейному размеру выходит `1.37e8 → ~1.2e7`, то есть **потолок ×11**.
Реально ждать `×5…×8`: часть заэкранного огрублению не поддаётся (та самая ярко
освещённая стена), плюс границы однородности требуют резать.

**Это лучший из известных нам множителей.** Для сравнения: индекс в марше
измерен ×1.85, у плотного цикла запас 6.35 команды на вещественную операцию.

## КРЫЛОВСКОЕ УСКОРЕНИЕ ИЗМЕРЕНО 07-28 — И ПЕРВЫЕ ТРИ ЗАМЕРА БЫЛИ ВЫРОЖДЕНЫ

**Итерация по рассеянию есть РЯД НЕЙМАНА — самый медленный способ решить
`(I − T)φ = b`.** Он складывает порядки рассеяния, и его скорость равна
спектральному радиусу `T`. Крыловский метод на том же операторе идёт по `√κ`.
Проверено прямо: `93/93`, `ccheck` CLEAN, `make test` зелёный (12 наборов).

Оператор построен ИЗ САМОЙ РАЗВЁРТКИ, а не выписан заново: `S(φ)` — один проход
(поле `warm_start` в `tr3_problem`), `b = S(0)`, `A·v = v − S(v) + b`. Одно
умножение на `A` есть один проход, поэтому сравнение честно вести по ЧИСЛУ
ПРОХОДОВ: BiCGStab делает два на итерацию.

**ЛОВУШКА, НАЗВАННАЯ ДО КОДА И ОКАЗАВШАЯСЯ ВЕРНОЙ.** Крылову нужен ЛИНЕЙНЫЙ
оператор, а в проходе сидит ограничитель положительности; крыловские векторы
физическим радиансом не являются, у них есть отрицательные компоненты, и
ограничитель на них сработает. Опыт поэтому ставится с ВЫКЛЮЧЕННЫМ ограничителем,
и законно это ровно на печи: там `nclip = 0` на сошедшемся решении (К31).
**Для сцен с отражением так нельзя** — в рендере ограничитель активен и на
сходимости (458 срезок), и нужен другой ход.

### ТРИ ЗАМЕРА ПОДРЯД ОКАЗАЛИСЬ ВЫРОЖДЕНЫ, И КАЖДЫЙ РАЗ ПО-РАЗНОМУ

| редакция | что вышло | почему негодно |
|---|---|---|
| однородная печь | ×69.5 за один шаг | точное решение ОДНОРОДНО, оно почти параллельно правой части |
| неоднородная `σ` | ×24 за один шаг | не помогло: причина не в среде |
| локальный источник `ε` | ×26 за один шаг | не помогло: причина не в источнике |

Диагноз получен измерением, а не рассуждением: `cos(b, A·b) = 1.000000` при
`cos(v, A·v) = 0.997` на СЛУЧАЙНОМ векторе, и `‖s‖/‖b‖ = 7.8e-17` после первого
шага. **Правая часть `b` оказалась ТОЧНЫМ доминирующим собственным вектором
оператора**, отчего решение просто кратно ей: `x = α·b`, `α = 1/(1−μ)`.
Подтверждается тем, что ряд Неймана сходится ровно со скоростью `μ`:

    σ_t·h = 0.05 : μ = 0.055 -> 13 проходов
    σ_t·h = 0.80 : μ = 0.524 -> 52
    σ_t·h = 4.00 : μ = 0.869 -> 227

### СВИП ПО ОПТИЧЕСКОЙ ТОЛЩИНЕ ЯЧЕЙКИ

| `σ_t·h` | ряд Неймана | BiCGStab | «ускорение» | `cos(v,A·v)` на случайном |
|---|---|---|---|---|
| 0.05 | 13 | 2 | ×6.5 | 0.999988 |
| 0.80 | 52 | 2 | ×26.0 | 0.996974 |
| 4.00 | 227 | 2 | ×113.5 | 0.981686 |
| 0.80, альбедо 0 | 2 | 2 | ×1.0 | — |

Ряд Неймана деградирует с толщиной, BiCGStab держит два прохода при любой. **Но
цитировать эти множители НЕЛЬЗЯ** — они получены на вырожденной правой части.

### РЕШАЮЩИЙ ОПЫТ: ТА ЖЕ СИСТЕМА, СЛУЧАЙНАЯ ПРАВАЯ ЧАСТЬ

    ‖s‖/‖b‖ = 2.57e-02 после первого шага — уже не машинный ноль

| `σ_t·h` | ряд Неймана | BiCGStab | достигнутая невязка |
|---|---|---|---|
| 0.05 | 13 | **6** | 1.32e-13 |
| 0.80 | 52 | **6** | 1.87e-14 |
| 4.00 | 227 | **6** | 1.45e-13 |

**ГЛАВНОЕ ЗДЕСЬ НЕ МНОЖИТЕЛЬ, А ТО, ЧТО ЦЕНА НЕ РАСТЁТ** (формулировка
пользователя, и она точнее моей): шесть проходов при ЛЮБОЙ толщине, до одной и
той же точности, тогда как ряд Неймана идёт `13 → 52 → 227`. То есть «нет
замедления», а не «есть ускорение».

И как показатель это ЦЕННЕЕ множителя: множитель сцено-зависим и, как выяснилось
выше, легко вырождается, а ограниченность цены — свойство метода.

**ЕЩ�ё ОДНА ОШИБКА БЫЛА В ПЕЧАТИ, А НЕ В МЕТОДЕ.** Первая редакцияпоказывала
деградацию невязки `2e-11 → 6e-8 → 2.5e-5` и выглядела как обрыв BiCGStab. На
деле метод останавливался на ПОЛОВИННОМ шаге, где настоящая невязка есть `s`, а
печаталось старое `r`, ещё не обновлённое. После правки видно, что во всех трёх
режимах достигнута одна точность.

### ЧТО ИЗ ЭТОГО СЛЕДУЕТ

1. **Скорость Крылова зависит НЕ ТОЛЬКО ОТ ОПЕРАТОРА, НО И ОТ ПРАВОЙ ЧАСТИ** — от
   того, сколько собственных мод в ней возбуждено. У ряда Неймана такой
   зависимости нет: он всегда идёт как `ρⁿ`, туго и предсказуемо. **Крылов может
   дать ×100, а может почти ничего, и заранее это не видно.** Замечание
   пользователя («Крылов не совсем для наших условий, может иногда выскочить»)
   подтвердилось в первом же прогоне.
2. **Мерить ускорение обязательно на СЛУЧАЙНОЙ правой части**, иначе физический
   источник даёт вырожденно хороший ответ. Это четвёртый за проект случай, когда
   эталон оказался слеп к тому, что якобы мерит (К12, К13, К44 — и теперь этот).
3. **Оператор рассеяния при `σ_t·h ~ 1` почти диагонален** (`cos = 0.997` на
   случайном векторе): свободный пробег равен ячейке, рассеянный свет почти не
   уходит из неё. В нашем рендере `σ_t·h = 0.015`, то есть другой конец, и там
   картина будет иной. Не измерено.

## К27 ЗАКРЫТА 07-28 — И ФАЛЬСИФИКАТОР ПРИШЛОСЬ ПЕРЕПИСЫВАТЬ ТРИЖДЫ

**Марш перебирает ВСЕ различные примитивы веера и берёт БЛИЖАЙШЕЕ попадание.**
`38/38`, `ccheck` CLEAN, ASan/UBSan чисты, `make test` зелёный (12 наборов).

Прежде брался первый же фасет с `surf ≥ 0`, и перебор на этом кончался: второе
тело в веере не проверялось вовсе. Берётся именно БЛИЖАЙШЕЕ, а не первое
найденное — на стыке двух тел порядок фасетов в веере произволен.

### АУДИТ ДО КОДА ПОКАЗАЛ, ЧТО ДЕФЕКТОВ ТРИ, А НЕ ОДИН

1. **`tr3_march` берёт первый `surf` из веера** — это и есть К27, закрыта здесь;
2. **`render3` кладёт в веер фасеты ТОЛЬКО ОДНОГО тела**: при встрече второго
   печатает «ячеек с ДВУМЯ телами» и молча его выбрасывает. Значит в ЭТОЙ сцене
   правка марша ничего не меняет — второго тела в веере просто нет;
3. **модель реза не умеет ОБЪЕДИНЕНИЯ**: материал есть ПЕРЕСЕЧЕНИЕ полуплоскостей,
   одно выпуклое тело на ячейку. Положить в веер оба тела — значит получить их
   пересечение вместо объединения, то есть стало бы хуже.

Пункт 2 без пункта 3 смысла не имеет, а пункт 3 — правка ядра общего слоя со
своим аудитом. **Оба остаются открытыми, и предупреждение в `render3` снимать
нельзя.**

### ФАЛЬСИФИКАТОР НЕ РАБОТАЛ ДВАЖДЫ, И ОБА РАЗА ЭТО ВИДНО ТОЛЬКО ПО КОНТРОЛЮ

**Первая редакция:** лучи вдоль оси сквозь два КАСАЮЩИХСЯ шара. Тест прошёл
(`0.000e+00`), но негативный контроль — возврат прежнего поведения — **тоже
прошёл**. Причина: лучи попадали в тела ЗАДОЛГО до ячеек стыка, и до
двухтельных ячеек дело не доходило.

**Вторая редакция:** сетка лучей, падающих СВЕРХУ на окрестность касания.
Контроль выстрелил — 3 промаха, — **но не на ту находку**: это оказалась К21
(скользящий луч теряется, потому что веер индексирует ФАСЕТНОЕ тело, а примитив
торчит наружу него на `dmax`). С правкой и без неё цифры совпадали, то есть К27
по-прежнему не проверялась.

**Третья редакция:** тела ПЕРЕСЕКАЮТСЯ (центры на 6.0 при радиусах 5 и 4), а не
касаются. При касании двухтельных ячеек всего 8, и в них первым в веере случайно
всегда оказывалось верное тело; при пересечении их 48, и ближнее тело в них то
одно, то другое.

| | прежнее поведение | с правкой |
|---|---|---|
| ячеек с двумя телами | 48 | 48 |
| max \|t − аналитика\| | **1.594** | **0.000e+00** |
| max ошибка нормали | **1.512** | **0.000e+00** |
| попаданий НЕ В ТО ТЕЛО | **12** | **0** |
| промахов (это К21) | 15 | 1 |

Эталон аналитический и посчитан независимо: ближайший входной корень уравнения
сферы. Побочно видно, что перебор помогает и скользящим лучам — промахи упали с
15 до 1, потому что при промахе первого примитива теперь пробуется второй.

**ОБЩЕЕ ПРАВИЛО, КОТОРОЕ СТОИТ ЗАПОМНИТЬ.** Тест, проходящий при ВНЕСЁННОЙ
ошибке, не проверяет ничего, и увидеть это можно ТОЛЬКО прогнав негативный
контроль. Здесь он спас дважды подряд: первый раз показал, что проверка пуста,
второй — что она ловит ДРУГУЮ находку. Это уже пятый случай за проект, когда
метрика оказалась слепа к тому, что якобы стережёт (К12, К13, К44, вырожденная
правая часть у Крылова — и теперь этот).

## ПЛАН ПЕРЕХОДА НА ПОЛНУЮ СХЕМУ УРАВНЕНИЯ 3 (записан 07-28)

Итог дневного разбора. **«Полная переделка» — неточное слово: уравнение,
геометрия реза, сетка граней и вся батарея аналитических эталонов ОСТАЮТСЯ.
Меняются два слоя — угловое представление и решатель, — плюс LOD и сбор.**

### ЧТО НЕ ТРОГАЕТСЯ (проверено, и переписывать это значит терять проверенное)

- **уравнение**: `(ω·∇)L = −σ_t L + σ_s ∫p L dω′ + ε`, граничный член —
  уравнение рендеринга. Оператор локален по пространству, нелокален только по
  углу и только в точке;
- **геометрия реза**: `poly3` (точное отсечение и разбиение дополнения), `surf`
  (примитивы и фасеты), `tet3` (моменты в замкнутой форме, квадратурных узлов
  ноль), `cut3` (флюид, матрицы масс, поверхностные элементы);
- **сетка граней**: грань есть ПЕРЕСЕЧЕНИЕ рёбер соседей, отсюда консервативность
  передачи потока между уровнями (К13);
- **эталоны**: H-функция Чандрасекара `O(ND⁻²)`, печь `1e-15`, полость
  `L_e/(1−ρ)` побитово, линейное поле с наклонами, энергетическое тождество
  `pin + psout = pout + pabs + psin` до `3.8e-15`, печь для зеркал побитово,
  К27 против аналитического пересечения. **Все они обязаны продолжать держаться
  после каждого этапа — это и есть страховка перехода;**
  **ПОПРАВЛЕНО К58 (ниже): в этом виде строка неверна для D и недостаточна для
  A.** Список разделён на три разряда, и разряд назван у каждого эталона;
- **К15** (примитив владеет ответом) и режим `facet_only`.
  **ПОПРАВЛЕНО К59 (ниже): этап A растеризует ЭЛЕМЕНТЫ, то есть многогранник, и
  К15 при этом ОТМЕНЯЕТСЯ, а не сохраняется.**

### ЧТО МЕНЯЕТСЯ

| | сейчас | станет |
|---|---|---|
| угол | КВАДРАТУРА: 32 ординаты, двойной Гаусс × квадранты | БАЗИС с локальным носителем, разбиением единицы и иерархией |
| решатель | ряд Неймана (итерация по рассеянию) | Крылов с предобуславливателем |
| сетка | равномерная 16³ | `L = εR`, схлопывание пустоты, квантование по двоичной лестнице |
| сбор | марш по лучу на пиксель | ПРОЕКЦИЯ элементов, растеризация (**К59: это отменяет К15**) |
| зеркала | прослеживание с пределом отскоков | связь ВНУТРИ системы **И прослеживание, а не вместо него — К56** |
| хранимое | 1 число (диффузное) | по ширине лепестка ПОТРЕБИТЕЛЯ |

**СТРОКА «ЗЕРКАЛА» ПРАВЛЕНА ПО К56: было «прослеживание → связь внутри системы»,
то есть союз «вместо».** Внутрисистемная связь возвращает в комнату ЭНЕРГИЮ
(измерено: зеркальные шары поглощают `654` против `277` у диффузных), а видимый
ОБРАЗ на разрешении `ND` она разрушает. Это две разные работы над одним объектом.

### ПОРЯДОК РАБОТ — ПО ЗАВИСИМОСТЯМ, А НЕ ПО ЖЕЛАНИЮ

Этапы НЕ независимы, и порядок задан этим, а не удобством. Этапов шесть: A…E плюс
**F (важность как отсечение)**, внесённый 07-28. **Раздел «цена на GPU» стоит
МЕЖДУ A и B намеренно:** К72 (разделение разрядности) обязана быть решена ДО A,
потому что от разрядности зависит трафик, а задача пропускно-ограничена.

**A. LOD И ПРОЕКЦИОННЫЙ СБОР.**
**ПЕРВАЯ РЕДАКЦИЯ ЭТОГО ПУНКТА ЧИТАЛА «ВМЕСТЕ, И ТОЛЬКО ВМЕСТЕ» И ОБОСНОВЫВАЛА
ЭТО ДВУМЯ ЧИСЛАМИ, ОБА НЕВЕРНЫ (К52, К53); ЗАЯВЛЕННЫЙ МНОЖИТЕЛЬ ТОЖЕ НЕВЕРЕН И
ИМЕЕТ ОБРАТНЫЙ ЗНАК (К51).** Пункт остаётся первым, но по другому доводу: он
единственный, который делает картинку РАЗРЕШЁННОЙ, а без этого сравнивать не с
чем (правило 6). Экономией он не является.

*Что он на самом деле стоит.* Собственный счёт раздела «LOD проверен счётом»:
`1.37e8` элементов × 32 × 47 × 561 = `1.16e14` операций, то есть **19 часов на
одном ядре против нынешних 1.9 с**. A увеличивает холодный старт примерно на
четыре порядка и уменьшает его только против равномерной сетки под пиксель
(`1.06e14` ячеек), которой мы никогда не строили. Выигрыш `×7.7e5` — против
несуществующей конфигурации, и цитировать его как выигрыш ЭТАПА нельзя.

**К52 ЗАКРЫТА ЗАМЕРОМ 07-28, И ДОВОД «ТОЛЬКО ВМЕСТЕ» ОТПАЛ.** Марш идёт по
НАСТОЯЩЕМУ маршу (`tr3_march` без геометрии) по двум деревьям при ОДНОМ `ε`:

| `ε` | пустота СХЛОПНУТА | весь ОБЪЁМ |
|---|---|---|
| `0.1` | ячеек `17 424`, вдоль луча медиана **18** | `109 096`, **70** |
| `0.05` | `58 948`, **21** | `603 940`, **114** |
| `0.025` | `198 360`, **22** | `3 353 652`, **176** |

**При схлопнутой пустоте число ячеек вдоль луча ПОЧТИ НЕ РАСТЁТ** (18 → 21 → 22
при учетверении `ε`, то есть логарифмически), тогда как ВСЕГО ячеек становится в
одиннадцать раз больше. У объёмного закона оно растёт как `1/ε`, и экстраполяция
к `ε = 6.77e-4` даёт `176 × 36.9 ≈ 6500` — **это и есть «7500» из первой редакции
этого пункта, то есть число принадлежало ОБЪЁМНОМУ закону, как К52 и
предсказала.** Со схлопыванием пустоты при том же `ε` выходит `~27` ячеек —
столько же, сколько сегодня на равномерной `16³` (23).

**Следствие прямое: LOD можно делать БЕЗ проекционного сбора, марш от этого не
дорожает.** Половины этапа A разделимы (К53), и первая же из них проверяема
отдельно.

*Фальсификаторы, ПРАВЛЕНЫ по К53, К54, К66:*
1. **проекция против марша НА ОДНОЙ И ТОЙ ЖЕ сетке 16³** — изолирует смену
   сбора. Предсказание: расхождение ≤ ошибки дискретизации, и **картинка обязана
   остаться БЛОЧНОЙ** (блоки — свойство сетки, а не сбора);
2. **марш на LOD-сетке против марша на 16³** — изолирует смену сетки, и заодно
   даёт число ячеек вдоль луча вместо предсказанного 7500 (К52);
3. **цена по ДВУМ осям раздельно (К66)**: разрешение при фиксированной сцене и
   содержимое сцены при фиксированном разрешении, предсказание для каждой оси
   своё. Прежняя редакция свипела одну ось и ссылалась на `lod3.c` — но `lod3.c`
   считал ЯЧЕЙКИ, а про цену СБОРА не мерил ничего, и К46 уже показала на этом же
   файле, что по одной оси отношение врёт (7.5 на 640×480 против 1.2 на 4K). У
   проекционного сбора цена есть `O(элементов в кадре, включая ПЕРЕКРЫТЫЕ слои)
   + O(покрытых пикселей)`, и первое слагаемое от разрешения не зависит вовсе;
4. `cut.nbad` — предсказание **не ноль, а больше нуля** (К54);
5. энергетическое тождество и линейное поле с наклонами — **на сетке с ПОЛНЫМ
   разбросом уровней**, а не на трёх (К58).

*Известные риски:* условие 1:1 у разрезанных ячеек при большом перепаде уровней
(К54 — не риск, а гарантированный исход); **Г50 и К21 переезжают сюда из
открытых хвостов (К60)**; квантование по двоичной лестнице обязано быть ВВЕРХ —
число «экономия `2.16`» опоры не имеет (К62) и подлежит замеру в `lod3.c`;
грубить можно ПРЕДСТАВЛЕНИЕ, но не ВКЛАД; **точный силуэт (К15) теряется, если
растеризуется элемент, а не примитив (К59)**.

**К52. `7500` ВЫВЕДЕНО ПО ОБЪЁМНОМУ ЗАКОНУ, А К49 ЕГО ОТВЕРГЛА.**
`ln(R_max/R_min)/ε` считает ячейки вдоль луча так, как если бы объём был ЗАПОЛНЕН
ячейками размера `εR`. Но П4 измерила ПОВЕРХНОСТНЫЙ закон `1/ε²`, а К49 назвала
схлопывание пустоты условием всей ставки: «без него никакой выходной
ограниченности не будет». Луч проводит почти весь путь в пустых КРУПНЫХ узлах и
встречает мелкие только у поверхности, в которую и упирается. **Измеряется за
час на уже написанном `lod3.c`: построить дерево при `ε = 6.77e-4` и посчитать
ячейки вдоль пучка лучей.** Предсказание записать до прогона; ожидание — сотни, а
не 7500, и рост ЛОГАРИФМИЧЕСКИЙ по размеру сцены, как в П2. Если выйдет 7500 —
значит пустота не схлопывается, и это находка крупнее самого марша.

**К53. ПОЛОВИНЫ A РАЗДЕЛИМЫ, И ВТОРОЙ ДОВОД НЕВЕРЕН ПО НАПРАВЛЕНИЮ.**
Написано: «проекция без LOD даёт дыры, потому что элемент в 53 пикселя на пиксель
не ложится». Дыры даёт СПЛАТ — одна выборка на элемент; РАСТЕРИЗАЦИЯ
многоугольника в 53×53 пикселя закрашивает 2809 пикселей и даёт БЛОКИ, а блоки на
стенах у нас и так есть. В одной и той же ячейке таблицы стоит «проекция
элементов, растеризация», а довод построен на сплате — то есть два разных
алгоритма названы одним словом.

**Из этого следует прямое:** проекцию можно поставить на НЕИЗМЕННОЙ сетке 16³ и
сверить с маршем — это и есть сужение выключениями, которым К38 нашла
автоколебание, и метод проекта требует менять по одному. «Только вместе»
запрещает единственную проверку, способную отличить ошибку проекции от ошибки
сетки. Цена такой проверки — часы, а не дни, потому что вся геометрия и всё
хранимое остаются на месте.

**К54. `cut.nbad` СЕГОДНЯ ПУСТ, А ПОД LOD ОБЯЗАН СТАТЬ НЕНУЛЕВЫМ.**
`cut.nbad` считает разрезанные ячейки, у которых грань сетки не совпала со
стороной коробки (`src/transport/cut3.c`, условие 1:1). На равномерной 16³
разбитых граней НЕТ ВООБЩЕ — тот же случай, что уже измерен в шаге B: «на
равномерной тождество площадей держится и так, 0 нарушений — контроль там слеп».
Значит `nbad == 0` в `tests/test_sweep3.c` сегодня не проверяет ничего.

Под `L = εR` вдоль ЛЮБОЙ протяжённой поверхности размер разрезанной ячейки растёт
с расстоянием, значит соседние разрезанные ячейки оказываются РАЗНЫХ уровней, и у
крупной сторона разбита на подграни. **`nbad > 0` не риск, а гарантированный
исход.** Лечение (обрезать флюидную часть прямоугольником грани либо запретить
перепад уровней у разрезанных ячеек) — названная работа ВНУТРИ A со своим
фальсификатором, а не побочный эффект. Предсказание до прогона: `nbad` растёт с
перепадом уровней и обращается в ноль только при запрете перепада.

**К68. `ε` — СВОБОДНЫЙ ПАРАМЕТР, А ПЛАН ОБРАЩАЛСЯ С НИМ КАК С КОНСТАНТОЙ. ЭЛЕМЕНТ
НЕ ОБЯЗАН БЫТЬ В ОДИН ПИКСЕЛЬ.**

**Заведена по замечанию пользователя, и это самый крупный рычаг, названный в этом
плане** — крупнее огрубления заэкранного, которое раздел «что выигрывается
огрублением» называет «лучшим из известных нам множителей» (`×5…×8`).

Везде в файле стоит `ε = 6.77e-4` — поле зрения, делённое на 1920 пикселей, то
есть элемент РОВНО в пиксель. Довода под этим нет ни одного. Собственное правило
LOD пишет `L = max(ε, ширина лепестка) × R`, то есть `ε` есть ПОЛ, и почему пол
лежит на пикселе, не сказано нигде. **В волновой линии у пола есть физическое
основание — дифракционный предел `λ/D`; в переносе фазы нет, дифракции нет, и
основания нет вовсе.** Пол задаётся требованием к КАРТИНКЕ, а не уравнением, и
потому подлежит замеру, а не назначению.

Размер элемента входит В КВАДРАТЕ (измеренный закон П4: уполовинивание `ε` даёт
`×3.4`, идеал поверхностного закона `×4`):

| элемент | элементов | против записанного |
|---|---|---|
| 1 пиксель — как записано в плане | `1.37e8` | — |
| 2 пикселя | `4.0e7` | `÷3.4` |
| **3 пикселя** | **`1.9e7`** | **`÷7.0`** |
| 4 пикселя | `1.2e7` | `÷11.6` |

**И это СКЛАДЫВАЕТСЯ с огрублением заэкранного и со сменой решателя.** Полный
счёт холодного старта по формуле самого плана (`N × ND × итераций × 561`):

**ПО ПРОИЗВОДИТЕЛЬНОСТИ, ПОДРАЗУМЕВАЕМОЙ СОБСТВЕННЫМ ЧИСЛОМ ПЛАНА** (`1.16e14`
операций = 19 часов ⇒ `1.7e9` оп/с на ядро; 16 ядер дают `×4.3`, измерено):

**СТОЛБЕЦ GPU ПЕРЕСЧИТАН 07-28 ПО ЗАМЕЧАНИЮ ПОЛЬЗОВАТЕЛЯ И СЧИТАЕТСЯ ТЕПЕРЬ КАК
`max(счёт, трафик)`, А НЕ ПО ОДНИМ ФЛОПСАМ** (К73). Допущения названы прямо:
**карты «6090» не анонсировано**, поэтому берётся 5090 (`104` Тфлопс FP32, `1.6`
FP64, `1.79` ТБ/с) и поколенческий множитель `×1.5` по обеим осям — `155` Тфлопс
FP32, `2.4` Тфлопс FP64, `2.7` ТБ/с. КПД: 20% на FP32, 30% на FP64, 70% по памяти.

| конфигурация | операций | трафик | одно ядро | 16 ядер\* | «6090», FP32 | «6090», FP64 |
|---|---|---|---|---|---|---|
| как записано, `1.37e8` эл., 47 итераций | `1.16e14` | 33 ТБ | 19 ч | 4.4 ч | **17 с** ← память | **161 с** ← счёт |
| 3 пикселя на элемент, `1.9e7` | `1.6e13` | 4.6 ТБ | 2.6 ч | 37 мин | **2.4 с** ← память | **22 с** ← счёт |
| + заэкранное `×6`, `3.1e6` | `2.6e12` | 0.75 ТБ | 25 мин | 5.9 мин | **0.39 с** ← память | **3.6 с** ← счёт |
| + Крылов, 6 проходов | `3.4e11` | 95 ГБ | **3.3 мин** | 46 с | **50 мс** ← память | **0.47 с** ← счёт |
| + переиспользование граней (KBA `×2.5`) | `3.4e11` | 38 ГБ | — | — | **20 мс** | 0.47 с ← счёт |

\* столбец «16 ядер» построен на измеренном `×4.3`, а К71 говорит, что это число
снято на сцене 4096 ячеек и не переносится. Оставлен помеченным, а не удалён.

**И ВОТ ЧТО ВЫЛЕЗЛО ПРИ ЗАМЕНЕ СТОЛБЦА — ЭТО ДОРОЖЕ САМИХ ЦИФР. В FP32 ЗАДАЧА
УПИРАЕТСЯ В ПАМЯТЬ, В FP64 — В СЧЁТ, И РАЗНИЦА `×10…×24`.** На игровой карте FP64
есть `1/64` от FP32, поэтому двойная точность превращает пропускно-ограниченную
задачу в счётно-ограниченную. **Прежний столбец «20 Тфлопс при 10%» этого не
показывал вовсе: он давал `0.17 с` там, где верные ответы — `50 мс` в FP32 и
`0.47 с` в FP64, то есть врал в обе стороны сразу.**

**Следствие: К72 (разделение разрядности) переходит из «хорошо бы» в «решить ДО
этапа A»** — оно стоит порядок, а не проценты. И отдельная оговорка: на
вычислительных картах (H100/B200-класс) FP64 есть `1/2`, и там картина
переворачивается. **Значит выбор разрядности зависит от ЦЕЛЕВОГО ЖЕЛЕЗА и обязан
быть назван явно, а не унаследован от `double` в исходнике.**

**«ДОЛИ СЕКУНДЫ» ОТНОСЯТСЯ ТОЛЬКО К GPU, И ПИСАТЬ ИХ БЕЗ ЭТОГО СЛОВА НЕЛЬЗЯ** (по
замечанию пользователя). На одном ядре лучшая строка есть МИНУТЫ. Отговоркой это
не является — `CLAUDE.md` ставит целью GPU, и раздел «LOD проверен счётом» уже
писал «архитектура GPU-образная, и это не пожелание, а следствие счёта», — но
цифра без носителя есть цифра не о том.

**Это первый расклад в файле, где холодный старт выходит на GPU-достижимое, и
получен он не оптимизацией кода, а признанием `ε` параметром.** Ни один из трёх
множителей не измерен; каждый выведен из уже измеренного закона, и потому подлежит
проверке, а не цитированию.

**ЧЕМ ПЛАТИМ — ТРИ ВЕЩИ, И У ВСЕХ ТРЁХ ОТВЕТ В ПЛАНЕ УЖЕ ЕСТЬ.**

1. **Гладкая часть затенения — почти ничем.** Хранимое на поверхностном элементе
   есть DG1 ПО ПОЛОЖЕНИЮ — это была правка К39, убравшая блоки со стен, — значит
   на трёх пикселях линейная рампа воспроизводится точно. Теряется вторая
   производная освещённости, а она мала везде, кроме кромок;
2. **Кромки теней — платим по-крупному, и лечение записано** (Р4: «на границе
   однородности ячейка РЕЖЕТСЯ», Р6: прямая составляющая — мелкая сетка с
   разрезами, косвенная — грубая). Кромка есть КРИВАЯ, а не площадь, поэтому её
   разрешение стоит `O(длина/h)`, и это ровно тот расклад `1.4e8 → 1.6e5`, который
   уже посчитан в разборе травы;
3. **Силуэт — и здесь «слегка кривые сплаты» слово в слово точны.** Элемент в три
   пикселя, растеризованный ПЛОСКИМ многоугольником, даёт трёхпиксельную огранку.
   Элемент, несущий свой ПРИМИТИВ, даёт точный контур при любом своём размере
   (измерено `0.0` побитово, 169/169 лучей). **Значит К59 решается в пользу ветки
   (ii) — растеризовать примитив, а не многогранник, — и решается она этой
   находкой, а не отдельным аудитом: именно кривизна делает крупный сплат
   неотличимым от мелкого.**

**ФАЛЬСИФИКАТОР, РЕШАЮЩИЙ ВОПРОС ОДНИМ ПРОГОНОМ.** Свип размера элемента
`1, 2, 3, 4, 6` пикселей против эталона в 1 пиксель; метрика — БУФЕР РАДИАНСА
(К19), раздельно ВНЕ кромок и НА кромках (К20).
*Предсказание до прогона:* вне кромок ошибка растёт как `h²` (хранимое линейно по
положению), на кромках — `O(1)` независимо от `h`. **Если общая кривая выйдет
`h²` — упирается гладкая часть, и грубить можно до предсказанного предела; если
выйдет ПОЛКА — упираются кромки, и ответ не «дробить», а «резать», то есть Р4/Р6.**
Оба исхода полезны, и различает их наклон, а не величина.
*Негативный контроль:* отключить DG1 у хранимого (константа на элемент) ⇒ при
трёх пикселях блоки обязаны вернуться на стены, как было до К39 (1536 граней из
1536). Если не вернутся — свип меряет не то, что думает.

**СВИП ВЫПОЛНЕН 07-28, И ПРЕДСКАЗАНИЕ НЕ СБЫЛОСЬ — ВЫШЕЛ ТРЕТИЙ ИСХОД.** Комната
`16³`, `32³` и `64³` при ФИКСИРОВАННОМ мире (`ROOM = 16`, единица дерева `ROOM/nc`)
и ФИКСИРОВАННОМ уровне фасетизации; эталон — `64³`; метрика на буфере радианса,
кромки отдельно (`tools/pfmdiff.c`).

| | отн. ошибка вне кромок, сред | макс | на кромках, макс |
|---|---|---|---|
| `16³` против `64³` | `3.03e-2` | `3.73e-1` | `3.55e-1` |
| `32³` против `64³` | `1.45e-2` | `1.85e-1` | `2.75e-1` |
| **отношение** | **2.09** | **2.02** | 1.29 |

**Это чистый ПЕРВЫЙ порядок — ни `h²`, ни полка.** Причина содержательная и
задним числом очевидная: сцена НЕ ГЛАДКАЯ. Кромки теней, фасетизованная сфера и
разрезанные ячейки дают решение с разрывными производными, и второго порядка от
DG1 здесь ждать было неоткуда. Предсказание молчаливо считало поле гладким —
ровно та ошибка, за которую T2 уже наказывала в обратную сторону (там предсказали
1…2, вышло 2.90).

**ЧТО ИЗ ЭТОГО СЛЕДУЕТ ДЛЯ САМОЙ К68, И ЗНАК БЛАГОПРИЯТНЫЙ.** При первом порядке
огрубление втрое стоит `×3` по ошибке, а не `×9`: коарсить ДЕШЕВЛЕ, чем
предсказывалось. Но абсолютные числа надо называть вместе с этим: даже `64³` есть
элементы по 15 пикселей при 640×480, а остаточная ошибка по Ричардсону (`p = 1`)
составляет `~1.5%` в среднем. До трёх пикселей на элемент отсюда ещё `×5` по
стороне.

**И ЧТО ОСТАЛОСЬ НЕ ИЗМЕРЕННЫМ:** свип шёл по СЕТКЕ при фиксированном
разрешении картинки, то есть по пикселям на элемент он покрыл `61 → 30 → 15`, а
не `1 → 3`. Область, ради которой К68 заводилась, лежит ЗА эталоном, и войти в
неё можно только сделав `128³` и мельче. Экстраполяция первого порядка туда —
предсказание, а не замер, и цитировать её как измеренное нельзя.

### ЦЕНА НА GPU — ПЕРЕСЧЁТ ПО ТРАФИКУ, А НЕ ПО ФЛОПСАМ (К70…К73)

Внесено 07-28 по решению пользователя снять ограничение «один поток, без
векторизации» и по его же вопросу о топовой видеокарте.

**К70. ПОРЯДОК ОБХОДА ЗАВИСИТ ТОЛЬКО ОТ ОКТАНТА, А НЕ ОТ ОРДИНАТЫ — ПРОВЕРЕНО В
КОДЕ.** `sweep3.c` строит входящие степени по `on = om[axis]` и смотрит ТОЛЬКО на
её знак. Грани сетки осепараллельны, поверхность входит граничным условием и в
граф обхода не попадает. Значит граф зависит от тройки знаков `ω`, то есть от
ОКТАНТА. Два следствия, и второе важнее:

1. топологическая сортировка нужна **8 раз на итерацию, а не `ND` раз** — сегодня
   она перестраивается на каждую ординату (сознательно, ради Р1). Экономия мелкая
   (около 1% профиля), но комментарий в коде надо поправить, иначе следующая
   сессия сохранит лишнюю работу как «требование Р1»;
2. **ординаты ОДНОГО ОКТАНТА идут по ячейкам в одном и том же порядке, то есть
   являются идеальной осью SIMD.** Это и есть ответ на вопрос «куда применять
   AVX-512»: `4×4`-солв слишком мал, по ячейкам мешает зависимость по потоку, а
   по ординатам внутри октанта зависимости нет вовсе. При `ND = 32` это 4 полосы
   (половина регистра), при `ND = 128` — 16 (два полных регистра). **Довод в
   пользу большего `ND`, которого раньше не было.**

**К71. `×4.3` НА 16 ЯДРАХ ИЗМЕРЕНО НА СЦЕНЕ 4096 ЯЧЕЕК И НЕ ПЕРЕНОСИТСЯ.** Работа
на поток там в тысячи раз меньше целевой, и доминирует сведение накоплений в `φ`.
Правило 6 запрещает цитировать до-LOD цифры — а `×4.3` цитируется в плане трижды.
**Число не годится ни как оптимистичное, ни как пессимистичное: оно снято не в тех
условиях, и параллелизм надо мерить заново на целевом размере.**

**К72. РАЗРЯДНОСТЬ — АРХИТЕКТУРНОЕ РЕШЕНИЕ, А НЕ ФЛАГ СБОРКИ, И ПРИНЯТЬ ЕГО НАДО
ДО ЭТАПА A.** На потребительских картах FP64 есть `1/64` от FP32 (5090-класс:
`104` Тфлопс против `1.6`). Наш код весь `double`. Разделение обязано пройти по
той же границе, которую `CLAUDE.md` уже провёл на разряд выше («целочисленное в
топологии дерева, float в значениях поля и в математике решателя»):

| что | разрядность | почему |
|---|---|---|
| топология дерева, сопоставление граней | **целое** | уже так |
| геометрия реза, вершины, плоскости, площади | **FP64** | Г50 даёт `3.6e-13` при координатах `16` уже в FP64; в FP32 тот же путь даёт `~1e-4`, то есть ВИДИМЫЕ щели, а водонепроницаемость по Г46 «держится именно на битах» |
| энергетическое тождество, эталоны | **FP64** | прибор К40 имеет чувствительность `3.3e-6 → 3.8e-15`; в FP32 пол округления `~1e-7`, и восемь порядков чувствительности исчезают |
| поле `L`, моменты `φ`, `4×4`-солвы, ограничитель | **FP32 допустим** | цель по точности `1e-3` (T3: при альбедо 0.9 `1e-3` требует `ND = 16`), у FP32 семь знаков |

**Фальсификатор:** прогнать всю батарею в смешанной разрядности. **Предсказание до
прогона:** геометрические эталоны (К27 `0.000e+00`, силуэт побитово, замкнутость
флюида `4.5e-14`) обязаны остаться НЕ ХУЖЕ прежних, а полевые (печь, полость,
баланс) — сесть на пол FP32 `~1e-7` и НЕ НИЖЕ. **Если полевые останутся на
`1e-15` — значит FP32 не применён там, где заявлено, и замер меряет старый код.**

**К73. ЗАДАЧА ПРОПУСКНО-ОГРАНИЧЕНА, И ВЕСЬ СЧЁТ «В ТЕРАФЛОПСАХ» МЕРИЛ НЕ ТО.**
Раздел «LOD проверен счётом» писал: «`1.16e14` операций; на GPU в 20 терафлопс это
`5.8 с`». Но арифметическая интенсивность одного обновления ячейки есть 561
операция на ~40 читаемых-пишемых чисел, то есть **`1.75` FLOP/байт при FP64 и
`3.5` при FP32**, тогда как баланс карты 5090-класса — около **`58` FLOP/байт**.
Упор в память с запасом в шестнадцать раз. **Считать надо ТРАФИК:**

    трафик за проход = N элементов × ND × байт на обновление
    (~40 чисел: 4 коэффициента с трёх верхних граней, три нижних, σ, источник, φ)

| конфигурация | элементов | FP64 | FP32 |
|---|---|---|---|
| 3 пикселя на элемент | `1.9e7` | **0.65 с** | **0.33 с** |
| + огрубление заэкранного `×6` | `3.1e6` | 107 мс | 53 мс |
| + переиспользование граней (KBA-блокировка, `×2.5`) | `3.1e6` | 43 мс | **21 мс** |
| то же на поколении `×1.5` по пропускной способности | | 29 мс | **14 мс** |

Кадр при проекции `3e6` элементов — меньше миллисекунды: это ровно та работа, под
которую GPU и сделан.

**СЛЕДСТВИЕ, КОТОРОЕ МЕНЯЕТ АРХИТЕКТУРНУЮ СТАВКУ.** Если решение стоит 20…50 мс,
то **камеронезависимость перестаёт быть нужна КАК АКТИВ**: поле можно
пересчитывать каждый кадр, и тогда поддерживаются движущийся свет и движущаяся
геометрия — то, чего нынешняя схема не умеет вовсе, и то, чем К46 подрубила
обещание `10³`. Актив не исчезает, но перестаёт быть единственным доводом.

**К74. РАЗВЁРТКА ЕСТЬ ВОЛНОВОЙ ФРОНТ, И НА МАЛОЙ СЦЕНЕ GPU УПИРАЕТСЯ В ЗАДЕРЖКУ, А
НЕ В ПАМЯТЬ. ЭТО ПЕРВЫЙ СЛУЧАЙ, КОГДА ИГРУШЕЧНАЯ КОНФИГУРАЦИЯ ВРЁТ В
ПЕССИМИСТИЧНУЮ СТОРОНУ.** Заведена по вопросу пользователя «комната с шарами при
том же размере ячеек — будет быстрее?».

Ответ — да, `1.93 с → ~2 мс` на карте 5090-класса, — но узкое место там другое:

| | нынешняя сцена `16³` | целевая, `1.9e7` элементов |
|---|---|---|
| трафик за проход | 21 МБ → `0.07` мс | 97 ГБ → `54` мс |
| арифметика за 6 проходов | `4.4e8` оп → `22` мкс | `1.3e11` оп |
| **волновых фронтов** (`3n−2`) | **46**, строго последовательны | ~800 |
| нитей в шаге (фронт × `ND`) | **6 тыс.** — карта пуста | `2.3e6` — карта полна |
| **что ограничивает** | **синхронизация: `46×6 = 276` точек** | **пропускная способность** |

**Следствие для замеров, и оно обратно всему предыдущему опыту проекта:** мерить
ускорение GPU на сцене `16³` нельзя, потому что оно ЗАНИЗИТ результат — при
`ND = 32` и 46 фронтах загрузка карты около трёх процентов. Все прежние промахи
игрушечной конфигурации (правило 6, К46, К71) были в оптимистичную сторону; этот —
в пессимистичную, и потому его легко принять за приговор методу.

**Штатное лечение известно и стандартно для `S_N`-кодов: конвейеризация KBA**
(Koch–Baker–Alcouffe) — группы ординат пускаются по фронтам со сдвигом, и конвейер
заполняется. В план не закладывается как решение, но записывается как названный
приём, чтобы «мало параллелизма в развёртке» не было прочитано как свойство метода.

**И ОТДЕЛЬНО, ЧТО ЭТИ 2 мс ДАЮТ НА САМОМ ДЕЛЕ.** Картинка при этом размере ячейки
остаётся блочной (`185×185` пикселей на ячейку, правило 6), и скорость на неверном
разрешении достижением не является. Ценность в другом: **отладочный цикл
становится интерактивным**, и находки этого аудита проверяются прогоном, а не
рассуждением.

### К79. ЦЕЛЕВАЯ МАШИНА ИЗМЕРЕНА 07-28, И ОДНО МОЁ ПРЕДСКАЗАНИЕ ОПРОВЕРГНУТО ЗАМЕРОМ

Всё выше в этом разделе — прикидки для абстрактных «16 ядер» и гипотетической
карты. Пользователь потребовал числа для СВОЕЙ машины, и первое же измерение
опровергло мою же оценку.

    CPU   Intel i9-9880H, 8 ядер / 16 потоков, 2.3…4.8 ГГц
          AVX2 + FMA, **AVX-512 НЕТ** (4 double либо 8 float на вектор)
          L3 16 МиБ, ОЗУ 125 ГБ
    GPU   Quadro RTX 3000 Mobile (TU106, Turing), **6 ГБ**, 336 ГБ/с
          FP32 5.3 Тфлопс, **FP64 = 1/32 → 166 Гфлопс**

**ИЗМЕРЕНО** (STREAM-triad, три массива по 384 МБ, инструмент в `/tmp/claude/`, в
репозиторий не вносился):

| потоков | 1 | 2 | 4 | 8 | 16 |
|---|---|---|---|---|---|
| ГБ/с | 19.4 | **22.7** | 22.9 | 22.6 | 22.1 |

**ПАМЯТЬ НАСЫЩАЕТСЯ НА ДВУХ ПОТОКАХ.** Отсюда баланс машины: `448` Гфлопс FP64
(8 ядер × AVX2 × 2 FMA) на `22.6` ГБ/с = **19.8 FLOP/байт**, тогда как
интенсивность нашего ядра `1.75`. **Значит и этот CPU пропускно-ограничен, с
запасом в одиннадцать раз.**

**Я ПРЕДСКАЗЫВАЛ ОБРАТНОЕ — что на CPU мы будем счётно-ограничены, — и ошибся;
опровергнуто замером, а не рассуждением.** Ошибка была в том, что я взял пиковые
флопсы и СПРАВОЧНУЮ пропускную способность DDR4-2666 (`41.6` ГБ/с), а машина даёт
`22.6`, то есть вдвое меньше. Ровно то, против чего заведено правило «эталон
стоимости до экстраполяции».

**И ЭТО ЗАКРЫВАЕТ К71 ИЗМЕРЕНИЕМ.** `×4.3` на 16 потоках было не «плохой
масштабируемостью из-за сведения накоплений», как записано в докладе 07-27, а
**упором в память**. Следствие практическое: **параллелизм сверх 2…4 потоков на
этой машине не даст ничего**, пока не поднята арифметическая интенсивность
блокировкой по кэшу — то есть той же KBA из К74. Векторизация AVX2 упрётся туда же.

**БЫСТРОДЕЙСТВИЕ НА ЭТОЙ МАШИНЕ** (трафик FP32; CPU по измеренным `22.6` ГБ/с,
GPU по `336` ГБ/с при КПД 70%):

| конфигурация | трафик | CPU 8 ядер + AVX2 | GPU RTX 3000, FP32 |
|---|---|---|---|
| план как записан, `1.37e8` эл. | 33 ТБ | **49 мин** | 140 с — **не влезает в 6 ГБ** |
| 3 пикселя на элемент, `1.9e7` | 4.6 ТБ | 3.4 мин | 20 с — **не влезает** |
| + заэкранное `×6`, `3.1e6` | 0.75 ТБ | 33 с | 3.2 с |
| + Крылов, 6 проходов | 95 ГБ | 4.2 с | 0.40 с |
| **+ К77 (Р6 `÷8`, Р2 `÷8`, DG `÷2`)** | 6.2 ГБ | **0.28 с** | **26 мс** |

**ЧАСОВ НА ЭТОЙ МАШИНЕ НЕ ВОЗНИКАЕТ НИГДЕ.** «19 часов» из раздела «LOD проверен
счётом» есть `1.16e14` операций, делённые на ОДНО СКАЛЯРНОЕ ЯДРО. Использование
машины целиком превращает их в 49 минут, а рычаги, УЖЕ ЗАПИСАННЫЕ в этом файле
(Крылов, Р6, Р2), — в доли секунды.

**ТРИ ЖЁСТКИХ ВЫВОДА ИМЕННО ПРО ЭТО ЖЕЛЕЗО.**

1. **6 ГБ VRAM ограничивают `N ≲ 4…6·10⁶` элементов.** Считая ~700 байт на
   элемент (моменты Р1, три матрицы масс на грань, геометрия реза), `3.1e6`
   занимают `2.2` ГБ и влезают впритык, `1.9e7` требуют `13` ГБ и не влезают.
   **Конфигурация «3 пикселя + огрубление заэкранного» здесь не оптимизация, а
   ТРЕБОВАНИЕ ЖЕЛЕЗА**;
2. **на GPU обязателен FP32.** Turing даёт FP64 как `1/32`; в двойной точности
   задача становится счётно-ограниченной и медленнее в разы. К72 на этой машине —
   не «решить до A», а условие того, что GPU вообще имеет смысл;
3. **идти на GPU стоит из-за ПРОПУСКНОЙ СПОСОБНОСТИ, а не из-за флопсов:**
   `336` против измеренных `22.6` ГБ/с — `×15`, и это ровно тот ресурс, в который
   мы упираемся.

**ЧТО ДАСТ УСКОРЕНИЕ ЗДЕСЬ, А ЧТО НЕТ.** Дадут размерные рычаги К77 (Р6 и Р2 —
оба уже написаны и не попали в этапы), Крылов и `ε` = 3 пикселя: вместе `×750` по
трафику, без GPU и без векторизации. Даст перенос на GPU — `×15`, но только с FP32
и только при `N ≲ 5e6`. **Почти не дадут параллелизм по ядрам и AVX2** — упрутся в
измеренные `22.6` ГБ/с.

**И К78 ЗДЕСЬ ЖЕ, В ЧИСЛАХ ЭТОЙ МАШИНЫ:** после сокращений узкое место переезжает
в проход 1 (прямой свет), которого ни в одной таблице цены нет — `3.1e11` операций
есть **8 с на CPU и 0.3 с на GPU**, то есть он становится главным. Лечение
записано: кэш облучённости, `×10…×100`.

**И ЧЕСТНАЯ ДРУГАЯ СТОРОНА, БЕЗ КОТОРОЙ ТАБЛИЦА ВЫШЕ — САМООБМАН.** На той же
карте трассировщик пути с аппаратными RT-ядрами и шумоподавителем считает
динамическое глобальное освещение на 30…60 кадрах. **По скорости выходит паритет,
а не победа.** Отличать линию будут ровно те три вещи, что записаны в «честной
оговорке»: отсутствие шума ПО ПОСТРОЕНИЮ, независимость поля от камеры и общая
иерархия с волновой линией. Ни одна из них не является скоростью, и подменять их
терафлопсами нельзя.

**B. КРЫЛОВ С РАЗВЁРТКОЙ КАК ПРЕДОБУСЛАВЛИВАТЕЛЕМ.**
Дёшево, ни от чего не зависит, снимает чувствительность к альбедо. Измерено:
голый Крылов держит **6 проходов при `σ_t·h` от 0.05 до 4.0**, тогда как ряд
Неймана идёт `13 → 227`.
**~~С развёрткой в предобуславливателе будет лучше.~~ СНЯТО ПО К65:** в том
замере развёртка была САМИМ ОПЕРАТОРОМ (`A·v = v − S(v) + b`), а не
предобуславливателем, и что именно станет `M⁻¹`, не сказано нигде. Обещание без
объекта.
*Препятствие, уже названное:* ограничитель положительности НЕЛИНЕЕН, и на сценах
с отражением он активен на сходимости (458 срезок). Нужен либо внешний
нелинейный цикл поверх линейного Крылова, либо ограничитель, применяемый только
к итогу.
**К76. ВСЕ ЗАМЕРЫ СХОДИМОСТИ СНЯТЫ ОТ НУЛЯ, И ТЁПЛЫЙ СТАРТ НЕ ИЗМЕРЕН НИ РАЗУ —
А ИМЕННО ОН РЕШАЕТ, «НА ГРАНИ» ЛИ МЕТОД.** Заведена по замечанию пользователя
(«FP32 — ну вот, на грани»).

`50 мс` из таблицы цены есть ОПТИМИСТИЧНЫЙ край: в строке сложены четыре
неизмеренных множителя (FP32, заэкранное `×6`, Крылов `47→6`, KBA `×2.5`), и
снятие любого одного выводит за сотню миллисекунд. **Но само «на грани» относится
ТОЛЬКО к случаю, когда решать надо каждый кадр:** при неподвижных свете и
геометрии `50 мс` платятся один раз, а кадр стоит меньше миллисекунды.

**А для подвижного света весь вопрос в том, сколько проходов нужно от ПРОШЛОГО
КАДРА, и этого никто не мерил.** 47 итераций холодного старта и 6 проходов
Крылова — все от нуля. Между соседними кадрами поле меняется на проценты. Поле
`warm_start` в `tr3_problem` уже существует (заведено для построения крыловского
оператора), то есть машинерия на месте, а замера нет.

**Фальсификатор, стоящий один день:** сдвинуть источник на малую величину и
померить число проходов до той же невязки, начиная с поля прошлого кадра.
**Предсказание до прогона:** число проходов падает как `log(δ)/log(0.6)` от
величины возмущения — при возмущении в 10% это `2…3` прохода вместо 6, при 1% —
`1…2`. **Негативный контроль:** стартовать с поля от ДРУГОЙ сцены (или с нуля) при
том же возмущении ⇒ число проходов обязано вернуться к 6. Если не вернулось —
тёплый старт не применяется, и замер меряет что-то другое.
*Если предсказание сбудется, кадр с движущимся светом стоит `8…17 мс`, и «на
грани» становится 60 кадрами.* Это единственный оставшийся рычаг, лежащий ровно на
той границе, и он дешевле всех прочих.

*Чего не хватало и что стоит копейки (К65):* шесть проходов сняты **на печи с
ВЫКЛЮЧЕННЫМ ограничителем и на СЛУЧАЙНОЙ правой части**, то есть в условиях,
которых на рабочей сцене нет. Решающий опыт — выключить ограничитель НА РЕНДЕРНОЙ
сцене. **Предсказание до прогона:** решение уходит в минус (в T2 без ограничителя
было `−0.39·L_b` на 2584 ячейках) и ответ меняется на величину, различимую в
буфере радианса. **Если НЕ уходит — препятствие B мнимое, внешний нелинейный
цикл не нужен, и этап дешевеет вдвое.** Обратный исход тоже полезен: он даёт
масштаб нелинейности, по которому выбирается схема внешнего цикла.

**C. ПЛОСКИЕ ЗЕРКАЛА ВНУТРЬ СИСТЕМЫ — ДЛЯ ЭНЕРГИИ, А НЕ ДЛЯ ОБРАЗА (правлено К56).**
Углового базиса НЕ ТРЕБУЕТ: набор ординат уже замкнут относительно ДЕВЯТИ
плоскостей (трёх осевых и шести диагональных, октаэдрическая группа), промах
`≤6.7e-16`, связь есть точная ПЕРЕСТАНОВКА. Нужно: направленное хранимое на
зеркальной грани (`ND` чисел вместо одного, `1.6 МБ` на комнату — цифра снята с
нынешней игрушечной сцены и под LOD растёт вместе с числом зеркальных элементов)
и связь `m → m′` вместо ламбертовой свёртки.
**ЧТО ЭТАП ДАЁТ И ЧЕГО НЕ ДАЁТ (К56).** Даёт ЭНЕРГИЮ: сегодня для развёртки
зеркало есть чёрное тело, свет на нём пропадает (измерено: поглощено `654`
против `277` у диффузных шаров), и комната выходит темнее физической. НЕ даёт
ОБРАЗА: по правилу 1 этого же раздела зеркалу нужно `(1/ε)²` направлений, а `ND`
их соответствует лепестку `~2π/ND`, то есть глянцу в десяток градусов. Сегодня
образ считается прослеживанием и ТОЧЕН (Ф1 побитово, `ρ^k` до `2.0e-16`).
**Значит прослеживание в сборе ОСТАЁТСЯ, а C добавляется к нему.** Это то же
разделение, что у прямого света: резкие концы пути считаются вне сетки
направлений, гладкая середина — на ординатах.
*Требует B:* два зеркала друг напротив друга дают ЦИКЛ, ряд Неймана на нём
ползёт как `(ρ₁ρ₂)ⁿ`.
*Требует правки прибора (К57):* тождество `pin + psout = pout + pabs + psin`
верно сегодня ровно потому, что перехваченное зеркалом уходит в `psin` и не
возвращается. После C зеркальная грань отдаёт обратно по перестановке, и не
внести этот путь в `psout` значит получить ЗАКОННУЮ невязку — то есть вернуть
прибор в состояние до К40, где `1.66e2` при пропускной способности `1.0e4` было
законно и потому слепо.
*Фальсификаторы, ПРАВЛЕНЫ по К55 — прежний был слеп:*
1. **ответ внутрисистемного зеркала против ПРОСЛЕЖИВАНИЯ** на той же сцене при
   большом пределе отскоков (Ф5 показала полку `5.1e-3 → 0`), поле
   **НЕОДНОРОДНОЕ**. Это единственная проверка, которая видит саму перестановку;
2. **печь для зеркал с РАЗНЫМИ радиансами стенок** (лечение К44 дословно): в
   однородном поле любая перестановка, включая тождественную и заведомо неверную,
   даёт то же `L_e/(1−ρ)`;
3. энергия: поглощённое зеркальными шарами обязано УПАСТЬ с `654` к значению
   диффузных при том же `ρ`, а комната — посветлеть.
*Негативный контроль:* подменить перестановку `m → m′` тождественной ⇒ расхождение
с прослеживанием обязано быть `O(1)`. **Если не обязано — проверка пуста, и это
шестой за проект случай метрики, слепой к тому, что она стережёт.**
**«Ответ не зависит от предела отскоков, его больше нет» — НЕ ФАЛЬСИФИКАТОР, а
пересказ изменения:** параметра нет, значит и свипа по нему нет.

**D. УГЛОВОЙ БАЗИС ВМЕСТО КВАДРАТУРЫ — САМОЕ КРУПНОЕ И САМОЕ НЕОПРЕДЕЛЁННОЕ.**
Даёт: адаптивность по углу, исчезновение лучевого эффекта, снятие запрета К3 на
интерполяцию, ленточные (а не плотные) угловые блоки, и — главное — КРИВЫЕ
ЗЕРКАЛА через локальные наборы с почти тождественной передачей между соседними
гранями.
*Цена, названная заранее:* дискретные ординаты суть ДИАГОНАЛИЗОВАННЫЙ угловой
базис, и с перекрывающимися носителями одновременной диагонализации нет —
**развёртка как прямой решатель `O(N)` исчезает**, а с ней и Р1 (угловое поле
перестаёт быть транзиентным, полный вектор надо держать). Значит предобуславливатель
из B тоже исчезает и нужен другой: многосеточный по пространству и углу.
*Не идти сюда, пока не сделаны A, B, C* — иначе нечем будет измерить, стало
лучше или хуже.
*Эталон T3 обязан получить НОВОЕ предсказание до прогона (К58):* `O(ND⁻²)` есть
свойство ГАУССОВОЙ квадратуры, у базиса с ограниченным носителем порядок свой.
Требовать `ND⁻²` от D значит завести красный тест на исправном коде — пятый
генератор ложной тревоги после К1, К6, К10 и К18.

**E. ЦВЕТ.** Три канала, независимые задачи. Цена по профилю `×1.5…×1.6` по
холодному старту (геометрия и марш общие), `×3` по памяти хранимого — **и это
число сам файл объявил неустановленным до замера; здесь оно процитировано без
оговорки, класс Г19.**
~~Ставится в любой момент после A.~~ **ПОПРАВЛЕНО К64: E ставится ДО A.**
Сильнейший записанный фальсификатор цвета — «три ОДИНАКОВЫХ канала обязаны дать
буферы, ПОБИТОВО равные прежнему одноканальному прогону» — опирается на
неизменную конфигурацию, а A меняет её целиком. После A эталона для него не
останется, и проверка сведётся к «похоже». Порядок 07-28 («по ущербу картинке»)
ставил цвет пятым пунктом, до всякого LOD; два порядка в одном файле
расходились.

**F. ВАЖНОСТЬ КАК ИНСТРУМЕНТ ОТСЕЧЕНИЯ — ПОСЛЕ БАЗЫ (решение пользователя 07-28).**
Восьмая форма (`I = ⟨W_e, L⟩ = ⟨W, L_e⟩`) возвращается в план ТРЕТЬЕЙ ролью, и её
надо отличать от двух прежних: как ДВИЖУЩАЯ СИЛА LOD она забракована (правило
`L = εR` бесплатно, важность требует сопряжённого солва ради выбора сетки), как
ИЗМЕРИТЕЛЬНАЯ — принята. Теперь третья: **отсечение малозначащего после того, как
база считается верно.** Прежние решения этим не отменяются.

*Заявленный множитель:* «отсекая более 90%, это более чем в 10 раз».

### К75. АУДИТ ЭТАПА F ДО КОДА — ЧЕТЫРЕ ПОПРАВКИ, И ГЛАВНЫЙ ВЫИГРЫШ НЕ НА ТОЙ ОСИ

**1. `×10` ЧАСТИЧНО ДВОЙНОЙ СЧЁТ.** Строка «+ заэкранное `×6`» в таблице цены уже
применена — это огрубление диффузно достижимого по ГЕОМЕТРИЧЕСКОМУ правилу
(лепесток × длина пути), с оценённым потолком `×11`. Важность даёт **тот же
множитель вычисленным, а не угаданным**, то есть подбирается к потолку, а не
умножается на него. **Сверх геометрического правила она даёт ровно одно, зато
ценное: ЗАСЛОНЁННОСТЬ.** Геометрия не знает, что стена за закрытой дверью в кадр
не светит; `W` знает. Этот остаток не оценён ничем и в доме с комнатами может быть
велик — но это предмет замера, а не назначения.

**2. «ОТСЕЧЬ» ПРОТИВ «ОГРУБИТЬ» — ЗДЕСЬ ДЕЙСТВУЕТ СОБСТВЕННОЕ ПРАВИЛО 07-28.**
«Грубить можно ПРЕДСТАВЛЕНИЕ, но не ВКЛАД; заэкранное не выбрасывается никогда,
только грубеет.» Отсечение по важности законно ТОЛЬКО как порог на ВКЛАД, и тогда
это агрегация, а не удаление. Разница не словесная: наполовину затенённая яркая
стена, схлопнутая в один элемент, даст верную суммарную энергию и неверную
картинку, а ВЫБРОШЕННАЯ — тихо потеряет свет, и **ничто в изображении об этом не
скажет**. Правдоподобный неверный результат, худший класс в этом проекте.

**3. ГЛАВНЫЙ ВЫИГРЫШ НА ДРУГОЙ ОСИ — УГЛОВОЙ, И ОН БОЛЬШЕ ПРОСТРАНСТВЕННОГО.**
Трафик есть `N × ND × байт` (К73), и `ND` стоит в нём наравне с `N`. Важность
сосредоточена по углу там, куда смотрит сенсор, значит она даёт критерий для
**переменного числа ординат ПО ПОЛОЖЕНИЮ**. Этот приём помечен в плане
возможностью и отвергнут словами «плохо дружит с развёрткой: порядок обхода и
передача потока между областями с разными наборами направлений». **С важностью у
него появляется основание, которого не было.** `32 → 8` в глубине объёма есть
`×4` и по трафику, и по счёту — сопоставимо со всем пространственным отсечением, и
на оси, которую никто не трогал.

**4. `W` КАМЕРОЗАВИСИМА, И ОТСЕЧЕНИЕ УГЛУБЛЯЕТ КАМЕРНУЮ ПРИВЯЗКУ.** LOD огрубляет
— при движении камеры элемент меняет уровень; отсечение УДАЛЯЕТ — при движении
камеры удалённое надо восстанавливать, а это расстановка, а не сбор. К46 уже
предупредила, что камерно-привязанный LOD вернёт часть расстановки в кадр;
отсечение по важности делает это резче, и цену надо назвать заранее.

*Цена самого инструмента:* сопряжённая задача есть ПОЛНЫЙ солв обращёнными
направлениями. На грубой сетке это проценты, но её надо поставить в бюджет явно —
иначе повторится С3 (прямой свет от площадного источника, которого в модели
стоимости просто не было).

*ФАЛЬСИФИКАТОРЫ, ДО КОДА:*
1. **взаимность `⟨W_e, L⟩ = ⟨W, L_e⟩` до `1e-14`** — она проверяет ОБА решателя
   разом и разными путями, и такой проверки у линии нет ни одной;
2. картинка после отсечения против картинки без отсечения: расхождение обязано
   быть НИЖЕ порога, по которому отсекали, — то есть порог обязан быть предсказанием,
   а не настройкой;
3. **требование к СЦЕНЕ, иначе это К44 заново:** сцена обязана содержать сильный
   КОСВЕННЫЙ вклад — ярко освещённую стену за спиной камеры, свет из дверного
   проёма. На сцене, где всё видно напрямую, отсечение по важности пройдёт при
   ЛЮБОЙ реализации, включая неверную.
*НЕГАТИВНЫЙ КОНТРОЛЬ:* отсечь по важности от ДРУГОЙ камеры либо по равномерной
`W` ⇒ картинка обязана сломаться на `O(1)`. Если не сломалась — либо отсечение не
работает, либо метрика слепа к тому, что стережёт.

### К77. БУХГАЛТЕРИЯ СОКРАЩЕНИЯ РАЗМЕРА: ДВА КРУПНЕЙШИХ РЫЧАГА УЖЕ НАПИСАНЫ В ЭТОМ ФАЙЛЕ И НЕ ПОПАЛИ НИ В ОДИН ЭТАП

Внесено 07-28 по запросу пользователя («как ещё уменьшить систему и слегка её
разрядить»). Размер есть `N элементов × ND направлений × 4 коэффициента DG1` —
**три сомножителя, а трогали только первый.** Таблица ведётся с пометкой
пересечений, потому что К75 показала, как легко множители удваиваются:

| рычаг | действует на | множитель | посчитан? | пересекается с |
|---|---|---|---|---|
| `ε` = 3 пикселя (К68) | `N` | `÷7.0` | **да** | — |
| огрубление заэкранного | `N` | `÷6` | **да** | Р6, F |
| **Р6: раздельные сетки прямой и косвенной** | `N` косвенной | **`÷8` осторожно, до `÷64`** | **НЕТ** | заэкранное |
| **Р2/`P1`: 32 ординаты → 4 момента** | `ND` | **`÷8`** (`÷5` в анизотропной, К4) | **НЕТ** | D, угловая часть F |
| **DG1 → DG0 в гладком ОБЪЁМЕ** | `4 → 1…2` | **`÷2…÷3`** | **НЕТ** | — |
| важность (этап F) | `N` | к потолку `×11` плюс заслонённость | частично | заэкранное |
| источник ВТОРОГО столкновения | `ND` и `N` | не оценён | **НЕТ** | — |

**Два крупнейших написаны в этом же файле годом раньше этапов и в A…F не попали.**
**Р6** («прямая и косвенная должны жить на РАЗНЫХ сетках») даёт косвенной части
сетку в 4–8 раз грубее по оси — а развёртка И ЕСТЬ косвенная часть, прямой свет
считается аналитически вне ординат. **Р2** (`P1` для ВСЕЙ косвенной части, а не
только при `τ≫1`) даёт `÷8` по угловой оси; К61 запретила цитировать оттуда
множитель `×70` по РАБОТЕ, но `÷8` по РАЗМЕРУ к тому спору отношения не имеет.

**Третий нигде не записан: DG1 → DG0 в объёме.** Наклоны есть 3 из 4 неизвестных,
и в гладкой середине они не нужны. **Оговорка жёсткая: на ПОВЕРХНОСТЯХ DG1
остаётся** — К39 измерила, что хранение константой давало блоки на всех 1536
гранях из 1536. Фальсификатор: индикатор наклона против ошибки схемы; предсказание
— доля ячеек, где наклон ниже ошибки DG1, и она обязана расти с удалением от
кромок тени.

**Вместе, осторожно: `4.0e8 → 3.1e6` неизвестных, то есть `÷128`.**

**РАЗРЕЖАТЬ ЖЕ ПОЧТИ НЕЧЕГО, И ЭТО НАДО ЗАПИСАТЬ, ЧТОБЫ НЕ «ОПТИМИЗИРОВАЛИ»
СДЕЛАННОЕ.** Матрица развёртки не формируется вовсе и треугольна в порядке обхода
(правило 3). Угловой блок при изотропном рассеянии **уже ранга один**, и это в
коде используется. Настоящая разрядка бывает двух видов, и оба уже суть этапы:
сокращение МНОЖЕСТВА неизвестных (F) и угловой базис с ограниченным носителем (D),
причём D убивает развёртку как прямой решатель.
**ЛОВУШКА, НАЗВАННАЯ ЗАРАНЕЕ:** асинхронное или частичное обновление (не все
ячейки каждую итерацию) выглядит дешёвой разрядкой и **ломает тождество К13**,
которое верно на ЛЮБОЙ итерации и потому служит ПРИБОРОМ. Прибор ради процентов не
меняют.

### К78. ВСЕ ТАБЛИЦЫ ЦЕНЫ СЧИТАЮТ ТОЛЬКО ПРОХОД 2; ПОСЛЕ СОКРАЩЕНИЯ УЗКОЕ МЕСТО ПЕРЕЕЗЖАЕТ В ПРОХОД 1, КОТОРОГО В НИХ НЕТ

Формула `N × ND × итераций × 561`, по которой посчитаны обе таблицы цены, есть
цена РАЗВЁРТКИ. **Прохода 1 — прямой свет маршем по лучу с накоплением `τ` от
каждого источника — в них нет вовсе**, а проход 3 (сбор) вынесен отдельно.

При сокращениях К77 развёртка стоит около МИЛЛИСЕКУНДЫ, и тогда:

    проход 1 на мелкой сетке: 3.1e6 элементов × 100 выборок площадного источника
                              × ~50 ячеек вдоль луча × ~20 операций  ≈  3.1e11

то есть **больше сокращённой развёртки и примерно равно всей нынешней строке
`3.4e11`.** Дальнейшее сокращение прохода 2 после этого бессмысленно.

**И это ровно то, о чём С3 и С4 предупреждали ЗАДОЛГО ДО КОДА:** «`O(элементы ×
источники × выборки)`… сопоставимо со ВСЕМ кадровым бюджетом» и «в мутной среде
это не запрос видимости, а МАРШ по лучу… значит `10⁸` занижены на
порядок-полтора». Находки записаны, а в модель цены так и не внесены — **класс
Г19: поправка есть в тексте и отсутствует в числе.**

**Лечение записано там же:** кэш облучённости — считать прямую освещённость на
более грубом множестве с интерполяцией и РАЗРЕЗАМИ на границах теней, `×10…×100`.
Это тот же приём Р4/Р6 («на границе однородности ячейка режется»), применённый к
проходу 1. **Фальсификатор:** ошибка кэша обязана быть `O(h²)` вне кромок и `O(1)`
на кромках — тот же наклон, что различает «дробить» и «резать» в К68.

**К69. ПЛАН ПЕРЕХОДА НЕ УПОМИНАЕТ T7 ВООБЩЕ, А «ДОМ С ДЕРЕВОМ» УПИРАЕТСЯ НЕ В
ЦЕНУ, А В ОТСУТСТВУЮЩЕЕ ПРЕДСТАВЛЕНИЕ.** Заведена по возражению пользователя
(«захочу дом с деревом — и приплыли»). Возражение верное, но причина не та, на
которую оно указывает.

**Счёт элементов дереву не мешает, и это ИЗМЕРЕНО.** П2: сцена выросла в 262 144
раза по объёму, элементов стало больше в 26; прибавка на октаву постоянна. Дерево
дальше нескольких метров есть подпиксельная структура, то есть среда или
поверхность с откликом, а не миллион листьев. Арифметика выдерживает.

**Ломается представление, и трёх кусков просто нет:**

1. **объединение тел в резе** — материал есть ПЕРЕСЕЧЕНИЕ полуплоскостей, ОДНО
   выпуклое тело на ячейку. Крона ломает это в первой же ячейке; сегодня
   `render3` при встрече второго тела печатает предупреждение и молча его
   выбрасывает. Стоит в списке открытого, но как хвост, а не как предусловие;
2. **гомогенизация со сгущением** (clumping) — раздел «гомогенизация не есть
   усреднение» в этом файле есть, кода нет ни строки. **И она провалилась между
   двумя файлами:** `PLAN_CUT.md` Р-5д сознательно её не пишет («это машинерия
   ПЕРЕНОСА и живёт в его плане»), а в плане переноса её тоже нет;
3. **критерий переключения по ПРОСВЕТУ, а не по листу** — записан в разборе
   полога («просветы крупнее листьев»), в коде отсутствует. Без него крона выйдет
   ровной зелёной массой без просветов неба, потому что RTE корреляционных
   эффектов не даёт.

**Все три — предусловия вехи T7 («сцена с травой и деревом»), и план A…E молчит и
про них, и про саму T7.** То есть план описывает, КАК переделать машинерию, и не
говорит, ради какой сцены. Либо T7 входит в план отдельным этапом с этими тремя
предусловиями, либо надо честно написать, что целевая сцена перехода — комната,
а не город и не растительность, — и тогда `CLAUDE.md` («target scene is a city»)
и этот план расходятся, и расхождение надо назвать.

### ДВЕ ПОПРАВКИ К СПИСКУ «ЧТО НЕ ТРОГАЕТСЯ» — К58 И К59

**К58. ЭТАЛОНЫ ДЕЛЯТСЯ НА ТРИ РАЗРЯДА, И СТРОКА «ВСЕ ОБЯЗАНЫ ДЕРЖАТЬСЯ» СКЛЕИВАЛА
ИХ В ОДИН.** Как написано, она даёт ложную уверенность там, где нужнее всего, и
ложную тревогу там, где код будет исправен.

| эталон | разряд | владелец |
|---|---|---|
| К27 против аналитического пересечения | действует как есть | — |
| печь `1e-15`, полость `L_e/(1−ρ)`, печь для зеркал | **держится, но СЛЕП**: точное решение однородно (К12, К13, К44) | — |
| линейное поле с наклонами, тождество `pin+psout=pout+pabs+psin` | **нужна НОВАЯ КОНФИГУРАЦИЯ**: проверены на ТРЁХ уровнях сетки, A даёт двадцать | A |
| тождество баланса | **нужна ПРАВКА ПРИБОРА**: путь `psout` у зеркала (К57) | C |
| H-функция `O(ND⁻²)` | **нужен НОВЫЙ ПРЕДСКАЗАННЫЙ ЗАКОН**: `ND⁻²` есть свойство гауссовой квадратуры | D |

Первая строка второго разряда — та самая, из-за которой строка «страховка
перехода» опасна: три из шести перечисленных эталонов имеют ОДНОРОДНОЕ точное
решение, а значит по К44 проверяют только те степени свободы, которые в
однородности участвуют. Страховать ими переход, меняющий сетку, угловое
представление и сбор, нельзя.

**К59. A ОТМЕНЯЕТ К15, А НЕ СОХРАНЯЕТ ЕГО.** «Проекция элементов, растеризация» —
это растеризация ПОВЕРХНОСТНЫХ ЭЛЕМЕНТОВ, то есть кусков ФАСЕТОВ, то есть
многогранника. Силуэт сферы тогда есть силуэт многогранника, и его ошибка равна
ровно тому, что T5а измеряла НЕГАТИВНЫМ КОНТРОЛЕМ: `max|Δt|` = `0.42` при `k = 0`
и `0.26` при `k = 1`, порядка `dmax`. Против `0.0` побитово у примитива на
169/169 лучах. **То есть рабочим режимом становится то, что было контролем.**

К50 называет три согласованных выхода и заканчивается словами «сегодня не выбрано
ничего». **A выбирает первый — всё есть многогранник — и делает это молча.**
Выбор законный: он снимает К50 целиком (сбор и развёртка начинают считать ОДНО
тело, огранка на диффузных шарах исчезает как рассогласование). Но его надо
назвать, и назвать вместе с ценой, иначе вернувшаяся огранка силуэта будет
прочитана как дефект новой машинерии.

*Развилка, которую надо пройти ДО кода:* либо (i) принять многогранник и
привязать `k` фасетизации к `εR` (тогда огранка уходит под пиксель по построению,
и это ровно шов Г34, который в `PLAN_CUT.md` сознательно не решался), либо (ii)
растеризовать ПРИМИТИВ — аналитический силуэт конического сечения, — и это
отдельная неназванная работа со своим аудитом.
*Фальсификатор, различающий их одним прогоном:* ошибка силуэта обязана ПАДАТЬ с
уровнем фасетизации `k`, если растеризуется многогранник, и НЕ ЗАВИСЕТЬ от `k`,
если примитив. Метрика уже есть — доля кромочных пикселей, сегодня 6.5% кадра.

### КОЛИЧЕСТВЕННЫЕ ПРАВИЛА, ВЫВЕДЕННЫЕ 07-28 — БЕЗ НИХ ПЛАН НЕ ЧИТАЕТСЯ

**1. ОБЪЁМ ХРАНИМОГО НА ЭЛЕМЕНТЕ ЕСТЬ `(ширина лепестка)⁻²`.**

    диффузный   лепесток ~1 рад  ->  1 число
    глянцевый   лепесток θ       ->  (1/θ)²   — десятки, сотни
    зеркальный  лепесток 0       ->  2π/ε² = 1.4e7 на элемент   (правлено К62)

**К62, АРИФМЕТИКА.** Написано было `(1/ε)² = 2.7e7`, и это не равно само себе:
`(1/6.77e-4)² = 2.2e6`, а `2.7e7` есть `4π/ε²` — число из раздела «что
выигрывается огрублением заэкранного», то есть ПОЛНАЯ сфера. Хранимому
исходящему нужна ПОЛУСФЕРА, `2π/ε² = 1.4e7`. Вывод (зеркало по всем направлениям
не хранится) не меняется, но число было выдано за посчитанное по написанной
формуле и ей не равнялось. Та же строка правил задаёт множители у глянца, и там
`2π` пропущено так же.

У зеркала `L_out(x,ω) = ρ·L_in(x, отражённое ω)`, то есть хранить исходящий по
всем направлениям значит хранить ВСЁ входящее угловое поле в точке — ровно то,
что Р1 объявил транзиентным.

**СЛЕДСТВИЕ, И ОНО АРХИТЕКТУРНОЕ: зеркала ломают КАМЕРОНЕЗАВИСИМОСТЬ, и только
её.** Чтение хранимого у зеркала ничем не отличается от диффузного — «есть поле,
есть направление». Но диффузное хранимое годится любому наблюдателю (одно число),
а зеркальное либо хранится по всем направлениям (невозможно), либо только для
ЭТОЙ камеры (и тогда пересчитывается при её движении). Главный актив линии —
«поворот камеры стоит только сбора» — на зеркала не распространяется.
Закон при этом НЕПРЕРЫВЕН: глянец переходит от диффузного к зеркальному без
скачка, отдельного случая заводить не надо.

**2. ЦЕНА `P1` ПРОТИВ ОРДИНАТ — `×70` НА ИТЕРАЦИЮ.**

    одна итерация по рассеянию:  32 направления × 561 операция = 17 950 на ячейку
    одно умножение на P1-матрицу: 7 ячеек стенсиля × блок 4×4  ≈     250 на ячейку

~~Четыре угловых коэффициента — не рост против скаляра радиосити, а СОКРАЩЕНИЕ
в восемь раз против нынешних 32 ординат.~~ **СНЯТО ПО К67, см. ниже: против
скаляра радиосити это РОСТ ВЧЕТВЕРО, и умолчание базы сравнения здесь давало
неверный ответ на прямой вопрос «станет ли система меньше».**
Вопрос целиком сводится к числу
итераций. Многосеточный `V`-цикл стоит **2…3 матвека**, а не 70 (работа по
уровням складывается в ряд `1 + 1/8 + 1/64 + …`), поэтому преимущество падает до
`×24`, но не исчезает, и число итераций перестаёт расти с размером сетки.

**К61. ЭТО СРАВНЕНИЕ РАЗНОРОДНОГО, И ОТНОСИТСЯ ОНО К ЭТАПУ, КОТОРОГО В ПЛАНЕ
НЕТ.** Три поправки, каждая своя:

1. **Слева стоит ТОЧНОЕ ОБРАЩЕНИЕ, справа — ОДИН матвек.** Правило 3 ниже
   говорит это буквально: проход развёртки есть применение обратного оператора.
   Сравнивать надо полное решение с полным: `6` проходов Крылова (измерено в B)
   × 17 950 = `1.1e5` операций на ячейку против `~10` `V`-циклов × 2…3 матвека
   × 250 ≈ `6e3`, то есть **`×18`, а не `×70` и не `×24`**;
2. **`P1` — ДРУГОЕ уравнение, а не другая реализация того же.** Оно даёт другой
   ответ, и К4 уже запретила его в анизотропной среде (полог канализирует свет,
   коэффициент диффузии тензорный). Ускорение между разными ответами не
   цитируется без метрики точности;
3. **`P1` не входит НИ В ОДИН из этапов A…E.** Крупный множитель, приписанный
   работе вне плана, — ровно то, что в шаге 1 назначило порядок работ по
   принадлежавшему не тому пункту `×130`. Либо `P1` становится этапом со своим
   местом в зависимостях, либо правило переезжает в «возможности».

**К67. «НЕ РОСТ ПРОТИВ СКАЛЯРА РАДИОСИТИ» — НЕПРАВДА, И БАЗУ СРАВНЕНИЯ НАДО
НАЗЫВАТЬ КАЖДЫЙ РАЗ.** Заведена по возражению пользователя, и возражение верное:
против одного скаляра радиосити четыре коэффициента `P1` есть **рост вчетверо**.
Написать «не рост» значит выбрать себе удобную базу (32 ординаты) и умолчать про
неудобную.

**Почему база «радиосити» всё-таки неприменима — но по ДРУГОЙ причине, чем
написано.** Тот единственный скаляр куплен ПЛОТНОЙ матрицей форм-факторов, и
каталог постановок этого же файла говорит про развилку прямо: «либо угол выброшен
и матрица плотная, либо матрица разрежена и угол — измерение; обоих сразу не
бывает». Значит сравнивать надо не числа на ячейку, а ПОЛНУЮ РАБОТУ, и тогда
`P1` выигрывает у радиосити не количеством чисел, а разреженностью.

**И ВТОРАЯ ПОЛОВИНА НАХОДКИ, БОЛЕЕ ВАЖНАЯ: СОКРАЩЕНИЕ ПО УГЛУ И РОСТ ПО
ПРОСТРАНСТВУ — РАЗНЫЕ ОСИ, И НАЗЫВАТЬ ОДНУ БЕЗ ДРУГОЙ НЕЛЬЗЯ.** На вопрос
«станет ли система меньше» этот раздел до сих пор отвечал угловой осью, а
пространственную (этап A) не приводил вовсе:

| ось | сегодня (16³, 32 ординаты) | после A | после A + `P1` |
|---|---|---|---|
| элементов (пространство) | 4 096 | `1.37e8` | `1.37e8` |
| чисел на элемент (угол × DG1) | 32×4 = **128** | 128 | 4×4 = **16** |
| неизвестных в проходе | `5.2e5` | `1.75e10` | `2.2e9` |

**Угол `×8` вниз, пространство `×33 000` вверх, итог `×4 200` ВВЕРХ.** Оба
утверждения по отдельности верны, вместе — вводят в заблуждение. Это тот же класс,
что К51 (величина верна в своём контексте и перенесена в чужой), только здесь
чужим контекстом оказался ВОПРОС, а не число. **Правило: всякий раз, когда
называется сокращение, называется и ось, по которой оно идёт, и что при этом
происходит с остальными.**

**ОГОВОРКА К СТОЛБЦУ «ПОСЛЕ A».** `1.37e8` — счёт по `L = εR`, то есть без
огрубления заэкранного, которое собранное правило LOD допускает (лепесток порядка
радиана у диффузно достижимого ⇒ `L ≈ R`). Раздел «что выигрывается огрублением
заэкранного» оценивает этот запас как потолок `×11`, реально `×5…×8`, и он НЕ
ПОСТРОЕН. То есть под полным правилом столбец даёт `~1.2e7…2.7e7`, а не `1.37e8`.
**Но рост при этом остаётся `×2900…×6600` против нынешних 4096, и списывать его
на «не то правило» нельзя: это цена РАЗРЕШЁННОЙ картинки, а нынешние 4096
недоразрешены на четыре-пять порядков (правило 6).**

**И ВТОРАЯ ОГОВОРКА, КРУПНЕЕ ПЕРВОЙ — К68: сам `ε` назначен, а не выведен.** При
трёх пикселях на элемент столбец «после A» даёт `1.9e7` вместо `1.37e8`, а вместе
с огрублением заэкранного `~3e6`, то есть **полтора экрана**. **База сравнения при
этом обязана называться каждый раз (это и есть содержание К67): против равномерной
сетки под пиксель — вниз на семь порядков; против нынешней игрушечной `16³` —
вверх, но её никто и не собирался сохранять.**

**3. МАТРИЦА РАЗВЁРТКИ НЕ ЕСТЬ `n²`.** Она РАЗРЕЖЕНА (шесть соседей по грани) и
вдобавок треугольна в порядке обхода; мы её никогда не формируем, а применяем
обратный оператор неявно за один проход. Размен не «плотное против ленточного», а
**точное обращение за проход против итераций по разреженной системе**.

**4. НАИМЕНЬШИЕ КВАДРАТЫ: `AᵀA` НЕ ОБРАЗОВЫВАТЬ.** Обусловленность в квадрат,
разреженность в заполнение (две строки, делящие столбец, становятся связанными).
Крыловские методы по `A`. Та же директива, что в волновой линии.

**5. ЗЕРКАЛО В ЦИФРАХ.** Сдвиг на пиксель по изображению шара радиуса `r` на
расстоянии `R` поворачивает отражённое направление на `2εR/r`. Для шара `r = 3`
на `R = 9.42` при `ε = 6.77e-4` это `4.25e-3` рад на пиксель, то есть грань
стенки в одну единицу с расстояния 8 занимает в отражении **29 пикселей**. Довести
до 16 стоит втрое больше граней на стенках; до 2 — в двести десять раз больше.
Выпуклое зеркало УМЕНЬШАЕТ, поэтому требование к его собственным граням остаётся
обычным пиксельным (`L < εR`), а не ужесточается в `R/r` раз.

**К63. ПРАВИЛО ПРОТИВОРЕЧИТ СОБСТВЕННОЙ ВТОРОЙ ПОЛОВИНЕ, И `×210` — ЭТО ЦЕНА
ЭТАПА A, А НЕ ЦЕНА ЗЕРКАЛ.** Отражённое направление поворачивается на `2εR/r`,
то есть окружение видно в зеркале ВШЕСТЕРО ГРУБЕЕ, чем прямо (`4.25e-3` против
`6.77e-4`) — это и есть «выпуклое уменьшает». Значит грань размера `L = εR`,
которая прямо занимает РОВНО пиксель, в отражении занимает `1/6.3` пикселя, и
дробить под зеркало НЕ НАДО НИЧЕГО. Нынешние `29` пикселей — свойство нынешней
грани в ОДНУ ЕДИНИЦУ (прямо она занимает `185` пикселей, правило 6), то есть то
же самое недоразрешение. **`×210` есть часть счёта этапа A и в бюджет зеркал
входить не должна — иначе она будет учтена дважды.** Для ВОГНУТОГО зеркала знак
обратный, и вот там правило работает; в нынешней сцене вогнутых нет.

**6. НЕДОРАЗРЕШЁННОСТЬ НЫНЕШНЕЙ КОНФИГУРАЦИИ.** При `ε = 6.77e-4` ячейка в одну
единицу на расстоянии 8 занимает `185×185 = 34 тысячи пикселей`, поверхностный
элемент `53×53 = 3 тысячи`. **Все цифры быстродействия, снятые до перехода на
LOD, относятся к конфигурации, недоразрешённой на четыре-пять порядков**, и
сравнивать их с трассировщиком, выдающим полноразмерную картинку, бессмысленно.

### ЧТО ОСТАЁТСЯ ОТКРЫТЫМ И В ЭТОТ ПЛАН НЕ ВХОДИТ

- **объединение тел в резе** (материал есть ПЕРЕСЕЧЕНИЕ полуплоскостей, одно
  выпуклое тело на ячейку) — без него «два тела в ячейке» не чинится, и вогнутые
  места не представимы;
- **К45**: сбор не ослабляет радианс средой на последнем отрезке;
- **К41/К42**: положительность по углам ЯЧЕЙКИ, жёсткий откат вместо срезки `α`.

**ТРИ СТРОКИ ОТСЮДА ПЕРЕЕХАЛИ В ЭТАП A ПО К59 И К60 — ОНИ НЕ ОТКРЫТЫЕ ХВОСТЫ, А
ЕГО ПРЕДУСЛОВИЯ, И БЕЗВРЕДНЫ ОНИ РОВНО ПОТОМУ, ЧТО СЕТКА СЕГОДНЯ РАВНОМЕРНАЯ:**

- **Г50** (побитовость на общей грани у соседей РАЗНЫХ уровней, `3.6e-13`) — на
  сетке 16³ разных уровней НЕТ ВООБЩЕ, поэтому находка ни разу не выстрелила. A
  создаёт перепад уровней вдоль КАЖДОЙ протяжённой поверхности. Фальсификатор
  готов и дёшев: замкнутость флюидной области, сегодня `4.5e-14` на равномерной,
  измеряется на перепаде 2:1, 4:1, 8:1 — предсказание записывается до прогона;
- **К21** (веер индексирует ФАСЕТНОЕ тело, примитив торчит наружу на `dmax`) —
  число потерянных скользящих лучей масштабируется с числом разрезанных ячеек, а
  A увеличивает его на порядки. Измерено `94 → 4` при `k: 0 → 1`, то есть эффект
  живёт;
- **К50** (сбор считает тело ПРИМИТИВОМ, развёртка — МНОГОГРАННИКОМ) — A её не
  оставляет открытой, а МОЛЧА РЕШАЕТ в пользу многогранника (К59). Решение
  законное, но оно обязано быть названо, потому что вместе с ним уходит `0.0`
  побитово у силуэта.

### ЧЕГО ЭТОТ ПЛАН НЕ ОБЕЩАЕТ

**ПЕРЕПИСАНО ПО К51: ИЗМЕРЕННОГО МНОЖИТЕЛЯ НЕТ НИ У ОДНОГО ЭТАПА, ВКЛЮЧАЯ A.**

Было написано: «У A он есть: 52% холодного старта плюс закон выходной
ограниченности, проверенный счётом». Неверно трижды.

1. **`52.2%` — это ДОЛЯ сбора в холодном старте `4.174 с`, а не выигрыш.** Сбор
   не исчезает, он меняет алгоритм, и цена проекции не измерена ничем;
2. **Знак обратный.** Собственный счёт плана даёт после LOD `1.16e14` операций,
   то есть **19 часов на ядре против 1.9 с**. A увеличивает холодный старт на
   четыре порядка; уменьшает он его только против равномерной сетки под пиксель,
   которой мы не строили;
3. **Это ДО-LOD число, а правило 6 этого же раздела запрещает такие цитировать.**
   Раздел одновременно объявляет всю нынешнюю конфигурацию недоразрешённой на
   четыре-пять порядков и опирается на снятый с неё процент.

Закон выходной ограниченности проверен — но проверен СЧЁТОМ ЯЧЕЕК, и он говорит
о РАЗМЕРЕ задачи, а не о цене этапа.

У B измерено, что цена не растёт с толщиной — **на печи, без ограничителя, на
случайной правой части** (К65). У C и D — только доводы. **До измерения ни одна
из цифр этого плана не считается установленной**, и **порядок этапов выбран НЕ по
множителю** (множителей нет), а по зависимостям и по тому, что A первым делает
картинку разрешённой, то есть создаёт основание для всех последующих замеров.
Это отличие важно: в шаге 1 порядок был назначен по величине множителя, и первый
же замер показал, что множитель принадлежит не тому пункту.

## АУДИТ ПЛАНА ПЕРЕХОДА 07-28 — СОРОК ЧЕТЫРЕ НАХОДКИ, ВСЕ ВНЕСЕНЫ ВЫШЕ

К51…К78 получены ДО КОДА, по заведённому порядку. **К79…К94 — ЗАМЕРОМ, уже после,
и это отмечено у каждой:** целевая машина (К79), ошибка в единицах (К80), пол
невязки (К81), возражение про город (К82). Перечень для истории; развёрнутые
формулировки — там, где их прочтут, то есть в тексте этапов, правил и доклада
шага 1.

| # | находка | где внесена |
|---|---|---|
| К51 | «A убирает 52% холодного старта» — это ДОЛЯ сбора, а не выигрыш, и знак обратный: собственный счёт плана даёт 19 часов против 1.9 с. Плюс это до-LOD число, запрещённое правилом 6 | этап A, «чего план не обещает» |
| К52 | «марш из 23 ячеек в 7500» выведено по ОБЪЁМНОМУ закону, который К49 отверг: под LOD луч идёт по схлопнутой пустоте, и число измеримо в `lod3.c` за час | этап A |
| К53 | половины A разделимы, а «дыры от проекции без LOD» неверны по направлению: растеризация крупного элемента даёт БЛОКИ, дыры даёт сплат. Изолирующие проверки (К38) запрещены формулой «только вместе» | этап A |
| К54 | фальсификатор «`cut.nbad` остаётся нулём» предсказывает не тот исход: на равномерной сетке разбитых граней нет вовсе (тест пуст), под `L = εR` соседние разрезанные ячейки РАЗНЫХ уровней неизбежны ⇒ `nbad > 0` гарантирован | этап A |
| К55 | **фальсификатор C — печь для зеркал — слеп РОВНО к перестановке ординат, ради которой этап и делается: в однородном поле её проходит любая, включая тождественную. Это К44 дословно** | этап C |
| К56 | C ставит зеркалу `ND` чисел, а правило 1 требует `2π/ε²`: внутрисистемная связь возвращает ЭНЕРГИЮ и разрушает ОБРАЗ, сегодня точный побитово. Таблица «что меняется» подменяет «и» на «вместо» | таблица, этап C |
| К57 | C ломает прибор К40: тождество баланса верно потому, что зеркало для развёртки ЧЁРНОЕ; после C нужен путь `psout`, иначе невязка станет ЗАКОННОЙ и прибор снова ослепнет | этап C |
| К58 | «все эталоны обязаны держаться после каждого этапа» неверно для D (`O(ND⁻²)` — свойство ГАУССОВОЙ квадратуры, у базиса с носителем порядок свой ⇒ красный тест на исправном коде) и недостаточно для A (печь и полость слепы к наклонам и потоку; К13 проверен на ТРЁХ уровнях, A даёт двадцать) | «что не трогается», этапы A и D |
| К59 | A растеризует ЭЛЕМЕНТЫ, то есть многогранник ⇒ **К15 отменяется**, а он объявлен нетронутым; заодно A молча решает К50 в пользу многогранника | «что не трогается», этап A |
| К60 | Г50 и К21 — не открытые хвосты, а ПРЕДУСЛОВИЯ A: безвредны они ровно потому, что сетка равномерная, а A создаёт перепад уровней вдоль каждой поверхности | список открытого → этап A |
| К61 | правило 2 (`P1` `×70`) сравнивает ТОЧНОЕ ОБРАЩЕНИЕ с ОДНИМ матвеком (честно `×18`), `P1` — другое уравнение и запрещён К4 в анизотропной среде, и он не входит ни в один этап A…E | правило 2 |
| К62 | два числа без опоры: `(1/ε)² = 2.7e7` не равно само себе (`2.2e6`; `2.7e7` — это `4π/ε²`, а нужна полусфера `1.4e7`), и «экономия `2.16`» у квантования не встречается в проекте нигде | правило 1, этап A |
| К63 | правило 5 противоречит своей же второй половине: выпуклое зеркало УМЕНЬШАЕТ вшестеро, значит грань `L = εR` в отражении меньше пикселя и дробить нечего. `×210` — цена этапа A, и в бюджет зеркал входить не должна | правило 5 |
| К64 | E поставлен ПОСЛЕ A, а его сильнейший фальсификатор (три одинаковых канала побитово равны одноканальному прогону) требует неизменной конфигурации ⇒ E идёт ДО A. Плюс `×1.5…×1.6` процитировано без собственной оговорки «не установлено» (класс Г19) | этап E |
| К65 | B: «с развёрткой в предобуславливателе будет лучше» — предобуславливателя не названо (в замере развёртка была САМИМ оператором); шесть проходов сняты без ограничителя и на случайной правой части. Решающий опыт стоит копейки и отсутствует | этап B |
| К66 | фальсификатор цены A свипит ОДНУ ось, а К46 уже показала, что одна ось врёт; и ссылка «закон проверен счётом в `lod3.c`» приписывает счётчику ячеек чужой результат — про цену СБОРА он не мерил ничего | этап A |
| К67 | «четыре коэффициента — не рост против скаляра радиосити» — неправда (это рост вчетверо); и главное, сокращение по УГЛУ (`×8`) названо без роста по ПРОСТРАНСТВУ от A (`×33 000`), отчего прямой вопрос «станет ли система меньше» получал неверный ответ | правило 2 |
| **К68** | **`ε` взято равным ПИКСЕЛЮ без единого довода, хотя в переносе физического пола (в отличие от `λ/D` волновой линии) нет вовсе. Размер элемента входит В КВАДРАТЕ: три пикселя на элемент дают `÷7.0` по измеренному закону П4, складываются с заэкранным и решателем и выводят холодный старт на `3.4e11`. Заодно решает К59 в пользу растеризации ПРИМИТИВА** | этап A |
| К69 | план A…E не упоминает T7 вообще; «дом с деревом» упирается не в счёт элементов (он выходно-ограничен, измерено), а в три отсутствующих куска представления — объединение тел, гомогенизация со сгущением (провалилась между двумя файлами), критерий по ПРОСВЕТУ | этапы, после E |
| К70 | порядок обхода зависит только от ОКТАНТА (проверено в `sweep3.c`): топологическая сортировка нужна 8 раз, а не `ND`; и ординаты внутри октанта — единственная годная ось SIMD, то есть довод за БОЛЬШЕЕ `ND` | новый раздел «цена на GPU» |
| К71 | `×4.3` на 16 ядрах снято на сцене 4096 ячеек и не переносится ни вверх, ни вниз; цитируется в плане трижды | там же |
| К72 | разрядность — архитектурное решение до этапа A: FP64 в геометрии и в эталонах (Г50 уже `3.6e-13`, прибор К40 имеет `3.8e-15`), FP32 допустим в поле (цель `1e-3`). На потребительских картах FP64 есть `1/64` | там же |
| К73 | задача ПРОПУСКНО-ограничена (`1.75…3.5` FLOP/байт против баланса `58`), и счёт «20 Тфлопс → 5.8 с» мерил не то; считать надо трафик | там же |
| К75 | этап F (важность как отсечение, решение пользователя): `×10` частично двойной счёт с уже применённым заэкранным `×6`; «отсечь» обязано быть «огрубить до вклада»; **главный выигрыш на УГЛОВОЙ оси (переменное `ND` по положению), а не на пространственной**; `W` камерозависима и углубляет привязку | этап F |
| К76 | все замеры сходимости сняты ОТ НУЛЯ; тёплый старт от прошлого кадра не измерен ни разу, хотя `warm_start` в коде есть. **Именно он решает, «на грани» ли метод при движущемся свете: 2…3 прохода вместо 6 дают `8…17 мс`** | этап B |
| К80 | **НАЙДЕНА ЗАМЕРОМ, а не рассуждением.** Сбор искал выход луча из куба МИРОВЫМИ координатами, сравнивая с числом ячеек: молчаливое `u = 1`, невидимое, пока кадр единичный. Класс Г21/Г46. Фальсификатор — ПОДОБИЕ: `0.0` побитово с правкой, `7.818` у прежнего кода | `gather3.c`, доклад шага 1 |
| К81 | **НАЙДЕНА ЗАМЕРОМ.** У невязки есть ПОЛ (`4e-8` на `64³`), а допуск абсолютный (`1e-9` при `φ ≈ 16`) ⇒ критерий недостижим, 4000 итераций вместо 69, 4 ч 41 мин вместо 5 мин. Не К38: скорость та же, `nclip`/`nfb` постоянны. Стоп по ЗАСТОЮ; картинка побитово та же | `sweep3.c`, доклад шага 1 |
| К52↑ | **ЗАКРЫТА ЗАМЕРОМ.** Ячеек вдоль луча: при схлопнутой пустоте `18 → 21 → 22` (логарифм), при заполнении объёма `70 → 114 → 176` (`1/ε`). Экстраполяция объёмного закона к `ε = 6.77e-4` даёт `6500` — то есть «7500» принадлежало ему. **Довод «A только вместе» отпал: половины разделимы** | этап A, `lod3.c` |
| К93 | **ЗАМЕР, этап A.** Построитель сцены обходил ЕДИНИЧНЫЕ ячейки и потому был равномерным ПО ПОСТРОЕНИЮ: на градуированном дереве ключи повторяются (Г45) и `render3` умирал с кодом 1 БЕЗ СЛОВА. Пока это держалось, `ε` при `≤0.05` не менял ничего — первая подпись артефакта. Лечение: обход ЛИСТЬЕВ, отбор по коробке листа, причина отказа наружу | `render3.c` |
| К94 | **ЗАМЕР, этап A.** LOD-картинка неверна (максимум `8.8` при эталонных `3.36`, `2.4` в худшем пикселе), а **баланс держится `7.4e-15` — инвариант к этому СЛЕП**. Третий раз узор К13/К40. Простой связи с `nbad` нет (8 нарушений ломают как 120), и превышение излучения стенки указывает на чтение DG1 вне элемента (класс К39), а не на потерю энергии | этап A |
| К91 | **ЗАМЕР, этап C.** Энергетическое тождество было СЛЕПО к объёмному источнику: `pin + psout = pout + pabs + psin` верно только при `ε = 0`, и невязка равнялась ВСЕЙ излучённой мощности (`1.14e+01` от втекшего). Ни один тест с `ε` баланс не проверял. Правка берёт `pemit` всеми четырьмя моментами — дословно К48, второй раз | `sweep3.c` |
| К92 | **ЗАМЕР, этап C.** Мой собственный негативный контроль оказался СЛЕП: подмена перестановки тождественной В ОБЕИХ задачах симметрична сама по себе, и метрика её не видит (`4.6e-16` вместо `O(1)`). Ломать надо ОДНУ сторону — тогда `0.349`. К55 в четвёртый раз, и впервые на своём же тесте | `test_sweep3.c` |
| К90 | **ЗАМЕР, этап E.** `×1.5…×1.6` были посчитаны для устройства с ОБЩЕЙ матрицей ячейки, а выбрано устройство с НЕЗАВИСИМЫМИ задачами; в плане они стояли рядом как оценка и решение. Измерено: развёртка `×2.67`, холодный старт `×2.36`, **кадр `×0.96` — даром**. Размен назван: `×1.2` лишней цены за невозможность смешения каналов по построению | доклад этапа E |
| К88 | **ЗАМЕР.** Допуск решателя `1e-9` при `φ ≈ 50` есть `2e-11` относительных, а ошибка дискретизации измерена в `3.0e-2` — система решалась на ДЕВЯТЬ ПОРЯДКОВ точнее, чем имеет смысл. `56 → 10` итераций (`×5.6`) при ошибке картинки вшестеро НИЖЕ дискретизации. Правило: допуск есть доля от оценённой ошибки сетки, а не константа | `render3.c`, доклад этапа B |
| К89 | **ЗАМЕР.** Допуск и Крылов ЗАМЕЩАЮТ друг друга, а не складываются: оба бьют в `ln(tol/e₀)/ln(ρ)`. При нужном картинке допуске Крылов даёт `×1.22`, а не `×2.87`; полное `56 → 9` есть `×6.2`. Долг этапа B этим и закрывается — при таком допуске ряд Неймана С ограничителем дешевле Крылова с починкой. Ценность B — НЕЧУВСТВИТЕЛЬНОСТЬ К АЛЬБЕДО (свип: Нейман `×1.76`, Крылов `×1.21`) | доклад этапа B |
| К86 | **ЗАМЕР, этап B.** Нелинейных переключателей ДВА, а `p->limiter` управлял одним: `corner_min < 0` в проекции хранимого стояло безусловно ⇒ выключение ограничителя оператор линейным НЕ делало. Вырождение элемента — свойство геометрии (линейно), положительность — свойство поля (переключатель) | `sweep3.c` |
| К87 | **ЗАМЕР, этап B.** `S(0)` холодным стартом — НЕ отображение, применённое к нулю: он сначала кладёт `bout = emit`. Крылов при такой правой части сходился безупречно (`7e-10`) к состоянию с `φ ≈ 0`, то есть решал не ту задачу и рапортовал об успехе | `krylov3.c` |
| К84 | **НАЙДЕНА НЕГАТИВНЫМ КОНТРОЛЕМ.** Невязка мерила одно `φ`, а **картинка делается из `bout`/`sout`** — критерий не измерял то, ради чего решение считается. Тёплый старт останавливался на НУЛЕВОЙ итерации и давал ложное `×47` при `max|Δφ| = 5.04`. Правка: невязка по всему состоянию; исходов стало три (допуск, застой, `maxit`) | `sweep3.c`, доклад шага 1 |
| К85 | **ЗАМЕР ОПРОВЕРГ МОЁ ЖЕ ОБЕЩАНИЕ.** Тёплый старт даёт `×0.93…×1.37`, а не `×20`: он уменьшает только `e₀`, а число итераций есть `ln(tol/e₀)/ln(ρ)`. Решает «на грани» то, что меняет `ρ` — то есть Крылов, а не тёплый старт | этап B, К76 |
| К83 | **ЗАМЕР.** 60% памяти (292 МБ из 491 на `64³`) занято матрицами масс ГРАНЕЙ — по три `4×4` на каждую из 798720, — тогда как у 96% граней геометрия одинакова и матрица аналитична: на равномерной сетке различных ТРИ. Отказ хранить даёт `×2.5` по памяти И по времени (задача пропускно-ограничена, К73) | доклад шага 1 |
| К82 | «5 минут» относятся к игрушке: на городе `159 часов` по измеренной цене за элемент, и рычаги плана доводят до `19 с` — но первым упирается ПАМЯТЬ (`8.4` ГБ против 6 ГБ видеопамяти), вторым проход 1 (К78). Барьер города К81 не тронула | доклад шага 1 |
| К79 | **ЦЕЛЕВАЯ МАШИНА ИЗМЕРЕНА: `22.6` ГБ/с, память насыщается на ДВУХ потоках. CPU тоже пропускно-ограничен (интенсивность `1.75` против баланса `19.8`) — моё предсказание «счётно-ограничен» опровергнуто замером. К71 закрыт: `×4.3` был упором в память. 6 ГБ VRAM ограничивают `N ≲ 5e6`, FP64 на Turing `1/32`. Часов не возникает нигде: `0.28 с` CPU / `26 мс` GPU с уже записанными рычагами** | новый раздел «цена на GPU» |
| К77 | размер есть `N × ND × 4`, а трогали только `N`. **Р6 (раздельные сетки, `÷8`) и Р2 (`P1` для всей косвенной, `÷8`) написаны в этом же файле и не попали ни в один этап**; плюс DG1→DG0 в объёме (`÷2…÷3`, но НЕ на поверхностях — К39). Вместе `÷128`. Разрежать при этом почти нечего: матрица не формируется, угловой блок уже ранга один | этапы, после F |
| К78 | все таблицы цены считают ТОЛЬКО проход 2; после сокращений К77 узкое место переезжает в проход 1 (`≈3.1e11`), которого в них нет вовсе. С3/С4 предупреждали об этом до кода, но в число поправка не внесена — класс Г19 | там же |
| К74 | развёртка есть ВОЛНОВОЙ ФРОНТ: на сцене `16³` GPU упирается в 276 точек синхронизации при загрузке 3%, а не в память. **Первый случай, когда игрушечная конфигурация врёт в ПЕССИМИСТИЧНУЮ сторону** | там же |

### ЧТО ЭТИ СОРОК ЧЕТЫРЕ ГОВОРЯТ О ПЛАНЕ В ЦЕЛОМ

**Классов ровно три, и все три уже названы в этом файле раньше.**

1. **МЕТРИКА, СЛЕПАЯ К ТОМУ, ЧТО СТЕРЕЖЁТ** — К55, К54, К66, К57. Это шестой,
   седьмой, восьмой и девятый случаи подряд (К12, К13, К44, вырожденная правая
   часть у Крылова, обойдённая проверкой `both == 0` К27). **Узор устойчив
   настолько, что его пора применять как ПРАВИЛО ПРОЕКТИРОВАНИЯ теста, а не
   ловить постфактум: у каждого фальсификатора обязан быть записан ответ на
   вопрос «какая ИМЕННО степень свободы его провалит», и если ответ — «никакая
   из тех, ради которых делается этап», тест не пишется.**
2. **ЧИСЛО, ВЗЯТОЕ НЕ ОТТУДА** — К51, К52, К61, К62, К63, К67. Общее у всех
   шестерых: величина верна в своём исходном контексте и перенесена в чужой (доля
   этапа → выигрыш; объёмный закон → луч; один матвек → полное решение; полная
   сфера → полусфера; цена LOD → цена зеркал; удобная база вместо названной).
3. **ЗАВИСИМОСТЬ, НАЗВАННАЯ НЕВЕРНО** — К53, К59, К60, К64, К65. Здесь порядок и
   состав этапов расходятся с тем, что этапы делают на самом деле.

**ЧЕТВЁРТЫЙ КЛАСС, КОТОРОГО В ПРЕЖНИХ АУДИТАХ НЕ БЫЛО ВОВСЕ — К68, К72, К73, К74:
НАЗНАЧЕННОЕ ЧИСЛО И НАЗНАЧЕННАЯ МАШИНА.** Это не ошибки в тексте, а параметры,
которые план держал константами, ни разу не спросив, откуда взялись их значения.
**`ε` назначен** (пиксель, без довода); **`ND = 32` назначен** (T3 измерила, что
при альбедо 0.9 для `1e-3` довольно 16 — а стоит 32, и К70 даёт довод за 128);
**уровень фасетизации `k = 1` назначен**; **разрядность `double` назначена**;
**«один поток, без векторизации» назначено** и снято пользователем 07-28.

**Все пять входят в стоимость крупно, и ни одно не свипалось.** Отдача этого
класса оказалась выше, чем у трёх остальных вместе: `1.16e14 → 3.4e11` по счёту и
переход от «19 часов» к «десяткам миллисекунд» по трафику. **Правило на будущее:
пройти по всем числам, которые в файлах стоят без вывода, и у каждого спросить —
оно ИЗМЕРЕНО, ВЫВЕДЕНО или НАЗНАЧЕНО. Третьи подлежат свипу, а не цитированию.**

**Отдача по проходам: 7 → 6 → 4 → 3 у аудитов 07-27, и вдруг 44 здесь.** Это не
возврат отдачи, а признак того, что раздел писался в один присест как ИТОГ
разбора, а не проходил аудит по частям, как вехи T1…T5. Вывод для порядка работ:
**крупный план, собранный из уже принятых решений, ревизуется НЕ ЛЕГЧЕ отдельной
вехи, а тяжелее — потому что каждое решение приезжает вместе со своим контекстом,
и именно контекст теряется при сборке.**

### САМАЯ ОПАСНАЯ И САМАЯ ЦЕННАЯ

**САМАЯ ОПАСНАЯ — К55.** Единственный фальсификатор этапа C проходит при
ПОЛНОСТЬЮ НЕВЕРНОЙ таблице перестановки ординат, включая тождественную, потому
что точное решение однородно. То есть этап, вся суть которого — направленная
связь `m → m′`, был бы сдан зелёным без единой проверки этой связи, и обнаружилось
бы это на картинке через несколько шагов, когда причина уже неотделима от
остального. Лечение записано в самом файле полтора раздела назад (К44: «стенкам
раздаются РАЗНЫЕ радиансы») — то есть находка была не новой мыслью, а
НЕПРИМЕНЁННЫМ СОБСТВЕННЫМ ПРАВИЛОМ, что делает её опаснее, а не безобиднее.

**САМАЯ ЦЕННАЯ — К68, И ОНА ПРИШЛА ОТ ПОЛЬЗОВАТЕЛЯ, А НЕ ИЗ АУДИТА.** Весь план
считал `ε` равным пикселю, потому что так было написано, и ни разу не спросил,
откуда это взялось. Между тем размер элемента входит в стоимость В КВАДРАТЕ, и три
пикселя на элемент дают `÷7` по уже измеренному закону П4, складываясь с
огрублением заэкранного и со сменой решателя: `1.16e14 → 3.4e11`, то есть переход
из «19 часов на ядре» в «доли секунды на GPU». **Это единственное место в файле,
где стоимость меняется на три порядка не оптимизацией кода, а тем, что назначенное
число признано параметром.** И оно же попутно закрывает развилку К59 в пользу
растеризации примитива.

**ВТОРАЯ ПО ЦЕННОСТИ — К51.** У этапа A записан единственный измеренный множитель
во всём плане, и он назван не той величиной (доля сбора вместо выигрыша), снят с
конфигурации, которую тот же раздел объявляет непригодной для цитирования, и
направлен в другую сторону. Ценность в том, что это меняет ОСНОВАНИЕ порядка
работ: A остаётся первым, но не как экономия, а как единственный этап, создающий
разрешённую картинку, без которой остальные измерять не на чем. Ровно этот
промах — порядок, назначенный по множителю, который принадлежит не тому пункту, —
уже стоил проекту целого пункта плана в шаге 1 (`×130` был приписан отсутствию
DSA, а принадлежал ошибке в коде).

**ТРЕТЬЕЙ ИДЁТ ПАРА К73/К74, И ОНА ЕДИНСТВЕННАЯ, ЧТО ОТНОСИТСЯ К ЖЕЛЕЗУ.** Обе
опровергают единственную GPU-оценку, которая в плане была («20 Тфлопс → 5.8 с»), и
опровергают в РАЗНЫЕ стороны: К73 говорит, что счёт надо вести по трафику, а не по
флопсам (`1.75…3.5` FLOP/байт против баланса `58`), а К74 — что на нынешней
игрушечной сцене узкое место вообще третье, синхронизация волновых фронтов, и
потому замер ускорения на ней ЗАНИЗИТ результат. **Вместе они означают, что ни
одного числа о цене на GPU у проекта нет, а то единственное, что было, мерило не
ту величину.**

**Отдельно стоит К59:** этап A молча отменяет `0.0`
побитово у силуэта — лучший измеренный результат линии по геометрии, — потому что
растеризует многогранник там, где марш спрашивал примитив. Это не ошибка, а
НЕНАЗВАННЫЙ РАЗМЕН, и назвать его надо до кода, иначе огранка на сферах вернётся
и будет прочитана как дефект новой машинерии.

## ШАГ 1 ПЛАНА ПЕРЕХОДА ВЫПОЛНЕН 07-28 — ТРИ ЗАМЕРА, И КАЖДЫЙ НАШЁЛ БОЛЬШЕ, ЧЕМ ИСКАЛ

Замеры ставились, чтобы решить развилки этапов A и B, ничего в алгоритме не меняя.
**Два из трёх вскрыли настоящие ошибки в коде, сданном зелёным, а третий опроверг
собственное предсказание.** `make test` зелёный (12 наборов), `ccheck` CLEAN.

### К65 ИЗМЕРЕНА: ДВЕ ОПЦИИ ЭТАПА B НЕ РАВНОЗНАЧНЫ

Ограничитель положительности на РЕНДЕРНОЙ сцене (`16³`, оба прогона одинаковы во
всём остальном):

| | срезок | min φ по углам ячеек | ячеек с минусом | картинка против ON |
|---|---|---|---|---|
| ограничитель ON | 458 | **ровно `0.0`** | 0 из 4096 | — |
| ограничитель OFF | 0 | **`−21.5`** | **6 из 4096** | **0.2% сред, 6.8% макс** |

Минус реален, но сидит в ШЕСТИ ячейках из 4096 и сходимость не трогает (47
итераций в обоих случаях). **Однако в картинке это до семи процентов.** Значит две
опции, которые этап B называл равнозначными («внешний нелинейный цикл ЛИБО
ограничитель, применяемый только к итогу»), **не равнозначны**: линейное решение с
последующей срезкой даёт ДРУГУЮ неподвижную точку, а не ту же с точностью до
мелочи. Этап B обязан назвать, к какой из них он сходится.

### К80. СБОР СЧИТАЛ СТЕНКУ В МИРОВЫХ КООРДИНАТАХ, СРАВНИВАЯ С ЕДИНИЧНЫМИ

**Найдена свипом К68 — тем, что свип ОБЯЗАН менять кадр, чтобы комната осталась
той же.** `wall_face` в `gather3.c` искала выход луча из куба МИРОВЫМИ
координатами, сравнивая их с числом ячеек и беря от них `floor`, то есть молча
предполагала `u = 1`. Кадр во всём проекте всегда был единичным, и ошибка **не
проявлялась вовсе**; при `u = 0.5` радианс уходил с `0.45…3.38` на `0.0…20.9`.

Класс дословно тот же, что Г21 и Г46 общего слоя: величина в единичных
координатах вместо мировых, невидимая на кубическом единичном кадре.

| проверка | предсказано | измерено |
|---|---|---|
| подобие: `u=1` против `u=2` при вдвое большей мировой геометрии | ≤1e-13 | **`0.000e+00` побитово** |
| лучей, доходящих до стенки (иначе проверка пуста) | > 500 | 4471 из 6912 |
| **негативный контроль (К44): переставить радиансы стенок** | `O(1)` | **`0.86`** |
| **прежний код, прогнан заново** | обязан валить | **`7.818`, тест красный** |
| правка при единичном кадре | ничего не меняет | **побитово** |

**Стенкам обязаны раздаваться РАЗНЫЕ радиансы** — в однородном поле промах по
стенке невидим, и это К44 в шестой раз.

### К81. У НЕВЯЗКИ ЕСТЬ ПОЛ, А КРИТЕРИЙ ОСТАНОВКИ АБСОЛЮТНЫЙ

На `64³` развёртка не останавливалась: **4000 итераций, невязка `4.06e-08` при
допуске `1e-9`, 4 ч 41 мин.** История невязки (штатный вывод, `tr3_problem.trace`)
показывает, что это НЕ медленная сходимость:

    it 35  resid 2.957e-06  phi0 16.224084304  nclip 4025  nfb 4
    it 45  resid 4.667e-08  phi0 16.224084731  nclip 4025  nfb 4
    it 50  resid 6.503e-08  phi0 16.224084734  nclip 4025  nfb 4   <- РОСЛА
    it 55  resid 3.690e-08  phi0 16.224084734  nclip 4025  nfb 4

Решение встаёт к 45-й итерации, скорость та же `0.63`, что на `16³` и `32³`, а
`nclip` и `nfallback` ПОСТОЯННЫ с двадцатой — то есть нелинейный переключатель
стоит, и К38 здесь ни при чём. Допуск же АБСОЛЮТНЫЙ: `1e-9` при `φ ≈ 16` есть
`6e-11` относительных, то есть слеп и к масштабу, и к сетке.

**ЛЕЧЕНИЕ БЕЗ МАГИЧЕСКОГО ПОРОГА:** следить не за ВЕЛИЧИНОЙ невязки, а за тем,
улучшается ли она. `HZ_SWEEP_STALL = 10` итераций подряд без улучшения лучшего
достигнутого значения. Число выведено, а не подобрано: ряд по рассеянию идёт как
`ρⁿ`, и даже при `ρ = 0.9` десять итераций дают множитель `0.35`; ноль улучшения
за десять итераций медленной сходимостью быть не может. Исходы РАЗЛИЧАЮТСЯ
(`tr3_stats.stalled`) — сливать «сошлось» и «упёрлось в пол» значит повторить Г38.

| | было | стало |
|---|---|---|
| итераций на `64³` | 4000 | **69** |
| время | **4:41:03** | **5:08** — `×55` |
| картинка против 4000-итерационной | — | **`0.000e+00` побитово** |
| `16³` и `32³` | — | побитово прежние (достигают допуска раньше застоя) |

**3931 итерация не изменила ничего.** И это прямой ответ на требование
пользователя «холодный старт в часы не устраивает»: часов больше нет, и убрал их
не разгон, а критерий остановки.

**ЧТО ПРИ ЭТОМ НЕ УСТАНОВЛЕНО, ЯВНО.** Механизм самого пола `~4e-8` НЕ НАЙДЕН.
Он не есть округление (пол на семь порядков выше машинного), не есть колебание
переключателя (`nclip` и `nfb` постоянны) и не есть медленная сходимость
(скорость та же). Записано открытым вопросом; на ответ «стоп по застою» это не
влияет, потому что решение к тому моменту уже стоит.

### И ДВЕ ВЕЩИ ПОМЕЛЬЧЕ, ОБЕ ИЗ ТОГО ЖЕ ШАГА

**К28 закрыта:** длина `sigma` сверяется одним сравнением на весь марш. Тест
двусторонний — годная длина проходит, короткая отвергается кодом, — иначе его
удовлетворяет `return ESIGMA;`.

**Свип упирался в ОСНАСТКУ, а не в схему:** построитель сцены сортировал боковую
таблицу ВСТАВКАМИ, `O(n²)` при 392-байтовой записи. На `16³` (тысяча записей)
незаметно, на `64³` — десятки минут ПЕРЕД развёрткой. `qsort`; `16³` остался
побитово прежним. Урок тот же, что в К36 и К38: прежде чем объяснять медленность
свойством метода, надо посмотреть, ГДЕ время.

## ЭТАП A, ШАГ 1 (LOD ПРИ МАРШЕВОМ СБОРЕ) — ЗАПУЩЕН 07-28 И НЕ РАБОТАЕТ, КАК И ПРЕДСКАЗАНО

**Вывод, и он отрицательный: LOD-дерево строится, `ε` действует, ячеек в 2.3 раза
меньше и развёртка вдвое быстрее — а КАРТИНКА НЕВЕРНА.** Это не провал шага, а его
результат: К54 предсказала `nbad > 0`, и предсказание сбылось. Записывается как
есть, потому что чинить надо названное, а не найденное задним числом.

### ЧТО РАБОТАЕТ

| `ε` | ячеек | 1:1 нарушений | радианс (эталон равномерной `32³`: `0.426…3.356`) |
|---|---|---|---|
| `0.30` | 1387 | 32 | `0.421…5.154` |
| `0.20` | 2563 | 20 | `0.429…3.212` |
| `0.10` | 8303 | **120** | `0.438…8.798` |
| `0.05` | 14015 | 8 | `0.440…8.809` |
| равномерная `32³` | 32768 | 0 | `0.426…3.356` |

Дерево живёт: `ε` меняет число ячеек монотонно и сильно, схлопывание пустоты даёт
`14015` против `32768` при том же разрешении у поверхностей, развёртка идёт
`12.65 с` против `27.52 с`. Равномерная `16³` при этом осталась ПОБИТОВО прежней —
правка построителя есть тождественное преобразование на равномерном дереве.

### К93. ПОСТРОИТЕЛЬ СЦЕНЫ БЫЛ РАВНОМЕРНЫМ ПО ПОСТРОЕНИЮ, И УМИРАЛ МОЛЧА

Первый же LOD-прогон завершился с кодом 1 **без единого слова на выход**. Причина:
боковая таблица и маска полных ячеек обходили ЕДИНИЧНЫЕ ячейки тройным циклом, а
на градуированном дереве несколько единичных позиций дают ОДИН лист — ключ
повторяется, `hz_cutmap_add` возвращает 2 (Г45), и `render3` отвечал голым
`return 1`.

**Два разных дефекта, и оба стоит различить.** Первый — предположение о
равномерности, зашитое в обход; лечится обходом ЛИСТЬЕВ, и отбор фасетов при этом
обязан идти по коробке ЛИСТА, а не единичной ячейки. Второй — молчаливая смерть:
код возврата `hz_cutmap_add` проверялся, но причина наружу не выходила. Теперь
выходит.

**И третье, чего без этого не увидеть:** пока построитель был равномерным, `ε` при
`≤0.05` не менял НИЧЕГО (`14015` ячеек и `8` нарушений при `0.05`, `0.02`, `0.01`
одинаково) — первая подпись артефакта в чистом виде. Работало только то, что
дробилось до предела всюду.

### ЧТО НЕ РАБОТАЕТ, И ЧТО ИЗ ЭТОГО СЛЕДУЕТ

**Картинка неверна везде, кроме `ε = 0.20`:** максимум радианса уходит на `8.8`
при эталонных `3.36`. Против равномерной `32³` расхождение вне кромок — `2.9%` в
среднем и `2.4` (то есть 240%) в худшем пикселе.

**Баланс энергии при этом ДЕРЖИТСЯ** (`7.4e-15` от втекшего), то есть главный
инвариант к этой поломке СЛЕП. Третий раз тот же узор: К13 (печь не ловит
передачу потока), К40 (баланс слеп к поверхностям), теперь баланс слеп к
геометрии разреза на смене уровня.

**Простой связи с числом нарушений 1:1 НЕТ:** восемь нарушений ломают картинку так
же, как сто двадцать. Значит `nbad` — не единственная причина, и называть её
единственной нельзя. Сверх того радианс `8.8` ПРЕВЫШАЕТ излучение стенки `6.0`,
чего у сходящейся задачи с `ρ < 1` быть не может, — это подпись чтения DG1 ВНЕ
своего элемента (класс К39), а не потери энергии.

**Названные подозреваемые, в порядке проверки:** условие 1:1 у разрезанных ячеек
(К54, лечение известно — обрезать флюид прямоугольником грани); К21 (веер
индексирует ФАСЕТНОЕ тело, а примитив торчит на `dmax` — при разных размерах
ячеек это перестаёт быть мелочью); Г50 (побитовость на общей грани у соседей
разных уровней). **Все три — предусловия, которые К60 и переносила в этот этап.**

**ЧЕГО НЕЛЬЗЯ ДЕЛАТЬ ДАЛЬШЕ:** брать `ε = 0.20` как «работающую конфигурацию».
Она даёт правдоподобный максимум при двадцати нарушениях 1:1 — то есть
правдоподобный неверный результат, худший класс в этом проекте.

## ЭТАП C ВЫПОЛНЕН ЧАСТИЧНО 07-28 — ЗЕРКАЛЬНАЯ СТЕНКА ВНУТРИ СИСТЕМЫ

**Вывод. Плоское зеркало вошло в развёртку ТОЧНОЙ ПЕРЕСТАНОВКОЙ ординат, а не
интерполяцией; фальсификатор переписан по К55 и негативный контроль выстрелил
после того, как первая редакция теста оказалась слепа.** `117/117`, `make test`
зелёный (12 наборов), `ccheck` CLEAN. **Частично** — потому что сделаны СТЕНКИ, а
не произвольные фасеты, и это ограничение названо ниже.

### ПЕРЕСТАНОВКА ВЫЧИСЛЯЕТСЯ, А НЕ ИЩЕТСЯ

План говорил: набор замкнут относительно девяти плоскостей, промах `≤6.7e-16`.
Это читалось как численное совпадение. **На деле связь есть перестановка
ИНДЕКСОВ:** набор — произведение двойного Гаусса по `μ` на Гаусс по квадрантам,
обе квадратуры симметричны, поэтому

    отражение по z:  μ → −μ,      (h, i) → (1−h, nmu−1−i)
    отражение по x:  φ → π − φ,   квадранты 0↔1, 3↔2, узел i → nphi−1−i
    отражение по y:  φ → −φ,      квадранты 0↔3, 1↔2, тот же разворот

| проверка | предсказано | измерено |
|---|---|---|
| перестановка есть БИЕКЦИЯ | точно (это индексы) | **0 нарушений** |
| и ИНВОЛЮЦИЯ | точно | **0 нарушений** |
| направление действительно отражённое | ≤1e-15 (координаты — не индексы) | `3.3e-16`, `6.9e-16` |
| вес при отражении | сохраняется | **`0.00e+00` побитово** |

Побитовости у КООРДИНАТ здесь взяться неоткуда (`cos(3π/4 − x)` и `−cos(π/4 + x)`
в плавающей арифметике не равны), и требовать её значило бы завести красный тест
на исправном коде — класс К1/К6/К10.

### К92. МОЙ СОБСТВЕННЫЙ НЕГАТИВНЫЙ КОНТРОЛЬ ОКАЗАЛСЯ СЛЕП — К55 В ЧЕТВЁРТЫЙ РАЗ

Фальсификатор построен по К55, то есть НЕ печью: проверяется СИММЕТРИЯ. Источник
несёт направленную часть `ε += eps_dir·ω`; отражение по `z` меняет знак `ω_z`,
значит задача с зеркалом на `−z` при `eps_dir = (0,0,+c)` обязана дать РОВНО то
же, что задача с зеркалом на `+z` при `(0,0,−c)`, отражённая по `z`.

**Первая редакция негативного контроля подменяла перестановку тождественной В
ОБЕИХ задачах — и не выстрелила: `4.6e-16` вместо `O(1)`.** Причина
содержательная: тождественная перестановка САМА симметрична относительно `z`,
поэтому одинаково сломанные задачи по-прежнему переходят друг в друга. Ломать
надо ОДНУ сторону — тогда вопрос звучит как надо: «зависит ли ответ от
перестановки вообще».

| | измерено |
|---|---|
| симметрия зеркальной стенки | **`2.07e-16`** |
| негативный контроль ПЕРВОЙ редакции (сломаны обе) | `4.6e-16` — **слеп** |
| негативный контроль ВТОРОЙ редакции (сломана одна) | **`0.349`** |

### К91. ЭНЕРГЕТИЧЕСКОЕ ТОЖДЕСТВО БЫЛО СЛЕПО К ОБЪЁМНОМУ ИСТОЧНИКУ

Проверка К57 («зеркало перестаёт быть чёрным, прибор обязан это пережить») сразу
дала невязку `1.14e+01` от втекшего. **Ошибка оказалась в ПРИБОРЕ, а не в
зеркале:** тождество `pin + psout = pout + pabs + psin` не содержит излучённой в
объёме мощности и верно только при `ε = 0`. Ни один тест с объёмным источником
баланс не проверял, поэтому дыра держалась с шага C.

Арифметика сошлась сразу: `4π·1.0·512 = 6433.98` против
`pout + pabs − pin = 4361.85 + 2636.10 − 563.97 = 6433.98`.

Правка: `pemit` берётся ВСЕМИ ЧЕТЫРЬМЯ моментами (у разрезанной ячейки
`∫b_i dV ≠ 0` при `i ≥ 1`) — **дословно К48, понадобившаяся второй раз**;
направленная часть `eps_dir·ω` при интегрировании по сфере даёт ноль и в
тождество не входит, и это сказано, а не опущено. После правки: `3.2e-14`.

### ЧТО ЭТАП C ДАЛ И ЧЕГО НЕ ДАЛ

**Дал:** зеркальную СТЕНКУ внутри системы — энергия больше не пропадает на ней,
связь `m → m′` точна, хранимое на такой грани направленное (`ND` чисел вместо
одного, и платится только за зеркальные грани).

**Не дал:** зеркальных ФАСЕТОВ произвольной ориентации. Перестановка существует
только для девяти плоскостей октаэдрической группы, а нормаль фасета произвольна;
план это и говорит («для гранёного тела конструкция достраивается… Не
построено»). Значит зеркальные ШАРЫ рендерной сцены по-прежнему чёрные для
развёртки.

**И не отменил прослеживания (К56).** Образ в зеркале разрешён здесь по набору
ординат, то есть до `2π/ND`; видимый образ по-прежнему берётся прослеживанием в
сборе. Внутрисистемная связь нужна ЭНЕРГИИ — это две работы над одним объектом,
а не замена одной другою.

## ЭТАП E ВЫПОЛНЕН 07-28 — ЦВЕТ ЕСТЬ, КАДР НЕ ПОДОРОЖАЛ ВОВСЕ

**Вывод. Три спектральных канала считаются, все четыре записанных фальсификатора
выполнены, и кадр стоит СТОЛЬКО ЖЕ, сколько при одном канале.** `make test`
зелёный (12 наборов), `ccheck` CLEAN. Ложной раскраски скалярной величины больше
нет: `hz_pfm_write` получает три РАЗНЫХ канала.

**ПОРЯДОК: E ПОСТАВЛЕН ПЕРЕД A, И ЭТО К64.** Сильнейший фальсификатор цвета —
«три ОДИНАКОВЫХ канала обязаны дать буферы, ПОБИТОВО равные одноканальному
прогону» — опирается на неизменную конфигурацию, а A меняет её целиком. После A
эталона для него не осталось бы, и проверка свелась бы к «похоже».

**УСТРОЙСТВО: КАНАЛЫ — НЕЗАВИСИМЫЕ ЗАДАЧИ ПЕРЕНОСА.** Развёртка гоняется по разу
на канал со своими свойствами; общего состояния у каналов нет вовсе, поэтому
смешаться они НЕ МОГУТ ПО ПОСТРОЕНИЮ. Сбор при этом ОДИН: марш по лучу есть почти
вся цена кадра, геометрия у каналов общая, и на попадании читаются три числа
вместо одного.

### ФАЛЬСИФИКАТОРЫ — ЧЕТЫРЕ ИЗ ЧЕТЫРЁХ, ВСЕ ЗАПИСАНЫ 07-27 И ВЗЯТЫ ДОСЛОВНО

| # | что мерится | предсказано | измерено |
|---|---|---|---|
| Ф1 | три ОДИНАКОВЫХ канала против одноканального прогона | ПОБИТОВО | **`0.000000e+00`** во всех метриках |
| Ф2 | канал без источников при светящихся остальных | РОВНО ноль | **`итераций 0, невязка 0.00e+00`; в файле `B = 0`** |
| Ф3 | удвоение излучения канала удваивает его ответ | точно | **`G/R = 2`**, невязка тоже ровно удвоилась |
| Ф4 | баланс энергии сходится НА КАЖДЫЙ канал | как при одном | `5.2e-14`, `5.1e-13`, `0` |

Ф2 стоит отдельного слова: канал без источников даёт `итераций 0` — то есть
задача распознана как тождественно нулевая, а не «сошлась к малому». Это и есть
«не могут смешаться по построению», предъявленное числом.

### ЦЕНА: КАДР ДАРОМ, ХОЛОДНЫЙ СТАРТ `×2.36` — А ПРЕДСКАЗАНО БЫЛО `×1.5…×1.6`

| | 1 канал | 3 канала | |
|---|---|---|---|
| развёртка | 2.552 с | 6.806 с | **`×2.67`** |
| **кадр (сбор по пикселю)** | 0.429 с | 0.412 с | **`×0.96` — даром** |
| холодный старт целиком | 3.124 с | 7.363 с | **`×2.36`** |
| память хранимого | | | `×3`, как и обещано |

**Кадр не подорожал вовсе, и это ровно то, что обещало устройство:** один марш,
три чтения. Для движущейся камеры цвет БЕСПЛАТЕН.

**А `×1.5…×1.6` не сбылись, и причина названа заранее в самом же плане.** Та
оценка опиралась на разделение матрицы ячейки между каналами («если среда СЕРАЯ,
`A` для трёх каналов одна, собирается и раскладывается ОДИН раз, решается для
трёх правых частей»), что давало `×2.2` по развёртке. **Выбранное устройство от
этого отказывается СОЗНАТЕЛЬНО:** общего состояния нет, значит и общей матрицы
нет, и 39% профиля, которые могли бы считаться однажды, платятся трижды.

**Это размен, и обе его стороны надо называть вместе:** `×1.2` лишней цены на
развёртке в обмен на невозможность смешения каналов по построению. При цветной
среде (`σ_t` свой на канал) разделения не было бы всё равно — план это отмечал.

### К90. `×1.5…×1.6` БЫЛИ ПОСЧИТАНЫ ДЛЯ ДРУГОГО УСТРОЙСТВА, ЧЕМ ВЫБРАНО

Формально противоречия нет: разложение профиля 07-27 верно для варианта с общей
матрицей, а выбран вариант с независимыми задачами. **Но в плане они стояли
рядом как оценка И решение, будто относятся к одному и тому же.** Цитировать
`×1.5…×1.6` применительно к принятому устройству было нельзя — и сам файл это
предупреждал («установленным считать нельзя, пока цвет не сделан и не замерен»).
Теперь замерено: `×2.36`.

**Класс тот же, что К51 и К82:** величина верна в своём контексте и перенесена в
чужой. Здесь чужим контекстом оказалось СОБСТВЕННОЕ архитектурное решение,
принятое позже оценки.

## ЭТАП B ВЫПОЛНЕН 07-28 — `src/transport/krylov3.{c,h}`: `×2.9`, И ДВЕ ОШИБКИ ПО ДОРОГЕ

**Вывод. Крылов на рендерной сцене даёт 23 прохода развёртки против 66 у ряда
Неймана** — `×2.87` по проходам и `×2.99` по времени. `make test` зелёный (12
наборов), `ccheck` CLEAN. **Предсказание (`6…12` проходов, `×5…×9`) НЕ СБЫЛОСЬ:**
измерения на печи не переносятся, там было шесть проходов при любой оптической
толщине, здесь — двадцать три.

**НЕИЗВЕСТНОЕ — ВСЁ СОСТОЯНИЕ `(φ, bout, sout)`, А НЕ ОДНО `φ`.** Прежний опыт в
`test_sweep3.c` применял оператор к `φ` и выбрасывал поверхности; в самом файле
стоит оговорка «для сцен с отражением так НЕЛЬЗЯ», и она верна: картинка делается
из поверхностей (К84). Длина вектора здесь `4·(ncell + nf + nse)`.

### ПРИБОР ПОСТАВЛЕН ДО СПОРА О ЧИСЛАХ, И ДВАЖДЫ ПОКАЗАЛ, ЧТО СПОРИТЬ РАНО

Заведена `tr3_krylov_residual`: невязка ЗАДАННОГО состояния под тем же оператором.
Требование простое — **сошедшийся ответ ряда Неймана обязан давать здесь ноль**.
Он давал `1.000`, и это дважды вскрыло настоящую ошибку.

### К86. НЕЛИНЕЙНЫХ ПЕРЕКЛЮЧАТЕЛЕЙ ДВА, А ФЛАГ УПРАВЛЯЛ ОДНИМ

В проекции хранимого стояло `if (tr3_project_plane(...) != 0 || corner_min(ee) < 0)`
— **безусловно, в обход `p->limiter`**. То есть выключение ограничителя оператор
линейным НЕ ДЕЛАЛО, и весь довод этапа B («ограничитель выключается, потому что
Крылову нужен линейный оператор») был исполнен наполовину.

**Разделение принципиальное, а не косметическое.** Вырождение элемента есть
свойство ГЕОМЕТРИИ: множество вырожденных элементов от поля не зависит, и откат на
них ЛИНЕЕН. `corner_min < 0` есть свойство ПОЛЯ, то есть переключатель, и он
обязан подчиняться `p->limiter` наравне с объёмным. Штатный путь (ограничитель
включён) при этом не изменился: 56 итераций, невязка `2.48e-09`, срезок 458,
баланс тот же до последнего знака.

### К87. `S(0)` В ХОЛОДНОМ СТАРТЕ — НЕ ОТОБРАЖЕНИЕ, ПРИМЕНЁННОЕ К НУЛЮ

Правая часть системы есть `b = S(0)`, и взять её холодным стартом соблазнительно.
**Неверно: холодный старт СНАЧАЛА кладёт `bout = emit` и лишь потом идёт
развёрткой**, то есть считает `S` применённым к состоянию `(0, emit)`, а не к
нулевому. Лишние полшага.

**Чем это опасно, а не просто неточно:** Крылов при такой правой части сходился
**безупречно — невязка `7e-10` — к состоянию с `φ ≈ 0`**, то есть решал НЕ ТУ
задачу и рапортовал об успехе. Поймано только прибором и встроенной проверкой
«ответы обязаны совпасть»; ни число проходов, ни невязка на это не намекали.

Правильно: правая часть считается ТЕМ ЖЕ путём, что оператор (`warm_start = 1`,
поверхности поданы явно нулями), только источники живые.

### ИЗМЕРЕНО ПОСЛЕ ОБЕИХ ПРАВОК

| проверка | предсказано | измерено |
|---|---|---|
| невязка ответа НЕЙМАНА под оператором Крылова (прибор) | ≈0 | **`4.22e-10`** |
| невязка ответа Крылова | ≤1e-9 | `5.12e-10` |
| **совпадение ответов**, `max|Δφ|` при `|φ| = 50.4` | до пола невязки | **`4.36e-09`** (отн. `8.6e-11`) |
| то же по `bout` при `|bout| = 6.09` | | **`1.28e-10`** |
| **проходов развёртки** | 6…12 | **23 против 66** |
| время | `×5…×9` | **`×2.99`** |

**НЕГАТИВНЫЙ КОНТРОЛЬ ВЫСТРЕЛИЛ ДВАЖДЫ И ОБА РАЗА БЫЛ ЕДИНСТВЕННЫМ УКАЗАНИЕМ:**
при не той правой части ответы расходились на `max|Δφ| = 50.4` при `|φ|max = 50.4`
(то есть `φ ≈ 0`), при неразделённых переключателях прибор давал `1.000`. Оба раза
метод «сходился», и оба раза — не к тому.

### К88. ДОПУСК РЕШАТЕЛЯ НА ДЕВЯТЬ ПОРЯДКОВ ТУЖЕ ОШИБКИ ДИСКРЕТИЗАЦИИ — И ЭТО `×5.6` НА ПОЛУ

**Заведена при попытке закрыть долг этапа B, и оказалась крупнее самого этапа.**

Наивный внешний нелинейный цикл считается на бумаге и не проходит: по закону К85
старт с линейного решения даёт `e₀ ≈ 0.002·50 = 0.1` вместо 37, то есть экономию
`ln(370)/ln(1/0.63) ≈ 13` итераций — `56 → 43`, плюс 23 прохода Крылова, итого
ХУЖЕ голого ряда Неймана. Причина не в цикле, а в допуске: **`1e-9` при `φ ≈ 50`
есть `2e-11` ОТНОСИТЕЛЬНЫХ, тогда как ошибка дискретизации на `16³` измерена в
`3.0e-2` (К68).** Система решается на девять порядков точнее, чем имеет смысл, и
платится за это итерациями. Число назначено, а не выведено, — четвёртый класс
аудита, тот же, что `ε` в К68.

**ИЗМЕРЕНО** (эталон — допуск `1e-11`; метрика на буфере радианса, вне кромок):

| допуск | итераций | ошибка картинки, отн. | против дискретизации `3.0e-2` |
|---|---|---|---|
| `1e-9` | 56 | `0` | — |
| `1e-6` | 33 | `4.4e-8` | ниже в `7e5` раз |
| `1e-4` | 24 | `4.2e-6` | ниже в 7000 раз |
| `1e-3` | 20 | `3.2e-5` | ниже в 900 раз |
| `1e-2` | 15 | `4.0e-4` | ниже в 75 раз |
| **`1e-1`** | **10** | **`5.0e-3`** | **ниже в 6 раз** |

Ошибка картинки строго пропорциональна допуску (`≈ tol/20`), то есть закон
простой и предсказание по нему возможно. **`56 → 10` итераций есть `×5.6`, и
куплено оно тем, что решение перестало быть точнее собственной сетки.**

**ПРАВИЛО, А НЕ ЧИСЛО:** допуск обязан задаваться ДОЛЕЙ от оценённой ошибки
дискретизации, а не константой. Оценка берётся по Ричардсону из свипа сетки (К68
измерила порядок — первый), и тогда допуск сам следует за сеткой: на `64³`
дискретизация меньше, и допуск обязан ужесточиться.

### К89. ДОПУСК И КРЫЛОВ — ЗАМЕЩАЮЩИЕ РЫЧАГИ, А НЕ СКЛАДЫВАЮЩИЕСЯ

| допуск | Нейман, проходов | Крылов, проходов | выигрыш Крылова |
|---|---|---|---|
| `1e-9` | 66 | 23 | `×2.87` |
| `1e-4` | 25 | 14 | `×1.79` |
| `1e-2` | 16 | 11 | `×1.45` |
| `1e-1` | 11 | 9 | **`×1.22`** |

**Преимущество Крылова тает вместе с допуском, и это следует прямо из К85:** оба
рычага бьют в одну величину `ln(tol/e₀)/ln(ρ)` — допуск через `tol`, Крылов через
`ρ`. При грубом допуске цифр не хватает, чтобы асимптотическое преимущество
`√κ` против `κ` проявилось, а BiCGStab ещё и платит два прохода за итерацию.

**Считать `×5.6 × ×2.9` нельзя.** Полное измеренное: `56 → 9` проходов, то есть
`×6.2`, из которых `×5.6` даёт допуск и лишь `×1.1` добавляет Крылов.

**И ОТСЮДА ЖЕ ЗАКРЫВАЕТСЯ ДОЛГ ЭТАПА B.** Открытым оставалось то, что Крылов
сходится к неограниченной неподвижной точке (К65: расхождение до 6.8%), а
внешнего нелинейного цикла нет. **При допуске, который нужен картинке, цикл не
нужен вовсе: ряд Неймана С ОГРАНИЧИТЕЛЕМ стоит 10 итераций против 9 проходов
Крылова.** То есть нелинейная задача решается напрямую и дешевле, чем линейная с
последующей починкой.

### И ТОГДА ЧЕМ ЖЕ ЦЕНЕН ЭТАП B — СВИП ПО АЛЬБЕДО

Множителем на этой сцене он не ценен. Ценен он тем, ради чего и заводился, и это
измерено ЗДЕСЬ, а не на печи (допуск `1e-4`):

| множитель альбедо стенок | Нейман | Крылов |
|---|---|---|
| `×1.0` | 25 | 14 |
| `×1.15` | 30 | 15 |
| `×1.3` | 39 | 17 |
| `×1.37` | **44** (`×1.76` от базы) | **17** (`×1.21` от базы) |

**Цена Крылова почти не растёт с альбедо, а цена ряда Неймана растёт.** Это
дословно формулировка пользователя от 07-28 («главное не множитель, а то, что
цена не растёт»), теперь воспроизведённая на сцене с отражением, а не на печи.
Для белой комнаты и снега (С1: сотни итераций при `ρ → 1`) этап B остаётся
единственным ответом; для нынешней сцены с `ρ = 0.573` он почти не нужен.

### ЧТО ЭТАП B ДАЛ И ЧЕГО НЕ ДАЛ

**Дал:** первый рычаг, который реально двигает число (`×2.9`), и он складывается со
всем прочим. Плюс две настоящие ошибки в коде и прибор, которым такие ошибки видно.

**Не дал обещанного `×5…×9`.** Печь и рендерная сцена — разные спектры, и цитировать
«шесть проходов при любой толщине» применительно к сцене с отражением было нельзя.
Это ровно К71/К82 в третий раз: замер, снятый в одних условиях, перенесён в другие.

**ОСТАЁТСЯ ОТКРЫТЫМ, ЯВНО:** Крылов сходится к неподвижной точке БЕЗ ограничителя,
а К65 измерила, что она отличается от ограниченной на 0.2% в среднем и 6.8% в
худшем пикселе. Внешний нелинейный цикл поверх линейного Крылова НЕ СДЕЛАН, и без
него `×2.9` куплены сменой ответа. Это названо, а не спрятано.

### К84. НЕВЯЗКА НЕ СМОТРЕЛА НА ТО, ИЗ ЧЕГО ДЕЛАЕТСЯ КАРТИНКА — И ЭТО ДАЛО ЛОЖНОЕ УСКОРЕНИЕ `×47`

**Найдена НЕГАТИВНЫМ КОНТРОЛЕМ замера 1c, и без него была бы доложена как
блестящий результат.** В опыт «тёплый старт против холодного» встроена проверка
«оба решения обязаны СОВПАСТЬ» — и она выстрелила: тёплый прогон останавливался
на НУЛЕВОЙ итерации, показывал `×47` и возвращал СТАРОЕ решение.

    возмущение света  0.1    max|Δφ| 5.037e+00 при |φ| 55.4
    возмущение света  0.01   max|Δφ| 5.037e-01
    возмущение света  0.001  max|Δφ| 5.037e-02

Расхождение строго линейно по возмущению — то есть ответ просто НЕ ОТРЕАГИРОВАЛ
на изменение света.

**МЕХАНИЗМ СТРУКТУРНЫЙ.** Состояние итерации есть тройка `(φ, bout, sout)`, и
обновляются они ПО ОЧЕРЕДИ: развёртка считает `φ` по СТАРЫМ поверхностям, и лишь
потом поверхности пересчитываются. Значит изменение, вошедшее через поверхности,
доходит до `φ` только на СЛЕДУЮЩЕЙ итерации, а критерий смотрел на одно `φ` и
объявлял сходимость раньше времени.

**И ВТОРОЙ ДОВОД, СИЛЬНЕЕ ПЕРВОГО: КАРТИНКА ДЕЛАЕТСЯ ИЗ `bout`/`sout`, А НЕ ИЗ
`φ`.** Сбор читает хранимый на поверхностях радианс; `φ` в изображение не входит
вовсе. Критерий остановки, который на них не смотрит, **не измеряет то, ради чего
решение считается**. Это третья подпись артефакта в чистом виде, и держалась она
с самого шага C.

**Правка:** невязка берётся по ВСЕМУ состоянию. Цена — `O(nf + nse)` на итерацию,
то есть ничто. Следствия измерены и названы:

| | было | стало |
|---|---|---|
| холодный старт `16³` | 47 итераций, `7.73e-10` | **56**, `2.48e-09` (по ЗАСТОЮ) |
| тёплый против холодного | `max|Δφ| 5.04` — РАЗНЫЕ ответы | **`3e-11…2e-10`** при `|φ| ≈ 50` |
| исходов у остановки | два (допуск / `maxit`) | **три: допуск, ЗАСТОЙ, `maxit`** |

У поверхностной части свой ПОЛ, выше чем у `φ`, поэтому теперь и `16³`
останавливается по застою. Тест К38 переписан: он утверждал «сошлось по допуску»,
а законных исходов два — допуск и застой, — и незаконен один, упор в `maxit`.
Сливать их нельзя (Г38).

### К85. ТЁПЛЫЙ СТАРТ НЕ РЕШАЕТ «НА ГРАНИ» — ЗАМЕР ОПРОВЕРГ МОЁ СОБСТВЕННОЕ ОБЕЩАНИЕ

К76 утверждала: «если малое изменение света обходится в 1…2 прохода вместо 6,
кадр стоит `8…17 мс`, и „на грани" становится 60 кадрами». **Это неверно, и
неверно было выведено.** Число итераций геометрической сходимости есть
`ln(tol/e₀)/ln(ρ)`; тёплый старт уменьшает ТОЛЬКО `e₀`, а показатель `ρ` не
трогает. При `ρ = 0.63`, `tol = 1e-9` и `φ ≈ 50` возмущение `δ` даёт экономию
`ln(50/(δ·50))/ln(1/0.63)` итераций — около 5 при `δ = 0.1` и 15 при `δ = 0.001`,
то есть `×1.1…×1.4`, а не `×20`. Ошибка была в том, что «сколько итераций нужно,
чтобы сократить ошибку в `δ` раз» подменяло «сколько нужно, чтобы достичь допуска».

**Предсказание в исправленном виде записано ДО прогона; измерено:**

| возмущение света | холодный | тёплый | ускорение |
|---|---|---|---|
| 10% | 59 | 61 | **×0.93** |
| 1% | 62 | 48 | ×1.37 |
| 0.1% | 50 | 40 | ×1.27 |

**Практически ничего**, а при 10% даже медленнее (разброс холодных прогонов
59/62/50 сам по себе больше эффекта — остановка по застою шумит).

**ЧТО ИЗ ЭТОГО СЛЕДУЕТ ДЛЯ ЭТАПА B, И ЭТО ГЛАВНОЕ.** Рычаг, который решает
«на грани», обязан менять `ρ` либо допуск, а не `e₀`. Крылов меняет именно `ρ`
(измерено: шесть проходов при ЛЮБОЙ оптической толщине), и потому он остаётся
единственным кандидатом. Тёплый старт из плана НЕ ВЫЧЁРКИВАЕТСЯ — он бесплатен и
его `×1.3` складывается со всем прочим, — но его роль понижается с «решающей» до
«попутной».

### К82. «5 МИНУТ» ОТНОСЯТСЯ К ИГРУШКЕ; НА ГОРОДЕ ЧАСЫ ВОЗВРАЩАЮТСЯ, И ПЕРВЫМ УПИРАЕТСЯ НЕ ВРЕМЯ

**Заведена по возражению пользователя, и возражение верное:** К81 убрала ДЕФЕКТ
(3931 бесполезная итерация), а не барьер масштабирования. Сцена `64³` с двумя
шарами — не достижение, и выдавать её `5:08` за ответ на вопрос о городе нельзя.

Счёт по ИЗМЕРЕННОЙ цене: `4365` мс на итерацию при `262144` ячейках × 32 ординаты
есть **520 нс на обновление ячейки** (одно ядро, FP64, без векторизации). Город по
цифре `CLAUDE.md` — `5e8` элементов при `ε` = пиксель:

| конфигурация | одно ядро, FP64, скалярно |
|---|---|
| как записано: 32 ординаты, 69 итераций | **159 часов** |
| + 3 пикселя на элемент (`÷7`, К68) | 22.8 ч |
| + огрубление заэкранного (`÷6`) | 3.8 ч |
| + Крылов (69 → 6 проходов) | **20 мин** |
| + `P1` (32 → 4 угловых, Р2) | 2.5 мин |
| + Р6 (сетка косвенной `÷8`) | **19 с** |

**ПАМЯТЬ: ПОПРАВЛЕНО ПО ВОЗРАЖЕНИЮ ПОЛЬЗОВАТЕЛЯ, И ПОПРАВКА СУЩЕСТВЕННАЯ.** Здесь
стояло «`8.4` ГБ не влезает в 6 ГБ видеопамяти», и это было **взято ограничение
ЭТОЙ МАШИНЫ и подано как барьер МЕТОДА**. `24` ГБ (RTX 3090/4090, A5000) — не
топовые карты, а середина верхнего сегмента. `1.2e7` элементов дают `8.4` ГБ в
FP32 и `17` ГБ в FP64, **и то и другое в 24 ГБ влезает**, FP32 — с запасом втрое.
Барьера памяти у города нет; он есть у Quadro RTX 3000 с её шестью гигабайтами,
и это ограничение РАЗРАБОТЧЕСКОЙ машины (К79), а не архитектуры.

Что остаётся верным: узким местом на городе становится **ПРОХОД 1**, которого нет
ни в одной таблице цены (К78), а у города источников не один.

### К83. ПАМЯТЬ НА ШЕСТЬДЕСЯТ ПРОЦЕНТОВ ЗАНЯТА ТЕМ, ЧТО НЕ НАДО ХРАНИТЬ

**Заведена при пересчёте по К82.** Замер: `64³` даёт RSS `491` МБ. Разложение по
объявленным массивам:

| что | размер на `64³` | доля |
|---|---|---|
| **`ffm`, `ffmb`, `ffmx` — по три матрицы `4×4` на КАЖДУЮ грань** | **292 МБ** | **60%** |
| `mvol` — матрица масс флюида на ячейку | 32 МБ | 7% |
| `phi` — само поле | 8 МБ | 2% |
| прочее (сетка граней, боковая таблица, `recs`) | ~159 МБ | 31% |

**Матрицы масс граней зависят ТОЛЬКО ОТ ГЕОМЕТРИИ, и у 96% граней геометрия
одинакова.** Разрезанных ячеек на `64³` — 8650 из 262144, то есть режущихся
граней около четырёх процентов; у остальных грань есть целая сторона коробки, и
её матрица масс аналитична и **одна и та же для всех граней данного уровня и данной
оси**. На равномерной сетке различных матриц ТРИ, а хранится их восемьсот тысяч.

**Следствие для города прямое:** отказ от хранения даёт `×2.5` по памяти сразу
(`8.4 → 3.4` ГБ в FP32), а по К73 задача пропускно-ограничена — значит это ещё и
`×2.5` по ВРЕМЕНИ, потому что трафик и есть цена. Ни один из прочих рычагов так
дёшево не даётся: это не новая физика и не новая схема, а отказ хранить константу.

*Фальсификатор:* картинка обязана совпасть ПОБИТОВО — это перестановка данных, а
не изменение расчёта (тот же критерий, что у пункта «индекс в марше»).
*Негативный контроль:* подставить матрицу НЕ ТОЙ оси ⇒ баланс энергии обязан
сломаться на `O(1)`. Если не сломался — матрицы граней в решение не входят, и
меряется не то.

**И ОГОВОРКА, БЕЗ КОТОРОЙ ВСЯ ТАБЛИЦА — САМООБМАН.** Она построена на том, что
город стоит `×2.5` от комнаты, а не `×1000`. Это верно ТОЛЬКО если работает LOD,
а он не сделан вовсе: все прогоны идут на равномерной сетке. Измерено пока лишь
то, что закон выходной ограниченности держится **на счётчике ячеек** (П2: сцена
`×262 144` по объёму → элементов `×26`), а не на решении. Плюс городу нужны все
три недостающих куска представления из К69 — объединение тел, гомогенизация,
критерий по просвету.

**Правильная формулировка достижения, а не подмена:** убран множитель `×55`,
который принадлежал ОШИБКЕ, и заодно получен прибор (история невязки), которым
такие ошибки видно сразу. Барьер города при этом не тронут ни на шаг.

### ЧТО ЭТОТ ШАГ ГОВОРИТ ОБ АУДИТЕ, КОТОРЫЙ ЕМУ ПРЕДШЕСТВОВАЛ

Двадцать девять находок аудита были получены рассуждением. **Три замера нашли то,
чего рассуждение не нашло и не могло:** ошибку в единицах (К80), пол невязки
(К81) и первый порядок вместо второго (К68). Все три видны только тогда, когда
эталон не сошёлся, — ровно как записано в выводе после четвёртого аудита 07-27:
«оставшиеся ошибки такого сорта, что рассуждением их не выловить». **Соотношение
цены при этом обратное ожидаемому: аудит стоил дня рассуждений, замеры — часов
счёта, и нашли они разное.**

## СЛЕДУЮЩАЯ РАБОТА — ПОРЯДОК, ЦЕНА И ЧТО ИЗВЕСТНО ЗАРАНЕЕ

Записано 07-27 перед сбросом контекста. Порядок не вкусовой: он по ВЕЛИЧИНЕ
МНОЖИТЕЛЯ, и пока стоит первый, остальные лечат не ту болезнь.

### 1. ~~DSA~~ — ПУНКТ ЗАКРЫТ НЕ ТЕМ, ЧЕМ ПЛАНИРОВАЛСЯ (см. доклад К38/К39 выше)

**ЭТА ОЦЕНКА БЫЛА НЕВЕРНА, И ОШИБКА ЗАПИСАНА, А НЕ ЗАТЁРТА.** «4000 итераций при
невязке 1.25» здесь прочитано как медленная сходимость, а на деле это было
автоколебание нелинейного отката поверх вырожденной проекции (К38/К39).
Множитель ×130 принадлежал ОШИБКЕ В КОДЕ, и после правки холодный старт стал
`1.93 с` за 47 итераций.

DSA остаётся нужным, но его собственный множитель ИЗМЕРЕН и он другой: печь при
альбедо ровно 1 берёт 156 итераций на равномерной сетке и 260 на градуированной,
то есть ×3…×5 в светлой комнате. Пункт переезжает ПОСЛЕ 2 и 3.
Записанное ниже — по-прежнему верно и пригодится, когда до него дойдёт черёд.

**ЧТО ИЗВЕСТНО ЗАРАНЕЕ, ЧТОБЫ АУДИТ НЕ НАЧИНАЛСЯ С НУЛЯ:**

- **С2 уже записала цену:** разгон возвращает ГЛОБАЛЬНУЮ связную систему —
  диффузия эллиптична. Честная формулировка оттуда: связная система есть либо 80
  на ячейку (без разгона), либо ОДИН скалярный эллиптический солв на всей сетке,
  хорошо обусловленный и решаемый многосеточно, но глобальный;
- **ГЛАВНАЯ ЛОВУШКА, КОТОРУЮ НАДО ЗАПИСАТЬ В АУДИТ ДО КОДА:** синтетический
  разгон обязан быть СОГЛАСОВАН С ТОЙ ЖЕ ДИСКРЕТИЗАЦИЕЙ. Диффузионный оператор,
  выписанный «независимо и разумно», при высоком альбедо и оптически толстых
  ячейках не ускоряет, а РАСХОДИТСЯ — это классический результат (Alcouffe,
  consistent differencing). Признак в прогоне: разгон помогает на тонких ячейках
  и ломается на толстых;
- **К4 действует:** для АНИЗОТРОПНОЙ среды коэффициент диффузии тензорный, шесть
  чисел. Сегодня среда изотропна, и скалярного разгона довольно — но это надо
  назвать ограничением, а не забыть;
- **фальсификатор очевиден и дёшев:** та же печь и та же замкнутая полость
  (`L_e/(1−ρ)`) обязаны сойтись К ТОМУ ЖЕ ответу, но за десятки итераций вместо
  тысяч. Негативный контроль: разгон, выписанный НЕсогласованно, обязан
  разойтись на толстых ячейках — если не разошёлся, тест не добрался до места.

### ЗАМЕРЫ ПОТОЛКОВ ДЛЯ ПУНКТОВ 2 И 3, СДЕЛАННЫЕ 07-27 ДО РАБОТЫ

**Оба записанных множителя проверены профилем ПРЕЖДЕ, чем начата работа, и
первый из них оказался завышен. Урок шага 1 применён: потолок меряется до, а не
после.**

**ПУНКТ 2 (индекс в марше): потолок ×1.85, а не ×10.** Профиль кадра FullHD
раскладывается так:

| часть сбора | доля кадра |
|---|---|
| `tr3_march` сам | 37% |
| `hz_cutmap_hspaces` — разворачивание полуплоскостей | 31% |
| `hz_oct_leaf_box` — спуск по дереву | 17% |
| `hz_cutmap_find` — двоичный поиск | 15% |

Прямой индекс убирает поиск и разворачивание, то есть 46% — ровно ×1.85.
Спуск по дереву (ещё 17%) прямым индексом по мелкой сетке лечить НЕЛЬЗЯ: такой
индекс ограничен самым мелким уровнем по всей области, то есть тем плотным
массивом, ради отсутствия которого октодерево и существует. Его лечат связи
соседей (ropes) — отдельная работа. **По решению пользователя пункт отложен:
ради ×1.85 он не стоит правки.**

**ПУНКТ 3 (плотный внутренний цикл): узкое место НЕ ТАМ, где записано.**
Замерено `perf stat`: IPC = 2.23, промахов LLC 4.6 млн на весь прогон, то есть
упор НЕ В ПАМЯТЬ. А вот команд на одно обновление ячейки — **3380** против
записанных в плане «примерно 500». То есть кандидаты из плана (топологическая
сортировка, `solve4`, обход граней через CSR) названы, а САМА ВЕЛИЧИНА
недооценена в семь раз, и прежде чем уплотнять цикл, надо понять, откуда семь
крат. Топологическая сортировка при этом заведомо ни при чём: она стоит
`32 × (4096 + 13056)` операций на итерацию против `6.2·10⁶ × 3380` в самих
обновлениях, то есть около 1%. **Значит и оговорка про Р1 («кэш порядков стоил
бы ND×ncell») ничего не защищает — защищать там нечего.**

### 2. ИНДЕКС В МАРШЕ. ~~Множитель ~×10 по КАДРУ~~ — ИЗМЕРЕНО ×1.85, ОТЛОЖЕНО

`1133 нс` на луч против ~100 у трассировщика. Остаток — двоичный поиск в боковой
таблице (`hz_cutmap_find`) и разворачивание полуплоскостей на КАЖДУЮ ячейку вдоль
луча. Лечится тем же приёмом, что К36: прямой индекс «ячейка → запись» вместо
поиска. Фальсификатор: картинка обязана совпасть С ПРЕЖНЕЙ ПОБИТОВО — это
перестановка данных, а не изменение расчёта.

### 3. ПЛОТНЫЙ ВНУТРЕННИЙ ЦИКЛ. Множитель ~×5…10 по ХОЛОДНОМУ СТАРТУ

`305 нс` на обновление ячейки при ~500 операциях в нём. Кандидаты: топологическая
сортировка перестраивается каждую итерацию (оставлено сознательно ради Р1 —
кэш стоил бы `ND×ncell`; но её можно считать ОДИН раз на направление и хранить
`nd` порядков ТОЛЬКО если Р1 будет пересмотрен явно); `solve4` с полным выбором
на матрице известной структуры; обход граней через CSR с косвенностью.

### 4. ПОТОМ, И НЕ РАНЬШЕ

Параллелизм (`×4.3` на 16 ядрах, структура готова, включается одной строкой);
глянец (Р3 + К3); зеркала и преломление (К5 — через ординаты НЕ идут вовсе,
нужен отдельный проход прослеживанием); цвет (сейчас один спектральный канал).

### ПОРЯДОК НА 07-28, ПЕРЕД СБРОСОМ КОНТЕКСТА

**РЕШЕНИЕ ПОЛЬЗОВАТЕЛЯ: сперва КОРРЕКТНОСТЬ целиком, оптимизация потом.**
Нынешнее время рендера (холодный старт 4.2 с целиком, кадр 2.2 с) для отладки
названо комфортным. Дальняя цель — **движущаяся камера, как в игрушках**, но
идти к ней после того, как всё считается верно.

Порядок по УЩЕРБУ КАРТИНКЕ, а не по размеру правки:

1. **К27 — марш берёт ТОЛЬКО ПЕРВЫЙ примитив из веера ячейки.** Это корень
   дефекта «ячеек с ДВУМЯ телами 2»: фасеты второго тела молча выбрасываются,
   часть шара ОТСУТСТВУЕТ, и свет сквозь неё проходит. Единственный из открытых,
   который портит ВИДИМУЮ геометрию. Баланс К40 к нему слеп (дефект
   геометрический, а не энергетический), тест T5а его ОБХОДИТ проверкой
   `both == 0` — то есть ограничение не просто не покрыто, а обойдено.
   Фальсификатор готов: луч сквозь две КАСАЮЩИЕСЯ сферы обязан попасть в
   БЛИЖНЮЮ. Лечение: перебирать все различные `surf` веера и брать ближайшее
   попадание; заодно снимает ограничение «одно выпуклое тело на ячейку».
2. **К45 — сбор не ослабляет радианс средой на последнем отрезке.** `sigma`
   передаётся в сцену марша как `NULL`, хотя среда есть и по комнате `τ ≈ 0.24`,
   то есть до 20% на дальней стенке. **Лечить ТОЛЬКО вместе с вкладом среды в
   луч** (внутреннее рассеяние по пути): одно ослабление без подсветки сделает
   картинку темнее правильной, а не точнее.
3. **К28 — длина `sigma` нигде не сверяется**, соглашение вместо проверки.
   Дёшево, и это ровно тот класс, который CLAUDE.md велит ловить санитайзером.
4. **К41/К42 — положительность и откат.** Положительность хранимого проверяется
   по углам ЯЧЕЙКИ, а не по самому элементу (консервативно, но срезает наклоны
   раньше нужного: запас `0.6c` вместо `c`), и откат ЖЁСТКИЙ — до константы
   целиком, тогда как в объёме уже есть срезка коэффициентом `α` в замкнутой
   форме. Сегодня срабатывает раз на 13732 элемента, так что цена мала.
5. **Цвет (RGB).** Устройство выбрано и записано: каналы — НЕЗАВИСИМЫЕ задачи
   переноса, поэтому развёртка гоняется три раза с разными наборами свойств, а
   сбор остаётся ОДИН (марш общий, на попадании читаются три числа). Так каналы
   не могут смешаться ПО ПОСТРОЕНИЮ. Фальсификаторы намечены: три ОДИНАКОВЫХ
   канала обязаны дать буферы, побитово равные прежнему одноканальному прогону;
   канал без источников при светящихся остальных обязан быть РОВНО нулём;
   удвоение излучения канала удваивает его ответ точно; баланс сходится НА
   КАЖДЫЙ канал; полость с разными `ρ` даёт `L^c = L_e^c/(1−ρ^c)`.
6. **Потом, и не раньше:** оптимизация (шаги 2 и 3 старого порядка, потолки
   измерены: индекс в марше ×1.85, плотный цикл — запас в 6.35 команды на
   вещественную операцию), параллелизм, DSA для высокого альбедо (×3…×5,
   измерено печью: 156 итераций на равномерной сетке, 260 на градуированной).

### ОТКРЫТЫЕ ХВОСТЫ, КОТОРЫЕ НЕЛЬЗЯ ПОТЕРЯТЬ

**ТРИ СТРОКИ НИЖЕ ПЕРЕСТАЛИ БЫТЬ ХВОСТАМИ ПО К59/К60** и стали предусловиями
этапа A: **Г50** (перепада уровней сегодня нет вовсе, A создаёт его повсюду),
**К21** (число разрезанных ячеек растёт на порядки) и **К50** (A решает её молча,
в пользу многогранника, ценой точного силуэта). Оставлены здесь для полноты, но
работа по ним принадлежит A.

| # | что | где |
|---|---|---|
| К28 | длина `sigma` нигде не сверяется — соглашение вместо проверки | `ray3.h` |
| — | `render3` печатает «ячеек с ДВУМЯ телами 2 (обязано быть 0)»: тела стоят слишком близко, и модель «материал = пересечение полуплоскостей» там неверна. Либо развести тела, либо поддержать объединение | `tools/render3.c` |
| Г50 | побитовость на общей грани у соседей РАЗНЫХ уровней не выполняется (`3.6e-13`); лечение — вершина из трёх плоскостей по Крамеру | `PLAN_CUT.md` |
| Г40/Г44 | `dmax` у фасетов из объёмных данных не вычисляется; волновая линия обязана от них отказываться | `PLAN_CUT.md` |
| К41 | положительность хранимого проверяется по углам ЯЧЕЙКИ, а не по самому элементу: консервативно, но срезает наклоны раньше нужного (`0.6c` вместо `c`) | `sweep3.c` |
| К44 | эталон с ОДНОРОДНЫМ точным решением слеп к тем степеням свободы, что в однородности не участвуют: печь не видит наклонов (К12) и передачи потока (К13), печь для зеркал не видит НОРМАЛИ | `test_gather3.c` |
| — | «ячеек с ДВУМЯ телами 2»: строитель сцены кладёт в веер только ОДНО тело. Марш теперь умеет несколько (К27 закрыта), но модель реза не умеет ОБЪЕДИНЕНИЯ — материал есть ПЕРЕСЕЧЕНИЕ полуплоскостей. Класть оба тела нельзя, пока нет объединения | `render3.c`, `poly3.c` |
| — | ЦВЕТА НЕТ: один спектральный канал, на картинке ложная раскраска. Настоящий RGB = три канала; цена по профилю `×1.5…×1.6` по старту (геометрия и марш ОБЩИЕ), но `×3` по памяти хранимого | вся линия |
| К46 | отношение холодного старта к кадру есть отношение размера СЦЕНЫ к размеру КАРТИНКИ, а не свойство метода: 7.5 на 640×480 и 1.2 на 4K. Обещание «~10³» снято до T7 | `PLAN_TRANSPORT.md` |
| К50 | сбор считает тело ПРИМИТИВОМ (К15), а развёртка — МНОГОГРАННИКОМ (`se.n` из плоскости фасета): хранимый радианс посчитан на одном теле, читается на другом. Отсюда огранка на диффузных шарах при точном силуэте | `cut3.c` |
| К45 | сбор не ослабляет радианс средой на последнем отрезке (`sigma = NULL`), хотя по комнате `τ ≈ 0.24`; лечится вместе с вкладом среды в луч | `render3.c` |
| К42 | откат проекции ЖЁСТКИЙ (до константы целиком), тогда как в объёме уже есть срезка коэффициентом `α` в замкнутой форме (T2) | `sweep3.c` |
