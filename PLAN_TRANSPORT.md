# PLAN_TRANSPORT.md — вторая линия: уравнение переноса

Начат 2026-07-27. Волновая линия (PLAN.md, `src/` кроме `src/transport/`)
ОСТАЁТСЯ и не сворачивается: она нужна для голограмм и она же будет поставлять
сюда BRDF. Здесь — отдельная линия, отдельные допущения, отдельные эталоны.

## ЗАЧЕМ ЭТА ЛИНИЯ ВООБЩЕ ПОЯВИЛАСЬ

Не потому что «волновое не получилось», а потому что за 07-26/27 измерена
конкретная величина, которая делает волновую постановку неприменимой к сценам с
тепловым светом, и она же делает перенос естественным. Величина одна:

**ND ≈ W / ℓ_⊥** — число направлений в ячейке равно числу длин пространственной
когерентности, которые в неё помещаются.

Для одномодового источника `ℓ_⊥` — метры, и волновая машина считает сцену в 4 м
за 111 неизвестных и 11 мс (измерено). Для лампы `ℓ_⊥` = 15 мкм, и ячейка
обязана быть 15 мкм. Полный разбор — PLAN.md, раздел «ГЛАВНАЯ ФОРМУЛА».

Перенос снимает это, потому что выбрасывает фазу ДО расчёта, а не после:
угловое разрешение начинает задавать наблюдатель, а не `kW`. Цена — исчезает
интерференция, то есть всё, ради чего волновая линия и сохраняется.

## ЧТО ИМЕННО РЕШАЕМ

Трёхточечная (поверхностная) форма уравнения рендеринга — Kajiya, SIGGRAPH '86,
DOI 10.1145/15922.15902:

    L(x → y) = L_e(x → y) + ∫_S f_r(z → x → y) · L(z → x) · G(x, z) · dA_z
    G(x, z)  = V(x, z) · cos θ_x · cos θ_z / |x − z|²

**Почему именно трёхточечная, а не полусферическая.** Её ядро `G(x,z)` есть
связь ПАРЫ ЭЛЕМЕНТОВ и не зависит от камеры — то есть это прямой аналог
`⟨B_i, L B_j⟩` из волновой линии, со всеми следствиями: переиспользование,
инкрементность, иерархия. Под неё есть готовая иерархическая теория (Hanrahan,
Salzman, Aupperle, «A Rapid Hierarchical Radiosity Algorithm», SIGGRAPH '91),
которая и есть LOD по паре элементов — то, чего волновой линии не хватало.

Среда (октодерево среды уже есть) добавляется уравнением переноса излучения:

    (ω·∇) L(x,ω) = −σ_t(x) L + σ_s(x) ∫_{S²} p(x, ω'·ω) L(x,ω') dω' + ε(x,ω)

Уравнение рендеринга есть его поверхностный предел. Среду включаем ПОЗЖЕ.

## ЧТО ЯВНО ВЫБРАСЫВАЕТСЯ (чтобы потом не выдавать за открытие)

Интерференция, спекл, дифракция, тонкие плёнки, дифракционный предел, фаза
Френеля. Ближнее поле и эванесцентная связь. Всё это остаётся в волновой линии.

## ЧТО ПЕРЕНОСИТСЯ ИЗ ВОЛНОВОЙ ЛИНИИ БЕЗ ИЗМЕНЕНИЙ

Октодерево, LOD, разрезы (граница тени — разрыв радианса, разрез там так же
дёшев: измерено +1.3% неизвестных против `h^0.6` слепым дроблением), оператор
камеры, линейность по источнику (перекрашивание и смена яркости бесплатны),
свойство «все отскоки решаются разом», отсечение по амплитуде (измерено: ошибка
= выброшенная амплитуда, коэффициент 1 на трёх декадах).

## ЧЕМ МЕРЯЕМ — ТОЛЬКО АНАЛИТИЧЕСКИЕ ЭТАЛОНЫ

«Похоже на картинку» эталоном НЕ является. Доступны замкнутые формы:

1. **Форм-фактор соосных дисков** и **параллельных прямоугольников** — классика,
   замкнутая форма.
2. **Печная задача (furnace test)**: замкнутая полость, альбедо 1, однородное
   излучение ⇒ радианс обязан быть однородным и равным излучению, ТОЧНО, при
   любом разбиении. Это тест сохранения энергии и главный ловец ошибок.
3. **Взаимность**: `A_i F_ij = A_j F_ji` — до машинной точности.
4. **Сумма по строке**: `Σ_j F_ij = 1` для замкнутой сцены — разбиение единицы
   для форм-факторов.

Корнеллова коробка аналитического решения НЕ имеет и эталоном быть не может.

## ФАЛЬСИФИКАТОРЫ И ПРЕДСКАЗАНИЯ — ДО ПЕРВОГО ПРОГОНА

- взаимность `A_i F_ij = A_j F_ji`: предсказание 1e-14 и лучше; если 1e-8 —
  где-то квадратура вместо замкнутой формы;
- сумма по строке: 1 с точностью 1e-14 на замкнутой сцене;
- печная задача: однородность до 1e-14 и НЕЗАВИСИМО от числа элементов; любая
  зависимость от разбиения = ошибка в ядре или в видимости;
- порядок сходимости при равномерном дроблении: предсказание `O(h)` для
  кусочно-постоянных элементов, `O(h²)` для линейных. Показатель ниже 0.8 при
  кусочно-постоянных = особенность, которую не разрешили (скорее всего граница
  тени), и это надо ловить разрезом, а не дроблением;
- **негативный контроль с ПРЕДСКАЗАННЫМ провалом**: намеренно нарушить
  взаимность (считать `F_ij` односторонне, без симметризации) ⇒ предсказание:
  печная задача поедет на величину порядка асимметрии ядра, то есть на проценты,
  а не на 1e-14. Если печь при этом сойдётся — тест ничего не проверяет.

## ТРИ ПОДПИСИ АРТЕФАКТА (из волновой линии, действуют и здесь)

1. число не меняется при изменении параметра, которым его меняют;
2. негативный контроль садится на пол метрики;
3. ошибка нормирована на сокращённый результат вместо натурального масштаба.

За два дня волновой линии этот список поймал три артефакта, из них два —
«значение используется раньше, чем установлено». Читать перед каждым прогоном.

## ПЕРВАЯ ВЕХА

`src/transport/formfactor.{c,h}` + `tests/test_formfactor.c`: замкнутые формы для
пар «диск–диск», «прямоугольник–прямоугольник», взаимность, сумма по строке.
Ничего иерархического, никакой видимости — только ядро `G` и его свойства.
Видимость и иерархия следующим шагом, после того как ядро закрыто тестами.

## ЧЕСТНАЯ ОГОВОРКА О НОВИЗНЕ

Перенос — зрелая технология с двадцатью годами чужой инженерии. Отличать эту
линию будет не сам перенос, а три вещи, и только они: BRDF, ПОСЧИТАННЫЕ волновым
способом вместо подогнанных; иерархия, общая с волновой линией; и голографический
режим, которого у трассировщиков нет. Если ни одно из трёх не подтвердится
измерением — линия не имеет самостоятельного смысла, и это надо будет признать.

## ПРАВКА ВЫБОРА ФОРМЫ: ПРЕЛОМЛЕНИЕ ЛОМАЕТ ТРЁХТОЧЕЧНОЕ ЯДРО (юзер, 07-27)

Вопрос был про воду с плавающим объектом. Он вскрывает слабое место выбранной
формы ДО кода, и это меняет план.

**Прозрачная среда — не объёмный эффект.** Чистая вода не рассеивает, свет внутри
идёт по прямой, объёмных неизвестных нет. Меняется три локальные вещи:
- преломление на границе (Снеллиус + Френель) — обычная поверхность;
- ЗАКОН n²: вдоль луча сохраняется L/n², а не L. При входе в воду радианс
  умножается на n² = 1.77. Забыть = не сойдётся энергия. Это basic radiance
  invariant, и он должен попасть в тесты первой вехи;
- поглощение по Бугеру вдоль пути.
Плавающий объект — обычная граница, Френель с парой n_вода/n_объект.

**НО: трёхточечная форма связывает две точки ПРЯМОЙ.** В ядре стоят V(x,z) и
1/|x−z|². С преломлением путь изгибается, связь двух точек перестаёт быть
замкнутой формулой (задача Ферма), и G(x,z) разваливается. Это известная главная
слабость семейства радиосити — зеркально-преломляющие цепочки, задача SDS.
У трассировки её нет, потому что она путь ПРОСЛЕЖИВАЕТ, а не соединяет.

**РАЗДЕЛЕНИЕ ПО ТИПУ ПУТИ**, и оно не произвольное, а по тому, где что дёшево:

| путь | чем считать | почему |
|---|---|---|
| диффузный | трёхточечная форма, иерархическая радиосити | ядро гладкое, LOD по паре элементов |
| зеркальный и преломляющий | ПРОСЛЕЖИВАНИЕ пути по геометрии | направления детерминированы геометрией, ветвление узкое |
| мутная среда | RTE, неизвестные на (x,ω) в объёме | здесь объём действительно есть, схлопывается октодеревом |

Зеркально-преломляющие цепочки — ровно тот случай, где ВОЛНОВАЯ линия дешева
(измерено 07-26: направлений единицы, задаются геометрией, отскоки бесплатны).
То есть разделение труда получается «каждый там, где дёшев», а не вынужденное.

СЛЕДСТВИЕ ДЛЯ ПЕРВОЙ ВЕХИ: она не меняется (ядро G и его свойства для прямых
путей), но в тесты добавляется закон n² на границе сред, а иерархия и видимость
проектируются с расчётом на то, что зеркально-преломляющая часть пойдёт другим
механизмом.

## СХЕМА ИЗ ТРЁХ ПРОХОДОВ, БЕЗ ВЫБОРКИ (уточнено с юзером 07-27)

Возражение «преломление ломает ядро» снимается тем, что зеркально-преломляющая
часть идёт ПРОСЛЕЖИВАНИЕМ, а не соединением точек. При прослеживании преломление
локально: попал в поверхность, применил Снеллиуса, пошёл дальше.

1. ДИФФУЗНЫЙ ПЕРЕНОС — иерархическая радиосити на трёхточечных связях. Зеркал в
   нём нет, ядро гладкое, LOD по паре элементов.
2. ОТ КАМЕРЫ — прослеживание зеркально-преломляющих цепочек. Каждый отскок даёт
   ОДНО направление (закон отражения / Снеллиус), ветвление узкое; дошли до
   диффузной поверхности — взяли готовую светимость из прохода 1.
3. ОТ ИСТОЧНИКА — то же прослеживание вперёд, чтобы посадить КАУСТИКИ на
   диффузные элементы (свет через воду на дно). Проходом 1 это не покрывается.

Ни в одном проходе нет сэмплирования и дисперсии. Выборка появляется только по
протяжённому источнику, отдельным множителем M с ошибкой ~1/sqrt(M).

ПРОХОД 3 — МЕСТО СТЫКА С ВОЛНОВОЙ ЛИНИЕЙ. Геометрическая каустика от точечного
источника бесконечна, волновая даёт конечный пик правильной ширины: собственная
ширина каустики (lam^2 rho)^(1/3) = 63 мкм при rho=1 м, размытие от протяжённого
источника theta_s*rho. Для лампы (3.3 см) геометрики хватает с запасом, для
Солнца (0.009 рад => 9 мм) разница уже на грани, для лазера волновой расчёт
обязателен. Критерий переключения: (lam^2 rho)^(1/3) против theta_s*rho.
