Читай `PLAN_ELEMENTS.md` с конца: свежие шаги — с §455 по §589, находки по А1026.
Дисциплина шага записана ниже и обязательна. Правила самоостановки — §564.

## ГДЕ МЫ СЕЙЧАС (08-13)

**ЦЕЛЬ, ПОСТАВЛЕННАЯ ПОЛЬЗОВАТЕЛЕМ:** видеть, насколько недостаёт до условного
UE5 — ВИЗУАЛЬНО и ПО ТАЙМИНГАМ. Отсюда: большие сцены, тайминги в каждом
прогоне, ракурсы, текстуры. В перспективе — интерактивный рендерер, «полетать
внутри сцены».

**ПОГУЛЯТЬ ПО СЦЕНЕ** (§589; `make build/pwalk` — отдельная цель, с окном):

    build/pwalk assets/bistro/exterior.obj 0.01 lev=9 walk thr=1 res=512 \
      sun=-0.35,-1,-0.25 cam=30,2,20,-30,4,-20 poly=8

    мышь — поворот, WASD — скольжение, пробел/LShift — тяга вперёд/назад,
    QE — крен, колесо — скорость, Tab — 6 м/с, ` — отпустить мышь, Esc — выход.

    ЧАСТОТА ЧЕСТНО (§590): `512²` — около `1.1` к/с. Числа «5 к/с» из первой
    редакции §589 БЫЛИ НЕВЕРНЫ и не цитируются: там печаталось `срез+свет+растр`,
    а не время ИТЕРАЦИИ, и `88 %` кадра (вставочная сортировка) в строку не
    входили вовсе. Строка кадра теперь печатает ПОЛНОЕ время и статью «прочее».
    `res=256` не рекомендуется: мелкий кадр экономит растр и НЕ экономит срез.

    КОСВЕННОГО СВЕТА В ХОДЬБЕ НЕТ, и это главный недостаток, а не оговорка:
    без него картинка есть обычное затенение по Фонгу (замечание пользователя
    08-13). Почему так и что менять — §591.

**РАБОЧАЯ КОМАНДА** (Bistro, улица, солнце, отскок, текстуры, мип):

    build/pfield assets/bistro/exterior.obj 0.01 lev=9 render thr=1 res=512 \
      sun=-0.35,-1,-0.25 hgather=0.5 cam=30,2,20,-30,4,-20 poly=8

    Сан-Мигель, аркада двора:
    build/pfield assets/San_Miguel/san-miguel-low-poly.obj 1.0 lev=9 render \
      thr=1 res=512 sun=-0.35,-1,-0.25 hgather=0.5 cam=8,2.0,2,30,4,2

**ЧИСЛА (Bistro, 2 833 330 тр, 512², L=9, 16 ядер, ракурс улицы)**

    ПОСТРОЙКА  занятость 16.2 с + рёбра 18.3 с + дерево 3.6 с = 38.1 с
               (ВХОД-ОГРАНИЧЕНА: от камеры не зависит вовсе)
    КАДР при poly=8   срез 368 + свет 21 + растр 480 = 869 мс   (после §585)
                      в растре: обход 151 + рисование 329
    отскок иерархией  ~3 с (вне кадра; 7.3× от распараллеливания)
    память дерева DC  338 МБ, текстуры 24.5 МБ

    СТРОКИ `poly=0` (852 мс) И `poly=4` ИЗ §584 ПЕРЕСНЯТЫ НЕ БЫЛИ и как текущие
    НЕ ДЕЙСТВУЮТ — §585 удешевил обход, а он входит в оба. Замерный долг.

**ГЛАВНОЕ, ЧТО ВЫЯСНЕНО ЗА 08-12/13 — ЧИТАТЬ ПЕРЕД ЛЮБЫМ ШАГОМ**

    §584  КРУПНЫЙ ЭЛЕМЕНТ НЕ МОЖЕТ НЕСТИ СВЕТ (поправка пользователя).
          Ячейка несёт ОБЛУЧЁННОСТЬ, растеризатор её интерполирует: на полигоне
          в 1000 пикселей три значения света, и тень там не изобразится никак.
          Замерено: полигоны крупнее 64 пикс кроют 45 % экрана (Bistro).
          Введён пол `poly=N`. Довод §6 «крупные полигоны и растеризация — парные
          решения» ВЕРЕН ДЛЯ ГЕОМЕТРИИ И НЕВЕРЕН ДЛЯ СВЕТА.
    §585  КРИТЕРИЙ СРЕЗА СЧИТАЛСЯ У ОДНОГО УЗЛА 26 РАЗ. Схема
          cellProc/faceProc/edgeProc спрашивает про узел как про ячейку, как про
          обе стороны каждой грани и как про каждый угол каждого ребра. Ответ
          один — запомнен по байту на узел. Обход 640.9 → 151.3 мс (4.2×),
          картинка и порядковая сумма потока побитово те же. Цель 80 мс НЕ взята
          и была предсказана невзятой: остаток — СКЕЛЕТ обхода.
          Заодно выяснено: обход трогает 105 856 внутренних узлов из 276 904
          (38 %), а не всё дерево, — формулировка §573 была шире правды.
          ИНКРЕМЕНТНОСТИ ШАГ НЕ ДАЛ: память живёт один обход (А1025).
    §573  ПОЛОВИНА «растеризации» — это ОБХОД ДЕРЕВА, а не рисование. Число
          222 мс из §573 НЕ ЦИТИРОВАТЬ: оно при poly=0 и до §585.
    §580  Отсечение по пирамиде В ОБХОДЕ не помогло: полигонов втрое меньше, а
          время +11 %, и картинка разошлась (многоугольник ДК соединяет вершины
          СОСЕДНИХ ячеек — отсечение не консервативно на границе кадра).
    §582  Распараллеливание: отскок 25.0 → 3.4 с, прямой свет 25.7 → 10.5 мс,
          рисование почти не делится (примитив ~ пиксель, вся цена в ПОСТАНОВКЕ).
          Картинка ПОБИТОВО та же — это требование, а не пожелание.
    §583  Автоматическая камера ОТВЕРГНУТА как признак: «самая открытая точка»
          максимизирует ПУСТОТУ (крыша, потом край сцены). Вид задаётся `cam=`.
    §563  Линия направленного свипа ОСТАНОВЛЕНА. Рабочий путь отскока —
          ИЕРАРХИЧЕСКИЙ СБОР.

**ДОВОД ПОЛЬЗОВАТЕЛЯ, КОТОРЫЙ ЗАМЕРЫ ПОДТВЕРЖДАЮТ (08-13).** «Примитивно
обработанный один полигон — хороший принцип, когда можешь позволить, чтобы их
было много. Ещё дальше в ту сторону — splat-техники.» Наши числа: ~300 тыс.
примитивов на 262 тыс. пикселей, то есть примитив уже размером с пиксель, и вся
цена — в ПОСТАНОВКЕ примитива и в ОБХОДЕ иерархии, а не в заливке. Это против
нынешней архитектуры («крупные полигоны») и за плотные примитивы.

## ЧТО ДЕЛАТЬ ДАЛЬШЕ

    1. СРЕЗ — 368 мс, теперь САМАЯ КРУПНАЯ статья кадра. Это ВТОРОЙ обход того
       же дерева с тем же критерием (`hz_slice_build`, `src/cut/dcslice.c`),
       которого §585 не касался. Ту же память ответа он не получил.
       Число: срез 368 мс → ниже 150.
    2. РИСОВАНИЕ — 329 мс. §582 установил, что вся цена в ПОСТАНОВКЕ примитива
       (~300 тыс. примитивов на 262 тыс. пикселей), а не в заливке; деление по
       полосам экрана поэтому ничего не даёт, нужно деление ПО ТРЕУГОЛЬНИКАМ с
       общим z-буфером. Число: рисование 329 мс → ниже 150.
    3. ОБХОД РАСТЕРИЗАТОРА — 151 мс. Повторы сняты (§585), остаток есть СКЕЛЕТ:
       58.9 млн `leafish` и 8.26 млн `process_edge`. Ниже 80 мс он берётся
       только сменой УСТРОЙСТВА — список видимых листьев, переживающий кадр
       (камера за кадр смещается мало), либо выдача полигонов ИЗ СРЕЗА.
    4. Постройка 38 с — вход-ограничена, от камеры не зависит. Растеризация
       треугольников в занятость 16 с + рёбра 18 с.
    5. Дальняя цель — интерактивность и, возможно, поворот к splat.

## ЛИНИЯ СВИПА ОСТАНОВЛЕНА 08-12 (§563, по замечанию пользователя)

Шесть шагов охоты за разрывом свипа с гатером (втрое) сдвинули число на `9 %` и
ни разу в нужную сторону; в ПЯТИ шагах из шести главное оказалось в ПРИБОРЕ, а не
в предмете (А958, А966, А974, А994, А1006). **РАБОЧИЙ ПУТЬ ОТСКОКА —
ИЕРАРХИЧЕСКИЙ СБОР:** `205` мс, `0.7460` против прямого эталона `0.8003`
(расхождение `7 %`, измерено против прямого счёта, а не против другой схемы).
Свип по направлениям сохранён как ИЗМЕРЕННАЯ АЛЬТЕРНАТИВА: сходится по `ND`,
дешевле сбора, но втрое ниже него, и причина не найдена.
**Строки `Ф15'` (бюджет излучения) НЕ ЦИТИРОВАТЬ — невязка `−158 %`, прибор сам
неверен.**

**ЗАМЕР ПРО РАЗРУШЕНИЕ СДЕЛАН И СОХРАНЯЕТ СИЛУ (§527, 08-12).** Удар сферой
`r = 0.20` м пачкает `0.73 %` листьев свипа (посещённых `0.73…1.08 %` с каймой
фронта) — порядок ПРОЦЕНТА, а не половины. Значит динамика по свипу возможна и
§523 идёт как написан. Обратный индекс «ячейка → связи» не нужен: грязь
толкается по готовым `nb[6]`.
**НО ПЕРЕД ДИНАМИКОЙ ЧИНИТЬ ОДНО (А924/А929):** при рабочем пороге дерева `32`
удар в `7.66` м даёт РОВНО `0.000 %` при `414` стёртых ячейках занятости —
дерево его не видит вовсе, потому что крупный ЗАНЯТЫЙ лист заслоном не служит
(`stree_build`, условие `s->size == 1`). Для статической сцены порог `32`
законен, для динамической — нет.
Ключи замера: `hitsweep`, `hitr=R` (НК — `2.0`), `hitnoocc` (ложный ноль).

## ЛИНИЯ, КОТОРАЯ ЖИВАЯ

**ФРОНТ — рабочий путь** (указание пользователя 08-11). Развёртка по ординатам
(`src/transport/sweep3.c`) остаётся ЭТАЛОНОМ физики и путём для участвующей
среды; марш остаётся эталоном видимости. Иерархический сбор (радиосити) сделан и
измерен, но он диффузен по построению и потому не заменяет фронт.

Состояние числами (комната, `L = 9`, вывод `512²`):

    прямой свет маршем            11.5 мс     эталон видимости
    свип по дереву                47 мс       расхождение с маршем 2.63 %
                                              (было 9.0 % — это была вина
                                              ИСТОЧНИКА, а не свипа, А966)
    отскок O(N²)                  1 600 мс    E_ind/E_dir = 0.8003  ЭТАЛОН
    отскок иерархией              205 мс      0.7460, связей 0.92 млн
    отскок с заслонами            70 с        0.5156; сквозь заслоны 35.57 %
    кадр со светом                126 мс      срез 2 + свет 12 + растр 113
    развёртка по ординатам        40 с        не сошлась (невязка 0.36)
    СВИП ПО НАПРАВЛЕНИЯМ (Ф8.)   132 мс      0.1732 при ND=64 — ДЕШЕВЛЕ сбора,
                                              но втрое ниже гатера 0.5156.
                                              ПРИЧИНА (§551 в редакции §555):
                                              правило консервативно ТОЧНО,
                                              разброс растёт как sqrt(k), снос
                                              мал. Коридор 8 ячеек пропускает
                                              1.65 % на 56. Короткие хар-ки
                                              (schar) сноса не оставляют, но
                                              размытие лечат лишь на 18 % и на
                                              комнате ХУЖЕ — НЕ приняты (§555).
                                              ПОЯЧЕЕЧНО (§559): p90/p10 = 7.38,
                                              то есть НЕ множитель. Но у ячеек
                                              в 0.5 м от излучателя отношение
                                              всего 0.41 — БОЛЕЗНЕЙ ДВЕ.
                                              Сперва искать ближний множитель
                                              (2.4x), потом схему выше порядком
    узость доли из материала     +0 мс       Ns/Ks читаются; ND=64 несёт лишь
                                              s~6 (полуширина 27°), дальше
                                              энергия верна, ОБРАЗ нет (§538)

## СЦЕНЫ — ЧИТАТЬ ОБЯЗАТЕЛЬНО

**ДВЕ СЦЕНЫ УДАЛЕНЫ ПО ПРЯМОМУ УКАЗАНИЮ ПОЛЬЗОВАТЕЛЯ. Не возвращать.**

    КОНФЕРЕНЦ-ЗАЛ  08-11. `12.63 %` рёбер с одним владельцем; на нём неизмеримы
                   ориентация, заливка внутренностей и знаковый объём. **Софт
                   для лечения входных сеток проект НЕ ПИШЕТ** — сцену с
                   негодной сеткой не чинят, а не берут.
    RUNGHOLT       08-12 (§565). Экспорт карты Minecraft, воксельный ПО
                   ПРОИСХОЖДЕНИЮ. Для роли МАСШТАБ это было условием, для
                   постановки «насколько недостаёт до UE5» — пороком: блочность
                   там от исходника, а не от рендерера. Убран и из
                   `fetch_scene.sh`. ГОРОДСКОЙ МАСШТАБНОЙ СТУПЕНИ В СОТНИ
                   МЕТРОВ У ПРОЕКТА БОЛЬШЕ НЕТ — это названный пробел.

    assets/synth/room.obj   ОСНОВНАЯ, scripts/gen_room.sh, масштаб 1.0.
                            Стены С ТОЛЩИНОЙ, три масштаба кривизны, колонна,
                            стол, 11 материалов (room.mtl). Открытых рёбер 0,
                            знаковый объём 36.4582 м³ сходится с заливкой и с
                            матрицами масс стыка.
    room_thin.obj           НК: лист нулевой толщины (метрику НЕ ломает).
    room_soup.obj           НК: несогласованная обмотка (ломает).
    spheres.obj             чистая кривизна, gen_spheres.sh.
    box.obj, plates.obj     печь и E_ind = ρ·E_dir.
    assets/bistro/          BISTRO (ORCA, CC-BY 4.0), заведён 08-13 (§583).
                            МАСШТАБ 0.01 (сцена в САНТИМЕТРАХ, найдено замером).
                            exterior.obj 2 833 330 тр, interior.obj 1 020 907.
                            410 текстур; переведённые лежат в bistro/ppm256
                            (готовит scripts/tex_prep.sh, размер 256).
                            Габарит при 0.01: 108.60 x 31.93 x 115.26 м.
                            Текстурные архивы качаются БЕЗ расширения:
                            .../bistro/BuildingTextures (не .zip).
    San_Miguel              РАЗБРОС ДЕТАЛИ и число полигонов: 4 963 199 тр,
                            281 материал, 323 текстуры. Это ДВОР, а не улица.
                            Если слишком подробна —
                            ОТКУСЫВАТЬ САМОЕ ДЕТАЛЬНОЕ (у Сан-Мигеля есть
                            low-poly), а не искать сцену помельче.

`HZ_CFG_HALL_OBJ` теперь указывает на комнату: полтора десятка старых замерных
инструментов завязаны на это имя, а комната построена вокруг ТЕХ ЖЕ камеры и ламп.

## ДИСЦИПЛИНА ШАГА — БЕЗ ИЗЪЯТИЙ

  1. план шага и ПРЕДСКАЗАНИЯ с интервалами — в конец `PLAN_ELEMENTS.md` ДО кода,
     отдельным коммитом;
  2. аудит плана до кода, находки серией А;
  3. код; гейт `CLAUDE.md` целиком (clang-format, `scripts/ccheck.sh` до CLEAN,
     `scripts/lean.sh`, санитайзеры; CBMC на опасном);
  4. прогон с НЕГАТИВНЫМ КОНТРОЛЕМ, который обязан провалиться предсказанным
     образом;
  5. доклад: вывод первым абзацем, две страницы, у всякого числа три пометы —
     на чём, какова ПОПУЛЯЦИЯ, чего НЕ покрывает;
  6. аудит исполнения: что не сбылось, что измерено не тем, что осталось.

Прерываться и спрашивать ТОЛЬКО при срабатывании «УБИВАЕТ» или если нужно править
`src/cut/poly3.c`.

## ПРАВИЛА, НАРУШЕННЫЕ 08-11/12 — НЕ ПОВТОРЯТЬ

    §498  считать байты на элемент и умножать на число элементов ДО того, как
          структура написана. Нарушено: узел излучателя вышел 616 Б (шесть
          корзин каждому узлу, всё в double) при нужных 90…140.
    А917  ОТСУТСТВИЕ пометки — это «неизвестно», а не значение по умолчанию.
          Нарушено: `hz_flat = 0` прочтено как «шероховатый» и запретило
          агрегацию везде. Третий случай за проект после dmax = 0 и noct = 0.
    А913  если величина не отзывается на измельчение — искать в ПОСТАНОВКЕ, а не
          в дискретизации. Трижды подряд искал в разрешении.
    А916  прежде чем чинить проводку параметра в третий раз — ПОСЧИТАТЬ, какого
          значения следует ждать.
    А824  проверять ПРИБОР до того, как строить на его числах шаг.
    А891  у всякой величины, которую картинка читает по ячейке, обязан быть
          счётчик покрытия рядом.

## КЛЮЧИ `pfield`, ЗАВЕДЁННЫЕ 08-11/12

    solid      заливка внутренностей и сверка со знаковым объёмом
    xfer       стык с переносом (фасеты + маска -> tr3_cut_build)
    xsweep     плюс сама развёртка по ординатам; nmu=N — ординаты; xtrace
    treesweep  свип по ДЕРЕВУ; sweepthr=N — дробление по допуску (действует от ~5)
    seed=R     засев окрестности источника маршем (R=1 даёт 5x по макс. ошибке)
    hgather=E  иерархический отскок; hspread=T — допуск разброса; onebin — НК
    lobetest   ГРАНИЦА УЗОСТИ доли: сколько несёт данное ND (Ф9., §538);
               dlobeflat — НК; dns=/dks= — перебивка узости и доли
    dsweep     радианс по НАПРАВЛЕНИЯМ на дереве (Ф8., §534); dnmu=, dnphi= —
               набор §2; dpass=N — отскоки; dirsall — НК; dblkopen — верхняя
               граница вилки
    emitalb2   вернуть СТАРОЕ неверное альбедо излучателя (§531, только сверка)
    albone     все альбедо = 1 (фальсификатор §531)
    ptlight    вернуть ТОЧЕЧНЫЙ источник (§543, НК; закон pi*L его отвергает)
    ffull      прежний булев заслон (§547, приёмка сведения); fnotrans —
               только ослабление излучения; fzero — вырожденный НК
    cmpcell    поячеечное сличение свипа с гатером (§559); cmpself — ложный
               ноль; gstride=N — прореживание гатера (НК)
    render     ТОЛЬКО РЕНДЕР: диагностика выключена, тайминги остаются (§566)
    sun[=x,y,z] СОЛНЦЕ — направленный источник, одна проба вместо 64 (§571)
    cam=ex,ey,ez,ax,ay,az   камера явно; camauto — снаружи габарита;
               caminside — внутрь по занятости (§574, §583: вид им НЕ выбирать)
    poly=N     ПОЛ по экранному размеру ячейки, пикс (§584). 8 — рабочее
    nomemo, memoscramble — НК §585. Первый выключает память ответа критерия
               среза (обход возвращается к 485 мс), второй переворачивает ответ
               у каждого 997-го узла с детьми, КРОМЕ КОРНЯ, — картинка обязана
               разойтись (перевёрнуто 87 ответов из 105 687, то есть 0.082 %)
    ceil=N     потолок по угловому размеру ячейки (§567); 0 — выключен
    texflat, texnomip, nofrustum, omp1, gam16 — НЕГАТИВНЫЕ КОНТРОЛИ
    diffbench=N СТЕНД (§551 в ред. §555), коробка N³: снос и разброс ПОРОЗНЬ;
               onenb, scharax — НК (резко, но мимо цели); schar — короткие
               характеристики (измерены, в рабочий путь НЕ приняты)
    hitsweep   доля листьев свипа, затронутых ударом; hitr=R — радиус (НК 2.0);
               hitnoocc — проверка на ложный ноль (занятость не правится)
    emit=T     огрублённые излучатели по камере (тупик, §510)
    indmeas    считать долю косвенного, идущую сквозь заслоны; indvis — отбрасывать
    ss2        растр в 2x и свёртка 2x2; res=N — разрешение растра
    lit        кадр со светом; thr=N — порог LOD среза
    passes=N, raysweep, noshift, nonsum, nrmflip, xcorner, xinnerfluid — НК

Картинки — в `img/`, не в `build/`. В `wave/` не входить и оттуда не читать.
Коммитить прямо в `main`. Сцен нет — `bash scripts/fetch_scene.sh assets`,
комната — `bash scripts/gen_room.sh`.
