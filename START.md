Действующий план — PLAN_ELEMENTS.md, §383 (ПЛАН НОВОЙ СТРУКТУРЫ) и §384 (аудит
этого плана, А664…А674; правки по нему в §383 уже внесены). Читай оба целиком
перед работой. Свежие шаги — с §455 (Ш7) по §492; находки — по А901.

Устройство ЗАКРЫТО решениями Р1…Р6 и сводкой §382 — не переоткрывай, если не
найдёшь противоречия. Линия marching cubes закрыта (Р6, А654, А660), замер
М1…М4 не проводится. Опись структур по полям — А659…А663.

Иди по шагам самостоятельно, не спрашивая по мелочам. Прерывайся и спрашивай
ТОЛЬКО при срабатывании условия «УБИВАЕТ» или если понадобится править
src/cut/poly3.c.

Для каждого шага порядок такой и другим быть не может:
  1. план шага и ПРЕДСКАЗАНИЯ записываются в конец PLAN_ELEMENTS.md ДО кода;
  2. код; гейт качества C из CLAUDE.md целиком (clang-format, scripts/ccheck.sh,
     scripts/lean.sh, санитайзеры), CBMC — на опасном;
  3. прогон, включая НЕГАТИВНЫЙ КОНТРОЛЬ, и он обязан провалиться предсказанным
     образом; оборвать его нельзя;
  4. ДОКЛАД в конец PLAN_ELEMENTS.md: вывод первым абзацем, две страницы на шаг;
  5. АУДИТ ИСПОЛНЕНИЯ отдельным разделом — что из предсказанного не сбылось, что
     измерено не тем, чем заявлено, что осталось непроверенным.

Дисциплина докладов записана в конце §383 и обязательна: у всякого числа три
пометы — на чём замерено, какова ПОПУЛЯЦИЯ и ЧЕГО ОНО НЕ ПОКРЫВАЕТ; популяция
приёмочной метрики обязана быть ВХОДОМ (уменьшение результата не смеет улучшать
метрику); всякое «следовательно» проверяй обратным ходом — годится ли основание
для отрицания вывода; чужое число под своё утверждение не подставляй, а при
отсутствии своего замера так и говори. Мерить хвост распределения, а не среднее.

## СЦЕНЫ (переписано 08-11 по прямому указанию пользователя)

**КОНФЕРЕНЦ-ЗАЛ УДАЛЁН. Не возвращать, не качать, не поминать как эталон.**
У него `12.63 %` рёбер с одним владельцем и несогласованная обмотка, а значит на
нём НЕИЗМЕРИМЫ: ориентация многоугольников (метрика даёт `max 180°` даже у
заведомо верных ячеек, А845), заливка внутренностей (протечка `1 572 696`
ячеек, §477), знаковый объём (`−88.17` м³ — величина без смысла).
**СОФТ ДЛЯ ЛЕЧЕНИЯ ВХОДНЫХ СЕТОК ПРОЕКТ НЕ ПИШЕТ** — это отдельный продукт, и он
не наш. Сцену с негодной сеткой не чинят, а не берут.

Что осталось и для чего:
  assets/synth/room.obj    ОСНОВНАЯ. Комната-инструмент (scripts/gen_room.sh):
                           стены С ТОЛЩИНОЙ, три масштаба кривизны, колонна,
                           стол. Замкнута, обмотка согласована, открытых рёбер
                           `0`. Знаковый объём `36.4582` м³ сходится и с
                           заливкой, и с матрицами масс стыка переноса.
                           Масштаб `1.0`; камера и лампы scene_cfg.h подходят к
                           ней по построению.
  assets/synth/room_thin.obj  НК: та же плюс лист нулевой толщины.
  assets/synth/room_soup.obj  НК: та же плюс полоса с несогласованной обмоткой.
  assets/synth/spheres.obj    Восемь замкнутых сфер на 2…37 м — чистая кривизна
                           без пола и света (scripts/gen_spheres.sh).
  assets/synth/box.obj, plates.obj  печь и `E_ind = ρ·E_dir`.
  assets/rungholt/rungholt.obj  МАСШТАБ: 6.7 млн треугольников, воксельный.
  assets/San_Miguel/*.obj  РАЗБРОС РАЗМЕРОВ ДЕТАЛИ, 10 млн треугольников.

**ЕСЛИ СЦЕНА СЛИШКОМ ПОДРОБНА — ОТКУСЫВАТЬ САМОЕ ДЕТАЛЬНОЕ** (указание
пользователя 08-11), а не искать сцену помельче: у Сан-Мигеля есть готовый
low-poly вариант (`HZ_CFG_MIGUEL_LOW_OBJ`), у Rungholt отрезается часть габарита.

Картинки пишутся в img/, не в build/. В wave/ не входить и оттуда не читать.
Если сцен нет на месте — bash scripts/fetch_scene.sh assets
