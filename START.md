Читай `PLAN_ELEMENTS.md` с конца: свежие шаги — с §637 по §773, находки по А1221.
Дисциплина шага записана ниже и обязательна. Правила самоостановки — §564.

## ЧТО ДЕЛАТЬ ПЕРВЫМ (обновлено 08-23, §777): ЛОКАЛИЗАЦИЯ/ЛЕЧЕНИЕ ПРОТЕЧКИ (§767 — закон снят ~h^0.7, лечение не начато; она же, вероятно, «печь 0.87»). «Дрейф» А1228 РАЗОБРАН И ЗАКРЫТ (§777): дрейфа не было, был ключ xnolim в реконструированной команде — все записи реабилитированы, КАНОНИЧЕСКАЯ КОМАНДА записана ниже. Историю читать как ленту 08-21…08-23.

## КАНОНИЧЕСКАЯ КОМАНДА РАБОЧЕГО СВИПА (записана §777 после урока А1236)

    build/pfield assets/bistro/exterior.obj 0.01 lev=7 nmu=2 xsweep \
      xthin=1e-2 xsemin=1e-2 xmatrho xit=40 xtol=1e-5

ОГРАНИЧИТЕЛЬ ПОЛОЖИТЕЛЬНОСТИ ВКЛЮЧЁН (ключ `xnolim` НЕ ставить: он
диагностический — линейность оператора для приборов xunit/К76-класса — и
сдвигает psin на +1.2…2 %; все записи §739…§773 сняты БЕЗ него). Cavity-пары
§766/§767: `cavity05.obj 1.0 lev=5 nmu=3 xsweep xfernosolid xmatrho
xthin=1e-2 xsemin=1e-2` (xit/xtol умолчания).

**ЗАДАНИЕ «ЧТО ОБЩЕГО У МИЛЛИОННОГО ХВОСТА» ВЫПОЛНЕНО (§727/§728). Ответ: общего
два, и ни одно не локальная величина.** (1) Хвост собирается ЦЕЛЫМИ ЯЧЕЙКАМИ:
`2.3…2.6` элемента на ячейку против `1.0…1.2` у случайной выборки — взрывается
связка «ячейка + её элементы» (§724 подтверждён на тысячах). (2) Хвост РАСТЁТ:
`1.72 % → 6.20 % → 16.21 %` популяции по тактам 2→4 — **«~870 устойчивых
адресов» НЕ СУЩЕСТВУЕТ** (`g > 100` это `9…21` тысяча элементов, А1101), чинить
поимённо нечего. Все шесть локальных величин у хвоста — как у популяции
(отношения медиан `[0.88, 1.25]`).

    build/pfield assets/bistro/exterior.obj 0.01 lev=7 nmu=2 xsweep \
      xthin=1e-2 xsemin=1e-2 xnolim xit=5 xtrace

**§729/§730 (08-21): УСИЛИТЕЛЬ НАЙДЕН ПРЯМЫМ СЧЁТОМ.** Ключ `xunit` применяет
оператор такта к ЕДИНИЧНОМУ состоянию при нулевых источниках (машинерия К76).
Bistro: из входа `≡ 1` за ОДИН такт выходит `8.4e+07` (элемент `70837`),
`φ/4π` до `2.3e+07` (ячейка `57238`), усилители — `4.3 %` элементов; медиана
здоровая (`0.655`). НК box: максимум `0.79 < 1`, усилителей ноль. **Усиление
ВНУТРИТАКТОВОЕ, в тракте `φ` (А1109): семь порядков набираются композицией
ячеечных DG1-обновлений вдоль направления в одном проходе — межтактовая петля
«ячейка ↔ элемент» лишь ретранслирует.**

    build/pfield assets/bistro/exterior.obj 0.01 lev=7 nmu=2 xsweep \
      xthin=1e-2 xsemin=1e-2 xnolim xunit xit=2 xtrace

**§731/§732: ТРАССА СДЕЛАНА (`xchain=`). Девять порядков = ШЕСТЬ СТУПЕНЕЙ:**
`321 × 43.6 × 43.3 × 40.5 × 6.6 × 6.1` на соседних ячейках (`81051` — доля
флюида `0.0198`, НАД порогом `xthin`; `57238`/`57320`/`57322` — доли
`0.47…0.56`; соседство `81060` из §718). Здоровый фон ступени (НК box) — до
`5.5`. **Усилитель — свойство ОТДЕЛЬНОГО ячеечного DG1-обновления**; порогом
щепок класс не лечится (А1118), «разрезанность» сама по себе не критерий
(А1119: 75 % против фона 76 %).

**§733/§734: МЕХАНИЗМ ВСКРЫТ (`xcell=`/`xdir=`). Баланс потока точен
(невязки машинные), а РЕШЕНИЕ НЕ ОГРАНИЧЕНО; два лица:** (а) РАСЩЕПЛЕНИЕ —
`57320`: влёт `1.95e+06`, полином качается `±1e+09`, вылеты `−8.5e+07` в грань
и `+4.8e+07`/`+3.3e+07` в элементы, сумма точна; сосед получает ×43.6 по
модулю, знак минус (А1122); (б) СРЕДНЕЕ БЕЗ ПОТОКА у щепок — `81051` (флюид
`0.0198`): потоки ≤ `0.73` не расщеплены, а среднее `365`; наружу — через
облучённость элементов из `cf` (А1123). Здоровая `80396`: углы `0.975…1.054`.

**§735/§736: ЛЕЧЕНИЕ СДЕЛАНО — РАЗВЁРТКА НА РАЗРЕЗЕ ВПЕРВЫЕ СХОДИТСЯ.**
Принцип максимума в обновлении ячейки (граница из верховых мажорант + вклад
источника; НЕПРЕРЫВНОЕ сжатие наклонов с сохранением потока строки 0 — жёсткий
клип давал предельный цикл, А1128). Рабочий прогон Bistro: **14 итераций,
невязка `9.6e-06`, сжатие `0.61` за такт**, клипы 7 % стабильны; НК `xnomaxp`
возвращает расходимость (тот же адрес). `make test` зелёный — в линейном
операторе Крылова клип выключен, как `limiter` (А1129: нелинейность ломала
тёплый старт). Косвенная доля `0.238` при ряде основной схемы `0.366` —
согласие ПО ПОРЯДКУ; точная сверка не выровнена (А1131: альбедо материальные
против 0.7, ординат 32, сток в сплошное 76 %).

**§737…§741: СВЕРКА ВЫРОВНЕНА; ПОСЛЕ ЧИНКИ АТРИБУЦИИ СВИП ВЫШЕ ЭТАЛОНА.**
Ключи `xmatrho` (материальные альбедо, атрибуция ПО БЛИЗОСТИ К ПЛОСКОСТИ) и
`xmatfar` (НК — наихудший выбор). Альбедо Bistro ≈ 1 по площади (`0.9992` —
цвет в текстурах), но СВЕТО-взвешенное `ρ_eff = 0.952` (печатается §739-строкой;
прибор сверен на точках 0.697/0.997). **Сработало УБИВАЕТ §739: атрибуция
ls[0] раздавала kd≈0 излучателей соседним стенам — 0.32 % элементов держали
львиную долю света (А1142); числа §738 НЕДЕЙСТВИТЕЛЬНЫ, пересдано в §741.**
Пересданная сверка: доля косвенного `0.384/0.388/0.388` при `nmu=2/3/4`
(полка), вилка произвола атрибуции `[0.388, 0.404]`; эталон `0.366` НИЖЕ на
~6 % (а сошедшийся по hcontrib эталон ~0.356 — на ~9 %). При ρ≈1 сжатие
`0.89`/такт (А1138).

**§742/§743: ПЕРЕЛЁТ РАЗОБРАН — РАСХОЖДЕНИЕ СХЕМ ДВУСТОРОННЕЕ ПО ЧЛЕНАМ
РЯДА.** Ключ `xrhoscale=` извлекает ряд отскоков масштабом альбедо (НК s=0
побитовый, линейность 1.4 %). Первый отскок свипа `B₁ = 0.249` НИЖЕ сбора
(`0.294…0.2998`), множитель второго `B₂/B₁ = 0.284` ВЫШЕ (`0.1697`); сумма
компенсируется до +6…9 %. **Интегральная доля — слабая величина сверки
(А1148); интегральный уровень ИСЧЕРПАН.** Который ряд правильный — неизвестно:
у сбора порог резал хвост, у свипа сток в сплошное и клипы; внешнего эталона
нет (§564).

**§744/§745: ПОЯЧЕЕЧНОЕ СЛИЧЕНИЕ СДЕЛАНО (`xcmp` — свип и ядро §597 в одном
процессе, мост `node_of_cell`, оба НК чисты). РАСХОЖДЕНИЕ ПЕРЕНОСНОЕ И
МОНОТОННОЕ:** отношение косвета свип/ядро падает на ЧЕТЫРЕ порядка от ламп к
дали (корзины r/med: 167 / 27.9 / 0.017). У свипа косвет сосредоточен у ламп и
умирает вдали — согласуется с §743 (первый отскок легче, ближний хвост
тяжелее). **Главный подозреваемый — СТОК В СПЛОШНОЕ (76 %, §706): каждый
дальний путь платит стоку на каждом переотражении (А1155).** Оговорки:
«ядро~0» у 47.6 % ячеек — частично мостовой артефакт глубины узлов (А1154);
вклад nmu=2 от стока не отделён; кто прав вдали — внешнего эталона нет (§564).

**§746/§747: СТОК ПРОВЕРЕН И ОКАЗАЛСЯ ГЛАВНЫМ ВИНОВНИКОМ.** Защита §735
расширена на ГРАНИ (`bfmax·hsum`, третий счётчик `maxp a+b+c`); стык включён и
держится: `max|bout| = 0.16` против `5.7e+52` у НК без защиты (§714-механизм).
Градиент корзин §744 схлопнулся: `167/27.9/0.017` (ρs=0) → `1.72/4.19/0.226`
(ρs=1) — дальняя корзина ×13 (А1159). Полость room чиста (потеря → 0.000,
недостача 0.0 %). НО: при ρs≈1 сходимость деградирует — застой К81 на 1.5e-2,
интегральные числа несошедшиеся (А1161); граневой клип спит, всё ловит
ячеечный (А1160); интегральная сверка с 0.366 при стыке отпадает по построению
(А1162), сличима только форма. Честное ρs стен не атрибуировано (0.9/1.0
параметрически). Лампы room без Ke (0 светящихся — не разобрано).

**§748…§751: РАЗГОН ВЫСОКОАЛЬБЕДНОГО РЕЖИМА — ЦИКЛ ПОГАШЕН, DSA-ПРОТОТИП
ПОСТРОЕН, УСКОРЕНИЯ ПОКА НЕТ.** (1) §748/§749: застой 1.5e-2 был ПРЕДЕЛЬНЫМ
ЦИКЛОМ клипов — погашен демпфированием `xrelax=0.7` (А1166); обнажилась
медленная мода 0.96…0.99/такт — честная физика ρ_eff ≈ 0.99. (2) §750/§751:
двухсеточный цикл `xdsa` (потактная прокидка + грубое звено etree/gather_apply
ядра §597 + продление `xdsagain`): механика чиста (мост 100 %, γ=−1
расходится — направление верно), но эффект НУЛЕВОЙ: **спектральное
несоответствие уровней (А1174)** — ρ_coarse ≈ 0.3…0.5 (обрезки ядра) против
ρ_fine 0.96…0.99; согласуется с §743/§745. Попутно А1173: НК γ=0 вскрыл баг
«демпфер §748 × прокидка К76» (bprev/sprev нулями → ложный пол 9.34e-2) —
починен, прежним прогонам не вредил (ω=1).

**§752/§753: ГАЛЁРКИНСКОЕ ЗВЕНО (`xdsagal`/`xdsalev`) ПОСТРОЕНО — ρ(A_c)
ПОДНЯТ ВТРОЕ (0.3…0.5 → 0.846…0.849), НО УСКОРЕНИЯ ВСЁ РАВНО НЕТ** (тонкая
мода 0.96…0.99, поправка ~2e-4 при resid ~2e-2). Измельчение агрегатов
27 → 88 сдвинуло ρ(A_c) на +0.003 — недостающее НЕ пространственное (А1179).
Кандидаты: зонды-ступеньки возбуждают клипы сильнее рабочего поля —
линеаризация не в той точке (А1180а, проверка дёшева: разностные зонды
x ± ε·индикатор); кусочно-постоянная база без наклонов (А1180б).

**§748…§757: ЛИНИЯ РАЗГОНА ВЫСОКОАЛЬБЕДНОГО РЕЖИМА ЗАКРЫТА (решение
пользователя 08-22 по УБИВАЕТ §756).** Итог серии одним прибором ρ(A_c)
против моды тонкого 0.96…0.99: ядро §597 — 0.3…0.5; галёркин-индикаторы —
0.8456 (измельчение 27→88: +0.003); разностные зонды вокруг рабочего поля —
0.8455 (НК ε×2 ±1e-4); раздвоение элементы/стык — 0.8466. **Четыре гипотезы
исключены; недостающее — внутриагрегатная ФОРМА моды, следующий уровень
(локальные моды) дорог и без гарантии — не строится.** Принято: ρs ≈ 1 —
МЕДЛЕННЫЙ КРАЙ (ω = 0.7, сжатие 0.96…0.99; только сверочная конфигурация;
рабочий ρs = 0 сходится за 14 тактов). Что остаётся полезного из стройки:
`xrelax` (гашение цикла, А1166), починка демпфер×прокидка (А1173), прибор
ρ(A_c) и вся зондовая машинерия (`xdsa*` — выключена по умолчанию).

**§758/§759: ПЕРВЫЙ КАДР РАЗВЁРТКОЙ СНЯТ** (`xframe`:
`img/pfield_lit_L7_1920x1080_sweep.ppm`, рядом базовый той же камерой).
Единицы инъекции `E·alb + Ke` верны без подгонок (медиана ядро/свип 0.598 —
А1191); расхождение кадров = переносное §745 (вне ×2 91.7 %, p90 6.8e3) плюс
**дыры покрытия 34 %** (кадровые поверхности без элементов свипа в поддереве —
А1190, долг моста). Ошибка курса ρs=1 против А1162 поймана в шаге и пересдана
(А1189).

**§760/§761: ДЫРЫ КАДРА ЗАКРЫТЫ ПОЛНОСТЬЮ** (`node_swE_best`: глубочайший
предок с полем): покрытие 100 %, 90 % дыр — подъём НА ОДИН уровень, заливки с
корня 0.0 % (НК чист) — дыры были локальными пропусками (А1194: фильтры и
обратные стороны тонких стен), не замурованными областями. Медиана сличения
0.598 → 0.778; вне ×2 90.7 % — переносное §745. Оговорка А1192 (подсветка
обратных сторон сквозь тонкую стену) в силе.

**§762/§763: ОСТАТОЧНЫЙ ×8 РАЗОБРАН И ЗАКРЫТ.** Мост ЧИСТ (флаг заполнения
`g_indfill`: поднятых 0 при 0 отказов — «ядро~0» у 47 % ячеек свипа это
ЧЕСТНЫЕ нули косвета ядра; А1154 опровергнута — А1197). Ординаты
второстепенны: градиент 7.61 → 6.15 → 5.56 при nmu 2/3/4, предел ~×5 (А1198).
**Остаток ~×5 — межсхемный без арбитра** (обрезка hcontrib/заслоны/ноль-косвет
у ядра против стык-замкнутости свипа); закрыт до ВНЕШНЕГО эталона (§564) —
А1199.

**§764/§765: ВНЕШНИЙ ЭТАЛОН ПОСТРОЕН И РАБОТАЕТ (§564 закрыт в первой
форме).** Интегрирующая сфера (`scripts/gen_cavity.sh`, сцены cavity05/09;
оболочке нулевой толщины нужен `xfernosolid` — А1206): аналитика доля =
ρ/(1−ρ), ряд ρⁿ. Первый же арбитраж: **у свипа ПРОТЕЧКА ПОЛОСТИ ≈ 15 % на
отскок** (три замера самосогласованы: q=0.4264, B₁=0.4252, 0.7677=0.9·0.853 —
А1203), у ядра потеря ~7.6 % (0.8613 при hbounce=8) — знак §743 подтверждён
внешним арбитром (А1204). Протечка — кандидат дальнего дефицита Bistro
(А1205: длинные пути платят (1−0.15)ⁿ).

**§766/§767: ЗАКОН ПРОТЕЧКИ СНЯТ.** L = 23.1/14.8/8.9 % при lev = 5/6/7
(×0.6 на уровень, ~h^0.7) — ГЕОМЕТРИЧЕСКАЯ (щели фасетизации); от nmu не
зависит вовсе (14.84/14.79/14.74 % при 2/3/4); НК-инвариант ρ_eff =
0.500 ± 0.001 во всех конфигурациях (А1207). На Bistro lev=7 свип теряет ~9 %
на отскок сверх альбедо только на протечке — дальний дефицит §745 и лёгкий
первый отскок §743 в заметной мере объяснены (А1208).

**§768…§771: ЛЕСТНИЦА СКОРОСТИ (а)-(б) ПРОЙДЕНА.** (а) Прибор вклада
(`xcontrib`, elem_rho-перевозмущения): дальше ~1.5× радиуса зоны интереса
вклад в неё < 0.01 % (0.00 % на 20/40 м; 8.2 % на 10 м; обратные 6.1 %; НК
побитовый) — порог огрубления получен ЧИСЛОМ (А1212). (б) Такт распараллелен
по направлениям (§770): ×4.06 на 16 ядрах (58 → 14.3 с mat-прогон) при
тождестве печатных чисел; trace-путь однопоточно-побитовый; Амдал назван
(А1216). Принята директива «огрубление по номеру отскока, агрессивность от
ширины лепестка» (§768-приписка).

XX_PLACEHOLDER
директива §768-приписки).**

**§772/§773: ГЕОМЕТРИЧЕСКОЕ ОГРУБЛЕНИЕ ПРИЁМНИКОВ ПОСТРОЕНО И УПЁРЛОСЬ В
ПРЕДЕЛ ПРЕДСТАВЛЕНИЯ (УБИВАЕТ §772).** Ключ `xcoarse=D0`: лестница D0·S,
бюджет плоскостей, ОБЪЁМНАЯ СВЕРКА fail-closed. Машинерия корректна (НК
D0=1e9 побитовый; при D0=10 сходимость/балансы чисты, Δpsin 1 %), но сверка
бракует 95.6 % surface-кандидатов: **union-рез продлевает плоскости
бесконечно, ограниченные куски поверхностей невыразимы** (А1220) — выигрыш
лишь 4 %. Инфраструктура градуированной сетки (mesh_build_lod, узлы-ячейки в
collect_rec, грубый рез cut3, К96-подграни) — рабочая и остаётся (А1221).
РЕШЕНИЕ 08-23: ветка (2) — **ОГРУБЛЕНИЕ ЧАСТОТЫ** (по номеру отскока,
§768-приписка), без геометрического реза; О-агрегация отложена.

**§774/§775 (08-23): ЧАСТОТА ОТСКОКОВ ОГРУБЛЕНА — ПРИЁМКА ВЫПОЛНЕНА.**
Ключ `xbounce=N`: лестница N прокидок К76 (такт = отскок, А1222) +
агрегатное замыкание хвоста `x + Δ_N·q̂/(1−q̂)`, q̂ измеряется по двум
последним приращениям (φ, bout, sout); `xtailq=` (0 — усечение, >0 — НК),
`xbcmp` — же-процессная база и сравнение. Bistro N=5: **2.35 с против
14.6 с (×6.2), Δ зоны +0.225 %, Δpsin −1.485 %**; N=8: ×3.7, −1.06 %/
+0.007 %. q̂ сверен с аналитикой сферы (0.3888 против 0.385); проводка
N=40 ПОБИТОВА; НК q=0.95 перелетел предсказанно (+25.6 % cavity ∈ [15,30];
×9.4 ошибки Bistro). Скалярное q̂ не закрывает поэлементный хвост (p90 25 %
при N=5, 5.1 % при N=8 — А1230); отрицательные eirr — свойство базы (28 %,
xnolim), не замыкания (А1229). Первый отскок не грубится по построению;
расписание k(n) ПО КАДРАМ — за инкрементностью (§652), долг.

**§776/§777 (08-23): «ДРЕЙФ» А1228 РАЗОБРАН — ЕГО НЕТ.** Бинарный поиск по
сборкам (до-§770, до-§772, §767-эпоха) показал: код чист, среда чиста
(nix-канал от 07-14), активы чисты; «дрейф» = ключ `xnolim` в
реконструированной §774-команде (взят из §729-диагностической строки, урок
А1236). HEAD воспроизводит записи ПОСИМВОЛЬНО: Bistro 2.3429/9.5554/5.75e-05,
cavity 36.110/22.199, Σ зоны 0.906. **§774 пересдан в канонике: N=5 — 2.23 с
против 13.40 с (×6.0), Δpsin −0.63 %, Δ зоны +0.38 %, q̂ 0.627, хвост
поэлементных ошибок макс 0.85 (был 6918 при xnolim), отрицательных eirr ноль
(А1229 переквалифицирована: отрицательные — свойство xnolim).**

**Потом: долги: печь 0.87, `p99=92` §722, счётчик растра, Ke-атрибуция
А1143, честное ρs стен, лампы room без Ke, НК-пара кадра, UB-сдвиг lev<6 и
утечки exit-пути (А1231); РАЗВИЛКА GPU (вопрос пользователя 08-23): свип —
волновой фронт и на GPU хуже сбора (SpMV-класс, применение 0.59 с); гибрид
«такт 1 свипом (тени) + поздние отскоки сбором/агрегатами» — кандидат в
отдельный замер, сравнение в одних условиях так и не делалось (§688).**
**Одиночный элемент проверялся трижды (§720, §722, §724) и не отличается от
типичного ни одной величиной; ансамбль (§727) показал то же по медианам.**

## ГДЕ МЫ СЕЙЧАС (08-21)

**ДВА ПУТИ, И ОНИ В РАЗНОМ СОСТОЯНИИ.** Буквы `А`/`Б` из обихода убраны (§687):
они дважды меняли смысл. Называть надо описательно.

    ОСНОВНАЯ СХЕМА (уравнение с ХРАНИМЫМ дальнодействующим оператором) —
    РАБОТАЕТ. Ядро §597: `370` млн геометрических связей, `17.7` млн после
    заслона, сборка `29.5` с, применение `0.59` с, ряд сжимается
    `0.2998 / 0.1697 / 0.2383`. ВСЕ КАРТИНКИ посчитаны ею.

    РАЗВЁРТКА (то же уравнение с ЛОКАЛЬНЫМ оператором по ординатам,
    `src/transport/sweep3.c`) — НЕ СХОДИТСЯ на разрезанной геометрии. Взята она
    не потому, что дешевле (сравнения в одних условиях НЕ БЫЛО, §688), а потому
    что у локального оператора инкрементность даром: §651 — обратный индекс
    «ячейка → связи» стоил бы сбору `0.8…16` ГБ, а свипу не нужен вовсе.

**КАДР (Bistro, `L = 9`, Full HD, `ceil=poly=15`): `2374` мс** = срез `661` +
прямой свет `2.8` + растр `1710`. Холодный старт `112` с. Подробная раскладка с
ценой за единицу — §650.

## ЧТО СДЕЛАНО 08-19…08-21, ПО УБЫВАНИЮ ВАЖНОСТИ

    §706  **НАЙДЕН И ИЗМЕРЕН СТОК: `76 %` излучённой энергии уходило в
          СПЛОШНЫЕ ячейки и не учитывалось нигде.** Тождество К40 замкнулось с
          невязкой `6.5e−08`. Печаталось это в каждом прогоне с §486 и не
          читалось.
    §707  Написано ГРАНИЧНОЕ УСЛОВИЕ на стыке «флюид — сплошное» (тем же
          механизмом, что стенки куба). **НО ОНО УСИЛИВАЕТ** (`bout` доходит до
          `4.9e+34` за проход, §714) и потому ВЫКЛЮЧЕНО по умолчанию: ключ
          `xsolidrho=`. Долг: проверить его на замкнутой полости с `ρ = 1`.
    §714  **ЩЕПКИ ПО ОБЪЁМУ — усилитель.** Убрать ячейки с долей флюида
          `< 1e−2`: `max|φ|` падает `3.65e+06 -> 18.1` при потере `0.008 %`
          объёма. Ключ `xthin=`.
    §718  **ВЫРОЖДЕННЫЕ ЭЛЕМЕНТЫ ПО ПЛОЩАДИ — второй усилитель.** Убрать
          элементы площадью `< 1e−2 h²`: первый проход `6.3e+10 -> 18.1`,
          откатов ноль (было `430`), два такта СЖИМАЮТСЯ (`0.185`). Ключ
          `xsemin=`.
    §647  Полоса размера полигона ставится (`ceil=poly=15`), но кадр НЕ
          полигонный: `11.3 ×` меньше треугольников дают `1.32 ×` растра.
          `16` нитей растра дают `5 %`; перерисовка `5.55 ×`; счётчик фрагментов
          ГОНОЧНЫЙ — прежние «глубина перекрытия» цитировать нельзя.
    §656  `11.5 %` ячеек Bistro несут ДВА И БОЛЕЕ куска поверхности (dual
          contouring сваривает их одной вершиной). Про CMS — §653; дешевле
          Manifold Dual Contouring, и код разделения на петли уже написан.
    §640  `dmax` (сверка среза с деревом) НЕ ПАДАЛ и не мог: метрика структурно
          слепа к кванту узла. Строка переименована (§642).

## ЧЕГО НЕТ, И ЭТО НАДО ЗНАТЬ

    развёртка НЕ СХОДИТСЯ на разрезанной геометрии — см. задание вверху;
    BRDF нет никакой (§625); глянец ординатами у́же `27°` при `ND = 64` не
        берётся (§538), а деление «прямой/косвенный» неверно — делить надо по
        УГЛОВОЙ ШИРИНЕ (§685);
    внешнего эталона отскока нет (§564): все эталоны — свой же код;
    инкрементности в коде нет, хотя у обеих схем она есть ПО УСТРОЙСТВУ (§652);
    при чистом ПОВОРОТЕ камеры срез можно не пересобирать (`lod_stop` зависит
        только от расстояния и пирамиды) — `661` мс платятся зря, не сделано.

## ОШИБКИ МЕТОДА, СОБРАННЫЕ ЗА ЭТОТ ЗАХОД (повторять не надо)

    А1067  у негативного контроля называть ПУТЬ СИГНАЛА и где он может умереть;
    §714   различающий замер НЕДЕЙСТВИТЕЛЕН, если в системе есть дефект
           БОЛЬШЕГО порядка (свип по порогу шёл при `1e+34` и ничего не показал);
    А1086  не предсказывать интервал, не спросив, может ли величина отличаться
           от предсказанного (`hs_out/hs_se ≡ 1` по симметрии набора);
    §720   `κ` матрицы масс ЭЛЕМЕНТА бессмысленна: она вырождена у всех по
           построению (К39). Спас только контроль «типичный рядом»;
    §726   медиана усиления `> 1` есть признак НАКОПЛЕНИЯ сходящегося ряда, а
           не расходимости. Критерий, не отличающий искомое от нормы, негоден;
    А1083/§718  не предсказывать ДОЛЮ, не измерив знаменатель (дважды за два дня).

## КЛЮЧИ, ЗАВЕДЁННЫЕ 08-19…08-21

    nodevq1    §638 НК: вершина УЗЛА в один бит на ось (сверка среза слепа к ней)
    xhall      §667: прежний источник-площадка от камеры (для воспроизведения)
    xrho=F     альбедо ФАСЕТА (умолчание 0.7); `0` — чистый влёт, точная приёмка
    xemitfacet §670 НК: излучение по ФАСЕТУ, а не по элементу (возвращает π·1.36)
    xit=N, xtol=F   потолок итераций и допуск развёртки (были зашиты 30 и 1e−4)
    xnolim     §677: выключить ограничитель — оператор становится ЛИНЕЙНЫМ
    xwholemass §697: матрица масс ЦЕЛОЙ ячейки вместо флюидной (различитель,
               прогон НЕФИЗИЧЕН)
    xconst     §699: печь на разрезе (влёт постоянный, альбедо 1). На Bistro
               ПУСТА: граница куба погружена в материал
    xthin=F    §711: ячейки с долей флюида ниже порога — в сплошные
    xsemin=F   §717: элементы площадью ниже порога `h²` — из носителя
    xsolidrho=F §707: альбедо стыка «флюид — сплошное». ВЫКЛЮЧЕНО (усиливает)
## СЦЕНЫ — ЧИТАТЬ ОБЯЗАТЕЛЬНО

**ДВЕ СЦЕНЫ УДАЛЕНЫ ПО ПРЯМОМУ УКАЗАНИЮ ПОЛЬЗОВАТЕЛЯ. Не возвращать.**

    КОНФЕРЕНЦ-ЗАЛ  08-11. `12.63 %` рёбер с одним владельцем; на нём неизмеримы
                   ориентация, заливка внутренностей и знаковый объём. **Софт
                   для лечения входных сеток проект НЕ ПИШЕТ** — сцену с
                   негодной сеткой не чинят, а не берут.
    RUNGHOLT       08-12 (§565). Экспорт карты Minecraft, воксельный ПО
                   ПРОИСХОЖДЕНИЮ. Для роли МАСШТАБ это было условием, для
                   постановки «насколько недостаёт до UE5» — пороком: блочность
                   там от исходника, а не от рендерера. Убран и из
                   `fetch_scene.sh`. ГОРОДСКОЙ МАСШТАБНОЙ СТУПЕНИ В СОТНИ
                   МЕТРОВ У ПРОЕКТА БОЛЬШЕ НЕТ — это названный пробел.

    assets/synth/room.obj   ОСНОВНАЯ, scripts/gen_room.sh, масштаб 1.0.
                            Стены С ТОЛЩИНОЙ, три масштаба кривизны, колонна,
                            стол, 11 материалов (room.mtl). Открытых рёбер 0,
                            знаковый объём 36.4582 м³ сходится с заливкой и с
                            матрицами масс стыка.
    room_thin.obj           НК: лист нулевой толщины (метрику НЕ ломает).
    room_soup.obj           НК: несогласованная обмотка (ломает).
    spheres.obj             чистая кривизна, gen_spheres.sh.
    box.obj, plates.obj     печь и E_ind = ρ·E_dir.
    assets/bistro/          BISTRO (ORCA, CC-BY 4.0), заведён 08-13 (§583).
                            МАСШТАБ 0.01 (сцена в САНТИМЕТРАХ, найдено замером).
                            exterior.obj 2 833 330 тр, interior.obj 1 020 907.
                            410 текстур; переведённые лежат в bistro/ppm256
                            (готовит scripts/tex_prep.sh, размер 256).
                            Габарит при 0.01: 108.60 x 31.93 x 115.26 м.
                            Текстурные архивы качаются БЕЗ расширения:
                            .../bistro/BuildingTextures (не .zip).
    San_Miguel              РАЗБРОС ДЕТАЛИ и число полигонов: 4 963 199 тр,
                            281 материал, 323 текстуры. Это ДВОР, а не улица.
                            Если слишком подробна —
                            ОТКУСЫВАТЬ САМОЕ ДЕТАЛЬНОЕ (у Сан-Мигеля есть
                            low-poly), а не искать сцену помельче.

`HZ_CFG_HALL_OBJ` теперь указывает на комнату: полтора десятка старых замерных
инструментов завязаны на это имя, а комната построена вокруг ТЕХ ЖЕ камеры и ламп.

## ДИСЦИПЛИНА ШАГА — БЕЗ ИЗЪЯТИЙ

  1. план шага и ПРЕДСКАЗАНИЯ с интервалами — в конец `PLAN_ELEMENTS.md` ДО кода,
     отдельным коммитом;
  2. аудит плана до кода, находки серией А;
  3. код; гейт `CLAUDE.md` целиком (clang-format, `scripts/ccheck.sh` до CLEAN,
     `scripts/lean.sh`, санитайзеры; CBMC на опасном);
  4. прогон с НЕГАТИВНЫМ КОНТРОЛЕМ, который обязан провалиться предсказанным
     образом;
  5. доклад: вывод первым абзацем, две страницы, у всякого числа три пометы —
     на чём, какова ПОПУЛЯЦИЯ, чего НЕ покрывает;
  6. аудит исполнения: что не сбылось, что измерено не тем, что осталось.

Прерываться и спрашивать ТОЛЬКО при срабатывании «УБИВАЕТ» или если нужно править
`src/cut/poly3.c`.

## ПРАВИЛА, НАРУШЕННЫЕ 08-11/12 — НЕ ПОВТОРЯТЬ

    §498  считать байты на элемент и умножать на число элементов ДО того, как
          структура написана. Нарушено: узел излучателя вышел 616 Б (шесть
          корзин каждому узлу, всё в double) при нужных 90…140.
    А917  ОТСУТСТВИЕ пометки — это «неизвестно», а не значение по умолчанию.
          Нарушено: `hz_flat = 0` прочтено как «шероховатый» и запретило
          агрегацию везде. Третий случай за проект после dmax = 0 и noct = 0.
    А913  если величина не отзывается на измельчение — искать в ПОСТАНОВКЕ, а не
          в дискретизации. Трижды подряд искал в разрешении.
    А916  прежде чем чинить проводку параметра в третий раз — ПОСЧИТАТЬ, какого
          значения следует ждать.
    А824  проверять ПРИБОР до того, как строить на его числах шаг.
    А891  у всякой величины, которую картинка читает по ячейке, обязан быть
          счётчик покрытия рядом.

## КЛЮЧИ `pfield`, ЗАВЕДЁННЫЕ 08-11/12

    solid      заливка внутренностей и сверка со знаковым объёмом
    xfer       стык с переносом (фасеты + маска -> tr3_cut_build)
    xsweep     плюс сама развёртка по ординатам; nmu=N — ординаты; xtrace
    treesweep  свип по ДЕРЕВУ; sweepthr=N — дробление по допуску (действует от ~5)
    seed=R     засев окрестности источника маршем (R=1 даёт 5x по макс. ошибке)
    hgather=E  иерархический отскок; hspread=T — допуск разброса; onebin — НК
    lobetest   ГРАНИЦА УЗОСТИ доли: сколько несёт данное ND (Ф9., §538);
               dlobeflat — НК; dns=/dks= — перебивка узости и доли
    dsweep     радианс по НАПРАВЛЕНИЯМ на дереве (Ф8., §534); dnmu=, dnphi= —
               набор §2; dpass=N — отскоки; dirsall — НК; dblkopen — верхняя
               граница вилки
    emitalb2   вернуть СТАРОЕ неверное альбедо излучателя (§531, только сверка)
    albone     все альбедо = 1 (фальсификатор §531)
    ptlight    вернуть ТОЧЕЧНЫЙ источник (§543, НК; закон pi*L его отвергает)
    ffull      прежний булев заслон (§547, приёмка сведения); fnotrans —
               только ослабление излучения; fzero — вырожденный НК
    cmpcell    поячеечное сличение свипа с гатером (§559); cmpself — ложный
               ноль; gstride=N — прореживание гатера (НК)
    render     ТОЛЬКО РЕНДЕР: диагностика выключена, тайминги остаются (§566)
    sun[=x,y,z] СОЛНЦЕ — направленный источник, одна проба вместо 64 (§571)
    cam=ex,ey,ez,ax,ay,az   камера явно; camauto — снаружи габарита;
               caminside — внутрь по занятости (§574, §583: вид им НЕ выбирать)
    poly=N     ПОЛ по экранному размеру ячейки, пикс (§584). 8 — рабочее
    nomemo, memoscramble — НК §585. Первый выключает память ответа критерия
               среза (обход возвращается к 485 мс), второй переворачивает ответ
               у каждого 997-го узла с детьми, КРОМЕ КОРНЯ, — картинка обязана
               разойтись (перевёрнуто 87 ответов из 105 687, то есть 0.082 %)
    ceil=N     потолок по угловому размеру ячейки (§567); 0 — выключен
    texflat, texnomip, nofrustum, omp1, gam16 — НЕГАТИВНЫЕ КОНТРОЛИ
    diffbench=N СТЕНД (§551 в ред. §555), коробка N³: снос и разброс ПОРОЗНЬ;
               onenb, scharax — НК (резко, но мимо цели); schar — короткие
               характеристики (измерены, в рабочий путь НЕ приняты)
    hitsweep   доля листьев свипа, затронутых ударом; hitr=R — радиус (НК 2.0);
               hitnoocc — проверка на ложный ноль (занятость не правится)
    emit=T     огрублённые излучатели по камере (тупик, §510)
    indmeas    считать долю косвенного, идущую сквозь заслоны; indvis — отбрасывать
    ss2        растр в 2x и свёртка 2x2; res=N — разрешение растра
    lit        кадр со светом; thr=N — порог LOD среза
    passes=N, raysweep, noshift, nonsum, nrmflip, xcorner, xinnerfluid — НК

Картинки — в `img/`, не в `build/`. В `wave/` не входить и оттуда не читать.
Коммитить прямо в `main`. Сцен нет — `bash scripts/fetch_scene.sh assets`,
комната — `bash scripts/gen_room.sh`.

## КЛЮЧИ, ЗАВЕДЁННЫЕ 08-13 (§585…§636)

    keonly     `b0` ТОЛЬКО из `Ke` — ни солнца, ни площадки (§618). Сцена светит
               сама; подразумевает `render` и кадр со светом
    nokemit    НК: не читать `Ke`. При `keonly` даёт РОВНО ноль
    hbounce=N  число отскоков (ряд Неймана, §607). Второй и дальше — 0.55 с
    hcontrib=F дробление ПО ВКЛАДУ, доля от масштаба сцены (§621). `1e-1` — рабочее
    hangle     НК: прежний УГЛОВОЙ критерий (`hgather=`), не сходится (§622)
    gpoint     НК: прежнее точечное ядро — отскок взрывается (§600)
    gwide      НК: поправка ядра в 1000 раз — далёкое поле обязано уехать
    gnonorm    НК: без нормировки суммы формфакторов (§611)
    gflatvis   НК: прежний ПЛОСКИЙ марш заслона вместо иерархического (§616)
    noindvis   НК: БЕЗ заслонов (прежнее умолчание до §613)
    nolinkcache НК: не хранить связи — каждый отскок снова 33 с (§613)
    indslice   НК: косвенный свет в СРЕЗЕ, как было до §597
    indnolift  НК: без подъёма по иерархии; indflat — подъём невзвешенный
    nomemo     НК: без памяти ответа критерия среза (§585)
    memoscramble НК: перевернуть ответ у каждого 997-го узла, кроме корня
    walk       ХОДЬБА (нужен `build/pwalk`, §589)
    res=WxH    разрешение; умолчание `1920x1080` (§631)

## КЛЮЧИ, ЗАВЕДЁННЫЕ 08-21…08-23 (§727…§773)

    xunit       §729: оператор такта на ЕДИНИЧНОМ состоянии (усилители с адресами)
    xchain=N    §731: трасса цепочки к ячейке (требует xunit)
    xcell=,xdir= §733: вскрытие обновления ячейки (A, rhs, раскладка строки 0)
    xnomaxp     §735 НК: выключить принцип максимума — вернуть расходимость
    xmatrho     §737/§739: материальные альбедо, атрибуция по близости к плоскости
    xmatfar     §739 НК: наихудший треугольник
    xrhoscale=F §742: множитель альбедо — извлечение ряда отскоков (B1, B2/B1)
    xcmp        §744: поячеечное сличение свипа с ядром §597 (мост node_of_cell)
    xrelax=F    §748: демпфирование состояния (0.7 — рабочее у ρs≈1)
    xdsa/xdsagal/xdsadiff/xdsasplit/xdsalev/xdsagain/xdsaeps — §750…§756:
                двухсеточная машинерия (линия закрыта §757, выключена)
    xframe      §758/§760: кадр развёрткой (свип→узлы→irr; дыры закрыты подъёмом)
    xcontrib    §768: прибор вклада (kill-перевозмущения ρ→0 по классам)
    xcoarse=D0  §772: огрубление приёмников лестницей D0·S (fail-closed;
                принимает мало — предел union-реза, §773)
    xbounce=N   §774: лестница N прокидок-отскоков + замыкание хвоста q̂/(1−q̂)
    xtailq=F    §774: 0 — чистое усечение; >0 — форсировать q (НК); умолч. измерить
    xbcmp       §774: же-процессная база + сравнение (зона, поэлементные eirr)
    ПАРАЛЛЕЛИЗМ §770: такт по направлениям (trace-путь однопоточно-побитовый)

## КЛЮЧЕВЫЕ ЧИСЛА ЗАХОДА (для сверки прогонов)

    А1228 ЗАКРЫТА (§777): psin 2.38905/psin_dir 1.7133 — это конфигурация С
        xnolim; каноника (ограничитель ВКЛ) даёт записанные 2.3429/1.6925
    лестница отскоков (§774 в канонике, Bistro N=5): 2.23 с против 13.40 с
        (×6.0), q̂ 0.6274, Δpsin −0.63 %, Δ зоны +0.38 %, хвост макс 0.85,
        отрицательных eirr 0; в xnolim-конфигурации: 2.35 с (×6.2), q̂ 0.5447,
        −1.49 %/+0.23 %, N=8: 4.0 с, −1.06 %, +0.007 %
    рабочий свип (mat, ρs=0, lev=7, nmu=2, 16 ядер): 40 тактов, 14.3 с,
        psin 2.3429, недостача 0.6 %; ×4.06 против одного потока (58.0 с)
    ряд свипа: B1 = 0.249, B2/B1 = 0.284; доля косвета 0.388 (эталон сбора 0.366)
    внешний эталон (cavity, gen_cavity.sh, xfernosolid!): протечка полости
        L = 23.1/14.8/8.9 % при lev 5/6/7 (~h^0.7), от nmu не зависит; ядро §597
        теряет 7.6 %/отскок — знак §743 подтверждён арбитром
    вклад в ближнюю зону (<15 м): дальше 20/40 м — 0.00 %; дальше 10 м — 8.2 %;
        обратные — 6.1 %
