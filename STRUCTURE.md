# STRUCTURE.md — структура нового представления (согласована 09-10)

> СТАТУС: структура принята в обсуждении с пользователем 09-10. КОДА НЕТ.
> Решатель — развёртка (свип, обход фронта) на CPU; GPU — потом.
> Старый путь (`tr3_cut`) остаётся ПРОДЮСЕРОМ и ЭТАЛОНОМ до паритета: новое
> представление сначала собирается как компактный снимок посчитанного старым.
> Первый потребитель — собственная синтетика (room/cavity есть; наклонные
> плоскости/складки, листва-карточки, инстанс-таблица — построить), UE5-пакеты
> после того, как структура покажет бюджет.
> Предшественник — `new-structure.md` (07-28): его находки (закон формы §2,
> свет §6, замеры §9bis) остаются в силе; его ПРЕДСТАВЛЕНИЕ (политопы,
> разрезы ячеек, плоскости-патчи) в новую структуру НЕ переносится.
> Слова «фасет» здесь нет и не будет: это производный полигон старой
> машинерии реза, самой машинерии больше нет.

## 0. Почему новая структура (одним абзацем)

Свип перечитывает 111.6 МБ матриц граней на каждом направлении каждой
итерации (≥150 ГБ за прогон при 14.3 с, эффективная полоса ~10 ГБ/с против
22.6 ГБ/с STREAM) — старый фронт платит полосой и латентностью, не
арифметикой. Причина структурная: клетки привязаны к резу геометрии, поэтому
перенос бокс→бокс не аналитичен и его приходится ХРАНИТЬ; поле живёт в
объёме у поверхности, и отсюда щепки, доли флюида, усилители
(§714/§718/§729–734). Новая структура убирает обе причины: клетки — боксы
пирамиды (перенос аналитичен, формула вместо матриц), поле — на поверхности.

## 1. Инварианты (проверяются, не обсуждаются)

1. **Поверхность вокселями не представляется НИГДЕ** — ни в растре, ни в
   BC, ни в агрегатах, ни в занятости. Воксель — индекс, марш и гранулярность
   поля, не геометрия. «Дубовая» геометрия невозижима: нет операции,
   которая делает геометрию кубами.
2. **Носители не перекрываются.** Поле живёт на РОВНО ОДНОМ уровне пирамиды
   в каждом регионе. Иерархического базиса нет — он убивает прямую
   развёртку (§3 `new-structure.md`).
3. **Узел = один локальный шаг переноса.** Оператор одинаков на всех узлах;
   иерархия меняет точность коэффициентов узла, не решатель.
4. **Закон потока:** итерация читает каждый байт полевых массивов не более
   одного раза на ординату; геометрия китов в итерациях не читается вовсе.
5. **Разбиение точно:** перекрывающихся полигонов нет; владение клеткой —
   корзина марша и гранулярность поля, НЕ геометрическая операция над
   поверхностью (клип куска выводим и не хранится).
6. **Никаких офлайн-предвычислений над всей сценой.** Всё, что зависит от
   геометрии, выводимо локально из (киты, размещения). Это условие
   разрушаемости: у Nanite ломается глобальный офлайн-DAG, здесь глобальных
   предвычисленных вещей нет ни одной.
7. **Бюджет байтов у всякой записи, счёт до кода** (урок §498).

## 2. Три слоя

### 2.1 Пирамида шагов переноса — единственный индекс

Регулярные уровни ×8 над габаритом сцены. Хранение РАЗРЕЖЕННОЕ: существуют
только непустые узлы и их предки; пустота стоит ноль байт.

Узел (8–16 Б): состояние (2 бита), офсет/ссылка (на детей | на куски | на
агрегат), слоты ошибки и флагов. Запись одинакова на всех уровнях; разные
РАЗМЕРЫ клеток в метрах не создают разных ЗАПИСЕЙ в байтах.

Три состояния узла:

    ПУСТ     — геометрии (с раздуванием на ошибку лестницы) нет: стоп,
               один шаг переноса; в воздухе σ=0, перенос точен.
    АГРЕГАТ  — дальний регион: всё содержимое участвует ОДНИМ BC-событием
               по отклику. Вниз не спускаемся — и внизу НИЧЕГО НЕ ХРАНИТСЯ.
    ДРОБЛЁН  — ближний регион: спуск до активного уровня, где лежат
               полигоны и куски поля.

Занятость консервативна (fail-closed): «пусто» только если геометрии нет
заведомо. На coarse-уровнях — с раздуванием на ошибку упрощения лестницы.
Активный уровень региона — ступенчатый, с гистерезисом; глубина спуска в
каждой точке = активный уровень этого региона. Верхние уровни резидентны в
кеше (общие для всех направлений и итераций).

### 2.2 Киты × размещения — геометрия ×1

- **КИТ**: вершины (одна копия), треугольники, материалы, локальная
  пирамида занятости. Поверхность кита — его ИСХОДНЫЕ треугольники; ничто
  их не переписывает. Нет DC, нет фасет, нет dmax — весь класс ошибок
  переписывания исчезает (вывернутости 9–15 %, чешуя, дыры §778).
- **РАЗМЕЩЕНИЕ** (экземпляр): аффинное преобразование + id кита, 32–48 Б.
  Клип — в локальных координатах кита; переносится биективно, разбиение
  остаётся точным. Поле — на ЭКЗЕМЛЯРЕ: массивы слоя 2.3 индексируются
  (размещение, кусок кита).
- **ЛЕСТНИЦА КИТА** (отложена до масштаба; слоты зарезервированы):
  офлайн-декимация в ЗАМЕЩАЮЩИЕ уровни (сумма лестницы ~×2 worst case,
  геометрическая прогрессия — уровни не дублируют, а заменяют); упрощение
  НЕ двигает вершины на запертых границах (щели на смене уровня невозможны
  по построению); каждый уровень несёт свою ошибку. От Nanite берётся
  ТОЛЬКО офлайн-лестница: per-frame отбор кластеров и софтверный растр не
  берутся. Клетки пирамиды индексируют АКТИВНЫЙ уровень своего региона.

### 2.3 Поле на экземплярах

- **НОСИТЕЛЬ ПОЛЯ**: кусок = (tri, клетка, E, материал), 12–16 Б. Это
  минимально возможная запись: полю надо где-то жить на поверхности, и
  дешевле «адрес + E» не бывает. Геометрия куска выводима клипом
  (tri ∩ коробка клетки) и НЕ хранится; нормаль одна на треугольник.
  На чистой мелкополигональной сцене кусок вырождается в треугольник
  (клип не отрезает ничего) — слой почти бесплатен; клип работает ровно
  там, где крупный полигон иначе дал бы безградиентное поле.
- **РАСКЛАДКА**: SoA [клетка][кусок][...], ордината co-упакована во
  внутреннем измерении — готовый SIMD-вектор (32 ординаты = 8/16 lane);
  заголовок куска читается 1 раз на итерацию, не 32.
- **ОРДИНАТНОЕ ПОЛЕ ТРАНЗИЕНТНО**, персистентен только E. Диффузный отскок
  в ординату ω — ρ_d·E/π на лету; узкий лепесток — LUT вне ординат
  (А1361); зеркало — лей-образ. Обоснование двоякое: байты (4 Б против
  128 Б на кусок — иначе рабочий набор вырастает в ~30 раз и вылетает из
  кеша) и паритет со старым сверяется через E (диагностический bout-слой
  при необходимости).
- **ОБЪЁМНАЯ СРЕДА**: редкий массив клеток с (σ_t, σ_s, индикатриса). У
  воздуха отсутствует. Тот же слот обслуживает печь, дым и облако осколков.
- **АГРЕГАТ**: угловой отклик (моменты/матрица; разрешение по ширине
  лепестка, А1361) + консервативная оболочка. Строится из треугольников
  АКТИВНОГО уровня, НИКОГДА из воксельной занятости (иначе блоки
  возвращаются в свет через заднюю дверь).

### Таблица байтов (бюджет; счёт до кода)

| запись | байт | популяция |
|---|---|---|
| узел пирамиды | 8–16 | непустые узлы + предки |
| кусок, персистент | 12–16 | куски ближнего кольца |
| кусок в CSR клетки | 4 | то же |
| вершина кита | 12 | ×1 на кит |
| треугольник кита | 16–20 (+вершины) | ×1 на кит |
| размещение | 32–48 | экземпляры |
| агрегат | моменты/матрица по лепестку | дальние регионы |
| среда (редкая) | ~24+поле клетки | клетки с σ>0 |
| ординатное поле | 0 (транзиент) | — |
| индекс (bbox-листья + предки) | 20·(кусок/клетка)²…(кусок/клетка)³ | замерено §833; в рабочем режиме кусок ≤ клетка ⇒ ≤ 8 пометок на кусок, 2–12 Б/кусок |

Город (записано 09-10, подтверждается замером): 4e7 м² → геометрия с
декимацией ~2–3 ГБ, поле ближнего кольца — единицы ГБ, запас ×10–20 в
64 ГБ. Дальние регионы кусков НЕ несут — только агрегаты.

## 3. Марш и фронт

- **ПОРЯДОК НАПРАВЛЕНИЯ = формула** от (доминирующая ось, знаки) —
  перестановка шага регулярной сетки. Октантная симметрия: 4 базовых
  порядка × 8 зеркальных флипов = 32 направления. Per-направленного
  хранения нет; топологических сортировок нет (в старой структуре — на
  каждое направление каждой итерации).
- **Цепь направления**: прыжки через ПУСТЫЕ → одно АГРЕГАТ-событие на
  дальний регион → события на кусках ДРОБЛЁНОГО → прыжки. Длина цепи =
  пересечения поверхности ближнего кольца + O(log) прыжков.
- **РАСКЛАДКА УРОВНЕЙ ПЛИТКАМИ**: 4³ клетки ≈ 512 записей ≈ 8 КБ,
  Morton-порядок — любой из 32 порядков фронта живёт одновременно в
  нескольких плитках; плитка целиком в L1.
- **Параллелизм**: по направлениям (×4.06 на 16 ядрах, §770) или блочными
  фронтами (шире фронт — лучше кеш, «ширина фронтов ×ND») — замер, не
  принцип.
- **ДВА МЕРЯЕМЫХ ПУНКТА** (померить прежде, чем обещать):
  (1) порядок СМЕШАННЫХ уровней — перестановки обязаны вкладываться
  (дети coarse-клетки идут подряд, их ω-интервал вложен в её интервал);
  циклы, если появятся, ловятся первым прогоном на синтетике;
  (2) БЛОЧНЫЕ ФРОНТЫ — совместимость 32 направлений в пространственном
  чанке (кусок по всем ординатам разом только когда по всем направлениям
  его верховые закрыты).

## 4. Три лестницы (не сращивать и не перемножать)

| лестница | чем делается | валюта |
|---|---|---|
| геометрия (насколько подробные треугольники) | офлайн-лестница китов | пиксели/ошибка |
| поле (насколько подробно свет) | отскоки (xbounce ×6.0, §774) + лепесток (А1361) + агрегаты | вклад (§768: <0.01 % за 1.5×R) |
| индекс | не LOD вовсе | — |

Поле не бывает тоньше геометрии своего региона; грубее — всегда (агрегаты).
Ш7: «геометрия и поле с разными разрешениями» (пол в тени стола).
Уровни ступенчатые во времени: двигается только набор активных уровней
региона — инкрементность.

**Иерархия направлений как ПЕРЕМЕННОЕ ЧИСЛО ординат отвергнута** (хвост на
редких ординатах: УБИВАЕТ-а §811, хвост несёт 71 % psin; глянец у́же 27° и
при ND=64 не берётся, §538 — углового разрешения МАЛО). Иерархия
направлений как МЕХАНИКА прохода — да: формула порядка, октантные флипы,
со-упаковка, прыжки пирамиды, параллелизм. Угловая работа дальнего края
живёт ВНУТРИ агрегатного отклика, не в числе ординат марша.

## 5. Рацион фронта: старая/новая (бюджет; приёмка ниже)

Эталон — Bistro, Np ≈ 2.6e6 кусков (кластеризованные §800 — та же
поверхность, что несла старая структура).

| статья | старая | новая |
|---|---|---|
| матрицы граней, за итерацию | 3.6 ГБ (32 × 111.6 МБ, замер) | **0** |
| состояние поля, за итерацию | ~2.9 ГБ (45 МБ double × 32 × r/w) | ~0.05 ГБ (E float + заголовок) |
| списки кусков | внутри матриц | 0.03–0.33 ГБ (CSR × 32; низ — с блочными фронтами) |
| узлы индекса | разрезанные клетки, косвенные | 0.01–0.05 ГБ (верх в L2) |
| итого за итерацию | ~4–6.5 ГБ ≈ 2.5 КБ/элемент | ~0.1–0.5 ГБ ≈ 40–190 Б/кусок |
| за прогон | ≥150 ГБ, 40 тактов, 14.3 с | 1–4 ГБ, K=5–8 (лестница ×6.0) |

**ПРИЁМКА первого прогона (фальсификаторы назначены заранее):**
- время ≥ ×3 против 2.23 с — старая С ЛЕСТНИЦЕЙ (xbounce N=5, замер):
  лестница ортогональна структуре, сравнивать против 14.3 с — приписывать
  чужой выигрыш;
- эффективная полоса ≥ 20 ГБ/с (старая сняла ~10 при STREAM 22.6);
- CSR-статья в нижней вилке при блочных фронтах;
- SIMD-эталон: скалярный прогон, затем векторизация по ординатам как
  сверка (после снятия матриц доминирует арифметика — 25–80 мс/итерацию
  скалярно; раскладка под вектор уже выбрана).
Трафик ×40–140 — это БАЙТЫ, не секунды; в секунды конвертируется ×3–10.
Замер §833 подтверждает носитель: кусок+CSR = 20 Б/кусок независимо от
уровня клетки (room и cavity05, 8 точек); индексная статья вне рабочего
режима (клетка ≪ кусок) растёт как (кусок/клетка)² — в полном конвейере
этот режим недостижим: клип держит кусок ≤ клетку (А1463).

## 6. Разрушение/взрывы

Замена экземпляра кита (разрушенный вариант — тоже кит) → перебиннинг
коробки изменения (счётчики/CSR локально, O(объёма изменения), узлы
меняют состояние — переходы спроектированы) → пересборка агрегата региона
→ инвалидация поля локально (тёплый старт §808; свет вне области не знает
о стене мгновенно). Облако осколков/дым = объёмная среда (слот 2.3);
вспышка = источник. Цена кадра с разрушением ∝ диаметр инвалидации.
Инстансинг нативно даёт тысячи осколков; поштучно полю они не по карману —
облако гомогенизируется в среду (§4.2 `new-structure.md`).

## 7. Классы болезней, которые исчезают по построению

| болезнь старой структуры | почему исчезла |
|---|---|
| щепки, доли флюида, усилители (§714/§718/§729–734) | поле не режет объём: у труб нет флюидных долей, у кусков — объёма |
| чешуя (§793–§803) | DC нет; нормали авторские |
| дыры покрытия/протечка (§766/§778) | поверхность не переписывается |
| предел union-реза (А1220) | coarse-узел описан откликом, резать нечего |
| матрицы граней, 143 ГБ/прогон | перенос бокс→бокс аналитичен |
| «Descent 1» большие полигоны | лестница китов; клип кусков даёт гранулярность поля |
| dmax/вывернутости (§463) | поверхность никто не двигал |

## 8. Что берётся из старого кода, что нет

БЕРЁТСЯ: гейт качества C целиком; генераторы синтетики (gen_room.sh,
gen_cavity.sh, gen_spheres.sh); приборы §831-класса; алгоритм лестницы
отскоков (xbounce ×6.0); модель отклика А1361; тёплый старт §808.
НЕ ПЕРЕНОСИТСЯ: разрез, DC, фасеты, матрицы граней, объёмное поле у
поверхности, per-направленные сортировки.

## 9. Порядок работ

1. **СНИМОК**: старый продюсер (room/cavity, xobjpiece) → новые массивы
   (куски, CSR, узлы, среда-слот), таблица бюджета, сверка счётчиков
   (Σ кусков, Σ площади, Σ эмиссии). Без свипа.
2. **МАРШ**: пирамида с тремя состояниями, порядок формулой, прыжки;
   сверка с марш-эталоном старого (НК: scramble занятости).
3. **СВИП**: итерации на новых массивах, паритет psin/E со старым;
   приёмка §5.
4. Лестницы поверх (отскоки, лепесток, агрегаты), инстанс-сцена,
   разрушение — после того, как 1–3 в допуске.
