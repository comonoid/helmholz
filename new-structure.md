# new-structure.md — обсуждение принципиального ускорения

Файл заведён 07-28 по просьбе пользователя. Сюда пишутся ТОЛЬКО те соображения,
которые пережили попытку опровержения, и у каждого назван фальсификатор — условие,
при котором оно перестаёт быть верным. Отвергнутое сюда не пишется (если явно не
попрошено); отвергнутое с причиной живёт в `PLAN*.md`.

Ничего в репозитории этот файл не меняет: это записная книжка обсуждения, а не план.

---

## 0. ПОСТАНОВКА: СТЕНА — РАЗМЕР СИСТЕМЫ, А НЕ ЕЁ РАЗРЕЖЕННОСТЬ

**Соображение пользователя (07-28), и оно верное:** на сцене уровня Unreal Engine
объёмы таковы, что сводят на нет все преимущества постановки. С ПАМЯТЬЮ ещё можно
работать. Но это всё — данные, из которых потом составляется линейная система, и
она будет медленной **несмотря на любую разреженность, просто из-за размера**.

Ниже это переведено в числа на ИЗМЕРЕННОЙ машине разработки (К79) и на карте
среднего верхнего сегмента (24 ГБ, поправка К82 — 6 ГБ есть ограничение машины, а
не метода).

### Исходные величины

| величина | значение | источник |
|---|---|---|
| элементов на городе | `5e8` | `CLAUDE.md`, лестница LOD |
| неизвестных | `n = 5e8 · ND` | ND = несущих на элемент |
| комплексный FP32 / FP64 | 8 Б / 16 Б | |
| рабочий набор Крылова | ≈ 5 векторов длины `n` | LSQR: `x, u, v, w` + временные |
| трафик на итерацию | ≈ 10 проходов по `n` | 2 matvec + ~5 axpy/dot |
| карта разработки | 6 ГБ, 336 ГБ/с, FP32 5.3 Тфлопс, **FP64 166 Гфлопс** | К79 |
| ОЗУ | 125 ГБ, **22.6 ГБ/с ЗАМЕРЕНО** (не 41.6 справочных) | К79 |
| итераций Трефтц-DG | 66…193 | `tools/tdg2d.c`, PLAN.md |

### Что из этого следует

**ОДИН вектор решения на городе при ND = 1 есть 4 ГБ в FP32.** Не матрица —
ВЕКТОР. Он один почти заполняет карту разработки.

| ND | вектор, FP32 | набор Крылова (×5) | где помещается | кадр (66 итераций) |
|---|---|---|---|---|
| 1 | 4 ГБ | **20 ГБ** | резидентно на 24 ГБ | `40 ГБ/ит ÷ 235 ГБ/с` → **11 с** |
| 4 | 16 ГБ | 80 ГБ | только ОЗУ | `160 ГБ/ит ÷ 22.6 ГБ/с` → **8 мин** |
| 10 | 40 ГБ | 200 ГБ | **не помещается в 125 ГБ ОЗУ** | — |

Это НИЖНЯЯ оценка: matrix-free, идеальный предобуславливатель, нулевая стоимость
оператора, ноль обменов. Всё, что делает решатель, — трогает векторы.

### Три вывода, которые режут классы ответов

**(1) «Лучший решатель» задачу не решает.** При ND = 4 восемь минут на кадр —
это стоимость ПАМЯТИ, при матрице ценой ноль. Предобуславливатель, снижающий
число итераций вдвое, даёт четыре минуты. Класс не меняется. Любое
принципиальное ускорение обязано уменьшать `n`, а не множитель перед ним.

**(2) Матрицу хранить нельзя ни при каком ND ≥ 2.** Скелетные связи Трефтц-DG:
6 граней на ячейку, блок `ND × ND`. При ND = 1 это `3e9` элементов = 24 ГБ,
больше всего набора векторов; при ND = 4 — 384 ГБ. Значит оператор
пересчитывается на лету всегда. Это выполнимо (аналитические элементы, FP32
5.3 Тфлопс), но это ТРЕБОВАНИЕ, а не выбор.

**(3) ND — единственная величина, решающая исход, и она НЕ ИЗМЕРЕНА.** Между
ND = 1 и ND = 10 лежит граница между 11 секундами и «не существует». Это ровно
то, что должен померить M9c, и ровно то, на что `tdg2d` уже дал предупреждение:
равномерному вееру нужно `ND ~ 2π·kW` (при W = 100λ — 4000 направлений), а
знание направления даёт единицы. **Вопрос «сколько несущих на элемент» — это не
вопрос точности, это вопрос существования продукта на городе.**

### Рычагов на `n` ровно три, других нет

- **(а) меньше элементов** — агрегация/LOD. Уже в архитектуре (она и даёт `5e8`
  вместо `1e22`), но подпёрта незакрытым: ошибка ЗАМЕНЫ структуры откликом не
  вычисляется ничем (`CLAUDE.md`, случай iii; `hz_facet.dmax` покрывает только
  СМЕЩЕНИЕ).
- **(б) меньше несущих на элемент** — то есть ЗНАТЬ направления, а не
  сэмплировать. См. (3).
- **(в) не держать весь `n` одновременно** — разложение области со скользящим
  рабочим набором вместо резидентного вектора. Единственный рычаг, который
  превращает 200 ГБ в помещающееся; и единственный, куда естественно ложится
  «сеточное обуславливание», если иерархия есть РАЗВЁРТКА (свип), а не V-цикл
  над резидентным вектором.

**Фальсификатор раздела:** любой замер, показывающий, что рабочий набор Крылова
на городе меньше 3 векторов длины `n`, либо что число итераций на городе
принципиально ниже 66, обрушивает таблицу. Числа `5e8` и «66…193 итераций»
получены на других постановках (лестница LOD и 2D-стенд соответственно) и на
город не проверялись — это перенос замера в другие условия, тот самый класс
К71/К82.

### Побочное следствие, неочевидное: направления обязаны быть FP64

Допуск на угол несущей `Δθ ≲ ε/(0.6·kW)` даёт при W = 10⁴λ величину
`0.05″ = 2.4e-7` рад. Машинный эпсилон FP32 — `1.2e-7`. **Запас — два бита.**
Значит амплитуды (решаемые коэффициенты) могут жить в FP32, что для пропускной
способности критично, а вот геометрия направлений — не может. Это разделение
типов, а не глобальный выбор точности; и оно бесплатно, потому что направлений
на элемент единицы, а коэффициентов — `n`.

---

## 1. АКТИВНОЕ ≠ РЕЗИДЕНТНОЕ: ТРЁХЪЯРУСНОЕ ХРАНЕНИЕ И ПОДКАЧКА

**Соображение пользователя (07-28):** непосредственно в памяти находятся только
LOD-урезанные детали. Видеопамять совсем дорогая, но и обычная память стоит
оптимизации и умения **подкачивать детали с диска по мере движения**.

### Сначала поправка, иначе выигрыш посчитается дважды

**`5e8` — это уже ПОСЛЕ LOD, а не до него.** Число получено как
`(c/ε²)·log2(R_max/R_min)`: на октаву дальности приходится `≈1.5e7` элементов
НЕЗАВИСИМО от дальности, город — 23 октавы. Это и есть «LOD-урезанные детали»;
сырое содержимое города в этой цифре не участвует вовсе. Урезать LOD ещё раз
поверх `5e8` нельзя — этот множитель уже потрачен, и именно он превращает
`1e4`-кратный рост линейного размера в `2…3`-кратный рост счёта.

**И культинг кадром недоступен**, это записано в `CLAUDE.md` и от перехода к
городу не меняется: свет приходит из-за пределов пирамиды видимости и от
поверхностей, которые не видны никогда. Активный набор покрывает ПОЛНУЮ СФЕРУ и
перекрытые слои, и цифры `2e8`/`5e8` это уже учитывают.

### Но само соображение верно, и оно про ДРУГОЕ

Активный набор `5e8` не обязан быть РЕЗИДЕНТНЫМ. Это отдельный рычаг —
пункт (в) раздела 0, — и он про иерархию хранения `диск → ОЗУ → видеопамять` с
предикатом резидентности, управляемым движением камеры. Аналог в растеризации
есть и работает: Nanite и виртуальные текстуры UE ровно это и делают.

**ДВА ОТЛИЧИЯ ОТ UE, И ОБА В НЕВЫГОДНУЮ СТОРОНУ:**

1. **Предикат другой.** У UE резидентность управляется видимостью; здесь
   видимость запрещена как критерий. Заменителем служит ВКЛАД В АПЕРТУРУ —
   вычислимая величина того же семейства, что учёт `dmax` (`CLAUDE.md`, пункт (3)
   в списке «чем заменяется культинг»). **Он не реализован и не измерен.**
2. **Подкачивается не только сцена, но и СОСТОЯНИЕ РЕШЕНИЯ.** Геометрия и
   материалы — это то, что UE и так стримит. А здесь между кадрами живёт ещё и
   поле `u`, которого у растеризатора нет вовсе; на нём держится вся ставка на
   инкрементность.

### ГЛАВНОЕ ОГРАНИЧЕНИЕ: ПОДКАЧКА СОВМЕСТИМА СО СВИПОМ И НЕСОВМЕСТИМА С ГЛОБАЛЬНЫМ КРЫЛОВЫМ

Подкачка окупается, только если подкачанное трогают `O(1)` раз за кадр. Крылов
трогает КАЖДУЮ неизвестную КАЖДУЮ итерацию, а скалярные произведения суть
глобальные редукции — порядка обхода, который можно было бы согласовать с
потоком с диска, у него нет. То есть подкачка под глобальный Крылов означает
перечитывание всего рабочего набора 66…193 раза за кадр.

При ND = 4 (набор 80 ГБ, 160 ГБ трафика на итерацию), 66 итераций:

| источник | скорость | на итерацию | кадр |
|---|---|---|---|
| видеопамять 24 ГБ | 235 ГБ/с | — | не помещается |
| ОЗУ | **22.6 ГБ/с (замер К79)** | 7 с | 8 мин |
| NVMe | ~5 ГБ/с (**НЕ ИЗМЕРЕНО**) | 32 с | **35 мин** |

**Вывод, и он структурный, а не количественный:** ярусное хранение не спасает
глобальный итерационный решатель — оно переносит стену на ярус ниже и делает её
выше. Чтобы подкачка работала, **та часть данных, которая едет с диска, обязана
участвовать в счёте `O(1)` раз за кадр**, а не `O(итераций)`.

Это же и говорит, какую форму обязана иметь иерархия. Она уже названа в
`CLAUDE.md` («УРОВЕНЬ = ПРОСТРАНСТВЕННЫЙ МАСШТАБ И ЧАСТОТА ОБНОВЛЕНИЯ»), но там
это записано как приём переиспользования, а здесь становится ТРЕБОВАНИЕМ
хранения:

- **ядро** (дальние октавы) — привязано к сцене, считается один раз, дальше
  только ЧИТАЕТСЯ. Его можно стримить: оно не в цикле решателя;
- **оболочка** (ближние октавы) — пересчитывается каждый кадр, обязана быть
  резидентной;
- **связь между ними** — вот единственное место, где остаётся вопрос, и он
  нетривиален: если ядро входит в ту же линейную систему, что оболочка, то оно
  в цикле решателя, и весь абзац выше рушится. Ядро обязано входить **входными
  данными на границе оболочки**, а не строками системы.

### Арифметика раскола, и она неудобная

На октаву приходится одинаковое число элементов, поэтому «малого ядра» не
получается: 23 октавы города при `≈1.5e7` каждая. Три ближние октавы —
`≈5e7` элементов (10%), остальные двадцать — `≈4.5e8` (90%).
**Ядро есть основная масса данных, а не хвост.** Для стриминга это хорошо (масса
статична), но только при условии, что ядро действительно сведено к граничным
данным. Иначе 90% данных сидят в цикле решателя, и подкачка бессмысленна.

**Фальсификаторы раздела:**
- скорость диска НЕ ИЗМЕРЕНА; `5 ГБ/с` — справочная цифра, а проект уже обжёгся
  ровно на этом (К79: ОЗУ дало 22.6 против 41.6 справочных). Мерить до опоры;
- раскол «ядро/оболочка» по трём октавам взят с потолка; настоящая граница
  задаётся тем, на какой дальности вклад в апертуру перестаёт меняться при
  движении камеры за кадр — это измеримо и не измерено;
- утверждение «ядро сводится к граничным данным» НЕ ДОКАЗАНО. Оно правдоподобно
  (число мод связи на границе задаётся счётом коммуникационных мод, а не числом
  элементов ядра), но это ровно тот класс утверждений, который в этом проекте
  дважды оказывался неверным.

---

## 2. ВАЖНОСТЬ ВМЕСТО ДАЛЬНОСТИ: ε ПО ВКЛАДУ В АПЕРТУРУ

**Соображение пользователя (07-28):** LOD в текущем виде помогает недостаточно.
Заметно помогут оптимизации детализации НЕВИДИМЫХ поверхностей — вне конуса
камеры и перекрытых. Без зеркальных поверхностей такие оптимизации несложные;
для зеркальных можно считать ВАЖНОСТЬ. Оценка выигрыша — `10…12×`.

### Почему это НЕ нарушает запрет на культинг

Запрет в `CLAUDE.md` касается УДАЛЕНИЯ: свет приходит из-за пирамиды видимости и
от невидимых поверхностей, поэтому выбрасывать их нельзя. **Огрублять — можно, и
это разный ход.** Более того, это уже названо там же как замена культингу, пункт
(3): «ПОРОГ ПО ВКЛАДУ, вычислимый, того же семейства, что учёт `dmax`». То есть
рычаг записан, но никогда не был ни определён, ни измерен. Здесь он определяется.

### Точная формулировка: ε перестаёт быть функцией дальности

Сейчас `L = εR`, где ε — угловой размер пикселя, и это применяется по ПОЛНОЙ
СФЕРЕ единообразно. Но разрешение пикселя нужно только тому, что попадает в
апертуру напрямую. Поверхности, питающей лишь диффузный вклад третьего порядка,
нужна детализация, заданная не расстоянием, а **тем, насколько её детали меняют
поле на апертуре**. Поэтому:

    ε(x)  =  ε_пикс / (вклад элемента x в апертуру)^(1/2)

### Как ВАЖНОСТЬ считается, и почему зеркала не требуют отдельного случая

Пользователь предлагает «для зеркальных поверхностей попробовать считать
важность». Это можно сделать не эвристикой, а точно, и тем же решателем:
**важность есть решение СОПРЯЖЁННОЙ задачи с апертурой в качестве источника.**
Классика (сопряжённый поток в переносе нейтронов; adjoint photon importance у
Кристенсена). Вклад элемента — произведение прямого и сопряжённого полей на нём.

Ключевое свойство: **сопряжённое поле проходит через зеркала САМО.** Оно
подчиняется тому же уравнению, поэтому «что видно в зеркале» получается как
результат распространения, а не как отдельная машинерия трассировки отражений.
Зеркальный случай перестаёт быть особым — исчезает ровно то место, где у
пользователя стояло «можно попробовать».

**И это же единственный способ получить перекрытие в этой постановке.** В
`CLAUDE.md` записано: «ПЕРЕКРЫТИЕ КАК ЗАТУХАНИЕ, а не флаг — оператор
дифференциальный и локальный, попарной видимости `V(x,z)` как объекта здесь нет».
Значит запроса «перекрыта ли поверхность» не существует; сопряжённое поле даёт
затухание за препятствием НАТИВНО, как амплитуду, и попутно правильно ведёт себя
на кромках (за краем не ноль — там дифракция).

**ЦЕНА — И ОНА МАЛА, ЧТО И ДЕЛАЕТ ХОД ЖИЗНЕСПОСОБНЫМ:** важность нужна с
точностью до множителя ~2, потому что она задаёт ε, а ε входит в счёт квадратом.
Одна значащая цифра. Значит сопряжённый прогон делается на грубом ε и малом ND и
стоит проценты от прямого, а не удвоение.

### АМДАЛЬ: `10…12×` — ЭТО ПОТОЛОК РЫЧАГА, А НЕ ОСТОРОЖНАЯ ОЦЕНКА

Пусть `f` — доля элементов, обязанных остаться в полной детализации, `c` —
во сколько раз огрубляется остальное. Тогда

    выигрыш = 1 / ( f + (1−f)/c )      →      1/f   при c → ∞

| `f` | `c = 10` | `c = 100` | потолок |
|---|---|---|---|
| 20% | 3.6× | 4.8× | 5× |
| 10% | 5.3× | 9.2× | **10×** |
| 5% | 6.9× | 15× | 20× |

**`10…12×` требует `f ≈ 8…10%` И огрубления невидимого примерно в СТО раз**
(то есть ε вверх в 10 раз на поверхностях). При `c = 10` та же `f` даёт всего
`5.3×`. Отсюда практический вывод: **этот рычаг близок к своему максимуму уже в
оценке пользователя, и подкрутить его до `30×` нельзя** — потолок `1/f` жёсткий,
а `f` задаётся тем, сколько сцены реально смотрит в апертуру.

### ЧТО ОСТАЁТСЯ ПОСЛЕ ЭТОГО РЫЧАГА (почему «недостаточно» — правда)

От таблицы раздела 0, `÷11`:

| ND | было | с важностью | до 1 с ещё нужно | до 30 мс ещё нужно |
|---|---|---|---|---|
| 1 | 11 с | 1.0 с | ≈1× | **33×** |
| 4 | 8 мин | 44 с | **44×** | 1500× |
| 10 | не влезает в 125 ГБ | ≈105 с (влезает в ОЗУ) | 105× | 3500× |

Рычаг переводит ND = 10 из «невозможно» в «две минуты» — это настоящий
качественный сдвиг по ПАМЯТИ. Но по ВРЕМЕНИ до интерактивности не хватает ещё
`33…1500×` в зависимости от ND, и это снова говорит, что **ND решает всё**
(раздел 0, вывод 3).

### Цена, которую надо назвать честно: инкрементность слабеет

Поле `u` от камеры не зависит — это архитектурный актив. Важность зависит.
Причём хуже, чем зависит дальность: **чистый ПОВОРОТ камеры не меняет `R` ни у
одного элемента, но меняет перекрытие и содержимое зеркал, то есть меняет ε
всюду.** Значит поворот, который по замыслу стоил только оператора камеры,
начинает стоить пересчёта представления. Величина эффекта неизвестна и
измерима: это M11 с включённой важностью, негативный контроль — поворот против
переноса камеры.

### 2.1 ПОПРАВКА ПОЛЬЗОВАТЕЛЯ: ЭВРИСТИКА И АННОТАЦИИ ПЕРВЫМИ, ВАЖНОСТЬ — ТОЧЕЧНО

**Возражение пользователя (07-28), принято:** эвристика будет работать довольно
неплохо и считается значительно быстрее важности, поэтому важность стоит
применять только там, где эвристика работает плохо. А в реальных движках подобное
вообще задаётся простыми аннотациями на этапе проектирования сцены.

Это верно и подтверждается практикой: Lightmass Importance Volume, cull distance
volumes, precomputed visibility / порталы, reflection captures — всё это
аннотации проектировщика, и стоят они в рантайме ноль.

**И это ложится на раскол ядро/оболочка из раздела 1 без натяжки:**

| | ядро (дальние октавы, ~90% данных) | оболочка (ближние, ~10%) |
|---|---|---|
| привязка | к сцене | к камере |
| частота | один раз | каждый кадр |
| чем задаётся ε | **аннотацией / эвристикой** | **важностью** |
| цена | ноль в рантайме | сопряжённый прогон на 10% данных |

То есть аннотации закрывают 90% объёма даром, а сопряжённый прогон, если он
нужен только на оболочке, — это 10% от 10%. Дешевле, чем оценивалось выше.

### 2.2 ГДЕ ЭВРИСТИКА ЛОМАЕТСЯ — И ЭТО РОВНО ЦЕЛЕВЫЕ СЦЕНЫ ВОЛНОВОЙ ЛИНИИ

Эвристики (дальность, отворот нормали, PVS, объёмы важности) ломаются в трёх
местах, и первые два — не экзотика:

1. **ФОКУСИРОВКА.** Линза или вогнутое зеркало собирают энергию малой области в
   яркое пятно. Эвристика по дальности говорит «мелкое и далёкое — огрубить»;
   физика говорит «эта область определяет самую яркую деталь кадра». Классический
   провал важностных эвристик в рендеринге.
2. **ЗЕРКАЛЬНЫЕ ЦЕПОЧКИ.** Зеркало, смотрящее в зеркало, или зеркало, показывающее
   область, помеченную проектировщиком как неважная. Аннотация статична, а цепочка
   зависит от положения камеры.
3. **ДИНАМИКА.** Аннотация авторская; разрушаемая или движущаяся сцена её
   обесценивает.

**И вот главное.** Волновая линия применима, по записанному в `CLAUDE.md`
критерию, ТОЛЬКО там, где геометрия аналитическая, а источник узкополосный —
линза, зеркало, решётка, голограмма. **То есть целевые сцены волнового продукта
есть в точности те сцены, где эвристика по дальности ломается.** Это не
совпадение, а тот же раскол по режиму когерентности, что делит два продукта:

- **линия ПЕРЕНОСА** (тепловой источник, диффузное) — аннотаций и эвристик
  достаточно, важность не нужна почти нигде;
- **линия ГЕЛЬМГОЛЬЦА** (линзы, зеркала) — эвристика промахивается системно, и
  важность здесь не «для трудных случаев», а по основному назначению.

Поэтому вывод пользователя «важность только там, где эвристика плоха» я принимаю
целиком, но с уточнением, куда это попадает: **в волновой линии таких мест не
меньшинство.**

### 2.3 ЧТО ЭВРИСТИКА СТОИТ В ЧИСЛАХ: ОНА ОБЯЗАНА БЫТЬ КОНСЕРВАТИВНОЙ

Эвристика не знает, ошиблась ли она, поэтому в сомнительном случае обязана
СОХРАНЯТЬ детализацию. Консервативность поднимает `f` — долю, оставшуюся в полной
детализации, — а по таблице Амдаля выше `f` решает всё:

| `f` | потолок рычага |
|---|---|
| 10% (точная важность) | 10× |
| 20% (консервативная эвристика) | **5×** |

**Значит цена эвристики измеряется не временем её счёта, а тем, насколько она
раздувает `f`.** Одна значащая цифра важности стоит процентов от кадра, а
удвоение `f` стоит половины всего рычага. Это и есть критерий, где проводить
границу между эвристикой и важностью, — и он вычислим, а не вкусовой.

**Фальсификаторы раздела:**
- `f ≈ 10%` — предположение, а не замер. Мерить долю элементов с вкладом выше
  порога на реальной сцене; при `f = 20%` весь рычаг даёт `5×`, а не `11×`;
- «эвристика раздувает `f` вдвое» — тоже предположение. Проверяется прямо:
  посчитать `f` по эвристике и по сопряжённому полю на одной сцене с зеркалом;

---

## 3. ПОЛИНОМЫ ВМЕСТО ИЕРАРХИЧЕСКИХ ВОКСЕЛЕЙ: НЕТ — НО ЕСТЬ ПРОДУКТИВНАЯ ФОРМА

**Вопрос пользователя (07-28):** менять иерархические воксели на просто полиномы
малопродуктивно — почти ничего не экономим, а сложность решений возрастает?

**Ответ: да, но по причине куда более жёсткой, чем «почти ничего не экономим».**
Замена отрицательна на восемь порядков, и это считается, а не оценивается.

### Почему полином не может заменить несущую

Чтобы полином степени `p` на элементе размера `W` воспроизвёл `e^{ikx}`, нужно
`p ≳ kW` (классическая дисперсионная оценка Айнсворта/Меленка–Заутера; ниже этого
порога появляется загрязнение, а не просто ошибка). При `W = 1000λ`:

    kW = 2π·1000 ≈ 6.3e3     ⇒     p ≈ 3e3…6e3 по каждому измерению
    неизвестных на элемент в 3D ≈ p³ ≈ 3e10…2e11

Трефтц на том же элементе: `ND` штук, единицы или десятки при ЗНАЕМЫХ
направлениях. Отношение `~1e8`. **Это не вопрос экономии — это ровно та стена,
ради обхода которой введена несущая.** Директива «дискретизация задаётся
геометрией, а не длиной волны» без несущей неисполнима: полином обязан разрешать
λ, и тогда возвращается сетка λ/8 и `3e24` элементов на комнату.

Второе следствие, независимое: полином не решает уравнение, значит возвращаются
ОБЪЁМНЫЕ интегралы с осциллирующим подынтегральным выражением. При `W = 1e5λ`
это `1e6` периодов, а «сэмплирование в сборке не вариант» (аудит M14, R3). У
Трефтца объёмных интегралов нет вовсе, всё сводится к граням в замкнутой форме
ценой, не зависящей от ω.

### ЕДИНСТВЕННЫЙ ЧЕСТНЫЙ ДОВОД ЗА ПОЛИНОМЫ — ОБУСЛОВЛЕННОСТЬ, И ЗАМЕР ЕГО НЕ ПОДДЕРЖИВАЕТ

Плоско-волновые базисы печально известны вырождением: при росте `ND` на
фиксированном `kW` волны становятся почти линейно зависимыми, число
обусловленности растёт экспоненциально (литература по UWVF/PWDG — Хиптмайр,
Мойола, Перуджа). У полиномов с этим лучше. И у проекта обусловленность болит:
LSQR сходится как CG на нормальных уравнениях, то есть по `cond²`.

**Но замер против:** `tdg2d` даёт `193 → 66` итераций при росте элемента от 1λ
до 10⁴λ. Число итераций ПАДАЕТ. На использованных `ND` обусловленность связывающим
ограничением не была. Довод остаётся в силе только как предупреждение о том, что
случится при росте `ND`, — и это ещё одна причина, по которой `ND` решает всё.

### И ЛОЖНАЯ АЛЬТЕРНАТИВА: ПОЛИНОМЫ УЖЕ ЗДЕСЬ

Базис проекта есть `φ(x/W−n)·e^{ik·x}`, где `φ` — кусочно-КВАДРАТИЧНЫЙ потенциал.
То есть постановка уже полиномиальна по амплитуде и волновая по фазе. Вопрос
«полиномы ВМЕСТО» не имеет предмета: полином — амплитуда, плоская волна — фаза,
и заменять одно другим нечего.

### ПРОДУКТИВНАЯ ФОРМА ВОПРОСА: ПОЛИНОМ В ПОКАЗАТЕЛЕ, А НЕ В АМПЛИТУДЕ

Здесь начинается то, ради чего вопрос стоит не закрывать целиком.

Измеренный закон (PLAN.md): на КРИВОЙ поверхности направление меняется поперёк
ячейки на `W/ρ`, константная несущая врёт на `W/2ρ`, фазовая ошибка
`kW²/2ρ ≲ 1` даёт

    W ≲ sqrt(ρλ/π)     при ρ = 0.5 м, λ = 550 нм   →   W ≈ 0.3 мм  (≈540λ)

**Размер элемента на кривизне ограничен тем, что несущая — КОНСТАНТНАЯ плоская
волна.** Если пустить в показатель квадратичный член, подогнанный под кривизну
(обобщённая плоская волна, GPW — Имбер-Жерар и Депре), ведущий член уходит и
ограничение задаётся кубическим:

    W ≲ (ρ²λ/2π)^(1/3)   при тех же ρ, λ   →   W ≈ 2.8 мм

Это `×9.4` по длине, то есть **`×88` по числу элементов на поверхности** —
существенно больше, чем даёт весь рычаг важности из раздела 2. И бьёт прямо в
`n`, то есть в стену раздела 0, а не в множитель перед ней.

**ЦЕНА, И ЕЁ НАДО ПРОВЕРИТЬ ПРЕЖДЕ ВСЕГО:** GPW уравнение Гельмгольца ТОЧНО не
решает — только с точностью до порядка. Значит возвращается объёмная невязка,
то есть ровно то, отсутствие чего делает постановку дешёвой. Весь вопрос в том,
мала ли она настолько, чтобы её можно было либо игнорировать, либо взять
замкнутой формой (квадратичный показатель даёт гауссов интеграл, а он берётся
аналитически — в отличие от общего случая).

**Оценка `×88` — МОЯ, на основе записанного закона, а НЕ ЗАМЕР.** Проверяется
дёшево и прямо на существующем стенде: `tools/tdg2d.c` уже умеет мерить ошибку
против углового промаха (ключ `dth=`); добавить квадратичный член в показатель и
повторить таблицу W. Фальсификаторы:
- если ошибка при квадратичном показателе не уходит в кубический закон, оценка
  `×88` неверна и вопрос закрыт целиком;
- если объёмная невязка GPW превышает скачки на скелете, постановка перестаёт
  быть Трефтцевой и возвращается к квадратуре — тогда лечение хуже болезни;
- негативный контроль: на ПЛОСКОЙ поверхности (`ρ = ∞`) квадратичный член обязан
  выходить в ноль, а числа — совпадать с текущими побитово.
- утверждение «важности хватает одной значащей цифры» проверяется прямо:
  посчитать кадр с точной важностью и с загрублённой вдвое, сравнить картинки;
- «сопряжённое поле дешевле прямого на порядок» — предсказание. Если сопряжённый
  прогон требует того же ND (а ND задаётся ЗНАНИЕМ НАПРАВЛЕНИЙ, которое у
  сопряжённой задачи не лучше), рычаг съедает половину собственного выигрыша.
