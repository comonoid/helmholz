# new-structure.md — обсуждение принципиального ускорения

Файл заведён 07-28 по просьбе пользователя. Сюда пишутся ТОЛЬКО те соображения,
которые пережили попытку опровержения, и у каждого назван фальсификатор — условие,
при котором оно перестаёт быть верным. Отвергнутое сюда не пишется (если явно не
попрошено); отвергнутое с причиной живёт в `PLAN*.md`.

Ничего в репозитории этот файл не меняет: это записная книжка обсуждения, а не план.

---

# ОБЛАСТЬ ОБСУЖДЕНИЯ: ТОЛЬКО ЛИНИЯ ПЕРЕНОСА. ВОЛНОВАЯ ОТКЛЮЧЕНА

**Директива пользователя, 07-28, повторена настойчиво.** Обсуждается ТОЛЬКО
линия уравнения рендеринга / переноса (`PLAN_TRANSPORT.md`, `src/transport/`).
**Волновую линию не поминать, не предлагать, не приводить как аналогию, не
пересчитывать в её единицах.** Не надо волнового.

Конкретно ВЫКЛЮЧЕНО из обсуждения: уравнение Гельмгольца, Трефтц-DG, несущий
базис, плоские волны, фаза, когерентность, потенциалы `φ`, `c_{S,d}`,
`ND ≈ W/ℓ_⊥`, `tdg2d`/`slab2d`/`slice2d`, `M9a/M9b/M9c`, разделы 3 и 4 этого
файла.

**ДЕЙСТВУЮТ (они не волновые, а общие):** раздел 0 — стена по размеру системы;
раздел 1 — трёхъярусное хранение и подкачка; раздел 2 — важность и сопряжённая
задача; раздел 5 — угловое представление в переносе. Плюс `PLAN_CUT.md` как
общий слой геометрии, если понадобится граница материала.

### КАК ВЕРНУТЬ (директива обратима, и это её условие)

Достаточно сказать «вернуть волновую линию». Тогда: снять этот баннер, снять
пометку «ОТЛОЖЕНО» с заголовков разделов 3 и 4, и удалить файл памяти
`scope-transport-only-wave-parked.md` вместе со строкой о нём в `MEMORY.md`.
Ничего в разделах 3 и 4 не удалено и не переписано — там лежат две дёшево
проверяемые оценки (`×88` по квадратичной фазе, `×25` по P1-амплитуде), и они
ждут в целости.

---

## 0. ПОСТАНОВКА: СТЕНА — РАЗМЕР СИСТЕМЫ, А НЕ ЕЁ РАЗРЕЖЕННОСТЬ

**Соображение пользователя (07-28), и оно верное:** на сцене уровня Unreal Engine
объёмы таковы, что сводят на нет все преимущества постановки. С ПАМЯТЬЮ ещё можно
работать. Но это всё — данные, из которых потом составляется линейная система, и
она будет медленной **несмотря на любую разреженность, просто из-за размера**.

Ниже это переведено в числа на ИЗМЕРЕННОЙ машине разработки (К79) и на карте
среднего верхнего сегмента (24 ГБ, поправка К82 — 6 ГБ есть ограничение машины, а
не метода).

### Исходные величины

| величина | значение | источник |
|---|---|---|
| элементов на городе | `5e8` | `CLAUDE.md`, лестница LOD |
| неизвестных | `n = 5e8 · ND` | ND = несущих на элемент |
| комплексный FP32 / FP64 | 8 Б / 16 Б | |
| рабочий набор Крылова | ≈ 5 векторов длины `n` | LSQR: `x, u, v, w` + временные |
| трафик на итерацию | ≈ 10 проходов по `n` | 2 matvec + ~5 axpy/dot |
| карта разработки | 6 ГБ, 336 ГБ/с, FP32 5.3 Тфлопс, **FP64 166 Гфлопс** | К79 |
| ОЗУ | 125 ГБ, **22.6 ГБ/с ЗАМЕРЕНО** (не 41.6 справочных) | К79 |
| итераций Трефтц-DG | 66…193 | `tools/tdg2d.c`, PLAN.md |

### Что из этого следует

**ОДИН вектор решения на городе при ND = 1 есть 4 ГБ в FP32.** Не матрица —
ВЕКТОР. Он один почти заполняет карту разработки.

| ND | вектор, FP32 | набор Крылова (×5) | где помещается | кадр (66 итераций) |
|---|---|---|---|---|
| 1 | 4 ГБ | **20 ГБ** | резидентно на 24 ГБ | `40 ГБ/ит ÷ 235 ГБ/с` → **11 с** |
| 4 | 16 ГБ | 80 ГБ | только ОЗУ | `160 ГБ/ит ÷ 22.6 ГБ/с` → **8 мин** |
| 10 | 40 ГБ | 200 ГБ | **не помещается в 125 ГБ ОЗУ** | — |

Это НИЖНЯЯ оценка: matrix-free, идеальный предобуславливатель, нулевая стоимость
оператора, ноль обменов. Всё, что делает решатель, — трогает векторы.

### Три вывода, которые режут классы ответов

**(1) «Лучший решатель» задачу не решает.** При ND = 4 восемь минут на кадр —
это стоимость ПАМЯТИ, при матрице ценой ноль. Предобуславливатель, снижающий
число итераций вдвое, даёт четыре минуты. Класс не меняется. Любое
принципиальное ускорение обязано уменьшать `n`, а не множитель перед ним.

**(2) Матрицу хранить нельзя ни при каком ND ≥ 2.** Скелетные связи Трефтц-DG:
6 граней на ячейку, блок `ND × ND`. При ND = 1 это `3e9` элементов = 24 ГБ,
больше всего набора векторов; при ND = 4 — 384 ГБ. Значит оператор
пересчитывается на лету всегда. Это выполнимо (аналитические элементы, FP32
5.3 Тфлопс), но это ТРЕБОВАНИЕ, а не выбор.

**(3) ND — единственная величина, решающая исход, и она НЕ ИЗМЕРЕНА.** Между
ND = 1 и ND = 10 лежит граница между 11 секундами и «не существует». Это ровно
то, что должен померить M9c, и ровно то, на что `tdg2d` уже дал предупреждение:
равномерному вееру нужно `ND ~ 2π·kW` (при W = 100λ — 4000 направлений), а
знание направления даёт единицы. **Вопрос «сколько несущих на элемент» — это не
вопрос точности, это вопрос существования продукта на городе.**

### Рычагов на `n` ровно три, других нет

- **(а) меньше элементов** — агрегация/LOD. Уже в архитектуре (она и даёт `5e8`
  вместо `1e22`), но подпёрта незакрытым: ошибка ЗАМЕНЫ структуры откликом не
  вычисляется ничем (`CLAUDE.md`, случай iii; `hz_facet.dmax` покрывает только
  СМЕЩЕНИЕ).
- **(б) меньше несущих на элемент** — то есть ЗНАТЬ направления, а не
  сэмплировать. См. (3).
- **(в) не держать весь `n` одновременно** — разложение области со скользящим
  рабочим набором вместо резидентного вектора. Единственный рычаг, который
  превращает 200 ГБ в помещающееся; и единственный, куда естественно ложится
  «сеточное обуславливание», если иерархия есть РАЗВЁРТКА (свип), а не V-цикл
  над резидентным вектором.

**Фальсификатор раздела:** любой замер, показывающий, что рабочий набор Крылова
на городе меньше 3 векторов длины `n`, либо что число итераций на городе
принципиально ниже 66, обрушивает таблицу. Числа `5e8` и «66…193 итераций»
получены на других постановках (лестница LOD и 2D-стенд соответственно) и на
город не проверялись — это перенос замера в другие условия, тот самый класс
К71/К82.

### Побочное следствие, неочевидное: направления обязаны быть FP64

Допуск на угол несущей `Δθ ≲ ε/(0.6·kW)` даёт при W = 10⁴λ величину
`0.05″ = 2.4e-7` рад. Машинный эпсилон FP32 — `1.2e-7`. **Запас — два бита.**
Значит амплитуды (решаемые коэффициенты) могут жить в FP32, что для пропускной
способности критично, а вот геометрия направлений — не может. Это разделение
типов, а не глобальный выбор точности; и оно бесплатно, потому что направлений
на элемент единицы, а коэффициентов — `n`.

---

## 1. АКТИВНОЕ ≠ РЕЗИДЕНТНОЕ: ТРЁХЪЯРУСНОЕ ХРАНЕНИЕ И ПОДКАЧКА

**Соображение пользователя (07-28):** непосредственно в памяти находятся только
LOD-урезанные детали. Видеопамять совсем дорогая, но и обычная память стоит
оптимизации и умения **подкачивать детали с диска по мере движения**.

### Сначала поправка, иначе выигрыш посчитается дважды

**`5e8` — это уже ПОСЛЕ LOD, а не до него.** Число получено как
`(c/ε²)·log2(R_max/R_min)`: на октаву дальности приходится `≈1.5e7` элементов
НЕЗАВИСИМО от дальности, город — 23 октавы. Это и есть «LOD-урезанные детали»;
сырое содержимое города в этой цифре не участвует вовсе. Урезать LOD ещё раз
поверх `5e8` нельзя — этот множитель уже потрачен, и именно он превращает
`1e4`-кратный рост линейного размера в `2…3`-кратный рост счёта.

**И культинг кадром недоступен**, это записано в `CLAUDE.md` и от перехода к
городу не меняется: свет приходит из-за пределов пирамиды видимости и от
поверхностей, которые не видны никогда. Активный набор покрывает ПОЛНУЮ СФЕРУ и
перекрытые слои, и цифры `2e8`/`5e8` это уже учитывают.

### Но само соображение верно, и оно про ДРУГОЕ

Активный набор `5e8` не обязан быть РЕЗИДЕНТНЫМ. Это отдельный рычаг —
пункт (в) раздела 0, — и он про иерархию хранения `диск → ОЗУ → видеопамять` с
предикатом резидентности, управляемым движением камеры. Аналог в растеризации
есть и работает: Nanite и виртуальные текстуры UE ровно это и делают.

**ДВА ОТЛИЧИЯ ОТ UE, И ОБА В НЕВЫГОДНУЮ СТОРОНУ:**

1. **Предикат другой.** У UE резидентность управляется видимостью; здесь
   видимость запрещена как критерий. Заменителем служит ВКЛАД В АПЕРТУРУ —
   вычислимая величина того же семейства, что учёт `dmax` (`CLAUDE.md`, пункт (3)
   в списке «чем заменяется культинг»). **Он не реализован и не измерен.**
2. **Подкачивается не только сцена, но и СОСТОЯНИЕ РЕШЕНИЯ.** Геометрия и
   материалы — это то, что UE и так стримит. А здесь между кадрами живёт ещё и
   поле `u`, которого у растеризатора нет вовсе; на нём держится вся ставка на
   инкрементность.

### ГЛАВНОЕ ОГРАНИЧЕНИЕ: ПОДКАЧКА СОВМЕСТИМА СО СВИПОМ И НЕСОВМЕСТИМА С ГЛОБАЛЬНЫМ КРЫЛОВЫМ

Подкачка окупается, только если подкачанное трогают `O(1)` раз за кадр. Крылов
трогает КАЖДУЮ неизвестную КАЖДУЮ итерацию, а скалярные произведения суть
глобальные редукции — порядка обхода, который можно было бы согласовать с
потоком с диска, у него нет. То есть подкачка под глобальный Крылов означает
перечитывание всего рабочего набора 66…193 раза за кадр.

При ND = 4 (набор 80 ГБ, 160 ГБ трафика на итерацию), 66 итераций:

| источник | скорость | на итерацию | кадр |
|---|---|---|---|
| видеопамять 24 ГБ | 235 ГБ/с | — | не помещается |
| ОЗУ | **22.6 ГБ/с (замер К79)** | 7 с | 8 мин |
| NVMe | ~5 ГБ/с (**НЕ ИЗМЕРЕНО**) | 32 с | **35 мин** |

**Вывод, и он структурный, а не количественный:** ярусное хранение не спасает
глобальный итерационный решатель — оно переносит стену на ярус ниже и делает её
выше. Чтобы подкачка работала, **та часть данных, которая едет с диска, обязана
участвовать в счёте `O(1)` раз за кадр**, а не `O(итераций)`.

Это же и говорит, какую форму обязана иметь иерархия. Она уже названа в
`CLAUDE.md` («УРОВЕНЬ = ПРОСТРАНСТВЕННЫЙ МАСШТАБ И ЧАСТОТА ОБНОВЛЕНИЯ»), но там
это записано как приём переиспользования, а здесь становится ТРЕБОВАНИЕМ
хранения:

- **ядро** (дальние октавы) — привязано к сцене, считается один раз, дальше
  только ЧИТАЕТСЯ. Его можно стримить: оно не в цикле решателя;
- **оболочка** (ближние октавы) — пересчитывается каждый кадр, обязана быть
  резидентной;
- **связь между ними** — вот единственное место, где остаётся вопрос, и он
  нетривиален: если ядро входит в ту же линейную систему, что оболочка, то оно
  в цикле решателя, и весь абзац выше рушится. Ядро обязано входить **входными
  данными на границе оболочки**, а не строками системы.

### Арифметика раскола, и она неудобная

На октаву приходится одинаковое число элементов, поэтому «малого ядра» не
получается: 23 октавы города при `≈1.5e7` каждая. Три ближние октавы —
`≈5e7` элементов (10%), остальные двадцать — `≈4.5e8` (90%).
**Ядро есть основная масса данных, а не хвост.** Для стриминга это хорошо (масса
статична), но только при условии, что ядро действительно сведено к граничным
данным. Иначе 90% данных сидят в цикле решателя, и подкачка бессмысленна.

**Фальсификаторы раздела:**
- скорость диска НЕ ИЗМЕРЕНА; `5 ГБ/с` — справочная цифра, а проект уже обжёгся
  ровно на этом (К79: ОЗУ дало 22.6 против 41.6 справочных). Мерить до опоры;
- раскол «ядро/оболочка» по трём октавам взят с потолка; настоящая граница
  задаётся тем, на какой дальности вклад в апертуру перестаёт меняться при
  движении камеры за кадр — это измеримо и не измерено;
- утверждение «ядро сводится к граничным данным» НЕ ДОКАЗАНО. Оно правдоподобно
  (число мод связи на границе задаётся счётом коммуникационных мод, а не числом
  элементов ядра), но это ровно тот класс утверждений, который в этом проекте
  дважды оказывался неверным.

---

## 2. ВАЖНОСТЬ ВМЕСТО ДАЛЬНОСТИ: ε ПО ВКЛАДУ В АПЕРТУРУ

**Соображение пользователя (07-28):** LOD в текущем виде помогает недостаточно.
Заметно помогут оптимизации детализации НЕВИДИМЫХ поверхностей — вне конуса
камеры и перекрытых. Без зеркальных поверхностей такие оптимизации несложные;
для зеркальных можно считать ВАЖНОСТЬ. Оценка выигрыша — `10…12×`.

### Почему это НЕ нарушает запрет на культинг

Запрет в `CLAUDE.md` касается УДАЛЕНИЯ: свет приходит из-за пирамиды видимости и
от невидимых поверхностей, поэтому выбрасывать их нельзя. **Огрублять — можно, и
это разный ход.** Более того, это уже названо там же как замена культингу, пункт
(3): «ПОРОГ ПО ВКЛАДУ, вычислимый, того же семейства, что учёт `dmax`». То есть
рычаг записан, но никогда не был ни определён, ни измерен. Здесь он определяется.

### Точная формулировка: ε перестаёт быть функцией дальности

Сейчас `L = εR`, где ε — угловой размер пикселя, и это применяется по ПОЛНОЙ
СФЕРЕ единообразно. Но разрешение пикселя нужно только тому, что попадает в
апертуру напрямую. Поверхности, питающей лишь диффузный вклад третьего порядка,
нужна детализация, заданная не расстоянием, а **тем, насколько её детали меняют
поле на апертуре**. Поэтому:

    ε(x)  =  ε_пикс / (вклад элемента x в апертуру)^(1/2)

### Как ВАЖНОСТЬ считается, и почему зеркала не требуют отдельного случая

Пользователь предлагает «для зеркальных поверхностей попробовать считать
важность». Это можно сделать не эвристикой, а точно, и тем же решателем:
**важность есть решение СОПРЯЖЁННОЙ задачи с апертурой в качестве источника.**
Классика (сопряжённый поток в переносе нейтронов; adjoint photon importance у
Кристенсена). Вклад элемента — произведение прямого и сопряжённого полей на нём.

Ключевое свойство: **сопряжённое поле проходит через зеркала САМО.** Оно
подчиняется тому же уравнению, поэтому «что видно в зеркале» получается как
результат распространения, а не как отдельная машинерия трассировки отражений.
Зеркальный случай перестаёт быть особым — исчезает ровно то место, где у
пользователя стояло «можно попробовать».

**И это же единственный способ получить перекрытие в этой постановке.** В
`CLAUDE.md` записано: «ПЕРЕКРЫТИЕ КАК ЗАТУХАНИЕ, а не флаг — оператор
дифференциальный и локальный, попарной видимости `V(x,z)` как объекта здесь нет».
Значит запроса «перекрыта ли поверхность» не существует; сопряжённое поле даёт
затухание за препятствием НАТИВНО, как амплитуду, и попутно правильно ведёт себя
на кромках (за краем не ноль — там дифракция).

**ЦЕНА — И ОНА МАЛА, ЧТО И ДЕЛАЕТ ХОД ЖИЗНЕСПОСОБНЫМ:** важность нужна с
точностью до множителя ~2, потому что она задаёт ε, а ε входит в счёт квадратом.
Одна значащая цифра. Значит сопряжённый прогон делается на грубом ε и малом ND и
стоит проценты от прямого, а не удвоение.

### АМДАЛЬ: `10…12×` — ЭТО ПОТОЛОК РЫЧАГА, А НЕ ОСТОРОЖНАЯ ОЦЕНКА

Пусть `f` — доля элементов, обязанных остаться в полной детализации, `c` —
во сколько раз огрубляется остальное. Тогда

    выигрыш = 1 / ( f + (1−f)/c )      →      1/f   при c → ∞

| `f` | `c = 10` | `c = 100` | потолок |
|---|---|---|---|
| 20% | 3.6× | 4.8× | 5× |
| 10% | 5.3× | 9.2× | **10×** |
| 5% | 6.9× | 15× | 20× |

**`10…12×` требует `f ≈ 8…10%` И огрубления невидимого примерно в СТО раз**
(то есть ε вверх в 10 раз на поверхностях). При `c = 10` та же `f` даёт всего
`5.3×`. Отсюда практический вывод: **этот рычаг близок к своему максимуму уже в
оценке пользователя, и подкрутить его до `30×` нельзя** — потолок `1/f` жёсткий,
а `f` задаётся тем, сколько сцены реально смотрит в апертуру.

### ЧТО ОСТАЁТСЯ ПОСЛЕ ЭТОГО РЫЧАГА (почему «недостаточно» — правда)

От таблицы раздела 0, `÷11`:

| ND | было | с важностью | до 1 с ещё нужно | до 30 мс ещё нужно |
|---|---|---|---|---|
| 1 | 11 с | 1.0 с | ≈1× | **33×** |
| 4 | 8 мин | 44 с | **44×** | 1500× |
| 10 | не влезает в 125 ГБ | ≈105 с (влезает в ОЗУ) | 105× | 3500× |

Рычаг переводит ND = 10 из «невозможно» в «две минуты» — это настоящий
качественный сдвиг по ПАМЯТИ. Но по ВРЕМЕНИ до интерактивности не хватает ещё
`33…1500×` в зависимости от ND, и это снова говорит, что **ND решает всё**
(раздел 0, вывод 3).

### Цена, которую надо назвать честно: инкрементность слабеет

Поле `u` от камеры не зависит — это архитектурный актив. Важность зависит.
Причём хуже, чем зависит дальность: **чистый ПОВОРОТ камеры не меняет `R` ни у
одного элемента, но меняет перекрытие и содержимое зеркал, то есть меняет ε
всюду.** Значит поворот, который по замыслу стоил только оператора камеры,
начинает стоить пересчёта представления. Величина эффекта неизвестна и
измерима: это M11 с включённой важностью, негативный контроль — поворот против
переноса камеры.

### 2.1 ПОПРАВКА ПОЛЬЗОВАТЕЛЯ: ЭВРИСТИКА И АННОТАЦИИ ПЕРВЫМИ, ВАЖНОСТЬ — ТОЧЕЧНО

**Возражение пользователя (07-28), принято:** эвристика будет работать довольно
неплохо и считается значительно быстрее важности, поэтому важность стоит
применять только там, где эвристика работает плохо. А в реальных движках подобное
вообще задаётся простыми аннотациями на этапе проектирования сцены.

Это верно и подтверждается практикой: Lightmass Importance Volume, cull distance
volumes, precomputed visibility / порталы, reflection captures — всё это
аннотации проектировщика, и стоят они в рантайме ноль.

**И это ложится на раскол ядро/оболочка из раздела 1 без натяжки:**

| | ядро (дальние октавы, ~90% данных) | оболочка (ближние, ~10%) |
|---|---|---|
| привязка | к сцене | к камере |
| частота | один раз | каждый кадр |
| чем задаётся ε | **аннотацией / эвристикой** | **важностью** |
| цена | ноль в рантайме | сопряжённый прогон на 10% данных |

То есть аннотации закрывают 90% объёма даром, а сопряжённый прогон, если он
нужен только на оболочке, — это 10% от 10%. Дешевле, чем оценивалось выше.

### 2.2 ГДЕ ЭВРИСТИКА ЛОМАЕТСЯ — И ЭТО РОВНО ЦЕЛЕВЫЕ СЦЕНЫ ВОЛНОВОЙ ЛИНИИ

Эвристики (дальность, отворот нормали, PVS, объёмы важности) ломаются в трёх
местах, и первые два — не экзотика:

1. **ФОКУСИРОВКА.** Линза или вогнутое зеркало собирают энергию малой области в
   яркое пятно. Эвристика по дальности говорит «мелкое и далёкое — огрубить»;
   физика говорит «эта область определяет самую яркую деталь кадра». Классический
   провал важностных эвристик в рендеринге.
2. **ЗЕРКАЛЬНЫЕ ЦЕПОЧКИ.** Зеркало, смотрящее в зеркало, или зеркало, показывающее
   область, помеченную проектировщиком как неважная. Аннотация статична, а цепочка
   зависит от положения камеры.
3. **ДИНАМИКА.** Аннотация авторская; разрушаемая или движущаяся сцена её
   обесценивает.

**И вот главное.** Волновая линия применима, по записанному в `CLAUDE.md`
критерию, ТОЛЬКО там, где геометрия аналитическая, а источник узкополосный —
линза, зеркало, решётка, голограмма. **То есть целевые сцены волнового продукта
есть в точности те сцены, где эвристика по дальности ломается.** Это не
совпадение, а тот же раскол по режиму когерентности, что делит два продукта:

- **линия ПЕРЕНОСА** (тепловой источник, диффузное) — аннотаций и эвристик
  достаточно, важность не нужна почти нигде;
- **линия ГЕЛЬМГОЛЬЦА** (линзы, зеркала) — эвристика промахивается системно, и
  важность здесь не «для трудных случаев», а по основному назначению.

Поэтому вывод пользователя «важность только там, где эвристика плоха» я принимаю
целиком, но с уточнением, куда это попадает: **в волновой линии таких мест не
меньшинство.**

### 2.3 ЧТО ЭВРИСТИКА СТОИТ В ЧИСЛАХ: ОНА ОБЯЗАНА БЫТЬ КОНСЕРВАТИВНОЙ

Эвристика не знает, ошиблась ли она, поэтому в сомнительном случае обязана
СОХРАНЯТЬ детализацию. Консервативность поднимает `f` — долю, оставшуюся в полной
детализации, — а по таблице Амдаля выше `f` решает всё:

| `f` | потолок рычага |
|---|---|
| 10% (точная важность) | 10× |
| 20% (консервативная эвристика) | **5×** |

**Значит цена эвристики измеряется не временем её счёта, а тем, насколько она
раздувает `f`.** Одна значащая цифра важности стоит процентов от кадра, а
удвоение `f` стоит половины всего рычага. Это и есть критерий, где проводить
границу между эвристикой и важностью, — и он вычислим, а не вкусовой.

**Фальсификаторы раздела:**
- `f ≈ 10%` — предположение, а не замер. Мерить долю элементов с вкладом выше
  порога на реальной сцене; при `f = 20%` весь рычаг даёт `5×`, а не `11×`;
- «эвристика раздувает `f` вдвое» — тоже предположение. Проверяется прямо:
  посчитать `f` по эвристике и по сопряжённому полю на одной сцене с зеркалом;

---

## 3. [ОТЛОЖЕНО — ВОЛНОВАЯ ЛИНИЯ, НЕ ЧИТАТЬ БЕЗ ОТМЕНЫ БАННЕРА] ПОЛИНОМЫ ВМЕСТО ИЕРАРХИЧЕСКИХ ВОКСЕЛЕЙ

**Вопрос пользователя (07-28):** менять иерархические воксели на просто полиномы
малопродуктивно — почти ничего не экономим, а сложность решений возрастает?

**Ответ: да, но по причине куда более жёсткой, чем «почти ничего не экономим».**
Замена отрицательна на восемь порядков, и это считается, а не оценивается.

### Почему полином не может заменить несущую

Чтобы полином степени `p` на элементе размера `W` воспроизвёл `e^{ikx}`, нужно
`p ≳ kW` (классическая дисперсионная оценка Айнсворта/Меленка–Заутера; ниже этого
порога появляется загрязнение, а не просто ошибка). При `W = 1000λ`:

    kW = 2π·1000 ≈ 6.3e3     ⇒     p ≈ 3e3…6e3 по каждому измерению
    неизвестных на элемент в 3D ≈ p³ ≈ 3e10…2e11

Трефтц на том же элементе: `ND` штук, единицы или десятки при ЗНАЕМЫХ
направлениях. Отношение `~1e8`. **Это не вопрос экономии — это ровно та стена,
ради обхода которой введена несущая.** Директива «дискретизация задаётся
геометрией, а не длиной волны» без несущей неисполнима: полином обязан разрешать
λ, и тогда возвращается сетка λ/8 и `3e24` элементов на комнату.

Второе следствие, независимое: полином не решает уравнение, значит возвращаются
ОБЪЁМНЫЕ интегралы с осциллирующим подынтегральным выражением. При `W = 1e5λ`
это `1e6` периодов, а «сэмплирование в сборке не вариант» (аудит M14, R3). У
Трефтца объёмных интегралов нет вовсе, всё сводится к граням в замкнутой форме
ценой, не зависящей от ω.

### ЕДИНСТВЕННЫЙ ЧЕСТНЫЙ ДОВОД ЗА ПОЛИНОМЫ — ОБУСЛОВЛЕННОСТЬ, И ЗАМЕР ЕГО НЕ ПОДДЕРЖИВАЕТ

Плоско-волновые базисы печально известны вырождением: при росте `ND` на
фиксированном `kW` волны становятся почти линейно зависимыми, число
обусловленности растёт экспоненциально (литература по UWVF/PWDG — Хиптмайр,
Мойола, Перуджа). У полиномов с этим лучше. И у проекта обусловленность болит:
LSQR сходится как CG на нормальных уравнениях, то есть по `cond²`.

**Но замер против:** `tdg2d` даёт `193 → 66` итераций при росте элемента от 1λ
до 10⁴λ. Число итераций ПАДАЕТ. На использованных `ND` обусловленность связывающим
ограничением не была. Довод остаётся в силе только как предупреждение о том, что
случится при росте `ND`, — и это ещё одна причина, по которой `ND` решает всё.

### И ЛОЖНАЯ АЛЬТЕРНАТИВА: ПОЛИНОМЫ УЖЕ ЗДЕСЬ

Базис проекта есть `φ(x/W−n)·e^{ik·x}`, где `φ` — кусочно-КВАДРАТИЧНЫЙ потенциал.
То есть постановка уже полиномиальна по амплитуде и волновая по фазе. Вопрос
«полиномы ВМЕСТО» не имеет предмета: полином — амплитуда, плоская волна — фаза,
и заменять одно другим нечего.

### ПРОДУКТИВНАЯ ФОРМА ВОПРОСА: ПОЛИНОМ В ПОКАЗАТЕЛЕ, А НЕ В АМПЛИТУДЕ

Здесь начинается то, ради чего вопрос стоит не закрывать целиком.

Измеренный закон (PLAN.md): на КРИВОЙ поверхности направление меняется поперёк
ячейки на `W/ρ`, константная несущая врёт на `W/2ρ`, фазовая ошибка
`kW²/2ρ ≲ 1` даёт

    W ≲ sqrt(ρλ/π)     при ρ = 0.5 м, λ = 550 нм   →   W ≈ 0.3 мм  (≈540λ)

**Размер элемента на кривизне ограничен тем, что несущая — КОНСТАНТНАЯ плоская
волна.** Если пустить в показатель квадратичный член, подогнанный под кривизну
(обобщённая плоская волна, GPW — Имбер-Жерар и Депре), ведущий член уходит и
ограничение задаётся кубическим:

    W ≲ (ρ²λ/2π)^(1/3)   при тех же ρ, λ   →   W ≈ 2.8 мм

Это `×9.4` по длине, то есть **`×88` по числу элементов на поверхности** —
существенно больше, чем даёт весь рычаг важности из раздела 2. И бьёт прямо в
`n`, то есть в стену раздела 0, а не в множитель перед ней.

**ЦЕНА, И ЕЁ НАДО ПРОВЕРИТЬ ПРЕЖДЕ ВСЕГО:** GPW уравнение Гельмгольца ТОЧНО не
решает — только с точностью до порядка. Значит возвращается объёмная невязка,
то есть ровно то, отсутствие чего делает постановку дешёвой. Весь вопрос в том,
мала ли она настолько, чтобы её можно было либо игнорировать, либо взять
замкнутой формой (квадратичный показатель даёт гауссов интеграл, а он берётся
аналитически — в отличие от общего случая).

**Оценка `×88` — МОЯ, на основе записанного закона, а НЕ ЗАМЕР.** Проверяется
дёшево и прямо на существующем стенде: `tools/tdg2d.c` уже умеет мерить ошибку
против углового промаха (ключ `dth=`); добавить квадратичный член в показатель и
повторить таблицу W. Фальсификаторы:
- если ошибка при квадратичном показателе не уходит в кубический закон, оценка
  `×88` неверна и вопрос закрыт целиком;
- если объёмная невязка GPW превышает скачки на скелете, постановка перестаёт
  быть Трефтцевой и возвращается к квадратуре — тогда лечение хуже болезни;
- негативный контроль: на ПЛОСКОЙ поверхности (`ρ = ∞`) квадратичный член обязан
  выходить в ноль, а числа — совпадать с текущими побитово.

---

## 4. [ОТЛОЖЕНО — ВОЛНОВАЯ ЛИНИЯ, НЕ ЧИТАТЬ БЕЗ ОТМЕНЫ БАННЕРА] КУСОЧНАЯ ГЛАДКОСТЬ ПОЛЯ

**Вопрос пользователя (07-28):** правильно ли, что и в этом сеттинге кусочная
дифференцируемость не прокатит — например, интерполировать поле кусочно-линейно
(и кусочно-постоянно, где надо)?

### СНАЧАЛА ФАКТ: C¹-ПОТЕНЦИАЛОВ В ЖИВОЙ ВОЛНОВОЙ ПОСТАНОВКЕ УЖЕ НЕТ

`CLAUDE.md` в верхней секции всё ещё описывает базис как C¹ кусочно-квадратичные
потенциалы `φ(x)=2−x²` с разбиением единицы. **Это устаревший текст того же
класса, что Г19** — постановка сменена 07-26 (PLAN.md, «ПОСТАНОВКА СМЕНЕНА НА
ТРЕФТЦ-DG»), и в `tools/tdg2d.c` записано прямым текстом:

> HERE cells do not overlap and the basis is PURE plane waves… there is no volume
> integral anywhere. Everything lives on the skeleton.

**То есть амплитуда УЖЕ кусочно-постоянна (одна константа на направление на
ячейку), а между ячейками поле УЖЕ разрывно.** Пользователь предлагает не
понижение, а описание текущего состояния.

### И ДИФФЕРЕНЦИРУЕМОСТЬ БЫЛА ТРЕБОВАНИЕМ УМЕРШЕЙ ПОСТАНОВКИ

C¹ нужна была НЕПРЕРЫВНОЙ (PUM) объёмной галёркинской форме. Она провалилась, и
не из-за гладкости, а структурно: несущая гасит `k²` аналитически ⇒ плоская
волна лежит в ЯДРЕ объёмного оператора, а непрерывность у C¹-базиса встроена и
уравнений не даёт. Осталась только внешняя граница: `NE²·ND` неизвестных против
`O(NE·ND)` условий. Замер (`tools/slab2d.c`): точное решение удовлетворяет
собранной системе до `3e-13`, LSQR гонит невязку до `9e-8` и УХОДИТ от него
(ошибка 0.84 → 1.13).

В DG не требуется даже C⁰: сшивка слабая, `[u] = 0` и `[∂ₙu]/(ik) = 0` на
гранях, обе величины одной размерности ⇒ **штрафного параметра нет вовсе**.
Разрыв — не уступка, а конструкция.

### ПОЭТОМУ НАСТОЯЩИЙ ВОПРОС ОБРАТНЫЙ: ХВАТАЕТ ЛИ КУСОЧНО-ПОСТОЯННОЙ?

И на него есть ЗАМЕР, прямо в `tools/tdg2d.c:206`:

> the drive is an exact trace rather than the staircase a per-cell envelope
> produced (**measured: that staircase pinned the residual at W/w, 0.16 for W=2
> w=8, and no amount of extra directions moved it**)

**Закон: кусочно-постоянная амплитуда представляет меняющуюся огибающую с
ошибкой `W/w`** (размер ячейки к масштабу огибающей), **и лишние направления её
НЕ ЛЕЧАТ.** Замер снят на ИСТОЧНИКЕ, а не на базисе решения, — это надо
перепроверить на базисе, — но принцип переносится: там, где огибающая меняется
ВНУТРИ ячейки (перетяжка пучка, каустика, край тени, источник), константа даёт
пол, который не сбивается ничем, кроме измельчения `h`.

Там же, рядом, лежит закон о `ND`, важный для раздела 0:

> the direction count of a LOCALISED field is `2 * height/width` — it is set by
> how tight the beam is, not by how far it travels or how many times it bounces

### ЛИНЕЙНАЯ АМПЛИТУДА НЕ ЗАПРЕЩЕНА, И ЕЁ НАРУШЕНИЕ ПАДАЕТ С РОСТОМ ЯЧЕЙКИ

Проверка прямая:

    (∂² + k²)[x·e^{ikx}] = 2ik·e^{ikx}

Не ноль — значит Трефтц нарушен. Но относительно члена `k²u` при `|x| ~ W`:

    невязка / (k²u)  ≈  2/(kW)

**Нарушение УБЫВАЕТ с ростом элемента** — при `W = 1000λ` это `3.2e-4`. Это
противоположно интуиции «выше порядок — сложнее» и делает P1-амплитуду
кандидатом именно на КРУПНЫХ ячейках, то есть там, где проект и работает.

Цена: возвращается объёмный интеграл. Но он берётся В ЗАМКНУТОЙ ФОРМЕ —
подынтегральное есть многочлен × `e^{i(k−k′)·x}`, а это интегрируется
аналитически (тот самый аппарат «exact phi × phi × plane-wave integration over
polygons», который в заголовке `tdg2d.c` назван ненужным для чистых волн).
То есть цена — АППАРАТ, а не квадратура и не зависимость от ω.

### СВЯЗЬ С РАЗДЕЛОМ 3: ОДНО СЕМЕЙСТВО ХОДОВ

Разделы 3 и 4 задают один вопрос — **что вообще может жить внутри трефтцевой
ячейки, не добавляя направлений.** Поскольку `ND` есть связывающая стоимость
(раздел 0, вывод 3), это единственное семейство, где обогащение ячейки не бьёт
в стену:

| ход | что покупает | нарушение Трефтца | статус |
|---|---|---|---|
| больше направлений `ND` | угловое содержание | нет | законно, но ЭТО И ЕСТЬ СТЕНА |
| квадратичная ФАЗА (GPW, разд. 3) | размер ячейки на кривизне, оценка `×88` | кубический порядок | не проверено |
| линейная АМПЛИТУДА (P1) | огибающую внутри ячейки, пол `W/w` | `2/(kW)`, убывает с `W` | не проверено |
| постоянная амплитуда | — | нет | текущее состояние |

**Фальсификаторы раздела:**
- «`2/(kW)` мало» — это ПОТОЧЕЧНОЕ отношение. Значимо же отношение СОБРАННЫХ
  членов: объёмный член против скелетного. Не выведено и не измерено; если
  объёмный член сравним со скачками на гранях, постановка перестаёт быть
  трефтцевой и весь ход отменяется;
- закон `W/w` снят на источнике, не на базисе решения. Перепроверить: пучок с
  перетяжкой `w`, свип по `W`, ожидание — пол невязки линеен по `W/w` и не
  движется от `ND`;
- негативный контроль для P1: на ПЛОСКОЙ волне (огибающая постоянна) линейный
  член обязан выходить в ноль, а числа — совпадать с текущими побитово.

### 4.1 КУСОЧНО-ПОСТОЯННАЯ ПЛОХА — ПОДТВЕРЖДЕНО СЧЁТОМ, И ЦЕНА ПОСЧИТАНА

**Возражение пользователя (07-28):** кусочно-постоянная плоха, достаточно любого
простого примера приближения — это ступеньки, и гладкого изображения почти
никогда не будет. Идея реализуема и на кусочно-квадратичной, но с линейной проще.

**Принято, и вот во что это обходится в числах.** Порядки приближения обычные:

    P0:  ошибка ≈ W/w          (w — масштаб огибающей)
    P1:  ошибка ≈ (W/w)²

При целевой ошибке `e` допустимый размер ячейки: `W₀ = e·w` против `W₁ = √e·w`.

| цель `e` | `W₁/W₀` | элементов на поверхности (`÷W²`) | DOF на ячейку (3D) | **ЧИСТО** |
|---|---|---|---|---|
| 10% | 3.2× | ÷10 | ×4 | **×2.5** |
| 1% | 10× | ÷100 | ×4 | **×25** |
| 0.1% | 32× | ÷1000 | ×4 | **×250** |

**При целевой ошибке 1% переход P0 → P1 даёт чистые `×25` по числу неизвестных**
— того же порядка, что `×88` из раздела 3, и в том же семействе «обогатить
ячейку, не добавляя направлений».

**СЧИТАТЬ ТОЛЬКО ВМЕСТЕ, НИКОГДА ПОРОЗНЬ.** Линейная амплитуда в 3D есть `×4`
неизвестных на ячейку (4 члена: `1, x, y, z`). По таблице раздела 0 это само по
себе переводит строку `ND = 1` (11 с) в строку `ND = 4` (8 мин). Выигрыш
существует ТОЛЬКО вместе с укрупнением ячейки; докладывать `×4` и `÷100` по
отдельности — жульничество того же класса, что доклад установившегося кадра без
холодного старта.

### 4.2 АРТЕФАКТ БУДЕТ НЕ СТУПЕНЬКАМИ, А ДИФРАКЦИОННЫМ ПРИЗРАКОМ

Здесь интуицию из «простого примера приближения» надо поправить, и поправка
существенна для того, ЧЕМ этот дефект ловить.

Поле в ячейке НЕ постоянно: оно есть константа × плоская волна, то есть гладкая
аналитическая осцилляция. Ступенчата ОГИБАЮЩАЯ, а не поле. Поэтому блочной
картинки, как у P0-интерполяции яркости, не будет.

Будет хуже и тоньше. Огибающая с ошибкой `W/w`, периодичная с шагом ячейки `W`,
на апертуре есть **амплитудная решётка с периодом `W`**. Решётка даёт порядки:

    призрак под углом  θ ≈ λ/W,  интенсивность ≈ (W/w)²

При `W/w = 0.16` (замеренное значение) это `2.5%` света, ушедшего в ложное
изображение, смещённое на `D/W` разрешаемых элементов от истинного. Не блоки —
**ореол и двоение**, нелокальные, светящие туда, где источника нет.

Прецедент в проекте есть: поломка разбиения единицы `Σφ = 4` давала «паразитную
амплитудную модуляцию, измерено 30% ошибки на стенде масштаба» (`CLAUDE.md`).
Тот же механизм, другая причина модуляции.

**Практическое следствие:** искать этот дефект глазами по «ступенькам»
бесполезно, его там не будет. Ловить надо спектром картинки — пик на
пространственной частоте `1/W` — либо негативным контролем: сместить сетку на
полячейки, призрак обязан сместиться, истинное изображение — нет.

**`(W/w)²` и `λ/W` — МОЙ ВЫВОД, не замер.** Фальсификатор дешёвый: сцена с
гладкой огибающей, свип по `W`, ожидание — доля света вне главного пятна растёт
как `(W/w)²`, а угол призрака падает как `1/W`. Если вместо этого ошибка
распределена широкополосно, механизм назван неверно.

### 4.3 ЛИНЕЙНАЯ ПРОТИВ КВАДРАТИЧНОЙ: ВЫБОР ЧИСТО ПО DOF

Пользователь прав, что реализуемо и то и другое, и что с линейной проще.
Добавлю, почему выбор здесь НЕ упирается в нарушение Трефтца: для амплитуды
степени `p`

    (∂² + k²)[xᵖ e^{ikx}]  →  ведущий член  2p·ik·x^{p−1} e^{ikx}
    нарушение / (k²u)  ≈  2p/(kW)

**Степень входит в нарушение ЛИНЕЙНО, а `kW` — в знаменателе.** При `W = 1000λ`
(`kW ≈ 6.3e3`) даже `p = 10` даёт `3e-3`. То есть на крупных ячейках
полиномиальная амплитуда почти-трефтцева при любой разумной степени, и
ограничитель — не физика, а СЧЁТ DOF: в 3D `(p+1)(p+2)(p+3)/6` = 1, 4, 10, 20.

    P0 → 1,  P1 → 4,  P2 → 10,  P3 → 20

Отсюда выбор P1 обоснован не только простотой: `×4` против `×10` при том, что
основной скачок точности (`W/w` → `(W/w)²`) берётся уже первым порядком. P2
покупает `(W/w)³`, то есть при цели 1% ещё `×4.6` по `W`, но ценой `×2.5` DOF —
чистых `×8`. **Не отвергать, но и не первым: P1 берёт большую часть выигрыша за
меньший аппарат, и это ровно то, что сказал пользователь.**

### 4.4 ОБЛАСТЬ ДЕЙСТВИЯ РАЗДЕЛОВ 3 И 4 — ВОЛНОВАЯ ЛИНИЯ, И ОНА ОТЛОЖЕНА

**Пользователь 07-28: интересует только линия уравнения рендеринга (перенос),
остальное долго ещё не понадобится.** Разделы 3 и 4 (квадратичная фаза, P1-
амплитуда, Трефтц) относятся к ВОЛНОВОЙ линии и к переносу не переносятся.
Оставлены как есть, потому что оценки `×88` и `×25` дёшево проверяемы и терять
их незачем, но на критический путь не идут.

**Разделы 0, 1 и 2 действуют в ОБЕИХ линиях** — стена по размеру системы,
трёхъярусное хранение, важность.

---

## 5. УГЛОВОЕ ПРЕДСТАВЛЕНИЕ В ПЕРЕНОСЕ: КОНУСА НЕТ, ЕСТЬ КВАДРАТУРА

**Вопрос пользователя (07-28): конус линейный?**

### Ординат — это дельта с весом, а не конус

`src/transport/dirs3.h`: `tr3_dirs` есть список ЕДИНИЧНЫХ направлений
`(ox, oy, oz)` и весов `w` с `Σw = 4π`. Набор — произведение ДВОЙНОГО Гаусса по
`μ` (узлы раздельно на `[−1,0]` и `[0,1]`, К10) на Гаусс ПО КВАДРАНТАМ азимута,
`n = 2·nmu · 4·nphi`.

**Значит углового элемента как области не существует.** Радианс есть `ND` чисел,
интеграл по сфере есть квадратурная сумма `Σ w_m I_m`. Вопрос «конус постоянный
или линейный» к текущему коду неприменим: там не кусочная аппроксимация по углу,
а квадратурное правило — другой объект с другим законом ошибки.

Хранится при этом ещё меньше: по Р1 угловое поле ТРАНЗИЕНТНО — наружу выходит
только скалярный поток `φ`, 4 числа на ячейку (DG1 по пространству), а полные
`ND` ординат живут внутри ОДНОГО прохода развёртки.

### И ровно поэтому «ступенчатость» здесь имеет другое имя: ЛУЧ-ЭФФЕКТ

Квадратура спектрально точна на ГЛАДКОМ радиансе и проваливается на негладком, и
в проекте это записано трижды:

- **К3** — интерполировать между ординатами ЗАПРЕЩЕНО, «иначе на картинке будут
  полосы»;
- `src/transport/scat1d.h` — «дельта по углу в набор ординат не попадает по
  построению, и влёт по ближайшей ординате дал бы **луч-эффект, который не
  лечится числом ординат**»;
- **К5**, `gather3.h` — отражённое направление в наборе ординат ОТСУТСТВУЕТ,
  поэтому зеркало идёт ПРОСЛЕЖИВАНИЕМ, а не через ординаты.

**Суть луч-эффекта: представление не содержит того, что в поле есть.** Добавление
ординат его не лечит — лечит только вынос негладкой части из углового
представления целиком.

### Продуктивная форма вопроса — линейный по углу — уже спланирована как Р2

`I(ω) ≈ (φ + 3 ω·F)/4π` — это и есть ЛИНЕЙНЫЙ по углу радианс: 4 момента вместо
32 ординат. В таблице К77 (заведена 07-28 по запросу пользователя «как ещё
уменьшить систему») это строка

| рычаг | действует на | множитель | посчитан? |
|---|---|---|---|
| **Р2/`P1`: 32 ординаты → 4 момента** | `ND` | **`÷8`** (`÷5` в анизотропной) | **НЕТ** |

**Ограничение записано как К4 и его нельзя обойти:** `P1` предполагает почти
изотропный радианс и СКАЛЯРНЫЙ коэффициент диффузии. Среда, которая свет
КАНАЛИЗИРУЕТ (полог, трава, ткань), изотропной не становится ни после какого
числа рассеяний ⇒ коэффициент диффузии ТЕНЗОРНЫЙ, шесть чисел вместо одного, и
это ДРУГОЕ уравнение. Критерий выбора — анизотропия `σ_t(ω)`, а не оптическая
толщина.

### ОТСЮДА ОТВЕТ НА ВОПРОС ЦЕЛИКОМ: РАДИАНС РАСЩЕПЛЁН НА ТРИ ЧАСТИ

Линейный по углу годится не «вместо ординат», а для ОДНОЙ из трёх частей, и
остальные две из углового представления вынесены:

| часть радианса | чем считается | почему не ординатами |
|---|---|---|
| прямой свет | аналитически, ВНЕ ординат (Р6) | дельта по углу в набор не попадает |
| зеркальная | прослеживанием ломаной (К5) | отражённого направления в наборе нет |
| косвенная диффузная | ординаты → **моменты, Р2** | гладкая ⇒ линейный по углу законен |

**То есть «конус линейный?» имеет ответ: линейный законен ровно там, где радианс
гладкий, а вся негладкость из угловой оси удалена ЗАРАНЕЕ, конструкцией.** Это
не обход вопроса — это то, чем плата за отказ от кусочной аппроксимации по углу
уже внесена.

### ПРОСТРАНСТВЕННЫЙ ПОРЯДОК ИДЁТ ВНИЗ, А НЕ ВВЕРХ

Рычагом в переносе значится **DG1 → DG0 в гладком ОБЪЁМЕ** (`÷2…÷3`, К77) —
ПОНИЖЕНИЕ порядка: наклоны суть 3 неизвестных из 4, а в гладкой середине не
нужны. Работает это потому, что негладкость (прямой свет, зеркальное) уже
вынесена в отдельные проходы, и в объёме остаётся гладкий остаток.
Оговорка К77 жёсткая: **на ПОВЕРХНОСТЯХ DG1 остаётся**, понижение только в объёме.

---

## 6. АЛЬТЕРНАТИВЫ УГЛОВОМУ ПРЕДСТАВЛЕНИЮ: КАРТА И ОДНА ПОПРАВКА К ПЛАНУ

**Запрос пользователя (07-28): обсудить альтернативы подробнее.**

### Карта: четыре семейства, и три из них уже в плане

| представление | что это | статус в проекте |
|---|---|---|
| **дискретные ординаты `S_N`** | дельты + квадратурные веса, `Σw = 4π` | **текущее**, `dirs3.c` |
| **моменты `P_1`/`SP_N`** | `I ≈ (φ + 3ω·F)/4π`, 4 числа | **Р2**, `÷8`, НЕ реализовано |
| **угловой БАЗИС с носителями** | конусы с формой внутри | **этап D**, «самое крупное и самое неопределённое» |
| **вынос негладкого из угла** | прямой свет аналитически, зеркало прослеживанием | **К9/К5/Р6**, частично сделано |

`S_N` есть квадратура: **веса, без носителей, без разбиения единицы, без
иерархии** (PLAN_TRANSPORT.md:2431) — оттуда и К3.

### Два режима отказа, и они ВЗАИМНО ДОПОЛНИТЕЛЬНЫ

- **дельты (`S_N`) → ЛУЧ-ЭФФЕКТ** на негладком. Измерено на К23: пучок влётом по
  ближайшей ординате дал `9.7e-3 → 4.8e-2 → 2.4e-2` НЕМОНОТОННО по `ND` — то есть
  добавление ординат не помогает и даже не монотонно;
- **моменты (`P_1`) → ГИББС И ОТРИЦАТЕЛЬНОСТЬ** на негладком. И это прямо
  конфликтует с уже записанным требованием: К6 и К14 требуют ПОЛОЖИТЕЛЬНОСТИ
  (ограничитель монотонности несовместим с Р1, осталась положительность).
  `P_1` её не гарантирует.

**Отсюда: ни одно из двух не лучше другого само по себе. Оба становятся законны
ТОЛЬКО после того, как негладкая часть вынесена** — источник первого столкновения
(К9: иначе двойной учёт), зеркала прослеживанием (К5). То есть **К9 и К5 не
«ещё рычаги», а ПРЕДПОСЫЛКА и для Р2, и для D.** Порядок работ этим и задан.

### ЭТАП D УЖЕ НАПИСАН, И ЕГО ЦЕНА НАЗВАНА ЗАРАНЕЕ

`PLAN_TRANSPORT.md:3208`, «D. УГЛОВОЙ БАЗИС ВМЕСТО КВАДРАТУРЫ — САМОЕ КРУПНОЕ И
САМОЕ НЕОПРЕДЕЛЁННОЕ». Даёт: адаптивность по углу, исчезновение луч-эффекта,
снятие К3, ленточные (а не плотные) угловые блоки, и главное — **КРИВЫЕ ЗЕРКАЛА**
через локальные наборы с почти тождественной передачей между соседними гранями.

Цена, как она там записана:

> дискретные ординаты суть ДИАГОНАЛИЗОВАННЫЙ угловой базис, и с перекрывающимися
> носителями одновременной диагонализации нет — **развёртка как прямой решатель
> `O(N)` исчезает**, а с ней и Р1 (угловое поле перестаёт быть транзиентным,
> полный вектор надо держать)

Это убийственно: развёртка есть то, что делает перенос `O(N)`, а Р1 — главная
статья экономии памяти (наружу выходит `φ`, 4 числа на ячейку, вместо `ND`).

### ПОПРАВКА, КОТОРУЮ Я ПРЕДЛАГАЮ ПРОВЕРИТЬ: ЦЕНА НАЗВАНА ДЛЯ *ПЕРЕКРЫВАЮЩИХСЯ* НОСИТЕЛЕЙ

В самой цитате стоит условие — «**с перекрывающимися** носителями». Оно не
обязательно: угловые элементы можно взять **РАЗРЫВНЫМИ** (не перекрывающимися) —
конусы, разбивающие сферу без наложения, форма внутри своя у каждого. Тогда

    ∫ ω · b_i(ω) b_j(ω) dω   ≠ 0   только при i, j в ОДНОМ конусе

то есть угловая матрица **БЛОЧНО-ДИАГОНАЛЬНА**, блок размера «число DOF на конус»
(3 для линейного по углу, 4 для полного). Развёртка тогда не исчезает: вместо
скаляра на ординату в ячейке решается маленький плотный блок `3×3`.

**УСЛОВИЕ, ПРИ КОТОРОМ ЭТО РАБОТАЕТ, ГЕОМЕТРИЧЕСКОЕ И ПРОВЕРЯЕМОЕ:** порядок
обхода задаётся знаком `ω·n`, поэтому **ни один конус не должен пересекать
поверхность `ω·n = 0` ни для одной грани**. Для коробок октодерева нормали
осевые, значит достаточно, чтобы конусы не пересекали координатные плоскости —
то есть были вложены в ОКТАНТЫ. Набор `dirs3` уже устроен так: Гаусс по
квадрантам, узлы строго внутри, направлений вдоль осей нет вовсе — **ровно из
этого же соображения** («такое направление дало бы грань с `ω·n = 0`, то есть
ячейку без вытока и вырожденный порядок обхода»). Конусы, вложенные в октанты,
наследуют это свойство.

**Если поправка верна, меняется многое:**
- развёртка `O(N)` СОХРАНЯЕТСЯ (блочная вместо скалярной);
- **Р1 сохраняется** — угловой вектор по-прежнему транзиентен внутри прохода,
  наружу выходит `φ`. Главная статья памяти не теряется;
- предобуславливатель этапа B не выбрасывается;
- D перестаёт быть «самым неопределённым» и становится обозримым.

**ЭТО ЗАЯВКА ПРОТИВ ЗАПИСАННОГО ПЛАНА, А НЕ ЗАМЕР.** Фальсификаторы:
1. **разрезанные ячейки.** У разреза нормаль произвольная, и при произвольном `n`
   какой-нибудь конус `ω·n = 0` пересечёт ОБЯЗАТЕЛЬНО. Сегодня это не мешает
   (`sweep3.h`: ячейка есть коробка, поверхность входит ГРАНИЧНЫМ УСЛОВИЕМ), но
   как только разрез станет потоковой гранью — условие рушится. **Проверять
   первым;**
2. **порядок сходимости обязан получить НОВОЕ предсказание ДО прогона (К58).**
   `O(ND⁻²)` есть свойство ГАУССОВОЙ квадратуры; у базиса с ограниченным
   носителем порядок свой. Потребовать `ND⁻²` от конусов значит завести красный
   тест на исправном коде — пятый генератор ложной тревоги после К1, К6, К10, К18;
3. **стоимость.** `NC` конусов × 3 DOF против `ND` ординат: работа на ячейку идёт
   как `NC·9` против `ND`. При `NC = 8` это `72` против `32` — **вдвое дороже за
   проход.** Оправдано только если луч-эффект уходит и нужных угловых DOF станет
   меньше вдвое с запасом. Не измерено;
4. **положительность.** Линейный по конусу может уйти в минус так же, как `P_1`.
   Требование К6/К14 остаётся, ограничитель по углу — отдельная работа;
5. **негативный контроль:** конусы с ПОСТОЯННОЙ формой внутри (DOF = 1) обязаны
   воспроизвести текущий `S_N` с точностью до выбора весов. Если не воспроизводят
   — сломана оснастка, а не метод.

**Порядок из плана менять не предлагаю:** там записано «не идти в D, пока не
сделаны A, B, C — иначе нечем будет измерить». Поправка касается ЦЕНЫ D, а не
его очереди.

---

## 7. НОСИТЕЛЬ С УПРАВЛЯЕМОЙ ЛОКАЛЬНОСТЬЮ: ЭЛЕМЕНТ ПО ГЕОМЕТРИИ И ПО ИСТОЧНИКУ

**ИДЕЯ ПОЛЬЗОВАТЕЛЯ (07-28), изложена полностью.** Сейчас носитель неизвестной
задан СЕТКОЙ — коробка листа октодерева, и элементов много, потому что мелко
порезано пространство. Надо, чтобы носитель задавался ГЕОМЕТРИЕЙ и ИСТОЧНИКОМ
СВЕТА: крупный элемент не «большой и развесистый», а подстроенный. На
поверхности куба — линейная интерполяция по грани или по нескольким полигональным
кускам. **Каждый подстроенный элемент обязан отвечать за гораздо больший объём.**
Пользователь уверен, что выигрыш «даже больше, чем в разы», и сам отмечает, что
за пределами поверхности — надо подумать. Раздел отвечает именно на это.

### 7.1 НА ПОВЕРХНОСТИ — ЭТО Р3, И ОНО УЖЕ ПОЛОВИНОЙ В ПЛАНЕ

Р3: на поверхностях хранить ИСХОДЯЩИЙ радианс, у диффузной это ОДНО число
вместо 80. Предложение пользователя уточняет НОСИТЕЛЬ этого числа: не фасет
разреза, а полигон геометрии с линейной интерполяцией по нему. Грань куба —
один элемент независимо от размера.

Это не трогает ни развёртку, ни объём, ни отсутствие `V(x,z)`. Самая дешёвая
часть идеи и самая бесспорная.
*Ограничитель записан отдельным правилом:* угловое разрешение ХРАНИМОГО задаётся
шириной лепестка ПОТРЕБИТЕЛЯ (развязка Р1/Р3). Линейная интерполяция по полигону
законна для диффузной части; для глянца ширина лепестка диктует своё, и это
разные числа на одной и той же грани.

### 7.2 ЗА ПРЕДЕЛАМИ ПОВЕРХНОСТИ: В ПРОЗРАЧНОМ ОБЪЁМЕ НЕИЗВЕСТНЫХ НЕТ ВОВСЕ

Здесь ответ даёт само уравнение, и он сильнее любой оценки.

    dI/ds = −σ I + S        вдоль луча

При `σ = 0` и `S = 0` радианс **ПОСТОЯНЕН вдоль луча**. Значит в прозрачной
области объём не несёт НИКАКОЙ независимой информации: поле в ней полностью
определено значениями на границе области. Представление по ячейкам там избыточно
**на целое измерение**:

| представление | счёт неизвестных |
|---|---|
| ячейки × ординаты (текущее) | `(L/h)³ · ND` |
| по лучам (2 поперечные координаты) | `(L/h)² · ND` |
| **отношение** | **`L/h`** |

При `L/h = 100` это `×100` — то есть «больше, чем в разы» не интуиция, а
следствие уравнения. **Это и есть «элемент отвечает за гораздо больший объём»:
вдоль луча он может быть сколь угодно длинным, потому что представлять там
нечего.**

При `σ > 0` вывод слабее, но не исчезает: вдоль луча стоит ОДНОМЕРНОЕ ОДУ,
решаемое ТОЧНО (экспонента), а не произвольная функция. Ячейки вдоль луча нужны
не для того, чтобы представить перенос — он аналитичен, — а для того, чтобы
представить **ИСТОЧНИК РАССЕЯНИЯ** `S`.

### 7.3 ОТСЮДА ГЛАВНОЕ: РАЗМЕР ЭЛЕМЕНТА ЗАДАЁТСЯ ГЛАДКОСТЬЮ `S`, А НЕ ГЛАДКОСТЬЮ `I`

И это выгодно, потому что `S` — самая гладкая величина в задаче: при изотропном
рассеянии `S = σ_s φ/4π`, а `φ = ∫I dω` есть УГЛОВОЕ СРЕДНЕЕ, гладкое даже там,
где сам `I` разрывен (луч-эффект — свойство `I`, не `φ`).

Где `φ` НЕ гладок: у источников и на границах полутени. **Полутени суть
ПОВЕРХНОСТИ в трёхмерии**, то есть множество меры нуль. Значит при носителях,
подстроенных под теневую структуру,

    объём:  O((L/h)³)  →  O((L/h)²) [полутени] + грубый объём

— снова целое измерение, и снова по той же причине, что у пользователя на кубе:
**информация живёт на поверхностях, а не в объёме. В объёме «поверхность» — это
граница полутени.**

Именно это и означает «подстраивался к геометрии И ИСТОЧНИКУ СВЕТА»: форма
носителя следует геометрии, дробление — теневой структуре источника. Вторая
половина есть в точности важность из раздела 2 (сопряжённая задача), и они
складываются, а не конкурируют.

### 7.4 СЛЕДСТВИЕ, КОТОРОЕ Я ПРЕДЛАГАЮ ДОБАВИТЬ: НОСИТЕЛЬ, ЗАВИСЯЩИЙ ОТ ОРДИНАТЫ

Если вдоль луча представлять нечего, то естественный элемент в объёме — **не
коробка, а ТРУБКА, вытянутая вдоль направления.** А раз развёртка обрабатывает
ординаты ПО ОДНОЙ и угловое поле транзиентно (Р1), каждая ордината может идти по
СВОЕЙ вытянутой сетке, а храниться будет по-прежнему только `φ` на общей
изотропной сетке.

    транзиентно:  на ординату ω — сетка, вытянутая вдоль ω (анизотропия A)
    хранимое:     φ на общей изотропной сетке, 4 числа на ячейку (Р1 цела)
    связь:        проекция между ними на каждом проходе

Работа развёртки на ординату падает в `A` раз — во столько же, во сколько меньше
ячеек пересекает марш. Предел на `A` задаёт скорость изменения `φ` ВДОЛЬ `ω`,
а поперечный размер — теневая структура и геометрия. В комнате с резкими тенями
это разные масштабы, и `A ~ 10…100` правдоподобно.

**Это ложится на уже записанное, а не спорит с ним:** порядок обхода на
анизотропной сетке всё равно надо строить топологической сортировкой заново для
каждой ординаты, `O(ячейки)` на ординату, и это в плане уже названо «дёшево, но
не бесплатно». Анизотропия ничего нового к этому не добавляет.
Плюс К4: в канализирующей среде (полог, трава) `σ_t` анизотропна физически, так
что вытянутый носитель там не трюк, а соответствие среде.

### 7.5 ПОТОЛОК ВЫИГРЫША, И ОН ЖЁСТКИЙ: ГЛАДКОСТЬ `φ`, А НЕ ГЕОМЕТРИЯ

**Верхняя граница НЕ задаётся тем, насколько крупные элементы позволяет
геометрия.** Она задаётся тем, на каком масштабе меняется `φ`:

| сцена | масштаб `φ` | выигрыш |
|---|---|---|
| комната/город, резкие тени | размер комнаты, кроме полутеней | **`×L/h`, то есть десятки-сотни** |
| туман умеренной плотности | длина свободного пробега | ограничен `L/ℓ` |
| **плотный полог, трава** | **длина пробега, всюду** | **`×1` — выигрыша НЕТ** |

То есть идея даёт максимум ровно там, куда целится город, и вырождается в
плотной рассеивающей среде. Это не возражение, а область применимости, и её надо
объявить заранее — иначе замер на игрушечной сцене даст множитель, который на
пологе не воспроизведётся (класс К71/К82, перенос замера в другие условия).

### 7.6 ПЕРЕСЕЧЕНИЕ С Р6 — ЕСТЬ, И ЕГО НАДО ВЫЧЕСТЬ

**Р6 («прямая и косвенная на РАЗНЫХ сетках», `÷8` осторожно, до `÷64`) есть
ЧАСТНЫЙ СЛУЧАЙ этой идеи** — равномерное огрубление косвенной сетки по тому же
самому доводу (косвенная гладкая). Идея пользователя обобщает его с равномерного
огрубления на подстроенный носитель.

**Значит множители Р6 и «носитель по геометрии» ПЕРЕСЕКАЮТСЯ и перемножать их
нельзя.** Ровно то, о чём предупреждает К75. Правильный учёт: идея ЗАМЕЩАЕТ Р6 и
даёт `÷8…÷64` плюс добавку за анизотропию и за подстройку формы; отдельной
строкой в таблицу К77 её вносить нельзя.

### 7.7 ФАЛЬСИФИКАТОРЫ

1. **Главный, и он про честность метрики:** свип по коэффициенту анизотропии `A`
   и по размеру объёмного элемента. Картинка обязана НЕ меняться до тех пор, пока
   элемент мельче масштаба `φ`, и обязана сломаться, как только крупнее. Если она
   не ломается НИКОГДА — метрика слепа (в проекте это уже случалось шесть раз);
2. **негативный контроль:** подстроить носитель под ДРУГОЙ источник света
   (сдвинутый), оставив геометрию ⇒ картинка обязана сломаться на `O(1)`. Если не
   сломалась — подстройка под источник не работает, а выигрыш приписан ей ложно;
3. **полутени:** сцена с площадным источником и резким окклюдером. Утверждение
   «негладкость `φ` живёт на поверхностях меры нуль» проверяется прямо — доля
   элементов, попавших в измельчение, обязана расти как `(L/h)²`, а не `(L/h)³`;
4. **плотная среда:** та же сцена с `σ_s`, дающим `τ ≫ 1`. Предсказание —
   выигрыш обязан УПАСТЬ до `×1`. Если он там сохранился, значит меряется не то,
   что заявлено;
5. **положительность (К6/К14)** обязана держаться при любом `A`; крупный
   вытянутый элемент — первое место, где ограничитель сорвётся.

### 7.8 ФОРМА В ОБЪЁМЕ: ЛИНЕЙНАЯ УЖЕ ЕСТЬ; ТЕТРАЭДРЫ ДОРОЖЕ; РАЗВИЛКА В СЛОВЕ «ИНТЕРПОЛЯЦИЯ»

**Уточнение пользователя (07-28):** для объёма напрашивается линейная
интерполяция по вокселям либо по триангулированным (на симплексы) вокселям;
трилинейная скорее всего не нужна.

**Про трилинейную — согласен, и она не нужна по счёту DOF:** трилинейная есть 8
коэффициентов против 4 у линейной, а рычаг К77 идёт в ОБРАТНУЮ сторону
(`DG1 → DG0`, `4 → 1…2`).

**Линейная в объёме УЖЕ СТОИТ.** `DG1` — это ровно линейная функция на ячейке:
`4` коэффициента = среднее + 3 наклона (К77: «наклоны есть 3 из 4 неизвестных»).
Не трилинейная. То есть эта часть предложения описывает текущее состояние.

**Триангуляция вокселя РАДИ ПОЛЯ — дороже, а не дешевле.** Куб делится на 6
симплексов; `DG1` на каждом есть `6 × 4 = 24` неизвестных на воксель против `4`
у `DG1` на коробке. **`×6` в худшую сторону.** Тетраэдры уместны там, где их
требует ГЕОМЕТРИЯ — в РАЗРЕЗАННОЙ ячейке (`src/transport/tet3.c`,
`PLAN_CUT.md`), — и там они уже запланированы. Вне разреза коробка дешевле.

**А вот в слове «ИНТЕРПОЛЯЦИЯ» лежит настоящая развилка, и она крупная.**

| | `DG1` на коробках (текущее) | НЕПРЕРЫВНАЯ узловая `P1` |
|---|---|---|
| где живёт неизвестная | в ячейке | **в вершине, ОБЩЕЙ у соседей** |
| DOF на воксель | `4` | **`≈1`** |
| разрыв на грани | есть, нужен для тени | нет, тень размажется |
| **развёртка как прямой решатель** | **есть** | **ЛОМАЕТСЯ** |

Непрерывная `P1` даёт `×4` по DOF — и это ровно та экономия, о которой речь. Но
ломает она то же самое и по той же причине, что перекрывающиеся носители в
разделе 6: **вершина принадлежит и верхним по потоку ячейкам, и нижним, значит
определить её из одних верхних нельзя, и порядок обхода перестаёт быть прямым
решателем.** Плюс непрерывный Галёркин для переноса неустойчив без стабилизации.

**Это один принцип, а не два совпадения: ПЕРЕКРЫТИЕ НОСИТЕЛЕЙ УБИВАЕТ ПРЯМУЮ
РАЗВЁРТКУ — что по углу (разд. 6), что по пространству.** Отсюда правило,
которое стоит держать при любой перестройке представления: **носители могут быть
какой угодно формы и какого угодно размера, но не должны перекрываться.**

**И `×4` достижимы дешевле.** В таблице К77 уже стоит `DG1 → DG0 в гладком
ОБЪЁМЕ`, `4 → 1…2`, то есть `÷2…÷3` — почти вся та же экономия, без потери
развёртки и без стабилизации. Оговорка К77 жёсткая: **на ПОВЕРХНОСТЯХ `DG1`
остаётся**, понижение только в объёме — что согласуется с 7.3 (негладкость `φ`
живёт на поверхностях).

**Порядок по ценности, если брать это семейство:**

| ход | множитель | ломает развёртку? |
|---|---|---|
| вытянутый носитель вдоль ординаты (7.4) | `×10…100`, где `φ` гладок | нет |
| `DG1 → DG0` в гладком объёме (К77) | `×2…3` | нет |
| непрерывная узловая `P1` | `×4` | **ДА** |
| триангуляция вокселя ради поля | `×0.17` | нет |

Первое даёт на порядок больше третьего и ничего не ломает. **Считать надо
сначала форму НОСИТЕЛЯ, и лишь потом число коэффициентов на нём.**

---

## 8. «ОДИН ЭЛЕМЕНТ ОТВЕЧАЕТ ЗА МНОГО ГЕОМЕТРИИ»: ДВА МЕХАНИЗМА, НЕ ОДИН

**Требование пользователя (07-28):** один крупный элемент должен отвечать за
много геометрии; тогда часто можно обойтись только крупными элементами, а
детализация будет стоить не `×8` мелких, а куда меньше — **рост не
экспоненциальный.**

Требование верное, но под ним ДВА разных механизма с разными условиями
применимости, и смешивать их нельзя.

### 8.1 РАЗРЕШЁННАЯ ГЕОМЕТРИЯ → РАЗРЕЗ. УЖЕ ПОСТРОЕН

Грань куба, стена, наклонный пол — геометрия, которую потребитель РАЗРЕШАЕТ.
Здесь крупный элемент держит геометрию не приближением, а **разрезом**: ячейка
любого уровня вместо дробления ССЫЛАЕТСЯ на аналитическую поверхность, и
получается многогранник с топологией. Лесенки нет, точность не теряется, размер
элемента от геометрии не зависит вовсе.

**Это уже сделано:** `src/cut/` (ядро отсечения `poly3.c`, таблицы `surf.c`,
форма `qef.c`, эрмитово дерево `dc.c`), `PLAN_CUT.md`, пункты 2/4/3/5 закрыты
07-27; в переносе есть тетраэдризация разрезанной ячейки (`tet3.c`) и крючок в
развёртке (`sweep3.h`: `const tr3_cut *cut`). Ждёт потребителя — пункт 6, T4.

**Пример пользователя с кубом — ровно этот случай, и он закрыт.**

### 8.2 НЕРАЗРЕШЁННАЯ ГЕОМЕТРИЯ → ГОМОГЕНИЗАЦИЯ. ЗАКОННА, НО НЕ ИЗМЕРЕНА

Трава, листва, ткань, дальний фасад — геометрия, которой потребитель НЕ
разрешает. Разрез здесь не помогает: поверхностей тысячи, и резать нечего.
Работает другое — **сохранять не ФОРМУ, а ОТКЛИК**: область становится
эффективной средой с эффективными `σ_t`, `σ_s`, индикатрисой.

**Вот это и есть «один элемент за много геометрии» в сильном смысле, и вот здесь
рост перестаёт быть экспоненциальным.** Счёт прямой:

| | что растёт с детализацией |
|---|---|
| дробление | `×8` ячеек на уровень, каждая `× 4` коэффициента `× ND` = `32·ND` чисел |
| гомогенизация | **ничего** по числу элементов; растёт лишь БОГАТСТВО отклика |

Богатство отклика растёт медленно: изотропная среда — `σ_t`, `σ_s`, `g`, три
числа; тензорная (К4, канализирующая: полог, пшеница) — шесть; полностью
угловая — порядка `ND`. **То есть `32·ND` чисел на уровень дробления против
`3…ND` один раз.** Рост переезжает из ЧИСЛА ЭЛЕМЕНТОВ в РАЗМЕРНОСТЬ ОТКЛИКА, а
она ограничена сверху `ND` и не зависит от того, сколько геометрии внутри.

**И это ЗАКОННАЯ машинерия переноса, а не заимствование:** `PLAN_CUT.md` прямо
относит гомогенизацию к переносу (Р-5д не пишется, потому что «гомогенизация —
машинерия ПЕРЕНОСА»), а Г17 уточняет, что средняя по ячейке постоянная даёт
оптическую толщину линейной `σ` ТОЧНО — «нужна гомогенизация, а не
интерполяция».

**ПРОБЕЛ, И ОН ЕДИНСТВЕННОЕ, ЧТО ОТДЕЛЯЕТ ИДЕЮ ОТ РЕАЛИЗУЕМОСТИ.** Записано в
`CLAUDE.md`: ошибка ЗАМЕНЫ структуры откликом есть ВТОРОЕ, независимое число, и
**ничто его пока не вычисляет** (`hz_facet.dmax` считает только СМЕЩЕНИЕ
поверхности). Без этого числа «насколько крупно можно взять элемент» не имеет
ответа, и вся экономия становится угаданной, а не посчитанной.
**Это и есть работа, которую идея требует завести:** определить и измерить
ошибку гомогенизации. Без неё раздел остаётся правдоподобным, а не доказанным.

### 8.3 ГДЕ ДРОБИТЬ ВСЁ-ТАКИ ПРИДЁТСЯ — И ТАМ `×8` ЗАМЕНЯЕТСЯ НА `×2`

Второе требование пользователя — чтобы детализация стоила не `×8`. Механизм
есть, и он независим от гомогенизации.

**ПОПРАВЛЕНО 07-28 ПО ВОЗРАЖЕНИЮ ПОЛЬЗОВАТЕЛЯ, И ВОЗРАЖЕНИЕ ВЕРНОЕ.** Здесь
стояло «база падает с 8 до 2, но рост остаётся экспоненциальным по глубине,
обещать „не экспоненциальный“ нельзя». Это **не то измеряет и потому вводит в
заблуждение.** Глубина `d` не независимая величина: `δ = L/2ᵈ`, значит
`d = log₂(L/δ)`, и «экспонента по `d`» есть СТЕПЕННОЙ ЗАКОН по отношению
масштабов. В правильной переменной картина другая, и она подтверждает
пользователя, а не возражает ему.

**И виновник не «экспонента», а ФОРМА: у октодерева она одна — куб.** Иерархия
сама по себе ничего не стоит, она ровно то, чем представление подстраивается под
уравнение. Платит несовпадение формы элемента с формой особенности.

Почти вся негладкость в переносе ДВУМЕРНА — поверхность, слой, граница полутени
(7.3). Цена разрешить плоскую особенность до `δ` в ячейке размера `L`:

| форма элемента | сколько ячеек | при `L/δ = 1000` |
|---|---|---|
| **куб** (октодерево, адаптивно) | `O((L/δ)²)` | `≈10⁶` |
| **плита** по нормали (анизотропное деление) | `O(log(L/δ))` | `≈10` |
| **патч/разрез** (полуплоскость по особенности) | **`O(1)`**, от `δ` не зависит | `≈7` |

**Степенной закон исчезает совсем, а не понижает основание.** Куб не может
локализовать плоскую особенность иначе как покрыв её мелкими кубами — отсюда
квадрат; плита ловит её одним делением по нормали — отсюда логарифм; патч
совпадает с ней по форме — отсюда константа.

*Оговорка, чтобы `O(1)` не было обещанием сверх меры:* константа держится, пока
особенность приближается НЕСКОЛЬКИМИ полуплоскостями. У кривой границы (радиус
`ρ`) кусочно-плоское приближение до `δ` даёт куски размера `√(ρδ)`, то есть
`O(L²/(ρδ))` патчей — всё ещё в `ρ/δ` раз меньше кубов, но уже не константа.

Для ТОЧЕЧНОЙ особенности разрыв скромнее: `7·log₂(L/δ)` у куба против `1 + nh` у
патча. Крупный выигрыш даёт именно двумерность негладкости, и потому он есть
следствие 7.3, а не отдельное допущение.

И это складывается с 7.4: носитель, вытянутый вдоль ординаты, есть та же
анизотропия, только по другой оси и по другой причине.

### 8.4 ИТОГ: ЛЕСТНИЦА ИЗ ТРЁХ СТУПЕНЕЙ, И ОНИ НЕ ПЕРЕСЕКАЮТСЯ

| геометрия в элементе | механизм | цена детализации | статус |
|---|---|---|---|
| разрешённая, аналитическая | **разрез** (`src/cut/`) | **ноль** — размер элемента от неё не зависит | **построено**, ждёт T4 |
| неразрешённая, структурная | **гомогенизация** | `3…ND` чисел ОДИН раз | законна; **ошибка не вычисляется ничем** |
| негладкость поля (тень, слой) | **анизотропное дробление** | `×2` вместо `×8` | не реализовано |

**Условие корректности всей лестницы одно и то же, и оно из 7.8:** носители
какой угодно формы и размера, но **без перекрытия** — иначе развёртка перестаёт
быть прямым решателем.

**Фальсификаторы:**
1. **разрез против дробления:** сцена с наклонной стеной, свип по глубине дерева
   при ВКЛЮЧЁННОМ разрезе. Картинка обязана НЕ меняться (геометрия точна на любой
   глубине). Если меняется — разрез не работает, а лесенка осталась;
2. **гомогенизация:** участок травы, посчитанный (а) явной геометрией на мелкой
   сетке и (б) одним элементом с эффективным откликом. Разность и есть та самая
   невычисляемая ошибка; свип по размеру элемента даёт её закон. **Пока этого
   замера нет, множители из 8.2 цитировать нельзя;**
3. **негативный контроль к гомогенизации:** заменить откликом ЛАМБЕРТОВСКИМ
   вместо измеренного ⇒ картинка обязана сломаться на `O(1)`. Не сломалась —
   значит отклик не участвует и экономия приписана не тому;
4. **анизотропное дробление:** свип по оси дробления. Деление по НЕПРАВИЛЬНОЙ оси
   (вдоль поверхности вместо поперёк) обязано не улучшать картинку вовсе. Если
   улучшает одинаково — критерий выбора оси слеп.

### 8.5 СОВМЕСТИТЬ ГЕОМЕТРИЮ И ПОЛЕ: ДЕЛИТЬ ТОПОЛОГИЮ ДА, ПАМЯТЬ НЕТ

**Мысль пользователя (07-28), высказана с собственной оговоркой «не знаю, нужно
ли»:** в таком способе удастся совместить геометрию и поле, и потенциально на
этом можно сэкономить.

**Оговорка уместна: ЗАМЕР говорит против, и говорит уже сделанным замером.**

К83: на `64³` из `491` МБ RSS **`292` МБ (60%) занимали матрицы масс ГРАНЕЙ** —
данные, ВЫВЕДЕННЫЕ ИЗ ГЕОМЕТРИИ, по три `4×4` на каждую из `798720` граней. При
этом у **96% граней геометрия одинакова**, а матрица аналитична: на равномерной
сетке различных всего ТРИ. **Отказ ХРАНИТЬ дал `×2.5` по памяти И по времени**
(задача пропускно-ограничена, К73/К79).

То есть выигрыш пришёл от того, что геометрию из памяти ВЫСЕЛИЛИ и стали считать
на месте, — а не от того, что её сселили с полем. Слияние двух представлений
раздуло бы 96% ячеек ради нужд 4%.

**Три довода против слияния ПАМЯТИ, все из уже записанного:**

1. **Разные времена жизни.** Геометрия статична; угловое поле ТРАНЗИЕНТНО (Р1) —
   живёт внутри одного прохода и умирает. Чередовать их в одном массиве значит
   переписывать статические кэш-линии каждый проход;
2. **Разные разрешения.** Р6 — «прямая и косвенная живут на РАЗНЫХ сетках»,
   косвенная в 4–8 раз грубее по оси. Это один из двух крупнейших рычагов
   (`÷8`…`÷64`), и работает он именно потому, что геометрия и поле РАЗВЯЗАНЫ.
   Слияние их снова связывает, то есть отменяет Р6;
3. **Разные судьбы.** Геометрию по К83 выгодно НЕ хранить вовсе; поле хранить
   обязательно. Слияние навязывает им одну судьбу.

**А вот что совмещать НАДО, и это не то же самое:** одну ТОПОЛОГИЮ — одно дерево,
один набор связей соседей, один порядок обхода на ординату. Обход у них
действительно общий, и дублировать топологию незачем.

    ОБЩЕЕ:    дерево, связи соседей, порядок обхода
    РАЗДЕЛЬНО: массив геометрии (или его отсутствие) и массив поля,
               каждый со своим разрешением и своим временем жизни

**Формулировка правила:** делить СТРУКТУРУ, не делить ПАМЯТЬ. На
пропускно-ограниченной машине (`22.6` ГБ/с замерено, интенсивность ядра `1.75`
против баланса `19.8`) это не стилистика, а прямая цена.

**Фальсификатор:** если найдётся класс ячеек, где геометрия нужна при КАЖДОМ
касании поля и не выводится аналитически (кандидат — разрезанные ячейки, те
самые 4%), то для НИХ слияние оправдано, и тогда решение не «да/нет», а «две
раскладки: коробки и разрезанные». Проверяется прямо: доля разрезанных ячеек в
реальной сцене и цена косвенного обращения на них.

---

## 9. ПАТЧИ ВМЕСТО ВОСЬМЁРКИ: РАЗВИЛКА В СЛОВЕ «ВСПЛЕСК», И ЯДРО УЖЕ НАПИСАНО

**Требование пользователя (07-28):** воксели не вполне подходят. Нужны крупные
элементы-«всплески», отвечающие за большой объём и согласованные с геометрией и
светом; а мелкие надстраивать УМНО — они должны быть «патчами», устраняющими
ключевые места, а не тупым делением пространства на 8.

### 9.1 «ВСПЛЕСК» ЧИТАЕТСЯ ДВОЯКО, И ЧТЕНИЯ РАСХОДЯТСЯ ПО ВСЕМУ

| | **ДОБАВОЧНЫЙ** (истинный всплеск) | **ЗАМЕЩАЮЩИЙ** (патч-разрез) |
|---|---|---|
| что делает мелкий | ПРИБАВЛЯЕТСЯ к крупному | ВЫРЕЗАЕТ из крупного |
| носители | **перекрываются** | не перекрываются |
| крупный после вставки | цел, тянется под патчем | становится «крупный минус патч» |
| **развёртка** | **ЛОМАЕТСЯ** | **ЦЕЛА** |

Добавочное чтение — это иерархический базис (Изерентант), и он ломает прямую
развёртку по той же причине, что и всё остальное в этом файле: **перекрытие
носителей.** Крупная функция тянется и выше по потоку, и ниже, поэтому её
коэффициент не определяется из одних верхних. Это ТРЕТИЙ случай одного принципа
(разд. 6 — по углу, 7.8 — узловая непрерывная, здесь — иерархическая).

**Замещающее чтение даёт ровно то, что вы описали, и развёртку не трогает.**

### 9.2 И ОНО УЖЕ РЕАЛИЗУЕМО: `hz_poly3_complement` НАПИСАН ПОД ПЕРЕНОС

`src/cut/poly3.h`:

> **ДОПОЛНЕНИЕ — до `nh` НЕПЕРЕСЕКАЮЩИХСЯ ВЫПУКЛЫХ кусков.** Внешность выпуклого
> тела не выпукла, **а переносу нужны настоящие тетраэдры и снаружи.** Точное
> разбиение: кусок `j` = коробка, отсечённая `h_0..h_{j−1}` как есть и `h_j`
> ПЕРЕВЁРНУТОЙ; куски не пересекаются, объединение есть ровно дополнение.

То есть «крупный элемент МИНУС патч» — решённая задача, ТОЧНАЯ, с ВЫПУКЛЫМИ
кусками, и написана она с оглядкой именно на перенос. Патч задаётся `nh`
полуплоскостями; ячейка распадается на `1 + (≤ nh)` выпуклых кусков:

    патч-плита (2 полуплоскости):   1 + 2 = 3 куска
    патч-угол  (3 полуплоскости):   1 + 3 = 4 куска
    патч-коробка (6):               1 + 6 = 7 кусков

Против `8` у октодерева — само по себе скромно. **Выигрыш не в «3 против 8», а в
том, что патч СТАВИТСЯ СРАЗУ КУДА НАДО И КАКОГО НАДО РАЗМЕРА, и рекурсии не
требует.**

### 9.3 СЧЁТ ВЫИГРЫША: ПАТЧ ПРОТИВ ПОДДЕРЕВА

Пусть в ячейке размера `L` надо разрешить особенность размера `δ`.

| способ | сколько ячеек |
|---|---|
| адаптивное октодерево | `log₂(L/δ)` уровней, на каждом рождается 7 ⇒ `≈ 7·log₂(L/δ)` |
| поставленный патч | `1 + nh`, **от `L/δ` НЕ ЗАВИСИТ** |

При `L/δ = 1000`: `≈70` ячеек против `3…7`. **`×10…×20`, и множитель РАСТЁТ с
отношением масштабов**, тогда как у октодерева он логарифмический всегда.

Это и есть ваше «не в 8 раз больше, а куда меньше», в проверяемом виде: **цена
детализации перестаёт зависеть от того, НАСКОЛЬКО мелкая деталь, и начинает
зависеть только от того, СКОЛЬКО их.**

### 9.4 ЧЕМ ЗАДАЁТСЯ, ГДЕ СТАВИТЬ ПАТЧ

«Согласованные с геометрией и светом» распадается на два уже названных источника,
и оба вычислимы:

- **геометрия** — разрез (8.1): поверхность уже есть аналитическим примитивом,
  её полуплоскости и суть полуплоскости патча. Патч не надо изобретать, он падает
  из геометрии;
- **свет** — важность (разд. 2, сопряжённая задача) плюс теневая структура (7.3):
  негладкость `φ` живёт на границах полутени, то есть на ПОВЕРХНОСТЯХ. Полутень —
  тоже пара полуплоскостей (для площадного источника — клин между ними), то есть
  тоже готовый патч-плита.

**Обе причины дают патчи ПЛОСКИМИ, а не кубическими** — и это ровно то, чего
октодерево дать не может: у него единственная форма деления есть куб.

### 9.5 ЧТО ПРОВЕРИТЬ ПРЕЖДЕ, ЧЕМ ОБЕЩАТЬ

1. **ЦИКЛЫ ОБХОДА — главный риск, и он уже записан как заноза.** Выпуклость
   каждого куска гарантирует, что знак `ω·n` на грани определён, но НЕ
   гарантирует ацикличности графа зависимостей между кусками: на неструктурной
   сетке трёхъячейковые циклы известны и без всяких зеркал. В плане это стоит как
   «циклы обхода при зеркалах и невыпуклых областях ломают треугольность… померить
   прежде, чем обещать». Лечение стандартное (обнаружить цикл, на обратном ребре
   взять предыдущую итерацию) и стоит нескольких лишних проходов;
2. **потеря аналитических матриц (К83).** На равномерной сетке у 96% граней
   геометрия одинакова и матрица масс аналитична — именно на этом взято `×2.5` по
   памяти и времени. У кусков дополнения геометрия РАЗНАЯ, значит на них это
   преимущество теряется. Пока патчей мало — не страшно; **порог считается, а не
   угадывается,** и его надо посчитать до внедрения;
3. **порядок полуплоскостей.** `*nout ≤ nh` и **зависит от порядка** — проверяемая
   величина есть Σ объёмов, а не число кусков (Г30). Значит «сколько кусков выйдет»
   не детерминировано формой патча, и в оценку `1 + nh` надо закладывать худший
   случай;
4. **положительность (К6/К14)** на вытянутых и тонких кусках дополнения — первое
   место, где ограничитель сорвётся;
5. **негативный контроль:** поставить патч в ЗАВЕДОМО НЕНУЖНОЕ место (там, где `φ`
   гладок) ⇒ картинка обязана не измениться, а стоимость вырасти. Если картинка
   изменилась — патч вносит артефакт на стыке, и это дефект сшивки, а не
   разрешения.

**Отдельным пунктом, к сведению, а не к правке (файлы репозитория не трогаю):**
верхняя секция `CLAUDE.md` описывает базис как C¹-потенциалы с разбиением
единицы и ссылается на замер «30% ошибки при поломке Σφ=4». Для волновой линии
это v1-текст: постановка с 07-26 разрывная, потенциалов в ней нет. Стоит
пометить там же, где помечены прочие устаревшие места, иначе вопрос вроде
заданного будет возникать снова.
- утверждение «важности хватает одной значащей цифры» проверяется прямо:
  посчитать кадр с точной важностью и с загрублённой вдвое, сравнить картинки;
- «сопряжённое поле дешевле прямого на порядок» — предсказание. Если сопряжённый
  прогон требует того же ND (а ND задаётся ЗНАНИЕМ НАПРАВЛЕНИЙ, которое у
  сопряжённой задачи не лучше), рычаг съедает половину собственного выигрыша.
