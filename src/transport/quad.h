/* quad.h — квадратура Гаусса–Лежандра, общая для линии переноса.
 * Вынесено из sweep.c, когда понадобилось второму потребителю (rte2d.c):
 * там узлы нужны по азимуту в квадранте, здесь — по μ в полусфере. */
#ifndef TR_QUAD_H
#define TR_QUAD_H

/* Узлы и веса Гаусса–Лежандра на [−1,1], n узлов, по ВОЗРАСТАНИЮ узлов.
 * Σw = 2. Массивы x,w длиной n, заполняются целиком. n ≥ 1. */
void tr_gauss_legendre(int n, double *x, double *w);

#endif /* TR_QUAD_H */
